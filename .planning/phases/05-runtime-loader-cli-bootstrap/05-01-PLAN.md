---
phase: 05-runtime-loader-cli-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/live_terminal_test.go
  - core/shell/provider.go
  - core/cli/cli.go
  - core/cli/store.go
  - core/cli/emitter.go
  - core/cli/cli_test.go
  - core/cmd/zsh-pro/main.go
autonomous: true
requirements: [BOOT-01, BOOT-02]
must_haves:
  truths:
    - "zsh-pro hook prints a zsh-n-parseable loader defining checkout/activate/deactivate/list/status + zp_capture_env/zp_restore_env + ZP_UNSET_SENTINEL"
    - "The loader helper/state surface AND the CLI->emit wiring are reconciled against emit.go's ACTUAL surface (or the assumed baseline is recorded when emit.go is absent) and the re-diff is a durably-recorded blocking phase-exit obligation, not just a SUMMARY note (C16, C2)"
    - "checkout <A> exports ZSHPRO_PROFILE=A after a successful eval; deactivate unsets it (status reports main); checkout calls Store.Checkout to validate before switching (C1, D-05/D-06/D-07)"
    - "A verb aborts with NO export and NO last-good update when the emit subcommand exits non-zero OR prints empty output, before any zsh -n / eval (C3)"
    - "checkout concatenates the deactivate-prior + apply-target emit outputs into ONE captured block, validates the concatenation once, then evals once (no per-half validation window) (C12)"
    - "In a live zsh, sourcing the loader then activate <A> makes A's alias/env/PATH entry present in the CURRENT shell; deactivate reverses with zero residue"
    - "ZP_BASE_PATH is captured once in the SHARED eval path used by both activate and checkout; checkout-first keeps $#path bounded; repeated activate A -> activate B -> deactivate does not grow $#path (M4)"
    - "A child shell that inherits ZSHPRO_PROFILE but not ZP_BASE_PATH is treated as fresh (nested-shell asymmetry guard) (M8)"
    - "list prints store branches; status reports whether a profile is activated in THIS terminal (reads ZSHPRO_PROFILE / last-good presence), not just a name (M1)"
    - "A composition-root-shaped nil-Store condition (store.New returned err) routes through fail, never panics (C10)"
    - "The loader const lives in core/shell/zsh (grep-verified); core/cli holds no zsh text"
  artifacts:
    - path: "core/shell/zsh/hook.go"
      provides: "loaderScript const + Provider.HookScript()"
      contains: "const loaderScript"
    - path: "core/shell/provider.go"
      provides: "Hooker sub-interface added to Provider composite"
      contains: "HookScript() string"
    - path: "core/cli/store.go"
      provides: "narrow cli-local Store interface satisfied by *store.Store"
      contains: "type Store interface"
    - path: "core/cli/emitter.go"
      provides: "narrow cli-local Emitter interface (CLI->emit seam) + pre-Phase-4 not-yet-available stub"
      contains: "type Emitter interface"
    - path: "core/cli/cli.go"
      provides: "hook/emit/list/status verb dispatch + typed-nil-safe store/emitter guards"
      contains: "case \"hook\""
  key_links:
    - from: "core/cli/cli.go"
      to: "core/shell/zsh loaderScript"
      via: "provider.HookScript()"
      pattern: "provider\\.HookScript\\(\\)"
    - from: "core/cmd/zsh-pro/main.go"
      to: "core/cli CLI"
      via: "cli.New(provider, s, emitter)"
      pattern: "cli\\.New\\("
    - from: "loader verb functions"
      to: "zsh-pro emit binary subcommand"
      via: "eval \"$(zsh-pro emit ...)\""
      pattern: "zsh-pro emit"
---

<objective>
Wire the live terminal to the `zsh-pro` binary: define the embedded zsh loader (the `hook` const + the two named env helpers + per-terminal runtime state + the five sourced verbs), the `hook`/`emit`/`list`/`status` CLI verbs, the `HookScript()` provider seam, the `Emitter` CLI->emit seam, and the composition-root store injection. This is the live-terminal wiring plan — it makes `activate`/`checkout`/`deactivate` visibly mutate the current shell.

The FIRST task is the C16 + C2 reconciliation gate (Phase 4's `emit.go`/`core/activate` do not exist on disk yet — confirmed by `ls` at plan time; but Phase 4 executes BEFORE Phase 5 in the plan-before-execute pipeline, so `emit.go` WILL exist at execute time), NOT loader implementation. The gate covers BOTH the loader helper/state surface AND the CLI->emit wiring, and records the re-diff as a durable, blocking phase-exit obligation.

Purpose: Requirement 1 (`hook` emits the embedded loader), Requirement 2 (live-terminal switch verbs incl. `checkout` + `ZSHPRO_PROFILE` lifecycle), Requirement 3 (`list`/`status`) — covering BOOT-01 (partial: the CLI surface the installer will bootstrap) and BOOT-02 (partial: the loader whose fail-open stub Plan 05-02 installs).
Output: `core/shell/zsh/hook.go`, `core/shell/provider.go` (Hooker), `core/cli/cli.go` + `core/cli/store.go` + `core/cli/emitter.go`, `core/cmd/zsh-pro/main.go` wiring, and the Wave-0 test scaffolds `hook_test.go` + `live_terminal_test.go`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-RESEARCH.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-EVIDENCE.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-PATTERNS.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-OPEN-QUESTIONS.md

<interfaces>
<!-- Extracted from the codebase at plan time. VERIFY AGAINST DISK before relying on any snippet
     (M0/C13): the executor MUST re-read the named files — these are a starting map, not a frozen
     contract. Files evolve; Phase 4 will have landed emit.go/core/activate by execute time. -->

From core/shell/provider.go (interfaces to mirror + extend — verified against disk at plan time):
```go
type Regenerator interface { Regenerate(e model.Entry) string }
type Provider interface {
    Parser
    Classifier
    Introspector
    Regenerator
}
```

From core/shell/zsh/introspect.go (the const-embed + method + compile-guard precedent):
```go
var _ shell.Provider = Provider{}
func (Provider) Categories() []model.Category { return model.Categories() }
const introspectScript = ` ... `   // opens with `emulate -L zsh` for ISOLATION — the loader must NOT (D-04)
func (p Provider) Introspect(path string) (model.IdentitySet, error) { ... }
```

From core/cli/cli.go (dispatch + fail contract — verified against disk; New currently takes ONLY the provider):
```go
type CLI struct{ provider shell.Provider }
func New(p shell.Provider) *CLI { return &CLI{provider: p} }
func (c *CLI) Run(args []string, stdout, stderr io.Writer) int {
    // switch args[0] { case "--version": ...; case buildinfo.Command: c.runAnalyze(...); default: ExitUsageErr }
}
func (c *CLI) fail(stdout, stderr io.Writer, asJSON bool, msg string) int // ExitRuntimeErr, JSON-to-stdout in --json
```

From core/model/exitcode.go:
```go
ExitClean=0  ExitRuntimeErr=1  ExitUsageErr=2  ExitActionable=3
```

From core/store/store.go (all EXIST — the cli-local Store interface is satisfied by *store.Store):
```go
func (s *Store) Branches(ctx context.Context) ([]string, error) // list
func (s *Store) Current() string                                // status (reads ZSHPRO_PROFILE, unset => "main")
func (s *Store) Checkout(ctx context.Context, name string) error // validates existence, does NOT export
func (s *Store) Read(ctx context.Context, branch string) (model.Profile, error) // emit source; ErrProfileNotFound on absent branch
```

From core/cmd/zsh-pro/main.go (composition root — the sole importer of core/shell/zsh + concrete store).
VERIFIED-AGAINST-DISK snippet (M0 correction — the prior plan omitted the `kc :=` line):
```go
provider := zsh.Provider{}
dir := storeDir()
kc := store.NewOSKeychainDriver(dir)       // <-- REQUIRED before store.New (was omitted in prior plan)
s, err := store.New(dir, provider, kc)
_, _ = s, err   // placeholder to remove — thread s + err into cli.New (see Task 4 / C10 typed-nil handling)
os.Exit(cli.New(provider).Run(os.Args[1:], os.Stdout, os.Stderr))
```
`store.New` returns `(nil, err)` on failure — see C10: a typed-nil `*store.Store` handed to an interface parameter is a non-nil interface wrapping a nil pointer; guard at main.go (Task 4).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: C16 + C2 reconciliation gate — pin BOTH the loader helper/state contract AND the CLI->emit wiring against emit.go's ACTUAL surface, recorded as a durable blocking re-diff obligation</name>
  <files>.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md (durable contract file — written IMMEDIATELY, not at SUMMARY time; this task edits no production source)</files>
  <read_first>
    - core/shell/zsh/introspect.go (the const-embed analog; note the emulate -L divergence per D-04)
    - core/cli/cli.go (verify the current New signature + dispatch against disk — M0)
    - core/cmd/zsh-pro/main.go (verify the kc/store.New/`_, _ = s, err` wiring against disk — M0)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-EVIDENCE.md (C16 REFUTED detail + the correct fact)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-OPEN-QUESTIONS.md (OQ-05-05, OQ-05-06, OQ-05-13)
    - .planning/phases/04-manifest-builder-emit/04-CONTEXT.md (D-12/OQ-5 — Phase 4 emit-vs-inline discretion; the INLINE reverse-op vocabulary)
  </read_first>
  <action>
    Establish TWO contracts BEFORE writing any loader body or CLI wiring, and write them to a DURABLE contract file `05-CONTRACT.md` IMMEDIATELY (not to SUMMARY — Task 2/3 read_first depend on it; LOW concern: a SUMMARY is written at completion and cannot be a read_first source).
    Run `ls core/shell/zsh/emit.go core/activate 2>&1` and `grep -rn 'zp_capture_env\|zp_restore_env\|zp_rebuild_path\|zp_shadow\|ZP_BASE_PATH\|__ZP_ORIG_\|_PRIOR_ALIAS_\|_PRIOR_FUNC_\|was_on' core/`.

    CONTRACT PART 1 — loader helper/state surface (C16):
    Case A — emit.go IS present (Phase 4 landed, the expected state at execute time): diff the DEFINITIVE loader surface (the two named env helpers `zp_capture_env`/`zp_restore_env` + the runtime STATE slots `ZP_BASE_PATH`, `ZP_UNSET_SENTINEL`, `__ZP_ORIG_*`, `ZP_<profile>_PRIOR_ALIAS_/FUNC_<name>`, per-option `was_on`) against emit.go's ACTUAL bare-call and state-read surface. Every bare `zp_*` call and every state-read emit.go produces MUST be satisfied by exactly one loader-provided helper or state slot. If emit.go names an additional helper (e.g. it did NOT inline PATH-rebuild), add exactly that helper — do NOT invent `zp_rebuild_path`/`zp_shadow_*` speculatively (per D-01/OQ-05-13, Phase 4 documents shadows/PATH/options INLINE).
    Case B — emit.go ABSENT (only if Phase 4 somehow has not executed): record the ASSUMED reconciliation baseline — the loader provides ONLY the two Phase-4-committed named env helpers `zp_capture_env <var>` / `zp_restore_env <var> <applied>` PLUS the runtime STATE slots the Phase-4 inline reverse ops read/write. Flag that this baseline MUST be re-diffed against emit.go when Phase 4 lands.

    CONTRACT PART 2 — CLI->emit wiring (C2, do NOT descope):
    Define the exact production emit path the verbs reach through: `checkout`/`activate`/`deactivate` shell out to `zsh-pro emit <apply|deactivate> <name>` (OQ-05-06 default). On the Go side, `core/cli` must reach the emitter WITHOUT importing `core/shell/zsh` (D-19 layering). Record: the CLI declares a narrow `Emitter` interface (Task 3), the concrete emitter is injected at the composition root (main.go), and the SUBCOMMAND SHAPE the emitter satisfies (`emit apply <name>`, `emit deactivate <name>`) is the contract Phase 4's emit.go must fulfill. Diff Phase 4's emit.go public entry points against this expected subcommand surface. Record the pre-Phase-4-window behavior explicitly: if emit.go is absent at execute time, the emit path is a compile-time seam present + runtime `fail` with a clear "emit path not yet available" message — NEVER fake apply code eval'd into a live shell.

    DURABLE BLOCKING OBLIGATION: In `05-CONTRACT.md`, record a checkbox obligation `[ ] RE-DIFF loader surface + CLI->emit subcommand surface against emit.go (BLOCKING phase-exit)`. This obligation is a phase-exit gate — the phase cannot be marked complete until the re-diff is done and the box is checked (recorded durably, not merely mentioned in a SUMMARY note).
    Do NOT hardcode `zp_rebuild_path` or `zp_shadow_*` into either contract.
    Addresses review concern: C16, C2 (durable blocking re-diff), M0 (verify-against-disk).
  </action>
  <verify>
    <automated>ls core/shell/zsh/emit.go core/activate 2>&1; test -f .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md && echo "CONTRACT written"; echo "speculative-helper-count:"; grep -rn 'zp_rebuild_path\|zp_shadow' core/ 2>/dev/null | grep -v '_test.go' | grep -v '^#' | wc -l; grep -c 'RE-DIFF' .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - `05-CONTRACT.md` exists (a DURABLE file, written by this task — NOT the SUMMARY) and is what Task 2/3 read_first cite.
    - `ls core/shell/zsh/emit.go core/activate` outcome is recorded (present/absent) in the contract.
    - CONTRACT PART 1: a table lists exactly helpers `zp_capture_env`/`zp_restore_env` and state slots `ZP_BASE_PATH`/`ZP_UNSET_SENTINEL`/`__ZP_ORIG_*`/`ZP_<profile>_PRIOR_ALIAS_/FUNC_<name>`/per-option `was_on` — with `zp_rebuild_path`/`zp_shadow_*` explicitly marked NOT-in-contract-unless-emit.go-names-them.
    - CONTRACT PART 2: the CLI->emit subcommand surface (`emit apply <name>` / `emit deactivate <name>`), the `Emitter` seam, composition-root injection, and the pre-Phase-4-window behavior (compile-time seam + runtime `fail`, never fake apply) are recorded.
    - A checkbox `[ ] RE-DIFF ... (BLOCKING phase-exit)` obligation is present (`grep -c RE-DIFF` >= 1).
    - Case A only: `grep` shows every `zp_*` bare call in emit.go maps to a loader-provided helper AND every emit subcommand the verbs call exists in emit.go; a diff line proves coverage.
    - Case B only: contract records the assumed baseline + the blocking re-check flag for when Phase 4 lands.
  </acceptance_criteria>
  <verification>
    `ls core/shell/zsh/emit.go core/activate 2>&1; grep -rn 'zp_rebuild_path\|zp_shadow' core/ | grep -v '_test.go' | wc -l` (expect 0 speculative helper references introduced by this task); `05-CONTRACT.md` holds both contract parts + the BLOCKING re-diff checkbox.
  </verification>
  <done>Both the loader helper+state surface AND the CLI->emit subcommand/seam contract are fixed in a DURABLE `05-CONTRACT.md` (reconciled against emit.go if present, else the assumed baseline), with a BLOCKING phase-exit re-diff obligation recorded durably; no speculative `zp_rebuild_path`/`zp_shadow_*` helper is in either contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embedded loader const (verbs + ZSHPRO_PROFILE lifecycle + checkout) + Hooker seam + hook_test/live_terminal_test scaffolds</name>
  <files>core/shell/zsh/hook.go, core/shell/provider.go, core/shell/zsh/hook_test.go, core/shell/zsh/live_terminal_test.go</files>
  <read_first>
    - core/shell/zsh/introspect.go (const-embed shape, method-on-zero-value receiver, `var _ shell.Provider` compile guard)
    - core/shell/zsh/introspect_test.go (LookPath("zsh") skip-guard + t.TempDir + os.WriteFile fixture)
    - core/shell/provider.go (Regenerator sub-interface + Provider composite to extend)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-RESEARCH.md (sub-area (a) helper table; Don't-Hand-Roll traps; E5/E6/E7/E8/E12/E16)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-05/D-06/D-07 verb structure + ZSHPRO_PROFILE lifecycle)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md (Task 1's recorded contract — the helper/state baseline + emit seam)
  </read_first>
  <behavior>
    - hook_test: `HookScript()` output piped to `zsh -n` exits 0 (E14/C11) — LookPath-guarded.
    - hook_test: output contains function definitions for checkout, activate, deactivate, list, status (grep each name in command-definition position).
    - hook_test: output defines `zp_capture_env`, `zp_restore_env`, and `typeset -g ZP_UNSET_SENTINEL` (grep).
    - hook_test (PURE Go, NOT behind LookPath): the loader's TOP-LEVEL (statements outside `funcname(){...}` bodies) contains no `$(`, no backtick, no `git ` — verb bodies legitimately carry `$(zsh-pro emit ...)` and are excluded (OQ-05-11 / C14).
    - live_terminal_test (LookPath-guarded, Wave-0 scaffold — may assert against a fake `zsh-pro emit` shim per E4/E15 since real emit.go is absent): source loader; capture PRE snapshot ($#path, an env var, an alias); `activate A` makes A's alias/env/PATH-entry live; `deactivate` restores byte-identical POST == PRE; repeated activate A -> activate B -> deactivate keeps $#path stable (C4/C5).
    - live_terminal_test (C1 — ZSHPRO_PROFILE + checkout lifecycle): `checkout A` then assert `$ZSHPRO_PROFILE == A`; `deactivate` then assert `ZSHPRO_PROFILE` is UNSET and `status` reports `main`. checkout drives `Store.Checkout`-style validation before switching (fake shim validates the name exists, then emits deactivate-prior+apply-target).
    - live_terminal_test (M4 — checkout-first base capture): FIRST verb invoked is `checkout` (not activate); assert `$#path` is bounded (base captured in the shared eval path, not left unset). ZP_BASE_PATH once-capture guard is in the SHARED path both activate and checkout run through.
    - live_terminal_test (M8 — nested/inherited shell asymmetry): export `ZSHPRO_PROFILE=A` but leave `ZP_BASE_PATH` unset (simulating a child shell); a verb detects `ZSHPRO_PROFILE` set + `ZP_BASE_PATH` unset and treats the terminal as fresh (re-captures base, does not assume prior undo slots exist).
    - hook_test (M1 — status reports activation state): `status` output reflects whether a profile is activated in THIS terminal (reads `ZSHPRO_PROFILE`/last-good presence), not merely a stored name.
  </behavior>
  <action>
    Create `core/shell/zsh/hook.go` with a `const loaderScript` raw-string (mirroring `introspectScript`'s shape but WITHOUT the `emulate -L zsh` header — D-04: apply/deactivate + option-touching code MUST be PLAIN so `setopt`/`unsetopt` escape function scope) and `func (Provider) HookScript() string { return loaderScript }` (no-error signature, like `Categories()`). The const defines:
    - `typeset -g ZP_UNSET_SENTINEL=<improbable marker>` once (D-03);
    - `zp_capture_env <var>` capturing the live prior into `__ZP_ORIG_<sanitized-var>` or the sentinel using `[[ "${(P)+var}" == "1" ]]` (C6, operand holds the NAME) written via `typeset -g`;
    - `zp_restore_env <var> <applied>` drift-guarded (`[[ "${(P)+var}" == "1" && "${(P)var}" == "$applied" ]]`), sentinel prior => `unset`, else `export`;
    - slot-name sanitize `${name//[^A-Za-z0-9_]/_}` applied BEFORE any `typeset -g`/`${(P)}` on a derived name (D-02/C8 — both a `typeset`-validity fix and an injection block);
    - the five verb functions (checkout/activate/deactivate/list/status). Per D-05/D-06/D-07 (Addresses review concern: C1):
      - `activate <name>` runs the SHARED eval path (below) for the apply-target code, then `export ZSHPRO_PROFILE=<name>` ONLY after a successful eval;
      - `checkout <name>` FIRST validates via the binary (Store.Checkout — the emit/validate call the fake shim mimics), then runs the SHARED eval path for the deactivate-prior + apply-target ONE block, then `export ZSHPRO_PROFILE=<name>` after success;
      - `deactivate` runs the SHARED eval path for the deactivate code, then `unset ZSHPRO_PROFILE` after success (=> `status` reports `main`);
      - `list` prints binary `zsh-pro list` output (read-only, never evals);
      - `status` reports whether a profile is active in THIS terminal by reading `ZSHPRO_PROFILE`/last-good presence (M1), falling back to the binary/`main`.
    - A SHARED eval-path helper (used by activate AND checkout) that: runs the `ZP_BASE_PATH` once-capture guard `[[ "${ZP_BASE_PATH+x}" == "x" ]] || typeset -g ZP_BASE_PATH="$PATH"` BEFORE any PATH mutation (D-08/C5/M4); implements the nested-shell asymmetry guard (M8) — if `ZSHPRO_PROFILE` is set but `ZP_BASE_PATH` is unset, treat the terminal as fresh (capture base, do not assume `__ZP_ORIG_*`/shadow slots exist). (The `zsh -n`-before-eval gate + the emit-exit/empty-output check (C3) and single-block concatenation for checkout (C12) are added to this shared path in Plan 05-02 Task 2; leave a clearly-marked seam here so 05-02 extends the same function.)
    - Shadow slots use `ZP_<sanitized-profile>_PRIOR_ALIAS_/FUNC_<sanitized-name>`; any shadow-restore in a verb body uses a `${slot+x}` set-test, NOT `[[ -n "$slot" ]]` (D-01/C7).
    Add a `Hooker` sub-interface (`HookScript() string`) to `core/shell/provider.go` with a doc comment noting it, like `Regenerator`, is a place zsh syntax originates (milestone invariant); add `Hooker` to the `Provider` composite. Write the two test files as the Wave-0 scaffolds per <behavior>, including the C1 checkout/ZSHPRO_PROFILE, M4 checkout-first, M8 nested-shell, and M1 status sub-cases.
    Reconciliation dependency: the helper/state surface built here MUST match Task 1's `05-CONTRACT.md` exactly.
    Addresses review concern: C1 (checkout + ZSHPRO_PROFILE export/unset), M1 (status activation state), M4 (base-capture in shared path + checkout-first), M8 (nested-shell asymmetry).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./core/shell/zsh/... -run 'Hook|ZeroSubprocess|LiveTerminal|Checkout|Nested|Status'</automated>
  </verify>
  <acceptance_criteria>
    - `zsh-pro hook | zsh -n` exits 0 (or, pre-CLI-wiring, `HookScript()` output through `zsh -n` exits 0).
    - `HookScript()` output: `grep -c` for each of `checkout`/`activate`/`deactivate`/`list`/`status` function definitions == 1 each; contains `zp_capture_env`, `zp_restore_env`, `ZP_UNSET_SENTINEL`.
    - The const does NOT contain `emulate -L` or `LOCAL_OPTIONS` (grep, filtered to exclude comment lines: `grep -v '^[[:space:]]*#' | grep -c 'emulate -L\|LOCAL_OPTIONS'` == 0).
    - Loader top-level (verb bodies excluded) has no `$(`, no backtick, no bare `git ` (structural grep, pure-Go test, OQ-05-11).
    - live_terminal_test: `checkout A` => `$ZSHPRO_PROFILE == A`; `deactivate` => `ZSHPRO_PROFILE` unset AND `status` == `main` (C1).
    - live_terminal_test: checkout-first keeps `$#path` bounded (M4); a nested-shell (ZSHPRO_PROFILE set, ZP_BASE_PATH unset) is treated as fresh (M8).
    - `status` reflects per-terminal activation state (M1), not just a stored name.
    - `core/shell/zsh` is the only package containing the loader text: `grep -rl 'zp_capture_env' core/ | grep -v _test` lists only `core/shell/zsh/hook.go`.
    - live_terminal_test asserts PRE == POST snapshot and $#path stable across A->B->deactivate (LookPath-guarded).
  </acceptance_criteria>
  <verification>
    `go build ./... && go vet ./...`; `go test ./core/shell/zsh/... -run 'Hook|ZeroSubprocess|LiveTerminal|Checkout|Nested|Status' -v`; `go run ./core/cmd/zsh-pro hook | zsh -n; echo "zsh-n rc=$?"` (expect 0 once Task 3 wires the CLI verb; before then run the equivalent on HookScript() output in the test).
  </verification>
  <done>`hook.go` defines the loader const (PLAIN, no emulate -L) + `HookScript()`; `Hooker` is on the Provider seam; the loader is `zsh -n`-clean, defines the 5 verbs (with the C1 checkout/ZSHPRO_PROFILE export-unset lifecycle + M1 status + M4 shared-path base capture + M8 nested-shell guard) + 2 named env helpers + sentinel, and its start-path is zero-subprocess; live_terminal_test proves live mutation + zero residue + $#path stability + the checkout lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CLI verbs (hook/emit/list/status) + cli-local Store + Emitter interfaces + typed-nil-safe guards</name>
  <files>core/cli/store.go, core/cli/emitter.go, core/cli/cli.go, core/cli/cli_test.go</files>
  <read_first>
    - core/cli/cli.go (Run switch dispatch, runAnalyze handler shape, fail error-envelope, CLI struct + New)
    - core/cli/cli_test.go (existing verb-dispatch test shape to extend)
    - core/store/store.go lines 44-49 (KeychainDriver declare-at-first-use precedent) + the Branches/Current/Checkout/Read signatures
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-06/D-07/D-16/D-18/D-19; OQ-05-06 emit shape, OQ-05-09 injection shape)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md (CLI->emit subcommand surface + pre-Phase-4-window behavior)
  </read_first>
  <behavior>
    - cli_test: `Run(["hook"])` prints `provider.HookScript()` to stdout and returns ExitClean; needs no store, no emitter.
    - cli_test: `Run(["list"])` with a fake Store returning ["main","dev"] prints both branch names, ExitClean.
    - cli_test: `Run(["status"])` prints the fake Store's Current() value (or "main"), ExitClean, and does not error when nothing is active.
    - cli_test: `Run(["emit","apply","dev"])` invokes the Emitter seam for "dev" and prints emitted code to stdout; ExitClean on success.
    - cli_test (C3 — exit/empty gating at the CLI layer): when the Emitter returns a non-nil error, `emit` routes through `fail` (non-zero exit, no partial stdout that a verb could eval); when the emit source is the pre-Phase-4 not-yet-available stub, `emit` returns a clear "emit path not yet available" error via `fail` (C2 defined Case-B behavior) — it NEVER prints fake apply code.
    - cli_test (C10 — typed-nil Store): a CLI constructed with an explicit nil Store interface (composition-root-shaped: store.New returned err) routes a store-backed verb (list/status) through `fail`, and does NOT panic. `hook`/`emit`-not-store verbs are unaffected.
    - cli_test: unknown verb => ExitUsageErr.
  </behavior>
  <action>
    Declare a narrow `type Store interface` IN `core/cli` (new file `core/cli/store.go`) exposing exactly `Branches(ctx) ([]string, error)`, `Current() string`, `Checkout(ctx, name) error`, `Read(ctx, branch) (model.Profile, error)` — satisfied by `*store.Store`, mirroring the `shell.Provider` injection discipline (OQ-05-09/D-19); `core/cli` must NOT import the concrete `*store.Store`.
    Declare a narrow `type Emitter interface` IN `core/cli` (new file `core/cli/emitter.go`) — the CLI->emit seam (Addresses review concern: C2). Exactly one method, e.g. `Emit(ctx context.Context, mode, name string) (string, error)` where `mode` is `apply`|`deactivate`. `core/cli` reaches emit ONLY through this interface — it must NOT import `core/shell/zsh` (D-19). Provide a `notReadyEmitter` value (in `core/cli/emitter.go`) whose `Emit` returns `("", errors.New("emit path not yet available"))` — the DEFINED pre-Phase-4-window behavior (C2): compile-time seam present, runtime `fail` with a clear message, NEVER fake apply code eval'd into a live shell. main.go injects the real emitter once Phase 4's emit.go lands.
    Extend `CLI` to hold the store and the emitter; change `New(p shell.Provider)` to `New(p shell.Provider, s Store, e Emitter) *CLI`. Add `switch args[0]` cases in `Run` (D-18):
    - `case "hook"` -> print `c.provider.HookScript()`, `ExitClean` (no store, no emitter);
    - `case "list"` -> guard `c.store == nil` (C10) -> `fail`; else call `store.Branches`, print names;
    - `case "status"` -> guard `c.store == nil` -> `fail`; else print `store.Current()` (or "main");
    - `case "emit"` -> parse `emit <apply|deactivate> <name>`; call `c.emitter.Emit(ctx, mode, name)`; on error (incl. the not-yet-available stub) route through `fail` (C3 — CLI-layer exit gating: non-zero exit yields no eval-able stdout); on success print the emitted code, `ExitClean`.
    Each store/emitter-backed handler routes errors through the existing `fail` helper (agent JSON contract preserved); reuse typed `model.ExitCode` returns. The C10 typed-nil guard is a plain `if c.store == nil` check inside each store-backed case (the composition root passes an explicit nil INTERFACE on err, per Task 4 — so `c.store == nil` is true, not a non-nil interface wrapping a nil pointer).
    Addresses review concern: C2 (Emitter seam + defined Case-B stub), C3 (CLI-layer emit exit/empty gating), C10 (typed-nil Store guard).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./core/cli/... -run 'Hook|List|Status|Emit|Store|NilStore|NotReady'; go run ./core/cmd/zsh-pro hook | zsh -n; echo "hook zsh-n rc=$?"; grep -rn '"zsh-pro/core/store"|"zsh-pro/core/shell/zsh"' core/cli/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `core/cli/store.go` declares `type Store interface` with exactly the 4 methods; `core/cli/emitter.go` declares `type Emitter interface` + a `notReadyEmitter`; `core/cli` imports NEITHER `core/store` NOR `core/shell/zsh` (grep == 0).
    - `Run(["hook"])` prints the loader (== `HookScript()`) and returns 0 without touching the store or emitter.
    - `Run(["list"])`/`Run(["status"])` print branch names / active profile from the injected Store; `Run(["emit","apply","<name>"])` reaches the Emitter seam.
    - C3: when the Emitter errors OR is the not-yet-available stub, `emit` routes through `fail` with a clear message and prints NO fake apply code (verified: stdout carries no eval-able code on the error path).
    - C10: a CLI built with an explicit nil Store interface routes list/status through `fail`, does NOT panic (test asserts non-panic + ExitRuntimeErr).
    - `zsh-pro hook | zsh -n` exits 0 end-to-end through the real CLI binary.
  </acceptance_criteria>
  <verification>
    `go build ./... && go vet ./...`; `go test ./core/cli/... -run 'Hook|List|Status|Emit|Store|NilStore|NotReady' -v`; `go run ./core/cmd/zsh-pro hook | zsh -n; echo rc=$?` (expect rc=0); `grep -rn '"zsh-pro/core/store"|"zsh-pro/core/shell/zsh"' core/cli/` (expect 0 matches — no concrete-store/zsh import in core/cli).
  </verification>
  <done>The 5-verb CLI surface dispatches from `Run`; the cli-local `Store` and `Emitter` interfaces are the only seams `core/cli` uses (no concrete import); the emit path has a DEFINED pre-Phase-4 not-yet-available behavior via `fail` (C2), CLI-layer exit gating on emit errors (C3), and typed-nil-Store guards (C10); `zsh-pro hook | zsh -n` is 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Composition-root store + emitter wiring with typed-nil-safe error handling (C10)</name>
  <files>core/cmd/zsh-pro/main.go, core/cli/cli_test.go</files>
  <read_first>
    - core/cmd/zsh-pro/main.go (VERIFY against disk: the `provider`/`dir`/`kc`/`store.New`/`_, _ = s, err`/`cli.New(provider)` lines — M0)
    - core/store/store.go lines 88+ (`func New(dir, regen, kc) (*Store, error)` — returns `(nil, err)` on failure)
    - core/cli/cli.go (the New(provider, s, e) signature from Task 3)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-19 composition-root injection; store-init non-fatal for non-store verbs)
  </read_first>
  <behavior>
    - cli_test (C10, composition-root-shaped): construct a CLI exactly as main.go will on the error path — pass an explicit nil `cli.Store` interface (NOT a typed-nil `*store.Store`) — and assert a store-backed verb routes through `fail` (ExitRuntimeErr) and does NOT panic.
    - Build-level: `go build ./...` succeeds with `cli.New(provider, s, emitter)` at main.go, `_, _ = s, err` removed, and `err` consumed (no unused-variable compile error — C10).
  </behavior>
  <action>
    In `core/cmd/zsh-pro/main.go`, remove `_, _ = s, err` and thread both `s` and `err` into the CLI (Addresses review concern: C10). Because `store.New` returns `(nil, err)` on failure and a typed-nil `*store.Store` handed to a `cli.Store` interface parameter becomes a non-nil interface wrapping a nil pointer (so `c.store == nil` would be false and `Branches()` would panic), main.go MUST convert the error case into an EXPLICIT nil interface:
    ```
    var cliStore cli.Store
    if err == nil {
        cliStore = s          // *store.Store satisfies cli.Store
    }                          // else: cliStore stays the zero-value nil INTERFACE (c.store == nil is true)
    ```
    (Alternatively a `storeErr` field or `cli.New(provider, s, err)` shape may be used — the invariant is that a typed-nil `*store.Store` NEVER reaches the `cli.Store` interface, and `err != nil` yields a `c.store == nil`-true condition that routes through `fail`, not a panic.) Inject the emitter: pass `cli.NotReadyEmitter()` (the pre-Phase-4 not-yet-available stub, C2) until Phase 4's emit.go lands; the RE-DIFF obligation (Task 1 `05-CONTRACT.md`) covers swapping in the real emitter. Call `cli.New(provider, cliStore, emitter)`. Keep store-init `err` NON-FATAL (analyze/hook/emit-stub/install do not need the store — it surfaces only inside a store-backed verb handler via `fail`), matching main.go's existing reserved-store comment. main.go remains the SOLE importer of `core/shell/zsh` and concrete `store` (D-19).
    Addresses review concern: C10 (typed-nil interface -> explicit nil interface, routes through fail not panic), C2 (emitter injection point + RE-DIFF hook).
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./core/cli/... -run 'NilStore|Store'; grep -c '_, _ = s, err' core/cmd/zsh-pro/main.go</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` succeeds with `cli.New(provider, ...)` at main.go and `_, _ = s, err` removed (`grep -c '_, _ = s, err'` == 0); `err` is consumed (no unused-variable error).
    - main.go converts the `err != nil` case to an explicit nil `cli.Store` interface (grep shows a `var cliStore cli.Store` / conditional-assign pattern, NOT a bare `cli.New(provider, s, ...)` on the error path).
    - The composition-root-shaped nil-Store test asserts a store-backed verb routes through `fail` and does NOT panic.
    - The emitter injected is the not-yet-available stub (`cli.NotReadyEmitter()` or equivalent) until Phase 4 lands.
  </acceptance_criteria>
  <verification>
    `go build ./... && go vet ./...`; `go test ./core/cli/... -run 'NilStore|Store' -v`; `grep -n 'cliStore\|cli.New(' core/cmd/zsh-pro/main.go` (shows explicit-nil-interface handling on the err path).
  </verification>
  <done>main.go threads `s`+`err` into `cli.New` such that a store-init failure yields an explicit nil `cli.Store` interface (never a typed-nil `*store.Store`), store-backed verbs route through `fail` not panic (C10), and the not-yet-available emitter stub is injected (C2) pending the Phase-4 RE-DIFF.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| binary stdout -> sourced verb `eval` | The verb `eval`s the binary's emitted apply/deactivate code into the LIVE shell — untrusted-shaped code crosses into execution here (mitigated by the `zsh -n` gate + emit-exit/empty check in Plan 05-02). |
| profile/var/alias/function name -> `typeset -g` slot target | A derived slot name embeds user-controlled profile/var names; an unsanitized name is an invalid `typeset` target AND a command-substitution injection vector. |
| provider seam -> core/cli | The loader zsh text crosses the `HookScript()` seam; core/cli must hold no zsh text (layering + reduces the surface where zsh syntax originates). |
| CLI Emitter seam -> core/cli | The emit path crosses the `Emitter` interface; core/cli reaches emit WITHOUT importing core/shell/zsh (D-19). The pre-Phase-4 stub fails closed with a clear message, never fakes apply code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege / Tampering | slot-name derivation in `zp_capture_env`/shadow slots (`hook.go`) | mitigate | Sanitize `${name//[^A-Za-z0-9_]/_}` BEFORE any `typeset -g`/`${(P)}` on a derived name (D-02/C8, verified E16 that `$(...)` in a raw name EXECUTES). Task 2 acceptance greps the loader for the sanitize expression on every derived-name path. |
| T-05-02 | Tampering | `zp_restore_env` reversing a hand-changed value | mitigate | Drift guard: reverse only if `[[ "${(P)+var}" == "1" && "${(P)var}" == "$applied" ]]` (D-01/C6). Task 2 live_terminal_test covers the reverse-only-if-unchanged behavior. |
| T-05-03 | Denial of Service | env unset-vs-empty mis-encoding (`${(P)+var}` vs `-z`) leaving residue | mitigate | Use `[[ "${(P)+var}" == "1" ]]` (operand holds the NAME, C6) + `${slot+x}` shadow set-test (C7), never `-z`/`-n` (which silently drop empty priors). Task 2 acceptance greps for the correct forms. |
| T-05-04 | Information disclosure | secret `SecretRef` deref leaking into emitted code/shell history | mitigate (Plan 05-02) | Phase 5 drives the keychain deref at apply-time (OQ-05-02) — a runtime secret path. The secret-history-leak mitigation (read from var/tempfile, disable history for the eval) is specified + tested in Plan 05-02 Task 2 (C8). This plan introduces the seam; 05-02 hardens the eval. Residual: end-to-end PROF-03 is Phase 6. |
| T-05-05 | Tampering | zsh text leaking into `core/cli` (bypassing the single-emit-path invariant) | mitigate | Loader const lives only in `core/shell/zsh/hook.go`, reached via `HookScript()` (D-20); the emit path crosses the `Emitter` interface, never importing `core/shell/zsh` (D-19). Task 2/3 acceptance greps that `core/cli` holds no zsh text and imports neither `core/store` nor `core/shell/zsh`. |
| T-05-11 | Denial of Service / Correctness | typed-nil `*store.Store` reaching the `cli.Store` interface -> panic on store-backed verb | mitigate | main.go converts `err != nil` into an EXPLICIT nil `cli.Store` interface; store-backed verbs guard `c.store == nil` -> `fail` (C10). Task 4 composition-root-shaped test asserts non-panic. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies in this plan (loader is an embedded `const`; `hyperfine`/`zsh -n` are dev/CI tools, not Go modules — SPEC constraint). No package-manager install task exists; no Package Legitimacy Gate required. |

No HIGH-severity threat is left uncovered.
</threat_model>

<verification>
- `make check` (fmt-check + vet + lint + test) stays green; the Phase 4 zero-residue property test (once landed) is not broken.
- `go run ./core/cmd/zsh-pro hook | zsh -n; echo $?` == 0.
- `go test ./core/cli/... ./core/shell/zsh/...` passes; LookPath-guarded zsh tests skip cleanly where zsh is absent.
- Structural zero-subprocess grep on the loader start-path passes (pure-Go, no zsh required).
- C1/C10 pinned: `checkout A` exports `ZSHPRO_PROFILE=A`, `deactivate` unsets it; a nil-Store CLI routes through `fail` without panicking.
- Falsifiable properties pinned by this plan: (2) live-terminal mutation reaches the CURRENT shell + `checkout`/`ZSHPRO_PROFILE` lifecycle (live_terminal_test); (4) `$#path` stable across A->B->deactivate incl. checkout-first (live_terminal_test). Properties (1) idempotency + (3) fail-open reaches prompt are pinned in Plan 05-02.
</verification>

<success_criteria>
- `zsh-pro hook` prints a `zsh -n`-clean loader defining the 5 verbs + `zp_capture_env`/`zp_restore_env` + `ZP_UNSET_SENTINEL`, living as a const in `core/shell/zsh`.
- The C16 + C2 contract is reconciled (or the assumed baseline recorded) in a DURABLE `05-CONTRACT.md` with a BLOCKING re-diff phase-exit obligation; no speculative `zp_rebuild_path`/`zp_shadow_*` helper.
- Sourcing the loader + `checkout A` mutates the CURRENT shell and exports `ZSHPRO_PROFILE=A`; `deactivate` is zero-residue and unsets `ZSHPRO_PROFILE`; `$#path` stable across switches incl. checkout-first (M4); nested-shell asymmetry guarded (M8) (verified against a fake emit source until Phase 4 lands).
- `list`/`status` are store-backed and wired through the CLI at main.go via `cli.New(provider, s, emitter)`; `status` reports per-terminal activation state (M1); `core/cli` imports no concrete store or zsh package; the `Emitter` seam has a defined not-yet-available behavior (C2); typed-nil Store routes through `fail` not panic (C10).
</success_criteria>

<output>
Create `.planning/phases/05-runtime-loader-cli-bootstrap/05-01-SUMMARY.md` when done. Include (or link to the durable `05-CONTRACT.md`) the C16 + C2 reconciliation table (helpers + state slots + CLI->emit subcommand surface; emit.go present/absent; the BLOCKING re-diff obligation status).
</output>