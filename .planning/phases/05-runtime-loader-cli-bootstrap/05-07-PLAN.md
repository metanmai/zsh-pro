---
phase: 05-runtime-loader-cli-bootstrap
plan: 07
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/live_terminal_test.go
autonomous: true
gap_closure: true
requirements: [BOOT-01, BOOT-02]
must_haves:
  truths:
    - "Every public list invocation uses the bounded, error-consuming runtime boundary and leaves direct ERR_EXIT/ERR_RETURN callers usable when the binary is missing, fails, or times out."
    - "Secret-bearing emitted source is staged only in an owner-controlled mode-0700 runtime directory and cannot be replaced, redirected, or read through a writable non-sticky TMPDIR."
    - "An original environment value equal to the legacy unset-marker string is restored verbatim; unset and empty originals remain distinguishable."
  artifacts:
    - path: "core/shell/zsh/hook.go"
      provides: "Private runtime staging, bounded list behavior, and presence-aware environment restoration"
      contains: "_zp_run_bounded"
    - path: "core/shell/zsh/live_terminal_test.go"
      provides: "Direct hostile-option list, shared-directory replacement, and sentinel-collision regressions"
      contains: "ERR_EXIT"
    - path: "core/shell/zsh/hook_test.go"
      provides: "Loader contract assertions for runtime state helper behavior"
      contains: "HookScript"
  key_links:
    - from: "list"
      to: "_zp_run_bounded"
      via: "shared timeout, capture, diagnostic, and zero-return public boundary"
      pattern: "_zp_run_bounded"
    - from: "_zp_run_bounded and _zp_eval_block"
      to: "owner-controlled runtime directory"
      via: "private capture and validation files never allocated under TMPDIR"
      pattern: "_zp_private_temp"
    - from: "zp_capture_env"
      to: "zp_restore_env"
      via: "separate sanitized presence metadata rather than a value-domain sentinel"
      pattern: "__ZP_ORIG_"
  flagged_assumptions:
    - statement: "SPEC fallback: BOOT-02 has a concurrency edge classified unclassified/unresolved; the non-sticky shared-directory replacement probe is mandatory evidence for the private staging boundary."
      status: unresolved
      flagged: true
---

<objective>
Close the loader-only fail-open, private-staging, and environment-restoration gaps with direct hostile zsh probes.

Root cause (single why): `list` bypassed the shared boundary, temporary files trusted any writable TMPDIR and were reopened by path, and D-03 encoded presence in a value that a legitimate environment variable can equal.

Purpose: make BOOT-02 safe for public read-only runtime calls and transient secret-bearing source, while preserving BOOT-01's zero-residue promise for every valid environment value.
Output: one bounded public list path, owner-controlled staging, presence-aware restoration, and direct adversarial live-zsh regressions.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-REVIEW.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route list through the fail-open boundary and stage runtime source only under an owner-controlled directory</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/live_terminal_test.go, core/shell/zsh/hook_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go (`_zp_private_temp`, `_zp_run_bounded`, `_zp_eval_block`, and `list`)
    - core/shell/zsh/live_terminal_test.go (`TestLiveTerminalPublicVerbsFailOpenUnderErrExitAndErrReturn`, timeout tests, and staging test)
    - core/shell/zsh/hook_test.go (zero-subprocess start-path contract)
    - core/cli/install.go (the installer-owned `${ZSHPRO_HOME:-$HOME/.zsh-pro}` cache root and mode-0700 setup)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md (list failure/timeout and non-sticky TMPDIR replacement evidence)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-REVIEW.md (CR-02 and CR-07)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-05, D-13, D-15, and D-16)
  </read_first>
  <behavior>
    - `list` prints successful branch output but on missing, failing, or sleeping binaries records a runtime error and returns zero to direct ERR_EXIT and ERR_RETURN callers after the following command runs.
    - `list` shares the existing configured timeout policy and emits no stale output after a failed or timed-out command.
    - `_zp_run_bounded` capture and `_zp_eval_block` validation files never use TMPDIR; a mode-0777 non-sticky TMPDIR replacement attempt cannot alter a target file, cause a substituted source to execute, or reveal a secret-like emitted payload.
    - Any absent, unowned, or non-private configured runtime root fails as a handled runtime failure before staging rather than falling back to a shared temporary directory.
  </behavior>
  <action>
    Make `list` use `_zp_run_bounded` exactly as the public emit path does: use the configured finite timeout, print `REPLY` only after success, call `_zp_runtime_error` for command failure or timeout, and always return zero from the public sourced function. Cover success, missing binary, nonzero binary, and sleeping binary under both ERR_EXIT and ERR_RETURN with a following `SURVIVED` assertion. This implements D-05's sourced-verb boundary and BOOT-02 without changing `status`'s local per-terminal reporting.

    Replace `_zp_private_temp`'s writable TMPDIR policy with an owner-controlled runtime directory policy. Use the existing configured zsh-pro cache root only after confirming it is usable as a private root; create a per-shell mode-0700 child beneath it before allocating capture or emitted-source files. A missing, unsafe, or uncreatable private root must report through the existing handled runtime error path. Keep each staging file mode 0600, retain the exact path only inside this private directory through write, zsh validation, and cleanup, and never reopen a pathname in TMPDIR. Preserve D-13's zero-subprocess source-start path: directory preparation occurs only inside explicit runtime verbs, not while the loader is sourced.

    Add a production-shaped race regression, not just a collision unit assertion: configure TMPDIR as a shared non-sticky mode-0777 directory, run an attacker replacement/read attempt against the old candidate naming shape, feed a secret-like emitted payload through `_zp_eval_block`, and assert that the shared directory receives no staged source, the attacker target is untouched, no replacement source is evaluated, and the public caller survives. Keep and adapt the existing cleanup/timeout regressions so every private file is removed only from the directory created by this runtime invocation. Cite D-13, D-15, and D-16 in the test intent.
  </action>
  <acceptance_criteria>
    - `list` no longer directly executes the binary outside `_zp_run_bounded`; direct hostile-option callers remain usable for failure and timeout cases.
    - Successful list output remains byte-for-byte the binary result expected by existing CLI behavior.
    - No emitted source or command-capture file is allocated in TMPDIR, and a shared non-sticky TMPDIR race cannot redirect or disclose the staged content.
    - The loader source path remains free of startup subprocess work; all directory work occurs only when a verb runs.
  </acceptance_criteria>
  <verify>
    <automated>go test ./core/shell/zsh -run 'LiveTerminal.*(List|ErrExit|ErrReturn|Timeout|Staging|Private)|HookScript' -count=1</automated>
  </verify>
  <done>Every public list failure is bounded and consumed, and transient runtime source cannot be raced through TMPDIR.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace value-sentinel restoration with explicit original-presence state</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/shell/zsh/live_terminal_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go (`zp_capture_env` and `zp_restore_env`)
    - core/shell/zsh/hook_test.go (loader surface contract)
    - core/shell/zsh/live_terminal_test.go (current-shell mutation test conventions)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-01 through D-04)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md (sentinel collision probe)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-REVIEW.md (WR-01)
  </read_first>
  <behavior>
    - A value exactly equal to the historical unset-marker text is restored as that value after capture, apply, and deactivate.
    - Originally unset, originally empty, and originally nonempty variables all retain their distinct restoration outcomes.
    - Drift protection remains: user changes made after application are not overwritten by restore.
    - Sanitized slot naming remains in force before every indirect parameter operation.
  </behavior>
  <action>
    Preserve D-03's required unset-versus-empty round trip while replacing its fixed-marker implementation, which the direct collision probe disproved. Make `zp_capture_env` record original presence in a separate sanitized private metadata slot alongside the original value, and make `zp_restore_env` decide unset versus export from that presence state rather than comparing the value to a magic marker. Remove any value-domain branch that interprets a legitimate prior string as absence; retain a compatibility declaration only if the reconciled emitted surface actually consumes it, never as the restoration encoding.

    Keep D-01's drift guard and D-02's sanitization before `typeset -g`/indirect access. Add direct live-zsh cases for legacy-marker-valued, empty, and absent originals, each applying a managed value then restoring it; assert exact result plus cleanup of both data and presence slots. Add a post-apply manual-change case to prove the guard still avoids overwriting a user change. This is a semantic correction to D-03's implementation detail, required by the verified WR-01 evidence, not a reduction of the locked zero-residue decision.
  </action>
  <acceptance_criteria>
    - The former sentinel string is treated as ordinary data and round-trips exactly.
    - Presence is represented independently of value for all managed environment captures.
    - Empty, unset, and drifted values preserve their established behavior; derived slot names remain sanitized.
  </acceptance_criteria>
  <verify>
    <automated>go test ./core/shell/zsh -run 'LiveTerminal.*(Env|Restore|Sentinel)|HookScript' -count=1</automated>
  </verify>
  <done>Environment restoration is collision-free for every valid original string while preserving unset-versus-empty and drift-safe semantics.</done>
</task>

</tasks>

## Artifacts this phase produces

- A public list path sharing the bounded, fail-open runtime boundary.
- Owner-controlled transient source staging for both subprocess capture and zsh validation.
- Presence-aware env restoration that cannot confuse ordinary data with absence.

<pre_mortem>

| Likely failure | Early mitigation in this plan |
| --- | --- |
| The new private root is accidentally created during startup or falls back to TMPDIR. | Source-path structural test remains mandatory; the shared TMPDIR probe asserts no staging there. |
| A list error is diagnosed but its public function still returns nonzero under ERR_RETURN. | Direct simple-command probes run both ERR_EXIT and ERR_RETURN and require the next command to execute. |
| Presence metadata is added but a drift branch or slot cleanup regresses. | The live matrix covers legacy-marker data, absent, empty, and user-modified cases with slot cleanup assertions. |
</pre_mortem>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| emitted shell source -> temporary staging | Resolved secrets and executable source cross into a transient filesystem location before validation/eval. |
| public list function -> zsh-pro child process | A read-only verb invokes an absent, failing, or slow external binary from an interactive shell. |
| original environment value -> undo bookkeeping | Arbitrary user-controlled values must be represented without collision or destructive restore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-07-01 | Information Disclosure / Tampering | `_zp_private_temp`, `_zp_run_bounded`, `_zp_eval_block` | high | mitigate | Owner-controlled mode-0700 runtime directory, 0600 files, no TMPDIR fallback, and non-sticky replacement/read regression. |
| T-05-07-02 | Denial of Service | public `list` | high | mitigate | Route through timeout/reaping boundary, consume public failure status, and test direct ERR_EXIT/ERR_RETURN calls. |
| T-05-07-03 | Tampering | `zp_restore_env` | high | mitigate | Separate original-presence state from data value and test legacy-marker collision, empty, absent, and drift cases. |
| T-05-SC | Tampering | package installation | high | mitigate | No package-manager installation occurs; only existing Go and zsh facilities are used. |
</threat_model>

<verification>
<automated>go test ./core/shell/zsh -run 'LiveTerminal|HookScript' -count=1 && go test ./core/shell/zsh -count=1 && go vet ./...</automated>
</verification>

<success_criteria>

- BOOT-02 public read-only runtime calls are bounded and fail open.
- Transient code, including secret-bearing emitted code, cannot be raced through a writable shared temporary directory.
- Loader environment undo is zero-residue even for the historical sentinel's literal value.

</success_criteria>

<output>
Create `.planning/phases/05-runtime-loader-cli-bootstrap/05-07-SUMMARY.md` when done.
</output>
