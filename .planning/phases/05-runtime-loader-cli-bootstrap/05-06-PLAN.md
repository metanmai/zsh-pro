---
phase: 05-runtime-loader-cli-bootstrap
plan: 06
type: execute
wave: 5
depends_on: ["05-03", "05-05"]
files_modified:
  - core/cli/install.go
  - core/cli/install_test.go
  - core/cli/cli.go
  - core/cli/cli_test.go
autonomous: true
gap_closure: true
requirements: [BOOT-01, BOOT-02]
must_haves:
  truths:
    - "A readable but corrupt cached loader is consumed by the installed stub, so sourcing .zshrc under ERR_EXIT reaches the next shell command."
    - "Malformed exact managed markers reject installation before cache-directory creation or cached-loader replacement, leaving a known-good cache and .zshrc byte-identical."
    - "A literal-nil or typed-nil shell.Provider makes hook, install, and analyze return the normal CLI runtime error without a panic or partial output."
  artifacts:
    - path: "core/cli/install.go"
      provides: "Prepared .zshrc transaction before cache mutation and an ERR_EXIT-safe cached-loader stub"
      contains: "renderInstallBlock"
    - path: "core/cli/install_test.go"
      provides: "Live hostile-option corrupt-loader and malformed-marker cache-preservation regressions"
      contains: "TestInstalledStub"
    - path: "core/cli/cli.go"
      provides: "Nil-like Provider normalization and provider-dependent command guards"
      contains: "isNilLike"
    - path: "core/cli/cli_test.go"
      provides: "Typed-nil Provider public-verb regressions"
      contains: "Provider"
  key_links:
    - from: "core/cli/install.go runInstall"
      to: "replaceManagedBlock and writeValidatedLoader"
      via: "prepare and validate the .zshrc replacement before any cache mutation"
      pattern: "replaceManagedBlock"
    - from: "renderInstallBlock"
      to: "cached loader source"
      via: "conditional source whose result is consumed before the terminating success status"
      pattern: "source"
    - from: "CLI.New"
      to: "hook/install/analyze handlers"
      via: "nil-like provider guard before HookScript, runInstall, or analyze.New dispatch"
      pattern: "provider"
  prohibitions:
    - statement: "MUST NOT turn malformed managed-marker input into a mutation of a user's known-good cached loader or unmanaged bootstrap file."
      status: unresolved
      flagged: true
  flagged_assumptions:
    - statement: "SPEC fallback: BOOT-01 has an unclassified/unresolved edge because byte-preserving bootstrap and transaction ordering are coupled; direct malformed-marker non-mutation probes remain required evidence."
      status: unresolved
      flagged: true
---

<objective>
Close the bootstrap transaction and public Provider-constructor gaps exposed by direct live-zsh probes.

Root cause (single why): the installer mutates its cache before it knows whether the managed .zshrc region is valid, the stub invokes a fallible source as an unguarded command, and CLI.New omitted the Provider from its existing nil-like boundary normalization.

Purpose: restore BOOT-01's non-destructive installer boundary and BOOT-02's fail-open startup/CLI contract without changing architecture or adding dependencies.
Output: transactional installer ordering, ERR_EXIT-safe stub sourcing, provider normalization, and production-shaped regressions.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-CONTRACT.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-REVIEW.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-03-SUMMARY.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prepare the managed-block transaction before cache promotion and contain corrupt cached-loader startup failures</name>
  <files>core/cli/install.go, core/cli/install_test.go</files>
  <read_first>
    - core/cli/install.go (`runInstall`, `renderInstallBlock`, `replaceManagedBlock`, `writeValidatedLoader`, and `atomicWrite`)
    - core/cli/install_test.go (`TestInstallRefusesMalformedExactMarkerOrderingsWithoutWriting`, `TestInstalledStubFailsOpenForDisabledMissingAndCorruptLoaders`, and validation-preservation tests)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md (corrupt cache and malformed-marker transaction probes)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-REVIEW.md (CR-01 and CR-04)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-09 through D-14)
  </read_first>
  <behavior>
    - With a syntactically corrupt regular cached loader, `setopt ERR_EXIT; source .zshrc; print SURVIVED` exits successfully and prints `SURVIVED`; the same direct probe is safe under ERR_RETURN when applicable.
    - A stub sources only a readable regular cached-loader file and consumes either result of that source command before its terminating success status.
    - For nested, stray, or unterminated exact markers, `runInstall` returns an error before cache-directory creation, cached-loader promotion, or .zshrc write.
    - A pre-existing recognizable loader and the malformed .zshrc bytes remain byte-identical after the failed install; the no-cache variant leaves no cache directory behind.
  </behavior>
  <action>
    Refactor `runInstall` so it resolves install paths once, reads the target .zshrc, and calls `replaceManagedBlock(current, renderInstallBlock())` before `os.MkdirAll`, `os.Chmod`, or `writeValidatedLoader`. Keep the prepared replacement in memory; only after exact-marker validation succeeds may it create/secure the cache directory, validate/promote the loader candidate, and atomically write the prepared .zshrc bytes. This preserves D-09/D-10/D-12 and fixes the ordering defect in CR-04 without falsely claiming that an unrelated later disk-write failure rolls back a completed rename.

    Update `renderInstallBlock` per D-11/D-13/D-14 so the cached-loader path is first required to be a regular readable file and its source result is evaluated in conditional control flow that consumes success and failure alike. The managed block must still remain thin, preserve the `ZSHPRO_DISABLE` no-op, invoke no binary or command substitution on shell start, and finish with a success status. This directly fixes CR-01 under hostile shell options.

    Extend `install_test.go` before implementation: add a real zsh direct-source regression that enables ERR_EXIT, replaces the installed cache with malformed zsh, and asserts the command after the .zshrc source runs. Extend the malformed exact-marker `runInstall` cases to seed a known-good cache and separately cover no cache directory; assert the cache bytes, cache-directory presence, and .zshrc bytes after rejection. Preserve the existing exact physical-line scanner tests rather than weakening them. Cite D-09, D-10, D-11, D-13, and D-14 in the test intent.
  </action>
  <acceptance_criteria>
    - The corrupt-loader direct live-zsh probe survives ERR_EXIT and reaches its following command.
    - The stub guards a regular readable cache and conditionally consumes the source result; it retains `ZSHPRO_DISABLE`, no-subprocess startup behavior, and a final success status.
    - Every malformed exact-marker fixture rejects before cache mutation; known-good cache bytes and .zshrc bytes match their pre-install values, and a missing cache directory remains absent.
    - Valid installs retain the existing secure cache promotion and byte-preserving .zshrc behavior.
  </acceptance_criteria>
  <verify>
    <automated>go test ./core/cli -run 'Install|ManagedBlock|Marker|Stub|CachedLoader' -count=1</automated>
  </verify>
  <done>Installer rejection is transactionally prepared before cache mutation, and a corrupt readable cached loader cannot abort a hostile-option shell startup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize shell.Provider at the public CLI boundary</name>
  <files>core/cli/cli.go, core/cli/cli_test.go</files>
  <read_first>
    - core/cli/cli.go (`CLI.New`, `Run`, `runInstall`, and `runAnalyze`)
    - core/cli/dependencies.go (`isNilLike`)
    - core/cli/cli_test.go (`TestTypedNilDependenciesFailClosedWithoutPanic` and runtime-verb seam tests)
    - core/shell/provider.go (Provider composition contract)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-VERIFICATION.md (typed-nil provider probe)
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-CONTEXT.md (D-18, D-19, and D-20)
  </read_first>
  <behavior>
    - A literal nil Provider and an interface containing `(*zsh.Provider)(nil)` make `hook`, `install`, and `analyze` return ExitRuntimeErr through `CLI.fail` without panic.
    - A rejected provider-dependent command emits no loader/source bytes and does not touch an install target.
    - Existing Store, Emitter, and SecretResolver typed-nil behavior remains covered by the constructor boundary tests.
  </behavior>
  <action>
    Apply the existing `isNilLike` normalization to `p` in `CLI.New`, then add one provider-availability guard used before every Provider method dispatch: `hook` before `HookScript`, `runInstall` before passing its Hooker, and `runAnalyze` before constructing the analyzer. Route each through the established `CLI.fail` error path with a stable provider-unavailable diagnostic; do not import concrete `core/shell/zsh` into production `core/cli`, per D-19/D-20.

    Add table-driven public-verb tests in `cli_test.go` using both literal nil and a typed-nil `*zsh.Provider`. Wrap each invocation with the existing non-panic helper, assert ExitRuntimeErr plus human-mode stderr, assert empty stdout for the rejected hook/source path, and use a disposable HOME for the install case to prove it causes no filesystem mutation. Keep the existing typed Store/Emitter/Resolver cases in the same test family so the constructor boundary is complete.
  </action>
  <acceptance_criteria>
    - `CLI.New` converts nil-like Providers into the unavailable state before any method call.
    - `hook`, `install`, and `analyze` fail predictably for literal and typed-nil Providers; no test relies on `recover` as the production behavior.
    - Production `core/cli` still depends only on the shell interface seam, not concrete zsh implementation types.
  </acceptance_criteria>
  <verify>
    <automated>go test ./core/cli -run 'TypedNil|Provider|RuntimeVerbs|Install' -count=1 && go vet ./core/cli</automated>
  </verify>
  <done>All public Provider-dependent CLI verbs fail through normal runtime diagnostics instead of dereferencing a typed-nil provider.</done>
</task>

</tasks>

## Artifacts this phase produces

- A prepared-before-mutation installer transaction in `core/cli/install.go`.
- Hostile-option bootstrap regressions in `core/cli/install_test.go`.
- Complete Provider nil-normalization coverage at the CLI composition boundary.

<pre_mortem>

| Likely failure | Early mitigation in this plan |
| --- | --- |
| A malformed marker still changes cache state through an overlooked early filesystem call. | Seed both an existing-cache fixture and an absent-cache fixture, then assert every relevant path and byte sequence after failure. |
| A trailing success command masks an ERR_EXIT abort before it can run. | Source the actual installed stub under direct ERR_EXIT and assert a following sentinel command executes. |
| Provider normalization protects `hook` but leaves an indirect dispatch path dereferencing nil. | Exercise hook, install, and analyze through `CLI.Run` for literal and typed nil inputs. |
</pre_mortem>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| cached loader -> sourced .zshrc | Cached code executes during shell startup and a malformed file must not terminate the caller. |
| managed-marker input -> installer filesystem writes | User-owned .zshrc structure decides whether cache and bootstrap files may be changed. |
| shell.Provider interface -> CLI handlers | An injected interface may hold a typed nil before a public CLI verb dispatches it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-05-06-01 | Denial of Service | installed stub in `renderInstallBlock` | high | mitigate | Conditional source-result consumption plus direct ERR_EXIT/ERR_RETURN corrupt-cache probes prevents startup termination. |
| T-05-06-02 | Tampering | `runInstall` cache and .zshrc transaction | high | mitigate | Prepare/validate exact marker replacement before cache-directory creation or loader promotion; assert known-good cache and user file preservation. |
| T-05-06-03 | Denial of Service | `CLI.New` Provider seam | high | mitigate | Normalize nil-like Provider values and guard every provider-dependent public verb before method dispatch. |
| T-05-SC | Tampering | package installation | high | mitigate | No package-manager installation occurs; implementation uses existing Go and zsh facilities only. |
</threat_model>

<verification>
<automated>go test ./core/cli -run 'Install|ManagedBlock|Marker|Stub|CachedLoader|TypedNil|Provider' -count=1 && go test ./core/cli -count=1 && go vet ./...</automated>
</verification>

<success_criteria>

- BOOT-01 malformed-marker input leaves bootstrap/cache state untouched.
- BOOT-02 startup survives a corrupt readable cached loader under ERR_EXIT.
- Provider injection is as fail-closed as the existing Store/Emitter/Resolver seams.

</success_criteria>

<output>
Create `.planning/phases/05-runtime-loader-cli-bootstrap/05-06-SUMMARY.md` when done.
</output>
