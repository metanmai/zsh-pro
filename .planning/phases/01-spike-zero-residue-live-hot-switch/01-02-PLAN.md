---
phase: 01-spike-zero-residue-live-hot-switch
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scratch/spike.zsh
  - scratch/spike_test.go
  - .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md
  - .planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md
autonomous: true
requirements: [SW-03]

must_haves:
  truths:
    - "Failure-mode 1 holds: `activate A -> activate B (auto-deactivates A) -> deactivate B` leaves the six-class snapshot byte-identical to the pre-activation snapshot"
    - "Failure-mode 2 holds: after >=5 A<->B switch cycles, $#path equals the pre-activation count (no PATH growth) and no alias/function/option from a prior profile survives a switch"
    - "Failure-mode 3 holds (drift guard): a managed env var changed by hand mid-session is NOT clobbered by deactivate (restore fires only when the live value still equals what was applied)"
    - "One pass against a slice of a real ~/.zshrc runs entirely in the sandbox, asserts the introduced delta round-trips, and reports whether bindkey/hook state is present"
    - "01-FINDINGS.md records an admit (MANAGED/switchable) or exclude (EXCLUDED to master block) verdict for each of the six classes, plus the overall go/no-go"
    - "01-MANIFEST-SHAPE.md captures the validated Manifest JSON shape with field names, justified by the per-class reverse operations the fixtures proved"
    - "Locked decisions covered by this plan: D-02: byte-identical admission test (admit -> MANAGED / fail -> EXCLUDED to master block); D-03: overall NO-GO only if core classes (aliases/env/PATH) fail; D-04: no PATH accumulation across cycles + conditional drift guard; D-05: completion measured fpath-reversible vs compinit-imperative; D-06: real ~/.zshrc reality-check pass; D-07: sandboxed zsh -f, never mutates the real shell; D-08: the two durable deliverables 01-FINDINGS.md + 01-MANIFEST-SHAPE.md"
  artifacts:
    - path: ".planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md"
      provides: "Go/no-go writeup: per-class admit/exclude verdict + overall verdict + the loader reference snippet (D-08)"
      contains: "go/no-go"
      min_lines: 40
    - path: ".planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md"
      provides: "The validated Manifest JSON shape (Phase 4's input) with field names and per-field justification"
      contains: "Manifest"
      min_lines: 30
    - path: "scratch/spike_test.go"
      provides: "Throwaway Go verdict pin driving the harness through `zsh -f -c` (build-tag isolated from the default suite)"
      contains: "go:build spike"
  key_links:
    - from: "scratch/spike_test.go"
      to: "zsh -f -c <script>"
      via: "exec.CommandContext with 5s timeout (mirrors introspect.go) + LookPath skip-guard"
      pattern: "exec.CommandContext"
    - from: "01-FINDINGS.md"
      to: "the six measured classes (aliases/functions/env/PATH/options/completion)"
      via: "per-class admit/exclude verdict derived from empty-vs-nonempty snapshot diff"
      pattern: "completion"
---

<objective>
Stress the harness from Plan 01-01 against the three failure modes that decide trustworthiness, run one reality-check pass against a slice of a real `~/.zshrc`, optionally pin the verdict in a throwaway build-tag-isolated Go test, and write the two durable deliverables: the go/no-go decision writeup (per-class admit/exclude + overall verdict) and the validated `Manifest` JSON-shape document. These two writeups are the only things this phase leaves behind (D-08); everything in `scratch/` is throwaway.

Purpose: Convert the proven mechanism (Plan 01-01) into an observable, defensible verdict. The three failure-mode assertions are exactly the conditions under which a switcher becomes unusable (residue, PATH accumulation, clobbering hand edits); proving the harness resists all three is what retires the milestone's single material risk. The Manifest shape is Phase 4's literal input.

Output: failure-mode assertions added to `scratch/spike.zsh`; an optional `scratch/spike_test.go` (behind `//go:build spike`); `01-FINDINGS.md` (go/no-go); `01-MANIFEST-SHAPE.md` (validated shape). The findings and manifest-shape docs are durable and committed; the scratch code is git-ignored throwaway.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-CONTEXT.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-RESEARCH.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-PATTERNS.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-01-SUMMARY.md

<critical_constraints>
This plan depends on Plan 01-01's harness (`scratch/spike.zsh` with `snapshot()` + plain `apply_*`/`deact_*` loaders + fixtures). Read `01-01-SUMMARY.md` first for the exact section layout and fixture contents.

D-08 (throwaway + two durable outputs): the ONLY durable artifacts are `01-FINDINGS.md` (go/no-go) and `01-MANIFEST-SHAPE.md` (validated shape). All `scratch/` code is throwaway and git-ignored (from Plan 01-01). If you add `scratch/spike_test.go`, it MUST be isolated from the default `go test ./...` run and the `core/testgen` oracle property pin: put `//go:build spike` as the FIRST line so the file is excluded unless `-tags spike` is passed. Document the explicit run command in the findings. Do NOT add any `_test.go` to a production `core/...` package.

D-07 (sandboxing): the real-`~/.zshrc` reality-check pass MUST run in the sandbox. Read a SLICE of the user's `~/.zshrc` (copy a portion to a temp file under `mktemp`/`t.TempDir()`), then run it inside `zsh -f -c` against that temp file. NEVER source the user's real file into the live shell, and NEVER write back to it. Because `zsh -f` still inherits the parent environment (Pitfall 5), the real-`~/.zshrc` pass asserts only the DELTA round-trips (set-subtract the pre-snapshot), not a clean-base byte-identity.

D-02/D-03 verdict structure: byte-identical reverse is the ADMISSION TEST. A class that reverses byte-identical is admitted -> MANAGED (switchable). A class that does not (expected: completion's `compinit`) is EXCLUDED to the unmanaged master block — a SCOPING outcome, NOT a product-killer. A true NO-GO fires ONLY if the CORE classes (aliases / env / PATH) cannot hit the bar. The findings MUST emit a per-class admit/exclude line plus the overall go/no-go.

Completion is measured as DATA, not veto (D-05): measure the `fpath` ARRAY reversal SEPARATELY from `compinit`. Expect the verdict "fpath-array switching is byte-reversible; the compinit invocation is imperative -> EXCLUDED to master block" (RESEARCH Pitfall 4, Open Question 2). Keybindings/hooks are OUTSIDE the six named classes (Assumption A3) — the real-`~/.zshrc` pass REPORTS their presence (`bindkey -L`, `$precmd_functions`/`$chpwd_functions`) without formally admitting/excluding them.
</critical_constraints>

<interfaces>
<!-- The Go verdict pin reuses the exact introspect subprocess + skip-guard + TempDir shapes. Executor copies these directly. -->

Subprocess shape to copy — core/shell/zsh/introspect.go:42-53:
- `ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second); defer cancel()`
- `cmd := exec.CommandContext(ctx, "zsh", "-f", "-c", script, ...)`; `var out bytes.Buffer; cmd.Stdout = &out`
- on `cmd.Run()` error -> graceful degrade (the spike test just fails or skips)

Skip-guard to copy — core/shell/zsh/introspect_test.go:11-13:
- `if _, err := exec.LookPath("zsh"); err != nil { t.Skip("zsh not installed; skipping...") }`

Fixture-via-TempDir to copy — core/shell/zsh/introspect_test.go:14-19:
- write the spike script / `~/.zshrc` slice to `t.TempDir()`, never to a real path

Divergences for the spike test (PATTERNS "Divergences for the spike test"):
- the `script` is the full snapshot->apply->switch->deactivate->snapshot sequence (sourcing `scratch/spike.zsh`), not `introspectScript`
- the assertion is a literal `diff S_pre S_post` (string equality), not field checks on `IdentitySet`
- per D-08 this test is THROWAWAY and MUST NOT touch the `testgen` oracle property test; isolate it with `//go:build spike`

Proposed Manifest shape to validate and document — RESEARCH "Proposed Manifest JSON Shape" (lines 372-412). Already instantiated by the Plan 01-01 fixtures; this plan confirms no field is missing for any admitted class and writes it up with field names.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the three failure-mode assertions + the real-zshrc reality-check pass</name>
  <behavior>
    - FM1 (no-op round-trip): `activate A -> activate B (auto-deactivates A) -> deactivate B` leaves the six-class snapshot byte-identical to pre-activation (empty diff).
    - FM2 (no accumulation): after >=5 repeated A<->B cycles, `$#path` equals the pre-activation count; no alias/function/option from a prior profile is present after a switch.
    - FM3 (drift guard): when a managed env var is changed by hand between apply and deactivate, deactivate leaves the hand-set value intact (restore fires only if live == applied).
    - Real-zshrc pass: a sliced copy of `~/.zshrc`, run in-sandbox, has its INTRODUCED delta round-trip cleanly; the pass also reports presence/absence of bindkey + hook state.
  </behavior>
  <action>
    Extend `scratch/spike.zsh` (the harness from Plan 01-01) with the three failure-mode checks, each emitting a clear PASS/FAIL line:

    FM1 — run the `apply_A; deact_A; apply_B; deact_B` sequence from Plan 01-01 and assert `diff S_pre S_post` is empty (this restates SC1 as the no-op round-trip on a clean base).

    FM2 — wrap apply/deact in a loop of at least five A<->B cycles BEFORE the final deactivate; capture `$#path` (the array element count) into the pre-snapshot and assert it is unchanged after the cycles (no PATH growth — RESEARCH Pattern 1, V3b); additionally assert that after deactivating, none of profile A's or B's aliases/functions/options remain set (grep the post-snapshot for their names -> must be absent). PATH must be rebuilt from `ZP_BASE_PATH` each apply (never appended, never `typeset -U`).

    FM3 — between `apply_A` and `deact_A`, simulate a hand edit: reassign one managed env var to a sentinel value (e.g. `A_VAR=user_changed_it`). Assert that after `deact_A` the var still equals the sentinel (the drift guard refused to clobber it — RESEARCH Pattern 3, V3e). Then in a separate sub-case where the var is left untouched, assert deactivate DOES reverse it (unset or restore-original per the `original`-key presence, Pattern 4) — proving the guard is conditional, not a blanket skip.

    Real-`~/.zshrc` reality-check pass (D-06/D-07): copy a SLICE of `~/.zshrc` to a `mktemp` temp file (read-only; never write back). Inside `zsh -f -c`, snapshot -> source the temp slice (this is the "apply") -> snapshot -> then assert the INTRODUCED delta (post-minus-pre, via set-subtraction) is what the slice declared, and that reversing that delta returns to pre. Because `zsh -f` inherits the parent env (Pitfall 5), assert only the delta, not a clean-base identity. Also dump and REPORT (not admit/exclude) whether `bindkey -L` and `$precmd_functions`/`$chpwd_functions` are non-empty (Assumption A3, Open Question 1) so Phase 4 has the data. If `~/.zshrc` is absent, skip this pass gracefully with a printed note (do not fail the spike).
  </action>
  <verify>
    <automated>zsh -f -c 'source scratch/spike.zsh; export PATH=/usr/bin:/bin; ZP_BASE_PATH="$PATH"; T=$(mktemp -d); snapshot>"$T/pre"; pre=$#path; i=0; while (( i<5 )); do apply_A; deact_A; apply_B; deact_B; (( i++ )); done; snapshot>"$T/post"; post=$#path; diff "$T/pre" "$T/post" >/dev/null && [[ $pre -eq $post ]] && echo FM12_PASS || { echo FM12_FAIL; diff "$T/pre" "$T/post"; }' | grep -qx FM12_PASS && zsh -f -c 'source scratch/spike.zsh; export PATH=/usr/bin:/bin; ZP_BASE_PATH="$PATH"; apply_A; A_VAR=user_changed_it; deact_A; [[ "$A_VAR" == user_changed_it ]] && echo DRIFT_PASS || echo DRIFT_FAIL' | grep -qx DRIFT_PASS && echo ALL_PASS</automated>
  </verify>
  <done>
    All three failure modes pass under sandboxed `zsh -f -c`: the no-op round-trip diff is empty (FM1); after 5+ A<->B cycles `$#path` is unchanged and no prior-profile alias/function/option survives (FM2); a hand-changed managed env var survives deactivate while an untouched one is correctly reversed (FM3 — the guard is conditional). The real-`~/.zshrc` pass runs in-sandbox against a sliced temp copy, asserts the introduced delta round-trips, reports bindkey/hook presence, and skips gracefully if `~/.zshrc` is absent. The user's real shell and rc files are never mutated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin the verdict (optional Go test, build-tag isolated) and write the two durable deliverables</name>
  <read_first>
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md, 01-MANIFEST-SHAPE.md (the files being created)
    - scratch/spike_test.go (the file being created, if the Go pin is used)
    - scratch/spike.zsh and .planning/phases/01-spike-zero-residue-live-hot-switch/01-01-SUMMARY.md — the harness + fixtures whose results these docs report
    - core/shell/zsh/introspect.go:42-53 — the `exec.CommandContext(ctx, "zsh", "-f", "-c", script, ...)` + 5s-timeout subprocess shape to copy
    - core/shell/zsh/introspect_test.go:10-49 — the LookPath skip-guard (`:11-13`) and the `t.TempDir()` fixture-write pattern (`:14-19`); the only test in the codebase that shells out to zsh
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-RESEARCH.md — "Proposed Manifest JSON Shape" (lines 372-412, the shape to document), the Summary's per-class GO/exclusion findings, Pitfall 4 (completion: fpath reversible, compinit imperative), Open Questions 1-2 (keybindings/hooks; completion granularity)
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-CONTEXT.md — D-02/D-03 (verdict structure: admit -> managed, fail -> master block, no-go only on core classes), D-08 (the two durable outputs)
  </read_first>
  <action>
    (Optional but recommended) Create `scratch/spike_test.go` to pin the verdict under the project's real subprocess shape. FIRST line MUST be `//go:build spike` (then a blank line, then `package main` or `package spike`) so it is EXCLUDED from `go test ./...` and never touches the `testgen` oracle pin (D-08). Copy the skip-guard from introspect_test.go:11-13 (`exec.LookPath("zsh")` -> `t.Skip`), write the spike script / `~/.zshrc` slice to `t.TempDir()` (introspect_test.go:14-19), and drive the snapshot->apply->switch->deactivate->snapshot sequence via `exec.CommandContext(ctx, "zsh", "-f", "-c", script, ...)` with the 5s timeout (introspect.go:42-53). Assert a literal empty `diff S_pre S_post` (string equality), not `IdentitySet` field checks. Confirm `go test ./...` STILL passes unchanged (the build-tagged file is invisible to it) and that `go test -tags spike ./scratch/...` runs the pin.

    Write `01-FINDINGS.md` — the go/no-go decision writeup (durable deliverable 1, D-08). It MUST contain: (a) a per-class verdict TABLE with one row per the six classes (aliases, functions, env, PATH, options, completion), each marked ADMITTED -> MANAGED (switchable) or EXCLUDED -> unmanaged master block, with the empty-vs-nonempty snapshot-diff evidence and a one-line reason; (b) the completion nuance recorded explicitly: fpath-array switching is byte-reversible but `compinit` is imperative -> completion EXCLUDED (or admittable only as fpath-membership without per-switch compinit — Open Question 2); (c) the bindkey/hook presence note from the real-`~/.zshrc` pass (Assumption A3 / Open Question 1), reported as data, not a verdict; (d) the OVERALL go/no-go — GO if the core classes (aliases/env/PATH) reversed byte-identical (D-03), with options' status and any exclusions stated; (e) the loader reference snippet (the plain `apply_*`/`deact_*` + the verified-loop driver) embedded inline as the D-08 surviving snippet, with a note that the `scratch/` code itself is throwaway.

    Write `01-MANIFEST-SHAPE.md` — the validated `Manifest` JSON shape (durable deliverable 2, D-08; Phase 4's literal input). Lift the shape from RESEARCH lines 372-412, present the final field names confirmed by the Plan 01-01 fixtures, and for EACH field give the one-line justification tied to the reverse operation it enables (env scalars + the `original`-key presence encoding unset-vs-empty and the drift guard; `lists` add/delete deltas vs the captured base, never an absolute PATH, compinit excluded; `aliases`/`functions` added + shadowed-with-prior-body; `options` enabled + was_on). State explicitly which classes have NO representation in the manifest because the verdict excluded them (expected: compinit) — that absence is the correct outcome.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md && test -f .planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md && grep -iq 'go/no-go\|go / no-go\|overall.*go' .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md && grep -iq 'completion' .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md && grep -iq 'manifest' .planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md && GOTOOLCHAIN=auto go test ./... >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <done>
    `01-FINDINGS.md` records an ADMITTED/EXCLUDED verdict for each of the six classes with diff evidence, the completion fpath-vs-compinit nuance, the bindkey/hook presence note, the overall go/no-go (GO iff core classes reversed byte-identical), and the embedded loader reference snippet. `01-MANIFEST-SHAPE.md` captures the validated Manifest JSON shape with final field names and per-field reverse-operation justifications, and names any excluded class's absence as intentional. If present, `scratch/spike_test.go` begins with `//go:build spike`, is invisible to `go test ./...` (which still passes), and runs under `-tags spike`. The `core/testgen` oracle property test is untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike subprocess -> user's real shell / `~/.zshrc` | The reality-check pass reads a SLICE of the real `~/.zshrc` but runs it only inside `zsh -f -c` against a temp copy; the live shell and the real file are never mutated (D-07). |
| `~/.zshrc` slice -> in-sandbox loader | The slice is the user's own config (not third-party/untrusted), executed in an isolated `zsh -f` subprocess; it can affect only that subprocess. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | real-`~/.zshrc` reality-check mutating the user's real shell or rewriting `~/.zshrc` | mitigate | The pass copies a slice to a `mktemp`/`t.TempDir()` temp file (read-only source), runs it inside `zsh -f -c`, and asserts only the in-subprocess delta; it never sources into the live shell and never writes back to `~/.zshrc` (Task 1 done-criteria). Graceful skip if `~/.zshrc` is absent. |
| T-01-05 | Tampering | throwaway Go verdict pin polluting the production test graph / breaking the testgen oracle | mitigate | `scratch/spike_test.go` carries `//go:build spike` as its first line, excluding it from `go test ./...`; verified by re-running the default suite green (Task 2 done-criteria). No `_test.go` is added to any `core/...` package. |
| T-01-06 | Elevation/Tampering (DEFERRED) | shell-code injection via escaping of arbitrary user config values into emitted `eval`'d zsh | accept (deferred to Phase 4) | OUT OF SCOPE here: even the real-`~/.zshrc` pass SOURCES a slice in a sandbox rather than emitting `eval`-able code from parsed user values. The injection threat (escaping arbitrary user values into emitted apply/deactivate code) belongs to Phase 4 (`core/shell/zsh/emit.go`). Named, not solved. |
</threat_model>

<verification>
- FM1/FM2/FM3 each print a PASS line under sandboxed `zsh -f -c`: empty no-op-round-trip diff; `$#path` unchanged after 5+ cycles with no surviving prior-profile state; the drift guard preserves a hand-changed var yet still reverses an untouched one.
- The real-`~/.zshrc` pass runs in-sandbox against a sliced temp copy, asserts the introduced delta round-trips, reports bindkey/hook presence, and skips gracefully when `~/.zshrc` is absent — the real shell and file are never mutated.
- `01-FINDINGS.md` exists with a six-row per-class admit/exclude table, the completion nuance, the bindkey/hook note, the overall go/no-go, and the embedded loader reference snippet.
- `01-MANIFEST-SHAPE.md` exists with the validated shape, final field names, and per-field justifications.
- If `scratch/spike_test.go` exists it starts with `//go:build spike`; `go test ./...` passes unchanged and `go test -tags spike ./scratch/...` runs the pin. The `core/testgen` oracle property test is untouched.
</verification>

<success_criteria>
- All three failure-mode assertions hold (byte-identical no-op round-trip; no PATH accumulation across cycles and no surviving prior-profile state; conditional drift guard) — SC1/SC2/SC3.
- A real-`~/.zshrc` reality-check pass runs entirely in the sandbox, validates the introduced delta round-trips, and reports bindkey/hook presence (D-06, Assumption A3).
- The go/no-go decision writeup (`01-FINDINGS.md`) records a per-class admit/exclude verdict plus the overall verdict — the first durable deliverable (SC4, D-08).
- The validated Manifest JSON shape (`01-MANIFEST-SHAPE.md`) is captured with field names — the second durable deliverable and Phase 4's input (D-08).
- The spike remains throwaway and isolated: `scratch/` is git-ignored, any Go pin is build-tagged, and `go test ./...` plus the testgen oracle pin are untouched.
</success_criteria>

<output>
Create `.planning/phases/01-spike-zero-residue-live-hot-switch/01-02-SUMMARY.md` when done. Record: the three failure-mode results, the real-`~/.zshrc` pass outcome (including bindkey/hook presence), the overall go/no-go verdict and any class exclusions, and the final Manifest field names. Note the two durable deliverables (`01-FINDINGS.md`, `01-MANIFEST-SHAPE.md`) as the phase's surviving outputs and confirm the scratch code is git-ignored throwaway.
</output>
