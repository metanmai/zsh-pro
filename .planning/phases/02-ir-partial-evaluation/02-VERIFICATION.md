---
phase: 02-ir-partial-evaluation
verified: 2026-06-27T03:10:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: initial verification (no prior VERIFICATION.md)
---

# Phase 2: IR + Partial Evaluation Verification Report

**Phase Goal:** Build a near-lossless, regenerable `model.Profile`/`Entry` constructed from the parser's existing `[]model.Block`, with each entry classified declarative-vs-imperative (the switchability gate) and tagged static-vs-dynamic for portability, then regenerated back to behavior-equivalent `.zsh` anchored on verbatim `Block.Text`.
**Verified:** 2026-06-27T03:10:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1 | `~/.zshrc` parses into a `model.Profile`; regenerating reproduces behavior-equivalent `.zsh` (untouched verbatim, only declarative slices templated). | ✓ VERIFIED | `TestRegenRoundTrip` (core/ir/roundtrip_test.go) RAN (not skipped — zsh 5.9 present) and PASSED: original and regenerated `.zsh` produce byte-identical `model.IdentitySet` across Aliases/Functions/Env/Options/Path. `ir.Regenerate` (regen.go:21-35) emits managed entries via `shell.Regenerator`, all else verbatim `Text`. |
| 2 | Each statement classified declarative vs imperative; uncertain → unmanaged (precision over recall). | ✓ VERIFIED | `routeManaged` (core/ir/route.go) admits ONLY exactly-one-name, plain-`=`, non-flagged shapes in the 5 reversible classes; Opaque + all unknown categories (CatMisc/Keybindings/Plugins/Local) + KindCompound/KindOther route imperative. Classifier confidence is never a routing gate (D-06). `TestRouteManaged` passes 29 subcases including all conservative-default shapes. |
| 3 | A value with $HOME/${...}/$(...)/backticks/conditional is tagged dynamic + late-bound verbatim; syntactic constant → static; $HOME round-trips intact. | ✓ VERIFIED | `wordIsDynamic` (parse.go:223-237) walks the AST for ParamExp/CmdSubst/ArithmExp/ProcSubst — no execution. `TestParseDynamicFlag` passes incl. `single-quoted-with-dollar` (single-quoted `$HOME` = static). Oracle asserts regenerated bytes still contain literal `$HOME/go` and `$HOME/bin:$PATH` (never resolved), and GOPATH/PATH late-bind identically in both snapshots. |
| 4 | Partial-eval performs NO execution (static AST inspection only); util.ExpandHome never used inside the IR. | ✓ VERIFIED | `grep ExpandHome core/ir/ core/model/` → NONE. No `core/util` import, no `os/exec`/`exec.Command`/`exec.LookPath` in non-test ir/model. Detection is pure `syntax.Walk`. |

**Score:** 4/4 roadmap success criteria verified.

### Plan Must-Haves (merged with roadmap SCs)

| # | Must-Have (source) | Status | Evidence |
| - | ------------------ | ------ | -------- |
| 1 | Block carries verbatim value/body/option-args + Dynamic flag from AST, no execution (02-01) | ✓ VERIFIED | block.go:37-40 (`Value`, `Dynamic`, `Append`, `Flagged`); parse.go offset-slices `a.Value` span and sets Dynamic via `wordIsDynamic`. |
| 2 | `ir.Build` → single ordered `Profile` of `Entry` in source order, category a field not storage (02-01) | ✓ VERIFIED | build.go:21-45 appends one Entry per block in order; profile.go: `Profile{Entries []Entry}`, `Category` is a field. |
| 3 | Each Entry routed managed iff in 5 admitted reversible classes; Opaque/unknown → imperative (02-01) | ✓ VERIFIED | route.go + `TestRouteManaged` (29 subcases pass). |
| 4 | $HOME/${...}/$(...)/backtick → Dynamic; constant → static; single-quoted → static (02-01) | ✓ VERIFIED | `TestParseDynamicFlag` all 7 subcases pass. |
| 5 | Per-entry ManagedOverride (auto/forced-managed/forced-unmanaged) defaults auto; override wins over auto (02-01) | ✓ VERIFIED | profile.go:6-50 `ManagedOverride` + `EffectiveManaged`; `TestEffectiveManaged` (5 subcases) + `TestEffectiveManagedOverrideWins` pass; Build sets `OverrideAuto`. |
| 6 | testgen oracle pin + corpus golden stay green after additive change (02-01) | ✓ VERIFIED | `TestOracleProperty` (10 seeds) PASS; `TestCorpusGolden` (6 fixtures) PASS. |
| 7 | Regenerating reproduces behavior-equivalent `.zsh`: imperative/Opaque verbatim, declarative templated from fields (02-02) | ✓ VERIFIED | regen.go + zsh/regen.go; oracle + `TestRegenerateSourceOrderAndRouting` pass. |
| 8 | Declarative entries rebuilt from structured fields, not echoed from Text (no tautology) (02-02) | ✓ VERIFIED | zsh/regen.go templates `export %s=%s` / `alias %s=%s` / `%s %s` from Names/Value/CmdName; oracle's class-presence sanity proves templated function+setopt regen. |
| 9 | Dynamic value emitted late-bound verbatim, never resolved (02-02) | ✓ VERIFIED | `TestRegenerateDynamicValueNeverResolved` + oracle `$HOME` literal byte-assertions. |
| 10 | Round-trip oracle sources orig+regen under sandboxed `zsh -f`, snapshots state, asserts byte-identical, skips cleanly if zsh absent (02-02) | ✓ VERIFIED | roundtrip_test.go uses `Provider.Introspect` (zsh -f + 5s timeout), `t.TempDir()`, `reflect.DeepEqual` per table; skip-guarded via `exec.LookPath`. Ran and passed. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `core/model/profile.go` | Profile, Entry, ManagedOverride + Override* consts | ✓ VERIFIED | All present; `EffectiveManaged` implemented. |
| `core/model/block.go` | additive Value, Dynamic (+Append, Flagged) | ✓ VERIFIED | Lines 37-40; stdlib-only package (no imports). |
| `core/shell/zsh/parse.go` | value/body capture + Dynamic via AST (`wordIsDynamic`) | ✓ VERIFIED | Append/Flagged captured; DeclClause + CallExpr paths covered. |
| `core/shell/zsh/regen.go` | `(Provider) Regenerate` declarative templater | ✓ VERIFIED | Total (default→Text); empty-Names guards (BL-01). |
| `core/shell/provider.go` | `shell.Regenerator` composed into Provider | ✓ VERIFIED | Regenerator interface added; Provider composes it. |
| `core/ir/build.go` | `Build([]Block, Classifier) Profile` | ✓ VERIFIED | Source-order; defensive Names copy (WR-03). |
| `core/ir/route.go` | `routeManaged` ING-02 gate | ✓ VERIFIED | Hardened gate; all adversarial shapes route imperative. |
| `core/ir/regen.go` | `Regenerate(Profile, Regenerator) []byte` source-order | ✓ VERIFIED | No zsh syntax in ir (delegates to seam). |
| `core/ir/roundtrip_test.go` | `TestRegenRoundTrip` byte-identical oracle | ✓ VERIFIED | Ran + passed with adversarial fixture. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| ir/build.go | shell.Classifier | constructor-injected interface, no re-parse | ✓ WIRED | `c.Classify(b)` called; `go list -deps` shows only model+shell. |
| parse.go describe() | Block.Value / Block.Dynamic | offset-slice + `wordIsDynamic` | ✓ WIRED | sliceSrc + Walk present and tested. |
| ir/regen.go | shell.Regenerator | `r.Regenerate(e)` for managed, else Text | ✓ WIRED | regen.go:26-30. |
| roundtrip_test.go | zsh.Provider.Introspect | snapshot orig vs regen IdentitySet | ✓ WIRED | Two Introspect calls, separate err vars. |
| zsh/regen.go | model.Entry.Value | emit captured Value verbatim | ✓ WIRED | `e.Value` emitted unquoted; dynamic-safe. |

### Layering / Architecture Invariants

| Invariant | Status | Evidence |
| --------- | ------ | -------- |
| `core/model` imports only stdlib | ✓ VERIFIED | No import lines / no `zsh-pro/` internal imports. |
| `core/ir` (non-test) never imports `core/shell/zsh` | ✓ VERIFIED | `go list -deps zsh-pro/core/ir` → only `core/model`, `core/shell`, `core/ir`. (A `grep -l` false-positive matched the doc-comment substring "core/shell/zsh"; actual import block confirmed clean.) |
| EVAL-01 no-execution: no `util.ExpandHome`, no exec in ir/model | ✓ VERIFIED | grep NONE; AST-walk only. |
| Forward zsh codegen confined to `core/shell/zsh` | ✓ VERIFIED | ir/regen.go has no `fmt.Sprintf` of zsh syntax; delegates to Regenerator seam. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Full build clean | `GOTOOLCHAIN=auto go build ./...` | exit 0 | ✓ PASS |
| Full test suite clean | `GOTOOLCHAIN=auto go test -count=1 ./...` | all ok, exit 0 | ✓ PASS |
| go vet clean | `GOTOOLCHAIN=auto go vet ./...` | exit 0 | ✓ PASS |
| golangci-lint clean | `golangci-lint run core/ir/... core/model/... core/shell/...` | 0 issues | ✓ PASS |
| Round-trip oracle ran (not skipped) | `go test -v -run TestRegenRoundTrip ./core/ir/` | `--- PASS` (zsh 5.9 present) | ✓ PASS |
| testgen regression pin | `go test -v -run TestOracleProperty ./core/testgen/` | 10 seeds PASS | ✓ PASS |
| corpus golden regression pin | `go test -v -run TestCorpusGolden ./core/analyze/` | 6 fixtures PASS | ✓ PASS |
| BL-01 no-panic on empty Names | `TestRegenerateNeverPanicsOnEmptyNames` | PASS | ✓ PASS |

### Code-Review Closure Verification (02-REVIEW.md: 2 blockers + 4 warnings)

The review's `status: resolved` claim was independently verified against the live source — fixes confirmed present, not just claimed:

| Finding | Fix Location | Confirmed |
| ------- | ------------ | --------- |
| BL-01 panic on empty Names | route.go:41,47 (`len==1` guards) + regen.go:32,40 (verbatim fallback) | ✓ Code present + test `TestRegenerateNeverPanicsOnEmptyNames` |
| BL-02 multi-name corruption | route.go:41,47 (`len != 1` → imperative); oracle adversarial `export MULTA=one MULTB=two` verbatim | ✓ Code present + oracle assertions |
| WR-01 `+=` rewritten to `=` | block.go:39 `Append`; parse.go captures `a.Append`; route.go:47 routes imperative | ✓ Code present + oracle `FPATH+=:/extra` assertion |
| WR-02 `alias -g` flag dropped | block.go:40 `Flagged`; parse.go:102-104; route.go:41 `!b.Flagged` | ✓ Code present + oracle `alias -g GG=` assertion |
| WR-03 Names slice aliasing | build.go:36 `append([]string(nil), b.Names...)` | ✓ Defensive copy present |
| WR-04 false-green oracle | roundtrip_test.go asserts `err == nil` + ingests all adversarial shapes | ✓ Present |
| IN-01 setopt trailing space | regen.go:52 returns Text on empty Names | ✓ Present |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| ING-01 | 02-02 | Regenerable categorized representation, round-trips to behavior-equivalent zsh; untouched verbatim, declarative templated | ✓ SATISFIED | `ir.Regenerate` + `TestRegenRoundTrip` byte-identical across 5 classes. |
| ING-02 | 02-01 | Classify declarative vs imperative; misclassified imperative = zero-residue violation | ✓ SATISFIED | `routeManaged` hardened gate; precision-over-recall conservative defaults; `TestRouteManaged`. |
| EVAL-01 | 02-01, 02-02 | Static resolved syntactically, dynamic kept late-bound; never resolved; portability | ✓ SATISFIED | `wordIsDynamic` AST-only; no ExpandHome in ir/model; `$HOME` round-trips intact (oracle byte-assert). |

No orphaned requirements: REQUIREMENTS.md maps exactly ING-01/ING-02/EVAL-01 to Phase 2, all claimed by plans, all marked Complete.

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER` in any phase-modified file; no stub returns or panics in production paths; vet + lint clean.

### Design-Resolution Note (not a gap)

Roadmap SC1 phrasing says entries are "grouped by category." The resolved design (D-09, documented in 02-01 PLAN/SUMMARY) stores `Category` as a per-Entry field and regenerates in **source order**, not a stored category-grouped view. Category IS captured per entry (enabling category views), and behavior-equivalence across all 5 categories is proven by the oracle. A stored `ByCategory()` view is deliberately deferred to Phase 4 (where the manifest builder needs it) to avoid untested dead code. This is an accepted, documented design decision — the SC's intent (category-aware, behavior-equivalent regeneration) is achieved.

### Human Verification Required

None. Every success criterion and must-have is programmatically verifiable and was verified against the live codebase (build, full test suite, the zsh-backed round-trip oracle that actually executes config under `zsh -f`, layering grep/go-list, and lint). No visual, real-time, or external-service behavior is involved.

### Gaps Summary

No gaps. All 4 roadmap success criteria and all 10 plan must-haves are verified against the actual source and a green test suite. The round-trip oracle executed (zsh 5.9 present) and proved byte-identical runtime state. All 8 code-review findings (2 blockers, 4 warnings, 2 info) were confirmed fixed in the live code — not merely asserted in the review frontmatter. Layering and the EVAL-01 no-execution invariant hold. Build, vet, lint, and all regression pins are clean.

---

_Verified: 2026-06-27T03:10:00Z_
_Verifier: Claude (gsd-verifier)_
