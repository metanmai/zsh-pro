# Phase 6 Findings Ledger

**Updated:** 2026-08-08
**Scope:** CodeRabbit, plan-checker, and source-audit findings for Phase 6

Status meanings:

- `OPEN` — actionable and not yet fixed.
- `FIXED` — a concrete change exists but has not passed the required independent verification.
- `VERIFIED` — the fix passed its focused checks and the relevant review gate.
- `DISMISSED WITH EVIDENCE` — non-actionable, duplicate, speculative, or out of scope, with exact evidence recorded.

A `VERIFIED` finding is reopened only when new code, test, runtime, or review evidence contradicts it.

| ID | Finding | Status | Evidence |
| --- | --- | --- | --- |
| P6-001 | Master derivation positive-selected recognized imperative forms and could drop parser-opaque or gap bytes. | VERIFIED | Commit `86b56b1` makes exact byte extents authoritative and retains imperative, opaque, comment, and gap bytes; plan structure, stale-term, and CodeRabbit rerun gates passed. This routing design is superseded only where P6-011/P6-012 explicitly revise physical startup composition. |
| P6-002 | Pre-commit compensation could abort Store state before classifying/restoring the filesystem. | VERIFIED | Commit `86b56b1` fixes the order to filesystem classify/restore-or-retain, then AbortIngest, loader/cache rollback, initializer rollback; later gates are disarmed on uncertainty. |
| P6-003 | Exchange recovery used ambiguous candidate/recovery basenames. | VERIFIED | Commit `86b56b1` defines one `exchangePeerBasename`, one independent `candidateEvidenceBasename`, and no second displaced-occupant artifact. |
| P6-004 | Atomic capability failure could occur after Store/cache/loader/target effects. | VERIFIED | Commit `86b56b1` requires a side-effect-free adapter check and authenticated same-filesystem exchange/no-replace probe before product effects. |
| P6-005 | Expected target identity and candidate evidence were conflated. | VERIFIED | Commit `86b56b1` separates immutable `expectedTarget` from independent `expectedCandidate` with distinct pre/post comparisons and tests. |
| P6-006 | 06-02 Task 1 claimed full model/store tests and vet but ran only focused selectors. | VERIFIED | Commit `bbd98dd` adds `go test ./core/model ./core/store` and `go vet ./core/model ./core/store`; exact selector parity and all five plan-structure checks passed. |
| P6-007 | `git update-ref --stdin` could write `commit` before backend/object publication durability. | VERIFIED | Commit `bbd98dd` stages start/mutation/prepare, waits for acknowledgements, completes backend publication plus fanout/root fsync, then writes commit. Focused ordering/failure selectors and an independent ledger audit passed. |
| P6-008 | Candidate, evidence, and journal file fsyncs omitted owning transaction-directory durability. | VERIFIED | Commit `bbd98dd` adds retained transaction-directory identity, file-then-directory barriers, per-transition directory fsync, all changed-directory syncs, and three exact failure selectors. Independent selector/evidence parity audit passed. |
| P6-009 | Unsupported native Darwin cleanup could be recorded as phase-complete PASS. | VERIFIED | Commit `bbd98dd` requires `TestDarwinQuarantineCleanupCapabilitySupported` plus every owned removal/sync row; unsupported-retained emits BLOCKED/nonzero and cannot complete Phase 6. Seven-selector parity and evidence wording passed independent audit. |
| P6-010 | A prepare mismatch was incorrectly required to receive an explicit `abort` after Git had already aborted/exited. | VERIFIED | Commit `bbd98dd` distinguishes Git's automatic prepare-rejection abort/exit from explicit abort after successful prepare; stale scans and independent re-review returned PASS. |
| P6-011 | Committing only EffectiveManaged entries makes `Store.Read(main) -> ir.Regenerate` unable to reproduce full source order. | VERIFIED | `06-CONTEXT.md` D-05/D-13 and `06-SPEC.md` Requirements 1/5 require the complete redacted source-ordered Profile in main, projection-only EffectiveManaged, an authority-safe SecretRef comparator, and direct activation/emitter inertness proof. `06-04-PLAN.md` Task 3 commits the complete Profile; `06-05-PLAN.md` Task 2 links Store-read/regenerate with activation tests. The focused independent plan-checker confirmed this contract is covered. |
| P6-012 | Replacing `.zshrc` with the unmanaged complement plus a loader-only EOF block can run dependent imperative code before removed managed declarations. | VERIFIED | `06-CONTEXT.md` D-06/D-07/D-12/D-14 and `06-SPEC.md` Requirements 3/5 preserve all ordinary outside-marker bytes, prepare one canonical loader/install candidate, prohibit post-commit target rewrite, and require built-binary pristine-vs-actual-installed `zsh -f` proof with an order-sensitive definition/use fixture. `06-03-PLAN.md` owns the one promotion; `06-05-PLAN.md` uses independently authored `expected-installed.zshrc`. The focused independent plan-checker confirmed this contract is covered. |
| P6-013 | The no-local-secret-copy claim contradicted the required atomic full-file exchange and a literal-bearing expected-installed fixture would create another copy itself. | VERIFIED | The contract names the unavoidable secret-bearing transaction peer as a bounded exception: it is confined below an authenticated current-EUID mode-0700 directory, never serialized or emitted, removed after durable finalize/restore, and retained only when recovery safety forbids deletion. Test fixtures contain a reviewed placeholder; a runtime-generated literal is written only to the temp original source, and expected bytes are expanded only in memory. The focused independent plan-checker confirmed both bounded-peer and placeholder-oracle contracts. |
| P6-014 | CONTEXT's execution gate omitted P6-013. | VERIFIED | CONTEXT's exact next-step gate names P6-011/P6-012/P6-013; the focused independent plan-checker confirmed the correction. |
| P6-015 | OQ-06-04 left marker-detector ownership contradictory instead of choosing the landed co-location. | VERIFIED | OQ-06-04 is CLOSED with the detector package-private beside landed `core/cli/install.go` marker ownership and no cross-package marker/zsh seam; the focused independent plan-checker confirmed coverage. |
| P6-016 | SPEC header retained the original 0.16 ambiguity score while its corrected assessment reported 0.08. | VERIFIED | SPEC metadata reports corrected 0.08 and labels 0.16 as the original assessment; the focused independent plan-checker confirmed coverage. |
| P6-017 | CONTEXT D-07's “No markers append” wording was grammatically ambiguous. | VERIFIED | D-07 explicitly says the no-marker case appends the canonical region with landed separator rules and leaves every other topology rule intact; the focused independent plan-checker confirmed coverage. |
| P6-018 | SPEC described expectedCandidate as an independent candidate rather than durable identity/digest/mode evidence. | VERIFIED | SPEC Requirement 3 binds expectedCandidate to independently durable identity/digest/mode evidence and confines source bytes to the exchange peer; the focused independent plan-checker confirmed coverage. |
| P6-019 | OQ-06-03 omitted required source/accounted statement fields and their invariant. | VERIFIED | OQ-06-03 locks managed/unmanaged/source/accounted fields plus the exact sum invariant; only DTO placement remains discretionary. The focused independent plan-checker confirmed coverage. |
| P6-020 | EVIDENCE presented not-yet-implemented CommitIngest/controller contracts as verified conclusions. | VERIFIED | The section is headed PLANNED/UNVERIFIED, states the production symbols/tests are absent pre-execution, and labels every listed contract PLANNED/UNVERIFIED. The focused independent plan-checker confirmed coverage. |
| P6-021 | 06-02 lacked one validated internal ref type and one typed mutation encoder shared by main ingest and private legacy branches. | VERIFIED | 06-02 requires opaque `validatedHeadRef`, a fixed-main public constructor, a private validated-branch constructor, one shared encoder, and exact legacy branch/no-raw-ref tests. Structure validation and the focused independent plan-checker passed. |
| P6-022 | 06-02 ambiguously promised typed outcomes from “Commit” despite legacy Store.Commit returning sentinel-compatible errors. | VERIFIED | 06-02 distinguishes typed `CommitIngest` outcomes on every post-Begin path from legacy `Store.Commit` `errors.Is`/report/committed-state mapping. Structure validation and the focused independent plan-checker passed. |
| P6-023 | 06-02 did not require no-deref updates/reads or rejection of symbolic/symlinked main refs. | VERIFIED | Every baseline/read/guard/update is fixed no-deref; symbolic and filesystem-symlink refs are rejected at initial and recovery seams with three exact tests in the focused selector. Structure validation and the focused independent plan-checker passed. |
| P6-024 | 06-03 did not compile or execute the unsupported-platform atomic adapter path. | VERIFIED | Task 2 cross-compiles FreeBSD amd64; Task 3 locally executes the exact other adapter with mutually exclusive `phase6_test_unsupported_atomic` tags and audits default selection. Structure validation and the focused independent plan-checker passed. |
| P6-025 | 06-03 claimed a late occupant is never relocated, although atomic exchange moves it to the exchange peer. | VERIFIED | Threat text promises no destruction/loss or relocation to a second recovery name, consistent with same-peer exchange/reverse/retain. Structure validation and the focused independent plan-checker passed. |
| P6-026 | 06-02 conflated start acknowledgement with acquisition of the prepared ref lock. | VERIFIED | Truths, behavior, action, threat model, and exact `TestUpdateRefSessionLocksOnlyAfterPrepareOK` require live-unlocked after start OK and locked only after prepare OK. Structure validation and the focused independent plan-checker passed. |
| P6-027 | 06-04/06-05 cited nonexistent `core/activate/activate.go`. | VERIFIED | All four plan references point to landed `core/activate/builder.go`, where `Build` skips `!EffectiveManaged()` and `!Representable()` entries. Both plan structures, the phase index, stale-reference scan, and `git diff --check` pass. |
| P6-028 | Post-implementation state metadata names completed Plan 06-03 while the checkpoint is after Plan 06-04, and its derived completed-plan counts disagree. | VERIFIED | `.planning/STATE.md` names completed Plan 06-04 in frontmatter/body and reports 39 completed plans consistently; exact metadata checks and the convergence CodeRabbit rerun passed. |
| P6-029 | The exact-SHA macOS verifier ignores untracked files under executable implementation scope. | VERIFIED | Commit `2703a42` blocks tracked-ignored and ordinary untracked files beneath `core` or the verifier path; its executable regression, full validation, and CodeRabbit rerun passed. |
| P6-030 | The SecretRef authority test indexes the reparsed source profile without independently checking its length. | VERIFIED | Commit `ce84089` checks the independently reparsed profile before indexing; the exact selector, full validation, and CodeRabbit rerun passed. |
| P6-031 | The ambiguity proxy assumes `/bin/bash` and Bash coprocess support without a version/capability preflight. | VERIFIED | Commit `ce84089` uses the built Go test binary as the real-Git proxy with no Bash-version skip; the real ambiguity selector, full validation, and CodeRabbit rerun passed. |
| P6-032 | A Unix `syscall.Stat_t` assertion lives in an untagged E2E test file and may break non-Unix compilation. | VERIFIED | Commit `ce84089` splits Linux/Darwin and other-platform authority helpers; platform selection, Linux/Darwin compile gates, full validation, and CodeRabbit rerun passed. |
| P6-033 | Ingest runtime-gated tests skip only when zsh is absent, not when the atomic adapter is unsupported. | VERIFIED | Commit `ce84089` gates both runtime-dependent sites on Linux/Darwin plus zsh; tagged unsupported coverage, full validation, and CodeRabbit rerun passed. |
| P6-034 | Journal transitions truncate and rewrite one stable inode, so interruption can leave no decodable pre- or post-transition record. | VERIFIED | Commits `0a0ab0e` and `f5ef58d` use alternating authenticated slots; torn prepared/exchanged and empty/all-invalid rows, full CLI, cross-builds, and CodeRabbit rerun passed. |
| P6-035 | Repeated post-promotion compensation blocks in `install.go` should be centralized. | VERIFIED | Commit `9a28de5` centralizes only identical non-recovery tails while leaving recovery-required branches explicit; compensation/full validation and CodeRabbit rerun passed. |
| P6-036 | `defaultCommitOutcome` contains a branch that assigns the same ref state already selected by default. | VERIFIED | Commit `aaea8e7` removes the dead branch; Store tests, full validation, and CodeRabbit rerun passed. |
| P6-037 | Darwin atomic rename passes Go-backed pathname pointers through a raw syscall without explicit `runtime.KeepAlive`. | VERIFIED | Commit `ce84089` keeps both pointers alive and adds an AST guard; Darwin cross-builds, full validation, and CodeRabbit rerun passed. |
| P6-038 | Secret-preparation test failures format whole secret-bearing structs and can disclose literals in test output. | VERIFIED | Commit `3472985` uses only counts/derived facts; focused secret tests, full validation, and CodeRabbit rerun passed. |
| P6-039 | Wrong-mode lock fixtures rely on requested creation modes that the process umask can alter. | VERIFIED | Commit `9a6fdf6` applies explicit chmod; unsafe-entry tests, full validation, and CodeRabbit rerun passed. |
| P6-040 | Store-root advisory lock acquisition is unconditionally blocking and does not observe caller cancellation/deadlines. | VERIFIED | Commit `a60094c` adds context-aware nonblocking flock retries; cancellation/deadline tests, full validation, and CodeRabbit rerun passed. |
| P6-041 | Transactional `git update-ref --stdin` is used without enforcing the Git version that introduced transaction commands. | VERIFIED | Git 2.27 release notes establish the floor; commit `33a24f7` fail-closes older/malformed versions before process start, and focused/full validation plus CodeRabbit rerun passed. |
| P6-042 | The macOS evidence verifier conflates the verified implementation SHA with a later evidence/documentation commit SHA. | VERIFIED | Commits `d2b26bb` and `28aaa86` separate the exact native-verified implementation SHA from the later derived evidence commit, require ancestry, and prove both verifier scripts plus `core` remain unchanged. Focused plan/script checks, full post-fix validation, and the 2026-08-08 CodeRabbit convergence pass verified this fix. |
| P6-043 | Task 2's executable verifier omits the full local pre-checkpoint package/vet/make/base/module/layer/scope gates required by its prose. | VERIFIED | Commits `d2b26bb` and `28aaa86` encode the touched-package, 12-selector, vet, make, module, four-target build/dependency/layering, scope-status, executable-mode, and verifier-regression gates at one candidate SHA. Focused structure/script checks, full post-fix validation, and the 2026-08-08 CodeRabbit convergence pass verified this fix. |
| P6-044 | The privacy contract does not explicitly require discovery and scanning of every retained quarantine object database. | VERIFIED | Commit `d2b26bb` makes exhaustive loose/packed discovery and scanning explicit; commit `c43aae6` dynamically authenticates and scans every retained quarantine without a fixed name/path/cardinality assumption. The ambiguity selector, full post-fix validation, and the CodeRabbit convergence pass verified this fix. |
| P6-045 | The target-root install lock blocks indefinitely and does not honor caller cancellation/deadlines. | VERIFIED | Commit `84ae744` uses context-aware nonblocking flock retries, preserves the full lock interval, and proves cancellation/deadline plus real subprocess contention and no-effect behavior. Focused selectors, full post-fix validation, and the CodeRabbit convergence pass verified this fix. |
| P6-046 | The native verifier's filesystem field may report file type rather than the mounted filesystem type. | VERIFIED | Commit `d2b26bb` resolves the repository mount and extracts native `FilesystemType` through `diskutil`/`plutil`, with a sanitized `unknown` fallback; commit `28aaa86` binds and runs its deterministic regression. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-047 | Published ingest transaction records receive baseline/quarantine field writes without `transactionMu`. | VERIFIED | Commit `471dde5` synchronizes baseline/quarantine publication and reads under `transactionMu`; its focused selector, race run, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-048 | The private legacy-ref begin path duplicates the public main begin state machine and can diverge from its recovery/accounting semantics. | VERIFIED | Commit `d96bbc5` routes public main and private validated refs through one begin state machine, with branch-specific registration only for non-main refs; focused selectors, full post-fix validation, and the CodeRabbit convergence pass verified this fix. |
| P6-049 | Commit cleanup can relabel candidate objects as removed even after they were hard-linked into the main object database. | VERIFIED | Commit `5894041` preserves retained/publication truth after hard-linking, including the lost-response path; its focused selector, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-050 | Linked loose object contents are not explicitly fsynced before ref publication. | VERIFIED | Commit `51d1875` authenticates and fsyncs each new linked object before directory barriers and ref publication; focused ordering/failure selectors, full post-fix validation, and the CodeRabbit convergence pass verified this fix. |
| P6-051 | Atomic rename rejects equal basenames even when the source and destination descriptors identify different directories. | VERIFIED | Commit `4d08e2d` rejects equal names only for the same descriptor and proves a cross-directory equal-name syscall reaches the adapter once. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-052 | The native-verifier regression does not independently prove an untracked root `go.work` is rejected. | VERIFIED | Commit `780f80f` adds a separate root-workspace fixture and proves an untracked `go.work` produces value-free BLOCKED output; the shell regression, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-053 | The native verifier omits tracked/untracked Go module and workspace inputs outside `core`. | VERIFIED | Commit `780f80f` validates the complete tracked and untracked repository worktree and binds module/workspace inputs into both local/evidence plan gates; the shell regression, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-054 | Plan 06-01 requires artifact cleanup evidence even for probe/read failure before a quarantine exists. | VERIFIED | Commit `479e604` distinguishes no-publication pre-quarantine terminalization from authenticated post-artifact cleanup. The corrected plan structure, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-055 | Plan 06-03 conflates staged-candidate regular-file validation with type-aware displaced-target validation after exchange. | VERIFIED | Commit `a18b38e` records no-follow typed displaced-entry identity and proves guarded live/recovery reversal of a symlinked late occupant without unsafe opening or relocation. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-056 | One programmatic SecretRef E2E selector lacks the Linux/Darwin runtime guard used by the fixture path. | VERIFIED | Commit `f57b341` applies the same Linux/Darwin guard before Store transaction calls; the exact selector, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-057 | The composition-root toolchain assertion inspects `main.go` instead of the test helper that builds the installed binary. | VERIFIED | Commit `7cb5980` isolates and audits the actual `buildInstalledBinary` helper, forbids automatic selection, and requires `GOTOOLCHAIN=local`; its exact selector, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-058 | Private install namespace/lock/transaction/evidence/journal creation relies on requested modes without post-create chmod. | VERIFIED | Commit `30fa20d` applies descriptor-relative chmod and exact type/mode checks to every private creation path; restrictive-umask coverage, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-059 | The rename ABI test hard-codes an exact Go 1.25 architecture set instead of auditing the supported minimum and available syscall definitions. | VERIFIED | Commit `a2eb2b8` retains the exact local-Go-1.25 requirement, requires Linux amd64/arm64, and validates every claimed trap against accepted local definitions without patch-specific dist-list equality. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-060 | The legacy recovery-committed report test uses a profile with no secret and therefore cannot prove the report is non-empty. | VERIFIED | Commit `29637da` commits a literal secret in the recovery row and proves the legacy report retains only its name/line. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-061 | Typed non-committed ingest outcomes may retain prepared Withheld entries. | VERIFIED | Commit `9f111a9` clears Withheld on every non-committed terminal outcome and covers backend, conflict, and recovery rows. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-062 | Store transaction namespace/lock creation relies on requested modes without post-create chmod. | VERIFIED | Commit `69d1a62` chmods and reauthenticates namespace/lock creation at 0700/0600 and proves restrictive-umask repair. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-063 | Install initialization records store root identity but do not use it to reject replaced or symlinked Store roots. | VERIFIED | Commit `303172e` captures with Lstat and reauthenticates the root identity before active initialization operations; replacement/symlink tests, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-064 | A post-publication Store lock failure can downgrade an already committed typed outcome to recovery-required status. | VERIFIED | Commit `580cf2f` preserves committed status/ref/object truth while setting recovery metadata; the post-publication lock-failure test, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-065 | Store-root lock retries remain unbounded when callers use a non-expiring context. | VERIFIED | Commits `69d1a62` and `4e89566` bound non-expiring acquisition to Git timeout plus one retry interval while preserving shorter caller cancellation/deadlines; focused tests, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-066 | A well-formed persisted SecretRef can be rejected as an unsafe shape solely because the active backend kind differs. | VERIFIED | Commit `bd645cd` preserves exactly one Kind call and nil-backend failure while classifying a structurally valid kind mismatch as backend-unavailable rather than malformed; zero data-method tests, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-067 | AbortIngest returns invalid-authority for terminal transactions that have no stored abort outcome. | VERIFIED | Commit `59cd604` synthesizes and persists terminal replay evidence from the begin/commit terminal outcome without cleanup effects; focused terminal rows, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-068 | Git `commit-tree -m` validation rejects safe commit messages beginning with a hyphen. | VERIFIED | Commit `69d1a62` validates message bytes separately from ref atoms and proves safe hyphen-prefixed messages commit. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-069 | Direct-ref validation does not reject a symlinked `packed-refs` entry. | VERIFIED | Commit `69d1a62` validates `packed-refs` no-follow metadata before Git starts; the symlink rejection row, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-070 | Reused loose objects that already exist are content-checked but not fsynced before ref publication. | VERIFIED | Commit `1371f05` fsyncs an authenticated reused loose object before ref publication and tests success/failure ordering. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-071 | Prepare acknowledgement handling maps every transport/process failure to a ref conflict. | VERIFIED | Commit `69d1a62` returns ref conflict only for Git’s prepare-lock diagnostic and preserves infrastructure error classification for killed/short sessions. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-072 | AbortIngest can report recovery-required in its outcome while returning no recovery-required error. | VERIFIED | Commit `90ca2dd` joins recovery-required with cancellation/deadline causes whenever cleanup is retained/uncertain; focused replay rows, full post-fix validation, and CodeRabbit convergence pass verified this fix. |
| P6-073 | Candidate alternate-object path construction does not protect colons in the Store path. | VERIFIED | Commit `69d1a62` C-quotes alternate object paths and proves a colon-bearing Store path remains readable through the authenticated alternate. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-074 | Descriptor-root Git commands use Linux `/proc/self/fd` paths on Darwin. | VERIFIED | Commit `69d1a62` uses a descriptor-identity checked Linux proc path and Darwin F_GETPATH resolver, with a supported-platform rename regression and Darwin cross-build coverage. Full post-fix validation and the CodeRabbit convergence pass verified this fix. |
| P6-075 | Native macOS 14.8 arm64 cannot hold a dangling-symlink descriptor, so a same-inode replacement needs no-follow link-target identity as well as Lstat identity. | VERIFIED | Failed runs `31273309084` and `31273469455`, plus probes `31273579494` and `31273957660`, established Darwin `O_NOFOLLOW` + `O_SYMLINK` reports ENOENT for the owned dangling link. New evidence that the initial descriptor-free fix could miss a same-inode replacement reopened the finding. Commit `1a83446` captures the no-follow target and checks it before and after both cleanup seams; 20 consecutive local replacement runs passed. Exact-SHA macOS run `31274061615` passed every runtime row; committed raw artifact hash `b35aa4d620e07e417bc038262a3dbdece745f383e84bd48165d1b2d445cf9b90` matches the downloaded artifact byte-for-byte; the final CodeRabbit convergence run raised no new finding. |
| P6-076 | The final verification report continued to state that `ROADMAP.md` rendered Phase 6 as 4/5 plans after the committed reconciliation already made it 5/5. | FIXED | The stale historical note has been removed from `06-VERIFICATION.md`; product evidence and its passed status remain unchanged. Pending the requested CodeRabbit rerun. |
| P6-077 | When `security` or `secret-tool` was selected, successful `Retrieve` calls always returned backend-unavailable, so secret-bearing ingest could not snapshot prior values and activation could not resolve a keychain SecretRef. | FIXED | Commit `3f74860` stores an ASCII versioned base64 transport and decodes only the command record terminator; it maps native missing-item results to `ErrSecretNotFound` and hides backend diagnostics. Deterministic fake-tool tests cover existing, empty, trailing-newline, multiline, missing, and failure rows. The built-binary composition proof covers both selected OS backends through ingest and `emit apply main` without retaining plaintext. Renewed exact-SHA macOS run `31278506938` passed `macos_keychain_round_trip` and `keychain_ingest_activation`; only the current CodeRabbit convergence rerun remains. |
| P6-078 | Production `install` and `ingest` passed non-expiring contexts to a target-root lock that retried indefinitely under live contention. | FIXED | Commit `548b2c7` bounds only a caller lacking a deadline to `validationTimeout` plus one retry, preserving explicit cancellation/deadlines and the complete held-lock interval. The real built binary is tested under external lock contention for both commands; it exits boundedly before any target, loader/cache, or Store effect. Renewed exact-SHA macOS run `31278506938` passed every runtime row; only the current CodeRabbit convergence rerun remains. |
| P6-079 | The actual native macOS built-binary install/ingest path rejected its `/var`-prefixed HOME before Store/keychain work because descriptor-only cache traversal encountered the system `/var -> /private/var` compatibility link. | FIXED | Failed exact-SHA run `31277119700` identified `open cached loader component "var" without following links: not a directory`. The repair recognizes only root-owned exact `/var` and `/tmp` aliases, rewrites them to fixed `/private/...` paths before descriptor traversal, keeps caller-selected links fail-closed, and adds direct Darwin plus built-binary composition coverage. Renewed exact-SHA macOS run `31278506938` passed `cache_traversal_aliases`; only the current CodeRabbit convergence rerun remains. |
| P6-080 | The store-root lock retry test panics on an unexpected fourth adapter call, hiding the violated contract. | FIXED | The response seam now fails with a bounded `t.Fatalf` before indexing; focused Store tests pending the requested CodeRabbit rerun. |
| P6-081 | Replacing a built-test fixture could leave a reviewed secret placeholder literal, allowing later secret assertions to pass without testing the runtime value. | FIXED | The helper now rejects multiple placeholders, substitutes the one reviewed placeholder with the fixture’s runtime secret, and has a direct regression test. Pending the requested CodeRabbit rerun. |
| P6-082 | A provenance-table label without a matching authored assertion passes because `strings.Contains(span, "")` is always true. | FIXED | The E2E source validator now rejects unknown labels before testing source spans. Pending the requested CodeRabbit rerun. |
| P6-083 | Phase 6 completion metadata omitted Plan 05 metrics and still named the Plan 04 checkpoint. | FIXED | `.planning/STATE.md` now records the 3-task Plan 05 row without invented duration/file metrics and names the completed Plan 05 convergence checkpoint. Pending the requested CodeRabbit rerun. |
| P6-084 | PROF-03’s checked requirement still said it would remain unchecked until Phase 6 completed. | FIXED | `.planning/REQUIREMENTS.md` now records the completed Phase 3/4/5/6 delivery sequence. Pending the requested CodeRabbit rerun. |
| P6-085 | `assertTask3ArtifactNames` assumes directory-entry order. | DISMISSED WITH EVIDENCE | The helper calls `os.ReadDir`, whose local Go 1.25 source documents and implements filename sorting (`$GOROOT/src/os/dir.go:109-124`); the expected basenames are lexical order. Adding an extra sort would only duplicate the API contract and does not improve coverage. |
| P6-086 | ROADMAP marked Phase 6 complete while the convergence ledger still contained unverified review findings. | FIXED | ROADMAP now reports 5/5 implemented plans with `Verification pending` until the final CodeRabbit rerun closes every actionable finding. |
| P6-087 | 06-02’s implemented integrity/privacy prohibitions remained labeled unresolved planning risks. | FIXED | The five prohibition records now carry exact P6-007/P6-023/P6-044/P6-071 execution evidence and are no longer flagged. Pending the requested CodeRabbit rerun. |
| P6-088 | 06-EVIDENCE was titled as current evidence while retaining a pre-execution source snapshot full of planned/unverified contracts. | FIXED | It is now explicitly labeled historical pre-execution evidence and points to final verification, ledger, and exact-SHA native evidence as post-execution authority. Pending the requested CodeRabbit rerun. |
| P6-089 | Snapshot-mode regression depends on ambient umask, so the intended 0640-to-0600 mode mutation can be absent. | FIXED | The fixture explicitly chmods to 0640 after creation before taking its snapshot. Its focused CLI regression, full local validation, and renewed exact-SHA macOS run `31278506938` passed; only the current CodeRabbit convergence rerun remains. |
| P6-090 | 06-02 omitted the invariant that non-committed typed outcomes clear Withheld before replay/rendering and legacy mapping. | FIXED | Must-haves, behavior, action, acceptance criteria, and legacy outcome mapping now require empty Withheld/report for every non-committed terminal outcome; `terminalizeIngestCommit` already enforces it at `core/store/store.go:380-398`. Pending the requested CodeRabbit rerun. |
| P6-091 | Four executed plans still labeled their implemented prohibitions `unresolved`, leaving Phase 6's completion record internally contradictory. | FIXED | 06-01, 06-03, 06-04, and 06-05 now cite their exact Store, guarded-promotion, no-source-execution, projection, and actual-built-binary tests. The refreshed final verifier independently passed every named behavior. Pending the requested CodeRabbit rerun. |
| P6-092 | Plan 06-03's self-check said 11 production/test files despite listing 12 in its created/modified inventory. | FIXED | The self-check now states 12, matching its six created and six modified production/test files. Pending the requested CodeRabbit rerun. |
| P6-093 | CONTEXT still described an executed Phase 6 as ready for planning and directed readers to execute all five plans. | FIXED | Lifecycle metadata now says executed with final CodeRabbit convergence pending and points to completion from the existing implementation, verifier, and exact-SHA native evidence. Pending the requested CodeRabbit rerun. |
| P6-094 | The final macOS evidence had passed, but four fixed-finding rows still said native evidence was pending. | FIXED | P6-077/P6-078/P6-079/P6-089 now cite successful exact-SHA run `31278506938` and identify CodeRabbit as their only remaining gate. Pending the requested CodeRabbit rerun. |
| P6-095 | The verification report abbreviated exact test selectors as `…`, which would not execute the claimed matrices. | FIXED | The report now records the complete Plan 06-05 selector alternations and fresh verbose output enumerating every selected test. Pending the requested CodeRabbit rerun. |

## Current convergence gate

P6-001 through P6-075 are `VERIFIED`; P6-076 through P6-084 and P6-086 through P6-095 are `FIXED`; P6-085 is `DISMISSED WITH EVIDENCE`. The final native macOS evidence has passed at final implementation SHA `cdb82ef21f8f3f10034a0c07442e1c52a8ef7802`; only the requested CodeRabbit rerun remains before the fixed findings can become `VERIFIED` and Phase 6 can return from `Verification pending` to complete.
