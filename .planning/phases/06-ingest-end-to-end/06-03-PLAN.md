---
phase: 06-ingest-end-to-end
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - core/cli/store.go
  - core/cli/install.go
  - core/cli/install_transaction.go
  - core/cli/atomic_rename.go
  - core/cli/atomic_rename_linux.go
  - core/cli/atomic_rename_darwin.go
  - core/cli/atomic_rename_other.go
  - core/cli/atomic_rename_test.go
  - core/cli/atomic_rename_supported_test.go
  - core/cli/atomic_rename_unsupported_test.go
  - core/cli/install_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-07/D-10: both ordinary install and ingest initialize the injected store before creating or opening the cache; loader promotion still precedes `.zshrc` promotion, including when explicit `ZSHPRO_HOME` makes the cache and store root identical."
    - "D-05/D-06: the exact pre-install `.zshrc` snapshot and explicit marker-topology table drive first adoption, installed-first-adoption, and re-ingest; only EffectiveManaged declarations are omitted from the imperative master, while comments, gaps, opaque lines, and imperative commands retain byte order."
    - "D-07/D-11/D-12: ordinary install keeps the landed topology behavior: no marker appends one region, one balanced region is replaced, multiple balanced regions collapse to one while preserving intervening ordinary bytes, and orphan END, unterminated START, nested, or interleaved topology fails before mutation. Ingest classifies post-END ordinary content separately and never treats a second balanced region as a generic append."
    - "For an existing target, one platform adapter atomically exchanges target and candidate in a single namespace syscall: Linux uses `renameat2(..., RENAME_EXCHANGE)`, Darwin uses descriptor-relative `renameatx_np(..., RENAME_SWAP)` from the `renamex_np` family, and every other platform or unsupported filesystem fails closed before target mutation. The target pathname always names the old or new file, never an intermediate absence."
    - "After exchange the displaced original remains at the candidate/recovery basename for validation and rollback. A durable same-directory journal authenticated to its transaction, parent descriptor identity, private descriptor metadata/digest/schema, and requested-link topology distinguishes unchanged, promoted-durable, restored-durable-stale, and recovery-required states; changed or unauthenticated evidence is never mutated or cleaned."
    - "Candidate bytes and independent candidate evidence are each fsynced and closed successfully before the prepared journal is written/fsynced and before any atomicRenameAt call. Candidate/evidence durability failure advances no journal, performs no namespace call, and leaves the target unchanged."
    - "Cooperating zsh-pro processes serialize each private random mode-0700 journal-namespace authenticate+effect interval with one real cross-process per-root exclusive lock held through postvalidation and directory fsync. This advisory lock does not exclude arbitrary external `.zshrc` writers; they remain in scope through exchange/no-replace, postvalidation, guarded reverse, or retained recovery."
    - "Atomic adapters use per-call syscall injection only: production passes syscall.Syscall6 directly, tests pass independent closures, supported attempts make exactly one raw call, and validation rejection makes zero. Common, Linux/Darwin-supported, and other-platform tests are build-tagged into separate files, and the Go 1.25 Linux ABI/fallback surface is proven from the authoritative local dist-list architecture set."
    - "An originally absent target follows a separate exclusive-create protocol and never calls exchange. Once no-replace succeeds, rollback to absence is recovery-required with target and journal retained because this phase has no separately audited identity-conditional inverse primitive."
    - "The final candidate is bound to the exact post-install snapshot produced by initial promotion: existence, requested-link topology, regular-file type, device/inode, digest, and permission bits. Target uid/gid, timestamps, ACLs, xattrs, and platform flags are explicitly outside snapshot equality; journal ownership authentication is a separate rule."
  artifacts:
    - path: "core/cli/install_transaction.go"
      provides: "Live install/ingest transaction, topology/layout snapshot with bounded equality, authenticated exchange journal, recovery, rollback ordering, and mutation-surface confinement"
      exports: ["preparedIngestInstall", "installSnapshot", "promotionOutcome"]
    - path: "core/cli/atomic_rename.go"
      provides: "Platform-neutral exchange/no-replace contract, basename validation, and unsupported/error classification"
      exports: ["atomicRenameAt", "atomicRenameMode", "ErrAtomicRenameUnsupported"]
    - path: "core/cli/atomic_rename_linux.go"
      provides: "Linux renameat2 RENAME_EXCHANGE/RENAME_NOREPLACE adapter"
    - path: "core/cli/atomic_rename_darwin.go"
      provides: "Darwin renameatx_np RENAME_SWAP/RENAME_EXCL adapter"
    - path: "core/cli/atomic_rename_other.go"
      provides: "Fail-closed implementation for non-Linux/non-Darwin targets"
    - path: "core/cli/atomic_rename_test.go"
      provides: "Platform-neutral validation, per-call injection isolation, and Go 1.25 Linux ABI/fallback AST audit"
    - path: "core/cli/atomic_rename_supported_test.go"
      provides: "Linux/Darwin direct exchange/no-replace and injected unsupported-error runtime tests"
    - path: "core/cli/atomic_rename_unsupported_test.go"
      provides: "Non-Linux/non-Darwin native unsupported and zero-raw-call tests"
    - path: "core/cli/install.go"
      provides: "Shared store-before-cache preparation and loader-before-target promotion used by install and ingest"
      contains: "runInstallWithStoreInitialization"
    - path: "core/cli/store.go"
      provides: "Narrow injected store initializer and ingest transaction interfaces using model-owned contracts"
      contains: "StoreInitialization"
    - path: "core/cli/install_test.go"
      provides: "Byte table, adoption modes, explicit-root overlap, substitution, recovery, and snapshot-binding proofs"
  key_links:
    - from: "runInstallWithStoreInitialization / prepareIngestInstallAt"
      to: "StoreInitializer -> runtime cache -> loader -> target"
      via: "one shared order; cache opening is forbidden until initialization succeeds"
      pattern: "StoreInitialization"
    - from: "pre-install layout"
      to: "adoption mode and imperative-master source"
      via: "exact marker scan plus durable ProfileObjectPresent from 06-01"
      pattern: "ProfileObjectPresent"
    - from: "prepared target snapshot"
      to: "one descriptor-relative atomic exchange"
      via: "authenticated parent, same-parent candidate, platform adapter, expected/displaced validation, journal state transitions"
      pattern: "promotionOutcome"
    - from: "initial promotion"
      to: "final candidate preparation/promotion"
      via: "the returned post-install snapshot is copied into the final prepared write and compared on the explicit bounded field set before any later effect"
      pattern: "postInstallSnapshot"
    - from: "authenticated parent/journal descriptors"
      to: "target mutation and artifact cleanup"
      via: "one audited descriptor-relative mutation helper holds the cooperating-process root lock while it freshly authenticates parent, journal, transaction, requested-link topology, and entry identities through each effect/postvalidation/sync interval"
      pattern: "mutateAuthenticatedTargetEntry"
  prohibitions:
    - statement: "MUST NOT destroy or lose a target occupant changed after validation. Atomic exchange may move that occupant to the authenticated recovery basename; postvalidation must guarded-reverse it or retain both artifacts with recovery-required evidence."
      category: safety
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT remove or rewrite content after the managed END marker during ingest."
      category: fidelity
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Turn the landed installer into a live, recoverable filesystem transaction that ingest can share without weakening install behavior.

Purpose: Fix the explicit store/cache overlap order, preserve exact source bytes and adoption provenance, and close target-path substitution without ever making an existing target pathname absent.
Output: A shared initialization/install order, an explicit topology scanner, Linux/Darwin atomic-exchange adapters, a durable promotion/recovery protocol, and focused filesystem transaction tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@core/cli/store.go
@core/cli/install.go
@core/cli/install_test.go

<interfaces>
Contracts to extend without bypassing:
- `runInstallWithStoreInitialization` is the shipped install composition point. Its store initializer must complete before cache preparation; its promotion order remains loader then `.zshrc`.
- `preparedWrite` currently stages and promotes by path. Replace its mutation internals with the authenticated transaction protocol; do not create an ingest-only overwrite writer.
- `StoreInitialization` is the CLI-neutral initializer result. It maps the exact injected store's root/ownership/rollback operations without importing `core/store`.
- 06-01 provides model-owned `IngestBaseline` and authenticated Begin/Abort quarantine cleanup; 06-02 provides the serialized prepared-ref CommitIngest contract. `IngestBaseline.ProfileObjectPresent` is the durable adoption discriminator.
- Landed build-tagged syscall wrappers in `core/cli/cache_syscalls_{linux,darwin}.go` and `runtime_openat_{linux,darwin}.go` are the local no-module-dependency pattern. The executor must verify the exact Linux and Darwin call surfaces against the installed Go 1.25 source/platform headers and cross-compile tests before coding; no guessed syscall number, non-atomic rename sequence, or link-based exchange fallback is permitted.
</interfaces>
</context>

## Artifacts this plan produces

- New `core/cli/install_transaction.go`: exact source/layout records and a durable same-parent promotion state machine shared by initial/final ingest writes.
- New build-tagged `atomic_rename*.go` files: exchange and exclusive-create contracts implemented with Linux `renameat2`, Darwin's descriptor-relative `renamex_np` family call, and a fail-closed unsupported-platform adapter.
- Extended `core/cli/store.go`: only narrow initializer/begin/commit/abort methods expressed in `core/model` types.
- Updated `core/cli/install.go`: one store-before-cache, loader-before-target order for ordinary install and ingest.
- No package installation, module change, external service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share initialization order and preserve exact adoption bytes</name>
  <files>core/cli/store.go, core/cli/install.go, core/cli/install_transaction.go, core/cli/install_test.go</files>
  <read_first>
    - core/cli/install.go: `runInstallWithStoreInitialization`, target/cache preparation, loader/target promotion, rollback order, and `preparedWrite`
    - core/cli/store.go: the landed injected Store/initializer seams and `StoreInitialization`
    - core/cli/install_test.go: installed-block byte fixtures, stale-target tests, explicit-home tests, and per-instance seam conventions
    - core/store/install_transaction.go: initializer ownership and rollback eligibility exposed through the CLI seam
    - .planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md: exact Begin/Abort contracts and initializer-safety outcome
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-05 through D-07, D-10 through D-12, D-14 through D-16
  </read_first>
  <behavior>
    - Ordinary install and ingest both call the same injected initializer before any runtime cache path is created/opened; an explicit shared store/cache root succeeds and rolls back target, cache/loader, then initializer-owned state.
    - Unequal ancestor overlap between store and runtime roots fails before cache or target mutation; equality is the supported explicit-home case.
    - Exact-line scanning recognizes only complete START/END marker lines and records an explicit topology plus all ordinary/managed byte spans and newline style.
    - Target snapshot equality is intentionally bounded to existence, requested-link topology, regular-file type, device/inode, content digest, and permission bits. Target uid/gid, timestamps, ACLs, xattrs, and platform flags neither authorize nor reject a later promotion; private journal ownership authentication is evaluated independently.
    - Topology table: no marker -> ordinary install appends one region and ingest uses full-source first adoption; one balanced region -> ordinary install replaces it and ingest selects installed-first-adoption or re-ingest from durable profile presence; multiple nonnested balanced regions -> ordinary install performs its landed one-region repair while preserving surrounding/intervening ordinary bytes, and ingest excludes every managed region from append classification before rescanning the repaired single-region result; orphan END, unterminated START, nested, or interleaved markers -> malformed and fail before initializer/cache/target effects.
    - Rendering covers LF, CRLF, empty, no-final-newline, multiline, existing-block, comments/gaps, and post-END cases without normalizing source bytes.
    - Adoption mode is derived only from pre-install marker topology and the baseline's durable profile-object presence: first adoption, installed-first-adoption, or re-ingest.
    - Ordinary nonblank/noncomment bytes after the canonical END are excluded from ingest input and retained byte-for-byte with one stable warning. A second balanced managed region is duplicate topology to repair, never generic post-END append content; EffectiveManaged declarations are omitted from the imperative master, and every other eligible line keeps source order and original StartLine provenance.
  </behavior>
  <action>
    Extend the CLI-neutral store contracts per D-15/D-16: the initializer returns canonical root identity, invocation ownership, rollback/finalize callbacks, and the narrow transaction interface names 06-01 Begin/Abort plus the completed 06-02 Commit through model-owned types. The concrete adapter is wired by the downstream 06-04 controller. Do not add a concrete-store import to `core/cli` and do not construct another store inside a callback.

    Refactor the shared install path per D-07/D-10 so target inspection may occur first but the initializer completes before `prepareInstall`, cache creation/opening, loader staging, or target mutation. Canonicalize store/runtime roots once. Equality is allowed and uses rollback order: abort clean ingest quarantine when present, restore target, undo loader/cache state and modes, then invoke initializer rollback. Disjoint roots use the same LIFO order. Reject unequal ancestor overlap before mutation because ownership would be ambiguous. Preserve ordinary install's existing output and loader-before-`.zshrc` contract.

    Add `prepareIngestInstallAt` and exact layout/value records in `install_transaction.go`. Read the requested path or D-01 default without mutation; resolve a symlink target once and record only the snapshot contract: requested-link topology, resolved-target existence, regular-file type, device/inode, content digest, and permission bits. Preserve the original byte slice. Explicitly exclude target uid/gid, atime/mtime/ctime/birth time, ACLs, xattrs, and platform flags from equality; those fields may vary without making the bounded content/identity snapshot stale. Keep private journal owner authentication separate. Refactor the landed `replaceManagedBlock` scan into one shared exact-physical-line topology parser used by ordinary install and ingest. Do not add a second marker implementation or change the literal constants.

    Implement and test the complete topology table, preserving the landed ordinary-install behavior per D-07. No markers append the existing managed block with the landed separator rules. Exactly one balanced region is replaced. Two or more sequential balanced regions collapse to one while preserving every ordinary byte before, between, and after them exactly as `TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent` and `TestReplaceManagedBlockOnlyRecognizesExactPhysicalMarkerLines` already require. An orphan END, unterminated START, nested START, or interleaved sequence returns the existing refusal before initializer/cache/target mutation. Ingest consumes the same parsed topology: it excludes all complete managed-region spans, never labels the second balanced region or its tool-owned body as a generic append, lets ordinary install canonicalize duplicate regions, then rescans the actual one-region post-install snapshot before source routing. Only ordinary nonblank/noncomment content after that canonical END produces D-11's warning; distinguish malformed topology from valid post-END ordinary content in typed preparation results.

    Implement the full D-05/D-06/D-11/D-12/D-14 byte table. Preserve LF versus CRLF, empty input, missing final newline, multiline command extents, comments, gaps, and canonical post-END bytes. Use the exact pre-install topology plus `IngestBaseline.ProfileObjectPresent` to choose first adoption, installed-first-adoption, or re-ingest; never infer adoption from entry counts or an in-memory empty Profile. Feed only eligible ordinary source spans to later classification. Build the imperative master from ordered non-EffectiveManaged/imperative/opaque source extents and carry original StartLine; do not duplicate declarations already represented by effective managed state. Return a single stable append warning only for ordinary post-END content while preserving/excluding those bytes as locked.

    Add exact-name tests for order, bounded snapshot equality, and bytes. `TestInstallSnapshotMatchesBoundedFields` changes each included field one at a time and requires mismatch, then changes excluded timestamp/uid/gid metadata where the host permits and requires equality; unsupported ACL/xattr/flag manipulation is documented as outside the contract rather than silently asserted. The explicit `ZSHPRO_HOME` test must use the same canonical root for store and cache, prove initializer runs before cache creation, exercise a later failure, and assert target/cache/store compensation in LIFO order. Table assertions compare raw byte slices and StartLine values, not trimmed text. Run all landed ordinary-install marker tests unchanged and add ingest cases for every topology row, including a second balanced region that is repaired/excluded rather than warned as appended ordinary content.
  </action>
  <acceptance_criteria>
    - Explicit store/cache-root equality no longer causes the cache to pre-create a path that initialization then rejects.
    - Ordinary install retains loader-before-target promotion and its existing success/failure behavior.
    - All six topology classes have explicit outcomes, and existing balanced-duplicate repair remains green.
    - Every newline/layout fixture has deterministic byte-exact output and original line provenance.
    - Durable profile-object presence plus pre-install topology, never entry count, selects all three adoption modes.
    - Post-END bytes are warned once, excluded from ingestion, and remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestInstallInitializesStoreBeforeCache TestInstallExplicitSharedRootOrdersStoreBeforeCache TestInstallRejectsAncestorStoreCacheOverlap TestInstallSnapshotMatchesBoundedFields TestScanZshrcMarkerTopologyTable TestRenderImperativeMasterByteTable TestPrepareIngestAdoptionModes TestPrepareIngestAppendWarning TestPrepareIngestRepairsBalancedDuplicates TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent TestReplaceManagedBlockRejectsEveryMalformedExactMarkerOrdering TestInstallRefusesMalformedExactMarkerOrderingsWithoutWriting' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestInstallInitializesStoreBeforeCache|TestInstallExplicitSharedRootOrdersStoreBeforeCache|TestInstallRejectsAncestorStoreCacheOverlap|TestInstallSnapshotMatchesBoundedFields|TestScanZshrcMarkerTopologyTable|TestRenderImperativeMasterByteTable|TestPrepareIngestAdoptionModes|TestPrepareIngestAppendWarning|TestPrepareIngestRepairsBalancedDuplicates|TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent|TestReplaceManagedBlockRejectsEveryMalformedExactMarkerOrdering|TestInstallRefusesMalformedExactMarkerOrderingsWithoutWriting)$' -count=1</automated>
  </verify>
  <done>The installer and ingest share a safe store-before-cache order, and ingest owns a byte-exact, provenance-preserving source/layout snapshot with all locked adoption and post-END behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and directly test the Linux/Darwin atomic-rename adapters</name>
  <files>core/cli/atomic_rename.go, core/cli/atomic_rename_linux.go, core/cli/atomic_rename_darwin.go, core/cli/atomic_rename_other.go, core/cli/atomic_rename_test.go</files>
  <read_first>
    - core/cli/cache_syscalls_linux.go: local raw `syscall` wrapper and descriptor-relative basename pattern
    - core/cli/cache_syscalls_darwin.go: local Darwin raw-trap precedent
    - core/cli/runtime_openat_linux.go and core/cli/runtime_openat_darwin.go: build-tagged platform split and fail-closed descriptor access
    - .planning/phases/06-ingest-end-to-end/06-EVIDENCE.md: current-source and verified syscall audit
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-07 and D-14
  </read_first>
  <behavior>
    - `atomicRenameAt(dirFD, from, to, atomicRenameExchange)` accepts two safe sibling basenames and performs exactly one namespace syscall: Linux `renameat2` with `RENAME_EXCHANGE`, Darwin descriptor-relative `renameatx_np` from the `renamex_np` family with `RENAME_SWAP`.
    - `atomicRenameAt(dirFD, from, to, atomicRenameNoReplace)` is a distinct absent-target operation: Linux uses `RENAME_NOREPLACE`, Darwin uses `RENAME_EXCL`; an existing destination returns the platform existence error and preserves both names.
    - Empty names, `.`, `..`, separators, embedded NULs, identical names, invalid modes, unknown Linux architectures, non-Linux/non-Darwin builds, and kernel/filesystem unsupported errors fail closed without either name changing.
    - There is no ordinary rename, hard-link sequence, delete-then-rename sequence, or retry under weaker flags. `ENOSYS`, `EINVAL` for an unsupported mode/filesystem, `ENOTSUP`/`EOPNOTSUPP`, and `EXDEV` become typed unsupported results for the caller.
    - `atomicRenameAtWithSyscall(call syscall6Fn,...)` receives its raw-call dependency per invocation. Production `atomicRenameAt` passes `syscall.Syscall6` directly; tests pass closures with independent counters. A validated supported attempt makes exactly one raw call, validation rejection makes zero, and no mutable package-level function variable exists.
    - `TestAtomicRenameLinuxGo125ABIAndFallbackSurface` derives the complete Linux GOARCH set from local Go 1.25 `go tool dist list`, parses the local Go syscall sources plus production AST, and proves the full trap map, flags 1/2, Syscall6 ABI argument order, one raw call, and absence of rename/link/unlink/remove fallbacks, retries, loops, or alternate flags.
  </behavior>
  <action>
    Before writing the adapters, require `GOTOOLCHAIN=local go env GOVERSION` to match Go 1.25.x, then verify the exact call surfaces from that accepted local toolchain and platform definitions. Inspect `GOTOOLCHAIN=local go env GOROOT` generated/vendored Unix syscall sources for Linux `renameat2` trap numbers by every supported `GOARCH`, Darwin `SYS_RENAMEATX_NP`, and the four flag values; cross-check against the installed Linux UAPI headers when present. Record the local toolchain mode, exact GOVERSION, and syscall audit in the task commit/summary. Do not permit an automatic toolchain download, import `golang.org/x/sys`, change `go.mod`, guess an unverified trap number, or substitute a weaker algorithm if a call surface is absent.

    Define the package-private `atomicRenameMode` values `atomicRenameExchange` and `atomicRenameNoReplace`, `ErrAtomicRenameUnsupported`, strict sibling-basename validation, and `syscall6Fn` matching the exact `syscall.Syscall6` ABI. `atomicRenameAtWithSyscall(call syscall6Fn,dirFD,from,to,mode)` owns validation plus one attempted platform call; production `atomicRenameAt` passes `syscall.Syscall6` directly at each call site, while tests supply a closure per invocation. Do not add a mutable package-level function variable. Keep error classification separate from transaction policy: invalid input is a validation error with zero raw calls; unavailable OS/architecture/kernel/filesystem capability wraps the sentinel; `EEXIST` from no-replace remains a stale-target result. The interface receives the already authenticated retained parent descriptor and never reopens an absolute parent path.

    Use the same existing platform file split to keep target-root locking cross-platform honest without an untagged Unix-only call: `atomic_rename_linux.go` and `atomic_rename_darwin.go` also provide the package-private descriptor-held `acquireTargetRootTransactionLock` primitive using the accepted local Go 1.25 blocking exclusive `syscall.Flock` surface with EINTR retry, while `atomic_rename_other.go` returns `ErrAtomicRenameUnsupported` before private journal or target mutation. The generic stable namespace/no-follow lock-entry authentication remains in `install_transaction.go`; only the OS lock operation is platform-owned. This lock adapter is not a mutable injection global and is never cited as protection against arbitrary external target writers.

    In the Linux adapter, make the supplied call exactly once for `renameat2` with flags 2 for exchange and 1 for no-replace and the audited ABI order: trap, old-directory FD, old-name pointer, new-directory FD, new-name pointer, flags, zero. Resolve the trap from an exact `runtime.GOARCH` table copied only after the audit; its key set must exactly equal the Linux architectures emitted by accepted local Go 1.25 `go tool dist list`, and unknown values return `ErrAtomicRenameUnsupported` with zero calls. In the Darwin adapter, make the supplied call exactly once for descriptor-relative trap 488 only after the audit confirms `renameatx_np`, using flag 2 for swap and 4 for exclusive creation in the audited argument order. Follow the repository's existing Darwin `syscall.Syscall6`/build-tag pattern. The other-platform adapter always returns the typed unsupported error without a raw call.

    Keep `atomic_rename_test.go` platform-neutral. Add exact `TestAtomicRenameAtRejectsUnsafeBasenames`, `TestAtomicRenamePerCallInjectionIsParallelSafe`, and `TestAtomicRenameLinuxGo125ABIAndFallbackSurface`. The parallel test runs independent closures/counters concurrently, proves exactly one call for every supported attempted operation and zero for every validation rejection, and detects any shared mutable seam. The ABI test treats local Go 1.25 `go tool dist list` Linux targets as authoritative, parses the matching Go syscall constants/generated sources and all `atomic_rename*.go` production ASTs, compares the complete trap map and flags, verifies Syscall6 argument order plus one raw call, and rejects ordinary rename/link/unlink/remove, retry/fallback loops, or alternate flags across the implementation. Task 3 owns the build-tagged runtime test split. Retain CGO-disabled Linux/Darwin amd64/arm64 cross-builds.
  </action>
  <acceptance_criteria>
    - Linux and Darwin adapters compile without a module change and use the audited exchange/exclusive flags.
    - Exchange preserves both file identities and no-replace preserves a concurrent destination.
    - Unsupported platforms, architectures, kernels, and filesystems return a typed fail-closed result with no weaker retry.
    - Unsafe basenames never reach the syscall.
    - Production raw-call dependencies are per invocation; parallel test counters are isolated, a supported attempt calls once, and validation rejects call zero times.
    - The complete local-Go-1.25 Linux architecture/trap/flags/ABI/fallback surface passes one exact AST/source test.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestAtomicRenameAtRejectsUnsafeBasenames TestAtomicRenamePerCallInjectionIsParallelSafe TestAtomicRenameLinuxGo125ABIAndFallbackSurface' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^TestAtomicRename') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestAtomicRenameAtRejectsUnsafeBasenames|TestAtomicRenamePerCallInjectionIsParallelSafe|TestAtomicRenameLinuxGo125ABIAndFallbackSurface)$' -count=1 &amp;&amp; build_dir=$(mktemp -d) &amp;&amp; trap 'rm -rf "$build_dir"' EXIT &amp;&amp; CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-linux-amd64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-linux-arm64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-darwin-amd64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-darwin-arm64.test" ./core/cli</automated>
  </verify>
  <done>The audited adapters have per-call raw-syscall injection, complete Go 1.25 Linux ABI/fallback proof, exact validation call counts, and no weaker or dependency-backed path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate atomic exchange, crash recovery, and exact snapshot binding</name>
  <files>core/cli/install.go, core/cli/install_transaction.go, core/cli/install_test.go, core/cli/atomic_rename_supported_test.go, core/cli/atomic_rename_unsupported_test.go</files>
  <read_first>
    - core/cli/atomic_rename.go after Task 2: audited exchange/no-replace contract and typed unsupported result
    - core/cli/install.go after Task 1: `preparedWrite`, cache/loader/target ownership, and rollback hooks
    - core/cli/install_transaction.go after Task 1: topology, source snapshot, requested symlink topology, resolved target identity, and adoption record
    - core/cli/install_test.go: existing target/symlink/mode/rollback and ordinary installer regression cases
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-07, D-10, D-11, and D-12
  </read_first>
  <behavior>
    - Preparation retains the authenticated target-parent descriptor; fsyncs and closes both candidate and independent candidate evidence successfully; only then writes/fsyncs the prepared journal containing parent device/inode, journal descriptor identity/mode/uid/gid/digest/schema/transaction ID, requested-link topology, expected target/candidate bounded identities, basenames, and original existence; only after all of that may any namespace syscall run.
    - Existing-target promotion revalidates requested symlink topology and the target snapshot, then performs one atomic exchange. The target pathname always names either the complete old file or complete new file; after the syscall the candidate/recovery basename holds the displaced occupant for postvalidation and rollback.
    - If a substitution wins between prevalidation and exchange, postvalidation detects the unexpected displaced identity. Exchange may move that late occupant to the authenticated recovery basename but must not destroy or lose it. The transaction reverses only while target still matches candidate evidence and recovery still matches the displaced identity; otherwise it retains both artifacts and returns recovery required.
    - Absent-target promotion never calls exchange. It uses atomic no-replace; a late target is preserved. After success the installed target and authenticated journal remain recovery evidence on any rollback request because deletion to recreate absence has no audited identity-conditional inverse in this phase.
    - A successful namespace syscall followed by parent fsync failure is state-uncertain. Generic pathname rollback is disarmed; journal and both available artifacts remain for authenticated next-run recovery.
    - Initial promotion returns one exact post-install snapshot. Final staging embeds that snapshot, controller pre-commit validation compares it, and final promotion rejects mismatch without adopting a later snapshot.
    - On Linux/Darwin, cooperating zsh-pro instances sharing the canonical target root hold one descriptor-backed cross-process exclusive advisory lock from before private journal-namespace authentication through target/recovery mutation, postvalidation, journal transition, and directory fsync. External/non-cooperating target writers remain in scope and are handled only by the atomic/postvalidation protocol.
    - `atomic_rename_test.go` always supplies common validation/ABI tests; `atomic_rename_supported_test.go` has `linux || darwin` build tags and direct exchange/no-replace/injected-unsupported tests; `atomic_rename_unsupported_test.go` has `!linux &amp;&amp; !darwin` tags and native unsupported/zero-call tests. Exact-name verification selects the platform list from local GOOS and always runs the common list.
  </behavior>
  <action>
    Route landed `preparedWrite` and ingest writes through one `install_transaction.go` protocol per D-07. Retain both an `os.Root` and raw directory descriptor for the resolved target parent through finalize/recovery; create a stable deterministic current-EUID mode-0700 transaction namespace for that canonical target root plus random per-transaction child basenames. Its fixed regular current-EUID mode-0600 lock entry is opened no-follow/descriptor-relative and held with a real Linux/Darwin exclusive advisory OS lock; the stable namespace/lock entry is not removed with a transaction. Candidate, independent evidence, journal, and recovery entries must be regular, private, same-device objects authenticated relative to retained handles. Persist parent device/inode and bind the journal to its opened descriptor identity, mode 0600, expected uid/gid, content digest, strict schema version, random transaction ID, requested-link topology, and every authenticated basename.

    All cooperating zsh-pro instances addressing that canonical target root acquire the lock before authenticating private journal state and hold its descriptor through namespace mutation, postvalidation, journal advancement, and directory fsync; authentication evidence never survives release/reacquisition. Open/acquire/authentication failure causes zero mutation and recovery-required retention. If the lock descriptor is deliberately closed/discarded, invalidate all prior evidence and perform no subsequent mutation; do not claim a portable oracle for arbitrary mid-interval advisory-lock loss. Advisory locking does not mitigate a non-cooperating or external `.zshrc` writer: reauthenticate all recorded fields immediately before target mutation and use exchange/no-replace plus displaced-identity postvalidation. Changed parent, journal descriptor/content, transaction/schema, or requested-link topology means zero mutation/cleanup and recovery required. Fail before cache/target/store mutation if the platform adapter is unsupported, the filesystem rejects the audited atomic mode, artifacts are ambiguous, or an older journal cannot be authenticated.

    Fsync and close the prepared candidate and its independent evidence successfully before writing the strict prepared journal; then write, fsync, and close the journal before mutation. Candidate/evidence fsync or close failure must produce zero journal advancement, zero `atomicRenameAt`/namespace calls, an unchanged target, and retained classified evidence. For an existing regular target, revalidate requested-link topology and expected device/inode/digest/mode immediately before `atomicRenameAt(...,atomicRenameExchange)`. After the single syscall, never create an ENOENT interval: target contains candidate and the candidate basename contains the complete displaced occupant. Validate target against independent candidate evidence and displaced against the expected snapshot. A late substitution is preserved at the recovery basename, not destroyed or lost. Reverse by another atomic exchange only after both names still match their post-swap identities; fsync the parent before reporting restored-durable-stale. If either changed, the reverse call fails, or durability is uncertain, retain journal/artifacts and return recovery required. Generic `rollbackPromoted` must switch on the typed promotion outcome and must not destroy/lose the retained occupant after stale recovery or uncertainty.

    Implement the absent-target branch separately. Journal original absence, then call no-replace; `EEXIST` is unchanged stale state with both target and candidate preserved. After success, target contains the complete candidate and candidate basename is absent. This phase has no separately audited atomic identity-conditional inverse for restoring absence, so any later rollback request returns recovery required with the authenticated target and journal retained; it must not unlink, move, or weaken that classification. Exchange remains forbidden when one side is absent.

    Define durability/crash seams at candidate-or-evidence fsync/close before journal creation, before the namespace syscall, immediately after syscall return but before journal advancement, after journal advancement but before parent fsync, and after parent fsync. `TestPromoteGuardedCandidateFsyncFailureBeforeJournalOrNamespace` is the exact pre-journal row and asserts zero journal advancement, zero namespace calls, and unchanged target. Integrate it into the crash table alongside the eight top-level existing/absent rows. Next-run recovery first acquires the root lock, freshly reauthenticates parent descriptor, journal descriptor/metadata/digest/schema/transaction, requested-link topology, and basenames, then derives state from both recorded identities: existing-target pre-swap is target=old/recovery=new; post-swap is target=new/recovery=old; absent pre-create is target absent/recovery=new; absent post-create is target=new/recovery absent. Existing-target completion/reversal is allowed only when one authenticated row matches exactly. An absent post-create row remains recovery required unless a separately audited identity-conditional inverse is added. Mixed/missing/extra/substituted state and any fsync ambiguity are recovery required with no mutation. Cleanup occurs only under the same held lock after durable success/restoration, fresh reauthentication, and parent sync.

    Make initial promotion return the exact `postInstallSnapshot`. Final candidate preparation stores that value; immediate pre-commit validation and final promotion both compare only existence, requested-link topology, regular-file type, device/inode, digest, and permission bits. Delete any API that resamples a new expectation. Target uid/gid, timestamps, ACLs, xattrs, and flags do not participate; journal owner authentication remains separate. A stale pre-commit result invokes store Abort and LIFO cache/initializer compensation only after restored-durable filesystem evidence; post-commit stale leaves the committed profile authoritative and reports target not updated.

    Confine namespace effects in `install.go` and `install_transaction.go` to one named authenticated descriptor-relative helper. Add `TestInstallTransactionMutationSurfaceAudit`, an AST-based static audit that rejects direct rename, remove, recursive remove, link, root mutation, or raw atomic-adapter invocation anywhere in those two files except that helper. The helper may act only while the real per-root lock descriptor is held, after candidate/evidence/journal durability and fresh authentication; it keeps the lock through postvalidation and parent fsync. Add helper-process tests proving same-root cooperating invocations block until release, different roots proceed, holder exit releases, and symlink/nonregular/wrong-mode lock entries fail before mutation. Keep the per-instance failure controller already required by repository patterns; do not add mutable package globals.

    Add deterministic tests for candidate/evidence fsync failure, direct substitution reversal, changed-name refusal to reverse, late absent-target creation, each of the four exact crash seams for existing and absent targets, parent-fsync uncertainty, changed parent/journal/requested-link authentication, unsupported-before-effects, in-place target mutation, root-lock serialization/rejection, unauthenticated-cleanup refusal, absent-post-create rollback refusal, initial stale full compensation, and final-snapshot mismatch. `TestPromotionJournalRejectsChangedJournalIdentityOrDigest` is a table over descriptor identity, mode, uid, gid, digest, schema version, and transaction ID, while sibling parent/link/lock tests change their fields independently; every rejected row asserts zero namespace and cleanup calls. Initial-stale compensation must prove exact external target preservation, exact loader/cache restoration, fresh-store removal or preexisting-store preservation as appropriate, no parse/Commit/final-promotion call, and no unauthenticated artifact cleanup.

    Create `atomic_rename_supported_test.go` with `//go:build linux || darwin`; move the direct exchange, absent no-replace, late-target, and per-call injected unsupported-error runtime tests there. Create `atomic_rename_unsupported_test.go` with `//go:build !linux &amp;&amp; !darwin`; prove the native adapter returns `ErrAtomicRenameUnsupported` and makes zero raw calls. Do not duplicate the common validation/parallel/ABI tests in either file. Run all landed ordinary-install tests unchanged.
  </action>
  <acceptance_criteria>
    - Existing-target promotion never makes the target pathname absent and leaves the displaced original or substituted occupant at an authenticated recovery basename.
    - Late substitution is atomically reversed only under matching identities; otherwise both sides are retained with recovery-required evidence.
    - Absent targets use exclusive creation, preserve a late-created destination, and retain target/journal with recovery-required evidence after a successful create cannot be safely inverted.
    - Every crash/fsync row has a deterministic recover-or-refuse outcome; uncertainty never becomes success or exact rollback.
    - Candidate and independent evidence are durable and closed before journal durability; their failure row advances no journal, performs no namespace operation, and preserves the target.
    - Cooperating same-root processes serialize the full private journal authenticate/effect interval, while external target replacement remains in-scope and is preserved through reverse-or-retain.
    - Local-GOOS exact-name preflight always includes common tests and exactly one supported/unsupported tagged platform list.
    - Final validation/promotion use only the original post-install snapshot.
    - Existing ordinary-install tests plus the focused transaction suite pass.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; common_tests='TestAtomicRenameAtRejectsUnsafeBasenames TestAtomicRenamePerCallInjectionIsParallelSafe TestAtomicRenameLinuxGo125ABIAndFallbackSurface'; local_goos=$(GOTOOLCHAIN=local go env GOOS); case "$local_goos" in linux|darwin) platform_tests='TestAtomicRenameAtExchangePreservesBothInodes TestAtomicRenameAtNoReplaceCreatesAbsentTarget TestAtomicRenameAtNoReplacePreservesLateTarget TestAtomicRenameAtUnsupportedLeavesBothNames' ;; *) platform_tests='TestAtomicRenameAtNativeUnsupportedZeroCalls' ;; esac; install_tests='TestPromoteGuardedCandidateFsyncFailureBeforeJournalOrNamespace TestPromoteGuardedDetectsAndReversesSubstitution TestPromoteGuardedRefusesUnsafeReverseExchange TestPromoteGuardedNoReplacePreservesLateTarget TestPromoteGuardedFsyncFailureRequiresRecovery TestRecoverGuardedExistingBeforeNamespaceSyscall TestRecoverGuardedExistingAfterExchangeBeforeJournalAdvance TestRecoverGuardedExistingAfterJournalAdvanceBeforeParentSync TestRecoverGuardedExistingAfterParentSync TestRecoverGuardedAbsentBeforeNamespaceSyscall TestRecoverGuardedAbsentAfterNoReplaceBeforeJournalAdvance TestRecoverGuardedAbsentAfterJournalAdvanceBeforeParentSync TestRecoverGuardedAbsentAfterParentSync TestPromoteGuardedDetectsInPlaceMutation TestPromoteGuardedUnsupportedBeforeEffects TestTargetTransactionCrossProcessRootLock TestTargetTransactionRejectsUnsafeRootLockEntry TestPromotionCleanupRejectsUnauthenticatedArtifact TestPromotionJournalRejectsChangedParentIdentity TestPromotionJournalRejectsChangedJournalIdentityOrDigest TestPromotionJournalRejectsChangedRequestedLinkTopology TestRollbackGuardedAbsentAfterNoReplaceRequiresRecovery TestRollbackGuardedRefusesChangedTarget TestInstallTransactionMutationSurfaceAudit TestIngestInitialStaleCompensation TestIngestFinalCandidateBoundToPostInstallSnapshot'; tests="$common_tests $platform_tests $install_tests"; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s for GOOS=%s, found %s\n' "$test_name" "$local_goos" "$count" &gt;&amp;2; exit 1; }; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cli -run "^(${selector})$" -count=1; GOTOOLCHAIN=local go test ./core/cli -count=1; GOTOOLCHAIN=local go vet ./core/cli</automated>
  </verify>
  <done>Filesystem promotion uses audited atomic exchange/exclusive creation, retains displaced evidence, recovers deterministic crash states, and binds final bytes to the exact initial post-install snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Requested `.zshrc` path -> resolved target descriptor | Symlinks and parent pathnames can change after validation. |
| Validated target -> atomic exchange/exclusive-create syscall | A concurrent writer can replace, recreate, or mutate the target around prevalidation. |
| Process memory -> filesystem durability | Successful syscalls are not durable until file and directory metadata are synced. |
| Recovery journal -> next invocation | Stale or attacker-created artifacts must not become arbitrary path operations; journal ownership authentication is separate from bounded target snapshot equality. |
| Cooperating zsh-pro process -> private journal namespace | A real per-root advisory lock serializes fresh authentication through effect/postvalidation/sync. Non-cooperating same-UID/privileged mutation of private internals is outside that guarantee; arbitrary external target writers remain in scope. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V5.1 validation maps to marker/journal/basename checks; V12.3 file execution/storage maps to authenticated roots, regular-file checks, audited exchange/no-replace syscalls, and fail-closed recovery; V7.4 error handling maps to stable classified outcomes. Execution is blocked while any critical/high mitigate-disposition threat lacks its named automated test; the accepted private-journal same-UID/privileged boundary is explicit, while external target-writer preservation remains fully mitigated and tested.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-03-01 | Tampering | target parent/path substitution | high | mitigate | Retain the parent descriptor, atomically exchange existing target/candidate, validate both post-swap identities, and reverse only under matching evidence; the late occupant may move to recovery but is never destroyed/lost and otherwise both artifacts are retained. |
| T-06-03-02 | Tampering | candidate/target in-place mutation | high | mitigate | Keep independent candidate evidence and compare the bounded target fields: existence, requested-link topology, regular type, device/inode, digest, and permission bits. |
| T-06-03-03 | Repudiation | candidate/journal/exchange/no-replace/fsync ambiguity | high | mitigate | Fsync and close candidate plus independent evidence before writing/fsyncing the journal, then sync the parent after one syscall; every uncertainty is recovery required with artifacts retained and the pre-journal failure test proves zero journal/namespace effect. |
| T-06-03-04 | Elevation of Privilege | recovery journal/artifacts | high | mitigate | Parent dev/inode and journal descriptor identity/mode/uid/gid/digest/schema/transaction/link-topology authentication precede every mutation or cleanup. |
| T-06-03-05 | Denial of Service | interrupted target transition | medium | mitigate | Durable pre-transition evidence plus target/recovery identity matrix recovers a complete pre/post state or fails closed. |
| T-06-03-06 | Information Disclosure | post-END source bytes | medium | mitigate | Preserve bytes in place but exclude them from classification/commit and expose only a fixed append warning. |
| T-06-03-07 | Tampering | target mutation surface | high | mitigate | One authenticated descriptor helper owns namespace effects; an AST audit rejects direct rename/remove/link/root mutation elsewhere in install.go and install_transaction.go. |
| T-06-03-08 | Repudiation | absent-target rollback | high | mitigate | After successful no-replace, retain authenticated target/journal and report recovery required because no audited identity-conditional inverse is available. |
| T-06-03-09 | Tampering | cooperating concurrent journal mutation | high | mitigate | A descriptor-backed per-root exclusive OS lock spans each private journal authenticate/effect/postvalidate/sync interval; helper-process tests cover same/different roots, holder exit, and unsafe lock entries. |
| T-06-03-10 | Tampering | non-cooperating same-UID/privileged private-journal writer | high | accept | Advisory locking is a cooperating-process contract and makes no private-internal exclusion guarantee beyond OS account isolation; detected mismatches still retain recovery evidence, while external target occupants remain fully in scope. |
| T-06-03-SC | Tampering | Go toolchain/package execution | high | mitigate | Every verifier and syscall audit rejects non-local or non-Go-1.25.x execution; `GOTOOLCHAIN=local` forbids automatic downloads and no package-manager/module change is planned. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Idempotent managed-block install plus append detection | 06-03 Tasks 1-3 | COVERED | Installer composition, topology table, and exact source/promotion semantics. |
| REQ | PROF-03 | Secret-safe end-to-end ingest | 06-01/06-02 store, then 06-03 transaction carrier | COVERED | This plan preserves only metadata/source boundaries; store redaction remains authoritative. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| REVIEW | R-07 | Local Go 1.25.x only | 06-03 Tasks 1-3 | COVERED | Every independent verifier preflights local GOVERSION; syscall audit and all cross-build/test/vet commands run with `GOTOOLCHAIN=local`. |
| CONTEXT | D-05 | EffectiveManaged is the declarative source | 06-03 Task 1 | COVERED | Imperative master excludes only effective managed extents. |
| CONTEXT | D-06 | Imperative/opaque master, no declaration duplication | 06-03 Task 1 | COVERED | Byte/provenance table. |
| CONTEXT | D-07 | Reuse landed installer | 06-03 Tasks 1-3 | COVERED | Shared topology/order, bounded snapshot, one authenticated mutation helper, and exact existing/absent crash rows. |
| CONTEXT | D-10 | Initialize then use fixed main baseline | 06-03 Task 1, 06-01 | COVERED | Initializer precedes cache; Begin consumes main. |
| CONTEXT | D-11 | Warn for post-END appends without mutation/error | 06-03 Task 1 | COVERED | Exactly one warning; bytes excluded/preserved. |
| CONTEXT | D-12 | Exact marker-line scan | 06-03 Task 1 | COVERED | Full-line byte scanner. |
| CONTEXT | D-14 | Deterministic fixtures | 06-03 Tasks 1-3, 06-05 | COVERED | Exact topology/byte/syscall/eight-row crash and changed-evidence tests here; fixture provenance in 06-05. |
| CONTEXT | D-15 | Same injected store at composition root | 06-03 Task 1, 06-04 | COVERED | Neutral seam extended; exact identity wired next. |
| CONTEXT | D-16 | Extend landed store injection seam | 06-03 Task 1 | COVERED | No parallel abstraction or concrete-store import. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. This plan projects the locked fidelity and safety edges into frontmatter: an occupant changed after validation may be exchanged to the authenticated recovery basename but must never be destroyed or lost, and post-END content is never ingested/removed. Postvalidation must guarded-reverse or retain both artifacts with recovery-required evidence; the named tests resolve both edges before completion.
</spec_fallback>

<verification>
Run exact-name preflighted order/layout/promotion/recovery tests, the complete CLI suite, and vet. The static mutation-surface test must prove only the authenticated descriptor helper can change target/artifact names; verify no API resamples target expectations and no ingest path prepares cache before the injected initializer.
</verification>

<success_criteria>
- Shared-root initialization succeeds because store initialization precedes cache access.
- Byte/layout/adoption decisions match D-05 through D-07 and D-11/D-12 exactly.
- Atomic exchange preserves an existing target continuously and never destroys/loses a late occupant moved to recovery; exclusive creation preserves a late-created absent-target occupant.
- Candidate/evidence durability, locked journal/parent/link reauthentication, eight explicit recovery rows, the exact pre-journal fsync-failure row, and retained evidence make durability claims truthful; absent post-create rollback remains recovery required.
- Final promotion can use only the exact post-install snapshot returned by initial promotion, compared on the declared bounded fields rather than target ownership/timestamps/ACL/xattr/flags.
- Every independent verifier refuses non-local or non-Go-1.25.x execution; no automatic toolchain download can satisfy a Phase 6 gate.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md` when done.
</output>
