---
phase: 06-ingest-end-to-end
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - core/cli/ingest_e2e_test.go
  - core/cmd/zsh-pro/main_test.go
  - core/testdata/fixtures/ingest_real.zsh
  - core/testdata/fixtures/ingest_secrets.zsh
  - core/testdata/fixtures/ingest_installed_append.zsh
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "A built `zsh-pro ingest` binary uses the real composition root to create/update `main`, install one loader block, preserve imperative source, and produce the same bytes on an unchanged second run, per D-01, D-02, D-10, D-15, and D-16."
    - "A real-shaped committed baseline plus its imperative master reconstructs source-order behavior equivalent to the original; regeneration uses behavior-bearing structural equivalence with only regenerated `StartLine` excluded, while a separate pre-commit oracle proves exact `StartLine` provenance and order across interleaved/gapped entries, per D-05, D-06, D-13, and D-14."
    - "A hand-authored expectation table—not Provider.Parse/ir.Build—owns exact fixture Text bytes, StartLine, routing disposition, and ordinal; controller capture, store, and master outputs are compared to it while real Parse/Build/Regenerate and native-zsh checks remain separate production/behavior paths, per D-05, D-06, D-13, and D-14."
    - "A literal secret fixture proves absence from every reachable and unreachable Git object plus `.zshrc` and CLI streams after success and a post-object-hash/pre-ref abort; an unchanged second ingest preserves the reference/backend without access or duplicate report, and a dynamic secret expression remains verbatim and unreported, per D-08 and D-09."
    - "A pre-installed fixture with trailing installer content emits a warning, exits successfully, and retains those bytes and their order, per D-11 and D-12."
    - "The integrated repository passes formatting, vet, lint, and the complete test suite."
    - "Final seal checks compare module files to the immutable Phase 6 execution-base commit, inspect transitive package dependencies without suppressed failures, and parse the deterministic API detector's typed `detected:false` result while requiring `COVERAGE.md` absent."
  artifacts:
    - path: "core/cli/ingest_e2e_test.go"
      provides: "Concrete store/provider secret, append, reconstruction, and live-zsh oracle coverage"
      contains: "TestIngestEndToEnd"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Built-binary composition-root and complete-file idempotency proof"
      contains: "TestBuiltBinaryIngest"
    - path: "core/testdata/fixtures/ingest_real.zsh"
      provides: "Deterministic real-shaped mixed declarative/imperative/dynamic source"
    - path: "core/testdata/fixtures/ingest_secrets.zsh"
      provides: "Literal and already-dynamic secret inputs"
    - path: "core/testdata/fixtures/ingest_installed_append.zsh"
      provides: "Exact managed block followed by foreign installer append bytes"
  key_links:
    - from: "built zsh-pro binary"
      to: "main.go -> cli.NewWithStoreInitializer -> *store.Store -> CLI.IngestStore"
      via: "real composition-root ingest dispatch"
      pattern: "ingest"
    - from: "Store.Read(main)"
      to: "ir.Regenerate + imperative master entries"
      via: "a hand-authored provenance table independently checks exact Text/StartLine/routing/ordinal before separate Parse/Build/Regenerate and native-zsh paths"
      pattern: "Regenerate"
    - from: "ingest_secrets.zsh"
      to: "Git object database and CLI output"
      via: "`git cat-file --batch-all-objects --batch` scans reachable and unreachable objects after success and injected abort, plus literal absence in file/stdout/stderr"
      pattern: "Withheld"
    - from: ".planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE"
      to: "go.mod / go.sum final gate"
      via: "committed, staged, and unstaged comparisons use the immutable pre-execution object instead of moving HEAD"
      pattern: "06-EXECUTION-BASE"
  prohibitions:
    - statement: "MUST NOT let a green end-to-end test depend on a fake parser, fake Git commit, or a shell-state oracle derived from the implementation under test."
      category: transparency
      status: unresolved
      verification: null
      flagged: true
  flagged_assumptions:
    - id: PROF-03-idempotency-edge
      status: resolved
      verification: explicit
      resolution: "The built binary runs twice against the same HOME/store and full-file bytes, marker cardinality, Git profile content, and output accounting are compared."
    - id: PROF-03-concurrency-edge
      status: resolved
      verification: explicit
      resolution: "Plan 06-02 deterministically tests CAS conflict, stale target refusal, post-commit promotion failure, and rerun convergence; this plan retains those tests in the full repository gate."
---

<objective>
Prove Phase 6 through the real binary, real Git store, real zsh provider, and an independent behavior oracle.

Purpose: Prevent unit-level success from masking broken composition wiring, secret leakage, source-order drift, append clobber, or second-run changes.
Output: Production-shaped fixtures, concrete end-to-end tests, a built-binary proof, and the final repository quality gate.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@core/cli/ingest.go
@core/cmd/zsh-pro/main.go
@core/cmd/zsh-pro/main_test.go
@core/ir/roundtrip_test.go
@core/store/store_test.go

<interfaces>
Completed Phase 6 interfaces to exercise without substitution:
- `CLI.Run([]string{"ingest", path, "--json"}, stdout, stderr)` is the user/agent entry point.
- `*store.Store` satisfies `cli.IngestStore`; `Read(ctx, "main")` returns the committed managed profile.
- `ir.Regenerate(profile, zsh.Provider{})` emits the committed managed source view.
- `zsh.Provider{}.Parse` and `Introspect` provide the real parse and native-zsh identity oracle; tests skip only when `exec.LookPath("zsh")` fails.
- Existing main-test helpers `buildInstalledBinary`, `installedBinaryEnv`, `runInstalledBinary`, and `gitRef` exercise the compiled composition root.
</interfaces>
</context>

## Artifacts this phase produces

- New paths: `core/cli/ingest_e2e_test.go`, `core/testdata/fixtures/ingest_real.zsh`, `core/testdata/fixtures/ingest_secrets.zsh`, and `core/testdata/fixtures/ingest_installed_append.zsh`.
- Modified path: `core/cmd/zsh-pro/main_test.go` gains built-binary ingest cases using existing build/environment helpers.
- New production symbols, struct fields, CLI flags, or commands: none; this plan verifies the `ingest` and `--json` surface created in Plan 06-02.
- New test helpers: fixture loader; a hand-authored `expectedSourceEntry{Text, StartLine, Disposition, Ordinal}` table; temporary real-store constructor with a deterministic counting/failing test keychain; a delegating Commit-capture store; `semanticProfileEqual` that excludes only regenerated location metadata; source-order reconstruction; an all-object Git batch scanner; and native-zsh identity comparison.
- Existing Phase 6 artifact consumed: `.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE`, captured by 06-01 before source edits and never rewritten.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise the built binary and complete-file second-run contract</name>
  <files>core/cmd/zsh-pro/main_test.go, core/testdata/fixtures/ingest_real.zsh, core/testdata/fixtures/ingest_installed_append.zsh</files>
  <read_first>
    - core/cmd/zsh-pro/main.go: actual provider/store/keychain/initializer construction
    - core/cmd/zsh-pro/main_test.go: `buildInstalledBinary`, `installedBinaryEnv`, `runInstalledBinary`, Git helpers, and current install rollback tests
    - core/cli/ingest.go and core/cli/install.go after Plan 06-02
    - core/testdata/fixtures/installer_junk.zsh for fixture conventions
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-04, D-10 through D-12, D-15, and D-16
  </read_first>
  <behavior>
    - The compiled binary ingests a HOME-scoped real-shaped `.zshrc`, creates a usable bare store with `main:profile.json` and `main:profile.zsh`, installs one canonical loader block, and retains each imperative statement in the master region.
    - Human output accounts for every source statement and reports main; JSON mode emits one object with command ingest and matching counts.
    - Running the built command again leaves complete `.zshrc` bytes equal, keeps one marker pair, and yields the same managed/master accounting without duplicate profile entries.
    - A fixture with exact markers and post-END installer lines exits cleanly with warnings and preserves the suffix byte-for-byte.
  </behavior>
  <action>
    Add exactly four top-level built-binary tests—`TestBuiltBinaryIngestEndToEnd`, `TestBuiltBinaryIngestJSON`, `TestBuiltBinaryIngestIdempotent`, and `TestBuiltBinaryIngestAppendWarning`—using the existing compilation and isolated environment helpers. Copy `ingest_real.zsh` to the selected HOME `.zshrc`; include alias/export/PATH, `export GOPATH=$HOME/go`, function-plus-invocation, `eval` command substitution, and `compinit`, but keep literal secret assertions in Task 2 so platform keychain selection cannot make this composition proof environment-dependent. Run human ingest, inspect the real bare Git branch and rewritten file, then capture the entire file and run JSON ingest again. Assert complete byte equality, exactly one exact marker pair, parseable one-object JSON with no trailing value, equal counts, real `profile.json`/`profile.zsh`, and imperative text presence. Add the pre-installed append fixture case and compare the precise post-END suffix before and after while requiring warning output and success. Use actual binary behavior and Git commands; do not invoke `runIngest` directly. The verification preflights each exact name so a missing test cannot pass as `[no tests to run]`.
  </action>
  <acceptance_criteria>
    - The real composition root reaches the concrete store and provider through the new ingest seam.
    - Full file, marker cardinality, store tree, and output accounting are stable on the second run.
    - Post-END append bytes and ordering survive with a warning and clean exit.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestBuiltBinaryIngestEndToEnd TestBuiltBinaryIngestJSON TestBuiltBinaryIngestIdempotent TestBuiltBinaryIngestAppendWarning' && listed=$(GOTOOLCHAIN=auto go test ./core/cmd/zsh-pro -list '^Test') && for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && GOTOOLCHAIN=auto go test ./core/cmd/zsh-pro -run '^(TestBuiltBinaryIngestEndToEnd|TestBuiltBinaryIngestJSON|TestBuiltBinaryIngestIdempotent|TestBuiltBinaryIngestAppendWarning)$' -count=1 && GOTOOLCHAIN=auto go test ./core/cmd/zsh-pro -count=1</automated>
  </verify>
  <done>The public compiled on-ramp creates the installed baseline and is byte-identical on an unchanged second invocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove secret absence and source-order behavior equivalence with real components</name>
  <files>core/cli/ingest_e2e_test.go, core/testdata/fixtures/ingest_real.zsh, core/testdata/fixtures/ingest_secrets.zsh</files>
  <read_first>
    - core/ir/roundtrip_test.go: real `zsh -f` skip guard, identity capture, byte/source-order assertions, and independent comparison pattern
    - core/store/store_test.go and core/store/secret_test.go: temporary bare store and keychain fakes, blob absence assertions, dynamic secret behavior, and CAS tests
    - core/cli/ingest.go: production partition, report, and file output
    - core/model/profile.go: `StartLine`, `Text`, `EffectiveManaged`, and secret reference fields
    - core/shell/zsh/introspect.go: native-zsh behavior oracle contract
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-05, D-06, D-08, D-09, D-13, and D-14
  </read_first>
  <behavior>
    - A hand-authored table literal enumerates every fixture statement's exact `Text` bytes, one-based `StartLine`, managed/master disposition, and source ordinal; no expected field or partition is computed by Provider.Parse, ir.Build, controller accounting, or regenerated output.
    - Controller Commit capture, `Store.Read(main)`, and exact master bytes/order are compared directly to the hand-authored table; real Parse/Build/Commit/Read/Regenerate over `ingest_real.zsh` is exercised separately as the production path.
    - The exact bytes from `ir.Regenerate(Store.Read(main), zsh.Provider{})` are parsed and rebuilt through `Provider.Parse` plus `ir.Build`; `semanticProfileEqual` compares entry count/order and every behavior-bearing field while excluding only regenerated `StartLine`, because compact regeneration necessarily assigns consecutive output locations.
    - Before Commit, a delegating store captures the controller's fresh reconstructed profile and compares exact original `Text`/`StartLine`/routing/ordinal against the hand-authored table; the fixture has managed and imperative statements interleaved at non-contiguous lines with blank/comment and multi-line gaps.
    - Sorting committed managed entries and parsed padded-master entries by their preserved provenance reconstructs original source order; native-zsh introspection of original and reconstructed files yields equal declarative identity state, and imperative bytes compare directly.
    - A deterministic injected keychain receives the literal secret while `git cat-file --batch-all-objects --batch` proves the value absent from every reachable and unreachable object; `.zshrc`, stdout, and stderr are clean and output names only variable/line.
    - A separate isolated real-store abort injects failure after object/commit hashing but before ref update, then runs the same all-object scan and file/stream checks; any unreachable object is secret-free.
    - The already-dynamic secret expression survives in the committed source and is absent from the withheld report.
    - An unchanged second ingest of the literal-secret fixture preserves the same SecretRef and backend value, performs no backend access/mutation, emits no duplicate withheld item, and keeps generated/file output free of the literal.
  </behavior>
  <action>
    Create `ingest_e2e_test.go` in package `cli` so it can call the public CLI with Plan 06-02's real controller while wiring `zsh.Provider`, a real temp `*store.Store`, and a deterministic counting/failing KeychainDriver. Create exactly five top-level tests: `TestIngestEndToEndProvenanceOracle`, `TestIngestEndToEndSemanticRoundTrip`, `TestIngestEndToEndSecretObjectDatabase`, `TestIngestEndToEndSecretAbortObjectDatabase`, and `TestIngestEndToEndNoExecution`. Make `ingest_real.zsh` deliberately non-contiguous: interleave managed statements with imperative commands at separated line numbers, include blank/comment gaps and at least one multi-line entry between them.

    Hand-author an `expectedSourceEntry` table literal for every real-fixture statement with exact `Text` bytes (including owned comments/multiline content), one-based `StartLine`, `managed` or `master` disposition, and zero-based source ordinal. The table is the sole expected provenance/partition source: do not fill it through Provider.Parse, `ir.Build`, controller accounting, shared production line-count helpers, or regenerated output. Wrap the real store in a delegating `IngestStore` that captures the exact candidate passed to Commit and forwards every operation. Compare captured entries directly to the managed table rows' exact fields/order, compare `Store.Read(main)` to those rows' persisted behavior-bearing fields, and compare the scanned pre-BEGIN master to the master rows' exact Text/order plus table-declared layout gaps. Reconstruct full source order from controller/store/master outputs by table ordinal and assert exact Text, StartLine, routing disposition, and ordinal. Separately run real Provider.Parse plus `ir.Build` over the fixture and assert the production path's behavior, but never use those results to construct expected values.

    For D-13's structural-equivalence wording, define `semanticProfileEqual` over behavior-bearing structure: require the same entry count/order and compare `Text`, `Category`, `Kind`, `CmdName`, `Names`, `Value`, `Exported`, `Managed`, `Override`, `Dynamic`, structural-fidelity flags, declaration/option flags, `Secret`, `ValueMode`, `RuntimeValue`, `FunctionBody`, and `ListValue`; make a copy and zero only `StartLine` before comparison. Pass the committed non-secret managed profile to `ir.Regenerate`, parse those exact generated bytes with the real provider, rebuild with `ir.Build`, and apply this comparator. Explain that compact regeneration creates consecutive source locations; D-13 structural equivalence covers behavior-bearing structure, while the hand-authored table independently covers exact locations. Do not weaken any other field. Build a test-only reconstructed source by merging committed managed entries and parsed padded-master entries using the table's original `StartLine` and ordinal; equal line positions preserve table ordinal. Following `core/ir/roundtrip_test.go`, skip only when zsh is unavailable, run real native-zsh introspection on original and reconstructed non-secret fixture paths, and compare full identity sets. Keep Parse/Build/Regenerate and native-zsh checks separate from the table-owned provenance oracle.

    For the successful secret case, copy the secret fixture into an isolated target, run ingest, and enumerate the isolated bare repository with `git -C <store> cat-file --batch-all-objects --batch`; scan the complete binary output rather than `rev-list`, branch traversal, or committed paths, so reachable and unreachable loose/packed objects are included. Require the unmistakable literal absent from every object, rewritten `.zshrc`, human/JSON stdout, and stderr while Name/StartLine is present. Snapshot SecretRef, backend value/counters, complete file, all generated object output, and first withheld output; run unchanged ingest again and require equal reference/backend, zero additional backend calls, stable file/generated output, no literal, and no duplicate withheld item. Assert the dynamic secret source remains verbatim and unreported.

    Add a second isolated real-store abort case whose associated `.zshrc` and captured streams are already literal-free. Drive `Store.Commit` with a literal-bearing in-memory candidate and a deterministic keychain whose Store callback fails after hash-object/commit-tree creation but before update-ref; assert the ref/tree/backend retain their pre-call state. Run the same `--batch-all-objects --batch` scan, including newly unreachable objects, and require the candidate literal absent from all object bytes plus the associated `.zshrc`, stdout, and stderr. This direct real-store abort isolates object-database leakage from the controller's intentionally exact rollback of user source. Do not accept a reachable-object-only scan. Include an adversarial shell substitution in each fixture and prove ingest itself never executes it by using a sentinel path that remains absent.
  </action>
  <acceptance_criteria>
    - Controller capture, committed profile, and master match the hand-authored Text/StartLine/disposition/ordinal table; no expected value is Parse/Build-derived.
    - Regenerate -> real Parse -> `ir.Build` is equal on every behavior-bearing profile field with only regenerated `StartLine` excluded, independently of the native-zsh identity comparison.
    - The fresh pre-commit reconstruction has exact original Text/StartLine/routing/ordinal on an interleaved/gapped fixture, and the reconstructed file has equal real-zsh identity state; provenance and behavior oracles are independent of production accounting and each other.
    - All-object enumeration after success and post-hash/pre-ref abort finds no literal in reachable or unreachable objects, `.zshrc`, stdout, or stderr; unchanged second ingest preserves the reference/value with zero backend calls/no duplicate report; the dynamic secret stays late-bound.
    - No command substitution sentinel is created during ingest.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestIngestEndToEndProvenanceOracle TestIngestEndToEndSemanticRoundTrip TestIngestEndToEndSecretObjectDatabase TestIngestEndToEndSecretAbortObjectDatabase TestIngestEndToEndNoExecution' && listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') && for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && GOTOOLCHAIN=auto go test ./core/cli -run '^(TestIngestEndToEndProvenanceOracle|TestIngestEndToEndSemanticRoundTrip|TestIngestEndToEndSecretObjectDatabase|TestIngestEndToEndSecretAbortObjectDatabase|TestIngestEndToEndNoExecution)$' -count=1 && GOTOOLCHAIN=auto go test ./core/cli -count=1</automated>
  </verify>
  <done>The Phase 2 behavior oracle and PROF-03 secret boundary both hold through real Phase 6 ingest components.</done>
</task>

<task type="auto">
  <name>Task 3: Run the final repository quality and architecture gates</name>
  <files>None — verification-only; do not modify files while running this task.</files>
  <read_first>
    - Makefile: `check`, `fmt-check`, `vet`, `lint`, and `test` targets
    - CLAUDE.md: repository verification and dependency constraints
    - core/shell/zsh/invariant_test.go: concrete-provider and zsh-syntax ownership checks
    - .planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE: immutable pre-execution commit captured by Plan 06-01
    - Plans 06-01 and 06-02 summaries: all produced symbols and resolved edge guarantees
  </read_first>
  <action>
    Run the complete repository `make check` after Tasks 1 and 2 pass. Resolve the full commit ID from `06-EXECUTION-BASE` and require it to exist as a commit. Compare `go.mod` and `go.sum` from that immutable object to execution HEAD, then check the index and worktree separately; all three comparisons must be clean so committed, staged, and unstaged dependency drift are detected instead of comparing only against moving HEAD.

    Query the transitive dependency closure for production `./core/cli`, `./core/ir`, and `./core/store` with `go list -deps -f '{{.ImportPath}}'`. Treat any `go list` failure as a blocking failure and require the exact import path `zsh-pro/core/shell/zsh` to occur zero times; do not suppress command errors or infer layering from direct imports alone.

    Run the deterministic API coverage detector over the exact Phase 6 ROADMAP section returned by `roadmap.get-phase 6 --pick section` plus all three final PLAN bodies. Capture its documented exit status explicitly: status 1 means no matching external surface, status 0 means detected, and status 2 means startup failure. Require status 1, parse the emitted JSON as a typed contract (`detected` is the boolean `false` and `signals` is an array), then require `COVERAGE.md` absent. Do not use a fallback echo, ignore detector failures, or hand-classify output. If the detector returns any other status, `detected:true`, malformed JSON, or a non-array signal field, stop phase execution before seal and follow the full coverage-matrix checkpoint. Preserve every final-gate failure as a blocker; do not weaken tests or exclude packages.
  </action>
  <acceptance_criteria>
    - `make check` passes with formatting, vet, lint, and all tests.
    - Production layering retains the sole concrete-zsh composition root, with zero forbidden paths in the complete transitive dependency set.
    - `go.mod` and `go.sum` match the immutable execution-base commit in HEAD, the index, and the worktree.
    - The API detector returns its documented no-surface status with typed `detected:false`/array output, and `COVERAGE.md` remains absent.
  </acceptance_criteria>
  <verify>
    <automated>make check && base_file=.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE && base_commit=$(tr -d '\n' &lt; "$base_file") && git cat-file -e "${base_commit}^{commit}" && git diff --exit-code "$base_commit" HEAD -- go.mod go.sum && git diff --cached --exit-code -- go.mod go.sum && git diff --exit-code -- go.mod go.sum && imports=$(GOTOOLCHAIN=auto go list -deps -f '{{.ImportPath}}' ./core/cli ./core/ir ./core/store) && printf '%s\n' "$imports" | awk '$0 == "zsh-pro/core/shell/zsh" { found++ } END { exit found != 0 }' && phase_section=$(node /home/metanmai/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 6 --pick section) && phase_scope=$(printf '%s\n' "$phase_section"; sed -n '1,$p' .planning/phases/06-ingest-end-to-end/06-0[123]-PLAN.md) && { api_json=$(printf '%s' "$phase_scope" | node /home/metanmai/.codex/gsd-core/bin/lib/api-coverage.cjs --json); api_status=$?; test "$api_status" -eq 1; } && API_JSON="$api_json" node -e 'const x = JSON.parse(process.env.API_JSON); if (typeof x.detected !== "boolean" || !Array.isArray(x.signals) || x.detected !== false) process.exit(1)' && test ! -e .planning/phases/06-ingest-end-to-end/COVERAGE.md</automated>
  </verify>
  <done>The end-to-end ingest phase passes every repository gate, retains transitive layering, proves dependency manifests unchanged from the immutable execution base, and seals only after a successful typed non-external-API decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fixtures/user source -> real zsh parser and CLI | Adversarial source must remain inert during ingest and retain semantics during oracle execution. |
| deterministic secret backend -> real Git object database | Test credentials must prove the same persistence boundary as production without consulting a host keychain. |
| built binary -> HOME/store/cache filesystem | Composition tests mutate only isolated temporary paths through actual production wiring. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies to the end-to-end verification boundary. V1.1 input-validation controls map to inert adversarial fixtures, V8.3 sensitive-data controls map to exhaustive Git/file/stdout/stderr secret scans, V1.14 configuration controls map to isolated filesystem roots and unchanged dependency manifests, and V7.4 error-handling controls map to fail-closed test outcomes. Execution and plan completion are blocked while any critical/high threat lacks both an executable mitigation and its automated verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-03-01 | Information Disclosure | secret E2E fixture and Git/output scans | high | mitigate | Use an unmistakable test literal, scan every reachable and unreachable object through Git's all-object batch interface after success and post-hash abort, scan file/stdout/stderr, and require only the injected backend to receive it. |
| T-06-03-02 | Elevation of Privilege | adversarial fixture command substitutions | high | mitigate | Assert ingest creates no sentinel; invoke zsh only in the explicit oracle over controlled temp files after ingest completes. |
| T-06-03-03 | Tampering | provenance and behavior-equivalence oracle | high | mitigate | Hand-author exact expected text, locations, dispositions, and ordinals independently of Parse/Build, then separately reconstruct by table positions and compare native-zsh identity sets. |
| T-06-03-04 | Tampering | built-binary filesystem test | medium | mitigate | Set isolated absolute HOME/ZSHPRO_HOME/XDG paths and reuse existing test cleanup/build helpers. |
| T-06-03-05 | Tampering | final dependency and API seal | high | mitigate | Compare module manifests against the immutable execution-base commit plus index/worktree, query transitive imports without suppression, and require the detector's documented no-surface status plus typed output. |
| T-06-03-SC | Tampering | package installation | low | accept | No package installation occurs; `make check` and the execution-base module comparison verify the dependency set is unchanged. |
</threat_model>

<pre_mortem>
| Likely failure | Early mitigation |
| --- | --- |
| Unit tests pass but main’s injected store is not recognized as IngestStore. | Run the compiled binary against a real bare store and inspect `main` objects. |
| The oracle derives expectations from Parse/Build and therefore agrees with the same defect. | Keep an exact hand-authored Text/StartLine/disposition/ordinal table, and run Parse/Build plus native-zsh behavior as separate evidence. |
| Secret checks traverse only refs while the literal survives in an unreachable object created before a failed ref update. | Enumerate the complete object database after both success and post-hash abort, then scan file plus both streams. |
| Second-run tests compare only markers and miss master drift. | Capture and compare complete `.zshrc` bytes across built-binary runs. |
| A dependency change is committed before the final gate, so a worktree-only diff passes. | Compare both manifest files to `06-EXECUTION-BASE`, then check index and worktree independently. |
</pre_mortem>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Real `.zshrc` through parse/classify/partial-eval/regenerate/main commit | 06-03 Tasks 1-2 | COVERED | Built binary plus real component oracle. |
| REQ | PROF-03 | Secret excluded and user told what was withheld | 06-03 Task 2 | COVERED | All Git/file/output surfaces scanned. |
| RESEARCH | — | None: research explicitly skipped | — | EXCLUDED | No RESEARCH.md required or created. |
| CONTEXT | D-01, D-02 | Public ingest path and dispatch | 06-03 Task 1 | COVERED | Compiled command exercised. |
| CONTEXT | D-03, D-04 | Human/JSON accounting | 06-03 Task 1 | COVERED | Both modes asserted. |
| CONTEXT | D-05, D-06 | Existing partition and master routing | 06-03 Task 2 | COVERED | Independent partition comparison. |
| CONTEXT | D-08, D-09 | Withheld report and fail-closed secret behavior | 06-03 Task 2 | COVERED | Literal/dynamic cases. |
| CONTEXT | D-10 | Fixed main baseline | 06-03 Tasks 1-2 | COVERED | Real Git ref/read. |
| CONTEXT | D-11, D-12 | Append warning, exact marker preservation | 06-03 Task 1 | COVERED | Installed append fixture. |
| CONTEXT | D-13 | End-to-end Phase 2 oracle | 06-03 Task 2 | COVERED | Hand-authored provenance table plus separate native-zsh identity comparison. |
| CONTEXT | D-14 | Deterministic real-shaped fixture coverage | 06-03 Tasks 1-2 | COVERED | Three focused fixtures, exact table expectations, and all-object scans. |
| CONTEXT | D-15, D-16 | Actual composition-root store seam | 06-03 Task 1 | COVERED | Built `main.go` path. |
| SPEC | 1 | Mixed declarative/imperative source commits main with zero dropped statements | 06-01 Task 2, 06-02 Task 2, 06-03 Tasks 1-2 | COVERED | Real binary and independent partition comparison. |
| SPEC | 2 | Literal withheld/dynamic verbatim secret behavior | 06-02 Task 2, 06-03 Task 2 | COVERED | Git/file/output scans and deterministic backend. |
| SPEC | 3 | Complete-file second-run idempotence | 06-01 Task 2, 06-02 Task 3, 06-03 Task 1 | COVERED | Helper, controller, and built-binary levels. |
| SPEC | 4 | Post-END append warning without mutation | 06-01 Task 2, 06-02 Task 3, 06-03 Task 1 | COVERED | Exact suffix equality. |
| SPEC | 5 | Committed baseline/master behavior-equivalence oracle | 06-03 Task 2 | COVERED | Independent exact provenance table, source-order reconstruction, and native-zsh identity comparison. |
</source_audit>

<spec_fallback>
The automatic two-stage prohibition probe recalled circular-oracle, fake-component, fixture-secret, routine test-cleanup, generic injection, and path-safety candidates. Precision filtering dropped routine test hygiene and referred generic security canon to the STRIDE register. It kept exactly one transparency prohibition for this plan: end-to-end evidence must not substitute fake parser/Git behavior or a circular oracle. It is projected verbatim in `must_haves.prohibitions` as descriptor-less, `status: unresolved`, `verification: null`, and flagged. Plan-set equality: 6 kept across the probe = 2 in 06-01 + 3 in 06-02 + 1 in 06-03; 6 projected, 0 dismissed, and 0 silently dropped.
</spec_fallback>

<capability_decisions>
- API coverage detector: execute the detector over the queried Phase 6 ROADMAP section plus all final PLAN bodies, require its documented status 1 for no matching external surface, parse its JSON fields as typed values, require `detected:false` and an array of signals, and require COVERAGE.md absent; status 0/2 or malformed output is blocking.
- Assumption-delta detector: false-positive `second` term in idempotency wording; baseline `main` remains the sole primary profile noun and no identity generalization is planned.
- Schema push detector: no ORM/schema path appears in any plan; no schema task or push is introduced.
</capability_decisions>

<verification>
Run both focused end-to-end packages, then run Task 3’s complete `make check`, transitive architecture import gate, immutable-base/index/worktree module-file gate, and typed deterministic API detector.
</verification>

<success_criteria>
- The compiled binary completes ingest and exact second-run idempotence through the actual composition root.
- Real Git objects contain the managed baseline; every reachable and unreachable object is free of the literal secret after success and injected abort, and reports identify withheld names/lines only.
- Every imperative statement is preserved in master output; the hand-authored provenance table matches exact routing/location/order and source-order reconstruction is behavior-equivalent under native zsh.
- Trailing installer appends warn and remain byte-identical.
- The full repository quality gate passes with transitive architecture, immutable dependency-manifest, and typed API-detector invariants intact.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md` when done.
</output>
