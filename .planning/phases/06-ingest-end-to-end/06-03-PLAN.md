---
phase: 06-ingest-end-to-end
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - core/cli/store.go
  - core/cli/install.go
  - core/cli/install_transaction.go
  - core/cli/atomic_rename.go
  - core/cli/atomic_rename_linux.go
  - core/cli/atomic_rename_darwin.go
  - core/cli/atomic_rename_other.go
  - core/cli/atomic_rename_test.go
  - core/cli/atomic_rename_supported_test.go
  - core/cli/atomic_rename_unsupported_test.go
  - core/cli/install_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-07/D-10: both ordinary install and ingest initialize the injected store before creating or opening the cache; loader promotion still precedes `.zshrc` promotion, including when explicit `ZSHPRO_HOME` makes the cache and store root identical."
    - "D-05/D-06: the exact original `.zshrc` snapshot is the ingest source and complete-Profile authority. Every eligible entry, managed or unmanaged, remains source-ordered for Store commit after redaction; EffectiveManaged is activation/reporting-only. The install candidate changes only exact zsh-pro marker regions, preserving every outside byte in original order."
    - "D-07/D-11/D-12: ordinary install keeps the landed topology behavior: no marker appends one region, one balanced region is replaced, multiple balanced regions collapse to one while preserving intervening ordinary bytes, and orphan END, unterminated START, nested, or interleaved topology fails before mutation. Ingest classifies post-END ordinary content separately and never treats a second balanced region as a generic append."
    - "For an existing target, one platform adapter atomically exchanges target and candidate in a single namespace syscall: Linux uses `renameat2(..., RENAME_EXCHANGE)`, Darwin uses descriptor-relative `renameatx_np(..., RENAME_SWAP)` from the `renamex_np` family, and every other platform or unsupported filesystem fails closed before target mutation. The target pathname always names the old or new file, never an intermediate absence."
    - "One physical `exchangePeerBasename` is the candidate basename: it holds new bytes before exchange, the displaced occupant after exchange, and the candidate again after a guarded reverse. A separate `candidateEvidenceBasename` authenticates the candidate; no additional recovery-only name or relocation exists. The durable same-directory journal and authenticated identities distinguish unchanged, promoted-durable, restored-durable-stale, and recovery-required states."
    - "The private transaction-directory FD remains retained through finalize/recovery. Candidate and evidence files are each fsynced/closed, then the transaction directory is fsynced; the prepared journal file is then fsynced/closed and the transaction directory fsynced again before any atomicRenameAt call. Every later journal transition fsyncs both its file and transaction directory, and every directory changed by a namespace syscall is synced; any failure retains recovery evidence."
    - "Cooperating zsh-pro processes serialize each private random mode-0700 journal-namespace authenticate+effect interval with one real cross-process per-root exclusive lock held through postvalidation and directory fsync. This advisory lock does not exclude arbitrary external `.zshrc` writers; they remain in scope through exchange/no-replace, postvalidation, guarded reverse, or retained recovery."
    - "Every newly created private namespace/probe/transaction directory is descriptor-relative chmod-verified at exact mode 0700, and every newly created lock/probe/evidence/journal file is descriptor chmod-verified at exact mode 0600 before use. Requested creation modes alone are never treated as authoritative because process umask may remove bits."
    - "Atomic adapters use per-call syscall injection only: production passes syscall.Syscall6 directly, tests pass independent closures, supported attempts make exactly one raw call, and validation rejection makes zero. Common, Linux/Darwin-supported, and other-platform tests are build-tagged into separate files. The explicit local Go 1.25.x contract remains mandatory, while the Linux ABI audit requires amd64/arm64 and validates every claimed production trap against an available definition instead of coupling support to one patch release's full dist-list enumeration."
    - "An originally absent target follows a separate exclusive-create protocol and never calls exchange. Once no-replace succeeds, rollback to absence is recovery-required with target and journal retained because this phase has no separately audited identity-conditional inverse primitive."
    - "Capability preflight is split: a side-effect-free OS/architecture adapter check runs first, then exchange and no-replace are exercised on authenticated private entries on the target filesystem before Store initialization, cache, loader, or target effects. Unsupported capability leaves Store/cache/loader/target unchanged; uncertain private-probe cleanup retains authenticated evidence and still performs none of those later effects."
    - "The one loader/install promotion keeps two independent identity axes: `expectedTarget` is the exact original snapshot, while `expectedCandidate` comes from independently durable canonical install-candidate evidence. Before exchange target must match expectedTarget and the regular exchange peer must match expectedCandidate; after exchange target must match expectedCandidate and the same peer is sampled as type-aware displacedObserved and compared with expectedTarget. Non-regular displaced occupants are authenticated without opening/following them and are guarded-reversed or retained in place. No post-commit target promotion exists."
    - "The full-byte exchange peer is the only permitted additional literal-secret-bearing file: its current-EUID mode-0700 parent is the confidentiality boundary, it is opened/authenticated descriptor-relatively, and source bytes never enter candidate evidence, journal, cache, output, or Store objects. Durable finalize/restore removes it; recovery uncertainty retains it privately and reports only typed value-free evidence."
  artifacts:
    - path: "core/cli/install_transaction.go"
      provides: "Live install/ingest transaction, topology/layout snapshot with bounded equality, authenticated exchange journal, recovery, rollback ordering, and mutation-surface confinement"
      exports: ["preparedIngestInstall", "installSnapshot", "promotionOutcome"]
    - path: "core/cli/atomic_rename.go"
      provides: "Platform-neutral side-effect-free capability check, exchange/no-replace contract, basename validation, and unsupported/error classification"
      exports: ["atomicRenameCapabilityCheck", "atomicRenameAt", "atomicRenameMode", "ErrAtomicRenameUnsupported"]
    - path: "core/cli/atomic_rename_linux.go"
      provides: "Linux renameat2 RENAME_EXCHANGE/RENAME_NOREPLACE adapter"
    - path: "core/cli/atomic_rename_darwin.go"
      provides: "Darwin renameatx_np RENAME_SWAP/RENAME_EXCL adapter"
    - path: "core/cli/atomic_rename_other.go"
      provides: "Fail-closed implementation for non-Linux/non-Darwin targets"
    - path: "core/cli/atomic_rename_test.go"
      provides: "Platform-neutral validation, per-call injection isolation, and Go 1.25 Linux ABI/fallback AST audit"
    - path: "core/cli/atomic_rename_supported_test.go"
      provides: "Linux/Darwin direct exchange/no-replace and injected unsupported-error runtime tests"
    - path: "core/cli/atomic_rename_unsupported_test.go"
      provides: "Non-Linux/non-Darwin native unsupported and zero-raw-call tests"
    - path: "core/cli/install.go"
      provides: "Shared store-before-cache preparation and loader-before-target promotion used by install and ingest"
      contains: "runInstallWithStoreInitialization"
    - path: "core/cli/store.go"
      provides: "Narrow injected store initializer and ingest transaction interfaces using model-owned contracts"
      contains: "StoreInitialization"
    - path: "core/cli/install_test.go"
      provides: "Outside-marker byte table, topology modes, explicit-root overlap, substitution, recovery, and original/candidate snapshot-binding proofs"
  key_links:
    - from: "runInstallWithStoreInitialization / prepareIngestInstallAt"
      to: "StoreInitializer -> runtime cache -> loader -> target"
      via: "one shared order; cache opening is forbidden until initialization succeeds"
      pattern: "StoreInitialization"
    - from: "original source/layout"
      to: "complete Profile input and canonical install candidate"
      via: "exact marker scan removes only tool-owned regions from parsing and changes only those regions in the candidate"
      pattern: "originalSnapshot"
    - from: "prepared target snapshot"
      to: "one descriptor-relative atomic exchange"
      via: "authenticated parent, same-parent candidate, platform adapter, expected/displaced validation, journal state transitions"
      pattern: "promotionOutcome"
    - from: "original snapshot plus canonical loader region"
      to: "one target promotion"
      via: "the original snapshot supplies expectedTarget; independently durable install-candidate evidence supplies expectedCandidate for pre/post-exchange checks"
      pattern: "expectedCandidate"
    - from: "authenticated parent/journal descriptors"
      to: "target mutation and artifact cleanup"
      via: "one audited descriptor-relative mutation helper holds the cooperating-process root lock while it freshly authenticates parent, journal, transaction, requested-link topology, and entry identities through each effect/postvalidation/sync interval"
      pattern: "mutateAuthenticatedTargetEntry"
  prohibitions:
    - statement: "MUST NOT destroy or lose a target occupant changed after validation. Atomic exchange may move that occupant to the authenticated exchange-peer basename; postvalidation must guarded-reverse it through that same basename or retain both names with recovery-required evidence."
      category: safety
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT remove or rewrite content after the managed END marker during ingest."
      category: fidelity
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Turn the landed installer into a live, recoverable filesystem transaction that ingest can share without weakening install behavior.

Purpose: Fix the explicit store/cache overlap order, preserve every ordinary startup byte, and close target-path substitution while promoting the canonical loader/install candidate exactly once without ever making an existing target pathname absent.
Output: A shared initialization/install order, an explicit topology scanner, Linux/Darwin atomic-exchange adapters, a durable promotion/recovery protocol, and focused filesystem transaction tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@core/cli/store.go
@core/cli/install.go
@core/cli/install_test.go

<interfaces>
Contracts to extend without bypassing:
- `runInstallWithStoreInitialization` is the shipped install composition point. Its store initializer must complete before cache preparation; its promotion order remains loader then `.zshrc`.
- `preparedWrite` currently stages and promotes by path. Replace its mutation internals with the authenticated transaction protocol; do not create an ingest-only overwrite writer.
- `StoreInitialization` is the CLI-neutral initializer result. It maps the exact injected store's root/ownership/rollback operations without importing `core/store`.
- 06-01 provides model-owned `IngestBaseline` and authenticated Begin/Abort quarantine cleanup; 06-02 provides the serialized prepared-ref CommitIngest contract. Baseline profile presence informs rerun/store conflict handling, but never authorizes destructive startup reconstruction.
- Landed build-tagged syscall wrappers in `core/cli/cache_syscalls_{linux,darwin}.go` and `runtime_openat_{linux,darwin}.go` are the local no-module-dependency pattern. The executor must verify the exact Linux and Darwin call surfaces against the installed Go 1.25 source/platform headers and cross-compile tests before coding; no guessed syscall number, non-atomic rename sequence, or link-based exchange fallback is permitted.
</interfaces>
</context>

## Artifacts this plan produces

- New `core/cli/install_transaction.go`: exact original source/layout records and one durable same-parent canonical loader/install promotion state machine.
- New build-tagged `atomic_rename*.go` files: exchange and exclusive-create contracts implemented with Linux `renameat2`, Darwin's descriptor-relative `renamex_np` family call, and a fail-closed unsupported-platform adapter.
- Extended `core/cli/store.go`: only narrow initializer/begin/commit/abort methods expressed in `core/model` types.
- Updated `core/cli/install.go`: one store-before-cache, loader-before-target order for ordinary install and ingest.
- No package installation, module change, external service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share initialization order and preserve ordinary startup bytes</name>
  <files>core/cli/store.go, core/cli/install.go, core/cli/install_transaction.go, core/cli/install_test.go</files>
  <read_first>
    - core/cli/install.go: `runInstallWithStoreInitialization`, target/cache preparation, loader/target promotion, rollback order, and `preparedWrite`
    - core/cli/store.go: the landed injected Store/initializer seams and `StoreInitialization`
    - core/cli/install_test.go: installed-block byte fixtures, stale-target tests, explicit-home tests, and per-instance seam conventions
    - core/store/install_transaction.go: initializer ownership and rollback eligibility exposed through the CLI seam
    - .planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md: exact Begin/Abort contracts and initializer-safety outcome
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-05 through D-07, D-10 through D-12, D-14 through D-16
  </read_first>
  <behavior>
    - Ordinary install and ingest both complete the Task 3 target-filesystem capability preflight before calling the same injected initializer; the initializer still precedes every runtime cache open/create. An explicit shared store/cache root succeeds and compensation follows filesystem restore-or-unchanged classification, Store Abort when applicable, loader/cache rollback, then initializer rollback.
    - Unequal ancestor overlap between store and runtime roots fails before cache or target mutation; equality is the supported explicit-home case.
    - Exact-line scanning recognizes only complete START/END marker lines and records an explicit topology plus all ordinary/managed byte spans and newline style.
    - Target snapshot equality is intentionally bounded to existence, requested-link topology, regular-file type, device/inode, content digest, and permission bits. Target uid/gid, timestamps, ACLs, xattrs, and platform flags neither authorize nor reject a later promotion; private journal ownership authentication is evaluated independently.
    - Topology table: no marker -> append one canonical loader region; one balanced region -> replace only that region; multiple nonnested balanced regions -> collapse exact regions to one while preserving every surrounding/intervening ordinary byte; orphan END, unterminated START, nested, or interleaved markers -> malformed and fail before initializer/cache/target effects.
    - Candidate rendering covers LF, CRLF, empty, no-final-newline, multiline, existing-block, comments/gaps, and post-END cases without normalizing any ordinary source byte.
    - Pre-install marker topology alone selects append/replace/collapse candidate construction. Durable profile-object presence affects Store rerun semantics downstream, never physical startup composition.
    - Ordinary nonblank/noncomment bytes after the canonical END remain part of eligible ingest input and stay byte-for-byte at their original position with one stable warning. A second balanced managed region is duplicate topology to repair, never generic post-END append content. No master/complement bytes are rendered.
  </behavior>
  <action>
    Extend the CLI-neutral store contracts per D-15/D-16: the initializer returns canonical root identity, invocation ownership, rollback/finalize callbacks, and the narrow transaction interface names 06-01 Begin/Abort plus the completed 06-02 Commit through model-owned types. The concrete adapter is wired by the downstream 06-04 controller. Do not add a concrete-store import to `core/cli` and do not construct another store inside a callback.

    Refactor the shared install path per D-07/D-10 so side-effect-free target inspection and Task 3's capability preflight may occur first, but Store initialization completes only after that preflight and still precedes `prepareInstall`, cache creation/opening, loader staging, or target mutation. Canonicalize store/runtime roots once. Equality is allowed. Compensation must first classify the target promotion and restore it durably or prove it unchanged; only those two outcomes may advance to clean ingest-quarantine Abort, loader/cache restoration, and initializer rollback in actual LIFO order. Any recovery-required, sync-uncertain, unsafe-reverse, or absent-target post-create outcome retains target/journal, Store quarantine, cache, and initializer state and makes later compensation call counts zero. Reject unequal ancestor overlap before mutation because ownership would be ambiguous. Preserve ordinary install's existing output and loader-before-`.zshrc` contract.

    Add `prepareIngestInstallAt` and exact layout/value records in `install_transaction.go`. Read the requested path or D-01 default without mutation; resolve a symlink target once and record only the snapshot contract: requested-link topology, resolved-target existence, regular-file type, device/inode, content digest, and permission bits. Preserve the original byte slice. Explicitly exclude target uid/gid, atime/mtime/ctime/birth time, ACLs, xattrs, and platform flags from equality; those fields may vary without making the bounded content/identity snapshot stale. Keep private journal owner authentication separate. Refactor the landed `replaceManagedBlock` scan into one shared exact-physical-line topology parser used by ordinary install and ingest. Do not add a second marker implementation or change the literal constants.

    Implement and test the complete topology table, preserving the landed ordinary-install behavior per D-07. No markers append the canonical loader region with landed separator rules. Exactly one balanced region is replaced. Two or more sequential balanced regions collapse to one while preserving every ordinary byte before, between, and after them exactly as `TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent` and `TestReplaceManagedBlockOnlyRecognizesExactPhysicalMarkerLines` already require. An orphan END, unterminated START, nested START, or interleaved sequence returns the existing refusal before initializer/cache/target mutation. Ingest consumes the same parsed topology, excludes only exact tool-owned region spans from source parsing, never labels a second balanced region as a generic append, and prepares one canonical install candidate directly from the original snapshot. Only ordinary nonblank/noncomment content after the resulting canonical END produces D-11's warning; keep those bytes eligible for the complete Profile and distinguish malformed topology from valid post-END ordinary content in typed preparation results.

    Implement the full D-05/D-06/D-11/D-12/D-14 byte table. Preserve LF versus CRLF, empty input, missing final newline, multiline commands, comments, gaps, and post-END bytes. Retain two explicit products from the original snapshot: (1) ordered eligible source bytes for `provider.Parse`/`ir.Build`, formed only by excluding exact zsh-pro-owned marker regions, and (2) an independently prepared install candidate formed only by append/replace/collapse of those regions. Do not subtract EffectiveManaged extents, positive-select imperative kinds, or reconstruct ordinary bytes from `Entry.Text`. Return one stable append warning for ordinary post-END content while leaving it both physically in place and logically eligible for the complete Profile.

    Add exact-name tests for order, bounded snapshot equality, and outside-marker bytes. `TestInstallSnapshotMatchesBoundedFields` changes each included field one at a time and requires mismatch, then changes excluded timestamp/uid/gid metadata where the host permits and requires equality; unsupported ACL/xattr/flag manipulation is documented as outside the contract rather than silently asserted. The explicit `ZSHPRO_HOME` test must use the same canonical root for store and cache, prove initializer runs before cache creation, exercise a later failure, and assert the filesystem -> Abort -> loader/cache -> initializer compensation order. `TestPrepareIngestPreservesEveryOutsideMarkerByte` uses one exact fixture containing managed, imperative, parser-opaque, comment/gap, post-END, CRLF, and no-final-newline rows; it asserts source parsing sees every eligible ordinary row once in order while the install candidate differs only at exact marker regions. Run all landed ordinary-install marker tests unchanged and add ingest cases for every topology row, including a second balanced region that is repaired rather than warned as appended ordinary content.
  </action>
  <acceptance_criteria>
    - Explicit store/cache-root equality no longer causes the cache to pre-create a path that initialization then rejects.
    - Ordinary install retains loader-before-target promotion and its existing success/failure behavior.
    - All six topology classes have explicit outcomes, and existing balanced-duplicate repair remains green.
    - Every newline/layout fixture has deterministic byte-exact output and original line provenance.
    - Pre-install topology alone selects append/replace/collapse; baseline presence never changes ordinary startup composition.
    - The mixed fixture proves every eligible ordinary extent reaches complete-Profile input and every outside-marker byte stays in place.
    - Post-END bytes are warned once, included in eligible ingestion, and remain byte-identical.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestInstallInitializesStoreBeforeCache TestInstallExplicitSharedRootOrdersStoreBeforeCache TestInstallRejectsAncestorStoreCacheOverlap TestInstallSnapshotMatchesBoundedFields TestScanZshrcMarkerTopologyTable TestPrepareIngestPreservesEveryOutsideMarkerByte TestPrepareIngestTopologyModes TestPrepareIngestAppendWarningRetainsEligibleSource TestPrepareIngestRepairsBalancedDuplicates TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent TestReplaceManagedBlockOnlyRecognizesExactPhysicalMarkerLines TestReplaceManagedBlockRejectsEveryMalformedExactMarkerOrdering TestInstallRefusesMalformedExactMarkerOrderingsWithoutWriting' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestInstallInitializesStoreBeforeCache|TestInstallExplicitSharedRootOrdersStoreBeforeCache|TestInstallRejectsAncestorStoreCacheOverlap|TestInstallSnapshotMatchesBoundedFields|TestScanZshrcMarkerTopologyTable|TestPrepareIngestPreservesEveryOutsideMarkerByte|TestPrepareIngestTopologyModes|TestPrepareIngestAppendWarningRetainsEligibleSource|TestPrepareIngestRepairsBalancedDuplicates|TestInstallCollapsesBalancedDuplicatesAndPreservesInterveningContent|TestReplaceManagedBlockOnlyRecognizesExactPhysicalMarkerLines|TestReplaceManagedBlockRejectsEveryMalformedExactMarkerOrdering|TestInstallRefusesMalformedExactMarkerOrderingsWithoutWriting)$' -count=1</automated>
  </verify>
  <done>The installer and ingest share a safe store-before-cache order, and ingest owns a byte-exact source/layout snapshot plus one canonical install candidate that preserves all ordinary startup bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and directly test the Linux/Darwin atomic-rename adapters</name>
  <files>core/cli/atomic_rename.go, core/cli/atomic_rename_linux.go, core/cli/atomic_rename_darwin.go, core/cli/atomic_rename_other.go, core/cli/atomic_rename_test.go</files>
  <read_first>
    - core/cli/cache_syscalls_linux.go: local raw `syscall` wrapper and descriptor-relative basename pattern
    - core/cli/cache_syscalls_darwin.go: local Darwin raw-trap precedent
    - core/cli/runtime_openat_linux.go and core/cli/runtime_openat_darwin.go: build-tagged platform split and fail-closed descriptor access
    - .planning/phases/06-ingest-end-to-end/06-EVIDENCE.md: current-source and verified syscall audit
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-07 and D-14
  </read_first>
  <behavior>
    - `atomicRenameCapabilityCheck(mode)` is a side-effect-free adapter preflight over OS, architecture, and the audited mode table. It opens no file, creates no namespace, invokes no syscall, and returns typed unsupported before Task 3's filesystem probe.
    - `atomicRenameAt(dirFD, from, to, atomicRenameExchange)` accepts two safe sibling basenames and performs exactly one namespace syscall: Linux `renameat2` with `RENAME_EXCHANGE`, Darwin descriptor-relative `renameatx_np` from the `renamex_np` family with `RENAME_SWAP`.
    - `atomicRenameAt(dirFD, from, to, atomicRenameNoReplace)` is a distinct absent-target operation: Linux uses `RENAME_NOREPLACE`, Darwin uses `RENAME_EXCL`; an existing destination returns the platform existence error and preserves both names.
    - Empty names, `.`, `..`, separators, embedded NULs, same-descriptor identical names, invalid modes, unknown Linux architectures, non-Linux/non-Darwin builds, and kernel/filesystem unsupported errors fail closed without either name changing. Equal basenames remain valid when the retained source and destination descriptors are distinct directories.
    - There is no ordinary rename, hard-link sequence, delete-then-rename sequence, or retry under weaker flags. `ENOSYS`, `EINVAL` for an unsupported mode/filesystem, `ENOTSUP`/`EOPNOTSUPP`, and `EXDEV` become typed unsupported results for the caller.
    - `atomicRenameAtWithSyscall(call syscall6Fn,...)` receives its raw-call dependency per invocation. Production `atomicRenameAt` passes `syscall.Syscall6` directly; tests pass closures with independent counters. A validated supported attempt makes exactly one raw call, validation rejection makes zero, and no mutable package-level function variable exists.
    - `TestAtomicRenameLinuxGo125ABIAndFallbackSurface` rejects any non-local or non-Go-1.25.x toolchain, requires the phase's Linux amd64/arm64 minimum in the production map, validates every claimed production trap against an available local Go syscall definition, and proves flags 1/2, Syscall6 ABI argument order, one raw call, and absence of rename/link/unlink/remove fallbacks, retries, loops, or alternate flags.
    - The exact other-platform adapter is both cross-compiled for FreeBSD amd64 and executed on the local host through a narrow `phase6_test_unsupported_atomic` build selector that excludes Linux/Darwin adapters and includes the unchanged production other adapter. This test-only selector cannot alter an ordinary build.
  </behavior>
  <action>
    Before writing the adapters, require `GOTOOLCHAIN=local go env GOVERSION` to match Go 1.25.x, then verify the exact call surfaces from that accepted local toolchain and platform definitions. Inspect `GOTOOLCHAIN=local go env GOROOT` generated/vendored Unix syscall sources for Linux `renameat2` trap numbers for every architecture the production map claims, require at least the phase's Linux amd64/arm64 targets, and inspect Darwin `SYS_RENAMEATX_NP` plus the four flag values; cross-check against the installed Linux UAPI headers when present. Architectures outside the claimed map remain deliberately fail-closed even if a later Go patch emits additional dist targets. Record the local toolchain mode, exact GOVERSION, and syscall audit in the task commit/summary. Do not permit an automatic toolchain download, import `golang.org/x/sys`, change `go.mod`, guess an unverified trap number, or substitute a weaker algorithm if a call surface is absent.

    Define the package-private `atomicRenameMode` values `atomicRenameExchange` and `atomicRenameNoReplace`, `ErrAtomicRenameUnsupported`, strict sibling-basename validation, and `syscall6Fn` matching the exact `syscall.Syscall6` ABI. Add `atomicRenameCapabilityCheck(mode)` as a pure OS/architecture/mode-table lookup with no filesystem, descriptor, or syscall access; it cannot claim filesystem support, which Task 3 proves separately on the authenticated target filesystem. `atomicRenameAtWithSyscall(call syscall6Fn,dirFD,from,to,mode)` owns validation plus one attempted platform call; production `atomicRenameAt` passes `syscall.Syscall6` directly at each call site, while tests supply a closure per invocation. Reject an identical basename only when the source and destination descriptor values are also identical; `atomicRenameBetweenAtWithSyscall` must preserve valid equal-basename cross-directory operations and forward both descriptors unchanged. Do not add a mutable package-level function variable. Keep error classification separate from transaction policy: invalid input is a validation error with zero raw calls; unavailable OS/architecture/kernel/filesystem capability wraps the sentinel; `EEXIST` from no-replace remains a stale-target result. The interface receives the already authenticated retained parent descriptor and never reopens an absolute parent path.

    Use the same existing platform file split to keep target-root locking cross-platform honest without an untagged Unix-only call: `atomic_rename_linux.go` and `atomic_rename_darwin.go` also provide the package-private descriptor-held `acquireTargetRootTransactionLock` primitive using the accepted local Go 1.25 blocking exclusive `syscall.Flock` surface with EINTR retry, while `atomic_rename_other.go` returns `ErrAtomicRenameUnsupported` before private journal or target mutation. Give the production platform files mutually exclusive tags that also support one test-only `phase6_test_unsupported_atomic` selection: it excludes the Linux/Darwin implementations and includes the exact other-platform implementation for local execution; default builds remain selected solely by GOOS. The generic stable namespace/no-follow lock-entry authentication remains in `install_transaction.go`; only the OS lock operation is platform-owned. This lock adapter is not a mutable injection global and is never cited as protection against arbitrary external target writers.

    In the Linux adapter, make the supplied call exactly once for `renameat2` with flags 2 for exchange and 1 for no-replace and the audited ABI order: trap, old-directory FD, old-name pointer, new-directory FD, new-name pointer, flags, zero. Resolve the trap from an exact `runtime.GOARCH` table copied only after the audit; require amd64 and arm64, require every table entry to have and equal an available accepted-local-Go-1.25 syscall definition, and let unknown values return `ErrAtomicRenameUnsupported` with zero calls. Do not require the table to equal every architecture emitted by a particular Go patch's `dist list`; omission is the intentional fail-closed architecture fallback. In the Darwin adapter, make the supplied call exactly once for descriptor-relative trap 488 only after the audit confirms `renameatx_np`, using flag 2 for swap and 4 for exclusive creation in the audited argument order. Follow the repository's existing Darwin `syscall.Syscall6`/build-tag pattern. The other-platform adapter always returns the typed unsupported error without a raw call.

    Keep `atomic_rename_test.go` platform-neutral. Add exact `TestAtomicRenameCapabilityCheckIsSideEffectFree`, `TestAtomicRenameAtRejectsUnsafeBasenames`, `TestAtomicRenameBetweenAtAllowsEqualBasenamesInDifferentDirectories`, `TestAtomicRenamePerCallInjectionIsParallelSafe`, and `TestAtomicRenameLinuxGo125ABIAndFallbackSurface`. The capability test covers every mode and adapter row with instrumented filesystem/raw-call seams and requires zero calls. The descriptor-aware test proves an equal basename with distinct retained directory descriptors reaches exactly one raw call with both descriptors intact, while the sibling wrapper still rejects the same name. The parallel test runs independent closures/counters concurrently, proves exactly one call for every supported attempted operation and zero for every validation rejection, and detects any shared mutable seam. The ABI test first enforces exact local Go 1.25.x, then parses the matching Go syscall constants/generated sources and all `atomic_rename*.go` production ASTs, requires amd64/arm64, validates every production trap entry against an available definition, verifies flags and Syscall6 argument order plus one raw call, and rejects ordinary rename/link/unlink/remove, retry/fallback loops, or alternate flags across the implementation. Task 3 owns the build-tagged runtime test split and exact tagged execution of the other adapter. Retain CGO-disabled Linux/Darwin amd64/arm64 cross-builds and add CGO-disabled FreeBSD amd64 cross-compilation.
  </action>
  <acceptance_criteria>
    - Linux and Darwin adapters compile without a module change and use the audited exchange/exclusive flags.
    - Exchange preserves both file identities and no-replace preserves a concurrent destination.
    - Unsupported platforms, architectures, kernels, and filesystems return a typed fail-closed result with no weaker retry.
    - Unsafe basenames never reach the syscall.
    - Equal basenames with distinct retained source/destination directory descriptors reach one syscall; identical descriptor/name pairs remain rejected.
    - The adapter capability check has no filesystem or syscall side effects and cannot substitute for the same-filesystem runtime probe.
    - Production raw-call dependencies are per invocation; parallel test counters are isolated, a supported attempt calls once, and validation rejects call zero times.
    - The exact local-Go-1.25 contract, required Linux amd64/arm64 traps, every claimed production trap definition, flags, ABI, and fallback surface pass one AST/source test without a patch-specific all-architecture equality assumption.
    - A FreeBSD amd64 test binary compiles, and Task 3 executes the exact other-platform adapter's zero-call test locally under the test-only selector without changing default platform selection.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestAtomicRenameCapabilityCheckIsSideEffectFree TestAtomicRenameAtRejectsUnsafeBasenames TestAtomicRenameBetweenAtAllowsEqualBasenamesInDifferentDirectories TestAtomicRenamePerCallInjectionIsParallelSafe TestAtomicRenameLinuxGo125ABIAndFallbackSurface' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^TestAtomicRename') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestAtomicRenameCapabilityCheckIsSideEffectFree|TestAtomicRenameAtRejectsUnsafeBasenames|TestAtomicRenameBetweenAtAllowsEqualBasenamesInDifferentDirectories|TestAtomicRenamePerCallInjectionIsParallelSafe|TestAtomicRenameLinuxGo125ABIAndFallbackSurface)$' -count=1 &amp;&amp; build_dir=$(mktemp -d) &amp;&amp; trap 'rm -rf "$build_dir"' EXIT &amp;&amp; CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-linux-amd64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-linux-arm64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-darwin-amd64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-darwin-arm64.test" ./core/cli &amp;&amp; CGO_ENABLED=0 GOOS=freebsd GOARCH=amd64 GOTOOLCHAIN=local go test -c -o "$build_dir/cli-freebsd-amd64.test" ./core/cli</automated>
  </verify>
  <done>The audited adapters have per-call raw-syscall injection, exact local-Go-1.25 Linux ABI/fallback proof for required and claimed architectures, exact validation call counts, and no weaker or dependency-backed path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate atomic exchange, crash recovery, and exact snapshot binding</name>
  <files>core/cli/install.go, core/cli/install_transaction.go, core/cli/install_test.go, core/cli/atomic_rename_supported_test.go, core/cli/atomic_rename_unsupported_test.go</files>
  <read_first>
    - core/cli/atomic_rename.go after Task 2: audited exchange/no-replace contract and typed unsupported result
    - core/cli/install.go after Task 1: `preparedWrite`, cache/loader/target ownership, and rollback hooks
    - core/cli/install_transaction.go after Task 1: topology, original source snapshot, canonical install candidate, requested symlink topology, and resolved target identity
    - core/cli/install_test.go: existing target/symlink/mode/rollback and ordinary installer regression cases
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-07, D-10, D-11, and D-12
  </read_first>
  <behavior>
    - Preflight has two ordered gates before Store initialization, cache, loader, or target effects: side-effect-free `atomicRenameCapabilityCheck` first, then an authenticated same-filesystem private probe that exercises exchange and no-replace only on random entries inside the target root's private transaction namespace. Unsupported capability leaves Store/cache/loader/target byte- and call-unchanged; uncertain probe cleanup retains authenticated private evidence and authorizes no later gate.
    - Preparation retains authenticated target-parent and private transaction-directory descriptors through finalize/recovery. It fsyncs/closes the new bytes at `exchangePeerBasename` and independent identity/digest/mode metadata at `candidateEvidenceBasename`, then fsyncs the transaction directory before creating the prepared journal. The mode-0700 transaction directory is the confidentiality boundary for the potentially literal-bearing peer; neither evidence nor journal contains source bytes. It fsyncs/closes that journal and fsyncs the transaction directory again before any target namespace syscall. The journal contains those two basenames plus `journalBasename`, parent/journal authentication, transaction ID, requested-link topology, expected target/candidate identities, and original existence; the schema has no additional recovery-only name.
    - Existing-target promotion revalidates requested symlink topology and compares target to `expectedTarget` (the original regular-file snapshot) and the staged regular exchange peer to `expectedCandidate` (independent canonical install-candidate evidence), then performs one atomic exchange. Afterward target is still regular-file-validated against expectedCandidate; the same exchange peer now holds and is sampled as type-aware `displacedObserved`, which records full entry mode/type, device/inode, uid/gid, a digest only for regular files, and link text only for symlinks without opening or following non-regular occupants.
    - If a regular or non-regular substitution wins between prevalidation and exchange, postvalidation detects it in displacedObserved at the same exchange-peer basename. The transaction reverses only while target still matches expectedCandidate and the exchange peer still matches the exact type-aware displacedObserved; after reverse target is displacedObserved and that same peer is the regular candidate again. Otherwise it retains both names and returns recovery required; it never removes or relocates the displaced occupant to another artifact. Recovery authenticates both exchanged and reversed type-aware rows before completing the same guarded reverse/finalize protocol.
    - Absent-target promotion never calls exchange. It uses atomic no-replace; a late target is preserved. After success the installed target and authenticated journal remain recovery evidence on any rollback request because deletion to recreate absence has no audited identity-conditional inverse in this phase.
    - Every journal transition requires journal-file fsync/close plus private transaction-directory fsync. After any namespace syscall, sync the target parent, transaction directory, and every other directory whose entries changed (sync a shared directory once). Failure of any required file/directory sync is state-uncertain; generic pathname rollback is disarmed and journal/artifacts remain for authenticated next-run recovery.
    - One promotion binds expectedTarget directly to the original snapshot and expectedCandidate to independent candidate evidence. Target change and candidate change are distinct zero-exchange failures; post-swap validation compares each physical name against its own axis without adopting a later snapshot or conflating the two. Commit success finalizes this transaction; it does not stage or promote another target file.
    - On Linux/Darwin, cooperating zsh-pro instances sharing the canonical target root hold one descriptor-backed cross-process exclusive advisory lock from before private journal-namespace authentication through target/exchange-peer mutation, postvalidation, every journal-file/transaction-directory transition sync, and every changed-directory sync. External/non-cooperating target writers remain in scope and are handled only by the atomic/postvalidation protocol.
    - `atomic_rename_test.go` always supplies common validation/ABI tests; `atomic_rename_supported_test.go` has `(linux || darwin) &amp;&amp; !phase6_test_unsupported_atomic` build tags and direct exchange/no-replace/injected-unsupported tests; `atomic_rename_unsupported_test.go` has `(!linux &amp;&amp; !darwin) || phase6_test_unsupported_atomic` tags and the native unsupported/zero-call test. Exact-name verification selects the normal platform list from local GOOS, and a separate local tagged invocation executes the exact other-platform adapter test.
  </behavior>
  <action>
    Route landed `preparedWrite` and ingest writes through one `install_transaction.go` protocol per D-07. Retain an `os.Root` plus raw descriptors for both the resolved target parent and private per-transaction directory through finalize/recovery; create that random current-EUID mode-0700 transaction directory inside the stable deterministic transaction namespace for the canonical target root. Creation mode is only an initial restriction: immediately chmod every newly created namespace/probe/transaction directory to exact 0700 and every newly created lock/probe/evidence/journal file to exact 0600 through the retained root or created descriptor, then stat-check exact type/mode before any use. Its fixed regular current-EUID mode-0600 lock entry is opened no-follow/descriptor-relative and held with a real Linux/Darwin exclusive advisory OS lock; the stable namespace/lock entry is not removed with a transaction. Define exactly three transaction artifact roles: `exchangePeerBasename`, the physical candidate basename that is a regular same-device file before exchange but may hold any atomically displaced occupant type afterward; `candidateEvidenceBasename`, independent immutable identity/digest/mode metadata for the new bytes and never their content; and `journalBasename`. Candidate evidence and journal remain regular private same-device objects authenticated relative to retained handles. Post-exchange displaced evidence is instead type-aware: lstat-authenticate device/inode/full mode/uid/gid, add a content digest only for regular files, and add readlink text plus repeated no-follow identity checks only for symlinks; never open a directory, FIFO, socket, device, or symlink merely to classify it. The mode-0700 parent, descriptor-only access, current-EUID checks, and value-free error surface are mandatory because the exchange peer necessarily duplicates literal-bearing source bytes while staged or displaced. Persist target-parent and transaction-directory identities and bind the journal to its opened descriptor identity, mode 0600, expected uid/gid, content digest, strict schema version, random transaction ID, requested-link topology, and those basenames. Do not add a recovery-only basename, a second displaced-occupant artifact, a source-byte copy in evidence/journal/cache/output, or a relocation step.

    Split capability preflight before Store initializer/cache/loader/target effects. First call side-effect-free `atomicRenameCapabilityCheck` for both exchange and no-replace; adapter/OS/architecture rejection causes zero filesystem calls. Only after it passes, retain/authenticate the target-parent descriptor, acquire the root lock, and create random private probe entries inside that target root's transaction namespace. Exercise one real exchange and one real no-replace on those same-filesystem private entries, validate resulting identities, restore/clean them descriptor-relatively, and sync the private parent before declaring support. Probe mutation may touch only the private namespace. Unsupported filesystem capability with provably clean probe cleanup returns unchanged; cleanup/sync uncertainty retains the authenticated private probe evidence and returns recovery required. Both cases leave Store initializer, quarantine, cache, loader, and target call counts at zero. Add a call-log test that asserts pure check -> private exchange/no-replace probe -> authenticated cleanup/sync -> initializer -> cache -> loader -> target exactly.

    After successful preflight, all cooperating zsh-pro instances addressing that canonical target root acquire the lock before authenticating private journal state and hold its descriptor through namespace mutation, postvalidation, journal advancement, journal-file/transaction-directory durability, and every changed-directory fsync; authentication evidence never survives release/reacquisition. Open/acquire/authentication failure causes zero target mutation and recovery-required retention. If the lock descriptor or retained transaction-directory descriptor is deliberately closed/discarded, invalidate all prior evidence and perform no subsequent mutation; do not claim a portable oracle for arbitrary mid-interval advisory-lock loss. Advisory locking does not mitigate a non-cooperating or external `.zshrc` writer: reauthenticate all recorded fields immediately before target mutation and use exchange/no-replace plus displaced-identity postvalidation. Changed parent, transaction directory, journal descriptor/content, transaction/schema, requested-link topology, or artifact basename/identity means zero target mutation/cleanup and recovery required.

    Enforce the prepared-state durability ladder on retained descriptors. Fsync and close the new bytes at exchangePeerBasename and independent evidence at candidateEvidenceBasename, then fsync the private transaction directory; failure here creates no prepared journal, makes zero target `atomicRenameAt` calls, leaves the target unchanged, and retains authenticated artifacts. Next write/fsync/close the strict prepared journal and fsync the transaction directory again; failure before that second directory sync makes zero target namespace calls and retains journal/artifacts as recovery required. Every journal state transition thereafter writes/fsyncs/closes the journal and fsyncs the transaction directory before its state can authorize the next action. After a namespace syscall, fsync every changed directory, including target parent and transaction directory when distinct, before reporting durable success/restoration or cleaning anything. Any file or directory sync uncertainty retains the journal and available artifacts with recovery required.

    For an existing regular target, establish `expectedTarget=originalSnapshot` and `expectedCandidate=independentInstallCandidateEvidence`. Immediately before `atomicRenameAt(...,atomicRenameExchange)`, regular-file-compare requested-link topology and target device/inode/digest/mode to expectedTarget, then regular-file-compare the physical exchange peer to expectedCandidate; either mismatch is a distinct zero-exchange result. After the single syscall, target must still regular-file-match expectedCandidate. Sample the same exchange peer as type-aware displacedObserved without assuming it remains regular: record device/inode/full mode/uid/gid for every type, digest regular bytes, and readlink symlinks between repeated no-follow lstat identity checks. Compare that evidence to expectedTarget and preserve it even when a different type reveals a late substitution. Reverse through that same peer only while target still matches expectedCandidate and peer still exactly matches displacedObserved; sync every directory changed by the reverse before reporting restored-durable-stale. After reverse, target is displacedObserved and peer is the regular expectedCandidate again. If either guard changes, the reverse call fails, or durability is uncertain, retain target, exchange peer, candidate evidence, and journal with recovery required. Generic `rollbackPromoted` must switch on the typed promotion outcome and must not destroy or relocate the retained occupant.

    Implement the absent-target branch separately. Journal original absence and authenticate exchangePeerBasename against expectedCandidate, then call no-replace; `EEXIST` is unchanged stale state with the late target and exchange peer preserved. After success, target contains expectedCandidate and exchangePeerBasename is absent. This phase has no separately audited atomic identity-conditional inverse for restoring absence, so any later rollback request returns recovery required with authenticated target, candidate evidence, and journal retained; it must not unlink, move, or weaken that classification. Exchange remains forbidden when one side is absent.

    Define durability/crash seams at candidate-or-evidence file fsync/close, artifact transaction-directory fsync before journal creation, prepared-journal file fsync/close, prepared-journal transaction-directory fsync before the target namespace syscall, immediately after syscall return but before journal advancement, every journal-transition file fsync, every journal-transition transaction-directory fsync, and every changed-directory fsync after forward/reverse/no-replace. Keep `TestPromoteGuardedCandidateFsyncFailureBeforeJournalOrNamespace`, and add exact `TestPromoteGuardedArtifactDirectoryFsyncFailureBeforePreparedJournal`, `TestPromoteGuardedPreparedJournalDirectoryFsyncFailureBeforeNamespace`, and `TestPromotionJournalTransitionDirectoryFsyncFailureRetainsRecovery`. The first two assert zero target namespace calls and unchanged target at their distinct barriers; the transition test covers each journal state and asserts retained journal/artifacts plus recovery required. Integrate these rows into the eight top-level existing/absent crash rows. Next-run recovery first acquires the root lock, freshly reauthenticates target-parent and transaction-directory descriptors, journal descriptor/metadata/digest/schema/transaction, requested-link topology, exchangePeerBasename, and candidateEvidenceBasename. Existing pre-swap is target=expectedTarget and regular peer=expectedCandidate; existing post-swap is regular target=expectedCandidate and type-aware peer=displacedObserved, with displacedObserved recorded/authenticated and compared to expectedTarget; existing post-reverse is type-aware target=displacedObserved and regular peer=expectedCandidate. A reversed row whose journal transition or directory sync was interrupted is reauthenticated, resynced, and finalized without reopening/following a non-regular occupant. Absent pre-create is target absent and peer=expectedCandidate; absent post-create is target=expectedCandidate and peer absent. Completion/reversal is allowed only when one authenticated row matches exactly. No row names or creates an additional displaced-occupant artifact. An absent post-create row remains recovery required unless a separately audited identity-conditional inverse is added. Mixed/missing/extra/substituted state and any fsync ambiguity are recovery required with no mutation. Cleanup removes only authenticated regular candidate/exchange-peer/candidate-evidence/journal artifacts under the same held lock after durable success/restoration, fresh reauthentication, journal-file+transaction-directory durability, and sync of every changed directory; a non-regular displaced occupant is never a cleanup target and must first be guarded-reversed or retained. `TestInstallSecretBearingPeerConfinedAndRemovedOnFinalize` injects a runtime literal only into the temp source, proves evidence/journal/cache/output exclude it, and proves durable success leaves no transaction artifact. `TestInstallSecretBearingPeerRetainedOnlyForRecovery` injects failure, proves the only extra match is the authenticated peer beneath the mode-0700 transaction directory, and proves value-free recovery reporting.

    Bind expectedTarget to the exact original snapshot before candidate construction and capture expectedCandidate independently from the fsynced/closed candidateEvidenceBasename. Validate both axes immediately before the one promotion, then target/candidate-displaced axes afterward. Delete any API for a second/final candidate, a post-install expectedTarget resample, or a post-commit target promotion. Target uid/gid, timestamps, ACLs, xattrs, and flags do not participate; journal owner authentication remains separate. On any failure after promotion and before Commit, or on a typed Store noncommit, first classify/restore-or-retain the filesystem. Only unchanged or restored-durable may advance to `AbortIngest` when applicable, then loader/cache rollback, then initializer rollback; recovery-required/uncertain evidence retains all later resources and makes their compensation counters zero. A committed outcome finalizes the already-promoted candidate.

    Confine namespace effects in `install.go` and `install_transaction.go` to one named authenticated descriptor-relative helper. Add `TestInstallTransactionMutationSurfaceAudit`, an AST-based static audit that rejects direct rename, remove, recursive remove, link, root mutation, or raw atomic-adapter invocation anywhere in those two files except that helper. The helper may act only while the real per-root lock and retained transaction-directory descriptors are held, after candidate/evidence/journal file+transaction-directory durability and fresh authentication; it keeps the lock through postvalidation, journal transition durability, and sync of every directory changed by the namespace operation. Add helper-process tests proving same-root cooperating invocations block until release, different roots proceed, holder exit releases, and symlink/nonregular/wrong-mode lock entries fail before mutation. Keep the per-instance failure controller already required by repository patterns; do not add mutable package globals.

    Add deterministic tests for capability preflight ordering/isolation, probe cleanup uncertainty, candidate/evidence fsync failure, direct regular and non-regular substitution reversal/recovery, same-peer basename recovery, changed-name refusal to reverse, late absent-target creation, each of the four exact crash seams for existing and absent targets, parent-fsync uncertainty, changed parent/journal/requested-link authentication, in-place target mutation, root-lock serialization/rejection, unauthenticated-cleanup refusal, absent-post-create rollback refusal, stale full compensation, exact private modes under a restrictive umask, and the two independent one-promotion identity axes. `TestAtomicRenameFilesystemProbePrecedesInitializerAndEffects` records the exact pure-check/probe-cleanup/initializer/cache/loader/target order. `TestInstallPrivateArtifactsForceExactModesAfterCreate` sets a restrictive umask after preparing the public fixture and proves preflight plus transaction preparation still force and verify 0700 directories and 0600 private files. `TestInstallPromotionAuthenticatesNonRegularDisplacedOccupant` races in a symlink, proves typed link/inode/mode evidence, performs exactly forward plus guarded reverse, preserves the link, and finalizes only the regular candidate peer. `TestRecoverInstallPromotionAuthenticatesNonRegularDisplacedOccupant` interrupts reversed-journal durability and proves next-run type-aware reauthentication/resync/finalize without relocation or cleanup of the restored symlink. Expand `TestPromoteGuardedUnsupportedBeforeEffects` across platform-adapter and target-filesystem unsupported rows; `TestAtomicRenameUnsupportedLeavesStoreCacheLoaderTargetUnchanged` adds full initializer/cache/loader/target byte and counter assertions. `TestAtomicRenameProbeCleanupUncertaintyRetainsPrivateEvidence` proves only private probe evidence remains and later counters are zero. `TestPromotionJournalUsesOneExchangePeerBasename` proves the same physical peer holds candidate before swap, displacedObserved afterward, and candidate again after guarded reverse, while the schema has no additional recovery-only name. `TestInstallPromotionRejectsChangedTargetBeforeExchange` and `TestInstallPromotionRejectsChangedCandidateBeforeExchange` each assert zero exchange independently; `TestInstallPromotionPostSwapSeparatesCandidateAndDisplacedIdentity` proves target is checked against expectedCandidate while the peer is checked against displacedObserved/expectedTarget. `TestPromotionJournalRejectsChangedJournalIdentityOrDigest` is a table over descriptor identity, mode, uid, gid, digest, schema version, and transaction ID, while sibling parent/link/lock tests change their fields independently; every rejected row asserts zero target namespace and cleanup calls. Stale compensation must prove exact external target preservation, exact loader/cache restoration, fresh-store removal or preexisting-store preservation as appropriate, no parse/Commit/second-promotion call, and no unauthenticated artifact cleanup.

    Create `atomic_rename_supported_test.go` with `//go:build (linux || darwin) &amp;&amp; !phase6_test_unsupported_atomic`; move the direct exchange, absent no-replace, late-target, and per-call injected unsupported-error runtime tests there. Create `atomic_rename_unsupported_test.go` with `//go:build (!linux &amp;&amp; !darwin) || phase6_test_unsupported_atomic`; exact `TestAtomicRenameAtNativeUnsupportedZeroCalls` must call the unchanged production other-platform adapter and prove `ErrAtomicRenameUnsupported` plus zero raw calls. Run that exact test locally with `-tags phase6_test_unsupported_atomic`, after exact-name preflight under the same tag, and cross-compile the default FreeBSD amd64 package in Task 2. Add an AST/build-constraint assertion that the test-only selector excludes both supported production adapters and has no effect without the tag. Do not duplicate the common validation/parallel/ABI tests in either file. Run all landed ordinary-install tests unchanged.
  </action>
  <acceptance_criteria>
    - Existing-target promotion never makes the target pathname absent; one authenticated exchange-peer basename holds candidate bytes before exchange and the displaced original/substituted occupant afterward, with no additional recovery-only artifact.
    - Regular and non-regular late substitutions are type-aware authenticated and atomically reversed through that same peer only under matching candidate/displaced identities; otherwise both names are retained with recovery-required evidence and no non-regular entry is opened, cleaned, or relocated.
    - Absent targets use exclusive creation, preserve a late-created destination, and retain target/journal with recovery-required evidence after a successful create cannot be safely inverted.
    - Every crash/fsync row has a deterministic recover-or-refuse outcome; uncertainty never becomes success or exact rollback.
    - Candidate/evidence file durability is followed by transaction-directory durability; prepared-journal file durability is followed by another transaction-directory fsync before namespace mutation; every journal transition and changed-directory sync has a deterministic recovery-required failure row.
    - Pure adapter preflight and the authenticated same-filesystem exchange/no-replace probe both pass before Store initialization; unsupported or cleanup-uncertain rows leave Store/cache/loader/target unchanged and retain only evidence whose cleanup is unsafe.
    - Cooperating same-root processes serialize the full private journal authenticate/effect interval, while external target replacement remains in-scope and is preserved through reverse-or-retain.
    - Restrictive umask cannot weaken availability or make requested modes authoritative: all created private directories/files are explicitly chmod-applied and immediately type/mode-checked at 0700/0600 before use.
    - Local-GOOS exact-name preflight always includes common tests and exactly one supported/unsupported tagged platform list.
    - The exact other-platform adapter zero-call test executes locally under `phase6_test_unsupported_atomic`, while an ordinary FreeBSD amd64 build selects the same adapter and compiles without the test-only tag.
    - The one promotion keeps expectedTarget from the original source snapshot independent from expectedCandidate; separate target/candidate mismatch tests make zero exchange and post-swap checks bind each name to the correct evidence.
    - Existing ordinary-install tests plus the focused transaction suite pass.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; unsupported_test=TestAtomicRenameAtNativeUnsupportedZeroCalls; unsupported_listed=$(GOTOOLCHAIN=local go test -tags phase6_test_unsupported_atomic ./core/cli -list '^TestAtomicRenameAtNativeUnsupportedZeroCalls$'); test "$(printf '%s\n' "$unsupported_listed" | awk -v name="$unsupported_test" '$0 == name { n++ } END { print n + 0 }')" -eq 1; GOTOOLCHAIN=local go test -tags phase6_test_unsupported_atomic ./core/cli -run '^(TestAtomicRenameAtNativeUnsupportedZeroCalls)$' -count=1; common_tests='TestAtomicRenameCapabilityCheckIsSideEffectFree TestAtomicRenameAtRejectsUnsafeBasenames TestAtomicRenameBetweenAtAllowsEqualBasenamesInDifferentDirectories TestAtomicRenamePerCallInjectionIsParallelSafe TestAtomicRenameLinuxGo125ABIAndFallbackSurface'; local_goos=$(GOTOOLCHAIN=local go env GOOS); case "$local_goos" in linux|darwin) platform_tests='TestAtomicRenameAtExchangePreservesBothInodes TestAtomicRenameAtNoReplaceCreatesAbsentTarget TestAtomicRenameAtNoReplacePreservesLateTarget TestAtomicRenameAtUnsupportedLeavesBothNames' ;; *) platform_tests='TestAtomicRenameAtNativeUnsupportedZeroCalls' ;; esac; install_tests='TestInstallPrivateArtifactsForceExactModesAfterCreate TestAtomicRenameFilesystemProbePrecedesInitializerAndEffects TestAtomicRenameUnsupportedLeavesStoreCacheLoaderTargetUnchanged TestAtomicRenameProbeCleanupUncertaintyRetainsPrivateEvidence TestPromoteGuardedCandidateFsyncFailureBeforeJournalOrNamespace TestPromoteGuardedArtifactDirectoryFsyncFailureBeforePreparedJournal TestPromoteGuardedPreparedJournalDirectoryFsyncFailureBeforeNamespace TestPromotionJournalTransitionDirectoryFsyncFailureRetainsRecovery TestPromoteGuardedDetectsAndReversesSubstitution TestInstallPromotionAuthenticatesNonRegularDisplacedOccupant TestRecoverInstallPromotionAuthenticatesNonRegularDisplacedOccupant TestPromoteGuardedRefusesUnsafeReverseExchange TestPromoteGuardedNoReplacePreservesLateTarget TestPromoteGuardedFsyncFailureRequiresRecovery TestRecoverGuardedExistingBeforeNamespaceSyscall TestRecoverGuardedExistingAfterExchangeBeforeJournalAdvance TestRecoverGuardedExistingAfterJournalAdvanceBeforeParentSync TestRecoverGuardedExistingAfterParentSync TestRecoverGuardedAbsentBeforeNamespaceSyscall TestRecoverGuardedAbsentAfterNoReplaceBeforeJournalAdvance TestRecoverGuardedAbsentAfterJournalAdvanceBeforeParentSync TestRecoverGuardedAbsentAfterParentSync TestPromoteGuardedDetectsInPlaceMutation TestPromoteGuardedUnsupportedBeforeEffects TestTargetTransactionCrossProcessRootLock TestTargetTransactionRejectsUnsafeRootLockEntry TestPromotionCleanupRejectsUnauthenticatedArtifact TestPromotionJournalRejectsChangedParentIdentity TestPromotionJournalRejectsChangedJournalIdentityOrDigest TestPromotionJournalRejectsChangedRequestedLinkTopology TestPromotionJournalUsesOneExchangePeerBasename TestInstallSecretBearingPeerConfinedAndRemovedOnFinalize TestInstallSecretBearingPeerRetainedOnlyForRecovery TestRollbackGuardedAbsentAfterNoReplaceRequiresRecovery TestRollbackGuardedRefusesChangedTarget TestInstallTransactionMutationSurfaceAudit TestIngestStaleCompensation TestInstallPromotionRejectsChangedTargetBeforeExchange TestInstallPromotionRejectsChangedCandidateBeforeExchange TestInstallPromotionPostSwapSeparatesCandidateAndDisplacedIdentity'; tests="$common_tests $platform_tests $install_tests"; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s for GOOS=%s, found %s\n' "$test_name" "$local_goos" "$count" &gt;&amp;2; exit 1; }; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cli -run "^(${selector})$" -count=1; GOTOOLCHAIN=local go test ./core/cli -count=1; GOTOOLCHAIN=local go vet ./core/cli</automated>
  </verify>
  <done>The one canonical loader/install promotion uses audited atomic exchange/exclusive creation, retains displaced evidence, recovers deterministic crash states, and binds installed bytes to the exact original snapshot plus independent candidate evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Requested `.zshrc` path -> resolved target descriptor | Symlinks and parent pathnames can change after validation. |
| Validated target -> atomic exchange/exclusive-create syscall | A concurrent writer can replace, recreate, or mutate the target around prevalidation. |
| Process memory -> filesystem durability | Successful syscalls are not durable until file and directory metadata are synced. |
| Recovery journal -> next invocation | Stale or attacker-created artifacts must not become arbitrary path operations; journal ownership authentication is separate from bounded target snapshot equality. |
| Cooperating zsh-pro process -> private journal namespace | A real per-root advisory lock serializes fresh authentication through effect/postvalidation/sync. Non-cooperating same-UID/privileged mutation of private internals is outside that guarantee; arbitrary external target writers remain in scope. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V5.1 validation maps to marker/journal/basename checks; V12.3 file execution/storage maps to authenticated roots, strict regular-file checks for staged candidate/evidence/journal roles, no-follow type-aware evidence for displaced occupants, audited exchange/no-replace syscalls, and fail-closed recovery; V7.4 error handling maps to stable classified outcomes. Execution is blocked while any critical/high mitigate-disposition threat lacks its named automated test; the accepted private-journal same-UID/privileged boundary is explicit, while external target-writer preservation remains fully mitigated and tested.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-03-01 | Tampering | target parent/path substitution | high | mitigate | Retain the parent descriptor, atomically exchange target with one exchange-peer basename, regular-file-validate the candidate target independently from no-follow type-aware displacedObserved evidence, and reverse only through that same peer under matching evidence; a regular or non-regular late occupant is never opened unsafely, destroyed, lost, cleaned, or relocated to a second recovery name. |
| T-06-03-02 | Tampering | candidate/target in-place mutation | high | mitigate | Keep expectedTarget from the original source snapshot independent from canonical install-candidate evidence; distinct pre-swap target/peer and post-swap target/peer comparisons have separate zero-exchange tests. |
| T-06-03-03 | Repudiation | candidate/journal/exchange/no-replace/fsync ambiguity | high | mitigate | Retain the transaction-directory FD; require candidate/evidence file sync then directory sync, prepared-journal file sync then directory sync before namespace effects, file+directory sync for every transition, and all changed-directory syncs afterward. Three exact directory-fsync tests retain recovery on every uncertainty. |
| T-06-03-04 | Elevation of Privilege | recovery journal/artifacts | high | mitigate | Requested create modes are followed by explicit descriptor/root-relative chmod and immediate exact 0700/0600 type/mode checks; parent dev/inode, retained transaction-directory descriptor identity/mode/uid/gid, and journal descriptor identity/mode/uid/gid/digest/schema/transaction/link-topology authentication precede every mutation or cleanup. |
| T-06-03-05 | Denial of Service | interrupted target transition | medium | mitigate | Durable pre-transition evidence plus the target/exchange-peer identity matrix recovers a complete pre/post/reversed state or fails closed without an additional recovery-only artifact. |
| T-06-03-06 | Information Disclosure | post-END source bytes | medium | mitigate | Preserve bytes in place, include eligible entries in complete-Profile classification/commit, and expose only a fixed value-free append warning. |
| T-06-03-07 | Tampering | target mutation surface | high | mitigate | One authenticated descriptor helper owns namespace effects; an AST audit rejects direct rename/remove/link/root mutation elsewhere in install.go and install_transaction.go. |
| T-06-03-08 | Repudiation | absent-target rollback | high | mitigate | After successful no-replace, retain authenticated target/journal and report recovery required because no audited identity-conditional inverse is available. |
| T-06-03-09 | Tampering | cooperating concurrent journal mutation | high | mitigate | A descriptor-backed per-root exclusive OS lock spans each private journal authenticate/effect/postvalidate/sync interval; helper-process tests cover same/different roots, holder exit, and unsafe lock entries. |
| T-06-03-10 | Tampering | non-cooperating same-UID/privileged private-journal writer | high | accept | Advisory locking is a cooperating-process contract and makes no private-internal exclusion guarantee beyond OS account isolation; detected mismatches still retain recovery evidence, while external target occupants remain fully in scope. |
| T-06-03-11 | Tampering | unsupported target-filesystem atomic capability | high | mitigate | Pure adapter check precedes a real same-filesystem private exchange/no-replace probe; exact ordering/isolation tests prove unsupported or uncertain cleanup leaves Store/cache/loader/target unchanged and retains authenticated private evidence. |
| T-06-03-SC | Tampering | Go toolchain/package execution | high | mitigate | Every verifier and syscall audit rejects non-local or non-Go-1.25.x execution; `GOTOOLCHAIN=local` forbids automatic downloads and no package-manager/module change is planned. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Idempotent managed-block install plus append detection | 06-03 Tasks 1-3 | COVERED | Installer composition, topology table, and exact source/promotion semantics. |
| REQ | PROF-03 | Secret-safe end-to-end ingest | 06-01/06-02 store, then 06-03 transaction carrier | COVERED | Store redaction remains authoritative; the unavoidable full-byte peer is privately confined, value-free, durably removed on success/restore, and retained only for recovery. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| REVIEW | R-07 | Local Go 1.25.x only | 06-03 Tasks 1-3 | COVERED | Every independent verifier preflights local GOVERSION; syscall audit and all cross-build/test/vet commands run with `GOTOOLCHAIN=local`. |
| CONTEXT | D-05 | Complete Profile persistence; EffectiveManaged projection only | 06-03 Task 1, 06-04 | COVERED | Original eligible bytes feed one ordered Profile; this plan never filters persistence by activation state. |
| CONTEXT | D-06 | No physical master/complement | 06-03 Task 1 | COVERED | Exact outside-marker byte fixtures prove ordinary managed, imperative, opaque, comment, and gap bytes all remain in place. |
| CONTEXT | D-07 | Reuse landed installer | 06-03 Tasks 1-3 | COVERED | Shared topology/order, bounded snapshot, one authenticated mutation helper, and exact existing/absent crash rows. |
| CONTEXT | D-10 | Initialize then use fixed main baseline | 06-03 Task 1, 06-01 | COVERED | Initializer precedes cache; Begin consumes main. |
| CONTEXT | D-11 | Warn for post-END appends without mutation/error | 06-03 Task 1 | COVERED | Exactly one warning; bytes preserved in place and eligible entries remain in complete-Profile input. |
| CONTEXT | D-12 | Exact marker-line scan | 06-03 Task 1 | COVERED | Full-line byte scanner. |
| CONTEXT | D-14 | Deterministic fixtures | 06-03 Tasks 1-3, 06-05 | COVERED | Exact topology/byte/syscall/eight-row crash and changed-evidence tests here; fixture provenance in 06-05. |
| CONTEXT | D-15 | Same injected store at composition root | 06-03 Task 1, 06-04 | COVERED | Neutral seam extended; exact identity wired next. |
| CONTEXT | D-16 | Extend landed store injection seam | 06-03 Task 1 | COVERED | No parallel abstraction or concrete-store import. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC's fidelity and safety edges are projected into frontmatter: a regular or non-regular occupant changed after validation may be exchanged to the authenticated exchange-peer basename but must never be opened unsafely, destroyed, lost, cleaned, or relocated to a second recovery name; post-END content is never removed or filtered from complete-Profile persistence merely because of its location. Postvalidation must type-aware authenticate and guarded-reverse through that same peer or retain both names with recovery-required evidence; the named tests resolve both edges before completion.
</spec_fallback>

<verification>
Run exact-name preflighted order/layout/one-promotion/recovery tests, including live and next-run non-regular displaced-occupant rows, the complete CLI suite, and vet. The static mutation-surface test must prove only the authenticated descriptor helper can change target/artifact names; verify the pure check and private same-filesystem probe precede initializer/cache/loader/target effects, no API resamples or conflates original expectedTarget/independent expectedCandidate, one exchange-peer basename owns the regular candidate and type-aware displaced roles, and no second/final target promotion API exists.
</verification>

<success_criteria>
- Shared-root initialization succeeds because store initialization precedes cache access.
- Byte/layout decisions match D-05 through D-07 and D-11/D-12 exactly: complete Profile input plus exact ordinary outside-marker preservation.
- Atomic exchange preserves an existing target continuously and type-aware authenticates, guarded-reverses, or retains any regular/non-regular late occupant at the same exchange-peer basename without unsafe opening, cleanup, loss, or relocation; exclusive creation preserves a late-created absent-target occupant.
- Candidate/evidence file+transaction-directory durability, prepared-journal file+transaction-directory durability, transition file+directory durability, all changed-directory syncs, eight recovery rows, and the three exact directory-fsync failure tests make durability claims truthful; absent post-create rollback remains recovery required.
- The one promotion uses the exact original snapshot only as expectedTarget and independent install-candidate evidence only as expectedCandidate; distinct pre/post comparisons prove neither identity axis can substitute for the other.
- Every independent verifier refuses non-local or non-Go-1.25.x execution; no automatic toolchain download can satisfy a Phase 6 gate.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md` when done.
</output>
