---
phase: 06-ingest-end-to-end
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - core/cli/cli.go
  - core/cli/ingest.go
  - core/cli/ingest_test.go
  - core/dto/ingest.go
  - core/cmd/zsh-pro/main.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-01/D-02: `zsh-pro ingest [path] [--json]` defaults to `~/.zshrc`, accepts at most one positional path, rejects unknown flags and a second path with exit 2, and dispatches through the landed `CLI.Run(args []string, stdout, stderr io.Writer) int`; ingest runtime/conflict/partial/recovery failures exit 1."
    - "D-03/D-04/D-08: human and JSON results deterministically reconcile every eligible source statement, expose managed/master counts, value-free withheld name/line metadata and warnings, and JSON emits exactly one object on stdout on both success and failure."
    - "D-05/D-06: orchestration calls the shipped provider and `ir.Build`, carries forward only durable EffectiveManaged baseline entries on re-ingest, routes current imperative/opaque extents verbatim to the master, and never executes source text."
    - "D-09/D-10: store commit uses the fixed message and exact Begin revision. Committed, not-committed, conflict, and recovery-required states come from the typed outcome; a plain error never authorizes rollback or success."
    - "Before store commit the controller validates the exact post-install snapshot embedded in the final candidate. After a committed outcome, final promotion uses that same snapshot; a stale target is preserved and reported as a partial runtime result."
    - "D-15/D-16: the composition root constructs one `*store.Store`; the exact same pointer performs initialization, Begin/Commit/Abort, and all existing store-backed verbs, while `core/cli` imports no concrete store/provider implementation."
  artifacts:
    - path: "core/cli/ingest.go"
      provides: "Parse/build/merge, transactional controller, classified compensation matrix, fixed safe result rendering"
      exports: ["runIngest"]
    - path: "core/dto/ingest.go"
      provides: "Stable value-free ingest JSON result and state fields"
      contains: "IngestResult"
    - path: "core/cli/cli.go"
      provides: "Strict ingest argument parser and CLI.Run dispatch"
      contains: "case \"ingest\""
    - path: "core/cmd/zsh-pro/main.go"
      provides: "Single concrete store/provider composition root"
      contains: "cliStore"
    - path: "core/cli/ingest_test.go"
      provides: "Usage/output, transaction-state, rerun, conflict, overlap, and snapshot-binding tests"
  key_links:
    - from: "CLI.Run ingest parser"
      to: "runIngest"
      via: "one expanded boundary path plus JSON mode; invalid arguments never enter the transaction"
      pattern: "ingest"
    - from: "prepared source/layout + IngestBaseline"
      to: "provider.Parse -> ir.Build -> deterministic baseline/current-master merge"
      via: "EffectiveManaged is the sole partition predicate and persisted baseline identity drives rerun merge"
      pattern: "EffectiveManaged"
    - from: "postInstallSnapshot"
      to: "validate -> CommitIngest -> final promote"
      via: "one immutable expected snapshot; no resampling API"
      pattern: "postInstallSnapshot"
    - from: "IngestCommitOutcome.Status"
      to: "final promote / clean compensation / retained recovery state"
      via: "explicit matrix over ref/backend/object/initializer evidence"
      pattern: "RecoveryRequired"
    - from: "core/cmd/zsh-pro/main.go cliStore"
      to: "CLI store and initializer"
      via: "one constructed pointer captured directly; no second store.New call"
      pattern: "cliStore"
  prohibitions:
    - statement: "MUST NOT execute, source, eval, or command-substitute any input `.zshrc` text during ingest."
      category: security
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT include secret values, raw errors, raw paths, Git object IDs, backend identifiers, or zsh source text in human/JSON failure output."
      category: privacy
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Compose the store and filesystem transactions into the strict `ingest` CLI contract.

Purpose: Deliver the real Parse → Build → baseline commit → master rewrite on-ramp with deterministic accounting, safe output, truthful partial/recovery states, rerun semantics, and exact one-store composition-root wiring.
Output: Strict dispatch/argument handling, ingest DTO, transaction controller, deterministic merge/accounting/output, same-store main wiring, and focused controller tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md
@core/cli/cli.go
@core/cli/store.go
@core/cli/install.go
@core/cmd/zsh-pro/main.go
@core/ir/build.go
@core/ir/route.go
@core/model/profile.go

<interfaces>
Contracts the controller must use exactly:
- `CLI.Run(args []string, stdout, stderr io.Writer) int` owns verb dispatch and returns the integer form of the existing `model.ExitCode`; `fail` owns JSON-to-stdout versus human-to-stderr failures. Do not add a context parameter or otherwise change this public signature.
- `shell.Provider.Parse` plus `ir.Build` is the only parser/classifier path. Ingest performs no shell execution and adds no second classification rule.
- 06-01 returns an exact `IngestBaseline` and typed Abort evidence; 06-02 implements typed Commit outcomes. Expected/candidate refs are internal evidence, never output fields.
- 06-03 `prepareIngestInstallAt` retains original/layout state, attaches initializer/baseline, promotes initial install, returns exact `postInstallSnapshot`, prepares the final candidate against it, and returns classified promotion/rollback results.
- Existing exit codes remain 0 clean, 1 runtime, 2 usage, 3 actionable. This verb uses 0 for complete/warning success, 2 for invalid invocation, and 1 for runtime, conflict, partial, or recovery-required outcomes.
</interfaces>
</context>

## Artifacts this plan produces

- New `core/cli/ingest.go` and `core/cli/ingest_test.go`.
- New `core/dto/ingest.go` with no store, Git, backend, path, or source-text fields.
- Updated CLI dispatch and one-pointer composition-root wiring.
- No package installation, module change, network service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict arguments, safe output, and one-store wiring</name>
  <files>core/cli/cli.go, core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go, core/cmd/zsh-pro/main.go</files>
  <read_first>
    - core/cli/cli.go: `CLI.Run`, `runAnalyze`, `fail`, stdout/stderr ownership, and existing typed exit conventions
    - core/cli/store.go after 06-03: the reconciled narrow store/initializer interfaces
    - core/cmd/zsh-pro/main.go: current `cliStore`, provider/keychain construction, and initializer closure that must stop constructing a second store
    - core/dto: existing domain-to-wire naming and `omitempty` conventions
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-04, D-08, D-09, D-15, and D-16
  </read_first>
  <behavior>
    - `ingest`, `ingest --json`, `ingest path`, and either ordering of one path plus `--json` dispatch; no path uses `~/.zshrc` and expands home exactly once at the CLI boundary.
    - An unknown flag or second positional path returns ExitUsageErr (2). Human mode writes one fixed usage line to stderr; JSON mode writes exactly one valid object plus LF to stdout with `ok=false` and `exit_code=2`.
    - Success output includes managed entries, master-block lines, total/accounted statements, profile-committed/target-updated/recovery-required state, withheld name/start-line items, and warning strings. Human ordering and JSON field ordering are deterministic.
    - Every failure category uses a fixed zsh-pro-owned message selected from command plus typed exit/state. Output never contains an injected path, raw cause, secret, Git ref/object ID, backend key, or source text.
    - The composition root constructs one `*store.Store`; the initializer and CLI store interface call that exact object. Analyze remains usable when store initialization is deferred/fails.
  </behavior>
  <action>
    Add `case "ingest"` to the existing `CLI.Run(args []string, stdout, stderr io.Writer) int` per D-01/D-02 without changing that signature. Determine whether `--json` is present across the complete remaining argv before reporting any validation failure, then validate without permissive fallback: repeated `--json` is idempotent, one non-flag is the path, and an unknown dash-prefixed token or any second positional token returns usage exit 2. Thus `path-a path-b --json` still emits the required one JSON usage object. Expand the selected path with `util.ExpandHome` exactly once at this boundary and pass only the expanded result to transaction preparation; no IR/store helper expands paths.

    Define value-free DTOs per D-03/D-04/D-08: `ok`, `exit_code`, `profile_committed`, `target_updated`, `recovery_required`, `managed_entries`, `master_block_lines`, `source_statements`, `accounted_statements`, `withheld[{name,start_line}]`, and `warnings`. Keep all arrays present and deterministically ordered. Do not expose paths, Profile/zsh text, transaction tokens, refs, object IDs, backend kind/key, internal evidence enums, or errors. Encode one JSON object plus LF directly to CLI stdout in JSON mode. Human success prints a stable summary followed by withheld items in name/line order and warnings in source order.

    Parameterize the ingest failure renderer by command, exit code, and a closed internal reason enum. Invalid arguments use the fixed usage sentence and exit 2. Read/parse/preparation/commit conflict/clean non-commit/post-commit stale/recovery-required cases each map to fixed `zsh-pro ingest:` text and exit 1; pass only the safe mapped error to the existing `fail` helper. Never format `%v` from an underlying error into output. Tests seed distinct canary path/cause/secret/ref/backend strings and assert none appears in either stream.

    Fix `core/cmd/zsh-pro/main.go` per D-15/D-16. Construct the provider, keychain, and `cliStore := store.New(...)` once. Inject that pointer for existing store-backed verbs and capture that exact pointer in the initializer adapter so it calls `cliStore.InitForInstall`; remove the initializer's second `store.New` construction. Preserve deferred store-init failure behavior so `analyze` does not require a working store. Do not import the concrete store or zsh provider from `core/cli`.

    Add strict table tests for all valid argument orderings, unknown flags, duplicate paths, both output modes, success field/accounting order, every failure reason, and one injected fake store object whose identity-tagged initializer/Begin/Commit/Abort calls prove the seam does not switch instances.
  </action>
  <acceptance_criteria>
    - Every invalid argument form returns exit 2, including the second positional path in human and JSON modes.
    - Existing callers compile against `CLI.Run(args []string, stdout, stderr io.Writer) int`; no context/signature migration occurs.
    - JSON success/failure/usage produces exactly one valid object on stdout and no secret/raw/internal data.
    - Human output is deterministic and contains the locked counts, withheld lines, and warnings.
    - One exact injected store instance handles initialization and all ingest transaction calls.
    - Analyze behavior and existing store-backed CLI verbs remain green.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestRunIngestArguments TestRunIngestUsageHumanAndJSON TestRunIngestResultAccountingHumanAndJSON TestRunIngestSafeFailureMatrix TestCLIUsesOneInjectedStoreInstance' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/cli -run '^(TestRunIngestArguments|TestRunIngestUsageHumanAndJSON|TestRunIngestResultAccountingHumanAndJSON|TestRunIngestSafeFailureMatrix|TestCLIUsesOneInjectedStoreInstance)$' -count=1</automated>
  </verify>
  <done>The verb has strict exit-2 usage behavior, deterministic safe human/JSON output, and a composition root that uses one concrete store instance end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate exact-revision commit, reruns, and classified compensation</name>
  <files>core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/ingest.go after Task 1: argument result, DTO mapping, safe reason enum, and injected seams
    - core/cli/install_transaction.go after 06-03: exact source/layout, adoption mode, post-install snapshot, final-candidate binding, and classified rollback
    - core/model/ingest_transaction.go after 06-01: baseline, commit/abort status and ref/backend/object/initializer evidence
    - core/ir/build.go and core/ir/route.go: `ir.Build` and the landed `EffectiveManaged` routing rule
    - core/model/profile.go: entry identity/category/override/SecretRef shapes used for deterministic re-ingest merge
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-03, D-05 through D-11, D-13, and D-14
  </read_first>
  <behavior>
    - Flow is initializer on the injected store -> exact Begin baseline -> attach baseline/initializer -> initial loader/target promotion -> parse/build the retained eligible original bytes -> deterministic rerun merge/master -> final candidate bound to post-install snapshot -> exact pre-commit validation -> typed Commit -> final promotion or classified compensation.
    - No source is executed. `provider.Parse` and `ir.Build` classify eligible source once; EffectiveManaged is the only managed/master partition.
    - First adoption builds from the source. Installed-first-adoption excludes the shipped managed block but adopts eligible pre-marker source. Re-ingest carries forward durable EffectiveManaged baseline entries, replaces matching identities with current managed source entries, rebuilds unmanaged entries/master from current source, and preserves valid stored SecretRefs without backend access.
    - A committed-empty `profile.json` uses re-ingest semantics. Changed imperative master bytes replace their previous current-source representation without duplicating durable managed entries.
    - Immediately before Commit, exact post-install target validation must pass. A mismatch aborts the store quarantine and runs only classified initial/cache/initializer compensation; the final candidate never receives a new expected snapshot.
    - Committed outcome proceeds to final promotion. Conflict/not-committed compensates only when store abort/outcome and filesystem evidence authorize it. Recovery-required retains target/cache/store/artifacts and exits 1. Post-commit stale preserves the external target, leaves `main` committed, and exits 1 with `profile_committed=true,target_updated=false`.
    - Two controllers ingesting different target paths against one store baseline produce one commit and one prepare conflict with no lost update; the loser restores its own target/cache only and performs no backend compensation against the winner.
  </behavior>
  <action>
    Implement `runIngest` as the single controller. After strict path handling, invoke the same-store initializer before cache work, prepare/recover the target transaction, call `BeginIngest(ctx,"main",initializer ownership)`, and attach both results before promotion. If any pre-promotion step fails, abort only resources whose typed ownership says they exist. Promote the shipped loader then initial `.zshrc` install via 06-03 and retain its exact post-install snapshot.

    Parse only the 06-03 eligible retained source bytes with the injected provider, call `ir.Build`, and partition solely by `Entry.EffectiveManaged()` per D-05/D-06. For first adoption, derive the commit Profile from only the EffectiveManaged subset and derive the master from the complementary source extents. For installed-first-adoption, ignore the shipped managed block and apply the same partition to eligible pre-marker source. For re-ingest, start with durable baseline EffectiveManaged entries (including structurally valid SecretRefs), replace same category/name identities with current EffectiveManaged source entries, and append new managed entries in source order; rebuild the master exclusively from current non-effective source extents and do not commit those entries. Reject ambiguous duplicate identities before effects rather than choosing silently. Use the fixed commit message `zsh-pro: ingest baseline`.

    Derive the master from exact current imperative/opaque extents retained by 06-03, preserve original order/bytes/StartLine, and prepare the final installer candidate against the exact post-install snapshot. Compute accounting from source statement extents: every eligible nonblank/noncomment statement has exactly one managed or master assignment; `managed_entries + master_block_lines` equals the locked eligible statement count, and `accounted_statements` equals `source_statements`. Treat a failed equality/duplicate assignment as an internal runtime failure before Commit. Post-END extents are neither counted nor committed and contribute only the D-11 warning.

    Immediately before store commit, validate the exact snapshot embedded in the final candidate. On mismatch, call `AbortIngest`, then apply the 06-03 rollback matrix only if abort cleanup, target restoration, cache ownership, and initializer eligibility are all authoritatively safe. Never substitute a new snapshot. Call `CommitIngest` and switch exhaustively on its typed status/evidence: committed proceeds; clean prepare conflict/not-committed rolls back the controller's initial effects only when `InitializerRollbackSafe` and filesystem evidence permit; recovery-required or incomplete evidence disarms destructive rollback and retains resources for recovery. Ignore neither a nonempty outcome nor its recovery flag because an accompanying error is nil/non-nil.

    After a committed outcome, promote the final candidate using the same embedded post-install snapshot. Promoted-durable returns exit 0. Restored-durable-stale or an external concurrent target returns exit 1 with committed true/target false and preserves that target. Filesystem recovery-required returns exit 1 with committed true/recovery true and retains journal/artifacts. Finalization removes only authenticated owned artifacts after both store and target durability are proven. Warning-only post-END content remains exit 0.

    Add state-matrix tests for pre-commit stale, prepare conflict, known non-commit, observed-candidate commit, store recovery-required, final promoted, final stale, and final sync ambiguity. Add two different-target concurrent controllers, explicit shared root, first/installed/re-ingest, committed-empty, changed master, valid stored SecretRef, append warning, and accounting mismatch tests. Assert outcome flags, exit codes, exact target/master bytes, ref winner, backend call counts, rollback order, and retained evidence.
  </action>
  <acceptance_criteria>
    - The controller follows the complete loader/target/store/final-target transaction with exact expected revisions and snapshots.
    - Reruns retain durable managed state without duplicate entries and rebuild imperative master from current source.
    - A valid stored SecretRef rerun performs no Retrieve/Store/Delete and emits no second withheld item.
    - Different-target concurrent ingests yield one commit/one conflict and no compensation over the winner.
    - Recovery-required never triggers destructive target/cache/store rollback.
    - Post-commit stale preserves the concurrent target and reports truthful partial state.
    - Zero-dropped accounting and D-11 warning behavior are falsifiable in both output modes.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestRunIngestTransactionOutcomeMatrix TestRunIngestDifferentTargetsPrepareConflict TestRunIngestRecoveryRequiredSkipsDestructiveRollback TestRunIngestFinalSnapshotBinding TestRunIngestIdempotentSecretRef TestRunIngestChangedMaster TestRunIngestCommittedEmptyBaseline TestRunIngestExplicitSharedRoot TestRunIngestAppendWarning' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/cli -run '^(TestRunIngestTransactionOutcomeMatrix|TestRunIngestDifferentTargetsPrepareConflict|TestRunIngestRecoveryRequiredSkipsDestructiveRollback|TestRunIngestFinalSnapshotBinding|TestRunIngestIdempotentSecretRef|TestRunIngestChangedMaster|TestRunIngestCommittedEmptyBaseline|TestRunIngestExplicitSharedRoot|TestRunIngestAppendWarning)$' -count=1 &amp;&amp; GOTOOLCHAIN=auto go test ./core/cli ./core/cmd/zsh-pro -count=1 &amp;&amp; GOTOOLCHAIN=auto go vet ./core/cli ./core/cmd/zsh-pro</automated>
  </verify>
  <done>The ingest controller delivers exact-revision baseline commits and exact-snapshot target updates, handles all rerun/race/partial states without unsafe inference, and exposes deterministic value-free results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI argv/path -> filesystem transaction | Untrusted arguments select a file and output mode. |
| `.zshrc` bytes -> provider/IR | Shell text must be parsed/classified statically and never executed. |
| Profile/store/filesystem outcomes -> user output | Secrets, paths, refs, backend identifiers, raw causes, and source text must not cross. |
| Store commit -> final target promotion | A durable profile may coexist with a stale or uncertain target and must be reported truthfully. |
| Composition root -> CLI interfaces | A second store instance would split init/transaction state. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V5.1 maps to strict argv/path and closed outcome validation; V5.2 maps to static parsing with no shell execution; V7.4 maps to fixed safe errors and complete state logging/output; V8.3 maps to value-free withheld results. Execution is blocked while any critical/high threat lacks the named automated test.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-04-01 | Elevation of Privilege | source ingestion | critical | mitigate | Only provider.Parse/ir.Build consume bytes; command-substitution canary tests prove zero execution. |
| T-06-04-02 | Information Disclosure | human/JSON failures/results | critical | mitigate | Closed DTO and fixed reason renderer omit values/raw paths/errors/refs/backend/source; canary matrix scans both streams. |
| T-06-04-03 | Tampering | outcome-to-rollback matrix | high | mitigate | Exhaustive typed state switch; recovery/incomplete evidence disarms destructive rollback and retains resources. |
| T-06-04-04 | Tampering | pre-commit/final target snapshot | high | mitigate | One post-install snapshot is embedded, validated before commit, and reused for final promotion; no resampling. |
| T-06-04-05 | Spoofing | split store instances | high | mitigate | One composition-root pointer supplies initializer and every store method; fake identity and main wiring tests prove it. |
| T-06-04-06 | Repudiation | partial result | medium | mitigate | DTO always records profile-committed/target-updated/recovery-required alongside exit code. |
| T-06-04-SC | Tampering | package installation | low | accept | No package-manager command or dependency change is planned. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Full real-file Parse → Build → baseline/master composition | 06-04 Tasks 1-2 | COVERED | Transactional CLI on-ramp. |
| REQ | PROF-03 | Secret-safe end-to-end ingest and report | 06-04 Tasks 1-2 with 06-01/06-02 | COVERED | Store excludes; CLI exposes name/line only. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| CONTEXT | D-01 | `ingest [path] [--json]`, default path | 06-04 Task 1 | COVERED | Strict one-positional parser. |
| CONTEXT | D-02 | CLI.Run, typed exits, one JSON object | 06-04 Task 1 | COVERED | Exit 2 usage; exit 1 runtime/partial/recovery. |
| CONTEXT | D-03 | Deterministic human reconciliation | 06-04 Tasks 1-2 | COVERED | Counts plus invariant. |
| CONTEXT | D-04 | Structured JSON envelope | 06-04 Task 1 | COVERED | Dedicated value-free DTO. |
| CONTEXT | D-05 | EffectiveManaged partition | 06-04 Task 2, 06-03 | COVERED | Sole routing predicate. |
| CONTEXT | D-06 | Imperative master/no duplication | 06-04 Task 2, 06-03 | COVERED | Current-source rebuild plus durable managed merge. |
| CONTEXT | D-07 | Reuse installer | 06-03, consumed by 06-04 Task 2 | COVERED | One transaction API. |
| CONTEXT | D-08 | Surface Store withheld report | 06-02 and 06-04 Task 1 | COVERED | Name/line only. |
| CONTEXT | D-09 | Commit failure is runtime/fail closed | 06-02 and 06-04 Tasks 1-2 | COVERED | Typed outcome strengthens plain-error handling. |
| CONTEXT | D-10 | Init then fixed main commit | 06-04 Task 2 | COVERED | Same-store initializer/Begin and fixed message. |
| CONTEXT | D-11 | Post-END warning, no failure/mutation | 06-03, surfaced by 06-04 | COVERED | Warning-only exit 0. |
| CONTEXT | D-12 | Exact marker scan | 06-03 | COVERED | Consumed layout. |
| CONTEXT | D-13 | Real E2E oracle | 06-05 | COVERED | Final executable proof. |
| CONTEXT | D-14 | Deterministic fixtures | 06-04 tests, 06-05 fixtures | COVERED | Unit matrices plus authored fixture. |
| CONTEXT | D-15 | Same injected concrete store | 06-04 Task 1 | COVERED | One pointer at main. |
| CONTEXT | D-16 | Extend landed seam | 06-03 and 06-04 Task 1 | COVERED | No parallel abstraction. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. This plan projects its static-only and privacy boundaries into frontmatter: no source execution, and no values/raw internal data in user output. The executor resolves both through the named canary matrices before completion.
</spec_fallback>

<verification>
Run exact-name preflighted argument/output/controller tests, full CLI/command suites, and vet. The final plan adds built-binary and real-store/provider proof; this plan must already prove every outcome-to-rollback branch with in-process deterministic seams.
</verification>

<success_criteria>
- Strict CLI behavior matches D-01 through D-04, including duplicate-path exit 2.
- Parse/build/merge/master/accounting behavior matches D-05/D-06 without shell execution.
- Secret exclusion/reporting and typed commit failure behavior match D-08/D-09.
- One exact store instance owns initialization and ingest transactions.
- Concurrent, stale, committed, non-committed, and recovery-required paths preserve authoritative external state.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-04-SUMMARY.md` when done.
</output>
