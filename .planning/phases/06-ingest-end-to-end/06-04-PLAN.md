---
phase: 06-ingest-end-to-end
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - core/cli/cli.go
  - core/cli/ingest.go
  - core/cli/ingest_test.go
  - core/dto/ingest.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-01/D-02: `zsh-pro ingest [path] [--json]` defaults to `~/.zshrc`, accepts at most one positional path, rejects unknown flags and a second path with exit 2, and dispatches through the landed `CLI.Run(args []string, stdout, stderr io.Writer) int`; ingest runtime/conflict/partial/recovery failures exit 1."
    - "D-03/D-04/D-08: human and JSON results deterministically reconcile every eligible source statement, expose separate managed-entry, master-statement, and physical master-block-line counts, value-free withheld name/line metadata and warnings, and JSON emits exactly one object on stdout on both success and failure."
    - "D-05/D-06: orchestration calls the shipped provider and `ir.Build`, carries forward only durable EffectiveManaged baseline entries on re-ingest, and renders the master from every ordered eligible source byte extent except the exact extents represented by EffectiveManaged entries in the committed Profile. A mixed imperative/parser-opaque fixture proves both survive in source order; ambiguous or unmapped association fails before commit and source is never executed."
    - "D-09/D-10: store commit uses the fixed message and exact Begin revision. Committed, not-committed, conflict, and recovery-required states come from the typed outcome; a plain error never authorizes rollback or success."
    - "All clean compensation is filesystem-first: only unchanged or restored-durable initial-promotion evidence may advance to AbortIngest (when pre-commit), loader/cache rollback, then initializer rollback. Recovery-required, sync-uncertain, unsafe-reverse, or absent no-replace evidence retains target/journal/Store quarantine/cache/initializer and gives later compensation counters zero."
    - "Before store commit the controller validates `expectedTarget` from the immutable post-install snapshot. Final promotion independently authenticates `expectedCandidate` from candidate evidence before and after exchange; neither identity axis is resampled or substituted for the other, and a stale target is preserved and reported as a partial runtime result."
    - "Filesystem preparation/promotion is durable only after 06-03's retained transaction-directory barriers: candidate/evidence file sync then directory sync, prepared-journal file sync then directory sync before namespace mutation, file+directory sync for every journal transition, and sync of every directory changed by a namespace syscall. Any failure is recovery-required retention, never success or destructive compensation."
    - "D-15/D-16: the composition root constructs one `*store.Store`; the exact same pointer issues the Store-bound initialization ID and performs main-only Begin/Commit/Abort plus existing store-backed verbs. A second Store rejects that ID before effects, proven by real command-package integration."
  artifacts:
    - path: "core/cli/ingest.go"
      provides: "Parse/build/merge, transactional controller, classified compensation matrix, fixed safe result rendering"
      exports: ["runIngest"]
    - path: "core/dto/ingest.go"
      provides: "Stable value-free ingest JSON result, separate statement/line accounting, and state fields"
      contains: "IngestResult"
    - path: "core/cli/cli.go"
      provides: "Strict ingest argument parser and CLI.Run dispatch"
      contains: "case \"ingest\""
    - path: "core/cmd/zsh-pro/main.go"
      provides: "Single concrete store/provider composition root"
      contains: "cliStore"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Real Store-bound initialization-ID identity proof across main composition, Begin, Commit, Abort, and second-Store rejection"
      contains: "TestMainInitToBeginUsesExactCLIStore"
    - path: "core/cli/ingest_test.go"
      provides: "Usage/output, transaction-state, rerun, conflict, overlap, and snapshot-binding tests"
  key_links:
    - from: "CLI.Run ingest parser"
      to: "runIngest"
      via: "one expanded boundary path plus JSON mode; invalid arguments never enter the transaction"
      pattern: "ingest"
    - from: "prepared source/layout + IngestBaseline"
      to: "provider.Parse -> ir.Build -> deterministic baseline/current-master merge"
      via: "EffectiveManaged is the sole partition predicate and persisted baseline identity drives rerun merge"
      pattern: "EffectiveManaged"
    - from: "postInstallSnapshot"
      to: "validate -> CommitIngest -> final promote"
      via: "immutable expectedTarget remains separate from expectedCandidate supplied by candidate evidence; no resampling or cross-axis substitution"
      pattern: "postInstallSnapshot"
    - from: "IngestCommitOutcome.Status"
      to: "final promote / clean compensation / retained recovery state"
      via: "filesystem restore-or-retain gate first, then typed Store outcome/Abort, loader/cache, and initializer evidence"
      pattern: "RecoveryRequired"
    - from: "core/cmd/zsh-pro/main.go cliStore"
      to: "CLI store and initializer"
      via: "one constructed pointer captured directly; real Store-bound ID test proves Begin/Commit/Abort identity and rejects a second Store"
      pattern: "cliStore"
  prohibitions:
    - statement: "MUST NOT execute, source, eval, or command-substitute any input `.zshrc` text during ingest."
      category: security
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT include secret values, raw errors, raw paths, Git object IDs, backend identifiers, or zsh source text in human/JSON failure output."
      category: privacy
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Compose the store and filesystem transactions into the strict `ingest` CLI contract.

Purpose: Deliver the real Parse → Build → baseline commit → master rewrite on-ramp with deterministic accounting, safe output, truthful partial/recovery states, rerun semantics, and exact one-store composition-root wiring.
Output: Strict dispatch/argument handling, ingest DTO, transaction controller, deterministic merge/accounting/output, same-store main wiring, and focused controller tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md
@core/cli/cli.go
@core/cli/store.go
@core/cli/install.go
@core/cmd/zsh-pro/main.go
@core/cmd/zsh-pro/main_test.go
@core/ir/build.go
@core/ir/route.go
@core/model/profile.go

<interfaces>
Contracts the controller must use exactly:
- `CLI.Run(args []string, stdout, stderr io.Writer) int` owns verb dispatch and returns the integer form of the existing `model.ExitCode`. Do not add a context parameter or otherwise change this public signature. Existing verbs may retain `fail`; ingest failures use a dedicated renderer whose only data inputs are a value-free `IngestResult` and closed safe-reason enum, never an underlying error.
- `shell.Provider.Parse` plus `ir.Build` is the only parser/classifier path. Ingest performs no shell execution and adds no second classification rule.
- 06-01 returns an exact `IngestBaseline` and typed Abort evidence; 06-02 implements typed Commit outcomes. Expected/candidate refs are internal evidence, never output fields.
- 06-03 `prepareIngestInstallAt` retains original/layout state, attaches initializer/baseline, promotes initial install, returns exact `postInstallSnapshot`, prepares the final candidate against it, and returns classified promotion/rollback results.
- Existing exit codes remain 0 clean, 1 runtime, 2 usage, 3 actionable. This verb uses 0 for complete/warning success, 2 for invalid invocation, and 1 for runtime, conflict, partial, or recovery-required outcomes.
</interfaces>
</context>

## Artifacts this plan produces

- New `core/cli/ingest.go` and `core/cli/ingest_test.go`.
- New `core/dto/ingest.go` with no store, Git, backend, path, or source-text fields.
- Updated CLI dispatch and one-pointer composition-root wiring.
- No package installation, module change, network service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict arguments and safe value-free output</name>
  <files>core/cli/cli.go, core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/cli.go: `CLI.Run`, `runAnalyze`, `fail`, stdout/stderr ownership, and existing typed exit conventions
    - core/cli/store.go after 06-03: the reconciled narrow store/initializer interfaces
    - core/dto: existing domain-to-wire naming and `omitempty` conventions
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-04, D-08, D-09, D-15, and D-16
  </read_first>
  <behavior>
    - `ingest`, `ingest --json`, `ingest path`, and either ordering of one path plus `--json` dispatch; no path uses `~/.zshrc` and expands home exactly once at the CLI boundary.
    - An unknown flag or second positional path returns ExitUsageErr (2). Human mode writes one fixed usage line to stderr; JSON mode writes exactly one valid object plus LF to stdout with `ok=false` and `exit_code=2`.
    - Success output includes managed entries, master statements, physical master-block lines, total/accounted statements, profile-committed/target-updated/recovery-required state, withheld name/start-line items, and warning strings. Human ordering and JSON field ordering are deterministic.
    - Every failure category uses a fixed zsh-pro-owned message selected by a closed safe reason. The ingest failure renderer accepts only a value-free IngestResult plus that reason, so JSON failure preserves all truthful status flags/counts/withheld/warnings while output cannot receive an injected path, raw cause, secret, Git ref/object ID, backend key, or source text.
  </behavior>
  <action>
    Add `case "ingest"` to the existing `CLI.Run(args []string, stdout, stderr io.Writer) int` per D-01/D-02 without changing that signature. Determine whether `--json` is present across the complete remaining argv before reporting any validation failure, then validate without permissive fallback: repeated `--json` is idempotent, one non-flag is the path, and an unknown dash-prefixed token or any second positional token returns usage exit 2. Thus `path-a path-b --json` still emits the required one JSON usage object. Expand the selected path with `util.ExpandHome` exactly once at this boundary and pass only the expanded result to transaction preparation; no IR/store helper expands paths.

    Define value-free DTOs per D-03/D-04/D-08: `ok`, `exit_code`, `profile_committed`, `target_updated`, `recovery_required`, `managed_entries`, `master_statements`, `master_block_lines`, `source_statements`, `accounted_statements`, `withheld[{name,start_line}]`, and `warnings`. `master_statements` counts routed imperative/opaque statement extents; `master_block_lines` independently counts physical lines in the rendered managed master block and may differ for multiline statements. Keep all arrays present and deterministically ordered. Do not expose paths, Profile/zsh text, transaction tokens, refs, object IDs, backend kind/key, internal evidence enums, or errors. Encode one JSON object plus LF directly to CLI stdout in JSON mode. Human success prints a stable summary followed by withheld items in name/line order and warnings in source order.

    Create an ingest-specific failure renderer with a closed signature equivalent to `(jsonMode bool, result dto.IngestResult, reason ingestSafeReason, stdout, stderr io.Writer) int`; it must not accept `error`, path, source, ref, backend, or arbitrary message parameters and must not route an underlying cause through the generic `fail` helper. Invalid arguments use one enum member, the fixed usage sentence, and exit 2. Read/parse/preparation/commit conflict/clean non-commit/post-commit stale/recovery-required members each map to fixed `zsh-pro ingest:` text and exit 1. JSON serializes the supplied value-free result, preserving profile/target/recovery flags, all four accounting counts, withheld metadata, and warnings, plus only the fixed safe reason/message; human mode selects fixed text and deterministic safe reconciliation lines. Never format an underlying error. Tests seed distinct canary path/cause/secret/ref/backend strings and assert none appears in either stream, including stale and recovery results with nonzero counts/warnings.

    Add strict table tests for all valid argument orderings, unknown flags, duplicate paths, both output modes, separate master-statement/master-line accounting order, every failure reason with preserved safe result fields, and one injected fake store object whose identity-tagged initializer/Begin/Commit/Abort calls prove the seam does not switch instances.
  </action>
  <acceptance_criteria>
    - Every invalid argument form returns exit 2, including the second positional path in human and JSON modes.
    - Existing callers compile against `CLI.Run(args []string, stdout, stderr io.Writer) int`; no context/signature migration occurs.
    - JSON success/failure/usage produces exactly one valid object on stdout and no secret/raw/internal data.
    - Human output is deterministic and contains separate managed-entry/master-statement/master-block-line counts, withheld lines, and warnings.
    - Analyze behavior and existing store-backed CLI verbs remain green.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestRunIngestArguments TestRunIngestUsageHumanAndJSON TestRunIngestResultAccountingHumanAndJSON TestRunIngestSafeFailureMatrix TestCLIUsesOneInjectedStoreInstance' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestRunIngestArguments|TestRunIngestUsageHumanAndJSON|TestRunIngestResultAccountingHumanAndJSON|TestRunIngestSafeFailureMatrix|TestCLIUsesOneInjectedStoreInstance)$' -count=1</automated>
  </verify>
  <done>The verb has strict exit-2 usage behavior, a closed ingest-only failure renderer that preserves value-free result state, and separate statement/line accounting without concrete store/provider imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove one exact Store owns initialization and every ingest transaction operation</name>
  <files>core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/cmd/zsh-pro/main.go: provider/keychain construction, `cliStore`, deferred construction error, and initializer closure that currently creates a second Store
    - core/cmd/zsh-pro/main_test.go: real command/store harnesses, installed-binary builder, and current GOTOOLCHAIN environment
    - core/store/install_transaction.go after 06-01: Store-bound InstallInitializationID and cross-Store rejection
    - core/cli/store.go after 06-03: neutral initializer and main-only Begin/Commit/Abort adapter surface
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-10, D-15, and D-16
  </read_first>
  <behavior>
    - Production constructs `cliStore := store.New(...)` exactly once and captures that exact pointer in the initializer adapter; it does not construct or retry with another Store inside initialization.
    - The Store-bound InstallInitializationID issued by `cliStore.InitForInstall` is accepted by public main-only Begin on that same pointer, and the same registry handles Commit and Abort terminalization.
    - A second Store over the same root rejects the first Store's initialization ID before registry, Git, backend, or filesystem effects.
    - Analyze remains available when store construction/initialization is deferred or fails, while store-backed verbs return the preserved construction failure rather than creating a replacement Store.
  </behavior>
  <action>
    Fix `core/cmd/zsh-pro/main.go` per D-15/D-16. Construct provider, keychain, and `cliStore := store.New(...)` once. Inject that exact pointer for existing store-backed verbs and capture it directly in the initializer adapter so it calls `cliStore.InitForInstall`; remove the initializer closure's second `store.New` call and any retry/fallback construction. Adapt the neutral CLI surface to public `BeginIngest(ctx, installInitializationID)` with no branch argument. The exact same pointer must own initialization ID issuance, provisional reservation, CommitIngest, AbortIngest, and terminal evidence. Preserve deferred store-construction errors so `analyze` stays independent and store-backed verbs return the original error. Do not import concrete store or zsh provider from `core/cli`.

    Add real command-package integration `TestMainInitToBeginUsesExactCLIStore` in `core/cmd/zsh-pro/main_test.go`. Use a temp real Store and deterministic backend through the actual main composition helper. In a success subtest, InitForInstall issues an ID, public Begin reserves main on the exact `cliStore`, and Commit reaches an immutable terminal result. In a forced post-Begin failure subtest, the same pointer performs Abort and aggregate initializer eligibility sees that terminal record. Construct a second Store over the same canonical root and prove it rejects the first Store's ID before registry/Git/backend/filesystem counters change. Instrument the composition factory locally/per-test so a second production `store.New` call fails the test immediately; do not add mutable package globals. Update the built-binary helper in this owned test file to force `GOTOOLCHAIN=local`, and make it reject `go env GOVERSION` unless it is Go 1.25.x before invoking any build.

    Replace the existing main-test installed-binary environment's `GOTOOLCHAIN=local` with `GOTOOLCHAIN=local`. The helper must verify local `go env GOVERSION` matches Go 1.25.x before building and must not download a toolchain. Keep project-wide Makefile/CLAUDE conventions untouched.
  </action>
  <acceptance_criteria>
    - The production composition root constructs one Store pointer and captures it for initializer plus all store-backed operations.
    - Real integration proves the Store-issued ID crosses Init -> main-only Begin -> Commit/Abort only on that exact pointer.
    - A second Store rejects the ID with zero effects, and no test relies only on fake interface identity.
    - Analyze remains independent of deferred/failed Store initialization.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestMainInitToBeginUsesExactCLIStore' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -run '^(TestMainInitToBeginUsesExactCLIStore)$' -count=1</automated>
  </verify>
  <done>The actual composition root, not only a fake CLI seam, proves one exact Store-issued ID and registry own Init, main Begin, Commit, Abort, and cross-Store rejection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestrate exact-revision commit, reruns, and classified compensation</name>
  <files>core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/ingest.go after Task 1 and command composition after Task 2: argument result, DTO mapping, safe reason enum, and exact-store injected seams
    - core/cli/install_transaction.go after 06-03: exact source/layout, adoption mode, post-install snapshot, final-candidate binding, and classified rollback
    - core/model/ingest_transaction.go after 06-01: baseline, commit/abort status and ref/backend/object/initializer evidence
    - core/ir/build.go and core/ir/route.go: `ir.Build` and the landed `EffectiveManaged` routing rule
    - core/model/profile.go: entry identity/category/override/SecretRef shapes used for deterministic re-ingest merge
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-03, D-05 through D-11, D-13, and D-14
  </read_first>
  <behavior>
    - Flow is side-effect-free atomic adapter check -> authenticated target-filesystem private probe -> initializer on the injected store -> exact Begin baseline -> attach baseline/initializer -> initial loader/target promotion -> parse/build retained eligible original bytes -> deterministic rerun merge/master -> independent expectedTarget/expectedCandidate preparation -> exact pre-commit validation -> typed Commit -> final promotion or classified compensation.
    - No source is executed. `provider.Parse` and `ir.Build` classify eligible source once; EffectiveManaged is the only managed/master partition.
    - First adoption builds from the source. Installed-first-adoption excludes the shipped managed block but adopts eligible pre-marker source. Re-ingest carries forward durable EffectiveManaged baseline entries, replaces matching identities with current managed source entries, and preserves valid stored SecretRefs without backend access. In every mode, the master is every ordered eligible current-source byte extent minus only exact extents represented by EffectiveManaged entries in the resulting committed Profile.
    - A committed-empty `profile.json` uses re-ingest semantics. Changed imperative master bytes replace their previous current-source representation without duplicating durable managed entries.
    - Immediately before Commit, exact expectedTarget validation must pass. Every failure after initial promotion and before Commit goes through one `compensatePreCommitFilesystemFirst` helper: classify and restore-or-retain the filesystem first; only unchanged/restored-durable may call AbortIngest, then roll back loader/cache, then initializer. Any recovery-required/uncertain/unsafe-reverse/absent-post-create result stops there with Abort/loader/cache/initializer counters zero.
    - Committed outcome proceeds to final promotion with expectedTarget and expectedCandidate kept independent. Conflict/not-committed uses the same filesystem-first gate, then its typed Store outcome in place of Abort, before loader/cache and initializer rollback. Recovery-required retains target/journal/Store quarantine/cache/initializer and exits 1. Post-commit stale preserves the external target, leaves `main` committed, and exits 1 with `profile_committed=true,target_updated=false`.
    - Two controllers ingesting different target paths against one store baseline produce one commit and one prepare conflict with no lost update; the loser restores its own target/cache only and performs no backend compensation against the winner.
  </behavior>
  <action>
    Implement `runIngest` as the single controller. After strict path handling, run 06-03's side-effect-free adapter check and authenticated same-filesystem private exchange/no-replace probe before invoking the exact same-store initializer, Begin, cache, loader, or target effects. Unsupported and probe-cleanup-uncertain outcomes return the typed failure with Store/quarantine/cache/loader/target counters and bytes unchanged; cleanup uncertainty retains only authenticated private probe evidence. After preflight, invoke the initializer before cache work, prepare/recover the target transaction, call public main-only `BeginIngest(ctx, installInitializationID)` with no branch argument, and attach both results before promotion. If any later pre-promotion step fails, terminalize only resources whose typed ownership says they exist; remember that Begin reserves before I/O and returns immutable failure evidence. Promote the shipped loader then initial `.zshrc` install via 06-03 and retain its exact postInstallSnapshot as expectedTarget.

    Parse only the 06-03 eligible retained source bytes with the injected provider, call the total `ir.Build` function, and partition solely by `Entry.EffectiveManaged()` per D-05/D-06. Do not invent an `ir.Build` error branch or failure injection: parse may fail, while Build returns its deterministic result. Model retained input as ordered byte-addressed extents and map each current entry to its exact extent without introducing another classifier. For first adoption, derive the commit Profile from selected EffectiveManaged entries. For installed-first-adoption, ignore the shipped managed block and apply the same partition to eligible pre-marker source. For re-ingest, start with durable baseline EffectiveManaged entries (including structurally valid SecretRefs), replace same category/name identities with current EffectiveManaged source entries, and append new managed entries in source order. In all modes, compute the master by removing only exact extents represented by those resulting committed EffectiveManaged entries; do not positive-select recognized imperative kinds or reconstruct bytes from `Entry.Text`. Imperative, parser-opaque, forced-unmanaged, comment, and gap bytes therefore survive verbatim. Reject ambiguous duplicate identities or ambiguous/unmapped entry-to-extent association before Commit rather than choosing or dropping silently. Use the fixed commit message `zsh-pro: ingest baseline`.

    Preserve the resulting master extents' original order/bytes/StartLine and prepare the final installer transaction with two independent values: expectedTarget is the exact postInstallSnapshot, while expectedCandidate comes only from the fsynced/closed independent candidate evidence. Final-candidate preparation is not complete until candidate/evidence files and the retained private transaction directory are durable, then the prepared journal file and transaction directory are durable; failure at either directory barrier is typed recovery-required candidate-preparation failure before Commit. Compute accounting from statement extents: every eligible nonblank/noncomment source statement has exactly one managed or master assignment; `master_statements` is the number of routed master statement extents, `master_block_lines` is the separately rendered physical line count, and `accounted_statements = managed_entries + master_statements = source_statements`. Never substitute master-block line count into that invariant. Treat a failed equality/duplicate assignment as an internal runtime failure before Commit. An exact fixture containing both an imperative statement and parseable parser-opaque `export $FLAGS FOO=1` must prove their raw bytes and StartLine values appear in master source order while the committed EffectiveManaged declaration extent does not. A multiline fixture must prove `master_statements != master_block_lines` while the statement invariant holds. Post-END extents are neither counted nor committed and contribute only the D-11 warning.

    Introduce one common `compensatePreCommitFilesystemFirst` helper for every failure after initial promotion and before `CommitIngest`: parse failure, duplicate/ambiguous identity or extent association, accounting mismatch, final-candidate preparation, and expectedTarget validation. Its event order is exact: classify the initial filesystem promotion; restore it when authorized or prove it unchanged; only restored-durable/unchanged advances to `AbortIngest`; only a clean Abort advances to loader/cache rollback; only clean loader/cache restoration advances to initializer rollback. Recovery-required, sync uncertainty, unsafe reverse, or absent no-replace retains target, journal, Store quarantine, cache, and initializer state and returns immediately with Abort/loader/cache/initializer counters zero. Any retained/uncertain result at a later gate stops the remaining destructive compensation. No branch in that interval may inline a different sequence or call Commit after the helper. Never substitute a new expectedTarget or expectedCandidate.

    Call `CommitIngest` only after the accounting invariant and expectedTarget pass, then switch exhaustively on its typed status/evidence: committed proceeds; prepare conflict/not-committed first applies the same filesystem restore-or-retain classification, then uses the typed Store outcome as the clean store gate before loader/cache and initializer rollback. Recovery-required or incomplete evidence disarms every later destructive rollback and retains resources for recovery. Ignore neither a nonempty outcome nor its recovery flag because an accompanying error is nil/non-nil.

    After a committed outcome, promote the final candidate with the immutable expectedTarget plus independent expectedCandidate. Before exchange, target change and exchange-peer candidate change are distinct zero-exchange failures. After exchange, validate target against expectedCandidate and the same peer's displacedObserved identity against expectedTarget; guarded reverse/retention follows 06-03 without a second displaced-occupant name. Each journal transition must be file-synced and transaction-directory-synced, and every directory changed by forward/reverse/no-replace must be synced before the controller reports durability or finalizes artifacts. Promoted-durable returns exit 0 only after all barriers pass. A transition/directory sync failure returns exit 1 with committed true/recovery true and retains journal/artifacts; restored-durable-stale or an external concurrent target returns committed true/target false and preserves that target. Finalization removes only authenticated owned artifacts after both store and target durability are proven. Warning-only post-END content remains exit 0.

    Add state-matrix tests for pre-commit stale, prepare conflict, known non-commit, observed-candidate commit, store recovery-required, final promoted, final stale, and final sync ambiguity. Expand final-candidate preparation and promotion rows across artifact-directory sync before journal, prepared-journal directory sync before namespace, every journal-transition directory sync, and each changed-directory sync after namespace; pre-commit rows never call CommitIngest and post-commit rows report committed true/recovery true with artifacts retained. `TestRunIngestAtomicUnsupportedBeforeAnyProductEffects` covers pure-adapter unsupported, target-filesystem unsupported with durable probe cleanup, and probe-cleanup uncertainty; every row asserts initializer/Begin/cache/loader/target calls are zero and target bytes/inode/mode/link topology are unchanged, while only the uncertain row retains authenticated private evidence. `TestRunIngestPreCommitCompensationFilesystemFirst` covers parse, duplicate identity, accounting, candidate preparation, and expectedTarget failure with an event log that equals filesystem classify/restore-or-unchanged -> AbortIngest -> loader/cache -> initializer. `TestRunIngestPreCommitUnsafeFilesystemDisarmsLaterCompensation` covers recovery-required, sync uncertainty, unsafe reverse, absent no-replace, and transaction-directory durability uncertainty; every row retains target/journal/Store quarantine/cache/initializer and has Abort/loader/cache/initializer counters zero. Do not add a fictional Build-error case. Add `TestRunIngestImperativeAndOpaqueMasterSourceOrder` with the exact mixed fixture and `TestRunIngestSeparatesExpectedTargetAndCandidateEvidence` with independent target-change/candidate-change zero-exchange rows plus post-swap axis assertions. Add two different-target concurrent controllers, explicit shared root, first/installed/re-ingest, committed-empty, changed master, valid stored SecretRef, append warning, multiline accounting, and accounting mismatch tests. Stale/recovery renderer cases must preserve flags/counts/withheld/warnings while excluding raw canary errors. Assert outcome flags, exit codes, exact target/master bytes, ref winner, backend call counts, rollback order, and retained evidence.
  </action>
  <acceptance_criteria>
    - The controller follows preflight, loader/target/store/final-target transaction ordering with exact expected revisions and independent expectedTarget/expectedCandidate evidence.
    - Reruns retain durable managed state without duplicate entries and rebuild the master from all ordered eligible current-source extents except exact extents represented by EffectiveManaged committed entries; the mixed imperative/opaque fixture is byte- and provenance-exact.
    - A valid stored SecretRef rerun performs no Retrieve/Store/Delete and emits no second withheld item.
    - Different-target concurrent ingests yield one commit/one conflict and no compensation over the winner.
    - Pre-commit compensation is filesystem classify/restore-or-unchanged -> AbortIngest -> loader/cache -> initializer; recovery/uncertainty at any gate stops later compensation and preserves every retained resource.
    - Transaction-directory durability failures before Commit call no CommitIngest; transition or changed-directory sync failures after Commit preserve committed truth and report recovery with journal/artifacts retained.
    - Post-commit stale preserves the concurrent target and reports truthful partial state.
    - Zero-dropped accounting uses managed entries plus master statements, not physical master lines, and D-11 warning behavior is falsifiable in both output modes.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestRunIngestTransactionOutcomeMatrix TestRunIngestDifferentTargetsPrepareConflict TestRunIngestRecoveryRequiredSkipsDestructiveRollback TestRunIngestFinalSnapshotBinding TestRunIngestAtomicUnsupportedBeforeAnyProductEffects TestRunIngestPreCommitCompensationFilesystemFirst TestRunIngestPreCommitUnsafeFilesystemDisarmsLaterCompensation TestRunIngestImperativeAndOpaqueMasterSourceOrder TestRunIngestSeparatesExpectedTargetAndCandidateEvidence TestRunIngestParseFailureCompensates TestRunIngestDuplicateIdentityCompensates TestRunIngestAccountingMismatch TestRunIngestCandidatePreparationFailureCompensates TestRunIngestMultilineAccountingSeparatesStatementsAndLines TestRunIngestStaleFailureRendering TestRunIngestRecoveryFailureRendering TestRunIngestIdempotentSecretRef TestRunIngestChangedMaster TestRunIngestCommittedEmptyBaseline TestRunIngestExplicitSharedRoot TestRunIngestAppendWarning' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestRunIngestTransactionOutcomeMatrix|TestRunIngestDifferentTargetsPrepareConflict|TestRunIngestRecoveryRequiredSkipsDestructiveRollback|TestRunIngestFinalSnapshotBinding|TestRunIngestAtomicUnsupportedBeforeAnyProductEffects|TestRunIngestPreCommitCompensationFilesystemFirst|TestRunIngestPreCommitUnsafeFilesystemDisarmsLaterCompensation|TestRunIngestImperativeAndOpaqueMasterSourceOrder|TestRunIngestSeparatesExpectedTargetAndCandidateEvidence|TestRunIngestParseFailureCompensates|TestRunIngestDuplicateIdentityCompensates|TestRunIngestAccountingMismatch|TestRunIngestCandidatePreparationFailureCompensates|TestRunIngestMultilineAccountingSeparatesStatementsAndLines|TestRunIngestStaleFailureRendering|TestRunIngestRecoveryFailureRendering|TestRunIngestIdempotentSecretRef|TestRunIngestChangedMaster|TestRunIngestCommittedEmptyBaseline|TestRunIngestExplicitSharedRoot|TestRunIngestAppendWarning)$' -count=1 &amp;&amp; GOTOOLCHAIN=local go test ./core/cli ./core/cmd/zsh-pro -count=1 &amp;&amp; GOTOOLCHAIN=local go vet ./core/cli ./core/cmd/zsh-pro</automated>
  </verify>
  <done>The ingest controller delivers exact-revision baseline commits and exact-snapshot target updates, handles all rerun/race/partial states without unsafe inference, and exposes deterministic value-free results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| CLI argv/path -> filesystem transaction | Untrusted arguments select a file and output mode. |
| `.zshrc` bytes -> provider/IR | Shell text must be parsed/classified statically and never executed. |
| Profile/store/filesystem outcomes -> user output | Secrets, paths, refs, backend identifiers, raw causes, and source text must not cross. |
| Store commit -> final target promotion | A durable profile may coexist with a stale or uncertain target and must be reported truthfully. |
| Composition root -> CLI interfaces | A second store instance would split init/transaction state. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V5.1 maps to strict argv/path and closed outcome validation; V5.2 maps to static parsing with no shell execution; V7.4 maps to fixed safe errors and complete state logging/output; V8.3 maps to value-free withheld results. Execution is blocked while any critical/high threat lacks the named automated test.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-04-01 | Elevation of Privilege | source ingestion | critical | mitigate | Only provider.Parse/ir.Build consume bytes; command-substitution canary tests prove zero execution. |
| T-06-04-02 | Information Disclosure | human/JSON failures/results | critical | mitigate | Ingest-specific renderer accepts only the value-free result and a closed safe reason; JSON preserves flags/counts/withheld/warnings while canary matrices scan both streams. |
| T-06-04-03 | Tampering | pre-commit compensation/outcome matrix | high | mitigate | One compensatePreCommitFilesystemFirst helper enforces filesystem unchanged/restored-durable -> AbortIngest -> loader/cache -> initializer; recovery/uncertainty retains all resources and keeps later counters zero. |
| T-06-04-04 | Tampering | pre-commit/final target identity | high | mitigate | expectedTarget comes only from postInstallSnapshot and expectedCandidate only from independent evidence; separate pre-swap mismatch and post-swap axis tests forbid resampling or conflation. |
| T-06-04-05 | Spoofing | split store instances | high | mitigate | One composition-root pointer supplies initializer and every store method; `TestMainInitToBeginUsesExactCLIStore` proves actual main wiring, second-Store rejection, Commit, and Abort. |
| T-06-04-06 | Repudiation | partial result/accounting | medium | mitigate | DTO always records profile-committed/target-updated/recovery-required plus managed_entries/master_statements/master_block_lines/source/accounted counts alongside exit code. |
| T-06-04-07 | Repudiation | filesystem transaction-directory durability | high | mitigate | Controller treats candidate/journal/transition transaction-directory sync and every changed-directory sync failure as retained recovery; outcome-matrix rows distinguish pre-commit zero-Commit from post-commit committed truth. |
| T-06-04-SC | Tampering | Go toolchain/package execution | high | mitigate | Every verifier and built-binary helper rejects non-local or non-Go-1.25.x execution; `GOTOOLCHAIN=local` forbids automatic downloads and no package-manager/module change is planned. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Full real-file Parse → Build → baseline/master composition | 06-04 Tasks 1-3 | COVERED | Transactional CLI on-ramp plus actual composition root. |
| REQ | PROF-03 | Secret-safe end-to-end ingest and report | 06-04 Tasks 1-3 with 06-01/06-02 | COVERED | Store excludes; CLI exposes name/line only. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| REVIEW | R-07 | Local Go 1.25.x only | 06-04 Tasks 1-3 | COVERED | Independent verifiers and the real command-package build helper enforce `GOTOOLCHAIN=local` plus Go 1.25.x before tests/builds. |
| CONTEXT | D-01 | `ingest [path] [--json]`, default path | 06-04 Task 1 | COVERED | Strict one-positional parser. |
| CONTEXT | D-02 | CLI.Run, typed exits, one JSON object | 06-04 Task 1 | COVERED | Exit 2 usage; exit 1 runtime/partial/recovery. |
| CONTEXT | D-03 | Deterministic human reconciliation | 06-04 Tasks 1 and 3 | COVERED | Managed entries plus master statements equals eligible/source statements; physical master lines remain separate. |
| CONTEXT | D-04 | Structured JSON envelope | 06-04 Task 1 | COVERED | Dedicated value-free DTO and closed failure renderer preserve truthful partial fields. |
| CONTEXT | D-05 | EffectiveManaged partition | 06-04 Task 3, 06-03 | COVERED | Sole routing predicate; exact selected committed entries map back to excluded extents, and ambiguous mapping fails closed. |
| CONTEXT | D-06 | Imperative master/no duplication | 06-04 Task 3, 06-03 | COVERED | Every other eligible byte extent is retained; one exact imperative-plus-parser-opaque fixture proves source order and provenance. |
| CONTEXT | D-07 | Reuse installer | 06-03, consumed by 06-04 Task 3 | COVERED | One transaction API. |
| CONTEXT | D-08 | Surface Store withheld report | 06-02 and 06-04 Task 1 | COVERED | Name/line only. |
| CONTEXT | D-09 | Commit failure is runtime/fail closed | 06-02 and 06-04 Tasks 1 and 3 | COVERED | Common pre-Commit abort/compensation helper and typed outcome prevent plain-error rollback inference. |
| CONTEXT | D-10 | Init then fixed main commit | 06-04 Tasks 2-3 | COVERED | Actual composition test proves same-store initializer/Begin; controller uses fixed main/message. |
| CONTEXT | D-11 | Post-END warning, no failure/mutation | 06-03, surfaced by 06-04 | COVERED | Warning-only exit 0. |
| CONTEXT | D-12 | Exact marker scan | 06-03 | COVERED | Consumed layout. |
| CONTEXT | D-13 | Real E2E oracle | 06-05 | COVERED | Final executable proof. |
| CONTEXT | D-14 | Deterministic fixtures | 06-04 Tasks 1-3, 06-05 fixtures | COVERED | Unit matrices, actual-main identity test, and authored fixture. |
| CONTEXT | D-15 | Same injected concrete store | 06-04 Task 2 | COVERED | Exact command-package test proves one `cliStore` pointer owns Init/Begin/Commit/Abort and a second Store cannot use its ID. |
| CONTEXT | D-16 | Extend landed seam | 06-03 and 06-04 Tasks 1-2 | COVERED | No parallel abstraction; actual main composition is tested. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. This plan projects its static-only and privacy boundaries into frontmatter: no source execution, and no values/raw internal data in user output. The executor resolves both through the named canary matrices before completion.
</spec_fallback>

<verification>
Run exact-name preflighted argument/output/controller tests, full CLI/command suites, and vet. The final plan adds built-binary and real-store/provider proof; this plan must already prove the exact filesystem-first compensation event order, zero later counters on unsafe evidence, mixed imperative/opaque extent retention, and independent expectedTarget/expectedCandidate checks with deterministic seams.
</verification>

<success_criteria>
- Strict CLI behavior matches D-01 through D-04, including duplicate-path exit 2.
- Parse/build/merge/master/accounting behavior matches D-05/D-06 without shell execution; all eligible extents except exact EffectiveManaged committed extents survive, and master statements/physical block lines are distinct.
- Secret exclusion/reporting and typed commit failure behavior match D-08/D-09.
- One exact store instance owns initialization and ingest transactions.
- Parse, duplicate/extent-association, accounting, candidate-preparation, stale, committed, non-committed, and recovery-required paths share the filesystem-first compensation/renderer contracts and preserve external state.
- Every independent verifier and command build helper refuses non-local or non-Go-1.25.x execution; no automatic toolchain download can satisfy this plan.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-04-SUMMARY.md` when done.
</output>
