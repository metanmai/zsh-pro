---
phase: 06-ingest-end-to-end
plan: 04
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - core/cli/cli.go
  - core/cli/ingest.go
  - core/cli/ingest_test.go
  - core/dto/ingest.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-01/D-02: `zsh-pro ingest [path] [--json]` defaults to `~/.zshrc`, accepts at most one positional path, rejects unknown flags and a second path with exit 2, and preserves the landed `CLI.Run(args []string, stdout, stderr io.Writer) int` signature."
    - "D-03/D-04: human and JSON results expose managed_entries, unmanaged_statements, optional unmanaged_source_lines, source/accounted counts, value-free withheld metadata, warnings, and truthful commit/install/recovery state; JSON emits exactly one object on success or failure."
    - "D-05/D-06: provider.Parse and ir.Build produce one complete source-ordered Profile. CommitIngest persists managed and unmanaged entries after Store redaction; EffectiveManaged is used only for activation/reporting and no physical complement is produced."
    - "D-08/D-09: literal source secrets may be recaptured/reported on re-ingest, while programmatic persisted SecretRef pass-through remains a separate Kind-only Store contract with zero Retrieve/Store/Delete. Output never claims source parsing reconstructed a SecretRef."
    - "D-07/D-12: the controller prepares and validates one canonical loader/install candidate with expectedTarget from the original snapshot and expectedCandidate from independent durable evidence, promotes loader then `.zshrc` once, commits the full Profile, and finalizes the already-promoted transaction on commit."
    - "All target-promoted pre-commit failures and Store noncommit outcomes are filesystem-first: restore/retain classification precedes AbortIngest when applicable, loader/cache rollback, and initializer rollback. Recovery uncertainty disarms every later destructive compensation."
    - "D-11: post-END ordinary content is warned once, remains byte-identical in place, and stays in complete Profile persistence/accounting when eligible."
    - "D-15/D-16: one exact `*store.Store` pointer issues the initialization ID and performs main-only Begin/Commit/Abort plus existing store-backed verbs; a second Store rejects the ID before effects."
  artifacts:
    - path: "core/cli/ingest.go"
      provides: "Complete-Profile parse/build controller, one install promotion, typed commit/finalize/compensation matrix, and safe result rendering"
      exports: ["runIngest"]
    - path: "core/dto/ingest.go"
      provides: "Stable value-free ingest JSON result with managed/unmanaged statement accounting"
      contains: "IngestResult"
    - path: "core/cli/cli.go"
      provides: "Strict ingest argument parser and CLI.Run dispatch"
      contains: "case \"ingest\""
    - path: "core/cmd/zsh-pro/main.go"
      provides: "Single concrete store/provider composition root"
      contains: "cliStore"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Real Store-bound initialization-ID identity proof"
      contains: "TestMainInitToBeginUsesExactCLIStore"
    - path: "core/cli/ingest_test.go"
      provides: "Usage/output, full-Profile, one-promotion, secret distinction, conflict, and filesystem-first compensation tests"
  key_links:
    - from: "original source/layout"
      to: "provider.Parse -> ir.Build -> CommitIngest"
      via: "complete source-ordered Profile; Store-owned redaction before serialization"
      pattern: "CommitIngest"
    - from: "original snapshot and independent install candidate"
      to: "one loader/target promotion"
      via: "expectedTarget/expectedCandidate stay independent across exchange and recovery"
      pattern: "promotionOutcome"
    - from: "typed CommitIngest outcome"
      to: "finalize or filesystem-first compensation"
      via: "commit finalizes the existing promotion; noncommit restores/retains before later compensation"
      pattern: "compensatePreCommitFilesystemFirst"
    - from: "core/cmd/zsh-pro/main.go cliStore"
      to: "initializer and ingest transaction methods"
      via: "one constructed pointer and Store-bound ID"
      pattern: "cliStore"
  prohibitions:
    - statement: "MUST NOT execute, source, eval, or command-substitute input `.zshrc` text during ingest."
      category: security
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT filter persisted Profile entries by EffectiveManaged, generate a physical complement, or perform a post-commit target rewrite."
      category: fidelity
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Compose the Store and filesystem transactions into the strict non-destructive `ingest` CLI contract.

Purpose: Deliver Parse → Build complete Profile → one canonical loader/install promotion → authenticated main commit → finalize, with safe output and filesystem-first compensation.
Output: Strict dispatch, value-free DTO, one-store composition, full-Profile controller, deterministic projection accounting, and focused transaction tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md
@core/cli/cli.go
@core/cli/store.go
@core/cli/install_transaction.go
@core/cmd/zsh-pro/main.go
@core/ir/build.go
@core/activate/builder.go

<interfaces>
- Preserve `CLI.Run(args []string, stdout, stderr io.Writer) int`. Ingest failures use a closed safe-reason renderer, never arbitrary error text.
- `provider.Parse` plus `ir.Build` is the only parsing/classification path. `ir.Build` is total; do not invent a Build error seam.
- 06-01 supplies exact baseline/Abort evidence; 06-02 supplies typed complete-Profile CommitIngest outcomes; 06-03 supplies original source/layout, one independently authenticated install candidate, one promotion outcome, and restore/finalize operations.
- Exit 0 means complete or warning-only success; 1 means runtime/conflict/recovery failure; 2 means usage.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce strict arguments and safe managed/unmanaged output</name>
  <files>core/cli/cli.go, core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/cli.go: CLI.Run, runAnalyze, fail, stream ownership, and exit conventions
    - core/dto: domain-to-wire naming and deterministic JSON conventions
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-04 and D-08/D-09
  </read_first>
  <behavior>
    - Accept no path or one path, in either order with `--json`; expand home once at the CLI boundary.
    - Reject unknown flags or a second path with exit 2. JSON mode emits exactly one object plus LF; human mode emits one fixed usage line.
    - Result fields are `ok`, `exit_code`, `profile_committed`, `startup_installed`, `recovery_required`, `managed_entries`, `unmanaged_statements`, optional `unmanaged_source_lines`, `source_statements`, `accounted_statements`, `withheld`, and `warnings`.
    - `managed_entries + unmanaged_statements = accounted_statements = source_statements`; physical lines never replace statement counts.
    - Safe failures preserve value-free status/count/report fields but cannot receive raw errors, paths, source, refs, object IDs, backend identifiers, or values.
  </behavior>
  <action>
    Add strict `ingest` dispatch without changing CLI.Run. Detect JSON mode across argv before reporting usage, accept repeated `--json` idempotently, and expand the chosen path exactly once.

    Define the value-free DTO and deterministic human/JSON rendering. Use `unmanaged_statements` and optional `unmanaged_source_lines`; do not expose physical generated-file terminology or zsh text. Keep arrays present and ordered.

    Add a closed ingest failure renderer whose parameters exclude arbitrary error/path/source/ref/backend data. Table-test valid/invalid arguments, both output modes, statement-versus-line accounting, all safe reasons, and injected canary strings absent from both streams.
  </action>
  <acceptance_criteria>
    - Invalid arguments return exit 2 in human and JSON modes.
    - JSON emits one value-free object; human output is deterministic.
    - Managed/unmanaged statement accounting is exact and physical source-line counts remain separate.
    - Existing verbs and CLI.Run callers remain green.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestRunIngestArguments TestRunIngestUsageHumanAndJSON TestRunIngestManagedUnmanagedAccountingHumanAndJSON TestRunIngestSafeFailureMatrix TestCLIUsesOneInjectedStoreInstance'; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || exit 1; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cli -run "^(${selector})$" -count=1</automated>
  </verify>
  <done>The ingest verb has strict usage behavior and a value-free managed/unmanaged result contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove one exact Store owns initialization and every ingest operation</name>
  <files>core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/cmd/zsh-pro/main.go: provider/keychain/store construction and initializer closure
    - core/store/install_transaction.go: Store-bound InstallInitializationID
    - core/cli/store.go: neutral initializer and Begin/Commit/Abort adapter surface
  </read_first>
  <behavior>
    - Production constructs `cliStore := store.New(...)` once and captures that exact pointer.
    - The same pointer issues the initialization ID and performs Begin, Commit, Abort, and terminal cleanup.
    - A second Store over the root rejects the first Store's ID before effects.
    - Analyze remains available when deferred store construction fails; store-backed verbs return the original failure without replacement construction.
  </behavior>
  <action>
    Remove any second Store construction from the initializer closure. Inject the same pointer into every store-backed CLI seam. Add `TestMainInitToBeginUsesExactCLIStore` using a real temp Store and deterministic backend: prove success Commit, forced Abort, aggregate initializer terminal evidence, and cross-Store rejection with unchanged counters. Keep instrumentation per-test and force local Go 1.25.x in build helpers.
  </action>
  <acceptance_criteria>
    - Exactly one concrete Store pointer owns initialization and all ingest transaction calls.
    - Cross-Store IDs fail before Git/backend/filesystem effects.
    - Analyze independence and existing store-backed verbs remain intact.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; listed=$(GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -list '^Test'); test "$(printf '%s\n' "$listed" | awk '$0 == "TestMainInitToBeginUsesExactCLIStore" { n++ } END { print n + 0 }')" -eq 1; GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -run '^(TestMainInitToBeginUsesExactCLIStore)$' -count=1</automated>
  </verify>
  <done>The actual composition root proves one Store instance owns Init, main Begin/Commit/Abort, and terminal evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit the complete Profile around one non-destructive startup promotion</name>
  <files>core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/install_transaction.go after 06-03: original source/layout, canonical candidate, independent identity evidence, one promotion, restore, and finalize
    - core/model/ingest_transaction.go after 06-02: baseline, complete-Profile commit, abort, and recovery evidence
    - core/ir/build.go and core/model/profile.go: ordered Profile and EffectiveManaged projection
    - core/activate/builder.go: ineffective/unrepresentable skip contract used by 06-05
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-05 through D-14
  </read_first>
  <behavior>
    - Flow is pure adapter check -> authenticated same-filesystem probe -> initializer -> main-only Begin -> original/layout/candidate preparation and validation -> provider.Parse/ir.Build complete Profile -> loader promotion -> one `.zshrc` promotion -> CommitIngest complete Profile -> finalize on commit or filesystem-first compensation on noncommit.
    - ExpectedTarget comes only from the original bounded snapshot; expectedCandidate comes only from independently durable canonical install-candidate evidence. No resampling or second target candidate exists.
    - Complete Profile entries remain in source order regardless of EffectiveManaged. Projection counts inspect EffectiveManaged without filtering commit input.
    - Post-END ordinary entries remain in Profile/accounting and in physical place while emitting one warning.
    - Programmatic persisted SecretRef pass-through is not simulated by source parsing. A source literal rerun may recapture/report; already-dynamic entries preserve all source-derived fields and remain unreported.
    - Committed outcome finalizes owned journal/artifacts. Store noncommit restores/retains the promoted target before any Store/loader/cache/initializer compensation.
  </behavior>
  <action>
    Implement `runIngest` as the single controller. Run pure and real-filesystem capability preflight before initializer/cache/loader/target effects. Initialize the exact Store, Begin main, prepare the exact original snapshot plus independently durable canonical install candidate, and statically parse/build the eligible original ordinary source. Reject malformed topology, ambiguous source association required by the existing IR contract, or accounting mismatch before target mutation.

    Pass the complete ordered Profile to CommitIngest; do not select, merge, or carry forward only EffectiveManaged baseline entries. Store exclusion owns redaction. Compute projection counts by inspecting the complete Profile. Keep post-END eligible entries in those counts and the commit.

    Validate expectedTarget/expectedCandidate, promote the loader, then promote `.zshrc` once. Immediately before Commit, validate that the installed target still matches expectedCandidate. Call CommitIngest with fixed message `zsh-pro: ingest baseline`. On committed evidence, finalize the already-promoted transaction; cleanup uncertainty preserves committed truth and reports recovery. Delete/reject any API or branch for a post-commit target candidate or promotion.

    Route every failure after target promotion and before Commit, plus prepare-conflict/known Store noncommit, through `compensatePreCommitFilesystemFirst`: filesystem classify/restore-or-retain -> AbortIngest when an active token still requires it -> loader/cache rollback -> initializer rollback. Any uncertainty stops later steps. Pre-promotion failures terminalize only typed owned resources.

    Test full first/installed/re-ingest Profiles, committed-empty baseline, post-END persistence/warning, source-literal recapture, already-dynamic preservation, separate programmatic persisted-reference seam, multiline statement/line accounting, two-target conflict, explicit shared root, one-promotion cardinality, expected-axis mismatch, journal sync ambiguity, safe result rendering, and no input execution. State-matrix assertions must make target promotion count exactly one on every committed path and zero post-commit promotions.
  </action>
  <acceptance_criteria>
    - CommitIngest receives the complete redacted source-ordered Profile.
    - Managed/unmanaged statement accounting covers every eligible source statement and post-END eligible content.
    - Loader then target promote once; commit finalizes rather than rewriting startup.
    - Store noncommit and all post-promotion pre-commit failures restore/retain filesystem state before later compensation.
    - Persisted-reference pass-through and source-literal re-ingest are tested and reported truthfully as different inputs.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestRunIngestTransactionOutcomeMatrix TestRunIngestCommitsCompleteOrderedProfile TestRunIngestEffectiveManagedIsProjectionOnly TestRunIngestPromotesStartupExactlyOnce TestRunIngestCommitFinalizesWithoutTargetRewrite TestRunIngestStoreNoncommitCompensatesFilesystemFirst TestRunIngestPreCommitCompensationFilesystemFirst TestRunIngestUnsafeFilesystemDisarmsLaterCompensation TestRunIngestSeparatesOriginalAndCandidateEvidence TestRunIngestPostEndWarningRetainsProfileAndBytes TestRunIngestSourceLiteralMayRecapture TestRunIngestProgrammaticSecretRefPassThroughIsSeparate TestRunIngestDynamicEntryRerunPreservesFieldsAndReporting TestRunIngestManagedUnmanagedMultilineAccounting TestRunIngestDifferentTargetsPrepareConflict TestRunIngestExplicitSharedRoot TestRunIngestNeverExecutesSource'; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || exit 1; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cli -run "^(${selector})$" -count=1; GOTOOLCHAIN=local go test ./core/cli ./core/cmd/zsh-pro -count=1; GOTOOLCHAIN=local go vet ./core/cli ./core/cmd/zsh-pro</automated>
  </verify>
  <done>The controller commits one complete Profile around one loader/install promotion and handles every noncommit/recovery path without destructive inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation |
| --- | --- | --- |
| CLI/path -> source | Untrusted input or execution | Strict argv, one boundary expansion, static Parse/Build only, execution canaries |
| Original target -> install candidate | Ordinary bytes could be rewritten | Exact marker topology and outside-byte oracle; candidate differs only at exact regions |
| Candidate -> target | Concurrent replacement/durability ambiguity | Independent expected axes, atomic exchange/no-replace, journaled reverse-or-retain |
| Profile -> Store/output | Entry loss or secret disclosure | Complete Profile commit, Store redaction before serialization, value-free renderer |
| Store noncommit -> compensation | Aborting before filesystem recovery | One filesystem-first helper and zero-later-call uncertainty tests |
| Composition root -> transaction registry | Split Store identity | One pointer and cross-Store ID rejection |

OWASP ASVS Level 1 applies. Critical/high threats require the named automated tests before completion.
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Real source to complete main Profile | Task 3 | COVERED | Parse/Build complete Profile and authenticated commit. |
| REQ | PROF-03 | Secret-safe ingest/report | Tasks 1/3 with 06-02 | COVERED | Redaction before serialization and truthful input distinctions. |
| CONTEXT | D-01-D-04 | Strict CLI/value-free accounting | Task 1 | COVERED | Managed/unmanaged statement fields. |
| CONTEXT | D-05-D-07 | Projection-only persistence and non-destructive install | Task 3 with 06-03 | COVERED | Complete Profile plus one exact-region promotion. |
| CONTEXT | D-08-D-10 | Secret and fixed-main transaction | Tasks 1-3 | COVERED | Literal/reference distinction and typed outcomes. |
| CONTEXT | D-11-D-12 | Post-END warning and one journal | Task 3 with 06-03 | COVERED | Content remains in Profile and source. |
| CONTEXT | D-13-D-14 | Three linked assertions | 06-05 | COVERED | This plan supplies the real controller and invariants. |
| CONTEXT | D-15-D-16 | One composition seam | Task 2 | COVERED | No second Store/writer. |
</source_audit>

<verification>
Run exact-name argument/output/controller tests, complete CLI/command suites, and vet. Stale scans must reject managed-only commit, physical complement, second/final target promotion, post-END exclusion, and source-SecretRef authority claims.
</verification>

<success_criteria>
- Strict CLI and value-free projection accounting pass.
- CommitIngest receives the complete ordered Profile after Store redaction.
- One original/candidate-bound loader/install promotion precedes commit and is finalized, never repeated, after commit.
- Filesystem-first compensation is exact for pre-commit and Store noncommit outcomes.
- Post-END content remains byte-identical and logically persisted.
- One Store instance owns the entire transaction.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-04-SUMMARY.md` when done.
</output>
