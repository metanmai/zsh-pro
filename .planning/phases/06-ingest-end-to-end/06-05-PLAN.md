---
phase: 06-ingest-end-to-end
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - core/cli/ingest_e2e_test.go
  - core/cmd/zsh-pro/main_test.go
  - core/cli/testdata/ingest/real.zshrc
  - core/cli/testdata/ingest/installed-with-appends.zshrc
  - core/cli/testdata/ingest/expected-installed.zshrc
  - scripts/verify-phase06-macos-runtime.sh
  - scripts/verify-phase06-macos-runtime_test.sh
  - .planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md
autonomous: false
requirements: [PROF-03]
user_setup:
  - service: native-macos-host
    why: "Darwin exchange/no-replace support requires implementation-real runtime evidence; Linux execution and cross-build cannot provide it."
    dashboard_config:
      - task: "Provide an attributable native macOS host or required macOS CI run that checks out the exact implementation SHA and returns unedited verifier output."
        location: "Native macOS host operator or CI job URL/run ID"
must_haves:
  truths:
    - "D-13: Store.Read(main) returns the complete redacted ordered Profile; ir.Regenerate preserves every non-secret entry's order, unmanaged Text verbatim, and managed semantics. SecretRef comparison is authority-safe and does not invent source metadata."
    - "D-05/D-13: activate.Build and the runtime emitter apply only EffectiveManaged/Representable entries, resolve SecretRefs, and leave unmanaged execution canaries inert."
    - "D-07/D-14: built-binary E2E sources pristine original and the actual installed `.zshrc` under isolated `zsh -f`; it compares values/order/aliases/functions/options/PATH, an order-sensitive managed-definition to imperative-use fixture, and a literal-secret equality assertion without printing the value."
    - "Every outside-marker byte in the actual installed file equals the original in order; the full installed bytes equal an independently authored `expected-installed.zshrc` template after one in-memory runtime-secret placeholder substitution. No literal-bearing expected file is written. Only an explicit reviewed allowlist of zsh-pro loader symbols may differ at runtime, and startup performs zero subprocesses."
    - "The built binary proves human/JSON success, usage exit 2, explicit shared-root first run, exactly one target promotion, byte-identical second run, and warning-only post-END preservation."
    - "The literal secret is absent from every loose or packed object in the final object database and every retained Store quarantine discovered beneath the authenticated transaction namespace, plus every fixture, expected file, journal, evidence record, cache, and output. No quarantine name, path, or cardinality is assumed, and no discovered object database may remain unscanned. The only permitted extra filesystem copy is the authenticated transaction peer required for atomic exchange; success/restore removes it, while an injected recovery row proves private mode-0700-parent confinement and value-free reporting."
    - "Programmatic persisted SecretRef pass-through is Kind-only with zero Retrieve/Store/Delete. Re-ingesting a source literal may recapture/report it; already-dynamic source fields remain exact and unreported."
    - "Ingest itself never executes source; only the explicit isolated startup oracle does."
    - "The reviewed execution base and immutable module/layering/dependency gates pass under local Go 1.25.x."
    - "Darwin remains incomplete until exact-SHA native evidence proves atomic operations and all production cleanup rows; unsupported-retained behavior is BLOCKED/nonzero safety evidence only."
  artifacts:
    - path: "core/cli/testdata/ingest/real.zshrc"
      provides: "Authored mixed managed/unmanaged/opaque/dynamic/runtime-secret-placeholder/order-sensitive/no-exec fixture"
    - path: "core/cli/testdata/ingest/installed-with-appends.zshrc"
      provides: "Authored exact-marker plus post-END append fixture"
    - path: "core/cli/testdata/ingest/expected-installed.zshrc"
      provides: "Independently authored actual-installed template with one reviewed runtime-secret placeholder"
    - path: "core/cli/ingest_e2e_test.go"
      provides: "Full Store-read/regenerate, activation projection, secret-object, persisted-reference, and no-exec proofs"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Built-binary actual-installed startup, one-promotion, idempotency, and append proofs"
    - path: "scripts/verify-phase06-macos-runtime.sh"
      provides: "Native-Darwin-only exact-SHA local-Go-1.25.x verifier"
    - path: "scripts/verify-phase06-macos-runtime_test.sh"
      provides: "Deterministic fail-closed and mounted-filesystem metadata regression"
    - path: ".planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md"
      provides: "Attributable native macOS runtime record"
  key_links:
    - from: "real.zshrc authored provenance"
      to: "Store.Read(main) and ir.Regenerate"
      via: "full ordered non-secret structural/semantic comparison plus bounded authoritative SecretRef placeholder comparison"
      pattern: "Regenerate"
    - from: "Store.Read(main)"
      to: "activate.Build and runtime emitter"
      via: "EffectiveManaged/Representable projection and SecretRef resolver spy"
      pattern: "EffectiveManaged"
    - from: "pristine real.zshrc"
      to: "actual installed `.zshrc`"
      via: "independent expected-installed bytes, outside-marker equality, and isolated zsh -f observable-state comparison"
      pattern: "expected-installed.zshrc"
    - from: "literal secret canary"
      to: "all final and retained object databases"
      via: "exhaustive authenticated-quarantine discovery plus loose-and-packed git cat-file batch-all-objects enumeration of the primary and every discovered object database"
      pattern: "batch-all-objects"
  prohibitions:
    - statement: "MUST NOT generate expected startup bytes or observable expectations from production output during the test."
      category: test-integrity
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT compare behavior against a generated substitute; the installed side is the actual file mutated by the built binary."
      category: fidelity
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Close Phase 6 with three linked correctness assertions plus built-binary, privacy, no-execution, layering, and native macOS evidence.

Purpose: Prove the complete stored model, activation projection, and actual installed startup all agree without destructive source rewriting.
Output: Independently authored fixtures, built-binary startup tests, real Store/activation E2E, complete object scans, immutable dependency gates, and native macOS evidence.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-04-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE
@core/ir/roundtrip_test.go
@core/activate/builder.go
@core/shell/zsh/emit.go
@core/shell/zsh/hook.go
@core/cmd/zsh-pro/main.go

<interfaces>
- Built-binary tests use isolated HOME/ZSHPRO_HOME, real Git, the actual installed target, and a deterministic environment.
- Real E2E uses the actual zsh provider, actual Git-backed Store, deterministic backend/resolver, actual controller, `activate.Build`, and runtime emitter.
- The SecretRef comparator may trust only Store.Read authority. It validates exact placeholder kind/key and source-reconstructible identity/semantics, but does not copy/pretend `Secret`, `ValueMode`, `RuntimeValue`, or `StartLine` came from source.
- All-object privacy discovers every retained Store quarantine under the authenticated transaction namespace and uses complete loose-and-packed object enumeration for the primary and every discovered object database; fixed paths, singular-cardinality assumptions, and partial scans are forbidden.
- Native macOS runtime evidence is a blocking external checkpoint; cross-build is compile evidence only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the built binary preserves actual startup behavior and bytes</name>
  <files>core/cmd/zsh-pro/main_test.go, core/cli/testdata/ingest/real.zshrc, core/cli/testdata/ingest/installed-with-appends.zshrc, core/cli/testdata/ingest/expected-installed.zshrc</files>
  <read_first>
    - core/cmd/zsh-pro/main.go after 06-04: exact composition and environment paths
    - core/cli/install.go and install_test.go: canonical marker/loader bytes, topology, and startup subprocess constraints
    - core/shell/zsh/hook.go: exact loader-owned symbols for the reviewed allowlist
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01-D-04, D-07, D-11-D-16
  </read_first>
  <behavior>
    - Built binary human/JSON/usage/shared-root cases use isolated real state and exact exits/output.
    - First adoption appends only the canonical region; re-ingest changes only exact marker regions; target promotion cardinality is one per successful ingest and zero after Store commit.
    - Actual installed bytes equal independently authored expected-installed template bytes after one in-memory runtime-secret substitution; outside-marker bytes equal original bytes exactly.
    - Pristine and actual installed files, each sourced in a separate isolated `zsh -f`, yield identical user variables and assignment order, aliases, function bodies/results, option states, and PATH ordering.
    - A managed definition precedes an imperative use that records its result; this catches the rejected reordered-complement architecture.
    - Literal secret behavior is compared with an in-process equality predicate/hash sentinel that never prints or writes the value.
    - With deterministic `security` and `secret-tool` executables present in the built binary's PATH, ingest commits a keychain SecretRef and `emit apply main` resolves it; the fake backend retains only a versioned non-plaintext transport.
    - Runtime-state differences are limited to an explicit static allowlist of exact loader-owned symbols/functions reviewed against hook.go; no wildcard prefix exemption.
    - Startup subprocess canaries/stub executable counters remain zero.
  </behavior>
  <action>
    Author `real.zshrc` with stable LF bytes/lines: managed alias/export/PATH/options/function declarations; an imperative use that depends on an earlier managed definition; parser-opaque and forced-unmanaged statements; one exact reviewed `__PHASE6_LITERAL_SECRET__` placeholder and an already-dynamic secret; comments/gaps; and subprocess/execution canaries. Author `installed-with-appends.zshrc` with one exact region and trailing ordinary lines. Author `expected-installed.zshrc` independently as the pristine template bytes plus the reviewed canonical Phase 5 region and the same one placeholder; never call production rendering to create it.

    Build the actual binary once with local Go 1.25.x. Generate an unpredictable detector-matching secret in memory, replace the one reviewed placeholder only while writing the temp original `.zshrc`, and never materialize another literal-bearing expected/source file. Expand the independently authored expected template only in memory for comparison. Run ingest, compare the actual mutated file to that in-memory expectation, and compare exact outside-marker slices to the in-memory pristine bytes. Run twice and assert complete byte identity plus one exact marker pair. Inspect transaction seams/counters or durable journal evidence to prove one target promotion and no post-commit rewrite. After success, scan the isolated root and require the installed target to be the only literal-bearing path; in a separate injected recovery case, permit exactly the authenticated exchange peer below the mode-0700 transaction directory and require every other file plus all output to exclude it.

    Execute pristine and actual installed startup only after ingest no-execution assertions. Use separate `zsh -f` sandboxes with identical controlled env and stub PATH tools. Capture a structured observable snapshot without secret values: order ledger, selected variables, alias definitions, function bodies/results, setopt state, PATH array, and a boolean secret-equality result. Compare exact snapshots after removing only explicitly enumerated loader-owned symbol/function rows. Assert every other parameter/function/option difference is absent and every stub subprocess counter is zero.

    Cover default human, explicit JSON, invalid usage, explicit shared root, idempotency, post-END warning/byte preservation/Profile inclusion, no execution during ingest, and each OS-keychain composition path. The OS-keychain fake must model its native missing-item exit code and never retain the runtime plaintext.
  </action>
  <acceptance_criteria>
    - The built binary mutates only exact marker regions and promotes the target once.
    - Pristine versus actual-installed startup behavior is equal across all required observables and the order-sensitive fixture.
    - Secret equality is proven without printing/writing the literal; only exact loader-owned symbols differ.
    - Both production OS-keychain selections complete secret-bearing built-binary ingest and activation with lossless transport and no plaintext backend state.
    - Startup invokes no subprocess and ingest invokes no source.
    - Post-END bytes remain exact, warn once, exit 0, and remain persisted when eligible.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestMainInitToBeginUsesExactCLIStore TestMainIngestHumanAndJSON TestMainIngestUsageExitTwo TestMainIngestExplicitSharedRoot TestMainIngestPromotesTargetExactlyOnce TestMainIngestExpectedInstalledAndOutsideBytes TestMainIngestIdempotent TestMainIngestAppendWarningPreservesAndPersists TestMainIngestActualInstalledStartupEquivalent TestMainIngestOrderSensitiveDefinitionBeforeUse TestMainIngestSecretBehaviorWithoutDisclosure TestMainIngestAndActivationWithOSKeychains TestMainIngestAllowsOnlyExactLoaderSymbols TestMainIngestStartupHasNoSubprocess'; listed=$(GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || exit 1; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -run "^(${selector})$" -count=1</automated>
  </verify>
  <done>The compiled command proves non-destructive bytes, exactly one promotion, and pristine-versus-actual-installed startup equivalence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove full Profile round trip, activation projection, and secret boundaries</name>
  <files>core/cli/ingest_e2e_test.go, core/store/keychain.go, core/store/keychain_os_test.go, core/cli/testdata/ingest/real.zshrc, core/cli/testdata/ingest/expected-installed.zshrc, scripts/verify-phase06-macos-runtime.sh, scripts/verify-phase06-macos-runtime_test.sh</files>
  <read_first>
    - core/cli/ingest.go after 06-04: controller and result mapping
    - core/store/store.go after 06-02: complete-Profile CommitIngest and object environments
    - core/ir/roundtrip_test.go: structural/verbatim/semantic oracle mechanics
    - core/activate/builder.go and core/shell/zsh/emit.go: projection and SecretRef resolution
    - .planning/phases/06-ingest-end-to-end/06-SPEC.md Requirements 1, 2, and 5
  </read_first>
  <behavior>
    - Authored provenance names every fixture entry's span/order/category/EffectiveManaged/Representable/secret disposition before production runs.
    - Store.Read returns every managed and unmanaged entry in source order after redaction.
    - Regenerate preserves non-secret entry order, unmanaged Text verbatim, and managed semantics.
    - The bounded SecretRef comparator validates only source-reconstructible fields and the exact inert kind/key placeholder against authoritative Store.Read data; it neither grants source authority nor claims equality for Secret/ValueMode/RuntimeValue/StartLine.
    - activate.Build/emitter ignores unmanaged/unrepresentable execution canaries, emits all expected managed entries, and resolves SecretRefs through a deterministic resolver without exposing values.
    - Literal values occur in no loose or packed object in the primary database or any exhaustively discovered retained Store quarantine. Programmatic persisted-reference pass-through is Kind-only; source-literal rerun may recapture/report; already-dynamic fields remain exact and unreported.
    - No source executes during successful or injected-failure ingestion.
  </behavior>
  <action>
    Create an authored provenance table over exact fixture spans and expected full Profile order. Validate raw fixture slices before invoking production. Expected Profile/order/semantics and expected-installed template bytes must come from authored data, not production output. The single secret placeholder is replaced with the same runtime-only value in in-memory source/expected buffers; only the temp original source buffer is written before ingest.

    Ingest with the actual provider, Store, backend, and controller. Read main and compare the entire redacted ordered Profile. Regenerate it and assert exact unmanaged Text/order plus managed behavior. For the literal-secret entry, compare exact placeholder kind/key and only fields actually reconstructible from source; use the authoritative committed entry solely to validate authority, never to fill in or claim source reconstruction of Secret, ValueMode, RuntimeValue, or StartLine. A forged sentinel must fail authority validation.

    Pass the same Store-read Profile to activate.Build and the real runtime emitter with deterministic resolver spies. Assert every EffectiveManaged/Representable entry contributes expected output, SecretRef resolution occurs only on that path, and unmanaged/unrepresentable canary names/text never appear or execute. This is separate from regeneration and startup assertions.

    Enumerate every Git object with `git cat-file --batch-all-objects`, including reachable, unreachable, loose, and packed objects. At the lost-update-ref-response ambiguity boundary, walk the authenticated Store transaction namespace and discover every retained quarantine dynamically; do not assume one directory, one generated name, or one fixed path. For each discovered quarantine, bind its object directory with the primary database as the alternate, enumerate all objects, prove the expected retained candidate coverage canary is visible, and scan for the runtime literal. Fail if any discovered quarantine cannot be enumerated, if discovery and scanned-database cardinalities differ, or if any database remains unscanned. Scan fixtures, expected files, candidate evidence, journals, cache, logs, and captured output and require no match. After durable success/restoration, the installed target is the only literal-bearing path. In the separate injected recovery row, allow exactly the descriptor-authenticated exchange peer beneath the current-EUID mode-0700 transaction directory, require no other match, and verify value-free recovery output; do not delete it merely to satisfy the test. Test programmatic persisted SecretRef pass-through directly with Kind-only/zero Retrieve/Store/Delete. Separately re-ingest the literal source and permit safe recapture/reporting without asserting the parser saw a reference. Preserve the already-dynamic entry's source-derived fields and output exclusion.

    Create the native macOS verifier as an exact-SHA/local-Go-1.25.x implementation-real runner. It exact-name-preflights atomic exchange/no-replace/late-target, the real `security` keychain round-trip, the built-binary keychain ingest/activation proof, and production cleanup capability/top-level/nested/symlink/replacement rows. Report the repository's mounted filesystem by resolving its mount point and extracting `FilesystemType` from native `diskutil info -plist` output with `plutil`; report `unknown` without fabrication when that probe is unavailable or invalid. Cover the successful extraction and unknown fallback deterministically in the shell regression. Unsupported cleanup records BLOCKED/nonzero and cannot complete the phase.

    At the exact Task 2 candidate SHA, and before the checkpoint, require clean candidate-matching implementation/verifier inputs; run all touched package suites, the exact 12-selector acceptance matrix, vet, local `make check`, execution-base/HEAD/index/worktree manifest equality, Linux/Darwin amd64/arm64 test-binary builds, provider/x/sys dependency and layering checks in all four target contexts, and the exact-status phase-scope detector. Require the verifier to be executable in both the candidate tree and worktree. Freeze Phase 6 implementation/test files after the Task 2 commit.
  </action>
  <acceptance_criteria>
    - Store.Read/Regenerate proves the complete ordered redacted Profile without forged SecretRef authority.
    - Activation/emitter proves EffectiveManaged-only behavior, SecretRef resolution, and inert unmanaged canaries.
    - Literal secrets are absent from loose and packed objects in the primary and every dynamically discovered retained-quarantine database, plus every output/new artifact; source literal and persisted reference behaviors remain distinct.
    - Ingest executes nothing.
    - The same committed candidate SHA passes touched packages, the exact 12 selectors, vet, local make, immutable-module, four-target compile/layering/dependency, scope-status, and executable-verifier gates before the native checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; candidate_sha=$(git rev-parse HEAD); git cat-file -e "${candidate_sha}^{commit}"; git diff --quiet "$candidate_sha" -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh; test -z "$(git ls-files --others -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh)"; git diff --quiet "$candidate_sha" -- .planning/phases/06-ingest-end-to-end/06-0[1-5]-PLAN.md; test -x scripts/verify-phase06-macos-runtime.sh; test -x scripts/verify-phase06-macos-runtime_test.sh; test "$(git ls-tree "$candidate_sha" scripts/verify-phase06-macos-runtime.sh | awk '{print $1}')" = 100755; test "$(git ls-tree "$candidate_sha" scripts/verify-phase06-macos-runtime_test.sh | awk '{print $1}')" = 100755; scripts/verify-phase06-macos-runtime_test.sh; GOTOOLCHAIN=local go test ./core/model ./core/store ./core/cli ./core/cmd/zsh-pro -count=1; tests='TestIngestE2EStoreReadReturnsCompleteOrderedProfile TestIngestE2ERegeneratePreservesNonSecretOrderTextAndSemantics TestIngestE2ESecretRefComparatorDoesNotForgeAuthority TestIngestE2EActivationUsesOnlyEffectiveManagedEntries TestIngestE2EActivationResolvesSecretRefs TestIngestE2EUnmanagedExecutionCanariesAreInert TestIngestE2ESecretAllObjectDatabases TestIngestE2ESecretAbsentAfterUpdateRefObservationAmbiguity TestIngestE2EProgrammaticSecretRefPassThroughKindOnly TestIngestE2ESourceLiteralRerunMayRecapture TestRunIngestDynamicEntryRerunPreservesFieldsAndReporting TestIngestE2ENeverExecutesSource'; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test'); for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done; selector=$(printf '%s' "$tests" | tr ' ' '|'); GOTOOLCHAIN=local go test ./core/cli -run "^(${selector})$" -count=1; GOTOOLCHAIN=local go vet ./...; GOTOOLCHAIN=local make check; base_commit=557c818845f4239d5d50ecab02e37731bc117f10; base_file=.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE; test "$(wc -l &lt; "$base_file")" -eq 1; test "$(tr -d '\n' &lt; "$base_file")" = "$base_commit"; git cat-file -e "${base_commit}^{commit}"; for manifest in go.mod go.sum; do git cat-file -e "${base_commit}:${manifest}"; git cat-file -e "${candidate_sha}:${manifest}"; git cat-file -e ":${manifest}"; cmp -s &lt;(git show "${base_commit}:${manifest}") &lt;(git show "${candidate_sha}:${manifest}"); cmp -s &lt;(git show "${base_commit}:${manifest}") &lt;(git show ":${manifest}"); cmp -s &lt;(git show "${base_commit}:${manifest}") "$manifest"; done; build_dir=$(mktemp -d); trap 'rm -rf "$build_dir"' EXIT HUP INT TERM; zsh_pkg=$(GOTOOLCHAIN=local go list -f '{{.ImportPath}}' ./core/shell/zsh); for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64; do target_os=${target%/*}; target_arch=${target#*/}; for pkg in ./core/model ./core/store ./core/cli ./core/cmd/zsh-pro; do artifact=${pkg#./}; artifact=${artifact//\//-}; CGO_ENABLED=0 GOOS="$target_os" GOARCH="$target_arch" GOTOOLCHAIN=local go test -c -o "$build_dir/${artifact}-${target_os}-${target_arch}.test" "$pkg"; done; for pkg in ./core/cli ./core/ir ./core/store; do deps=$(CGO_ENABLED=0 GOOS="$target_os" GOARCH="$target_arch" GOTOOLCHAIN=local go list -deps -f '{{.ImportPath}}' "$pkg"); if printf '%s\n' "$deps" | grep -Fx "$zsh_pkg" &gt;/dev/null; then printf 'forbidden transitive provider dependency: %s/%s %s -&gt; %s\n' "$target_os" "$target_arch" "$pkg" "$zsh_pkg" &gt;&amp;2; exit 1; fi; if printf '%s\n' "$deps" | awk '$0 == "golang.org/x/sys" || index($0,"golang.org/x/sys/") == 1 { found=1 } END { exit !found }'; then printf 'forbidden golang.org/x/sys dependency: %s/%s %s\n' "$target_os" "$target_arch" "$pkg" &gt;&amp;2; exit 1; fi; done; done; phase_section=$(node /home/metanmai/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 6); scope=$(printf '%s\n' "$phase_section"; cat .planning/phases/06-ingest-end-to-end/06-0[1-5]-PLAN.md); set +e; detector_json=$(printf '%s' "$scope" | node /home/metanmai/.codex/gsd-core/bin/lib/api-coverage.cjs --json); detector_status=$?; set -e; test "$detector_status" -eq 1; printf '%s' "$detector_json" | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{let r;try{r=JSON.parse(s)}catch{process.exit(1)}if(r.detected!==false||!Array.isArray(r.signals)||r.signals.length!==0)process.exit(1)})'; test ! -e .planning/phases/06-ingest-end-to-end/COVERAGE.md; if find .planning/phases/06-ingest-end-to-end -maxdepth 1 -type f -name '*-COVERAGE.md' -print -quit | grep -q .; then exit 1; fi; git diff --quiet "$candidate_sha" -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh; test -z "$(git ls-files --others -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh)"</automated>
  </verify>
  <done>The real E2E path closes complete-Profile regeneration, activation projection, all-object privacy, truthful secret-input distinctions, and no-execution.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Obtain exact-SHA native macOS runtime evidence</name>
  <files>.planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md</files>
  <action>Run the prepared verifier on an attributable external native macOS host; this environment cannot provide native Darwin runtime evidence.</action>
  <instructions>
    Check out the exact Task 2 implementation SHA and run `scripts/verify-phase06-macos-runtime.sh "$implementation_sha"` under `GOTOOLCHAIN=local` with Go 1.25.x. The verifier must observe `HEAD == implementation_sha`; a different SHA, edited output, skip, cross-build, automatic toolchain, unsupported adapter, or unsupported cleanup capability does not qualify.

    Record PASS evidence with `implementation_sha` equal to the exact native-verified commit from the verifier output, authority, time, native macOS/architecture, the mounted-filesystem probe result (including an honest `unknown` fallback), local toolchain/GOVERSION, exact command, unedited stdout/stderr, exit 0, exchange/no-replace/late-target PASS, macOS-keychain round-trip PASS, keychain ingest/activation PASS, cleanup capability supported, and top-level/nested-file/nested-directory/symlink/before-replacement/after-replacement PASS. Do not store or guess the evidence-file commit inside the evidence file. Commit the completed evidence file in a distinct later commit; final verification derives that `evidence_commit` from Git history, proves strict implementation-before-evidence ancestry, and proves the implementation tree is unchanged across the record commit. Unsupported-retained output is `status: BLOCKED`, nonzero, and cannot resume completion.
  </instructions>
  <verification>After resume, run the exact automated evidence/local final verifier below.</verification>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; GOTOOLCHAIN=local go test ./core/model ./core/store ./core/cli ./core/cmd/zsh-pro -count=1; GOTOOLCHAIN=local go vet ./...; GOTOOLCHAIN=local make check; scripts/verify-phase06-macos-runtime_test.sh; base_commit=557c818845f4239d5d50ecab02e37731bc117f10; base_file=.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE; test "$(wc -l &lt; "$base_file")" -eq 1; test "$(tr -d '\n' &lt; "$base_file")" = "$base_commit"; git cat-file -e "${base_commit}^{commit}"; for manifest in go.mod go.sum; do git cat-file -e "${base_commit}:${manifest}"; git cat-file -e "HEAD:${manifest}"; git cat-file -e ":${manifest}"; cmp -s &lt;(git show "${base_commit}:${manifest}") &lt;(git show "HEAD:${manifest}"); cmp -s &lt;(git show "${base_commit}:${manifest}") &lt;(git show ":${manifest}"); cmp -s &lt;(git show "${base_commit}:${manifest}") "$manifest"; done; evidence=.planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md; test -s "$evidence"; test -z "$(git status --porcelain --untracked-files=all -- "$evidence")"; test "$(awk -F': ' '$1 == "implementation_sha" { n++ } END { print n + 0 }' "$evidence")" -eq 1; implementation_sha=$(awk -F': ' '$1 == "implementation_sha" { print $2 }' "$evidence"); case "$implementation_sha" in *[!0-9a-f]*|'') exit 1 ;; esac; test "$(printf '%s' "$implementation_sha" | wc -c)" -eq 40; git cat-file -e "${implementation_sha}^{commit}"; evidence_commit=$(git log -1 --format=%H -- "$evidence"); case "$evidence_commit" in *[!0-9a-f]*|'') exit 1 ;; esac; test "$(printf '%s' "$evidence_commit" | wc -c)" -eq 40; git cat-file -e "${evidence_commit}^{commit}"; test "$implementation_sha" != "$evidence_commit"; git merge-base --is-ancestor "$implementation_sha" "$evidence_commit"; git merge-base --is-ancestor "$evidence_commit" HEAD; git diff --quiet "$implementation_sha" "$evidence_commit" -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh; git diff --quiet "$evidence_commit" HEAD -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh; git diff --quiet HEAD -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh; test -z "$(git ls-files --others -- core go.mod go.sum go.work go.work.sum scripts/verify-phase06-macos-runtime.sh scripts/verify-phase06-macos-runtime_test.sh)"; rg -q '^status: PASS$' "$evidence"; rg -q '^authority: (ci|operator):' "$evidence"; rg -q '^native_os: macOS ' "$evidence"; rg -q '^native_arch: ' "$evidence"; rg -q -x 'filesystem: (unknown|[A-Za-z0-9._+-]+)' "$evidence"; rg -q -x 'go_toolchain: local' "$evidence"; rg -q -x 'go_env_goversion: go1[.]25[.][0-9]+' "$evidence"; rg -q '^exchange: PASS$' "$evidence"; rg -q '^no_replace_absent: PASS$' "$evidence"; rg -q '^no_replace_late_target: PASS$' "$evidence"; rg -q '^macos_keychain_round_trip: PASS$' "$evidence"; rg -q '^keychain_ingest_activation: PASS$' "$evidence"; rg -q '^cleanup_capability_supported: PASS$' "$evidence"; rg -q '^cleanup_top_level: PASS$' "$evidence"; rg -q '^cleanup_nested_file: PASS$' "$evidence"; rg -q '^cleanup_nested_directory: PASS$' "$evidence"; rg -q '^cleanup_symlink: PASS$' "$evidence"; rg -q '^cleanup_replacement_before: PASS$' "$evidence"; rg -q '^cleanup_replacement_after: PASS$' "$evidence"; rg -q '^cleanup_capability: supported$' "$evidence"</automated>
  </verify>
  <resume-signal>Commit complete PASS evidence with `cleanup_capability: supported` at the exact path in a distinct later evidence-record commit, then type `evidence ready`; uncommitted or BLOCKED evidence does not resume this checkpoint.</resume-signal>
  <done>Attributable native macOS evidence names the exact earlier implementation SHA; its separately derived later evidence commit preserves that implementation tree and records passing atomic plus every owned cleanup/sync row.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
| --- | --- | --- |
| Expected values authored by production | high | Independently authored fixture/provenance/expected-installed bytes before production runs |
| Regenerated substitute hides startup reordering | critical | Source actual installed target from built binary and use order-sensitive definition/use fixture |
| Unmanaged entry activates | critical | Direct activate.Build/emitter canaries and output scan |
| Secret leaks outside the bounded target/transaction peer or into objects/output | critical | Runtime-only placeholder injection, exhaustive retained-quarantine discovery, loose-and-packed batch-all-objects enumeration of every discovered database, full isolated-root/output scans, durable-success cleanup, and exact recovery-peer confinement |
| Source sentinel gains SecretRef authority | high | Store-authoritative bounded comparator and forged-sentinel rejection |
| Startup launches subprocess | high | Stub PATH counters and isolated zsh -f |
| Cross-build falsely completes Darwin | high | Exact-SHA attributable native checkpoint with supported cleanup capability |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | G-06-01 | Complete source Profile commit | Task 2 | COVERED |
| GOAL | G-06-02 | Non-destructive loader adoption | Task 1 | COVERED |
| GOAL | G-06-03 | Secret boundary/report | Task 2 | COVERED |
| GOAL | G-06-04 | Three linked assertions | Tasks 1-2 | COVERED |
| REQ | PROF-03 | Literal exclusion and reference behavior | Task 2 | COVERED |
| CONTEXT | D-01-D-04 | Built CLI/output | Task 1 | COVERED |
| CONTEXT | D-05-D-07 | Full Profile/projection/one install | Tasks 1-2 | COVERED |
| CONTEXT | D-08-D-12 | Secret/transaction/warning | Tasks 1-2 | COVERED |
| CONTEXT | D-13-D-14 | Regenerate/activate/actual startup | Tasks 1-2 | COVERED |
| CONTEXT | D-15-D-16 | Composition/layering | Tasks 1-3 | COVERED |
| REVIEW | R-07 | Local Go 1.25.x/native Darwin | Tasks 1-3 | COVERED |
</source_audit>

<verification>
Run built-binary actual-installed startup tests, real Store/activation E2E, complete object scans, touched packages, vet, local make, immutable manifests, dependency/layering gates, phase-scope detector, and blocking exact-SHA native macOS evidence.
</verification>

<success_criteria>
- All five SPEC requirements and three linked assertions have executable coverage.
- Actual installed startup is behavior-equivalent to pristine source and outside-marker bytes are exact.
- Store persists the complete redacted ordered Profile; activation applies only its managed projection.
- No literal secret exists in any loose or packed object in the primary or any exhaustively discovered retained-quarantine database, fixtures/expected files, journal/evidence/cache, or output; only the installed target and a recovery-required authenticated transaction peer may contain it.
- Ingest and startup subprocess canaries remain inert as required.
- Dependencies/layering are unchanged and native macOS evidence passes on the exact SHA.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-05-SUMMARY.md` when done.
</output>
