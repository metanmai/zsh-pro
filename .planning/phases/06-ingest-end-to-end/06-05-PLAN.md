---
phase: 06-ingest-end-to-end
plan: 05
type: execute
wave: 5
depends_on: ["06-04"]
files_modified:
  - core/cli/ingest_e2e_test.go
  - core/cmd/zsh-pro/main_test.go
  - core/cli/testdata/ingest/real.zshrc
  - core/cli/testdata/ingest/installed-with-appends.zshrc
  - core/cli/testdata/ingest/expected-master.zshrc
  - scripts/verify-phase06-macos-runtime.sh
  - .planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md
autonomous: false
requirements: [PROF-03]
user_setup:
  - service: native-macos-host
    why: "Darwin exchange/no-replace support requires implementation-real runtime evidence; Linux execution and cross-build cannot provide it."
    dashboard_config:
      - task: "Provide an attributable native macOS host or required macOS CI run that can check out the exact implementation SHA and return unedited verifier output."
        location: "Native macOS host operator or CI job URL/run ID"
must_haves:
  truths:
    - "D-13/D-14: a hand-authored real-shaped fixture drives built-binary and real provider/store ingestion, then Store.Read(main) -> ir.Regenerate -> the Phase 2 structural/byte/behavior oracle; test-local authority-seeded normalization covers only SecretRef metadata that source text cannot reconstruct before full structural equality."
    - "The built binary proves human success, JSON success, usage exit 2, explicit shared-root first run, idempotent second run, and warning-only post-END appends against isolated HOME/ZSHPRO_HOME state."
    - "The fixture's literal secret is absent from every final object, including unreachable loose/packed objects, and from retained quarantine objects after injected Git update-ref commit-observation ambiguity; deterministic backend, object-publication, and cleanup failures are never mislabeled ambiguous."
    - "A persisted SecretRef rerun performs no second backend Retrieve/Store/Delete and adds no second withheld item. The already-dynamic entry preserves Text, Value, RuntimeValue, Dynamic, and ValueMode exactly and appears in neither human nor JSON withheld output."
    - "Ingest itself never executes command substitutions/functions/compinit: fixture canaries remain absent until the explicitly sandboxed oracle stage."
    - "`06-EXECUTION-BASE` equals the reviewed literal `557c818845f4239d5d50ecab02e37731bc117f10`; that commit, HEAD, index, and worktree agree byte-for-byte on go.mod/go.sum. Transitive dependency inspection proves core/cli, core/ir, and core/store depend on neither the concrete zsh provider nor `golang.org/x/sys` or any of its subpackages."
    - "Darwin support remains incomplete until an external native macOS authority runs the exact implementation SHA under `GOTOOLCHAIN=local` with Go 1.25.x through both the exchange/no-replace adapter suite and real core/store quarantine-cleanup helper suite. Evidence records separate PASS rows for top-level, nested file, nested directory, symlink, and before/after replacement seams; unsupported cleanup passes only as untouched retained/recovery behavior, never injected cleanup success."
    - "The deterministic phase-scope coverage detector returns status 1 with `detected:false` and an empty signals array, and no coverage matrix artifact exists."
  artifacts:
    - path: "core/cli/testdata/ingest/real.zshrc"
      provides: "Authored mixed declarative/imperative/dynamic/secret/function/compinit/no-exec fixture"
    - path: "core/cli/testdata/ingest/installed-with-appends.zshrc"
      provides: "Authored exact-marker plus post-END append fixture"
    - path: "core/cli/testdata/ingest/expected-master.zshrc"
      provides: "Authored byte oracle for imperative/opaque routing"
    - path: "core/cli/ingest_e2e_test.go"
      provides: "Real store/provider round trip, provenance, all-object secret scans, abort quarantine, rerun, and no-exec proofs"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Built-binary CLI contract and one-store/shared-root/idempotency/append proofs"
    - path: "scripts/verify-phase06-macos-runtime.sh"
      provides: "Native-Darwin-only, local-Go-1.25.x exact-SHA verifier for atomic exchange/no-replace plus production core/store cleanup across top-level, nested file/directory, symlink, and replacement seams"
    - path: ".planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md"
      provides: "Externally attributable native macOS runtime record with exact SHA, local toolchain mode, exact Go 1.25.x GOVERSION, environment, commands, unedited output, and PASS/FAIL state"
  key_links:
    - from: "real.zshrc authored source extents"
      to: "committed Profile + expected-master.zshrc"
      via: "line/byte provenance table tied directly to fixture offsets"
      pattern: "real.zshrc"
    - from: "Store.Read(main)"
      to: "ir.Regenerate -> provider.Parse -> behavior oracle"
      via: "the same structural, verbatim-byte, and guarded zsh -f comparisons used by Phase 2, with exact redaction-contract validation before trusted SecretRef metadata normalization"
      pattern: "Regenerate"
    - from: "literal secret canary"
      to: "final and retained-quarantine object enumeration"
      via: "git cat-file batch-all-objects enumeration, never refs-only traversal"
      pattern: "batch-all-objects"
    - from: "06-EXECUTION-BASE"
      to: "go.mod/go.sum HEAD/index/worktree"
      via: "exact equality to reviewed SHA 557c818845f4239d5d50ecab02e37731bc117f10, then Git object reads plus byte comparisons"
      pattern: "06-EXECUTION-BASE"
    - from: "scripts/verify-phase06-macos-runtime.sh"
      to: "06-MACOS-RUNTIME-EVIDENCE.md"
      via: "an external native macOS host checks out the exact implementation SHA and records unedited adapter plus real store-cleanup command output with one evidence row per required behavior"
      pattern: "implementation_sha"
  prohibitions:
    - statement: "MUST NOT derive expected fixture routing from production output generated during the test."
      category: test-integrity
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT enumerate only ref-reachable Git objects when proving secret absence."
      category: privacy
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Close Phase 6 with built-binary, real-store/provider, round-trip, privacy, no-execution, layering, dependency, and native macOS runtime evidence.

Purpose: Prove the planned transaction model works through the actual composition root and final artifacts, not only mocked controller branches, while sealing every roadmap/spec/context source item.
Output: Three authored fixtures, built-binary tests, real E2E/oracle and all-object scans, immutable module/layering gates, a native-macOS verifier/evidence record, and a complete source-coverage audit.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-03-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-04-SUMMARY.md
@.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE
@core/ir/roundtrip_test.go
@core/cli/ingest.go
@core/cmd/zsh-pro/main.go

<interfaces>
Evidence boundaries:
- Built-binary tests execute `core/cmd/zsh-pro` with isolated `HOME`, explicit `ZSHPRO_HOME`, deterministic environment, real Git, and authored files.
- Real E2E uses the actual zsh provider, actual store Git plumbing, deterministic test backend, and the actual CLI/controller; mocks are reserved for injected failure timing.
- The Phase 2 oracle compares committed Profile structure, imperative verbatim bytes, regenerated parsing, and guarded `zsh -f` behavior. A parser cannot mint trusted SecretRef metadata from an inert redaction sentinel, so the test first validates every source-reconstructible secret-entry field and exact sentinel kind/key contract against Store.Read, then overlays only the trusted non-source SecretRef/ValueMode metadata in a test-local copy before full structural equality. It skips only the execution subcase when zsh is not on PATH; structural/byte checks always run.
- All-object privacy proof enumerates Git's complete object database rather than `rev-list --all`; quarantine proof supplies its object directory through Git object-environment variables.
- The repository has no implementation-real required native-macOS workflow. Darwin cross-build remains a compile gate only; phase completion therefore requires the explicit external native-runtime evidence checkpoint in Task 3.
</interfaces>
</context>

## Artifacts this plan produces

- Three testdata files authored independently of production output.
- Two E2E test files plus a native-macOS runtime verifier and evidence record; no production file changes.
- No package installation, module change, network service, user account, or dashboard mutation. Task 3 does require access to an attributable external native macOS host or required macOS CI run.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the real built-binary CLI and shared-root lifecycle</name>
  <files>core/cmd/zsh-pro/main_test.go, core/cli/testdata/ingest/real.zshrc, core/cli/testdata/ingest/installed-with-appends.zshrc</files>
  <read_first>
    - core/cmd/zsh-pro/main.go after 06-04: provider/keychain/store construction, environment paths, and exact one-pointer injection
    - core/cli/ingest_test.go after 06-04: locked human/JSON fields and fixed failure messages
    - core/cli/install_test.go after 06-03: exact marker helpers and explicit shared-root expectations
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-04, D-10 through D-12, D-14 through D-16
  </read_first>
  <behavior>
    - A built binary ingests the authored default `HOME/.zshrc` in human mode with isolated state and exits 0; one store root/main ref and one cache/loader layout exist.
    - The same binary ingests an explicit path in JSON mode and emits one valid result object with exact managed-entry/master-statement/master-block-line/source/accounted counts, state, withheld metadata, and warnings.
    - Unknown flag and duplicate path each exit 2; JSON usage emits one object and human usage emits the fixed stderr line.
    - Explicit `ZSHPRO_HOME` shared-root first run succeeds, proving the composition root's one Store and store-before-cache order.
    - A second unchanged ingest leaves the managed block and outside-marker bytes byte-identical with exactly one START and one END line.
    - The installed-with-appends fixture exits 0 with one warning and leaves every post-END byte unchanged.
  </behavior>
  <action>
    Author `real.zshrc` directly under testdata per D-14 with deterministic LF bytes and stable line positions: alias/export/PATH declarations; `export GOPATH=$HOME/go`; imperative `eval "$(...)"`; a function definition and invocation; `compinit`; literal `export API_TOKEN=sk-abc123`; already-dynamic `export TOKEN=$(op read ...)`; comments/blanks; and a command-substitution canary that would create `ingest-executed` if sourced. Do not generate this source from production rendering. Author the installed variant with exactly one shipped START/END region followed by `# added by nvm` and `export NVM_DIR=...` as post-END bytes.

    Add `main_test.go` harnesses that first set `GOTOOLCHAIN=local`, require `go env GOVERSION` to match Go 1.25.x, build the actual command once into `t.TempDir`, run it with isolated HOME/ZSHPRO_HOME/PATH and stable locale/time environment, and capture raw stdout/stderr/exit code. Use the real binary and real Git; do not call `runIngest` directly in these tests. Copy authored fixture bytes before each case and hash source/target regions around runs. Replace the landed helper's automatic-toolchain setting; a download must never satisfy this build.

    Cover human default-path success, JSON explicit-path success, unknown-flag usage, duplicate-path usage, and explicit shared store/cache root. Parse JSON and require one object/no trailing second value; compare human lines exactly. Assert main ref/store/cache/loader/target layout demonstrates one successfully initialized store rather than split initializer/transaction roots.

    Run `TestMainInitToBeginUsesExactCLIStore` unchanged as the command-package identity prerequisite, then run unchanged ingest twice and compare managed-region plus outside-region raw bytes after the first and second run; count marker lines only after filtering exact full non-comment source lines so comments cannot satisfy the gate. Run installed-with-appends and require exit 0, exactly one warning, and byte/hash equality for the post-END region. Assert the execution canary is absent immediately after every ingest process.
  </action>
  <acceptance_criteria>
    - The real binary satisfies valid/invalid human and JSON contracts with exact exits.
    - Explicit store/cache root equality succeeds on a clean first run.
    - One exact store layout and main ref prove composition-root identity behavior.
    - Second ingest is byte-idempotent with one exact marker pair.
    - Post-END appends are warned once, unchanged, and non-failing.
    - Ingest creates no execution canary.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestMainInitToBeginUsesExactCLIStore TestMainIngestHumanAndJSON TestMainIngestUsageExitTwo TestMainIngestExplicitSharedRoot TestMainIngestIdempotent TestMainIngestAppendWarning' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -run '^(TestMainInitToBeginUsesExactCLIStore|TestMainIngestHumanAndJSON|TestMainIngestUsageExitTwo|TestMainIngestExplicitSharedRoot|TestMainIngestIdempotent|TestMainIngestAppendWarning)$' -count=1</automated>
  </verify>
  <done>The compiled command proves strict CLI behavior, one-store shared-root initialization, idempotency, append preservation/warning, and static-only ingestion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove authored provenance, round trip, secret absence, and retained-abort safety</name>
  <files>core/cli/ingest_e2e_test.go, core/cli/testdata/ingest/real.zshrc, core/cli/testdata/ingest/expected-master.zshrc, scripts/verify-phase06-macos-runtime.sh</files>
  <read_first>
    - core/cli/ingest.go after 06-04: real controller construction and result mapping
    - core/store/store.go after 06-02: Begin/Commit outcome paths and quarantine naming/object environment
    - core/ir/roundtrip_test.go: structural, verbatim-byte, zsh lookup, sandbox, and behavior-diff oracle mechanics
    - core/cli/testdata/ingest/real.zshrc after Task 1: authored exact source bytes/lines
    - .planning/phases/06-ingest-end-to-end/06-SPEC.md Requirements 1, 2, and 5
  </read_first>
  <behavior>
    - A hard-authored provenance table names each eligible fixture statement's exact line/span/category/routing; raw fixture slices must equal the table before ingest and assertions derive from that table, not generated output.
    - Real provider + real store + deterministic backend ingest commits main. Store.Read returns the expected managed profile/valid SecretRef and current imperative extents produce expected-master bytes exactly.
    - Regeneration reparses every non-secret entry to the Phase 2 structural equality contract. For a committed SecretRef, the parsed entry must match name/category/routing/text/value/dynamic/runtime and the exact inert kind:key sentinel; a test-local normalizer seeded only by Store.Read overlays Secret/ValueMode, then the complete Profile must be structurally equal. A forged source sentinel never gains authority. Imperative bytes are identical; guarded zsh execution remains behavior-equivalent.
    - The literal secret appears in no object from complete final object enumeration and no object in a retained quarantine created by injected Git update-ref commit-response observation ambiguity. Deterministic backend/object/cleanup failures remain directly classified. The dynamic secret remains verbatim and is absent from withheld metadata.
    - Re-ingesting the committed SecretRef uses only backend Kind, performs no Retrieve/Store/Delete, produces no second withheld item, and preserves the committed reference. Re-ingesting the already-dynamic entry preserves its exact Text, Value, RuntimeValue, Dynamic, and ValueMode fields and excludes it from both human and JSON withheld lists.
    - No fixture command/function/substitution runs during either successful or injected-failure ingestion; execution happens only inside the explicitly guarded oracle subprocess.
    - The exact-SHA native Darwin verifier exact-name-preflights and runs the production `core/store` cleanup helper for top-level, nested file, nested directory, symlink, and before/after replacement matrices with injected-success seams disabled. A real unsupported capability passes only when artifacts/replacements are untouched and cleanup is retained/uncertain with recovery required.
  </behavior>
  <action>
    Author `expected-master.zshrc` independently from production output using the imperative/opaque source bytes and exact order from `real.zshrc`. In `ingest_e2e_test.go`, declare a provenance table with stable fixture line numbers, literal byte spans, expected category, EffectiveManaged routing, and expected master/withheld disposition. Before invoking code, split the fixture independently and require every table span to match its authored bytes; fail if the fixture changes without table review. Expected profiles/master/counts must derive from the authored table, never by invoking the production renderer as the expected-value generator.

    Instantiate the actual zsh provider, actual Git-backed Store, deterministic in-memory backend, and actual CLI/controller against temp HOME/ZSHPRO_HOME. Ingest the fixture, read `main`, and compare managed identities/values, SecretRef shape, withheld name/StartLine, accounting, and expected-master raw bytes. Immediately assert `ingest-executed` and function/compinit canaries do not exist. Snapshot the already-dynamic entry's Text, Value, RuntimeValue, Dynamic, and ValueMode, then re-ingest and require those fields byte/value-identical. Assert the backend spy sees no Retrieve/Store/Delete for the persisted SecretRef and only the allowed Kind validation, no new withheld item, the same reference shape, and absence of the dynamic entry name from both exact human output and parsed JSON withheld arrays.

    Exercise the Phase 2 oracle per D-13: `Store.Read(main)` -> `ir.Regenerate` -> provider.Parse/ir.Build, plus byte comparison for every imperative provenance span. First require raw Phase 2 structural equality for every non-secret committed entry. Raw equality for the literal-secret entry is intentionally impossible because source parsing must not infer a trusted SecretRef from inert text. Implement a test-local redaction-aware comparator seeded exclusively by the authoritative Store.Read Profile: match entries deterministically; require exact name/category/managed/override/dynamic/runtime/start-line normalization contract and exact Text/Value sentinel carrying the committed SecretRef kind/key; then copy only Secret and ValueMode from the trusted entry into the parsed comparison copy. Require full Profile structural equality after that bounded overlay. Add a forged-sentinel case proving untrusted source text neither creates a SecretRef nor passes normalization without an authoritative matching committed entry. Do not change parser/IR production behavior for the oracle. If `zsh` is absent, skip only the subprocess behavior comparison. Otherwise run original and regenerated forms under isolated `zsh -f` sandboxes with identical controlled env and a deterministic stub `op`; compare captured behavior/state and ensure host HOME/startup files are inaccessible. Record that this deliberate oracle execution occurs after the no-execution ingest assertions.

    Prove secret absence with complete object enumeration. Invoke Git `cat-file --batch-all-objects` and read every blob from the final object database, including unreachable and packed objects; do not use refs-only traversal. Then inject only the allowed ambiguous boundary: a temp PATH Git wrapper delegates the update-ref prepared/commit effect but loses the commit response so authoritative ref observation is unreadable or third-state. Require recovery-required/artifact retention, locate only authenticated transaction-quarantine siblings under the test temp parent, and enumerate their object directory with the exact private index/object/alternate environment. Scan all blob bytes in both sets for `sk-abc123` and fail on any occurrence. Deterministic backend, final-object publication, and cleanup failure cases must assert non-ambiguous typed axes. Also scan human/JSON outputs and safe error strings.

    Create `scripts/verify-phase06-macos-runtime.sh` as an implementation-real checkpoint runner, not a cross-compiler. It accepts an expected full implementation SHA, exports `GOTOOLCHAIN=local`, rejects unless `go env GOTOOLCHAIN` is `local` and `go env GOVERSION` matches Go 1.25.x, then refuses unless `uname -s` and local `go env GOOS` are Darwin and the checked-out commit equals that SHA with a clean production/test tree. Record `go_toolchain: local`, `go_env_goversion: go1.25.<patch>`, `sw_vers`, `uname -m`, `go version`, target temporary filesystem description from `stat -f`, exact local-mode command lines, and unedited verbose output.

    Exact-name-preflight and run `TestAtomicRenameAtExchangePreservesBothInodes`, `TestAtomicRenameAtNoReplaceCreatesAbsentTarget`, `TestAtomicRenameAtNoReplacePreservesLateTarget`, and `TestAtomicRenameAtUnsupportedLeavesBothNames` from the Linux/Darwin tagged file. Separately exact-name-preflight and run `TestDarwinQuarantineCleanupRealTopLevel`, `TestDarwinQuarantineCleanupRealNestedFile`, `TestDarwinQuarantineCleanupRealNestedDirectory`, `TestDarwinQuarantineCleanupRealSymlink`, `TestDarwinQuarantineCleanupRealReplacementBeforeFinalCheckMatrix`, and `TestDarwinQuarantineCleanupRealReplacementAfterFinalCheckMatrix` in `core/store`. The cleanup tests must use the production platform helper and real per-root lock with injected-success removal disabled. For each object class, real supported cleanup proves owned removal+sync; real unsupported capability instead proves the original/replacement is byte/inode/type intact with retained-or-uncertain cleanup and recovery required. The script fails on any skip, missing/duplicate exact test, adapter unsupported result, fabricated cleanup-success seam, non-local toolchain, or mismatched SHA. It writes stdout for external capture and does not manufacture a PASS evidence document itself.

    Before this task commits and the native checkpoint begins, treat all Phase 6 production/test code as frozen. Require local `go env GOVERSION` to match Go 1.25.x; run every touched package suite, `go vet ./...`, and `GOTOOLCHAIN=local make check`. Verify the literal execution-base commit and byte-compare `go.mod`/`go.sum` across that base, HEAD, index, and worktree. Derive and reject forbidden production dependencies with local `go list` under explicit Linux/Darwin amd64/arm64 contexts. Run the exact-status Phase 6 scope detector and require status 1, `detected:false`, empty signals, and no coverage artifact. A failure routes back to the owning task; never weaken or skip it. These are automatic pre-checkpoint gates, while Darwin cross-build remains compile evidence only.
  </action>
  <acceptance_criteria>
    - Fixture assertions are traceable to authored line/byte spans before production code runs.
    - Real store/provider ingest and Store.Read/Regenerate close raw non-secret structure, redaction-aware full Profile structure, exact bytes, and guarded behavior round trips without granting authority to source sentinels.
    - Literal secret absence covers all final objects and retained quarantine objects, not only reachable refs.
    - Dynamic entry fields and persisted SecretRef behavior match D-08 without extra backend access/reporting; both human and JSON withheld outputs exclude the dynamic entry.
    - Ingest never triggers source execution canaries.
    - The prepared native verifier covers every required real Darwin cleanup class/seam and accepts unsupported cleanup only as untouched retained/recovery evidence.
    - Local Go 1.25.x package/vet/make, immutable-manifest, layering, and exact phase-scope gates pass before the native checkpoint is presented.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; tests='TestIngestE2EAuthoredProvenanceAndRoundTrip TestIngestE2ERedactionAwareStructuralOracle TestIngestE2ESecretAllObjectDatabases TestIngestE2ESecretAbsentAfterUpdateRefObservationAmbiguity TestIngestE2EPersistedSecretRefNoBackendAccess TestRunIngestDynamicEntryRerunPreservesFieldsAndReporting TestIngestE2ENeverExecutesSource' &amp;&amp; listed=$(GOTOOLCHAIN=local go test ./core/cli -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=local go test ./core/cli -run '^(TestIngestE2EAuthoredProvenanceAndRoundTrip|TestIngestE2ERedactionAwareStructuralOracle|TestIngestE2ESecretAllObjectDatabases|TestIngestE2ESecretAbsentAfterUpdateRefObservationAmbiguity|TestIngestE2EPersistedSecretRefNoBackendAccess|TestRunIngestDynamicEntryRerunPreservesFieldsAndReporting|TestIngestE2ENeverExecutesSource)$' -count=1 &amp;&amp; test -x scripts/verify-phase06-macos-runtime.sh</automated>
  </verify>
  <done>The real E2E path is pinned to authored evidence, closes the Phase 2 oracle, scans every final/retained object for literal secrets, preserves SecretRefs without repeat backend work, and proves ingest executes nothing.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Obtain exact-SHA native macOS runtime evidence</name>
  <files>.planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md</files>
  <action>Run the prepared verifier on an attributable external native macOS host; the agent's environment has no native macOS runtime and cannot perform this action.</action>
  <instructions>
    Tasks 1-2 created the implementation-real verifier and passed every automatable local, module, layering, scope, cross-build, privacy, and no-execution gate. Do not change Phase 6 production/test/script files after the Task 2 commit.

    Set `implementation_sha` to that exact Task 2 commit. On an attributable native macOS host/filesystem, check out the SHA and run `scripts/verify-phase06-macos-runtime.sh "$implementation_sha"`. Linux execution, Darwin cross-build, a different SHA, skipped tests, an unsupported atomic adapter, fabricated cleanup success, or edited output do not qualify. A production cleanup primitive that is genuinely unsupported qualifies only when the native tests prove untouched artifacts/replacements plus retained/uncertain recovery.

    Create `.planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md` from the unedited output. Record `status: PASS`, `authority: ci:<URL/run-id>` or `authority: operator:<attestation>`, exact implementation SHA/timestamp, `native_os: macOS ...`, architecture, `go_toolchain: local`, exact `go_env_goversion: go1.25.<patch>`, filesystem, exact command, complete stdout/stderr, exit status, and these exact rows: `exchange: PASS`, `no_replace_absent: PASS`, `no_replace_late_target: PASS`, `cleanup_top_level: PASS`, `cleanup_nested_file: PASS`, `cleanup_nested_directory: PASS`, `cleanup_symlink: PASS`, `cleanup_replacement_before: PASS`, and `cleanup_replacement_after: PASS`. Also record `cleanup_capability: supported` or `cleanup_capability: unsupported-retained`; the latter is valid only when unedited test output proves untouched artifacts/replacements and recovery-required retention. Do not create the plan summary or mark Phase 6 complete yet.
  </instructions>
  <verification>After resume, the executor runs the exact automated evidence/local final verifier in this task's verify block.</verification>
  <verify>
    <automated>set -eu; go_version=$(GOTOOLCHAIN=local go env GOVERSION); printf "%s\n" "$go_version" | awk "/^go1[.]25[.][0-9]+$/ { ok=1 } END { exit !ok }"; test "$(GOTOOLCHAIN=local go env GOTOOLCHAIN)" = local; GOTOOLCHAIN=local go test ./core/model ./core/store ./core/cli ./core/cmd/zsh-pro -count=1; GOTOOLCHAIN=local go vet ./...; GOTOOLCHAIN=local make check; base_file=.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE; test "$(wc -l &lt; "$base_file")" -eq 1; test "$(tr -d '\n' &lt; "$base_file")" = 557c818845f4239d5d50ecab02e37731bc117f10; git cat-file -e '557c818845f4239d5d50ecab02e37731bc117f10^{commit}'; for manifest in go.mod go.sum; do git cat-file -e "557c818845f4239d5d50ecab02e37731bc117f10:${manifest}"; git cat-file -e "HEAD:${manifest}"; git cat-file -e ":${manifest}"; cmp -s &lt;(git show "557c818845f4239d5d50ecab02e37731bc117f10:${manifest}") &lt;(git show "HEAD:${manifest}"); cmp -s &lt;(git show "557c818845f4239d5d50ecab02e37731bc117f10:${manifest}") &lt;(git show ":${manifest}"); cmp -s &lt;(git show "557c818845f4239d5d50ecab02e37731bc117f10:${manifest}") "$manifest"; done; zsh_pkg=$(GOTOOLCHAIN=local go list -f '{{.ImportPath}}' ./core/shell/zsh); for target in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64; do target_os=${target%/*}; target_arch=${target#*/}; for pkg in ./core/cli ./core/ir ./core/store; do deps=$(CGO_ENABLED=0 GOOS="$target_os" GOARCH="$target_arch" GOTOOLCHAIN=local go list -deps -f '{{.ImportPath}}' "$pkg"); if printf '%s\n' "$deps" | grep -Fx "$zsh_pkg" &gt;/dev/null; then printf 'forbidden transitive provider dependency: %s/%s %s -> %s\n' "$target_os" "$target_arch" "$pkg" "$zsh_pkg" &gt;&amp;2; exit 1; fi; if printf '%s\n' "$deps" | awk '$0 == "golang.org/x/sys" || index($0,"golang.org/x/sys/") == 1 { found=1 } END { exit !found }'; then printf 'forbidden golang.org/x/sys dependency: %s/%s %s\n' "$target_os" "$target_arch" "$pkg" &gt;&amp;2; exit 1; fi; done; done; phase_section=$(node /home/metanmai/.codex/gsd-core/bin/gsd-tools.cjs query roadmap.get-phase 6); scope=$(printf '%s\n' "$phase_section"; cat .planning/phases/06-ingest-end-to-end/06-0[1-5]-PLAN.md); set +e; detector_json=$(printf '%s' "$scope" | node /home/metanmai/.codex/gsd-core/bin/lib/api-coverage.cjs --json); detector_status=$?; set -e; test "$detector_status" -eq 1; printf '%s' "$detector_json" | node -e 'let s="";process.stdin.on("data",d=&gt;s+=d).on("end",()=&gt;{let r;try{r=JSON.parse(s)}catch{process.exit(1)}if(r.detected!==false||!Array.isArray(r.signals)||r.signals.length!==0)process.exit(1)})'; test ! -e .planning/phases/06-ingest-end-to-end/COVERAGE.md; if find .planning/phases/06-ingest-end-to-end -maxdepth 1 -type f -name '*-COVERAGE.md' -print -quit | grep -q .; then exit 1; fi; evidence=.planning/phases/06-ingest-end-to-end/06-MACOS-RUNTIME-EVIDENCE.md; test -s "$evidence"; rg -q '^status: PASS$' "$evidence"; evidence_sha=$(awk -F': ' '$1 == "implementation_sha" { print $2 }' "$evidence"); test "$(printf '%s' "$evidence_sha" | wc -c)" -eq 40; test "$evidence_sha" = "$(git rev-parse HEAD)"; git cat-file -e "${evidence_sha}^{commit}"; rg -q '^authority: (ci|operator):' "$evidence"; rg -q '^native_os: macOS ' "$evidence"; rg -q '^architecture: ' "$evidence"; rg -q -x 'go_toolchain: local' "$evidence"; rg -q -x 'go_env_goversion: go1[.]25[.][0-9]+' "$evidence"; rg -q '^filesystem: ' "$evidence"; rg -q '^exchange: PASS$' "$evidence"; rg -q '^no_replace_absent: PASS$' "$evidence"; rg -q '^no_replace_late_target: PASS$' "$evidence"; rg -q '^cleanup_top_level: PASS$' "$evidence"; rg -q '^cleanup_nested_file: PASS$' "$evidence"; rg -q '^cleanup_nested_directory: PASS$' "$evidence"; rg -q '^cleanup_symlink: PASS$' "$evidence"; rg -q '^cleanup_replacement_before: PASS$' "$evidence"; rg -q '^cleanup_replacement_after: PASS$' "$evidence"; rg -q '^cleanup_capability: (supported|unsupported-retained)$' "$evidence"; git diff --quiet "$evidence_sha" -- core scripts/verify-phase06-macos-runtime.sh</automated>
  </verify>
  <resume-signal>Place the complete evidence file at the exact path and type `evidence ready`; the executor then runs the automated verification above.</resume-signal>
  <done>Attributable native macOS evidence matches the exact implementation SHA, records local Go 1.25.x, passes atomic rows plus every real store-cleanup class/seam row with truthful supported-or-retained semantics, and the unchanged implementation passes every final automated gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Authored fixtures -> expected assertions | Production output must not author its own oracle. |
| Secret-bearing Profile -> final/quarantine Git objects | Refs-only scans can miss leaked unreachable objects. |
| Ingest parser -> host process | Source commands must remain inert until the explicit sandbox oracle. |
| Built binary -> isolated user state | HOME/cache/store/path effects must stay under test-owned directories. |
| Planning scope -> seal gates | Detector invocation/status/JSON failures must not silently become non-detection. |
| External native macOS authority -> phase completion | Cross-build or unauthenticated/mismatched output must not substantiate Darwin atomic or production quarantine-cleanup behavior. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V8.3 maps to complete object-database secret scans; V5.2 maps to no-execution canaries and isolated oracle execution; V12.3 maps to temp-owned binary/filesystem state; V7.4 maps to exit/output/partial-state proof. Execution is blocked while any critical/high threat lacks the named automated test.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-05-01 | Information Disclosure | final and quarantine Git objects | critical | mitigate | Enumerate every object with Git batch-all-objects in final and retained object environments; scan blob bytes for the literal canary. |
| T-06-05-02 | Elevation of Privilege | fixture command substitutions | critical | mitigate | Ingest canaries must remain absent; only guarded isolated zsh -f oracle execution is allowed afterward. |
| T-06-05-03 | Tampering | expected fixture/SecretRef oracle | high | mitigate | Authored provenance validates raw spans; redaction-aware normalization requires an authoritative committed match and exact sentinel contract, while a forged source sentinel fails. |
| T-06-05-04 | Spoofing | built-binary store composition | high | mitigate | Isolated explicit shared-root run proves one initialized store/main/cache layout and exact CLI behavior. |
| T-06-05-05 | Tampering | module/layer boundaries | high | mitigate | Base-vs-HEAD/index/worktree byte checks and go-list transitive dependency gate. |
| T-06-05-06 | Repudiation | phase-scope detector | medium | mitigate | Capture status/output separately; require status 1 plus typed false/empty JSON and absent matrix artifact. |
| T-06-05-07 | Spoofing | native macOS runtime evidence | high | mitigate | Native-only verifier binds exact implementation SHA and local Go 1.25.x; evidence records authority/toolchain/GOVERSION/platform/filesystem/unedited output and blocks completion until atomic plus all six production-cleanup rows pass with truthful capability classification. |
| T-06-05-SC | Tampering | Go toolchain/package execution | high | mitigate | Every test/vet/list/build/make/native verifier rejects non-local or non-Go-1.25.x execution; `GOTOOLCHAIN=local` forbids automatic downloads and immutable manifests prove no package surface change. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | G-06-01 | Real file Parse → classify/build → baseline commit | 06-04; 06-05 Tasks 1-2 | COVERED | Built binary and real store/provider. |
| GOAL | G-06-02 | Declarative captured, imperative routed, none dropped | 06-03/06-04; 06-05 Task 2 | COVERED | Provenance and accounting oracle. |
| GOAL | G-06-03 | Idempotent install plus append warning/preservation | 06-03/06-04; 06-05 Task 1 | COVERED | Two-run and authored append fixture. |
| GOAL | G-06-04 | Behavior-equivalent committed round trip | 06-05 Task 2 | COVERED | Phase 2 oracle mechanics. |
| REQ | PROF-03 | Secret exclusion/reference plus value-free report | 06-01/06-02/06-04; 06-05 Task 2 | COVERED | Final and retained-object scan plus rerun backend counts. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| REVIEW | R-07 | Local Go 1.25.x only | 06-05 Tasks 1-3 | COVERED | Built-binary builds, E2E, dependency inspection, final make gate, and native evidence all enforce `GOTOOLCHAIN=local` plus Go 1.25.x. |
| CONTEXT | D-01 | Canonical verb/path/json/default/strict positional | 06-04; 06-05 Task 1 | COVERED | Built binary valid/invalid cases. |
| CONTEXT | D-02 | CLI.Run and exits/one JSON object | 06-04; 06-05 Task 1 | COVERED | Exact exit/output gates. |
| CONTEXT | D-03 | Human summary and zero-drop reconciliation | 06-04; 06-05 Tasks 1-2 | COVERED | Exact output plus provenance counts. |
| CONTEXT | D-04 | Structured JSON | 06-04; 06-05 Task 1 | COVERED | One parsed object. |
| CONTEXT | D-05 | EffectiveManaged only | 06-03/06-04; 06-05 Task 2 | COVERED | Authored routing table. |
| CONTEXT | D-06 | Imperative master, no declaration duplication | 06-03/06-04; 06-05 Task 2 | COVERED | Expected-master byte oracle. |
| CONTEXT | D-07 | Reuse landed installer | 06-03; 06-05 Tasks 2-3 | COVERED | Shared order/promotion transaction plus native real-helper Darwin cleanup evidence. |
| CONTEXT | D-08 | Store withheld report, no values | 06-02/06-04; 06-05 Task 2 | COVERED | Name/line and all-object scans plus exact dynamic-entry rerun exclusion in human/JSON output. |
| CONTEXT | D-09 | Commit failures runtime/fail closed | 06-02/06-04; 06-05 Task 2 | COVERED | Only update-ref response observation is ambiguous; deterministic backend/object/cleanup failures remain classified. |
| CONTEXT | D-10 | Init then deterministic main commit | 06-01/06-04; 06-05 Task 1 | COVERED | Same-store first run and main ref. |
| CONTEXT | D-11 | Post-END warning, no error/mutation | 06-03/06-04; 06-05 Task 1 | COVERED | Authored append case. |
| CONTEXT | D-12 | Exact marker-line scan | 06-03; 06-05 Task 1 | COVERED | Exact marker count/bytes. |
| CONTEXT | D-13 | Real E2E Phase 2 oracle | 06-05 Task 2 | COVERED | Raw non-secret plus authority-bounded SecretRef normalization, then full structure/byte/guarded behavior. |
| CONTEXT | D-14 | Deterministic authored fixture | 06-05 Tasks 1-2 | COVERED | Three testdata sources, provenance, dynamic rerun snapshot, and exact named gates. |
| CONTEXT | D-15 | Same injected Store; layering | 06-03/06-04; 06-05 Tasks 1,3 | COVERED | Built binary and go-list gate. |
| CONTEXT | D-16 | Extend landed injection seam | 06-03/06-04; 06-05 Task 1 | COVERED | One composition path. |
| SPEC | S-06-01 | Main profile, managed subset, imperative master, zero drop | 06-04; 06-05 Task 2 | COVERED | Real provenance/accounting. |
| SPEC | S-06-02 | Literal secret excluded/reported; dynamic secret retained | 06-02/06-04; 06-05 Task 2 | COVERED | All-object and rerun proofs. |
| SPEC | S-06-03 | Byte-idempotent rerun/one marker pair | 06-03/06-04; 06-05 Task 1 | COVERED | Built binary two-run diff. |
| SPEC | S-06-04 | Append warning/byte preservation/exit success | 06-03/06-04; 06-05 Task 1 | COVERED | Authored installed fixture. |
| SPEC | S-06-05 | Behavior-equivalent Store.Read/regenerate round trip | 06-05 Task 2 | COVERED | Full structural equality after exact redaction-contract normalization, plus byte/behavior oracle. |

### Excluded deferred ideas

| Source | Item | Disposition |
| --- | --- | --- |
| CONTEXT Deferred | Runtime SecretRef dereference | EXCLUDED — owned by its existing runtime scope, not Phase 6 ingest. |
| CONTEXT Deferred | Replacing the installer writer | EXCLUDED — D-07 requires reuse/extension of the landed installer. |
| CONTEXT Deferred | Override-editing CLI | EXCLUDED — not part of ingest. |
| CONTEXT Deferred | Multiple input files | EXCLUDED — D-01 locks zero/one path. |
| CONTEXT Deferred | Auto-cd, sharing, other shells | EXCLUDED — outside this zsh ingest phase. |
| CONTEXT Deferred | Ingesting post-END appends | EXCLUDED — D-11 locks warning/preservation only. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. The plan set carries its implicit test-integrity/privacy boundaries explicitly: authored fixtures cannot be derived from production output, and secret absence must cover unreachable/retained objects. Both are resolved through Task 2's named tests.
</spec_fallback>

<verification>
Run the built-binary and real E2E suites, all touched package suites, vet, `GOTOOLCHAIN=local make check`, exact reviewed-base module comparison across base/HEAD/index/worktree, concrete-provider plus x/sys transitive dependency inspection, the exact-status phase-scope detector, and the blocking native macOS exact-SHA/local-Go-1.25.x atomic plus real store-cleanup checkpoint.
</verification>

<success_criteria>
- All four ROADMAP truths, PROF-03, all 16 locked decisions, and all five SPEC requirements have executable coverage.
- Real command/store/provider evidence proves CLI, transaction, rerun, append, secret, and round-trip behavior.
- No literal secret exists in any final or retained candidate object.
- Ingest performs no source execution.
- Dependencies and layering are unchanged from the literal reviewed execution base.
- Native macOS exchange/no-replace/late-target behavior and production quarantine cleanup for top-level/nested-file/nested-directory/symlink/before-replacement/after-replacement rows pass on the exact implementation SHA under an attributable external authority; unsupported cleanup is accepted only as untouched retained/recovery proof, and cross-build is not runtime proof.
- Every independent verifier, built-binary build, cross-build, dependency query, aggregate check, and native run refuses non-local or non-Go-1.25.x execution; no automatic toolchain download can satisfy completion.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-05-SUMMARY.md` when done.
</output>
