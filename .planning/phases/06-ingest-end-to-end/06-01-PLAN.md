---
phase: 06-ingest-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/cli/install.go
  - core/cli/install_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "The ingest on-ramp reuses the landed Phase 5 loader-cache and exact-marker installer, including its rollback and path-safety behavior, per D-07 and D-12."
    - "A first ingest can replace only the pre-marker source region with the imperative master bytes while retaining one canonical managed block and every post-END append byte-for-byte, per D-06 and D-11."
    - "Concurrent zsh-pro writers are serialized from snapshot through promotion, and an external edit detected during the guarded promotion interval is preserved and returns a stale-target error; the plan does not claim a portable strict CAS against non-cooperating processes."
    - "The idempotency edge is explicit: preparing and promoting the same ingest result twice leaves the complete file byte-identical and retains exactly one exact BEGIN/END pair."
  artifacts:
    - path: "core/cli/install.go"
      provides: "Reusable explicit-target installer, exact managed-region scan, imperative-master rendering, serialized promotion, and recoverable stale-target handling"
      contains: "prepareIngestZshrc"
    - path: "core/cli/install_test.go"
      provides: "Mutation-resistant marker, append-preservation, idempotency, and stale-writer tests"
      contains: "TestPrepareIngestZshrc"
  key_links:
    - from: "runInstallWithStoreInitialization"
      to: "runInstallAt"
      via: "the existing install command delegates to the same explicit-target transaction ingest will call"
      pattern: "runInstallAt"
    - from: "scanZshrcLayout"
      to: "installBegin and installEnd"
      via: "one exact physical-line scanner using the landed marker constants"
      pattern: "install(Begin|End)"
    - from: "prepareAtomicWriteAgainst"
      to: "preparedWrite.promoteGuarded"
      via: "a per-target lease spans snapshot through promotion while a same-directory recovery hard link preserves writes to the original inode detected during the promotion interval"
      pattern: "promoteGuarded"
  prohibitions:
    - statement: "MUST NOT clobber, move, normalize, or silently absorb installer-appended content after the exact managed END marker."
      category: safety
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT overwrite a user-edited .zshrc with an ingest candidate prepared from older bytes."
      category: safety
      status: unresolved
      verification: null
      flagged: true
  flagged_assumptions:
    - id: PROF-03-idempotency-edge
      status: resolved
      verification: explicit
      resolution: "Focused tests run the same transform and guarded promotion twice, require complete-file byte equality, and require one exact marker pair."
    - id: PROF-03-concurrency-edge
      status: resolved
      verification: explicit
      resolution: "The implementable guarantee is explicit: an OS advisory lease serializes cooperating zsh-pro writers from read through promotion; a same-directory recovery hard link to the original inode plus post-promotion validation preserves and restores an external edit injected after validation but before replacement. Portable filesystems do not offer content-CAS against arbitrary non-cooperating writers, so the plan does not claim that impossible guarantee; uncertain recovery retains both files and reports their paths. Cross-resource recovery after a store commit remains owned by Plan 06-02."
---

<objective>
Turn the landed Phase 5 installer into the safe filesystem boundary the ingest flow can reuse.

Purpose: Preserve the installer’s cache validation, exact markers, rollback, symlink defense, and byte-safe replacement while adding the one missing operation: replace the pre-marker source with an imperative master and refuse a stale final write.
Output: An explicit-target install transaction, a shared exact-region scanner, deterministic master preparation, and guarded atomic promotion.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-02-SUMMARY.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-06-SUMMARY.md
@core/cli/install.go
@core/cli/install_test.go

<interfaces>
Current installer contracts to preserve:
- `installBegin` / `installEnd` are the single exact physical-line marker constants.
- `resolveInstallPaths() (installPaths, error)` validates absolute HOME, ZDOTDIR, and ZSHPRO_HOME.
- `runInstallWithStoreInitialization(shell.Hooker, StoreInitializer) error` owns the loader + .zshrc transaction.
- `replaceManagedBlock(current, block []byte) ([]byte, error)` rejects malformed exact markers and preserves outside bytes.
- `prepareAtomicWrite(path, content []byte, defaultMode os.FileMode) (preparedWrite, error)` prepares a same-directory fsynced candidate.
- `(*preparedWrite).promote() error` renames the prepared candidate and syncs its directory.
</interfaces>
</context>

## Artifacts this phase produces

- New path: none.
- New CLI flag or verb: none in this plan.
- New private symbols in `core/cli/install.go`: `runInstallAt`, `zshrcLayout`, `scanZshrcLayout`, `renderImperativeMaster`, `prepareIngestZshrc`, `prepareAtomicWriteAgainst`, `targetSnapshot`, `targetLease`, and `(*preparedWrite).promoteGuarded` (exact names may retain Go-private casing, but there must be one role per symbol and no duplicate marker scanner).
- New struct fields: `zshrcLayout` records marker presence plus byte-exact `before` and `after` regions; `targetSnapshot` records existence and a content digest; `preparedWrite` records the optional expected snapshot used only by guarded ingest promotion.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse the Phase 5 install transaction at an explicit ingest target</name>
  <files>core/cli/install.go, core/cli/install_test.go</files>
  <read_first>
    - core/cli/install.go: `runInstallWithStoreInitialization`, `resolveInstallPaths`, `replaceManagedBlock`, `prepareAtomicWrite`, and loader rollback ordering
    - core/cli/install_test.go: `TestInstallIdempotentPreservesUserContent`, exact-marker cases, cache-preservation cases, and promotion-failure rollback cases
    - .planning/phases/05-runtime-loader-cli-bootstrap/05-02-SUMMARY.md and 05-06-SUMMARY.md for the landed transaction guarantees
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-06, D-07, D-11, and D-12
  </read_first>
  <behavior>
    - Existing `zsh-pro install` behavior remains byte-for-byte compatible and delegates to a helper that accepts one already-resolved absolute `.zshrc` target.
    - The explicit-target helper installs and validates the same cached loader, renders the same canonical block, initializes storage through the same callback, and retains the same rollback order.
    - A relative, empty, or symlink-unsafe explicit mutation target is rejected before cache, store, or `.zshrc` mutation.
  </behavior>
  <action>
    Extract `runInstallAt(provider shell.Hooker, zshrcPath string, initializeStore StoreInitializer) error` from the current `runInstallWithStoreInitialization` body. Keep `resolveInstallPaths` authoritative for the runtime cache directory and the ordinary install command’s `.zshrc`; the new helper receives only an absolute explicit `.zshrc` target so Phase 6 can honor D-01’s path override without changing ZSHPRO_HOME cache resolution. Make the existing installer delegate to this helper, retaining current preparation-before-mutation ordering, candidate validation deadline, fsync, symlink resolution, loader-first promotion, `.zshrc` rollback, and store compensation. Add focused tests proving the ordinary install result is unchanged, an explicit target receives the canonical block and cached loader, and unsafe targets fail without mutation. This is a refactor of the landed installer per D-07/D-12, not a second block writer.
  </action>
  <acceptance_criteria>
    - `runInstallWithStoreInitialization` and `runInstallAt` share one transaction implementation and one marker renderer.
    - Existing installer safety/idempotency tests remain green.
    - An explicit absolute target is installed successfully; rejected target shapes leave cache, store callback effects, and target bytes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/cli -run 'Test.*Install.*(Explicit|Idempotent|Marker|Rollback|Promotion|Path)' -count=1</automated>
  </verify>
  <done>The ingest command can invoke the complete landed installer transaction against its selected file without duplicating marker, cache, or rollback logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare a byte-stable imperative master and reject stale promotion</name>
  <files>core/cli/install.go, core/cli/install_test.go</files>
  <read_first>
    - core/cli/install.go: exact-line loop in `replaceManagedBlock`, `preparedWrite`, `resolveWriteTarget`, `writeTemp`, and `syncDirectory`
    - core/cli/install_test.go: exact physical marker tests, duplicate-region behavior, symlink/mode preservation, and direct byte snapshots
    - core/model/profile.go: `Entry.Text`, `Entry.StartLine`, `EffectiveManaged`, and `Representable`
    - core/ir/regen.go: newline and source-order behavior used by the Phase 2 oracle
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-05, D-06, D-11, D-12, and D-14
  </read_first>
  <behavior>
    - One exact scanner returns the bytes before the managed BEGIN line and after the managed END line, rejects every malformed ordering the installer already rejects, and identifies non-blank/non-comment physical lines after END without parsing or moving them.
    - A first adoption writes imperative `Entry.Text` in source order before the canonical managed block; managed entries contribute newline placeholders so surviving imperative `StartLine` positions remain reconstructable.
    - Re-preparing an already ingested unchanged file leaves its master, managed block, and trailing append bytes identical and reports append warnings without error.
    - Guarded promotion holds a per-target OS advisory lease across snapshot, preparation, validation, replacement, and directory fsync so concurrent zsh-pro install/ingest writers serialize.
    - An external write to the original inode injected after pre-promotion validation but before replacement is detected through a same-directory recovery hard link; the edited original bytes are restored atomically and the candidate is retained or discarded only after recovery is proven.
  </behavior>
  <action>
    Factor the exact physical-line scan used by `replaceManagedBlock` into `scanZshrcLayout` so marker recognition has one source of truth. The scan must expose byte slices before BEGIN and after END without normalizing CRLF or final-newline state and must return line-number warnings only for subsequent non-blank/non-comment content, per D-11/D-12. Add `renderImperativeMaster(profile model.Profile) ([]byte, statementCount, physicalLineCount int)` that consumes D-05’s existing `EffectiveManaged()` partition: copy unmanaged `Text` verbatim in source order and replace managed entry spans with the same number of newline separators so original statement positions remain available to the end-to-end oracle; add no classifier or shell execution. Add `prepareIngestZshrc(current, master []byte, replaceMaster bool)` that uses `renderInstallBlock` and the shared scanner: for initial adoption replace the pre-BEGIN region with the supplied master, for re-ingest preserve it, and always preserve post-END bytes while returning warnings. Extend `preparedWrite` with an optional expected target snapshot populated by `prepareAtomicWriteAgainst`. Add a per-resolved-target lease backed by a sibling lock file and an OS advisory exclusive lock, using only the standard library/syscall surface supported by the project's macOS/Linux targets; acquire it before the target snapshot and hold it through final directory fsync so every zsh-pro writer uses one serialized transaction. Implement `promoteGuarded` as a recoverable protocol: create a unique same-directory `os.Link` recovery name for the validated target so writes through an already-open descriptor remain visible on the same inode, fsync the directory, verify target identity/content against the snapshot, invoke a package-private test seam immediately after that validation, atomically promote the candidate, then validate the recovery hard link against the bytes that were promoted over before unlinking it. If the seam or a non-cooperating writer changes the old inode during that interval, atomically restore the recovery link over the candidate, fsync the directory, return the stable stale-target error, and do not report success. If hard-link setup is unsupported, fail before replacement; if recovery cannot be proven, retain both candidate/recovery artifacts and return an error that names their paths rather than deleting possible user content. Keep the existing unguarded `promote` behavior for the already-tested install transaction. Explicitly document that the strict serialization guarantee applies to cooperating zsh-pro writers and the adversarial hard-link test covers writes to the original inode; arbitrary pathname replacement by external processes does not participate in the advisory lease and is not presented as portable strict CAS. Test first adoption, installed-first adoption, unchanged second run, CRLF/no-final-newline inputs, exact marker count, trailing append warnings/no-clobber, two concurrent zsh-pro writers, and an adversarial edit injected after validation but before replacement that is recovered byte-for-byte.
  </action>
  <acceptance_criteria>
    - Marker topology is parsed by one implementation used by install and ingest.
    - Every imperative entry is present verbatim in the master output; managed entries are absent as executable statements but retain line-position padding.
    - A second unchanged transform is complete-file byte-identical and retains one marker pair.
    - Trailing appended content produces warnings and remains byte-identical.
    - Cooperating zsh-pro writes serialize across the comparison-through-promotion interval.
    - The adversarial after-validation/before-replacement mutation test returns stale, restores the edited bytes byte-for-byte, and leaves no ambiguous success; an unchanged target retains the landed atomic-write guarantees.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/cli -run 'Test.*(PrepareIngestZshrc|RenderImperativeMaster|ZshrcLayout|PromoteGuarded|ConcurrentPromotion|ManagedBlock)' -count=1</automated>
  </verify>
  <done>The filesystem half of ingest is deterministic and idempotent, serializes zsh-pro writers, preserves trailing appends, and recovers an external edit observed during the guarded promotion seam without claiming impossible portable CAS semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| user-owned `.zshrc` -> marker scanner | Arbitrary bytes and marker-shaped text decide which region may be replaced. |
| selected path -> filesystem mutation | User/environment-controlled paths cross symlink and ownership boundaries. |
| observed target -> prepared candidate rename | The target can change after read and before promotion. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies to this local CLI boundary. V1.1 input-validation controls map to exact marker/path validation, V1.14 configuration controls map to symlink-safe resolved targets and private sibling transaction files, and V7.4 error-handling controls map to non-success stale/recovery outcomes that do not discard user bytes. Execution and plan completion are blocked while any critical/high threat lacks both an executable mitigation and its automated verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-01-01 | Tampering | `scanZshrcLayout` / `prepareIngestZshrc` | high | mitigate | Recognize only exact physical marker lines, reject malformed topology before mutation, preserve post-END bytes, and test hostile marker-shaped content. |
| T-06-01-02 | Elevation of Privilege | `runInstallAt` / `resolveWriteTarget` | high | mitigate | Require an absolute selected target and retain the landed symlink-safe resolution, same-directory candidate, mode preservation, and parent validation. |
| T-06-01-03 | Tampering | `promoteGuarded` | high | mitigate | Hold the per-target advisory lease through promotion, preserve a same-inode recovery hard link, inject mutation after validation, and prove stale detection restores the edited original byte-for-byte. |
| T-06-01-04 | Denial of Service | interruption during file replacement | medium | mitigate | Fsync the candidate, use same-directory atomic rename, sync the directory, and test that pre-promotion failure leaves the original target and candidate cleanup path intact. |
| T-06-01-SC | Tampering | package installation | low | accept | No package-manager command or dependency change occurs; the plan uses the existing Go standard library and landed zsh provider. |
</threat_model>

<pre_mortem>
| Likely failure | Early mitigation |
| --- | --- |
| A new scanner recognizes a different region than Phase 5 and silently moves user bytes. | Extract one shared exact-line scanner first and run all existing malformed/CRLF marker tests against it. |
| Idempotence is asserted only for the managed block while master or append bytes drift. | Compare complete-file bytes across the second transform and separately assert one exact marker pair. |
| A prepared candidate overwrites an installer append created during promotion. | Inject the edit after final validation but before replacement and require `promoteGuarded` to restore the edited recovery copy and return stale; serialize all cooperating zsh-pro writers with the target lease. |
</pre_mortem>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Compose real-file ingest with idempotent managed install and append warnings | 06-01 Tasks 1-2 | COVERED | Filesystem foundation for the full on-ramp. |
| REQ | PROF-03 | Safe end-to-end secret-excluding ingest | 06-01 Tasks 1-2 | COVERED | Provides the non-destructive target transaction consumed by secret-aware commit. |
| RESEARCH | — | None: research was explicitly skipped | — | EXCLUDED | PATTERNS.md and current landed code are the implementation sources. |
| CONTEXT | D-05 | Consume `EffectiveManaged` without reclassification | 06-01 Task 2 | COVERED | Master renderer uses the existing predicate. |
| CONTEXT | D-06 | Distinct installer-preserved master region | 06-01 Task 2 | COVERED | Pre-marker master, canonical managed block, preserved suffix. |
| CONTEXT | D-07 | Reconcile against actual Phase 5 installer | 06-01 Task 1 | COVERED | Current `runInstallWithStoreInitialization` is extended directly. |
| CONTEXT | D-11 | Warn and preserve post-END appends | 06-01 Task 2 | COVERED | Read-only scan plus byte-equality tests. |
| CONTEXT | D-12 | Reuse exact marker constants; no AST append scan | 06-01 Task 2 | COVERED | One shared exact-line scanner. |
| CONTEXT | D-14 | Real-shaped idempotency/append fixture coverage | 06-01 Task 2, 06-03 | COVERED | Unit properties here; production-shaped fixtures in 06-03. |
</source_audit>

<spec_fallback>
The automatic two-stage prohibition probe ran because 06-SPEC.md has no Prohibitions section. Stage 1 recalled risks across loss of user-owned bytes, stale-writer clobber, routine marker-parser correctness, path traversal, and shell injection. Stage 2 dropped routine correctness items and referred generic path/injection canon to the STRIDE register. It kept exactly two bespoke safety prohibitions for this plan: post-END append preservation and stale-target overwrite prevention. Both are projected verbatim in `must_haves.prohibitions` as descriptor-less, `status: unresolved`, `verification: null`, and flagged. Equality: 2 kept = 2 projected; 0 dismissed and 0 silently dropped.
</spec_fallback>

<verification>
Run the focused installer/ingest filesystem tests, then `GOTOOLCHAIN=auto go test ./core/cli -count=1` to prove the refactor preserves every landed Phase 5 installer regression.
</verification>

<success_criteria>
- The existing install command and the new explicit-target path share one cache/marker transaction.
- Imperative master preparation is source-ordered, statement-accounted, and repeatable without byte drift.
- Exact post-END appends warn but are never mutated.
- Cooperating zsh-pro writers serialize, and an external edit injected during promotion is recovered byte-for-byte with a stale error rather than false success.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md` when done.
</output>
