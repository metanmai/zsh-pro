---
phase: 06-ingest-end-to-end
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE
  - core/model/withheld.go
  - core/model/ingest_transaction.go
  - core/store/git.go
  - core/store/store.go
  - core/store/install_transaction.go
  - core/store/store_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-10: baseline discovery resolves `refs/heads/main` once; durable `profile.json` presence and Profile bytes come from that exact immutable revision, and later commit receives the same expected revision."
    - "A committed empty Profile reports persisted=true while the fixed Init-only empty-tree baseline reports false; an operational probe failure fails Begin rather than becoming absence."
    - "Candidate blobs, trees, and commits remain in a unique mode-0700 sibling object quarantine through commit-tree and are visible only through an explicit Git object environment."
    - "A clean failure after commit-tree but before any final-object/backend/ref effect durably removes only the authenticated quarantine object held by the transaction and leaves a fresh initializer's sealed store tree eligible for exact rollback."
    - "Abort retains authenticated parent/quarantine handles plus recorded device/inode identity from creation. It refuses recursive cleanup when the current quarantine basename is missing, replaced, symlinked, or identity-mismatched before cleanup or after the first identity check; the replacement is untouched and the result is recovery-required with initializer rollback disabled."
    - "Every transaction is identified by an opaque random token registered on the exact Store instance; concurrent Begin calls cannot overwrite one another's expected revision/quarantine state."
    - "D-15/D-16: the baseline/status contracts live in `core/model`, so `core/cli` can extend the landed injection seam without importing the concrete store."
  artifacts:
    - path: ".planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE"
      provides: "Immutable pre-execution commit for final module-manifest comparison"
    - path: "core/model/ingest_transaction.go"
      provides: "Opaque baseline token and typed commit/abort ref/backend/object evidence"
      exports: ["IngestBaseline", "IngestCommitOutcome", "IngestAbortOutcome"]
    - path: "core/model/withheld.go"
      provides: "Store-neutral value-free withheld name/line metadata used by the typed outcome"
      exports: ["WithheldSecret", "WithheldReport"]
    - path: "core/store/store.go"
      provides: "Exact-revision Begin/Abort transaction registry"
      contains: "BeginIngest"
    - path: "core/store/git.go"
      provides: "Fallible exact-revision probes and quarantine/alternate object environment"
      contains: "GIT_OBJECT_DIRECTORY"
    - path: "core/store/install_transaction.go"
      provides: "Fresh initializer ownership eligibility plus descriptor-bound quarantine ownership and identity-safe abort cleanup"
  key_links:
    - from: "Store.BeginIngest"
      to: "resolve main -> exact ls-tree/profile read -> transaction quarantine"
      via: "one expected revision stored under an opaque transaction ID"
      pattern: "BeginIngest"
    - from: "candidate Git plumbing"
      to: "quarantine objects + final alternates"
      via: "explicit per-command environment; mutable branch names are not used for read-tree/parent"
      pattern: "GIT_ALTERNATE_OBJECT_DIRECTORIES"
    - from: "Store.AbortIngest"
      to: "InitForInstall rollback eligibility"
      via: "retained parent/quarantine handles, device/inode revalidation, descriptor-relative cleanup, and durable parent sync leave the sealed final-store tree unchanged"
      pattern: "InitializerRollbackSafe"
  prohibitions:
    - statement: "MUST NOT treat a failed exact-revision profile-object probe as an absent profile."
      category: safety
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT place candidate ingest objects in the final store before a ref transaction is prepared."
      category: integrity
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Create the exact-revision baseline token and isolated candidate-object transaction that every later ingest effect depends on.

Purpose: Eliminate mutable-branch probe/read races, distinguish committed-empty from Init-only state, and keep clean failed candidates outside a fresh initializer-owned store so rollback evidence remains truthful.
Output: Immutable execution base, neutral transaction/outcome types, exact Begin/Abort surface, quarantine Git plumbing, initializer-ownership integration, and focused tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/04-manifest-builder-emit/04-11-SUMMARY.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-06-SUMMARY.md
@core/store/store.go
@core/store/git.go
@core/store/install_transaction.go
@core/store/store_test.go

<interfaces>
Current contracts to preserve and extend:
- `(*Store).InitForInstall(ctx) (InstallInitialization, error)` seals a newly created store tree and removes it only while ownership/tree evidence still matches.
- `(*Store).Read(ctx, branch)` currently addresses a mutable branch and cannot combine exact durable presence with the same-revision Profile read.
    - `gitRunner` owns Git subprocess environment construction, object writers, refs, and per-instance test seams.
    - The fixed Init baseline is main pointing at an empty tree without `profile.json`; a later committed empty Profile contains a durable empty profile object and must select re-ingest semantics.
    - Landed `installInitializationState.rollback` snapshots by pathname and calls `os.RemoveAll`; Phase 6 must not reuse that pathname-recursive pattern for transaction quarantine cleanup.
</interfaces>
</context>

## Artifacts this plan produces

- One immutable execution-base file captured before any Phase 6 Go edit.
- One neutral model transaction/outcome file; no store type crosses into CLI.
- Exact Begin/Abort store methods and candidate object quarantine.
- No package installation, module change, network service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the execution base and define truthful transaction evidence</name>
  <files>.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE, core/model/withheld.go, core/model/ingest_transaction.go, core/store/install_transaction.go, core/store/store_test.go</files>
  <read_first>
    - core/store/install_transaction.go: fresh-root creation, sealed-tree snapshot, rollback/finalize callbacks, and CreatedPath semantics
    - core/store/store_test.go: InitForInstall fresh/preexisting ownership, mutation-refusal, and mode restoration cases
    - core/model/profile.go: Profile and entry/SecretRef value shapes that may cross the neutral boundary
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-09, D-10, D-15, and D-16
  </read_first>
  <behavior>
    - Before production/test edits, `06-EXECUTION-BASE` contains exactly the current full 40-character HEAD commit plus LF and is never rewritten.
    - Model-owned evidence can represent committed, not-committed, conflict, recovery-required; expected/candidate/other/unknown ref state; backend unchanged/applied/restored/uncertain; objects quarantined/removed/published/retained/uncertain; fixed failure code; recovery flag; and initializer-rollback eligibility.
    - `IngestBaseline` carries opaque token, branch, exact expected revision, exact-revision Profile, durable profile-object presence, and invocation-created-store ownership without user-output annotations.
    - InitForInstall ownership remains tied to canonical root plus sealed final-store state; only a proven clean pre-publication abort may retain rollback eligibility.
  </behavior>
  <action>
    As the first executor action, before any Go edit, resolve `git rev-parse --verify HEAD^{commit}` and create `06-EXECUTION-BASE` containing exactly that 40-character ID plus one LF. Fail if Phase 6 source/test edits predate capture; preserve orchestrator-listed unrelated worktree paths. Never rewrite this artifact in later tasks/plans.

    Define `WithheldSecret{Name,StartLine}` and `WithheldReport` in `core/model/withheld.go`. Define value-only contracts in `core/model/ingest_transaction.go`: opaque transaction ID; baseline branch/expected revision/Profile/profile-object-presence/created-store ownership; typed status/evidence enums for ref, backend, objects, failure code, recovery requirement, initializer rollback, and the value-free withheld report. Raw hashes are internal fields consumed by the controller/store contract but are never DTO/user-output fields.

    Reconcile `InstallInitialization` with transaction ownership. Retain canonical root and exact invocation-created identity; expose only idempotent rollback/finalize behavior needed by the CLI adapter. Preserve the existing sealed-tree refusal when the final store changed. Add an explicit eligibility input/result so later Abort/Commit evidence, rather than `CreatedPath` alone, decides whether a fresh root may be removed. Do not delete a store when outcome evidence is uncertain or any final object/ref/backend effect may remain.

    Add tests for neutral status completeness, immutable execution-base format, fresh/preexisting initializer identity, clean eligible rollback, changed-tree refusal, repeated callback safety, and mismatched root/token refusal.
  </action>
  <acceptance_criteria>
    - Execution base is one valid immutable commit ID captured before Go changes.
    - Neutral evidence covers every state downstream plans switch on without a concrete-store import.
    - Fresh initializer deletion requires canonical ownership, sealed final tree, and explicit transaction eligibility.
    - Preexisting or uncertain stores are never removed.
  </acceptance_criteria>
  <verify>
    <automated>base_file=.planning/phases/06-ingest-end-to-end/06-EXECUTION-BASE &amp;&amp; test "$(wc -l &lt; "$base_file")" -eq 1 &amp;&amp; base_commit=$(tr -d '\n' &lt; "$base_file") &amp;&amp; test "$(printf '%s' "$base_commit" | wc -c)" -eq 40 &amp;&amp; git cat-file -e "${base_commit}^{commit}" &amp;&amp; tests='TestInitForInstallTransactionEligibility TestInitForInstallRejectsChangedOrMismatchedOwnership TestIngestTransactionEvidenceComplete' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/store -run '^(TestInitForInstallTransactionEligibility|TestInitForInstallRejectsChangedOrMismatchedOwnership|TestIngestTransactionEvidenceComplete)$' -count=1</automated>
  </verify>
  <done>The execution base and neutral evidence exist, and initializer rollback now requires explicit, exact transaction eligibility rather than fresh-path inference alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind durable baseline reads to an isolated candidate quarantine</name>
  <files>core/model/ingest_transaction.go, core/store/git.go, core/store/store.go, core/store/install_transaction.go, core/store/store_test.go</files>
  <read_first>
    - core/store/store.go: `Init`, `Read`, mutable ref probes, per-instance ref seams, and store fields suitable for a transaction registry
    - core/store/git.go: command/run/runCommit environment construction, `read-tree`, `hash-object`, `write-tree`, `commit-tree`, and exact object/ref probes
    - core/store/install_transaction.go after Task 1: transaction eligibility input and root validation
    - core/store/store_test.go: committed/uncommitted Read behavior and per-instance seam patterns
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-09, D-10, D-15, and D-16
  </read_first>
  <behavior>
    - `BeginIngest(ctx,"main",createdByInvocation)` resolves `refs/heads/main` once, probes `profile.json` with a fallible exact-revision operation, and reads `<expected>:profile.json` from that same revision.
    - Init-only absence and committed-empty presence are distinct; probe/read/decode failure aborts Begin with no transaction registration/effect.
    - A successful Begin creates a random opaque ID and unique mode-0700 sibling quarantine outside the final store root, retains authenticated handles for its parent and the quarantine itself, and records the quarantine device/inode from that live handle; two same-revision Begins have independent state.
    - Candidate hash/read-tree/write-tree/commit-tree uses the quarantine as object directory, final store as alternate, and exact expected revision as tree seed/parent; candidate objects do not appear under final objects.
    - A clean Abort authenticates the current basename against the retained quarantine handle and recorded device/inode, walks/removes children only through that retained handle, rechecks identity after the test seam and immediately before the final non-recursive basename removal, fsyncs the retained parent, and reports initializer rollback safe.
    - Replacement before cleanup or after the first identity check is detected by the repeated identity check; Abort does not traverse, unlink, rename, or rewrite the replacement and reports objects retained/uncertain, recovery required, initializer rollback unsafe.
  </behavior>
  <action>
    Add `BeginIngest` on `*Store`. Validate branch/root/created-store ownership, resolve the exact full ref once, and use `git ls-tree` or equivalent fallible exact-revision inspection to distinguish missing `profile.json` from operational failure. Decode the object through `<expected>:profile.json`; never call a branch-name existence boolean followed by branch-name Read. Register state behind a per-Store mutex under a cryptographically random opaque token; reject unknown, reused, cross-Store, and concurrently finalized tokens.

    Extend `gitRunner` with explicit object-environment execution. Candidate commands receive `GIT_OBJECT_DIRECTORY=<quarantine>/objects` and `GIT_ALTERNATE_OBJECT_DIRECTORIES=<final>/objects`; ordinary ref/final-store reads keep their existing environment. Create the unique mode-0700 quarantine as a validated basename beneath an authenticated retained parent handle outside the sealed final root. Immediately open and retain the quarantine directory handle without following symlinks, obtain device/inode from that handle, and register parent handle, quarantine handle, basename, device/inode, mode/owner evidence, and token together. Seed the temporary index/read-tree and candidate parent from the captured expected revision, never the mutable branch spelling. Keep candidate objects isolated through commit-tree and expose per-instance seams after commit-tree for deterministic abort tests.

    Implement idempotent `AbortIngest` without `os.RemoveAll(quarantinePath)` or any other recursive pathname cleanup. First compare a no-follow descriptor-relative stat of the registered basename under the retained parent with the retained quarantine handle's current stat and recorded device/inode/mode/owner. On mismatch, do not traverse or mutate that basename. On match, invoke a deterministic test seam, repeat the same identity comparison, then recursively enumerate/open/delete only relative children through the retained quarantine handle; never follow symlinks and reject unsupported entry types. Before removing the now-empty directory entry, repeat the parent-basename versus retained-handle identity check and use only a non-recursive descriptor-relative directory removal. Fsync the retained parent and close/clear registry state only after durable success. Any mismatch before cleanup, replacement injected after the first identity check, recursive cleanup uncertainty, final identity ambiguity, or parent fsync failure leaves replacement/unowned state untouched, retains recoverable evidence/handles as applicable, and returns objects retained-or-uncertain, recovery required, initializer rollback false. It never deletes the final store itself; the initializer callback performs that later only after matching clean evidence and its sealed tree.

    Make legacy `Read` delegate to an exact snapshot helper where practical so probe/read semantics cannot drift. Add exact-revision mutation tests that advance the branch between test seams and prove returned Profile/presence remain bound to expected; add committed-empty, probe failure, independent concurrent Begin, candidate isolation, and clean abort cases. Add two deterministic identity-race tests: replace the quarantine basename before Abort begins, and replace it from the seam after the first identity check but before descent/final removal. Each test must preserve the replacement inode, bytes, mode, and all descendants exactly, return recovery required with initializer rollback unsafe, and prove no recursive operation ran against the replacement.
  </action>
  <acceptance_criteria>
    - Profile presence and bytes are derived from one expected revision.
    - Committed-empty, Init-only, and probe failure have distinct results.
    - Candidate objects remain outside the final object database through commit-tree.
    - Clean abort leaves the final store sealed tree identical and authorizes initializer rollback; uncertain cleanup does not.
    - Quarantine replacement before cleanup or after the first identity check is never recursively deleted or otherwise mutated and always yields recovery-required evidence.
    - Concurrent tokens cannot overwrite/cross-finalize one another.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestBeginIngestBindsReadToExpectedRef TestBeginIngestDistinguishesCommittedEmpty TestBeginIngestProbeFailureFailsClosed TestBeginIngestIndependentTransactions TestIngestQuarantinePostCommitTreeAbort TestIngestAbortPreservesInitializerOwnership TestAbortIngestRejectsQuarantineReplacementBeforeCleanup TestAbortIngestRejectsQuarantineReplacementAfterIdentityCheck' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/store -run '^(TestBeginIngestBindsReadToExpectedRef|TestBeginIngestDistinguishesCommittedEmpty|TestBeginIngestProbeFailureFailsClosed|TestBeginIngestIndependentTransactions|TestIngestQuarantinePostCommitTreeAbort|TestIngestAbortPreservesInitializerOwnership|TestAbortIngestRejectsQuarantineReplacementBeforeCleanup|TestAbortIngestRejectsQuarantineReplacementAfterIdentityCheck)$' -count=1 &amp;&amp; GOTOOLCHAIN=auto go test ./core/model ./core/store -count=1 &amp;&amp; GOTOOLCHAIN=auto go vet ./core/model ./core/store</automated>
  </verify>
  <done>One opaque store transaction now owns exact baseline discovery, same-revision durable presence, isolated candidate objects, and truthful clean/uncertain Abort evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Mutable main ref -> exact baseline token | Concurrent commits can otherwise mix probe, Profile bytes, tree seed, and parent. |
| Candidate object environment -> final store | A failed candidate must not mutate a fresh initializer's sealed tree before ref preparation. |
| Opaque token -> Store transaction registry | Cross-instance/reused tokens could abort another transaction. |
| Abort cleanup -> initializer rollback | Cleanup/durability evidence decides whether fresh-root deletion is safe. |
| Quarantine pathname -> retained quarantine object | A concurrent rename/replacement must not redirect recursive cleanup into unowned state. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V1.1 maps to exact token/ref/root validation; V7.4 maps to typed state evidence; V12.3 maps to private owned quarantine and fail-closed cleanup. Execution is blocked while any critical/high threat lacks the named automated test.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-01-01 | Tampering | baseline presence/Read | high | mitigate | Resolve once and address profile/tree/parent by exact revision; mutation tests prove no mixed tips. |
| T-06-01-02 | Tampering | candidate objects/fresh initializer | high | mitigate | Quarantine candidates outside final root and authorize initializer rollback only after durable zero-publication cleanup. |
| T-06-01-03 | Spoofing | transaction token/store identity | high | mitigate | Random opaque IDs plus per-Store registry/root/ownership validation reject cross-instance/reused tokens. |
| T-06-01-04 | Repudiation | abort durability | medium | mitigate | Typed object/recovery/initializer evidence records clean removal versus retained uncertainty. |
| T-06-01-05 | Tampering | quarantine cleanup pathname | high | mitigate | Retain parent/quarantine handles and device/inode, repeat no-follow identity checks around the race seam, recurse only through the retained handle, and preserve replacements on mismatch. |
| T-06-01-SC | Tampering | package installation | low | accept | No package-manager command or dependency change is planned; existing Git and Go standard library mechanisms are used. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Commit a real baseline against an exact prior state | 06-01 foundation; 06-02 commit | COVERED | This plan creates the exact baseline token/candidate isolation. |
| REQ | PROF-03 | Secret-safe end-to-end ingest | 06-01 isolation; 06-02 redaction | COVERED | Candidate isolation is ready before value redaction/commit. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| CONTEXT | D-09 | Commit failure fails closed | 06-01 typed evidence; 06-02 commit | COVERED | Plain error cannot claim rollback. |
| CONTEXT | D-10 | Init then fixed main baseline | 06-01 Tasks 1-2 | COVERED | Init-only versus committed-empty and exact main ref. |
| CONTEXT | D-15 | CLI stays concrete-store-free | 06-01 Task 1; 06-03/06-04 | COVERED | Neutral model contract. |
| CONTEXT | D-16 | Extend landed store injection seam | 06-01 Task 1; 06-03/06-04 | COVERED | Same Store implements token surface. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. This plan projects two integrity edges into frontmatter: exact-probe failures cannot become absence, and candidate objects cannot enter the final store before ref preparation. Both are resolved by the named tests.
</spec_fallback>

<verification>
Run the exact-name baseline/quarantine tests, complete model/store suites, and vet. The next store plan must consume the same token for ref prepare/commit and may not recreate baseline discovery.
</verification>

<success_criteria>
- Immutable execution base exists.
- One exact revision owns profile presence, bytes, tree seed, parent, and later expected-ref input.
- Candidate objects stay quarantined through commit-tree.
- Clean versus uncertain abort/initializer eligibility is explicit and tested.
- No task edits more than five files; the ref/backend/secret commit is isolated in 06-02.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md` when done.
</output>
