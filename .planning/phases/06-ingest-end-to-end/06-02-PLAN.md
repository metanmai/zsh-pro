---
phase: 06-ingest-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - core/model/ingest_transaction.go
  - core/store/git.go
  - core/store/store.go
  - core/store/store_test.go
  - core/store/secret.go
  - core/store/secret_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "D-08/D-09: `prepareSecrets` redacts before candidate object creation; Commit returns value-free withheld name/line metadata and a typed outcome on every post-Begin path, never a state-inference plain error."
    - "Git `update-ref --stdin` prepare sees the quarantined candidate through alternates and follows an exact RefPresent wire table: absent ref uses create/zero-old semantics; present ref uses update with the Begin expected ID; mismatch aborts with zero publication and authenticated cleanup."
    - "Two different Profiles begun at one expected revision yield one committed result and one prepare conflict; the loser performs zero backend mutations/publication and never compensates over the winner."
    - "Only Git update-ref commit-response observation may be outcome-ambiguous: observed candidate is committed; observed expected permits guarded compensation; third/unreadable ref requires recovery. Backend, object-publication, and quarantine-cleanup failures remain directly classified on separate evidence axes."
    - "CommitIngest atomically claims the exact Store+InstallInitializationID+token registry record, derives every path/handle internally, and serializes Commit, Abort, and terminal cleanup; forged paths/tokens and token swaps cannot redirect effects."
    - "Authenticated quarantine cleanup runs after committed and clean-not-committed outcomes. Cleanup removed/retained/uncertain is independent of publication truth; cleanup failure preserves the committed/not-committed status, retains evidence, and requires recovery."
    - "A valid persisted SecretRef is structurally recommitted with backend Kind validation but no Retrieve/Store/Delete and no second withheld item; malformed reference shape fails before ref preparation, backend write, object publication, or ref commit."
    - "The existing `Store.Commit` signature remains source-compatible as an adapter over Begin/CommitIngest; ingest consumes the typed transaction directly."
  artifacts:
    - path: "core/store/store.go"
      provides: "Claimed expected-ref commit state machine, independent publication/cleanup evidence, and compatibility adapter"
      contains: "CommitIngest"
    - path: "core/store/git.go"
      provides: "Long-lived cancel-safe update-ref start/update/prepare/commit/abort transaction"
      contains: "update-ref"
    - path: "core/store/secret.go"
      provides: "Side-effect-free redaction/pending mutations plus validated persisted SecretRef pass-through"
      contains: "prepareSecrets"
    - path: "core/store/store_test.go"
      provides: "Ref-present wire table, claim/race, publication/cleanup, adapter, and different-Profile concurrency proof"
  key_links:
    - from: "06-01 IngestBaseline"
      to: "candidate parent/read-tree and update-ref expected value"
      via: "one claimed Store+initialization+token record; RefPresent selects create/zero-old or update(expected)"
      pattern: "RefPresent"
    - from: "candidate quarantine"
      to: "update-ref prepare"
      via: "the update-ref process receives the quarantine as an alternate, so candidate existence validates without final publication"
      pattern: "prepare"
    - from: "prepared ref lock"
      to: "backend mutation -> exclusive object publication -> ref commit"
      via: "the losing ref update is rejected before backend/object side effects"
      pattern: "CommitIngest"
    - from: "ref-commit error"
      to: "observed ref + replacement recovery guard"
      via: "candidate means committed; expected compensation is guarded; other/unknown means retain/recover"
      pattern: "RecoveryRequired"
    - from: "persisted SecretRef"
      to: "redacted candidate Profile"
      via: "validated structural pass-through with zero Retrieve/Store/Delete"
      pattern: "SecretRef"
    - from: "transaction registry claim"
      to: "Commit / Abort / final cleanup"
      via: "one mutex-serialized active-to-finalizing transition; every path comes from registered handles"
      pattern: "finalizing"
  prohibitions:
    - statement: "MUST NOT mutate the secret backend before the expected ref is prepared and locked."
      category: integrity
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT restore backend priors after ref uncertainty unless an exact expected-ref recovery guard is held."
      category: integrity
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT write a literal secret into quarantine or final Git objects."
      category: privacy
      status: unresolved
      verification: null
      flagged: true
---

<objective>
Publish quarantined candidates through a prepared expected-ref transaction with truthful backend/object/ref recovery and persisted-secret reruns.

Purpose: Prevent lost updates and winner-overwriting compensation, confine ambiguity to update-ref observation, preserve publication truth independently from quarantine cleanup, and retain Phase 3 secret exclusion without re-accessing stored references.
Output: Ref-present wire protocol, atomically claimed transaction state, typed publication/cleanup outcomes, guarded backend compensation, durable object publication, SecretRef pass-through, exact legacy Commit adapter, and focused tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-PATTERNS.md
@.planning/phases/06-ingest-end-to-end/06-EVIDENCE.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@core/store/store.go
@core/store/git.go
@core/store/secret.go
@core/store/store_test.go
@core/store/secret_test.go

<interfaces>
Contracts from 06-01 and Phase 3:
- `BeginIngest` supplies one opaque transaction bound to Store+InstallInitializationID, separate RefPresent/ProfileObjectPresent evidence, private index/object quarantine, and registered authenticated handles.
- `prepareSecrets` is side-effect free and returns a redacted Profile, value-free report, and pending backend mutations before any durable effect.
- `gitRunner.updateRefCAS` is the existing one-shot primitive; this plan adds a long-lived `git update-ref --stdin` transaction so prepare locks the expected ref before backend writes.
- `Store.Commit(ctx,branch,profile,msg) (WithheldReport,error)` remains source-compatible through an explicit outcome adapter matrix; only ingest uses the full typed publication/cleanup outcome.
- Per-instance seams, never package globals, drive failure timing and authoritative ref reads.
</interfaces>
</context>

## Artifacts this plan produces

- Ref-locked CommitIngest with quarantined object alternates and typed outcomes.
- Model-owned withheld metadata plus store aliases for source compatibility.
- Stored SecretRef validation/pass-through.
- No package installation, module change, network service, or user setup.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the expected ref before backend/object publication and classify every effect</name>
  <files>core/model/ingest_transaction.go, core/store/git.go, core/store/store.go, core/store/store_test.go, core/store/secret.go</files>
  <read_first>
    - core/store/store.go after 06-01: transaction registry, candidate quarantine, current Commit effect/compensation order, ref seams, and compatibility callers
    - core/store/git.go after 06-01: object environments, candidate plumbing, ref commands, subprocess lifecycle/error wrapping
    - core/store/secret.go: `prepareSecrets`, pending backend mutation shape, prior-value snapshot, coalescing, and compensation errors
    - core/model/ingest_transaction.go after 06-01: complete outcome/ref/backend/object/failure evidence enums
    - core/store/store_test.go: ambiguous-effect, CAS, secret compensation, and per-instance seam patterns
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-08 through D-10
  </read_first>
  <behavior>
    - CommitIngest first atomically claims exactly one active registry tuple Store nonce + InstallInitializationID + token; Commit, Abort, and terminal cleanup cannot overlap, and every quarantine/index/object path comes only from the claimed record.
    - Redacted candidate commit-tree completes in quarantine. RefPresent=false emits exact `create <ref> <candidate>` (or the documented zero-old equivalent); RefPresent=true emits exact `update <ref> <candidate> <expected>`; prepare succeeds before backend prior reads/writes or final-object publication.
    - Prepare mismatch aborts/closes the process with zero backend/publication, then invokes authenticated cleanup. Status remains conflict/not-committed even when cleanup is retained/uncertain; only cleanup/recovery/initializer axes change.
    - While the original prepared ref lock is held, backend/publication failure restores backend priors before abort; failed restoration or uncertain publication/durability is recovery required and retained.
    - Ref-commit process error is the only ambiguous outcome boundary: observed candidate returns committed/report; observed expected is restored only while a newly prepared exact-ref recovery guard is held; third/unreadable/guard-conflict returns ref-observation recovery without destructive compensation.
    - Object publication uses exclusive content-addressed links/equivalent no-overwrite operation and fsyncs affected object fanout directories plus object root before ref commit.
    - Committed and clean-not-committed paths both finish through authenticated quarantine cleanup and store independent cleanup removed/retained/uncertain evidence; cleanup failure never rewrites publication truth.
    - Two same-expected different-Profile transactions yield one commit and one pre-backend prepare conflict with winner readable and loser backend call count zero.
  </behavior>
  <action>
    Add a long-lived, cancel-safe `git update-ref --stdin` wrapper in `gitRunner`. Its process receives only the claimed transaction's sanitized object alternates and supports explicit start/create-or-update/prepare/commit/abort messages. Encode the exact wire table: RefPresent=false uses `create` (or one chosen zero-old form, pinned by tests); RefPresent=true uses `update` with the captured expected ID; mismatch must reject at prepare. Validate framing and guarantee bounded process cleanup. Keep per-instance seams; do not add mutable package globals.

    Implement `CommitIngest` as one typed state machine beginning with an atomic registry claim. Validate Store nonce, initialization ID, token, branch, and active state under the Store mutex, transition to committing/finalizing, and copy only registry-held handles/basenames/environment. Reject forged path fields, unknown/cross-Store/swapped tokens, and Commit-versus-Abort races before any effect. Call `prepareSecrets`, generate only the redacted candidate in the claimed quarantine/private index, queue the chosen RefPresent wire form, and require prepare. A mismatch is conflict before backend/publication; abort the ref process and run the authenticated cleanup path.

    After prepare holds the ref lock, snapshot pending backend priors and apply writes, then publish quarantine loose objects into final content-addressed paths without overwriting existing objects. On an existing destination, verify the rooted entry is a regular non-symlink Git object whose content hashes to the expected object ID before treating it as preexisting; otherwise retain/recover. Record created versus verified-preexisting links and fsync every affected fanout plus object root. On backend/publication failure while the lock lives, restore priors before abort; partial/uncertain publication or failed backend restoration sets recovery required, retains evidence, and forbids initializer deletion.

    Send ref commit only after backend/object durability. If and only if the update-ref commit response/process is lost, read the ref authoritatively and mark the ref-observation axis ambiguous until resolved. Candidate observed means committed. Expected observed is compensable only after a second no-change update-ref verify/prepare guard succeeds; restore backend while guarded, then abort. Third/unreadable/guard failure requires recovery. Do not call deterministic backend failure, object-publication failure, cleanup failure, or directory-sync failure an ambiguous commit outcome; classify each directly on its own axis.

    After committed and every clean-not-committed result, invoke 06-01 authenticated quarantine finalization while the transaction remains claimed. Record cleanup as removed, retained, or uncertain independently from publication status and only then publish the immutable terminal outcome to the registry. Cleanup failure retains handles/evidence and sets recovery required/initializer unsafe without converting committed to not-committed or vice versa. Abort called while Commit/final cleanup owns the claim is rejected; repeated terminal reads replay the stored outcome.

    Add exact wire tests for absent/create, present/update-expected, and mismatch/abort-cleanup/zero-publication. Add claim tests for forged paths, cross-Store or swapped initialization/token authority, Commit-vs-Abort, and Commit-vs-final-cleanup serialization. Add committed and clean-not-committed cleanup-axis tests for removed/retained/uncertain. Keep deterministic backend/publication and update-ref observation tests, asserting only lost commit observation uses ambiguous-ref language. Add the two-transaction different-Profile race with exact backend/ref/object/cleanup assertions.
  </action>
  <acceptance_criteria>
    - Expected ref is prepared/locked before any backend or final-object effect.
    - RefPresent drives an exact tested create-versus-update(expected) wire contract.
    - Store+initialization+token is atomically claimed; caller paths and concurrent Abort/final cleanup cannot redirect Commit.
    - Losing transactions perform no backend mutation/publication and cannot compensate over the winner.
    - Candidate/expected/third/unreadable ref states have distinct, truthful outcomes.
    - Expected-state backend restoration after commit-process ambiguity occurs only under a replacement ref guard.
    - Publication status and cleanup status remain independent; retained/uncertain cleanup never authorizes fresh-store deletion.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestCommitIngestRefAbsentUsesCreate TestCommitIngestRefPresentUpdatesExpected TestCommitIngestRefMismatchCleansWithZeroPublication TestCommitIngestClaimsRegisteredTransaction TestCommitIngestRejectsForgedOrSwappedToken TestCommitAbortAndCleanupSerialize TestCommitIngestCommittedCleanupAxis TestCommitIngestNotCommittedCleanupAxis TestCommitIngestCleanupFailurePreservesPublication TestCommitIngestObservedCandidateIsCommitted TestCommitIngestExpectedRefRecoveryGuard TestCommitIngestUnknownRefRequiresRecovery TestCommitIngestDifferentProfilesConflict' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/store -run '^(TestCommitIngestRefAbsentUsesCreate|TestCommitIngestRefPresentUpdatesExpected|TestCommitIngestRefMismatchCleansWithZeroPublication|TestCommitIngestClaimsRegisteredTransaction|TestCommitIngestRejectsForgedOrSwappedToken|TestCommitAbortAndCleanupSerialize|TestCommitIngestCommittedCleanupAxis|TestCommitIngestNotCommittedCleanupAxis|TestCommitIngestCleanupFailurePreservesPublication|TestCommitIngestObservedCandidateIsCommitted|TestCommitIngestExpectedRefRecoveryGuard|TestCommitIngestUnknownRefRequiresRecovery|TestCommitIngestDifferentProfilesConflict)$' -count=1</automated>
  </verify>
  <done>Store commit claims exact registry authority, emits the RefPresent wire form, prepares before effects, confines ambiguity to lost update-ref observation, and preserves publication truth independently from authenticated cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve value-free reports and recommit validated SecretRefs without backend access</name>
  <files>core/store/store.go, core/store/store_test.go, core/store/secret.go, core/store/secret_test.go</files>
  <read_first>
    - core/store/secret.go after Task 1: pure preparation order, redaction sentinel construction, pending backend plan, and unsafe-shape checks
    - core/store/secret_test.go: literal, dynamic, malformed, unavailable-backend, defensive-copy, and compensation matrices
    - core/model/profile.go and core/model/secretref.go: persisted SecretRef/ValueMode/category/name/dynamic/runtime shapes
    - core/store/store.go after Task 1: report aliases and compatibility Commit adapter
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-08 and D-09
  </read_first>
  <behavior>
    - `core/model.WithheldSecret{Name,StartLine}` / `WithheldReport` are value-only; store aliases preserve existing callers and no secret value can enter the report.
    - Literal CatSecrets entries are redacted in both Text and Value before hash-object; pending backend work occurs only after ref prepare.
    - Already-dynamic secret assignments remain verbatim and are not reported or stored.
    - A persisted reference is accepted only when kind/key/name/category/redacted Text+Value/ValueMode/RuntimeValue/Dynamic all match the shipped structural contract and backend Kind matches.
    - At CommitIngest level, valid persisted references call Kind exactly once, appear byte/field-identical in the candidate/readback Profile, add no pending work/report, and perform no Retrieve/Store/Delete. Any malformed field fails before the update-ref process starts or backend/publication occurs.
    - Legacy Store.Commit maps exact outcomes: committed -> value-free report/nil; conflict -> fixed conflict error; clean not-committed -> fixed not-committed error; recovery -> fixed recovery error exposing committed truth and returning a report only when commit truth is committed.
  </behavior>
  <action>
    Use the model-owned withheld Name/StartLine record created in 06-01 and leave source-compatible store aliases. Ensure CommitIngest/Commit copy and return only value-free metadata in deterministic source order; no formatter/error includes the pending literal or backend key.

    Before generic ValueMode rejection in `prepareSecrets`, add a persisted-reference validation branch. Accept only a single-name CatSecrets assignment whose recognized Secret kind equals `kc.Kind()`, key equals the sole name, redacted Text and Value exactly match the shipped exported/non-exported redaction sentinel, ValueMode is unsupported, RuntimeValue is nil, and Dynamic is false. Require a non-nil backend for kind validation but call only Kind; never Retrieve/Store/Delete, append pending mutation, or add withheld metadata.

    Reject wrong kind/key/category/name/redaction/value/mode/runtime/dynamic/multi-name shapes with `ErrUnsafeSecretShape` while preparation is still side-effect free. Preserve the shipped literal redaction, unavailable-backend, already-dynamic, coalescing, defensive-copy, and compensation behavior from Task 1.

    Add commit-level exact pass-through and exhaustive one-field-at-a-time malformed matrices. For the valid case call CommitIngest, read the candidate/committed Profile, and require the entire SecretRef entry unchanged, Kind exactly once, Retrieve/Store/Delete zero, empty withheld report/pending work, and no literal in candidate objects. For malformed cases require failure before the update-ref process starts with backend/publication counters zero; prepareSecrets unit tests remain supporting coverage, not the only gate.

    Specify the legacy `Store.Commit` adapter table exactly. It creates/claims only an internal Store-issued non-removable initialization authority, never fabricates caller ownership, and delegates to Begin/CommitIngest once. Clean committed returns the value-free report and nil. Clean conflict and clean not-committed return no report plus stable typed sentinel errors. Any outcome with recovery required returns a stable recovery error implementing a small committed-state interface; include the report only if publication truth is committed. Pin `errors.Is`, report shape, and absence of raw Git/backend/cleanup text.
  </action>
  <acceptance_criteria>
    - Withheld metadata contains only name/start line and remains source-compatible.
    - Literal secrets reach neither quarantine nor final objects.
    - Dynamic secrets remain verbatim/unreported.
    - Valid stored SecretRefs recommit unchanged with no Retrieve/Store/Delete or second report.
    - Every inconsistent persisted-reference field fails before any durable effect.
    - Legacy Commit has one exact, tested mapping for committed, conflict, clean not-committed, and recovery-required outcomes.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestCommitIngestExistingSecretRefPassThrough TestCommitIngestRejectsMalformedSecretRefBeforeRefProcess TestPrepareSecretsLiteralRedactedBeforeObjects TestPrepareSecretsDynamicVerbatim TestLegacyCommitAdapterOutcomeMatrix TestLegacyCommitAdapterReportContainsNoValues TestWithheldReportContainsNoValues' &amp;&amp; listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') &amp;&amp; for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" &gt;&amp;2; exit 1; }; done &amp;&amp; GOTOOLCHAIN=auto go test ./core/store -run '^(TestCommitIngestExistingSecretRefPassThrough|TestCommitIngestRejectsMalformedSecretRefBeforeRefProcess|TestPrepareSecretsLiteralRedactedBeforeObjects|TestPrepareSecretsDynamicVerbatim|TestLegacyCommitAdapterOutcomeMatrix|TestLegacyCommitAdapterReportContainsNoValues|TestWithheldReportContainsNoValues)$' -count=1 &amp;&amp; GOTOOLCHAIN=auto go test ./core/model ./core/store -count=1 &amp;&amp; GOTOOLCHAIN=auto go vet ./core/model ./core/store</automated>
  </verify>
  <done>Secret exclusion remains value-free and fail-closed, persisted references pass unchanged through CommitIngest with one Kind call/no backend data access, and legacy Commit maps every typed outcome exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Redacted Profile/quarantine -> ref lock/backend/final objects | Expected ref must be locked before durable side effects. |
| Lost update-ref commit observation -> guarded backend compensation | Only an unread commit response may obscure ref publication; restoration without a replacement expected-ref guard could overwrite a winner. |
| Claimed Store+initialization+token -> Commit/Abort/cleanup | Forged paths, token swaps, or concurrent terminal actions must not redirect or duplicate effects. |
| Profile secret fields -> Git object bytes/output report | Literal values must be removed before object creation and never enter metadata. |
| Persisted SecretRef -> backend | Structural recommit must not retrieve or rewrite existing secret data. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies. V8.3 maps to redaction before quarantine and value-free reports; V1.1 maps to exact ref/token/SecretRef validation; V7.4 maps to typed state evidence and guarded recovery. Execution is blocked while any critical/high threat lacks the named automated test.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-02-01 | Information Disclosure | prepareSecrets -> object databases/output | critical | mitigate | Redact before commit-tree, validate stored refs, and scan final/retained objects in 06-05. |
| T-06-02-02 | Tampering | expected ref/backend publication | high | mitigate | Prepare/lock exact expected ref before backend/final objects; loser test requires zero side effects. |
| T-06-02-03 | Tampering | lost update-ref commit observation/backend compensation | high | mitigate | Only the lost update-ref response invokes observation recovery: candidate is committed, expected restoration requires a replacement ref guard, and third/unreadable state is retained for recovery. |
| T-06-02-04 | Repudiation | publication versus quarantine cleanup | high | mitigate | Publication truth and removed/retained/uncertain cleanup are independent immutable axes; cleanup failure cannot rewrite committed or not-committed status. |
| T-06-02-05 | Denial of Service | claimed transaction lifecycle | medium | mitigate | Atomically claim Store+initialization+token, serialize Commit/Abort/final cleanup, bound the ref subprocess, and retain authenticated evidence on cleanup uncertainty. |
| T-06-02-06 | Spoofing | persisted SecretRef | high | mitigate | Commit-level structural validation and one Kind check occur before ref processing; valid entries pass unchanged with Retrieve/Store/Delete counters at zero. |
| T-06-02-SC | Tampering | package installation | low | accept | No package-manager command or dependency change is planned. |
</threat_model>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Commit a real baseline without lost update or leaky partial state | 06-02 Tasks 1-2 | COVERED | Store transaction and secret boundary. |
| REQ | PROF-03 | Exclude literal secrets, persist references, report name/line | 06-02 Task 2 | COVERED | Redaction applies before quarantine/final objects. |
| RESEARCH | — | No Phase 6 RESEARCH.md | — | EXCLUDED | Current code, SPEC, CONTEXT, PATTERNS, and evidence ledger are authoritative. |
| CONTEXT | D-08 | Consume Store withheld report; no values | 06-02 Task 2; 06-04/06-05 | COVERED | Commit-level valid SecretRef pass-through and legacy adapter return only value-free reports with no backend data access. |
| CONTEXT | D-09 | Commit error is runtime/fail closed | 06-02 Tasks 1-2; 06-04 | COVERED | Exact legacy mapping and independent publication/cleanup axes preserve committed, conflict, clean-not-committed, and recovery-required truth. |
| CONTEXT | D-10 | Fixed main commit after Init | 06-01 baseline; 06-02 Task 1 | COVERED | RefPresent selects exact create/zero-old versus update(expected) wire behavior under one claimed initialization/token. |
| CONTEXT | D-15 | CLI remains concrete-store-free | 06-01 model contract; 06-03/06-04 | COVERED | No concrete type crosses. |
| CONTEXT | D-16 | Extend landed store seam | 06-02 CommitIngest; 06-03/06-04 | COVERED | Same Store implements methods. |
</source_audit>

<spec_fallback>
The Phase 6 SPEC has no explicit Prohibitions section. This plan projects its privacy/integrity edges into frontmatter: backend mutation waits for the prepared ref, only lost update-ref commit observation enters guarded ambiguity handling, cleanup never rewrites publication truth, and literal values enter neither object database. Named tests plus 06-05 scans resolve them.
</spec_fallback>

<verification>
Run exact-name ref/backend/object/secret tests, complete model/store suites, and vet. 06-03 executes next and consumes this plan's finalized transaction/outcome contract; 06-04 waits for the serialized 06-03 summary.
</verification>

<success_criteria>
- Ref prepare locks exact expected state before backend/final-object effects.
- Different-Profile concurrency has one winner and a zero-effect loser.
- Only lost update-ref commit observation may be ambiguous; backend, object, and cleanup failures remain directly classified, with compensation guarded by an exact expected ref.
- Publication status and authenticated quarantine cleanup status remain independent for committed and clean-not-committed outcomes.
- Literal/dynamic/persisted secret paths preserve Phase 3 guarantees, including exact CommitIngest SecretRef pass-through and a fully pinned legacy Commit outcome matrix.
- No task edits more than five files.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md` when done.
</output>
