---
phase: 06-ingest-end-to-end
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - core/model/withheld.go
  - core/store/git.go
  - core/store/store.go
  - core/store/store_test.go
  - core/cli/store.go
  - core/cli/cli.go
  - core/cli/ingest.go
  - core/cli/ingest_test.go
  - core/dto/ingest.go
  - core/store/secret.go
  - core/store/secret_test.go
autonomous: true
requirements: [PROF-03]
must_haves:
  truths:
    - "`zsh-pro ingest [path] [--json]` installs the landed loader, statically parses the selected file, consumes the existing IR routing verdict, commits only declarative entries to `main`, and writes every imperative statement verbatim to the master region, per D-01 through D-07."
    - "Literal secrets are excluded only by `Store.Commit`; a validated stored `SecretRef` is recommitted unchanged without retrieval or backend mutation and without another withheld item, while malformed or backend/key-mismatched references fail closed, per D-08 and D-09."
    - "First-run initialization and every fresh reconstructed repeat commit use the same composition-root store instance and baseline `main`; analyze/hook remain available when storage is unavailable, per D-10, D-15, and D-16."
    - "Human and JSON success output deterministically reconciles source statements as managed plus master statements and includes managed count, master physical-line count, withheld list, and append warnings, per D-03 and D-04."
    - "Every failure before `main` advances—including Init, durable-baseline probe, Read, Parse/Build, secret preparation, and Commit—rolls back the live Plan 06-01 transaction to exact pre-ingest target/cache state plus created-store removal or preexisting logical-store/backend preservation; only Commit-success/final-promotion-failure is recoverably partial."
    - "Public ingest errors are a typed allowlist with fixed path-free/value-free messages; raw parser, Git, backend, promotion, filesystem, and unknown causes never reach human stderr or JSON stdout, and JSON failure remains exactly one object."
    - "Adoption never depends on profile entry count: pre-install exact-marker topology plus a fallible durable `main:profile.json` probe selects first-adoption, installed-first-adoption, or re-ingest, including an empty committed baseline."
  artifacts:
    - path: "core/model/withheld.go"
      provides: "Shell/store-neutral withheld-report contract shared by Store and CLI"
      exports: ["WithheldSecret", "WithheldReport"]
    - path: "core/cli/store.go"
      provides: "IngestStore extension over the landed Store seam"
      exports: ["IngestStore"]
    - path: "core/cli/ingest.go"
      provides: "Static ingest orchestration, durable adoption selection, live transaction ownership, partition, commit/promote ordering, safe public errors, and deterministic output"
      contains: "runIngest"
    - path: "core/dto/ingest.go"
      provides: "Stable JSON ingest envelope with no domain/store values"
      exports: ["IngestEnvelope", "IngestWithheld"]
    - path: "core/cli/ingest_test.go"
      provides: "TDD coverage for routing, secret reporting, idempotency, and recoverable partial outcomes"
      contains: "TestRunIngest"
    - path: "core/store/secret_test.go"
      provides: "Stored SecretRef pass-through validation and no-backend-mutation regression coverage"
      contains: "TestPrepareSecretsExistingRef"
    - path: "core/store/store_test.go"
      provides: "Fallible durable profile-object existence probe, including committed-empty baseline coverage"
      contains: "TestProfileObjectExistsDistinguishesEmptyBaseline"
  key_links:
    - from: "CLI.Run case ingest"
      to: "CLI.runIngest"
      via: "D-02 command dispatch with path and JSON arguments"
      pattern: "case \"ingest\""
    - from: "CLI.runIngest"
      to: "Provider.Parse -> ir.Build -> Entry.EffectiveManaged"
      via: "static-only Phase 2 pipeline and one existing routing predicate"
      pattern: "ir.Build"
    - from: "CLI.runIngest"
      to: "StoreInitializer -> IngestStore.ProfileObjectExists/Read/Commit(main) -> promoteGuarded"
      via: "the live ingestInstallTransaction remains rollback-capable until Commit succeeds, then final promotion owns the sole permitted partial outcome"
      pattern: "Commit.*main"
    - from: "model.WithheldReport"
      to: "dto.IngestWithheld"
      via: "field-by-field Name and StartLine mapping"
      pattern: "StartLine"
    - from: "IngestStore.Read(main) stored SecretRef entry"
      to: "Store.Commit -> prepareSecrets"
      via: "source-ordered structural merge preserves the pointer, and validated pass-through queues no backend write and no withheld report"
      pattern: "Secret"
    - from: "publicIngestError"
      to: "CLI.failCommand"
      via: "errors.As maps only allowlisted codes to fixed messages; unknown causes collapse to a stage-level message before either output mode"
      pattern: "ingestError(Code|Message)"
  prohibitions:
    - statement: "MUST NOT execute, source, introspect, or evaluate user configuration while ingesting it; dynamic shell expressions remain inert source data."
      category: safety
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT print, serialize into the committed Git tree, or include in diagnostics any literal secret value detected during ingest."
      category: privacy
      status: unresolved
      verification: null
      flagged: true
    - statement: "MUST NOT silently drop an imperative statement or promote it into switchable managed state with new classification logic."
      category: safety
      status: unresolved
      verification: null
      flagged: true
  flagged_assumptions:
    - id: PROF-03-idempotency-edge
      status: resolved
      verification: explicit
      resolution: "An unchanged second CLI ingest must return success, retain one exact marker pair, preserve complete `.zshrc` bytes, and retain equal managed/master accounting."
    - id: PROF-03-concurrency-edge
      status: resolved
      verification: explicit
      resolution: "Store.Commit's landed CAS and secret compensation protect the ref/backend; Plan 06-01 protects stale file promotion. No cross-resource atomic primitive exists, so a commit-success/file-failure is an explicit runtime error naming the advanced baseline and idempotent rerun recovery, never a false success."
---

<objective>
Ship the real `ingest` command over the landed parser, IR, store, installer, and composition root.

Purpose: Give a user one static, non-lossy on-ramp from their existing `.zshrc` to a secret-safe baseline profile and preserved imperative master, with truthful output and recoverable failure semantics.
Output: Shared withheld types, an ingest-capable CLI store seam, the command/controller, stable human/JSON results, and focused tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-ingest-end-to-end/06-CONTEXT.md
@.planning/phases/06-ingest-end-to-end/06-SPEC.md
@.planning/phases/06-ingest-end-to-end/06-01-SUMMARY.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-01-SUMMARY.md
@.planning/phases/05-runtime-loader-cli-bootstrap/05-08-SUMMARY.md
@core/cli/cli.go
@core/cli/store.go
@core/cli/install.go
@core/store/store.go
@core/store/secret.go
@core/ir/build.go
@core/model/profile.go

<interfaces>
Current contracts to consume directly:
- `shell.Provider` composes Parser, Classifier, Introspector, Regenerator, and Hooker; ingest may call only Parse/Classify/Regenerate/HookScript paths and must not call Introspect.
- `ir.Build(blocks []model.Block, c shell.Classifier) model.Profile` performs the existing classification and managed routing.
- `model.Entry.EffectiveManaged() bool` is the D-05 partition predicate.
- `cli.Store` currently exposes Branches, Current, Checkout, and Read and is supplied by the composition root to `cli.NewWithStoreInitializer`.
- `(*store.Store).Init(context.Context) error` is idempotent.
- Plan 06-01 records the immutable pre-execution commit in `06-EXECUTION-BASE` and supplies `beginIngestInstallAt` plus `ingestInstallTransaction.{attachStoreInitialization,rollbackBeforeCommit,acceptCommit,finalize}`; ingest must not call a completed ordinary `runInstallAt` and lose compensation.
- `(*store.Store).Commit(context.Context, string, model.Profile, string) (store.WithheldReport, error)` performs secret preparation, backend mutation, ref CAS, and compensation.
- `UnmarshalProfile` preserves `Entry.Secret{Kind,Key}` from authoritative `profile.json`; `prepareSecrets` currently treats every `CatSecrets` entry as dynamic or literal and therefore needs an explicit validated-reference branch before literal handling.
- `(*store.Store).Read(context.Context, string) (model.Profile, error)` returns an empty profile for initialized-but-uncommitted main.
- Plan 06-01 supplies `ingestAdoptionMode`, `scanZshrcLayout`, `captureSourceLayout`, `renderImperativeMaster`, `prepareIngestZshrc`, `prepareAtomicWriteAgainst`, typed `promotionResult`, and guarded promotion under one target lease.
</interfaces>
</context>

## Artifacts this phase produces

- New path `core/model/withheld.go` with exported `model.WithheldSecret{Name, StartLine}` and `model.WithheldReport`.
- New path `core/dto/ingest.go` with exported `dto.IngestWithheld{Name, StartLine}` and `dto.IngestEnvelope{Tool, Version, Command, OK, ExitCode, Profile, SourceStatements, ManagedEntries, MasterStatements, MasterLines, Withheld, Warnings}`.
- New path `core/cli/ingest.go` with private `runIngest`, `ingestPlan`, `buildIngestPlan`, `commitAndPromote`, `renderIngestHuman`, and `renderIngestJSON` symbols.
- New path `core/cli/ingest_test.go`.
- New interface `cli.IngestStore` embedding the landed `cli.Store` and adding fallible `ProfileObjectExists(ctx, branch) (bool, error)` plus `Commit` with `model.WithheldReport`; initialization uses the existing rollback-capable `StoreInitializer`, not a second untracked Init call.
- New `CLI` struct field `ingestStore IngestStore`, populated by safe type assertion from the same injected store object; no new constructor parameter.
- New CLI verb `ingest`; new `--json` flag on that verb; optional positional path with default `~/.zshrc`.
- `store.WithheldSecret` and `store.WithheldReport` remain source-compatible aliases to the new model-owned types.
- `CLI.failCommand` (or equivalent parameterized helper) reports the actual command in JSON errors; existing `fail` retains analyze-compatible callers.
- New private error contract in `core/cli/ingest.go`: typed `ingestErrorCode`/`publicIngestError` (names may vary) with an exhaustive code-to-fixed-message table and no cause/path/source/backend fields serialized or printed.
- New fallible object probe in `core/store/git.go`/`store.go` distinguishes missing `main:profile.json` from a git execution failure without exposing raw stderr; an empty committed profile still returns present.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the landed store seam with a neutral withheld-report contract</name>
  <files>core/model/withheld.go, core/store/git.go, core/store/store.go, core/store/store_test.go, core/cli/store.go, core/cli/cli.go, core/cli/ingest_test.go</files>
  <read_first>
    - core/store/store.go: `WithheldSecret`, `WithheldReport`, `Store.Init`, `Store.Commit`, and `Store.Read`
    - core/store/git.go: `gitRunner.run`, `catFileExists`, `mapGitError`, and exit-status handling needed for a fallible object probe
    - core/store/store_test.go: `TestReadDistinguishesAbsentFromUncommitted`, empty-profile Commit coverage, and store test construction
    - core/cli/store.go: the landed narrow `Store` and initializer transaction
    - core/cli/cli.go: `CLI` fields, `New`, `NewWithStoreInitializer`, `newCLI`, and nil-like normalization
    - core/cli/dependencies.go: `isNilLike`
    - core/cli/cli_test.go and core/cli/emitter_test.go: existing store doubles that must keep satisfying their narrow runtime interfaces
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-08, D-10, D-15, and D-16
  </read_first>
  <behavior>
    - `*store.Store` satisfies both the unchanged Phase 5 runtime `Store` interface and the new `IngestStore` extension without a composition-root adapter.
    - Store callers retain source-compatible `store.WithheldReport` names while CLI can declare Commit without importing `core/store`.
    - Existing list/status/emitter test stores do not need ingest methods; `ingest` alone reports store-unavailable when the injected object lacks `IngestStore`.
    - Literal and typed-nil ingest stores normalize to unavailable and never panic.
    - `ProfileObjectExists(main)` returns false for an absent/uninitialized object, true for a committed empty or nonempty profile, and an error for probe execution failure; it never infers adoption from `Read(main).Entries` length.
  </behavior>
  <action>
    Move the two-field withheld producer/consumer shape to `core/model/withheld.go`; replace the declarations in `core/store/store.go` with aliases so `Store.Commit` and existing store tests retain source compatibility while the actual return type is model-owned. Add a fallible git object-existence primitive that distinguishes `cat-file -e`'s not-found exit from timeout/launch/other execution failures, mapping the latter to the existing safe typed store errors and never including stderr. Expose `(*store.Store).ProfileObjectExists(ctx, branch) (bool, error)` after branch validation; it probes `<branch>:profile.json`, returns false when the object is genuinely absent (including before initialization), true after any successful Commit including `model.Profile{}`, and propagates probe failures. This is the durable adoption evidence; `Read(main)` remains unchanged and entry cardinality is never consulted.

    In `core/cli/store.go`, add `IngestStore` that embeds the landed Phase 5 `Store` and adds `ProfileObjectExists` plus `Commit`. Do not add `Init`: `runIngest` must invoke the already injected rollback-capable `StoreInitializer` and attach its `StoreInitialization` to Plan 06-01's live transaction, so initialization cannot fall outside compensation. Do not widen the runtime emitter/list/status interface and do not add a constructor argument. In `newCLI`, safely type-assert the already injected store to `IngestStore`, normalize typed nil with `isNilLike`, and record it in `CLI.ingestStore`. Add exact compile-time/public-dispatch tests for real signature compatibility, narrow fake compatibility, nil-like failure, and profile-object states: fresh/uninitialized false, Init-only false, committed empty true, committed nonempty true, injected probe error returned. This implements D-10/D-15/D-16 without concrete imports or entry-count adoption.
  </action>
  <acceptance_criteria>
    - Existing Store/Emitter test doubles compile unchanged unless they explicitly test ingest.
    - `*store.Store` reaches the ingest field through the same composition-root argument already used by Phase 5.
    - Withheld reports have one neutral definition and store aliases preserve callers.
    - Nil-like ingest dependencies return the normal runtime error rather than panicking.
    - The fallible durable object probe distinguishes uncommitted empty state from committed-empty state and fails closed on execution errors.
  </acceptance_criteria>
  <verify>
    <automated>store_tests='TestProfileObjectExistsDistinguishesEmptyBaseline' && cli_tests='TestIngestStoreCompatibility TestIngestStoreTypedNilUnavailable TestWithheldReportAliases' && store_listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') && cli_listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') && for test_name in $store_tests; do count=$(printf '%s\n' "$store_listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && for test_name in $cli_tests; do count=$(printf '%s\n' "$cli_listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && GOTOOLCHAIN=auto go test ./core/store -run '^TestProfileObjectExistsDistinguishesEmptyBaseline$' -count=1 && GOTOOLCHAIN=auto go test ./core/cli -run '^(TestIngestStoreCompatibility|TestIngestStoreTypedNilUnavailable|TestWithheldReportAliases)$' -count=1 && GOTOOLCHAIN=auto go test ./core/model ./core/store ./core/cli -count=1 && GOTOOLCHAIN=auto go vet ./core/model ./core/store ./core/cli</automated>
  </verify>
  <done>The actual Phase 5 injection seam exposes durable baseline evidence and Commit while initialization remains on the existing compensatable path, with committed-empty adoption state distinguished safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement static ingest, secret-safe output, and truthful partial outcomes</name>
  <files>core/cli/cli.go, core/cli/ingest.go, core/cli/ingest_test.go, core/dto/ingest.go</files>
  <read_first>
    - core/cli/cli.go: `Run`, `runAnalyze`, `providerOrFail`, and `fail`
    - core/cli/install.go after Plan 06-01: explicit-target install, layout scan, master renderer, candidate preparation, and guarded promotion
    - core/ir/build.go and core/ir/route.go: existing classifier/routing pipeline
    - core/model/profile.go: `EffectiveManaged`, `Representable`, `Text`, and `StartLine`
    - core/store/secret.go: dynamic-versus-literal behavior and fail-closed shapes
    - core/dto/envelope.go and core/render/json.go: stable agent-envelope conventions
    - core/cli/cli_test.go: exactly-one-JSON-object success/failure tests
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-01 through D-12
  </read_first>
  <behavior>
    - `ingest`, optional path, and `--json` parse like analyze; the default is `~/.zshrc`, one positional value selects the read/write target, an unknown flag is usage error, and the final mutation target is absolute.
    - Before installation, exact marker topology plus `ProfileObjectExists(main)` selects first-adoption, installed-first-adoption, or re-ingest; a committed empty profile is re-ingest and no branch depends on `len(Read(main).Entries)`.
    - Ingest calls Plan 06-01's `beginIngestInstallAt`, then holds its live rollback/finalize transaction across initializer, Read, Parse/Build, secret preparation, and Commit; every failure before Commit success restores the exact pre-ingest target/cache plus created-store removal or preexisting ref/tree/backend preservation.
    - Initial adoption parses the whole absent-marker source; installed-first adoption parses exact pre-BEGIN source; re-ingest builds a fresh profile by structurally merging stored entries with independently parsed current pre-BEGIN master entries in preserved `StartLine`/source order and writes a freshly rendered current master exactly once.
    - Adoption calls `Provider.Parse` and `ir.Build`, partitions solely on `EffectiveManaged`, renders every unmanaged entry verbatim into the master, and commits only managed entries to fixed branch `main` with a deterministic message.
    - No ingest path calls `Provider.Introspect`, sources input, or resolves dynamic expressions.
    - A stored `Entry.Secret` is never rendered to placeholder source and reparsed: its reference-bearing entry reaches Commit structurally intact. Literal secret output contains only name and source line on first capture; dynamic secrets and unchanged stored references are absent from later withheld lists; any Commit error prevents master promotion.
    - After Commit succeeds, guarded promotion either completes or returns a distinct runtime error stating that `main` advanced but `.zshrc` was not updated and an unchanged rerun repairs it.
    - Human and JSON success reconcile `source_statements == managed_entries + master_statements`; JSON stdout contains exactly one object and clean stderr.
    - Every failure uses an allowlisted error code and fixed safe message. Parser/Init/Read/Commit/backend/promotion/unknown causes containing literal secrets, path-like data, and newlines are absent from human stderr and JSON stdout; JSON failure is exactly one object.
  </behavior>
  <action>
    Add `case "ingest"` to `CLI.Run` and implement `runIngest`. Parse D-01's positional path/`--json`, reject more than one positional path, expand `~` only at the CLI boundary, resolve the mutation target absolute, and require provider, ingest store, and rollback-capable store initializer. Enter `withIngestTargetLease` before the first target read. Read the original target with the Plan 06-01 absent-target contract, run `scanZshrcLayout`, call fallible `ProfileObjectExists("main")`, and select `ingestAdoptionMode` exclusively from `{target absent or complete marker topology} x {durable baseline absent/present}`: absent marker + absent baseline is first-adoption; complete marker + absent baseline is installed-first-adoption; present baseline is re-ingest even when `Read(main)` later returns zero entries. A malformed marker topology or probe error exits before installation. Never inspect profile entry count to select the mode.

    Still inside the lease, call `beginIngestInstallAt` so the initial guarded target/cache promotion completes and returns a live `ingestInstallTransaction`; do not call ordinary `runInstallAt`. Only then invoke the existing `StoreInitializer` and attach its returned `StoreInitialization`, satisfying D-10 through the same `InitForInstall` path. Call `Read("main")`, then choose source/layout: first-adoption parses the exact original whole-file bytes; installed-first-adoption parses exact original pre-BEGIN bytes; re-ingest parses exact current pre-BEGIN master and structurally merges those new entries with stored managed entries by `StartLine`, using stable ordinal for ties. Preserve stored SecretRef-bearing entries as objects; never regenerate/reparse their inert text. Build routing only through `Provider.Parse`, `ir.Build`, and `EffectiveManaged`. Render the final master from the current parsed source/layout (not from stored entries), including newly imperative content and removing newly managed content; all three modes call `prepareIngestZshrc` with this fresh result, so changed re-ingest cannot preserve a stale master. Exclude post-END bytes from adoption and return only D-11 warnings.

    Prepare the final target against the exact installed snapshot before Commit. On initializer, Read, Parse/layout/Build, secret-preparation, or Commit failure, invoke `rollbackBeforeCommit` before returning: exact original `.zshrc` bytes/mode and cache state return; a store created by this invocation is removed; a preexisting store retains its root/mode, refs/trees, and secret-backend contents; Commit's own CAS/backend compensation remains authoritative for failures after object preparation. Tests must snapshot those states and assert Parse/Build/Commit/final-promotion call counts appropriate to the failed stage. After Commit succeeds, immediately call `acceptCommit` so no later failure can remove/rewind the advanced baseline or undo the initial installed state. Promote the final candidate under the same lease and a fresh typed promotion result; on success `finalize`, and on any failure report the sole permitted partial state: `main` advanced, initial installed file/cache remain, and an idempotent rerun repairs the master. A `stale-already-recovered` result is never fed to generic target rollback.

    Define a closed `ingestErrorCode`/`publicIngestError` mapping and make renderers consume only that public value, never `err.Error()`. Use fixed code/message pairs for invalid arguments, unavailable dependency, target unavailable, target changed, initial install, store initialization, baseline probe, baseline read, parse/build/layout, secret preparation, Commit, recovery uncertainty, and post-Commit final promotion. Messages name only the stage and recovery action; they contain no user path, source Text, backend kind/detail, raw newline, or secret value. `errors.Is`/`errors.As` may classify allowlisted internal sentinels; every unrecognized cause collapses to the fixed message for its current stage. Parameterize `failCommand` so ingest JSON uses `command: ingest`, `ok:false`, typed `error_code`, fixed `error`, and exit 1 in exactly one object; human mode writes exactly one fixed `zsh-pro:` line to stderr and no stdout. Preserve analyze and other verbs unchanged.

    Map `model.WithheldReport` field-by-field into the D-04 DTO and render deterministic success accounting. Add adversarial stage tests in both human and JSON modes for target read/path, initial promotion, Init, baseline probe, Read, parser/layout/build, secret-backend preparation, Commit, final promotion, recovery uncertainty, and an unknown error. Each injected cause contains a unique literal, secret-looking path segment, backend text, and embedded CR/LF; require all raw cause bytes absent, JSON parseable as exactly one top-level value with no trailing value, human stdout empty, JSON stderr empty, and only the allowlisted code/message visible. Use a panic-on-Introspect provider to prove D-01 through D-09 remain static-only.
  </action>
  <acceptance_criteria>
    - Mixed source produces a `main` Commit containing exactly its managed entries; every unmanaged entry appears verbatim in the prepared master; statement accounting has no gap or overlap.
    - The three adoption modes are selected by marker topology plus durable profile-object existence; a committed empty baseline is proven re-ingest and every changed mode promotes a freshly rendered master once.
    - The parser/classifier path is static-only and dynamic expressions remain unchanged.
    - Human and JSON outputs contain required counts, names/lines, warnings, and no secret values; JSON is one parseable object.
    - Init/Read/parse/backend/Commit failures after initial promotion restore exact pre-ingest target/cache and created/preexisting logical store/backend state; no final master promotion occurs.
    - Post-Commit file failure is the only partial state and is reported with the fixed repair message, never clean.
    - Every internal stage and unknown error is sanitized in both modes; no source/backend/path/cause bytes or raw message newlines escape.
    - An unchanged installed second run rebuilds an equal fresh profile without reparsing stored reference placeholders and retains complete target bytes; editing the pre-marker master before rerun changes the fresh `main` partition as routing dictates without duplicate managed or imperative content.
  </acceptance_criteria>
  <verify>
    <automated>tests='TestRunIngestAdoptionModes TestRunIngestStaticMixedSource TestRunIngestRollbackBeforeCommit TestRunIngestCommitAndPromoteOutcomes TestRunIngestPublicErrorAllowlist TestRenderIngestResult' && listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') && for test_name in $tests; do count=$(printf '%s\n' "$listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && GOTOOLCHAIN=auto go test ./core/cli -run '^(TestRunIngestAdoptionModes|TestRunIngestStaticMixedSource|TestRunIngestRollbackBeforeCommit|TestRunIngestCommitAndPromoteOutcomes|TestRunIngestPublicErrorAllowlist|TestRenderIngestResult)$' -count=1 && GOTOOLCHAIN=auto go test ./core/cli ./core/store -count=1</automated>
  </verify>
  <done>The public ingest verb owns one compensatable transaction through Commit, selects adoption from durable evidence, emits a freshly routed master, and exposes only typed fixed errors with one-object JSON.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin SecretRef-safe idempotent rerun, repair, and append warnings</name>
  <files>core/store/secret.go, core/store/secret_test.go, core/cli/ingest.go, core/cli/ingest_test.go</files>
  <read_first>
    - core/cli/ingest.go after Task 2: installed-versus-adoption decision, accounting, and commit/promote order
    - core/cli/ingest_test.go Task 2 fakes and promotion failure cases
    - core/cli/install_test.go Plan 06-01 complete-file idempotency and append-preservation tests
    - core/store/store.go: CAS conflict and compensation comments around Commit
    - core/store/secret.go: `prepareSecrets`, `secretRefValue`, `isExcludableSecretShape`, pending backend writes, and report construction
    - core/store/dto.go: authoritative `MarshalProfile`/`UnmarshalProfile` preservation of `Entry.Secret`
    - core/model/secretref.go and core/model/profile.go: allowed reference kinds, `SecretRef{Kind,Key}`, and redacted entry fields
    - .planning/phases/06-ingest-end-to-end/06-SPEC.md acceptance criteria 3 and 4
    - .planning/phases/06-ingest-end-to-end/06-CONTEXT.md D-03, D-10, D-11, and D-12
  </read_first>
  <behavior>
    - Two unchanged `CLI.Run(["ingest", path])` calls produce equal complete target bytes, one exact marker pair, equal accounting, and no duplicated managed entries.
    - On the second run of a secret-bearing input, the stored reference is bit-for-bit equal, the backend retains the original literal, no Store/Retrieve/Delete call occurs, generated output contains only the inert reference and never the backend value, and the withheld report is empty.
    - A preexisting SecretRef passes through only when its shape, key, backend kind, redacted fields, and placeholder are internally consistent; malformed, unknown-kind, wrong-key, active-backend-mismatched, or literal-bearing references fail closed before backend or Git mutation.
    - Editing the current pre-marker master between runs causes re-ingest to rebuild and commit a fresh source-ordered profile: newly declarative content moves once to `main`, changed imperative text remains once in master, and stored/pre-marker entries retain their `StartLine` ordering.
    - Existing non-blank/non-comment lines after END produce deterministic warning entries, exit cleanly, and are byte-identical after ingest.
    - A simulated Commit-success/promotion-failure followed by a normal rerun converges to the same bytes and baseline content as an uninterrupted run.
    - A simulated store CAS conflict exits before `.zshrc` master promotion and the live transaction restores exact pre-ingest `.zshrc`, cache, store ref/tree/backend, and store-root existence/mode state.
    - A committed empty `main:profile.json` is durable re-ingest state: an unchanged empty profile does not re-adopt installer bytes or duplicate the managed block.
  </behavior>
  <action>
    First extend `prepareSecrets` with the minimal stored-reference pass-through before its dynamic/literal switch. A reference-bearing entry is accepted only when it is an excludable single-name `CatSecrets` assignment; `Secret.Key` is non-empty and exactly `Names[0]`; `Secret.Kind` is one of the model's recognized kinds and exactly matches the injected active driver's `Kind`; `ValueMode` is `unsupported`; `RuntimeValue` is nil; `Dynamic` is false; and `Value` plus exported/non-exported `Text` exactly equal `secretRefValue(*Secret)` with no other literal-bearing representation. Require a non-nil backend for this validation but never call `Retrieve`, `Store`, or `Delete`. On a valid reference, copy the entry unchanged, append neither `pendingSecret` nor `WithheldSecret`, and proceed to serialization; any malformed or mismatched reference returns the existing `ErrUnsafeSecretShape` before backend mutation, object hashing, or ref movement (`ErrSecretRefConflict` remains reserved for concurrent Git ref movement). This makes authoritative `profile.json` metadata pass through Commit without exposing the backend value or replacing it with its inert placeholder.

    Then extend the CLI-level tests to execute the whole Task 2 controller twice over the same installed target and stateful fake/real store, comparing complete bytes rather than only the marker slice. The secret-bearing second-ingest regression must begin with a literal such as `API_TOKEN=sk-original`, capture it on run one, snapshot `Store.Read(main).Entries[...].Secret`, backend contents, `.zshrc`, Git `profile.json`/`profile.zsh`, stdout, stderr, and first withheld report, then run unchanged ingest again. Assert the durable baseline probe selects re-ingest independently of entry count; the reference is deeply equal; the backend still contains `sk-original` and its mutation/retrieval counters did not change; no committed/generated artifact or stream contains the literal; only the first run reports `API_TOKEN`; and complete `.zshrc` plus committed output remain stable. Add an explicitly committed empty profile case and prove its present `main:profile.json` selects re-ingest rather than first adoption.

    Add table tests for empty/unknown/mismatched Kind, empty/wrong Key, wrong category/name cardinality, non-redacted Value/Text/RuntimeValue, dynamic/reference conflicts, and active-backend mismatch, proving fail-closed no-mutation behavior. Add a changed-master rerun: after the first successful ingest, edit imperative text and add a declarative export in the pre-marker region, rerun, and assert the current master is freshly rendered and promoted exactly once, the merged profile reflects the new routing, each statement appears once in preserved StartLine/ordinal order, and stored SecretRef entries remain structural rather than reparsed placeholders. Seed an exact block followed by comment-plus-export installer append lines and assert warning output plus byte identity.

    Exercise the transaction boundary with deterministic failures after initial promotion at initializer, Read, Parse/layout/Build, secret-backend preparation, and Commit CAS. For each, snapshot original target bytes/mode, cache tree/modes, store-root presence/mode/refs/trees, backend values/counters, transaction artifacts, and downstream call counters; require `rollbackBeforeCommit` restores the snapshot, removes a store created by this invocation or leaves a preexisting store logically exact, and never calls final promotion. Exercise initial guarded stale recovery separately, proving the recovered external edit survives outer rollback. For Commit-success/final-promotion failure, require `acceptCommit` leaves the baseline and initial installation durable, fixed partial-state output, and a normal rerun converges without duplicate statements, reports, markers, or backend access. Reuse Plan 06-01's lease/typed promotion protocol; do not add another lock or journal.
  </action>
  <acceptance_criteria>
    - Full-file idempotence is proven through `CLI.Run`, not only a pure helper.
    - A changed-master rerun updates the fresh committed partition and master without duplication while preserving `StartLine`/source order.
    - A validated stored SecretRef survives unchanged re-ingest with zero backend calls and zero duplicate withheld items; its original backend literal never appears in source, Git, or output.
    - Every malformed or backend/key-mismatched SecretRef fails before backend, Git ref, or `.zshrc` final-promotion mutation.
    - Append warnings are deterministic and non-mutating.
    - A committed empty profile is distinguished from an uncommitted empty branch by durable object existence.
    - Every pre-Commit failure after initial promotion restores the pre-ingest target/cache and created/preexisting logical store/backend state; an idempotent rerun repairs the only permitted post-Commit partial state.
  </acceptance_criteria>
  <verify>
    <automated>store_tests='TestPrepareSecretsExistingRef TestCommitExistingSecretRef' && cli_tests='TestRunIngestIdempotentSecretRef TestRunIngestCommittedEmptyBaseline TestRunIngestChangedMaster TestRunIngestAppendWarning TestRunIngestPreCommitFailureRollback TestRunIngestRepairAfterFinalPromotionFailure TestRunIngestCommitConflictRollback' && store_listed=$(GOTOOLCHAIN=auto go test ./core/store -list '^Test') && cli_listed=$(GOTOOLCHAIN=auto go test ./core/cli -list '^Test') && for test_name in $store_tests; do count=$(printf '%s\n' "$store_listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && for test_name in $cli_tests; do count=$(printf '%s\n' "$cli_listed" | awk -v name="$test_name" '$0 == name { n++ } END { print n + 0 }'); test "$count" -eq 1 || { printf 'expected exactly one %s, found %s\n' "$test_name" "$count" >&amp;2; exit 1; }; done && GOTOOLCHAIN=auto go test ./core/store -run '^(TestPrepareSecretsExistingRef|TestCommitExistingSecretRef)$' -count=1 && GOTOOLCHAIN=auto go test ./core/cli -run '^(TestRunIngestIdempotentSecretRef|TestRunIngestCommittedEmptyBaseline|TestRunIngestChangedMaster|TestRunIngestAppendWarning|TestRunIngestPreCommitFailureRollback|TestRunIngestRepairAfterFinalPromotionFailure|TestRunIngestCommitConflictRollback)$' -count=1 && GOTOOLCHAIN=auto go test ./core/store ./core/cli -count=1</automated>
  </verify>
  <done>Unchanged secret-bearing reruns preserve the authoritative reference and backend without duplicate reporting, while ordinary reruns remain byte-stable, stale/conflicting writes do not clobber, and the declared partial state converges on retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| arbitrary zsh source -> Parser/IR | User configuration may contain executable shell syntax, substitutions, malformed input, and secrets. |
| model profile -> secret backend and Git ref | Literal secret data crosses into the keychain/vault while only redacted data may reach committed blobs. |
| CLI-selected path -> install/master mutation | A user-selected file is read, installed, and atomically rewritten. |
| store commit -> final file promotion | Two separately durable resources can fail at different points. |
| domain result -> human/JSON output | Withheld metadata is public while secret values and zsh text are private. |

## ASVS Level 1 Baseline

OWASP ASVS Level 1 applies to ingest's local CLI and persistence boundaries. V1.1 input-validation controls map to static parser-only handling and exact path/marker validation, V8.3 sensitive-data controls map to Commit-only secret exclusion and value-free output DTOs, V4.1 access controls map to the narrow injected store/provider seams, and V7.4 error-handling controls map to explicit conflict and partial-state outcomes. Execution and plan completion are blocked while any critical/high threat lacks both an executable mitigation and its automated verification.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-06-02-01 | Elevation of Privilege | `runIngest` source handling | critical | mitigate | Use only Provider.Parse + ir.Build; never call Introspect/source/eval; inject a panic-on-Introspect provider and retain dynamic expressions verbatim in tests. |
| T-06-02-02 | Information Disclosure | `Store.Commit`, stored `SecretRef`, and ingest renderers | critical | mitigate | Reuse Commit's fail-closed exclusion, validate and structurally pass through existing references with zero backend access/reporting, map only Name/StartLine into DTOs, and assert literal absence from committed blobs and both output modes across a second ingest. |
| T-06-02-03 | Tampering | managed/imperative partition | high | mitigate | Use only `EffectiveManaged`, account every parsed block exactly once, commit only the managed slice, and compare every unmanaged Text byte in master output. |
| T-06-02-04 | Tampering | selected path and stale promotion | high | mitigate | Resolve an absolute target through the landed installer boundary and use Plan 06-01's serialized, recoverable guarded promotion with its adversarial mutation test. |
| T-06-02-05 | Tampering | concurrent `main` commits | high | mitigate | Rely on landed Commit ref CAS and secret compensation; test conflict prevents file promotion and never report success. |
| T-06-02-06 | Repudiation | commit-success/file-failure outcome | high | mitigate | Return a distinct error naming the advanced `main` baseline and unchanged file, then prove idempotent rerun convergence. |
| T-06-02-07 | Denial of Service | malformed markers/store/backend failure | medium | mitigate | Reuse installer/store fail-closed paths; prepare all file bytes before Commit and preserve source on any pre-promotion error. |
| T-06-02-08 | Information Disclosure | internal error -> human/JSON renderer | high | mitigate | Convert every stage to an allowlisted code/fixed message, collapse unknown causes, and inject secret/path/newline-bearing errors at parser, Init, probe, Read, backend, Commit, promotion, and unknown seams in both output modes. |
| T-06-02-09 | Tampering | initial install -> Init/Read/Commit handoff | high | mitigate | Hold Plan 06-01 compensation live across every pre-Commit stage; snapshot target/cache/store/backend; roll back on each injected failure; and disarm only after successful Commit. |
| T-06-02-SC | Tampering | package installation | low | accept | No package-manager install or new dependency is part of this plan. |
</threat_model>

<pre_mortem>
| Likely failure | Early mitigation |
| --- | --- |
| Ingest calls the Provider's introspection path and executes user startup code. | Use a panic-on-Introspect test provider and verify dynamic command substitutions remain source text. |
| Moving WithheldReport into a shared package breaks Phase 5 store/emitter doubles. | Use aliases in store and a segregated `IngestStore` interface so existing `Store` consumers stay narrow. |
| A fresh Phase 5-installed file or committed-empty baseline is assigned the wrong adoption behavior. | Select from pre-install marker topology plus fallible `main:profile.json` existence; test installed-uncommitted and committed-empty states without inspecting entry count. |
| Store commit succeeds but the file rename fails and CLI reports clean. | Centralize ordering in `commitAndPromote`, return a distinct partial-state error, and test retry convergence. |
| An Init/Read/parser/backend error occurs after initial promotion and the completed installer has already discarded rollback. | Use `beginIngestInstallAt` and keep its transaction armed through Commit; inject each stage failure and compare exact state snapshots. |
| A backend or parser error includes a literal/path/newline and reaches an agent response. | Render only typed codes and fixed messages; adversarially inject every stage and parse the one-object JSON output. |
</pre_mortem>

<source_audit>
| Source | ID | Feature/Requirement | Plan coverage | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Full parse/classify/partial-eval/regenerate/main-commit on-ramp | 06-02 Tasks 1-3 | COVERED | Store Commit regenerates profile.zsh; 06-03 proves the oracle. |
| REQ | PROF-03 | Default secret exclusion plus exact withheld report | 06-02 Tasks 1-2 | COVERED | Shared report types and Commit-only exclusion. |
| RESEARCH | — | None: research explicitly skipped | — | EXCLUDED | Current code and PATTERNS.md were reconciled. |
| CONTEXT | D-01 | `ingest [path] [--json]`, default `~/.zshrc` | 06-02 Task 2 | COVERED | CLI parsing and target resolution. |
| CONTEXT | D-02 | Dispatch from `CLI.Run`; typed exits and fail contract | 06-02 Task 2 | COVERED | Actual Phase 5 switch is extended. |
| CONTEXT | D-03 | Deterministic human summary and accounting | 06-02 Tasks 2-3 | COVERED | Managed/master/source counts. |
| CONTEXT | D-04 | One structured JSON envelope | 06-02 Task 2 | COVERED | Dedicated DTO, exact command errors. |
| CONTEXT | D-05 | Existing `EffectiveManaged` partition | 06-02 Task 2 | COVERED | No new classifier. |
| CONTEXT | D-06 | Imperative text to distinct master | 06-02 Task 2 | COVERED | Verbatim master renderer from 06-01. |
| CONTEXT | D-07 | Reuse landed installer | 06-02 Task 2 | COVERED | Calls the live `beginIngestInstallAt` variant over the shared landed transaction and retains compensation through Commit. |
| CONTEXT | D-08 | Surface Commit's report, no values | 06-02 Tasks 1-2 | COVERED | Neutral types and DTO mapping. |
| CONTEXT | D-09 | Commit errors fail closed | 06-02 Tasks 2-3 | COVERED | No promotion on commit error. |
| CONTEXT | D-10 | Init and commit fixed `main` | 06-02 Task 2 | COVERED | Idempotent Init, deterministic message. |
| CONTEXT | D-11 | Append warning without failure/mutation | 06-02 Task 3 | COVERED | CLI-level bytes/output proof. |
| CONTEXT | D-12 | Exact marker scan, not reparse | 06-01 and 06-02 Task 3 | COVERED | Shared landed marker constants. |
| CONTEXT | D-15 | Same composition-root store injection | 06-02 Task 1 | COVERED | Same constructor object, segregated extension. |
| CONTEXT | D-16 | Extend landed seam, do not compete | 06-02 Task 1 | COVERED | `IngestStore` embeds current Store. |
</source_audit>

<spec_fallback>
The automatic two-stage prohibition probe recalled executable-input, secret disclosure, silent routing loss, ordinary error handling, path traversal, and shell-injection candidates. Precision filtering dropped routine engineering items and referred generic traversal/injection canon to the STRIDE register. It kept exactly three product-specific safety/privacy prohibitions for this plan: static-only non-execution, literal-secret non-disclosure, and no silent imperative loss or promotion. All three are projected verbatim in `must_haves.prohibitions` as descriptor-less, `status: unresolved`, `verification: null`, and flagged. Equality: 3 kept = 3 projected; 0 dismissed and 0 silently dropped.
</spec_fallback>

<assumption_delta>
The deterministic detector fired only on ROADMAP wording that says the operation runs a "second time" in an idempotency context. This is a false positive for identity-model generalization. No identity model changes: the primary noun remains the single baseline profile on branch `main`; no plural user/account/profile identity abstraction, selector, schema, or migration is introduced.
</assumption_delta>

<verification>
Run all ingest-focused tests plus the existing store secret/CAS tests. Then run `GOTOOLCHAIN=auto go test ./core/cli ./core/store ./core/model -count=1` and `GOTOOLCHAIN=auto go vet ./core/cli ./core/store ./core/model`.
</verification>

<success_criteria>
- The actual CLI exposes `ingest [path] [--json]` with correct human/agent output.
- Mixed input is partitioned without silent loss; managed state reaches `main`, imperative text reaches the master.
- Literal secrets are absent from Git/output and named by line; dynamic references remain late-bound.
- Re-ingest is complete-file idempotent and post-END appends warn without mutation.
- Adoption is based on durable profile-object existence plus marker topology, including committed-empty profiles; changed re-ingest promotes its freshly rendered master exactly once.
- Every pre-Commit failure restores the pre-ingest target/cache and created/preexisting logical store/backend state; Commit-success/final-promotion-failure is the only partial outcome.
- Human and JSON errors expose only allowlisted codes/fixed messages and never raw parser, filesystem, Git, backend, promotion, path, newline-bearing, or unknown causes.
</success_criteria>

<output>
Create `.planning/phases/06-ingest-end-to-end/06-02-SUMMARY.md` when done.
</output>
