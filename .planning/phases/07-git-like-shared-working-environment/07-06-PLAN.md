---
phase: 07-git-like-shared-working-environment
plan: 06
type: execute
wave: 5
depends_on: ["07-03", "07-05"]
files_modified:
  - core/worktree/service.go
  - core/worktree/service_test.go
  - core/cli/store.go
  - core/cli/worktree.go
  - core/cli/worktree_test.go
  - core/cli/cli.go
autonomous: true
requirements: [WORK-03, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "[WORK-03] Status/diff read one shared branch/base/revision/dirty/conflict truth; diff is categorized and deterministic, and sourced status may add the caller shell's applied/behind state only from a strictly validated opaque `ZSHPRO_SHELL_ID`."
    - "[WORK-03] Commit includes every effective dirty identity through BuildEffective/CommitWorktree, then clears only the proven committed overlay after ref CAS and canonical state save."
    - "[WORK-03] Branch forks current base, dirty checkout is blocked, and only explicit `reset --hard` discards shared dirty state."
    - "[SYNC-01] Persisted auto-apply default accepts exact lowercase true/false, remains distinct from shell override, and status reports effective source."
    - "[SYNC-02] Proven Git publication plus uncertain canonical state save reports committed/recovery-required evidence and repairs from the exact ref; it never claims rollback or clean state."
    - "Unsupported staging, advanced history, remote, and multiple-worktree commands fail before any dependency call."
  artifacts:
    - path: "core/worktree/service.go"
      provides: "Locked Commit, Branches, Branch, Checkout, ResetHard, and SetAutoApplyDefault workflow policy"
      contains: "func (s *Service) ResetHard"
    - path: "core/cli/worktree.go"
      provides: "Strict Git-shaped public command parsing and value-safe rendering"
      contains: "func (c *CLI) runWorktree"
    - path: "core/cli/store.go"
      provides: "Consumer-owned WorktreeReader and WorktreeWorkflow interfaces"
      contains: "type WorktreeWorkflow interface"
  key_links:
    - from: "Service.Commit"
      to: "activate.BuildEffective then Store.CommitWorktree"
      via: "source-plus-overlay projection and expected base under one worktree lock"
      pattern: "CommitWorktree"
    - from: "CLI commands"
      to: "WorktreeWorkflow"
      via: "complete argv validation before service access"
      pattern: "runWorktree"
    - from: "sourced `zsh-pro status`"
      to: "Service.Status"
      via: "Plan 07-09 scopes exported ZSHPRO_SHELL_ID to `command zsh-pro`; worktree.go validates the opaque ID before lookup and never treats it as branch/root authority"
      pattern: "ZSHPRO_SHELL_ID"
    - from: "Store publication result"
      to: "State.BaseOID/overlay"
      via: "committed/conflict/recovery-required typed outcome"
      pattern: "WorktreeCommitResult"
  prohibitions:
    - statement: "MUST NOT discard a dirty shared diff except through the explicit destructive reset operation."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT silently broaden the local single-worktree workflow into staging, advanced history editing, remote collaboration, or externally authored profile application."
      category: product-safety
      status: unresolved
      flagged: true
---

<objective>
Expose the shared materialized worktree as a truthful basic local Git workflow.

Purpose: Give users status/diff/commit/current-base branch/dirty checkout/reset/config behavior without mixing Store mechanics, runtime hooks, or ingest recovery into the CLI controller.
Output: Locked workflow service methods, narrow CLI interfaces, strict commands, and value-safe output.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-03-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-05-SUMMARY.md
@core/worktree/service.go
@core/cli/store.go
@core/cli/cli.go

<interfaces>
- Worktree Service owns dirty/shared policy and receives narrow repository/projection dependencies; CLI never receives concrete Store.
- Public binary surface: `status`, `diff`, `commit -m <message>`, `branch`, `branch <name>`, `checkout <name>`, `checkout -b <name>`, `reset --hard`, and `config set auto-apply true|false`; `list` remains branch-list compatibility. Plan 07-09's sourced `zsh-pro()` wrapper invokes this binary through `command zsh-pro`.
- The only current-shell context accepted by the binary is the optional opaque `ZSHPRO_SHELL_ID` environment value. Validate it with the same shell-ID contract before Service access; absence yields shared-only status, while malformed input fails value-safely. It never selects branch, root, revision, or storage.
- `sync` and `sync --resolve shared` are sourced-loader operations added in Plan 07-09, not alternate direct mutation paths here.
</interfaces>
</context>

## Artifacts this phase produces

- `core/worktree/service.go` gains locked basic Git workflow and persisted auto-apply default operations, including exact ref/state split recovery.
- `core/cli/store.go` gains narrow reader/workflow interfaces.
- New `core/cli/worktree.go` parses/renders the exact accepted public surface and validates optional sourced-shell identity context for status.
- `core/cli/cli.go` dispatches the new surface and keeps analyze/ingest/runtime compatibility.
- Tests cover clean/dirty/current/existing/CAS/recovery/config matrices, shared-only versus valid/malformed sourced-shell status, plus zero-service-call negative commands.
- No ingest participant, composition-root change, runtime private command, hook, package, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce locked all-dirty commit, branch, checkout, reset, and config policy</name>
  <files>core/worktree/service.go, core/worktree/service_test.go</files>
  <read_first>
    - core/worktree/service.go: Plan 07-03 transaction/status/diff/attach/resolve state machine
    - core/worktree/atomic_unix.go: single authenticated lock boundary
    - core/activate/builder.go: BuildEffective committed projection
    - core/store/store.go: Plan 07-05 exact repository operations/outcomes
    - .planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md: workflow semantics
  </read_first>
  <action>
    Extend Service with the exact workflow methods in the artifact inventory. Hold the existing worktree transaction across dirty policy, expected base capture, repository call, and canonical state decision. Commit builds the complete committed projection and sends all dirty identities; after proven CAS, update base and clear exactly the committed overlay. If the ref is proven moved but state save is uncertain, return exact recovery-required evidence and repair by `ReadWorktreeRevision`/observed ref on the next status before claiming clean. Branch uses current base without switching. Checkout and checkout-create reject any dirty overlay, read exact target worktree DTO, and publish one revision event for changed identities. ResetHard is the only discard path and restores the exact base projection. SetAutoApplyDefault preserves explicit false and does not alter shell overrides or worktree identity. Add barrier/fault tests for every clean/dirty/CAS/split outcome.
  </action>
  <acceptance_criteria>
    - Commit includes every dirty identity and has no partial/staging input.
    - Branch/checkout/reset behavior matches the accepted Git model and dirty state is never implicitly lost.
    - Git/state split evidence remains truthful and repairable from an exact committed object.
    - Auto-apply default changes no branch/base/overlay/per-shell override.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/worktree -run 'Test(Worktree|Commit|Branch|Checkout|Reset|AutoApply|Recovery)' -count=1</automated>
  </verify>
  <done>The service owns one strict, shared, unstaged, dirty-safe, recoverable local Git workflow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse and render the exact public workflow surface</name>
  <files>core/cli/store.go, core/cli/worktree.go, core/cli/worktree_test.go, core/cli/cli.go</files>
  <read_first>
    - core/cli/store.go: current consumer-owned Store interfaces
    - core/cli/cli.go: strict dispatcher, noArgumentUsage, typed-nil, and exit-code behavior
    - core/cli/ingest.go: closed parser and safe renderer analogs
    - core/model/worktree.go: status/diff/workflow result types
    - core/worktree/service.go: Task 1 exact methods/outcomes
  </read_first>
  <action>
    Preserve current CLI typed-nil and output behavior. Add focused `WorktreeReader`/`WorktreeWorkflow` interfaces and a `worktree.go` controller. Validate the complete argv before dependency/filesystem access. Render shared versus current-shell applied/behind/auto-source fields explicitly; render categorized values only from admitted non-secret diff types; never print raw object IDs/errors/captured secret values except documented value-safe data. For status, read the optional `ZSHPRO_SHELL_ID` environment context, validate its opaque grammar/size before dependency access, and pass it only as a shell-state lookup key. Missing context renders shared truth plus an explicit unattached/no-current-shell view; malformed context fails value-safely with zero service calls. The ID cannot select branch/root/revision or appear in diagnostics. Accept only exact lowercase config booleans and exact verb/flag shapes. Preserve list compatibility. Add table tests for every accepted and rejected arity/flag, independent CLI shared status, valid/missing/malformed shell-ID status, stable category order, and zero calls for add/stage/partial commit, merge/rebase/cherry-pick/remote/push/pull, multiple worktrees, malformed config, and unknown verbs.
  </action>
  <acceptance_criteria>
    - Two CLI instances render the same shared worktree portion; a valid sourced shell ID adds only that shell's applied/behind fields.
    - Accepted verbs/flags delegate once with typed requests; invalid/forbidden input delegates zero times.
    - Status/config clearly distinguishes persisted default from shell override.
    - Outputs and diagnostics preserve the value/secret boundary.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/cli -run 'Test(Worktree|Status|SourcedShellID|Diff|Commit|Branch|Checkout|Reset|AutoApply|UnsupportedGit)' -count=1</automated>
  </verify>
  <done>The public command surface is exact, value-safe, dependency-narrow, and closed against scope expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CLI argv -> workflow policy | Branch names/messages/flags request durable effects |
| worktree service -> Git Store | Effective document and expected base cross publication boundary |
| Git success -> state save | Two durable systems can split after ref publication |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-23 | Tampering | dirty checkout/reset | high | mitigate | One lock, explicit dirty guard, sole ResetHard discard, and real-service tests in Task 1. |
| T-07-24 | Repudiation | Git-success/state-failure | high | mitigate | Exact committed/recovery outcome, observed-ref repair, and no false rollback/clean claim in Task 1. |
| T-07-25 | Elevation of Privilege | CLI args/shell context -> service/Git | high | mitigate | Complete argv and opaque shell-ID validation, narrow typed requests, no identity-as-authority use, existing branch/message checks, and zero-call hostile tests in Task 2. |
| T-07-26 | Information Disclosure | diff/status/errors | high | mitigate | Admitted typed diff only, value-free conflicts/errors, and canary output tests in Task 2. |

ASVS Level 1 applies; all high-severity workflow and output gates are blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/worktree ./core/cli -count=1`
- `GOTOOLCHAIN=local go vet ./core/worktree ./core/cli`
- Focused real-Git tests cover current-base create, all-dirty commit, dirty checkout, reset, ref race, and split recovery.
</verification>

<success_criteria>
- Status/diff/commit/branch/checkout/reset/config implement the accepted Git mental model with no staging or implicit discard.
- Workflow/output remain causal, recoverable, value-safe, and closed to out-of-scope operations.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-06-SUMMARY.md` when done.
</output>
