---
phase: 07-git-like-shared-working-environment
plan: 11
type: execute
wave: 9
depends_on: ["07-10"]
files_modified:
  - core/worktree/state.go
  - core/worktree/state_test.go
  - core/worktree/registry.go
  - core/worktree/registry_test.go
  - core/worktree/service.go
  - core/worktree/service_test.go
autonomous: true
requirements: [WORK-01, WORK-02, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "A safe identity admitted after attach is durably owned by the one canonical worktree generation as kind/name metadata only; no captured value, capability, verifier, or shell-local snapshot becomes ownership metadata."
    - "Fresh Registry and Service instances restore exactly the same admitted set under the current policy, while ambient inherited, volatile, bookkeeping, and secret-like identities remain unowned."
    - "After A publishes a newly admitted identity, late shell C can attach through a fresh Service, prepare the exact current head, apply it, and acknowledge that head without the identity being filtered out."
    - "Repeated publication/replay is idempotent, and concurrent disjoint admissions serialize into one deterministic duplicate-free identity set without losing either owner."
    - "The generalized identity remains one canonical shared worktree/generation; fresh services and independent terminals are authenticated views, not additional worktrees or sources of truth."
  artifacts:
    - path: "core/worktree/state.go"
      provides: "Strict value-free admitted identity field in the canonical State DTO"
      contains: "AdmittedIdentities"
    - path: "core/worktree/registry.go"
      provides: "Policy-revalidated restoration of durable admitted identities"
      contains: "RestoreAdmitted"
    - path: "core/worktree/service.go"
      provides: "Transaction-local registry restoration and atomic recording of newly admitted identities"
      contains: "seedRegistryFromState"
    - path: "core/worktree/service_test.go"
      provides: "Fresh-service A-publishes/late-C attach-prepare-ack regression"
      contains: "TestFreshServiceLateShellUsesDurableAdmission"
  key_links:
    - from: "State.AdmittedIdentities"
      to: "Registry.RestoreAdmitted"
      via: "seedRegistryFromState under the authenticated StateStore transaction before every policy decision"
      pattern: "seedRegistryFromState"
    - from: "Registry.Admit result"
      to: "canonical worktree.json"
      via: "appendAdmittedIdentity in the same Publish/Resolve/Acknowledge transaction as the accepted state"
      pattern: "appendAdmittedIdentity"
    - from: "late shell C initial snapshot"
      to: "exact head acknowledgement"
      via: "fresh Service attach -> prepare -> apply-equivalent snapshot -> acknowledge"
      pattern: "TestFreshServiceLateShellUsesDurableAdmission"
---

<objective>
Close the durable admitted-identity ownership gap without broadening the admission boundary.

Purpose: Make every fresh Registry/Service interpret the same canonical value-free ownership metadata so later terminals receive identities that earlier terminals safely introduced.
Output: Strict canonical admission metadata, policy-checked registry restoration, transactionally recorded admissions, and fresh-instance concurrency/idempotency proof.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-10-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md

<interfaces>
- `State` is the single canonical generation. Persist admitted ownership as normalized `[]model.Identity`; values and shell capability material are forbidden.
- `Registry.Seed` remains source ownership. `Registry.RestoreAdmitted` restores only previously admitted kind/name pairs after re-running the same safety policy.
- `Service` must seed source ownership and restore durable admission inside each StateStore transaction before Attach, Publish, ResolveShared, Acknowledge, materialization repair, or workflow replacement interprets identities.
- Existing schema-version-1 payloads without the field may migrate only from already-canonical `State.Shared` identities that pass the live policy and are not source-owned. Never infer ownership from `ShellState`, the caller's current snapshot, environment, or `PresentAtAttach`.
- Preserve all current user edits and tests in the listed files. Read the live file immediately before editing and merge narrowly.
</interfaces>
</context>

## Artifacts this phase produces

Production symbols and fields:

- `State.AdmittedIdentities []model.Identity` and JSON field `admitted_identities` in `core/worktree/state.go`.
- `(*Registry).RestoreAdmitted([]model.Identity) error` in `core/worktree/registry.go`.
- `seedRegistryFromState(*Registry, *State) error` and `appendAdmittedIdentity(*State, model.Identity) error` in `core/worktree/service.go`.

Test-only artifacts:

- `TestStateAdmittedIdentitiesStrictValueFreeRoundTrip` and `TestRegistryRestoreAdmittedRevalidatesPolicy`.
- `TestFreshServiceLateShellUsesDurableAdmission` and `TestConcurrentDurableAdmissionsAreDeterministic`.

No new file, command, package, worktree, remote, staging area, synchronized process state, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and restore the exact value-free admitted identity set</name>
  <files>core/worktree/state.go, core/worktree/state_test.go, core/worktree/registry.go, core/worktree/registry_test.go</files>
  <read_first>
    - core/worktree/state.go: State/stateDTO strict encoding, validation, cloning, and materialization defaults
    - core/worktree/state_test.go: canonical JSON, unknown-field, normalization, size, and corruption conventions
    - core/worktree/registry.go: source seeding, policy-owned set, attach admission, and secret pinning
    - core/worktree/registry_test.go: admission adjacency, exclusion, and value-free assertion patterns
    - .planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md: failed durable-ownership truth and exact late-C scenario
  </read_first>
  <behavior>
    - RED: a normalized admitted identity round-trips through strict State JSON as kind/name only; duplicates, invalid identities, unsafe/pinned identities at restoration, and value-bearing or unknown fields fail closed.
    - RED: two fresh registries seeded from the same Source plus AdmittedIdentities return identical ownership decisions; an ambient present-at-attach identity remains excluded.
    - RED: absent-field compatibility derives candidates only from canonical Shared under policy, while a newly materialized state emits a present empty admitted list.
  </behavior>
  <action>
    Preserve unrelated working-tree changes. First add the named failing tests, run the focused command, confirm failure for missing durable restoration, and commit the RED contract before production edits. Then add `State.AdmittedIdentities` to validation, clone, marshal, strict decode, size accounting, and materialization initialization. Canonically sort and reject duplicates; retain no live values, capabilities, verifiers, operation IDs, or errors in this field. Add `Registry.RestoreAdmitted`, which validates every identity through the existing model and live policy, rejects bookkeeping/volatile/secret-like identities, and merges idempotently with source ownership without changing SecretRef pinning. Preserve missing-versus-empty decode information long enough for the Service migration in Task 2; do not infer from any ambient shell input. Run the focused tests GREEN and refactor without weakening strict decoding.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/worktree -run '^(TestStateAdmittedIdentitiesStrictValueFreeRoundTrip|TestRegistryRestoreAdmittedRevalidatesPolicy)$' -timeout=20s</automated>
  </verify>
  <acceptance_criteria>
    - `worktree.json` represents admitted ownership only as deterministic identity kind/name records.
    - Fresh registries restore identical ownership and reject all unsafe identity classes under the current policy.
    - Repeated restoration and duplicate input cannot create duplicate owners or change source/secret semantics.
    - The RED test commit precedes the GREEN implementation commit.
  </acceptance_criteria>
  <done>The canonical State contract and Registry restoration seam are strict, deterministic, value-free, and policy-equivalent across instances.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record admissions atomically and prove late-shell exact-head convergence</name>
  <files>core/worktree/service.go, core/worktree/service_test.go</files>
  <read_first>
    - core/worktree/service.go: every Registry.Seed call, Attach, Publish, PreparePull, ResolveShared, Acknowledge, and admittedChanges/admittedSnapshot
    - core/worktree/service_test.go: fresh Service/Registry fixtures, operation replay, concurrent publish, and exact pending/ack assertions
    - core/worktree/state.go: Task 1 admitted identity invariants and missing-field compatibility representation
    - core/worktree/registry.go: Task 1 RestoreAdmitted semantics
    - .planning/phases/07-git-like-shared-working-environment/07-10-PLAN.md: canonical 21-edge phase matrix that remains the phase-level disposition
  </read_first>
  <behavior>
    - RED: A attaches, creates and publishes a safe new identity, closes its Service/Registry, and late C uses a fresh Service/Registry to attach, prepare the exact head, submit the exact resulting snapshot, and acknowledge that head.
    - RED: replaying A's publish or admitting the same identity twice does not duplicate metadata or advance the revision twice.
    - RED: concurrent disjoint safe admissions serialize with both identities present in canonical order; same-identity concurrency remains one owner and the existing conflict/replay rules remain authoritative.
  </behavior>
  <action>
    Preserve all current tests and add the RED fresh-instance and concurrency cases before code. Commit that failing contract. Replace scattered source-only seeding with `seedRegistryFromState`, invoked under the current transaction lock everywhere identity ownership is interpreted. For new states restore Source plus AdmittedIdentities. For an older strict state whose field was absent, derive only safe non-source identities already present in canonical Shared, write that value-free set during the same transaction, and never consult ShellState or the incoming snapshot for migration. Change admitted filtering so each successful `Registry.Admit` calls `appendAdmittedIdentity` before an accepted live change can be committed; record the identity in the same canonical transaction as Shared/events/receipts. If validation or persistence fails, accept neither owner nor live change. Keep operation replay idempotent and deterministic under the existing lock. Prove the exact A-to-late-C Attach/PreparePull/Acknowledge sequence with newly constructed Registry and Service objects and an exact head/fingerprint assertion. Run focused and full worktree tests GREEN.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/worktree -run '^(TestFreshServiceLateShellUsesDurableAdmission|TestConcurrentDurableAdmissionsAreDeterministic|TestWorktreeEdgeContract)$' -timeout=30s</automated>
  </verify>
  <acceptance_criteria>
    - A's accepted new identity and its new revision are one atomic canonical outcome.
    - Late C's fresh Registry/Service includes the identity in its pending exact-head target and accepts C's exact acknowledgement.
    - Replay, repeated admission, and concurrent disjoint publication preserve a duplicate-free deterministic set and existing revision/conflict rules.
    - No ambient inherited identity or secret value becomes durable ownership, and no diagnostic or test artifact retains a value canary.
    - The RED test commit precedes the GREEN implementation commit.
  </acceptance_criteria>
  <done>Canonical admitted ownership survives Service lifetimes and drives exact late-terminal convergence without a second source of truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shell snapshot -> Registry policy | Untrusted ambient names and values request admission into canonical ownership. |
| worktree.json -> fresh Service | Durable identity metadata is parsed and restored as authorization-relevant ownership. |
| private credential -> Service transaction | Shell capability authenticates the view but must never become ownership metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-11-01 | Information Disclosure | State.AdmittedIdentities/errors/tests | high | mitigate | Store kind/name only; strict DTO rejects value-bearing extras; canary assertions cover JSON, errors, logs, receipts, and retained artifacts. |
| T-07-11-02 | Elevation of Privilege | Registry.RestoreAdmitted | high | mitigate | Re-run model and policy validation; reject secret-like, volatile, bookkeeping, malformed, and ambient-derived ownership before mutation. |
| T-07-11-03 | Tampering | concurrent/replayed Publish | high | mitigate | Update ownership and Shared under the same authenticated StateStore lock; canonicalize, deduplicate, and retain receipt idempotency. |
| T-07-11-04 | Spoofing | fresh Service credential view | high | mitigate | Keep existing verifier-only under-lock authorization before receipts; restored ownership grants no shell capability. |
| T-07-11-05 | Repudiation | late-C acknowledgement | medium | mitigate | Preserve exact operation receipt, revision, token, and fingerprint checks without logging values. |
| T-07-11-06 | Denial of Service | admitted identity set | medium | mitigate | Apply existing snapshot/record/byte limits and deterministic validation before allocation or persistence. |
| T-07-11-07 | Tampering | convergence reporting | high | mitigate | Exact pending fingerprint and acknowledgement are required before C is at-head; an admission failure remains behind/error. |

ASVS L1 gate: every high-severity mitigation above is blocking; the plan is incomplete until its automated proof passes.
</threat_model>

<verification>
- Run both task-focused commands and `GOTOOLCHAIN=local go test -count=1 ./core/worktree -timeout=60s`.
- Inspect the fresh-instance fixture's canonical JSON and assert the admitted identity name appears while its value/capability canaries do not.
- Confirm no new dependency or package-manager action exists.
</verification>

<success_criteria>
- The verification report's durable admitted-identity truth is executable and green across fresh instances.
- The gap-specific boundary, idempotency, and concurrency predicates pass without weakening the 07-10 21-edge matrix.
- One canonical worktree/generation remains the sole truth; multiple worktrees, staging, remotes, PWD/jobs/history/buffer/process synchronization, and false convergence remain prohibited.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | One canonical durable shared worktree interpreted consistently by independent terminals | COVERED by both tasks and fresh-instance late-C proof. |
| REQ | WORK-01, WORK-02, SYNC-01, SYNC-02 | COVERED explicitly in frontmatter and acceptance criteria. WORK-03 remains fully disposed by executed Plan 07-10 and is not affected by this gap. |
| RESEARCH | Value-free admission, Source/Registry policy, authenticated combined generation, exact snapshot/fingerprint acknowledgement | COVERED without changing ownership tiers. |
| CONTEXT | No Phase 7 CONTEXT.md exists; user-directed assumption delta says one canonical worktree/generation and per-shell views only | COVERED as a no-change constraint; no deferred idea is introduced. |
| SPECless fallback | Boundary, idempotency, and concurrency evidence for newly durable ownership | COVERED by the focused tests; the executed 07-10 21-edge matrix remains the phase-level disposition for all generic items. |

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-11-SUMMARY.md` when done.
</output>
