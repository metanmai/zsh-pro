---
phase: 07-git-like-shared-working-environment
plan: 09
type: execute
wave: 7
depends_on: ["07-03", "07-07", "07-08"]
files_modified:
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-02, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "[WORK-02] Sourcing the loader spawns zero subprocesses. The first safe hook/public-command boundary captures and retains one initial semantic snapshot, performs idempotent attach, and returns without publication; every later publication is gated on that successful no-delta attach."
    - "[WORK-02] Real-provider behavior keeps identities inherited at attach excluded while safe identities created after attach can be admitted and published."
    - "[SYNC-01] `precmd` publishes only; `zle-line-finish` pulls only; explicit `zsh-pro sync` uses the same prepare/apply/verify/ack path."
    - "[SYNC-01] `zsh-pro sync --resolve shared` deliberately resolves same-key and dirty history-gap conflicts by adopting shared state, discarding this shell's durable unpublished losing delta even when the capture baseline already locally acknowledged that value, and clearing conflict only after verify/ack."
    - "[SYNC-01] The installed sourced `zsh-pro()` dispatcher is the exact public route: sync/resolve and checkout/reset parent mutation use the shared apply pipeline, other verbs use `command zsh-pro`, and legacy checkout/activate/status/list functions cannot bypass shared workflow truth."
    - "[SYNC-01] Persisted default plus exact shell-local `ZSHPRO_AUTO_APPLY=true|false` override is visible; invalid override records an error and follows persisted default."
    - "[WORK-03/SYNC-01] Sourced checkout and reset capture and publish the current shell first; any timeout, conflict, unpublished remainder, or failed publication aborts the branch/reset mutation while retaining the local delta."
    - "[SYNC-02] Attach allocates a cryptographically random 256-bit shell ID plus independent capability after descriptor authentication and returns them only through the private framed response; every later private call carries the capability inside one bounded versioned stdin frame, never argv or environment, while Plan 07-03 Service derives/stores/verifies only its verifier under the canonical transaction."
    - "[SYNC-02] Prepare/resolve stdout is one whole zsh-n-validated private transition program ending in the exact emitter-owned `ZP_WORKTREE_REPLY_PROTOCOL`, `REVISION`, `TOKEN`, `FINGERPRINT`, and `COMPLETE` assignments; the parent clears/parses those values, captures fresh state, and supplies them to Acknowledge, so no in-process-only metadata is lost when the helper exits."
    - "[SYNC-02] Every Attach/Publish/Prepare/Acknowledge/Resolve runtime operation starts the production-default 250 ms budget before authentication, descriptor binding, one-shot frame I/O, service/render work, and Prepare/Resolve zsh -n validation; an unexported injectable monotonic clock/budget seam proves the exact cumulative boundary without creating a CLI, environment, or unbounded production override."
    - "[SYNC-02] A separately guarded parent-shell transition carries the same absolute default budget across buffered prepare, protected eval, replacement reverse ownership, fresh capture/match, and idempotent acknowledgement. Fake time proves cumulative 249 ms is admitted and cumulative 251 ms times out even when the final increment occurs at a late stage; active real-time fail-open tests use generous well-below/clearly-over margins at every stage and retain the old acknowledged revision on timeout."
    - "[SYNC-02] The sourced loader builds the private frame with zsh builtins under private locals and restored xtrace/history protections, clears every call-local capability/control/frame copy on every exit, and a blocked-helper process inspection proves the raw capability is absent from cmdline and environ before shell-ID-only/cross-shell replay is rejected."
    - "Apply order is bounded prepare/render -> exact zsh -n -> private buffered transport -> protected parent eval -> replacement reverse -> fresh snapshot match -> idempotent acknowledge; foreground/process-local state is never mutated asynchronously."
  artifacts:
    - path: "core/cli/runtime.go"
      provides: "Private attach/publish/prepare/acknowledge/resolve allowlist with one bounded versioned stdin protocol, post-auth shell-credential allocation/transport, validation-first late binding, exact executable reply transport, and the sealed production/injectable-test monotonic budget"
      contains: "const DefaultWorktreeTransitionBudget"
    - path: "core/shell/zsh/hook.go"
      provides: "Zero-subprocess source, stdin-only authenticated shell credential transport, private-local cleanup, lazy attach, pre-mutation publication, exact sourced zsh-pro dispatcher, emitter-reply parsing/cleanup, precmd publish, line-finish pull, and fully bounded sync/resolution lifecycle"
      contains: "zsh-pro()"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "RuntimeWorktreeFactory configured for post-auth operation-scoped binding to the canonical authority from Plan 07-07"
      contains: "RuntimeWorktreeFactory"
  key_links:
    - from: "first safe hook/public-command boundary"
      to: "Service.Attach"
      via: "one retained pure-zsh initial snapshot, post-auth descriptor-bound adapter, measured/tunable finite production-default context, and no publication on the attachment boundary"
      pattern: "_zp_worktree_ensure_attached"
    - from: "precmd"
      to: "Service.Publish"
      via: "versioned bounded stdin control frame carrying the attached shell ID/capability, operation ID, acknowledged revision, and live records with no argv/environment credential"
      pattern: "_zp_worktree_publish"
    - from: "sync --resolve shared"
      to: "ResolveShared then Acknowledge"
      via: "durable unpublished loser, pending resolution token, authoritative patch, replacement reverse, fresh snapshot"
      pattern: "ResolutionToken"
    - from: "sourced `zsh-pro()`"
      to: "public WorktreeWorkflow and parent-shell sync pipeline"
      via: "scoped ZSHPRO_SHELL_ID plus `command zsh-pro` for binary verbs; checkout/reset first publish or abort, then mutate and sync the parent; sync/resolve never bypass the sourced path"
      pattern: "command zsh-pro"
    - from: "runRuntime worktree operation"
      to: "authenticated RuntimeWorktreeFactory binding/service/validator"
      via: "DefaultWorktreeTransitionBudget plus an unexported monotonic budget object before auth/bind/I/O/service/render and exact-buffer zsh -n, coordinated with the parent-shell transition watchdog"
      pattern: "DefaultWorktreeTransitionBudget"
    - from: "Attach allocation"
      to: "all five private operations"
      via: "crypto/rand shell ID plus capability allocated after descriptor authentication, returned by a private bounded response, then forwarded unchanged only inside the versioned stdin frame; Plan 07-03 Service owns verifier derivation and under-lock validation"
      pattern: "ShellCapability"
    - from: "sourced private helper invocation"
      to: "runRuntime worktree frame decoder"
      via: "zsh-builtin length-framed stdin with exact EOF, trailing-byte rejection, call-local cleanup, and no argv/environment/fallback credential channel"
      pattern: "WorktreeRuntimeFrameVersion"
    - from: "runRuntime prepare/resolve stdout"
      to: "Service.Acknowledge inputs"
      via: "one buffered emitter-owned transition program sets exact protocol/revision/token/fingerprint/complete reply globals; parent validates and clears them around apply/fresh capture/ack"
      pattern: "ZP_WORKTREE_REPLY_COMPLETE"
  prohibitions:
    - statement: "MUST NOT mutate a foreground command or synchronize PWD, jobs, command buffers, history, process trees, or terminal UI as though independent shells were one process."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT make a helper, lock, state, renderer, validator, eval, or acknowledgement failure prevent the user's next prompt or entered command."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT hide that auto-apply is disabled, invalidly overridden, behind, or conflicted while presenting a shell as converged."
      category: transparency
      status: unresolved
      flagged: true
---

<objective>
Install the complete bounded lazy-attachment, exact-public-routing, publication, convergence, and conflict-resolution lifecycle in the real sourced loader.

Purpose: Make independent terminals safely converge and deliberately recover conflicts without inherited-state capture, foreground interference, prompt DoS, false acknowledgement, or stale reverse ownership.
Output: Bounded stdin-framed private runtime commands, a sealed/injectable-test whole-transition budget, authenticated stable shell capabilities absent from argv/environment, zero-subprocess lazy loader hooks, exact sourced public dispatch, and late-bound runtime composition.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-03-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-07-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-08-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/cli/runtime.go
@core/shell/zsh/hook.go
@core/cmd/zsh-pro/main.go

<interfaces>
- Private argv syntax keeps only the existing integer outer timeout and exactly five non-authorizing dispatch names: `runtime worktree <attach|publish|prepare|acknowledge|resolve> <seconds>`. There is no credential or operation payload after the timeout. Every invocation reads exactly one `ZPWT` version-1 stdin envelope containing a duplicated operation tag that must match argv, attach allocation/commit mode, shell ID and 32-byte capability when required, stable operation ID, canonical revision/token fields, and length-framed operation records. The complete envelope is capped at exactly 2,101,248 bytes (`model.MaxSnapshotBytes + 4 KiB`) and 10,016 records (`model.MaxSnapshotRecords + 16`); it is read once with a plus-one overflow sentinel, requires the declared final record followed by EOF, rejects duplicate/unknown/out-of-order fields, partial lengths, early EOF, trailing bytes, excessive counts/bytes, and closes every pipe/reader on all exits. No argv/environment/path fallback exists. Attach allocation is the sole credential-less frame mode; after descriptor authentication it returns a new ID/capability through a separate bounded private response without state mutation. Attach commit and the other four operations carry the exact credential and payload in the stdin envelope. Runtime validates syntax/encoding/bounds and forwards `ShellCredential`; only Service derives/compares the verifier under `WithTransaction`.
- After argv validation, every worktree operation creates one unexported `transitionBudget` from `DefaultWorktreeTransitionBudget = 250*time.Millisecond` and an injected monotonic `transitionClock` before root authentication, `RuntimeWorktreeFactory.Bind`, one-shot stdin read/decode, Service/render work, prepare/resolve `validateRuntimeSource`, or output. Production constructors always use Go's monotonic clock and the finite constant; only package-private test constructors accept a fake clock, and no CLI flag, environment value, public config, zero/negative duration, or unbounded sentinel can replace it. The default is measurement-backed rather than a claimed universal SLA and may change only with retained production measurements.
- Bind returns an operation-scoped `RuntimeWorktree` plus close ownership. `runRuntime` defers close and never passes a path or eagerly constructed public Service into the factory; binding failure cannot fall back or reopen.
- Attach/publish/acknowledge receive bounded frames as applicable; prepare/resolve receive private `runtimePatchPayload`, run `zsh -n` over its exact source with `opCtx`, and atomically write that complete buffered transition program to private stdout after validation. Per Plan 07-08, the program ends with emitter-owned global assignments `ZP_WORKTREE_REPLY_PROTOCOL=1`, canonical decimal `ZP_WORKTREE_REPLY_REVISION` and `ZP_WORKTREE_REPLY_TOKEN`, 64-lowercase-hex `ZP_WORKTREE_REPLY_FINGERPRINT`, and `ZP_WORKTREE_REPLY_COMPLETE=1`. A successful at-head no-op is exact zero stdout; a pending target with zero mutations still carries the metadata-only program. No operation accepts caller paths/refs/Git argv/shell commands/output destinations.
- The sourced loader defines exact `zsh-pro sync` and `zsh-pro sync --resolve shared` parent-shell operations; direct binary sync remains rejected. It scopes only `ZSHPRO_SHELL_ID` (never the capability) to `command zsh-pro` for public binary verbs. Every private invocation emits the versioned control envelope directly to stdin with zsh builtins under byte-counting locale, private call-local variables, and locally disabled/restored xtrace/history; the raw capability is never in argv or exported environment. The stable non-exported capability marker is the only long-lived shell copy required for re-source, while all call-local credential/control/frame copies and response buffers are cleared in `always` on success, failure, signal, and timeout. Checkout/reset must complete current-shell capture/publication with no unpublished remainder before invoking the binary, then use the same bounded parent sync pipeline; all other supported/unknown verbs are forwarded without recursion.
- A zsh-native transition guard starts before prepare and remains active through eval, reply parsing, replacement-reverse update, fresh capture/match, and acknowledgement. Before invocation and in an `always` cleanup, unset all five reply globals and clear the source/control/call-local credential buffers. Exact zero stdout with all reply globals absent is the only no-op and starts no eval/watchdog. For a nonempty response, protected eval must leave protocol/complete exactly `1`, revision/token in canonical non-overflow unsigned-decimal form, and fingerprint as 64 lowercase hex; missing, malformed, partial, stale, or unexpected reply state suppresses acknowledgement and leaves pending/recovery truth intact. Use only built-in/module facilities, a locally scoped/restored trap, and a short-lived cleanup-owned watchdog process at safe boundaries; it must not overwrite user traps, survive as a background watcher, or run for a no-op. The installed production loader hard-wires the finite 250 ms budget and has no environment/config clock override; an unexported hook renderer/test harness may inject deterministic monotonic ticks solely into tests. Fake cumulative time must admit exactly 249 ms and time out at 251 ms even when the last increment occurs during acknowledgement. Real-time stage tests use short 25 ms success stalls and clearly-over-budget 500 ms failure stalls rather than exact-boundary sleeps. Timeout before/during mutation retains the old acknowledged revision plus pending/recovery/reverse evidence, suppresses acknowledgement, clears reply/source/control locals, and returns control to the prompt. Operation-ID receipts make a timed-out/lost-response acknowledgement replay-safe; retry Prepare/Resolve returns the same pending metadata until a valid post-apply acknowledgement commits.
</interfaces>
</context>

## Artifacts this phase produces

- `core/cli/runtime.go` adds `DefaultWorktreeTransitionBudget`, the sealed production/injectable-test monotonic budget, exactly attach/publish/prepare/acknowledge/resolve, one `ZPWT` version-1 stdin decoder capped at 2,101,248 bytes/10,016 records, post-auth crypto-random shell ID/capability allocation and bounded private response, strict credential forwarding to Plan 07-03 Service, deadline-before-auth/bind/one-shot-I/O, operation-scoped adapter closure, and buffered exact whole-transition-program zsh validation/output under the same context.
- `core/shell/zsh/hook.go` adds `_zp_worktree_ensure_attached`, retained initial semantic snapshot storage, stable non-exported shell ID/capability markers, builtin-only stdin framing with call-local credential/control cleanup and xtrace/history restoration, `_zp_worktree_publish`, `_zp_worktree_pull`, `_zp_worktree_sync`, `_zp_worktree_resolve_shared`, exact emitter-reply global clearing/parsing, a whole-transition watchdog/recovery wrapper plus deterministic test timing seam, capture/effective-config helpers, applied/pending/behind/conflict/error/guard globals, and cooperative hook registration without a source-time subprocess.
- Public sourced-loader `zsh-pro()` dispatches the exact installed syntax: `sync`, `sync --resolve shared`, checkout/reset with parent-shell convergence, and all other verbs through scoped-ID `command zsh-pro`; exact `ZSHPRO_AUTO_APPLY` override. Legacy `checkout`, `activate`, `status`, and `list` delegate into this dispatcher and cannot execute the former per-terminal branch path.
- `core/cmd/zsh-pro/main.go` configures Plan 07-08 RuntimeWorktreeFactory constructors/policy; Plan 07-09 runtime code binds an operation-scoped Service only after validation and RuntimeRoot authentication.
- Tests include zero-subprocess loader source, descriptor-authenticated 256-bit ID/capability allocation, one-shot bounded stdin transport, blocked-helper cmdline/environ absence, ID-only/wrong-pair cross-shell replay rejection, frame partial/trailing/oversize/cleanup behavior, Service-owned verifier integration, collision/spoof/re-source stability, lazy attach/no pre-attach publish/no-delta boundary, exact public installed syntax/legacy rewiring/shell-ID transport, checkout/reset failed-publish preservation, exact reply-variable grammar/order, at-head zero-byte and pending metadata-only cases, malformed/partial/stale reply suppression, reply/source cleanup, post-apply acknowledgement, replay after ack response loss, fake cumulative 249/251 ms boundary proof plus 25/500 ms active fail-open stalls at every stage, leakage canaries, real-provider inherited/post-attach admission, already-locally-acknowledged same-key/dirty-gap resolution, line-finish timing, foreground/fail-open, helper loss, hostile shell options/traps, and canonical-authority composition without pointer-identity assumptions.
- No Git hot-path call, child-zsh capture, background watcher, persistent sourced patch, dependency, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze five bounded credential-forwarding runtime operations and exact reply output</name>
  <files>core/cli/runtime.go, core/cli/runtime_test.go</files>
  <read_first>
    - core/cli/runtime.go: current allowlist, integer outer timeout, root authentication, bounded stdin/stdout, and safe error mapping
    - core/cli/runtime_test.go: current typed-nil, descriptor replacement, deadline, and source-redaction regressions
    - core/cli/emitter.go: RuntimeWorktreeFactory exact typed methods
    - core/cli/runtime_root_unix.go: authenticated retained descriptors and close ownership
    - core/model/worktree.go: exact args/revisions/tokens/frame results
    - core/worktree/service.go: Attach/Publish/Prepare/Acknowledge/ResolveShared semantics
  </read_first>
  <action>
    Preserve current descriptor-close and typed-nil hardening. Addresses review concerns CodeRabbit 3/7/9, H3, M1, both revision-checker gaps, and the adjusted security-runtime blocker. Keep exactly Attach/Publish/Prepare/Acknowledge/Resolve, but reduce argv to the non-authorizing operation tag plus the existing 1-99 second outer watchdog. Define `WorktreeRuntimeFrameVersion = 1`, `MaxRuntimeWorktreeFrameBytes = model.MaxSnapshotBytes + 4096` (2,101,248 bytes), and `MaxRuntimeWorktreeFrameRecords = model.MaxSnapshotRecords + 16` (10,016 records). Decode one binary-safe length-framed stdin envelope containing magic/version, the duplicated operation tag, attach allocation/commit mode, shell ID and canonical lowercase 64-hex capability when required, stable operation ID, canonical revision/token fields, record count, operation records, and an explicit final record. Read stdin exactly once through a plus-one bounded reader; accept only the declared records followed immediately by EOF. Reject a mismatched operation tag, duplicate/unknown/out-of-order control fields, partial length/payload, early EOF, missing final record, trailing bytes, excessive bytes/records, or inappropriate credential/payload for the operation, and close reader/pipe ownership on every exit. There is no credential/payload fallback to argv, environment, pathname, or a second read.

    Attach allocation is the only credential-less frame mode. It runs only after RuntimeRoot authentication, uses `crypto/rand` for independent 256-bit shell ID/capability values, changes no state, and writes the pair only through the separate bounded private credential response. Attach commit and every later private operation receive the exact credential only inside stdin, decode it into Plan 07-03 `ShellCredential`, and forward it unchanged. Runtime may validate encoding/size but must not hash/compare/cache the capability, inspect receipts, or claim authorization; Plan 07-03 Service alone derives/persists/verifies the verifier under lock before receipts/transitions. Public status receives ID only. Caller-chosen allocation, malformed control data, shell ID without its capability, reallocation for a valid attached pair, and invalid frame shape fail before Service; valid-shaped wrong capability, collision, cross-shell replay, and receipt replay are resolved by Service under lock.

    Add an unexported `transitionClock` and `transitionBudget` in runtime.go. The production constructor always uses `time.Now`'s monotonic component and `DefaultWorktreeTransitionBudget = 250*time.Millisecond`; only package-private tests can inject a fake monotonic clock. Expose no flag, environment variable, public config, zero/negative duration, or unbounded sentinel. Create the absolute budget immediately after argv validation and before root authentication, one-shot stdin reading, `RuntimeWorktreeFactory.Bind`, Service/render work, prepare/resolve `validateRuntimeSource`, or output; derive `opCtx` from its remaining finite duration and never reset it per stage. Authenticate RuntimeRoot under that context, then bind the descriptor-scoped adapter, defer exactly-once close, and forbid path reopen, eager public-Service reuse, singleton, or fallback authority. Add table tests that advance fake cumulative time through early and late stages: 249 ms total is admitted, 251 ms total times out, and a final increment during validation/output cannot receive a fresh budget. Use 25 ms versus 500 ms injected real stalls for active completion/cancellation/fail-open coverage; exact 249/251 assertions must contain no real sleep.

    For prepare/resolve, accept only the Plan 07-08 `runtimePatchPayload` obtained after successful frame decoding. `transition=false` requires zero source/metadata and writes exact zero stdout. `transition=true` requires the complete emitter-owned program and fixed metadata, runs `validateRuntimeSource(opCtx, payload.source)` over all bytes including the reply-assignment footer, and performs one all-or-nothing stdout write only after successful validation. Keep stdout empty on malformed input/payload, render/validator/deadline/write-preparation failure; do not extract or keep revision/token/fingerprint on an in-process control path because the parent must receive them from the emitted assignments after this process exits. Never serialize source, presented capability, verifier, arbitrary metadata, or frame bytes into errors/status/events. The outer seconds context remains a larger defense-in-depth watchdog only. Add at-head zero-byte, pending metadata-only, malformed/incomplete/duplicate footer, whole-buffer zsh-n, short-write/no-partial-output, frame 0/exact/max/plus-one, partial/trailing/duplicate/operation-mismatch, descriptor-close, fake cumulative 249/251 at early and late stages, generous real-stall fail-open, valid Service operation-ID replay after lost responses, and wrong/cross-shell credential zero-effect integration. Invalid or timed-out operations make zero unintended calls, retain the old acknowledged revision, and return value-free errors.
  </action>
  <acceptance_criteria>
    - Every private worktree operation, including authenticated binding and prepare/resolve zsh -n, is terminated by the same finite production-default budget.
    - All five operation names/arities are pinned; prepare remains distinct from pull/query and exact source remains private.
    - Shell ID/capability allocation is post-auth crypto-random and privately transported; Plan 07-03 Service, not runtime.go, performs every durable capability check under lock.
    - Prepare/resolve write either exact zero bytes for at-head or one complete emitter-owned transition program carrying parent-visible revision/token/fingerprint; no successful pending response strands metadata in-process.
    - Fake monotonic cumulative-time tests prove exact 249 ms admission and 251 ms cancellation at early and late stages; separate 25 ms/500 ms real stalls prove live fail-open behavior without an exact-boundary sleep.
    - The raw capability and operation payload are accepted only from one versioned stdin envelope with exact 2,101,248-byte/10,016-record caps, one-shot read, final-record/EOF enforcement, and no argv/environment/path fallback.
    - Runtime adapter construction happens after validation/authentication, closes once, and cannot reopen or fall back to another authority.
    - Outer integer syntax remains compatible but cannot extend internal work.
    - Arbitrary runtime paths/refs/commands and malformed args are rejected before auth/service.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/cli -run 'Test(RuntimeWorktree|RuntimeWorktreeFrame|RuntimeCredentialForwarding|RuntimeCapabilityTransport|RuntimeReplyOutput|RuntimeNoOp|RuntimeMetadataOnly|RuntimeWorktreeBindOrder|RuntimeWorktreeClose|WorktreeDeadline|WorktreeFakeClock|WorktreeValidatorDeadline|DeadlineBeforeAuth|RuntimeDescriptor|NoRuntimeReopen)' -count=1</automated>
  </verify>
  <done>The five-operation private runtime surface allocates credentials after authentication, accepts them only through one bounded fail-closed stdin protocol, delegates durable validation to Service, and returns an exact parent-consumable transition program under a deterministically tested finite default budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach lazily, route exact public commands, and converge at safe boundaries</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: bounded helper, protected eval, retained reverse, markers, fail-open wrappers, and install behavior
    - core/shell/zsh/hook_test.go: no-source-time-subprocess, timeout, fail-open, and function surface tests
    - core/cli/runtime.go: Task 1 exact operations/deadline
    - core/cmd/zsh-pro/main.go: Plan 07-07 public Service plus late-bound runtime constructor configuration over one canonical authority
    - core/cli/worktree.go: Plan 07-06 public workflow surface and ZSHPRO_SHELL_ID status context
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: attach-derived registry, precmd/line-finish, and ack order
  </read_first>
  <action>
    Preserve existing loader/runtime hardening. Loader source may define functions/globals and cooperatively register hooks, but must execute zero external commands, command substitutions, private runtime helpers, Git calls, child-zsh introspection, attach operations, or publications; add an explicit source-time process counter assertion. At the first safe `precmd`, `line-finish`, or sourced public-command boundary, `_zp_worktree_ensure_attached` captures one semantic initial snapshot with zsh builtins and retains it across retries. If no valid existing non-exported ID/capability pair exists, invoke authenticated private attach allocation with the credential-less version-1 stdin envelope, read its bounded private response with shell builtins under disabled/restored xtrace/history, retain the crypto-random pair, then send that pair plus the exact retained capture through attach-commit stdin; on success clear the frame cache/mark attached/return without publishing. Re-sourcing preserves the single valid non-exported pair and confirms it through a credential-bearing stdin frame whose Plan 07-03 Service validates the verifier under lock; malformed/spoofed/mismatched markers fail open without allocating authority under the caller's value. Every publish calls ensure-attached first and refuses while unattached, so initial attach is idempotent/no-delta and cannot be bypassed. Register namespaced precmd publish and pull-only line-finish through cooperative helpers with idempotent remove/re-add and recursion guard.

    Build every private request directly on helper stdin with zsh builtins in byte-counting locale; do not place the shell capability, operation ID, revision/token, capture payload, or patch source in command arguments or exported variables, and do not fall back if stdin framing fails. The stable non-exported marker is the only long-lived capability copy; each helper call uses private `typeset` locals for credential/control lengths and clears those locals, frame buffers, response buffers, and temporary descriptors in one `always` block on every exit. Scope and restore xtrace/history/options/traps around both writes and reads so neither the raw capability nor source enters xtrace, history, diagnostics, or public output. Add a blocking test seam that pauses a real helper after stdin acceptance but before Service invocation; while blocked, inspect `/proc/<pid>/cmdline` and `/proc/<pid>/environ` (or the platform process-inspector abstraction) and assert the capability canary appears in neither. Then invoke a sibling shell with the observed shell ID but no capability, and with the first shell's capability paired to the sibling ID, and prove Service rejection with zero receipt/state/event change. EOF, malformed, partial, oversized, and trailing-frame failures must still return control to the prompt.

    Define the exact sourced `zsh-pro()` dispatcher. It scopes `ZSHPRO_SHELL_ID="$ZP_WORKTREE_SHELL_ID"` only on `command zsh-pro` public child invocations and never exports/passes the capability outside stdin-fed private helpers. `zsh-pro sync` and `zsh-pro sync --resolve shared` call the parent-shell pipeline directly; other sync shapes and all direct binary sync attempts fail value-safely. Addresses review concern CodeRabbit 2/5: before `zsh-pro checkout <name>`, `zsh-pro checkout -b <name>`, or `zsh-pro reset --hard`, ensure attachment, capture fresh state, and complete private Publish. Proceed only if the result proves no timeout/conflict/unpublished remainder; otherwise leave the local frame/durable unpublished delta intact and make zero checkout/reset workflow calls. After a successful binary mutation, call the same parent-shell sync so the current shell applies the new shared revision. Status/diff/commit/branch/list/config/ingest and unknown verbs forward once through `command zsh-pro` with no function recursion; status receives the validated opaque ID from Plan 07-06. Replace legacy bare `checkout` and `activate` with delegates to `zsh-pro checkout`, bare `status` with `zsh-pro status`, and bare `list` with `zsh-pro list`; remove former Store.Current/_zp_switch/status paths. Keep deactivate parent-local and unable to redefine shared authority.

    Use one worktree apply sequence for auto, checkout/reset follow-up, explicit sync, and resolve. Addresses the complete-transition deadline and cross-process acknowledgement reviews: before each prepare/resolve invocation, unset `ZP_WORKTREE_REPLY_PROTOCOL`, `ZP_WORKTREE_REPLY_REVISION`, `ZP_WORKTREE_REPLY_TOKEN`, `ZP_WORKTREE_REPLY_FINGERPRINT`, and `ZP_WORKTREE_REPLY_COMPLETE`, and initialize the exact source/control buffers empty. Exact successful zero stdout with all reply globals absent is the at-head no-op: do not eval, start a watcher, capture, or acknowledge. For nonempty stdout, start one cleanup-owned watchdog before the stdin-fed private prepare/resolve call and keep its absolute production-default budget active through helper one-shot read/whole-buffer zsh-n, private buffered transport, protected eval, reply validation, replacement of `ZP_ACTIVE_REVERSE_FN`, fresh semantic capture/match, and stdin-fed idempotent acknowledgement. After eval require protocol/complete exactly `1`, canonical unsigned-decimal revision/token with no sign/whitespace/overflow/noncanonical leading zero, and a 64-character lowercase-hex fingerprint; compare the fresh managed snapshot fingerprint before calling Acknowledge with those exact values and a stable operation ID. A pending target with zero live mutations still follows this metadata-only eval/capture/ack path. Missing, partial, malformed, stale, or unexpected reply globals are a recovered value-free failure: never acknowledge, retain pending/conflict/reverse truth, and retry through Prepare/Resolve. In one `always` cleanup, clear source/control/call-local credential/response buffers and unset all five reply globals on success, no-op, malformed output, eval failure, timeout, signal, and acknowledgement response loss; disable/restore xtrace/history protections so metadata/capability/source cannot leak. Use a locally scoped/restored trap and zsh builtin/module timing so user traps/options are unchanged; always reap the short-lived watchdog. On timeout before mutation, keep old state. On timeout during/after mutation, retain the old acknowledged revision plus pending/recovery/reverse ownership, do not newly acknowledge, compensate when safe within the bounded recovery contract, and let the same acknowledgement operation-ID replay determine whether a lost response actually committed. Do not call the legacy seconds-budget validator again.

    Factor budget accounting behind an unexported hook-render/test seam whose production route is sealed to the finite 250 ms absolute guard and whose test route accepts deterministic monotonic ticks only from Go package tests, never a user environment/config setting. Prove cumulative fake elapsed time of exactly 249 ms is admitted and 251 ms times out when consumed during prepare, eval, reverse replacement, fresh capture, or acknowledgement; exact boundary tests use no real sleep. Retain active real-time fail-open tests at authentication, bind, helper read/execute, private transport, protected eval, reverse replacement, fresh capture, and acknowledgement with 25 ms success stalls and 500 ms failure stalls, asserting the same parent prompt remains usable and the old acknowledged revision remains on timeout. `sync --resolve shared` applies authoritative changes that replace the durable unpublished loser even when capture baseline already contains it; failed prepare/validation/transport/eval/reply-parse/reverse replacement/capture/match/ack retains conflict and causal baselines. Auto config uses persisted default plus exact shell override. Configure RuntimeWorktreeFactory in main with constructors/provider policy only; it forwards credentials into Plan 07-03 Service and owns no validator. Add malformed/truncated/duplicate/stale reply, exact no-op, pending metadata-only, post-apply acknowledgement, cleanup/leakage, acknowledgement replay, stdin-frame/proc-inspection/replay, and deterministic/real-time deadline tests alongside zero-process source, capability allocation/collision/spoof/re-source, failed-publish checkout/reset, exact installed syntax, legacy retirement, ID-only public status, inherited/post-attach admission, already-acknowledged same-key and dirty-gap resolution, next-command alias timing, later-head/lost-ack replay, foreground non-interference, missing helper, hostile existing traps/options, ERR_EXIT/ERR_RETURN, and canonical authority.
  </action>
  <acceptance_criteria>
    - Sourcing the loader starts no process; one retained snapshot attaches at the first safe boundary, and no shell can publish on or before that no-delta attachment succeeds.
    - Exact installed `zsh-pro ...` syntax routes every public verb once, checkout/reset mutate the parent through sync, and legacy functions cannot use per-terminal bypass behavior.
    - Status receives only a scoped validated opaque shell ID; it cannot influence durable branch/root/revision authority.
    - A private crypto-random ID/capability pair persists across re-source; the capability is absent from argv/environment/public output, travels only inside bounded stdin frames, and rejects ID-only, wrong-pair, collision, and cross-shell replay.
    - Durable spoof/replay rejection is exercised through Plan 07-03 Service; loader/runtime only validate encoding and privately transport/forward the presented credential.
    - Checkout/reset make zero workflow calls after a failed/conflicted publication and retain the unpublished delta.
    - Inherited identities remain excluded while safe identities created after attach publish through the real provider policy.
    - Same-key and dirty history-gap conflicts resolve only through explicit shared adoption and verified acknowledgement.
    - Every failure leaves applied revision/conflict truth unchanged and the prompt/entered command usable.
    - One finite parent transition budget covers prepare through acknowledgement; fake monotonic time proves exact cumulative 249/251 behavior including late-stage consumption, while generous real stalls prove active fail-open termination without flaky boundary sleeps.
    - Exact zero-byte no-op, pending metadata-only, malformed/partial reply, cleanup, post-apply acknowledgement, and lost-ack replay tests prove revision/token/fingerprint survive the helper exit without leaking or falsely acknowledging.
    - Hooks run only at prescribed safe boundaries and invoke neither Git nor child-zsh introspection; worktree source is zsh-n validated before transport under the same internal deadline.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh ./core/cmd/zsh-pro -run 'Test(WorktreeLoaderSourceZeroProcess|WorktreeLazyAttach|WorktreePublicDispatcher|WorktreeLegacyDelegates|WorktreeShellIDTransport|WorktreeCapabilityStdin|WorktreeCapabilityProcessVisibility|WorktreePrivateFrameCleanup|WorktreeReplyProtocol|WorktreeReplyCleanup|WorktreeNoOp|WorktreeMetadataOnly|WorktreeTransitionFakeClock|WorktreeHook|Precmd|LineFinish|AutoApply|ExplicitSync|ResolveShared|Acknowledge|AcknowledgeReplay|FailOpen|Foreground|MainWorktree|CanonicalWorktreeAuthority)' -count=1</automated>
  </verify>
  <done>Independent shells source with zero subprocesses, attach before publish, keep capabilities out of argv/environment through one fail-closed stdin protocol, route exact public commands through shared truth, carry acknowledgement metadata across the helper exit, converge/resolve only through the parent pipeline, and fail open under one deterministically verified finite whole-transition budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hook -> runtime helper stdin | Prompt path crosses process, descriptor, framed-input, credential-secrecy, and time boundaries |
| sourced zsh-pro dispatcher -> binary/public workflow | Parent-shell mutation and opaque shell context cross a recursion/authority boundary |
| initial shell -> Attach | Ambient current state initializes admission/baseline authority |
| generated bytes -> eval/ack | Durable changes become parent-shell state and acknowledged truth |
| helper stdout -> parent reply globals -> Acknowledge | Revision/token/fingerprint cross a process boundary and must be complete, canonical, short-lived, and bound to the freshly captured state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-35 | Denial of Service | runtime helpers/validator/parent transition | high | mitigate | Sealed finite 250 ms default before auth/bind/one-shot I/O/service/render plus a cleanup-owned parent-shell watchdog through eval/reverse replacement/capture/ack; fake cumulative 249/251 proofs, generous 25/500 ms active stalls, recursion guard, old-ack retention, and fail-open recovery in Tasks 1-2. |
| T-07-36 | Information Disclosure | attach/publish/source/errors | high | mitigate | Production admission before persistence, in-memory source, value-free errors/status, and real-provider canary tests in Task 2. |
| T-07-37 | Tampering | first publication | high | mitigate | Zero-process source, retained first-safe-boundary snapshot, mandatory idempotent no-delta attach, publish refusal while unattached, durable initial baseline/exclusions, and tests. |
| T-07-38 | Tampering | resolution/apply/reply/ack | high | mitigate | Exact emitter-owned protocol/revision/token/fingerprint/complete assignments, protected eval, canonical reply validation, fresh fingerprint match before ack, always-cleanup, and malformed/no-op/timeout/replay tests. |
| T-07-39 | Tampering | foreground/process-local state | high | mitigate | precmd publish, line-finish pull, explicit sync only, no watcher/model kinds, and foreground tests. |
| T-07-40 | Spoofing | public dispatcher/private operation/service | high | mitigate | Exact public/private allowlists and arities, scoped validated shell ID, command-recursion prevention, post-auth late binding to canonical authority, no reopen/fallback, and zero-call invalid tests. |
| T-07-40A | Spoofing | shell acknowledgement authority | high | mitigate | Post-auth crypto-random ID/capability allocation, versioned stdin-only credential transport, blocked-helper cmdline/environ absence, Plan 07-03 verifier-at-rest and under-lock validation before receipts/mutations, and ID-only/wrong-pair/collision/cross-shell/re-source tests across Tasks 1-2. |
| T-07-40B | Tampering | checkout/reset unpublished local state | high | mitigate | Mandatory fresh capture/publish receipt before mutation, zero-call failure path, and retained-delta tests. |
| T-07-40C | Information Disclosure | private reply/source/credential transport | high | mitigate | No capability in argv/environment, exact bounded stdin frames, private locals, xtrace/history protection, exact namespaced reply variables, always-block local/buffer/descriptor cleanup, `/proc` canaries, and timeout/malformed cleanup tests in Task 2. |

ASVS Level 1 applies; every high-severity deadline, attachment, privacy, resolution, foreground, and authority gate is blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/worktree ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro -count=1`
- `GOTOOLCHAIN=local go vet ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro`
- `GOTOOLCHAIN=local go build ./...`
- Structural/tests confirm the finite default begins before root/bind/one-shot frame I/O/service/render/zsh-n, fake monotonic cumulative 249/251 behavior includes late-stage consumption, the parent watchdog spans eval/reverse replacement/capture/ack, raw capability is absent from helper argv/environment, loader source spawns zero processes, legacy functions delegate to the sourced dispatcher, and hook paths contain no Git/child-zsh introspection.
</verification>

<success_criteria>
- Every shell sources with zero subprocesses and has an executable lazy no-delta attach lifecycle before first publication.
- Exact installed public syntax, pre-publish-protected checkout/reset parent mutation, default/manual sync, and explicit shared conflict resolution use one validated apply/reverse/fresh-snapshot/ack path; legacy bypasses are retired.
- Service, validator, parent eval, reverse replacement, fresh capture, and acknowledgement share the sealed finite default with deterministic exact-boundary and generous real-time proof; runtime/shell authority binds only after authentication, and every failure retains the old acknowledged revision while remaining visible, replay-safe, and fail-open.
- Runtime owns only post-auth credential allocation/private transport and exact reply delivery; Plan 07-03 owns verifier persistence and under-lock authorization, while emitter-owned reply assignments deliver every acknowledgement input across the helper exit.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-09-SUMMARY.md` when done.
</output>
