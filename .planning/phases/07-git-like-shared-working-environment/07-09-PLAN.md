---
phase: 07-git-like-shared-working-environment
plan: 09
type: execute
wave: 7
depends_on: ["07-03", "07-07", "07-08"]
files_modified:
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-02, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "[WORK-02] Loader startup performs explicit idempotent attach with the first validated snapshot before precmd publication; attach stores baseline/applied/exclusions and publishes no delta."
    - "[WORK-02] Real-provider behavior keeps identities inherited at attach excluded while safe identities created after attach can be admitted and published."
    - "[SYNC-01] `precmd` publishes only; `zle-line-finish` pulls only; explicit `zsh-pro sync` uses the same prepare/apply/verify/ack path."
    - "[SYNC-01] `zsh-pro sync --resolve shared` deliberately resolves same-key and dirty history-gap conflicts by adopting shared state, discarding only this shell's unacknowledged local delta, and clearing conflict only after verify/ack."
    - "[SYNC-01] Persisted default plus exact shell-local `ZSHPRO_AUTO_APPLY=true|false` override is visible; invalid override records an error and follows persisted default."
    - "[SYNC-02] Every attach/publish/prepare/ack/resolve runtime operation creates a fixed internal 250 ms context before authentication, frame I/O, or service calls; the existing 1-99 second outer watchdog is defense in depth only."
    - "[SYNC-02] 249 ms injected work completes, 251 ms work is terminated value-safely, and all failures leave the prompt/entered command usable with old applied revision/conflict evidence."
    - "Apply order is prepare -> exact zsh -n -> protected eval -> replacement reverse -> fresh snapshot match -> acknowledge; foreground/process-local state is never mutated asynchronously."
  artifacts:
    - path: "core/cli/runtime.go"
      provides: "Private attach/publish/prepare/ack/resolve allowlist with fixed internal deadline"
      contains: "const WorktreeHelperDeadline"
    - path: "core/shell/zsh/hook.go"
      provides: "Composable attach, precmd publish, line-finish pull, explicit sync, and shared-resolution lifecycle"
      contains: "_zp_worktree_attach"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "RuntimeWorktreeFactory bound to the one exact Service from Plan 07-07"
      contains: "RuntimeWorktreeFactory"
  key_links:
    - from: "loader source/activation"
      to: "Service.Attach"
      via: "validated initial current-shell frame and fixed 250 ms runtime context; no publication delta"
      pattern: "_zp_worktree_attach"
    - from: "precmd"
      to: "Service.Publish"
      via: "attached shell ID, acknowledged revision, bounded framed stdin"
      pattern: "_zp_worktree_publish"
    - from: "sync --resolve shared"
      to: "ResolveShared then Acknowledge"
      via: "pending resolution token, authoritative patch, replacement reverse, fresh snapshot"
      pattern: "ResolutionToken"
    - from: "runRuntime worktree operation"
      to: "authenticated factory/service"
      via: "context.WithTimeout(parent, 250*time.Millisecond) before auth/I/O"
      pattern: "WorktreeHelperDeadline"
  prohibitions:
    - statement: "MUST NOT mutate a foreground command or synchronize PWD, jobs, command buffers, history, process trees, or terminal UI as though independent shells were one process."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT make a helper, lock, state, renderer, validator, eval, or acknowledgement failure prevent the user's next prompt or entered command."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT hide that auto-apply is disabled, invalidly overridden, behind, or conflicted while presenting a shell as converged."
      category: transparency
      status: unresolved
      flagged: true
---

<objective>
Install the complete bounded attachment, publication, convergence, and conflict-resolution lifecycle in the real sourced loader.

Purpose: Make independent terminals safely converge and deliberately recover conflicts without inherited-state capture, foreground interference, prompt DoS, false acknowledgement, or stale reverse ownership.
Output: Fixed-deadline private runtime commands, composable hooks, public sync/resolve commands, and exact runtime composition.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-03-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-07-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-08-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/cli/runtime.go
@core/shell/zsh/hook.go
@core/cmd/zsh-pro/main.go

<interfaces>
- Private syntax keeps the existing integer outer timeout: `runtime worktree <attach|publish|prepare|ack|resolve> <seconds> -- ...`.
- Every worktree operation immediately derives `opCtx` with `WorktreeHelperDeadline = 250*time.Millisecond` from the already bounded outer parent before authentication, stdin read/decode, Service, rendering, or output.
- Attach/publish/ack receive bounded frames as applicable; prepare/resolve emit exact source/revision/token; no operation accepts caller paths/refs/Git argv/shell commands/output destinations.
- Public loader functions accept `zsh-pro sync` and `zsh-pro sync --resolve shared`; all other sync flags fail value-safely and return control to the interactive shell.
</interfaces>
</context>

## Artifacts this phase produces

- `core/cli/runtime.go` adds `WorktreeHelperDeadline`, exact private operations attach/publish/prepare/ack/resolve, strict validation, internal deadline-before-auth/I/O, and buffered value-safe output.
- `core/shell/zsh/hook.go` adds `_zp_worktree_attach`, `_zp_worktree_publish`, `_zp_worktree_pull`, `_zp_worktree_sync`, `_zp_worktree_resolve_shared`, capture/effective-config helpers, opaque shell ID, applied/pending/behind/conflict/error/guard globals, and cooperative hook registration.
- Public sourced-loader `zsh-pro sync` and `zsh-pro sync --resolve shared`; exact `ZSHPRO_AUTO_APPLY` override.
- `core/cmd/zsh-pro/main.go` injects Plan 07-08 RuntimeWorktreeFactory bound to the exact Plan 07-07 Service and concrete zsh seams.
- Tests include 249/251 ms stalls for each operation class, no pre-attach publish, no-delta attach, real-provider inherited/post-attach admission, same-key/dirty-gap resolution, ack order, line-finish timing, foreground/fail-open, helper loss, hostile shell options, and exact composition identity.
- No Git hot-path call, child-zsh capture, background watcher, persistent sourced patch, dependency, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed-250-ms private attach, publish, prepare, acknowledge, and resolve commands</name>
  <files>core/cli/runtime.go, core/cli/runtime_test.go</files>
  <read_first>
    - core/cli/runtime.go: current allowlist, integer outer timeout, root authentication, bounded stdin/stdout, and safe error mapping
    - core/cli/runtime_test.go: current typed-nil, descriptor replacement, deadline, and source-redaction regressions
    - core/cli/emitter.go: RuntimeWorktreeFactory exact typed methods
    - core/model/worktree.go: exact args/revisions/tokens/frame results
    - core/worktree/service.go: Attach/Publish/Prepare/Acknowledge/ResolveShared semantics
  </read_first>
  <action>
    Preserve current descriptor-close and typed-nil hardening. Extend the private allowlist with exactly attach/publish/prepare/ack/resolve. Parse operation, outer 1-99 second watchdog, delimiter, opaque shell ID, strict unsigned revision/token fields, and bounded frame expectations before dependency access. Immediately after syntactic validation, derive `opCtx := context.WithTimeout(parent, WorktreeHelperDeadline)` with the constant fixed at 250 ms; use opCtx for root authentication, stdin/frame decoding, Service, rendering, and buffered response. The outer seconds context remains only a larger defense-in-depth process watchdog and never defines prompt budget. Add injectable blocking seams/tests proving 249 ms completes and 251 ms is canceled for attach, publication, preparation/render, and acknowledgement/resolution; assert timeout begins before authentication/I/O. Invalid or timed-out operations make zero unintended calls and return value-free errors.
  </action>
  <acceptance_criteria>
    - Every private worktree operation is terminated by the same real 250 ms internal context.
    - 249/251 ms tests prove boundary behavior, not just elapsed measurement.
    - Outer integer syntax remains compatible but cannot extend internal work.
    - Arbitrary runtime paths/refs/commands and malformed args are rejected before auth/service.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/cli -run 'Test(RuntimeWorktree|WorktreeDeadline|DeadlineBeforeAuth|RuntimeDescriptor)' -count=1</automated>
  </verify>
  <done>The private runtime surface is exact, authenticated, value-safe, and actually terminated at 250 ms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach first, converge at safe boundaries, and resolve conflicts deliberately</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: bounded helper, protected eval, retained reverse, markers, fail-open wrappers, and install behavior
    - core/shell/zsh/hook_test.go: no-source-time-subprocess, timeout, fail-open, and function surface tests
    - core/cli/runtime.go: Task 1 exact operations/deadline
    - core/cmd/zsh-pro/main.go: one exact Service/Provider graph from Plan 07-07
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: attach-derived registry, precmd/line-finish, and ack order
  </read_first>
  <action>
    Preserve existing loader/runtime hardening. Generate one opaque shell ID, capture the newly applied/current managed shell once, and call private attach before installing/enabling publication; exact re-source attach is idempotent and emits no delta. If attach fails, hooks remain fail-open but publish refuses until attach succeeds. Register namespaced `precmd` publish and pull-only `line-finish` through cooperative helpers with idempotent remove/re-add and recursion guard. Implement one prepare -> exact zsh -n -> protected eval -> replacement `ZP_ACTIVE_REVERSE_FN` -> fresh capture/target match -> ack sequence used by auto and `zsh-pro sync`. Implement `sync --resolve shared` by calling resolve prepare with the fresh snapshot, applying only authoritative changes that replace this shell's unacknowledged local delta, then acknowledging the exact resolution token; failed render/eval/match/ack retains conflict and old baseline. Auto config uses persisted default plus exact shell override. Wire RuntimeWorktreeFactory to the exact existing Service/provider in main. Add fake-binary plus real-provider tests for inherited exclusion/post-attach admission, same-key and dirty-gap resolution, next-command alias timing, later-head race, foreground non-interference, no-op repeat, missing helper, timeout, ERR_EXIT/ERR_RETURN, and exact service identity.
  </action>
  <acceptance_criteria>
    - No shell can publish before its initial no-delta attachment succeeds.
    - Inherited identities remain excluded while safe identities created after attach publish through the real provider policy.
    - Same-key and dirty history-gap conflicts resolve only through explicit shared adoption and verified acknowledgement.
    - Every failure leaves applied revision/conflict truth unchanged and the prompt/entered command usable.
    - Hooks run only at prescribed safe boundaries and invoke neither Git nor child-zsh introspection.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh ./core/cmd/zsh-pro -run 'Test(WorktreeAttach|WorktreeHook|Precmd|LineFinish|AutoApply|ExplicitSync|ResolveShared|Acknowledge|FailOpen|Foreground|MainWorktree)' -count=1</automated>
  </verify>
  <done>Independent shells attach safely, publish/pull only at safe boundaries, deliberately resolve conflicts, and fail open under a real 250 ms deadline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hook -> runtime helper | Prompt path crosses process, descriptor, and time boundaries |
| initial shell -> Attach | Ambient current state initializes admission/baseline authority |
| generated bytes -> eval/ack | Durable changes become parent-shell state and acknowledged truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-35 | Denial of Service | runtime helpers/hooks | high | mitigate | Fixed internal 250 ms context before auth/I/O, 249/251 cancellation tests, recursion guard, and fail-open wrappers in Tasks 1-2. |
| T-07-36 | Information Disclosure | attach/publish/source/errors | high | mitigate | Production admission before persistence, in-memory source, value-free errors/status, and real-provider canary tests in Task 2. |
| T-07-37 | Tampering | first publication | high | mitigate | Mandatory idempotent no-delta attach, publish refusal while unattached, durable initial baseline/exclusions, and tests. |
| T-07-38 | Tampering | resolution/apply/ack | high | mitigate | Exact token, shared-only prepare, reverse replacement/fresh match before ack, and failure-retains-conflict tests. |
| T-07-39 | Tampering | foreground/process-local state | high | mitigate | precmd publish, line-finish pull, explicit sync only, no watcher/model kinds, and foreground tests. |
| T-07-40 | Spoofing | private operation/service | high | mitigate | Exact allowlist/arity/IDs/revisions, authenticated factory, one exact Service, and zero-call invalid tests. |

ASVS Level 1 applies; every high-severity deadline, attachment, privacy, resolution, foreground, and authority gate is blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/worktree ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro -count=1`
- `GOTOOLCHAIN=local go vet ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro`
- `GOTOOLCHAIN=local go build ./...`
- Structural scan confirms fixed 250 ms internal context precedes root/I/O/service and hook paths contain no Git/child-zsh invocation.
</verification>

<success_criteria>
- Every shell has an executable no-delta attach lifecycle before first publication.
- Default/manual sync and explicit shared conflict resolution use one validate/apply/reverse/fresh-snapshot/ack path.
- Prompt helper stalls are terminated at 250 ms and every failure remains visible and fail-open.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-09-SUMMARY.md` when done.
</output>
