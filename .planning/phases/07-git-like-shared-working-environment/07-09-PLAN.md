---
phase: 07-git-like-shared-working-environment
plan: 09
type: execute
wave: 7
depends_on: ["07-03", "07-07", "07-08"]
files_modified:
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-02, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "[WORK-02] Sourcing the loader spawns zero subprocesses. The first safe hook/public-command boundary captures and retains one initial semantic snapshot, performs idempotent attach, and returns without publication; every later publication is gated on that successful no-delta attach."
    - "[WORK-02] Real-provider behavior keeps identities inherited at attach excluded while safe identities created after attach can be admitted and published."
    - "[SYNC-01] `precmd` publishes only; `zle-line-finish` pulls only; explicit `zsh-pro sync` uses the same prepare/apply/verify/ack path."
    - "[SYNC-01] `zsh-pro sync --resolve shared` deliberately resolves same-key and dirty history-gap conflicts by adopting shared state, discarding only this shell's unacknowledged local delta, and clearing conflict only after verify/ack."
    - "[SYNC-01] The installed sourced `zsh-pro()` dispatcher is the exact public route: sync/resolve and checkout/reset parent mutation use the shared apply pipeline, other verbs use `command zsh-pro`, and legacy checkout/activate/status/list functions cannot bypass shared workflow truth."
    - "[SYNC-01] Persisted default plus exact shell-local `ZSHPRO_AUTO_APPLY=true|false` override is visible; invalid override records an error and follows persisted default."
    - "[SYNC-02] Every attach/publish/prepare/ack/resolve runtime operation creates a fixed internal 250 ms context before authentication, descriptor binding, frame I/O, service/render work, or prepare/resolve zsh -n validation; the existing 1-99 second outer watchdog is defense in depth only."
    - "[SYNC-02] Active 249/251 ms stalls prove both service work and the separate zsh validator are terminated value-safely, and all failures leave the prompt/entered command usable with old applied revision/conflict evidence."
    - "Apply order is prepare/render -> exact zsh -n inside the same 250 ms runtime operation -> buffered transport -> protected eval -> replacement reverse -> fresh snapshot match -> acknowledge; foreground/process-local state is never mutated asynchronously."
  artifacts:
    - path: "core/cli/runtime.go"
      provides: "Private attach/publish/prepare/ack/resolve allowlist with validation-first late binding and one fixed internal deadline through prepare/resolve zsh -n"
      contains: "const WorktreeHelperDeadline"
    - path: "core/shell/zsh/hook.go"
      provides: "Zero-subprocess source, lazy attach, exact sourced zsh-pro dispatcher, precmd publish, line-finish pull, explicit sync, and shared-resolution lifecycle"
      contains: "zsh-pro()"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "RuntimeWorktreeFactory configured for post-auth operation-scoped binding to the canonical authority from Plan 07-07"
      contains: "RuntimeWorktreeFactory"
  key_links:
    - from: "first safe hook/public-command boundary"
      to: "Service.Attach"
      via: "one retained pure-zsh initial snapshot, post-auth descriptor-bound adapter, fixed 250 ms context, and no publication on the attachment boundary"
      pattern: "_zp_worktree_ensure_attached"
    - from: "precmd"
      to: "Service.Publish"
      via: "attached shell ID, acknowledged revision, bounded framed stdin"
      pattern: "_zp_worktree_publish"
    - from: "sync --resolve shared"
      to: "ResolveShared then Acknowledge"
      via: "pending resolution token, authoritative patch, replacement reverse, fresh snapshot"
      pattern: "ResolutionToken"
    - from: "sourced `zsh-pro()`"
      to: "public WorktreeWorkflow and parent-shell sync pipeline"
      via: "scoped ZSHPRO_SHELL_ID plus `command zsh-pro` for binary verbs; checkout/reset sync the parent after shared mutation; sync/resolve never bypass the sourced path"
      pattern: "command zsh-pro"
    - from: "runRuntime worktree operation"
      to: "authenticated RuntimeWorktreeFactory binding/service/validator"
      via: "context.WithTimeout(parent, 250*time.Millisecond) before auth/bind/I/O/service/render and exact-buffer zsh -n"
      pattern: "WorktreeHelperDeadline"
  prohibitions:
    - statement: "MUST NOT mutate a foreground command or synchronize PWD, jobs, command buffers, history, process trees, or terminal UI as though independent shells were one process."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT make a helper, lock, state, renderer, validator, eval, or acknowledgement failure prevent the user's next prompt or entered command."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT hide that auto-apply is disabled, invalidly overridden, behind, or conflicted while presenting a shell as converged."
      category: transparency
      status: unresolved
      flagged: true
---

<objective>
Install the complete bounded lazy-attachment, exact-public-routing, publication, convergence, and conflict-resolution lifecycle in the real sourced loader.

Purpose: Make independent terminals safely converge and deliberately recover conflicts without inherited-state capture, foreground interference, prompt DoS, false acknowledgement, or stale reverse ownership.
Output: Fixed-deadline private runtime commands including validation, zero-subprocess lazy loader hooks, exact sourced public dispatch, and late-bound runtime composition.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-03-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-07-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-08-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/cli/runtime.go
@core/shell/zsh/hook.go
@core/cmd/zsh-pro/main.go

<interfaces>
- Private syntax keeps the existing integer outer timeout: `runtime worktree <attach|publish|prepare|ack|resolve> <seconds> -- ...`.
- After full syntactic validation, every worktree operation derives `opCtx` with `WorktreeHelperDeadline = 250*time.Millisecond` from the already bounded outer parent before root authentication, `RuntimeWorktreeFactory.Bind`, stdin read/decode, Service/render work, prepare/resolve `validateRuntimeSource`, or output.
- Bind returns an operation-scoped `RuntimeWorktree` plus close ownership. `runRuntime` defers close and never passes a path or eagerly constructed public Service into the factory; binding failure cannot fall back or reopen.
- Attach/publish/ack receive bounded frames as applicable; prepare/resolve buffer exact source/revision/token, run `zsh -n` over the exact source with `opCtx`, and write stdout only after validation succeeds. No operation accepts caller paths/refs/Git argv/shell commands/output destinations.
- The sourced loader defines exact `zsh-pro sync` and `zsh-pro sync --resolve shared` parent-shell operations. It scopes `ZSHPRO_SHELL_ID` to `command zsh-pro` for binary verbs; checkout/reset call the binary workflow and then the same sync pipeline, while all other supported/unknown verbs are forwarded without recursion.
</interfaces>
</context>

## Artifacts this phase produces

- `core/cli/runtime.go` adds `WorktreeHelperDeadline`, exact private operations attach/publish/prepare/ack/resolve, strict validation, internal deadline-before-auth/bind/I/O, operation-scoped adapter closure, and buffered exact-source zsh validation/output under the same context.
- `core/shell/zsh/hook.go` adds `_zp_worktree_ensure_attached`, retained initial semantic snapshot storage, `_zp_worktree_publish`, `_zp_worktree_pull`, `_zp_worktree_sync`, `_zp_worktree_resolve_shared`, capture/effective-config helpers, opaque shell ID, applied/pending/behind/conflict/error/guard globals, and cooperative hook registration without a source-time subprocess.
- Public sourced-loader `zsh-pro()` dispatches the exact installed syntax: `sync`, `sync --resolve shared`, checkout/reset with parent-shell convergence, and all other verbs through scoped-ID `command zsh-pro`; exact `ZSHPRO_AUTO_APPLY` override. Legacy `checkout`, `activate`, `status`, and `list` delegate into this dispatcher and cannot execute the former per-terminal branch path.
- `core/cmd/zsh-pro/main.go` configures Plan 07-08 RuntimeWorktreeFactory constructors/policy; Plan 07-09 runtime code binds an operation-scoped Service only after validation and RuntimeRoot authentication.
- Tests include zero-subprocess loader source, lazy attach/no pre-attach publish/no-delta boundary, exact public installed syntax/legacy rewiring/shell-ID transport, 249/251 ms service and validator stalls, real-provider inherited/post-attach admission, same-key/dirty-gap resolution, ack order, line-finish timing, foreground/fail-open, helper loss, hostile shell options, and canonical-authority composition without pointer-identity assumptions.
- No Git hot-path call, child-zsh capture, background watcher, persistent sourced patch, dependency, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fixed-250-ms late-bound runtime operations and exact-buffer validation</name>
  <files>core/cli/runtime.go, core/cli/runtime_test.go</files>
  <read_first>
    - core/cli/runtime.go: current allowlist, integer outer timeout, root authentication, bounded stdin/stdout, and safe error mapping
    - core/cli/runtime_test.go: current typed-nil, descriptor replacement, deadline, and source-redaction regressions
    - core/cli/emitter.go: RuntimeWorktreeFactory exact typed methods
    - core/cli/runtime_root_unix.go: authenticated retained descriptors and close ownership
    - core/model/worktree.go: exact args/revisions/tokens/frame results
    - core/worktree/service.go: Attach/Publish/Prepare/Acknowledge/ResolveShared semantics
  </read_first>
  <action>
    Preserve current descriptor-close and typed-nil hardening. Extend the private allowlist with exactly attach/publish/prepare/ack/resolve. Parse operation, outer 1-99 second watchdog, delimiter, opaque shell ID, strict unsigned revision/token fields, and bounded frame expectations before dependency access. Immediately after syntactic validation, derive `opCtx := context.WithTimeout(parent, WorktreeHelperDeadline)` with the constant fixed at 250 ms. Under opCtx authenticate RuntimeRoot, then and only then call `RuntimeWorktreeFactory.Bind(root)` to obtain an operation-scoped adapter plus close ownership; defer exactly-once close and forbid path reopen, eager public-Service reuse, or fallback binding. Use opCtx for binding, stdin/frame decoding, Service, rendering, and buffered response. For prepare/resolve, keep source/revision/token private until `validateRuntimeSource(opCtx, exactSource)` runs zsh -n on the exact buffered bytes; stdout remains empty on render, validator, deadline, or write-preparation failure. The outer seconds context remains only a larger defense-in-depth process watchdog and never defines prompt budget. Add injectable active blocking seams/tests proving 249 ms completes and 251 ms is canceled for authentication/binding, attach/publication, preparation/render, the zsh validator itself, and acknowledgement/resolution; assert timeout starts before auth/bind/I/O and that validator cancellation kills the child. Invalid or timed-out operations make zero unintended calls and return value-free errors.
  </action>
  <acceptance_criteria>
    - Every private worktree operation, including authenticated binding and prepare/resolve zsh -n, is terminated by the same real 250 ms internal context.
    - Active 249/251 ms service and validator tests prove completion/cancellation behavior, not just elapsed measurement.
    - Runtime adapter construction happens after validation/authentication, closes once, and cannot reopen or fall back to another authority.
    - Outer integer syntax remains compatible but cannot extend internal work.
    - Arbitrary runtime paths/refs/commands and malformed args are rejected before auth/service.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/cli -run 'Test(RuntimeWorktree|RuntimeWorktreeBindOrder|RuntimeWorktreeClose|WorktreeDeadline|WorktreeValidatorDeadline|DeadlineBeforeAuth|RuntimeDescriptor|NoRuntimeReopen)' -count=1</automated>
  </verify>
  <done>The private runtime surface is exact, post-auth descriptor-bound, value-safe, validates the exact source under the same context, and is actually terminated at 250 ms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach lazily, route exact public commands, and converge at safe boundaries</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: bounded helper, protected eval, retained reverse, markers, fail-open wrappers, and install behavior
    - core/shell/zsh/hook_test.go: no-source-time-subprocess, timeout, fail-open, and function surface tests
    - core/cli/runtime.go: Task 1 exact operations/deadline
    - core/cmd/zsh-pro/main.go: Plan 07-07 public Service plus late-bound runtime constructor configuration over one canonical authority
    - core/cli/worktree.go: Plan 07-06 public workflow surface and ZSHPRO_SHELL_ID status context
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: attach-derived registry, precmd/line-finish, and ack order
  </read_first>
  <action>
    Preserve existing loader/runtime hardening. Loader source may define functions/globals and cooperatively register hooks, but must execute zero external commands, command substitutions, private runtime helpers, Git calls, child-zsh introspection, attach operations, or publications; add an explicit source-time process counter assertion. Generate one opaque shell ID from process-local zsh state only. At the first safe `precmd`, `line-finish`, or sourced public-command boundary, `_zp_worktree_ensure_attached` captures one semantic initial snapshot with zsh builtins, retains it in structured process-local storage across retries, frames that exact retained snapshot to private attach, and on success clears the cache/marks attached/returns without publishing. Every publish calls ensure-attached first and refuses while unattached, so initial attach is idempotent/no-delta and cannot be bypassed. Register namespaced precmd publish and pull-only line-finish through cooperative helpers with idempotent remove/re-add and recursion guard.

    Define the exact sourced `zsh-pro()` dispatcher. It scopes `ZSHPRO_SHELL_ID="$ZP_WORKTREE_SHELL_ID"` only on `command zsh-pro` child invocations. `zsh-pro sync` and `zsh-pro sync --resolve shared` call the parent-shell pipeline directly; other sync shapes fail value-safely. `zsh-pro checkout <name>`, `zsh-pro checkout -b <name>`, and `zsh-pro reset --hard` ensure attachment, invoke the exact binary workflow once, and after success call the same parent-shell sync so the current shell applies the new shared revision. Status/diff/commit/branch/list/config/ingest and unknown verbs forward once through `command zsh-pro` with no function recursion; status thereby receives the validated opaque shell context from Plan 07-06. Replace legacy bare `checkout` and `activate` with delegates to `zsh-pro checkout`, bare `status` with `zsh-pro status`, and bare `list` with `zsh-pro list`; remove their former per-terminal Store.Current/_zp_switch/status rendering paths. Keep deactivate as a parent-local recovery/teardown operation and never let it redefine shared authority.

    Use one worktree apply sequence for auto, checkout/reset follow-up, explicit sync, and resolve: private prepare/resolve performs render plus exact zsh -n inside Task 1's same opCtx before returning bytes; the loader performs protected eval, replaces `ZP_ACTIVE_REVERSE_FN`, captures a fresh semantic snapshot, compares the target, and acknowledges exact revision/token. Do not call the legacy seconds-budget validator again for worktree patches. `sync --resolve shared` applies only authoritative changes that replace this shell's unacknowledged local delta; failed prepare/validation/transport/eval/match/ack retains conflict and old baseline. Auto config uses persisted default plus exact shell override. Configure RuntimeWorktreeFactory in main with constructors/provider policy only; `runRuntime` performs post-auth operation binding, and tests assert public/runtime Services share canonical state without pointer identity. Add fake-binary plus real-provider tests for zero-process source, lazy attach retention, exact installed syntax and one-call routing, legacy bypass retirement, shell-ID status transport, inherited exclusion/post-attach admission, same-key and dirty-gap resolution, next-command alias timing, later-head race, foreground non-interference, no-op repeat, missing helper, validator timeout, ERR_EXIT/ERR_RETURN, and canonical authority.
  </action>
  <acceptance_criteria>
    - Sourcing the loader starts no process; one retained snapshot attaches at the first safe boundary, and no shell can publish on or before that no-delta attachment succeeds.
    - Exact installed `zsh-pro ...` syntax routes every public verb once, checkout/reset mutate the parent through sync, and legacy functions cannot use per-terminal bypass behavior.
    - Status receives only a scoped validated opaque shell ID; it cannot influence durable branch/root/revision authority.
    - Inherited identities remain excluded while safe identities created after attach publish through the real provider policy.
    - Same-key and dirty history-gap conflicts resolve only through explicit shared adoption and verified acknowledgement.
    - Every failure leaves applied revision/conflict truth unchanged and the prompt/entered command usable.
    - Hooks run only at prescribed safe boundaries and invoke neither Git nor child-zsh introspection; worktree source is zsh-n validated before transport under the same internal deadline.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh ./core/cmd/zsh-pro -run 'Test(WorktreeLoaderSourceZeroProcess|WorktreeLazyAttach|WorktreePublicDispatcher|WorktreeLegacyDelegates|WorktreeShellIDTransport|WorktreeHook|Precmd|LineFinish|AutoApply|ExplicitSync|ResolveShared|Acknowledge|FailOpen|Foreground|MainWorktree|CanonicalWorktreeAuthority)' -count=1</automated>
  </verify>
  <done>Independent shells source with zero subprocesses, attach before publish at the first safe boundary, route exact public commands through shared truth, converge/resolve only through the parent pipeline, and fail open under one real 250 ms prepare/validation deadline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| hook -> runtime helper | Prompt path crosses process, descriptor, and time boundaries |
| sourced zsh-pro dispatcher -> binary/public workflow | Parent-shell mutation and opaque shell context cross a recursion/authority boundary |
| initial shell -> Attach | Ambient current state initializes admission/baseline authority |
| generated bytes -> eval/ack | Durable changes become parent-shell state and acknowledged truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-35 | Denial of Service | runtime helpers/hooks/validator | high | mitigate | Fixed internal 250 ms context before auth/bind/I/O/service/render/zsh-n, active 249/251 service and validator cancellation tests, recursion guard, and fail-open wrappers in Tasks 1-2. |
| T-07-36 | Information Disclosure | attach/publish/source/errors | high | mitigate | Production admission before persistence, in-memory source, value-free errors/status, and real-provider canary tests in Task 2. |
| T-07-37 | Tampering | first publication | high | mitigate | Zero-process source, retained first-safe-boundary snapshot, mandatory idempotent no-delta attach, publish refusal while unattached, durable initial baseline/exclusions, and tests. |
| T-07-38 | Tampering | resolution/apply/ack | high | mitigate | Exact token, shared-only prepare, reverse replacement/fresh match before ack, and failure-retains-conflict tests. |
| T-07-39 | Tampering | foreground/process-local state | high | mitigate | precmd publish, line-finish pull, explicit sync only, no watcher/model kinds, and foreground tests. |
| T-07-40 | Spoofing | public dispatcher/private operation/service | high | mitigate | Exact public/private allowlists and arities, scoped validated shell ID, command-recursion prevention, post-auth late binding to canonical authority, no reopen/fallback, and zero-call invalid tests. |

ASVS Level 1 applies; every high-severity deadline, attachment, privacy, resolution, foreground, and authority gate is blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/worktree ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro -count=1`
- `GOTOOLCHAIN=local go vet ./core/cli ./core/shell/zsh ./core/cmd/zsh-pro`
- `GOTOOLCHAIN=local go build ./...`
- Structural/tests confirm fixed 250 ms internal context precedes root/bind/I/O/service/render/zsh-n, loader source spawns zero processes, legacy functions delegate to the sourced dispatcher, and hook paths contain no Git/child-zsh introspection.
</verification>

<success_criteria>
- Every shell sources with zero subprocesses and has an executable lazy no-delta attach lifecycle before first publication.
- Exact installed public syntax, checkout/reset parent mutation, default/manual sync, and explicit shared conflict resolution use one validated apply/reverse/fresh-snapshot/ack path; legacy bypasses are retired.
- Service and zsh validator stalls are terminated inside the same 250 ms operation, runtime authority binds only after authentication, and every failure remains visible and fail-open.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-09-SUMMARY.md` when done.
</output>
