---
phase: 07-git-like-shared-working-environment
plan: 19
type: execute
wave: 15
depends_on: ["07-18"]
files_modified:
  - core/cli/cli.go
  - core/cli/worktree.go
  - core/cli/worktree_test.go
  - core/cli/emitter.go
  - core/cli/emitter_test.go
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/worktree_live_test.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-03, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "[CR-05 / WORK-03 / SYNC-01 / SYNC-02] Sourced checkout, checkout -b, and reset use `RuntimeWorktree.Attach` with strict mutate/mutation-result envelope modes under the existing five-method `RuntimeWorktree` contract, one exact bounded stdin frame, and the original descriptor/capability authority; they never invoke the unbounded public binary mutation path."
    - "Each retained-zsh mutation returns within the original absolute 250 ms budget when the service completes but the response is lost, preserves non-exported request identity, and accepts a subsequent command without leaked descriptors or surviving owned children."
    - "A later explicit sync performs authenticated result-only recovery, returns the exact committed/recovery-required/revision receipt from Plan 07-18, never executes checkout/reset again, and converges through prepare/apply/fresh-capture/acknowledge exactly once."
    - "Immediate and recovered mutation truth remains separate from shell reconciliation truth: not committed performs no pull, uncertain truth remains pending, and committed plus failed reconciliation reports a fixed committed-and-reconciliation-pending class."
    - "Malformed, partial, trailing, replay-mismatched, guessed, wrong-capability, cross-shell, stale, or foreign-root recovery frames never claim completion, repeat mutation, disclose values, or clear pending authority."
    - "The built loader's exact symbol allowlist explicitly names every mutation/recovery helper and every `ZP_WORKTREE_MUTATION_*` parameter while rejecting all unintended global deltas."
  artifacts:
    - path: core/cli/runtime.go
      provides: "strict mutate and mutation-result modes on the existing attach operation tag with value-free result encoding"
    - path: core/cli/emitter.go
      provides: "unchanged five-method RuntimeWorktree boundary, mode-aware Attach envelope, and private descriptor-bound service/Repository adapter"
    - path: core/cli/worktree.go
      provides: "context-carrying public CLI adapter and fixed mutation outcome rendering"
    - path: core/shell/zsh/hook.go
      provides: "bounded sourced mutation, pending-result recovery, and exact reconciliation lifecycle"
    - path: core/shell/zsh/worktree_live_test.go
      provides: "separate deterministic 250 ms lost-response oracles for checkout, checkout -b, and reset"
    - path: core/cmd/zsh-pro/main_test.go
      provides: "exact installed loader parameter/function allowlist for mutation and recovery symbols"
  key_links:
    - from: core/shell/zsh/hook.go
      to: core/cli/runtime.go
      via: "_zp_worktree_mutate and _zp_worktree_recover_mutation encode attach modes mutate/mutation-result inside the existing ZPWT frame"
      pattern: "_zp_worktree_(mutate|recover_mutation)|mode.*mutation-result"
    - from: core/cli/runtime.go
      to: core/cli/emitter.go
      via: "runRuntimeWorktree builds the mode-aware Attach envelope; RuntimeWorktree.Attach dispatches only behind the unchanged five-method boundary to private service mutation/recovery methods"
      pattern: "RuntimeWorktree.*Attach|runtimeWorktreeService|ExecuteWorkflowMutation|RecoverWorkflowMutation"
    - from: core/cli/emitter.go
      to: core/cmd/zsh-pro/main.go
      via: "composition root binds both StateStore and concrete runtime Store Repository from the same authenticated RuntimeRoot descriptors"
      pattern: "NewRuntimeWorktreeFactory|store.NewRuntime|BindRuntimeWorktree"
    - from: core/shell/zsh/hook.go
      to: core/worktree/service.go
      via: "exact recovered revision drives the existing prepare/apply/capture/ack path; pending mutation clears only after exact acknowledgement"
      pattern: "ZP_WORKTREE_MUTATION_|ZP_WORKTREE_APPLIED_REVISION|_zp_worktree_pull"
    - from: core/shell/zsh/hook.go
      to: core/cmd/zsh-pro/main_test.go
      via: "TestMainIngestAllowsOnlyExactLoaderSymbols enumerates each new mutation/recovery helper and parameter by exact name"
      pattern: "_zp_worktree_mutate|_zp_worktree_recover_mutation|ZP_WORKTREE_MUTATION_|TestMainIngestAllowsOnlyExactLoaderSymbols"
---

<objective>
Complete CR-05 by carrying Plan 07-18's durable mutation truth through the CLI/runtime frame and retained parent shell under one bounded, authenticated, replay-safe recovery flow.

Purpose: A lost helper response must never make an interactive user guess whether checkout/reset happened or risk repeating a durable operation.
Output: Context-aware CLI adapter, descriptor-bound runtime repository wiring, strict mutate/result frames, pending-result shell recovery, and real retained-zsh checkout/checkout -b/reset convergence evidence.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-14-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-15-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-18-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/cli/cli.go
@core/cli/worktree.go
@core/cli/worktree_test.go
@core/cli/emitter.go
@core/cli/emitter_test.go
@core/cli/runtime.go
@core/cli/runtime_test.go
@core/shell/zsh/hook.go
@core/shell/zsh/hook_test.go
@core/shell/zsh/worktree_live_test.go
@core/cmd/zsh-pro/main.go
@core/cmd/zsh-pro/main_test.go
</context>

<interfaces>
Current contracts and Plan 07-18 inputs:

- `core/cli/cli.go::Run` dispatches public commands without a caller context; `core/cli/worktree.go::runWorktree` creates `context.Background()`. Add `RunContext` and thread it through without changing direct `Run` compatibility.
- `core/cli/runtime.go::runRuntimeWorktree` already owns the exact five operation tags, `DefaultWorktreeTransitionBudget = 250*time.Millisecond`, one `context.WithTimeout`, descriptor-authenticated `RuntimeRoot`, bounded one-shot ZPWT-v1 stdin, strict final-record/EOF validation, value-free failures, and a response-stage test seam.
- `core/cli/emitter.go::RuntimeWorktree` is contractually exactly five methods — `Attach`, `Publish`, `Prepare`, `Acknowledge`, and `Resolve` — as enforced by `TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations`. Do not add `Mutate`, `RecoverMutation`, or any sixth method. Extend only the internal mode-aware Attach request/response envelope and the private `runtimeWorktreeService` behind its adapter.
- `runtimeWorktreeFrame.validateShape` gives `attach` exact `allocate`/`commit` modes, with release added by Plan 07-17. Plan 07-18 supplies durable mutation service contracts; add `mutate` and `mutation-result` as exact attach envelope modes rather than introducing a sixth operation tag or method.
- `core/cli/emitter.go::RuntimeWorktreeFactory.Bind` currently creates `worktree.NewService(store, registry)` without a Repository. Extend construction so the same authenticated `RuntimeRoot` supplies `store.NewRuntime` as the Repository; never reuse the path-bound public Service or reopen `StoreRoot()` after descriptor authentication.
- Plan 07-18 provides `ExecuteWorkflowMutation`, `RecoverWorkflowMutation`, strict request/result types, and exact `{Committed, RecoveryRequired, Revision}` truth. Result-only recovery cannot begin a journal or execute a mutation.
- `core/shell/zsh/hook.go::zsh-pro`, checkout/reset arm currently calls `command zsh-pro "$@"` and then `_zp_worktree_pull`. `_zp_worktree_invoke` already owns exact descriptors/direct children and the single deadline after Plan 07-16; `_zp_worktree_apply_transition` already owns prepare/eval/reverse/fresh-capture/ack.
</interfaces>

<scope_fence>
- Close the CLI/runtime/parent-shell half of CR-05 only. Plan 07-18 owns journal/ref/state durability. Do not change the journal protocol or derive outcome from shell-local belief.
- Keep WR-01 excluded and preserve CR-01 through CR-04: portable owned cleanup, semantic equality, structural footer validation, authenticated shell retirement, exact-five operation names, value-free surfaces, and native macOS evidence.
- Argv remains `runtime worktree <attach|publish|prepare|acknowledge|resolve> <seconds>`. Shell ID, capability, operation ID, mode, verb, create bit, branch, and recovery identity travel only in the bounded stdin frame; no credential/request value enters argv or environment.
- Persistent parent-shell mutation fields are non-exported and limited to operation ID, verb, create bit, branch, request fingerprint/status, and exact revision. Never retain a raw reply, generated source, captured state, token, journal bytes, descriptor, or call-local capability copy.
- No Git invocation in shell code, no ambient root discovery after runtime authentication, no sleep-based acceptance test, no automatic mutation replay, no deadline reset between attach/publish/mutate/recover/pull/acknowledge, and no new dependency.
</scope_fence>

<scope_budget>
- Plan budget: 3 tasks and 12 unique files; no task owns more than 5 files.
- Task 1: seven independently failing RED contracts across 5 test files. Task 2: CLI/frame/private-adapter implementation across 4 production files. Task 3: parent-shell/composition-root wiring plus exact loader-symbol regression across 3 files.
- Durable journal/store/service code is consumed from 07-18 and is not reopened here, preserving the persistence/runtime subsystem split requested by the checker.
</scope_budget>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit failing CLI, frame, pending-recovery, and retained-zsh contracts</name>
  <files>core/cli/worktree_test.go, core/cli/emitter_test.go, core/cli/runtime_test.go, core/shell/zsh/hook_test.go, core/shell/zsh/worktree_live_test.go</files>
  <behavior>
    - `TestWorktreeMutationCLIOutcomeRenderingAndContext`: a cancelled/deadline context reaches worktree mutation; terminal not-committed, committed, and committed/recovery-required results render fixed value-free truth.
    - `TestRuntimeMutationFactoryBindsDescriptorRepository`: runtime binding creates StateStore and Repository from the same retained authenticated root descriptors, remains bound after pathname replacement, and exposes mutation/recovery only through the existing Attach method and private service adapter.
    - `TestRuntimeMutationFrameRejectsMalformedReplayedAndForeignAuthority`: exact mutate/result Attach envelope modes accept one canonical request; partial/trailing/duplicate/oversize/stale/replayed-request/wrong-capability/cross-shell/foreign-root inputs fail before disclosure or mutation, while the existing exact-five-method contract remains GREEN.
    - `TestWorktreeMutationPendingLifecycle`: hook source keeps one absolute deadline, stores only non-exported recovery identity, checks pending result before publish, never calls execute mode from recovery, and clears pending only after exact applied revision acknowledgement.
    - `TestWorktreeCheckoutLostResponseRecoversExactOutcome`, `TestWorktreeCheckoutCreateLostResponseRecoversExactOutcome`, and `TestWorktreeResetHardLostResponseRecoversExactOutcome`: each retained real zsh loses the complete helper response after service completion, returns by 250 ms, accepts a sentinel command, recovers through one sync, performs the repository/state mutation once, and converges to the exact revision.
  </behavior>
  <action>
    Add the seven named tests. For the three retained-zsh cases, use distinct top-level test names, not only subtests, so RED and final evidence prove checkout, checkout -b, and reset separately. Build the production binary and interpose a test-owned helper/pipe barrier that buffers and withholds stdout only after the real runtime service has returned; release it through a deterministic channel or descriptor event. Do not add a production environment fault switch or use `sleep` to cross 250 ms.

    Each retained test must inspect durable state/ref/receipt before releasing the withheld-response barrier, prove the parent invocation finishes by the original deadline and exact owned children/descriptors are gone, send a subsequent sentinel command through the same `zsh -f`, then run `zsh-pro sync`. Count Git create calls, generation revisions/events, receipt entries, prepare/apply/acknowledge calls, and prove final shell/shared equality. Include a committed result whose reconciliation is faulted once and verify the fixed committed-pending class survives until the next sync.

    Runtime negative cases must include missing/extra/out-of-order records, early EOF/trailer, invalid canonical integers, oversized branch/frame, exact operation-ID replay with a different request, guessed operation ID, wrong capability, cross-shell credential, stale revision, and descriptor-root replacement. Assert mutate and mutation-result travel through `RuntimeWorktree.Attach`'s tagged request/response envelope and the private service spy, then keep `TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations` GREEN with exactly `Attach`, `Publish`, `Prepare`, `Acknowledge`, and `Resolve`. Scan stdout/stderr/state for branch/source/live-value/capability/token/root-path canaries while allowing only the documented fixed status/revision reply.

    Run all seven tests separately against the 07-18 implementation, prove each exact `--- FAIL:` line, and commit `test(07-19): add failing bounded mutation recovery contract`. Do not edit production in this task.
  </action>
  <verify>
    <automated>bash -o pipefail -c "set -e; run_red() { red_pkg=\$1; red_name=\$2; red_log=\$3; set +e; GOTOOLCHAIN=local go test -count=1 \"\$red_pkg\" -run \"^\${red_name}\$\" -timeout=180s &gt;\"\$red_log\" 2&gt;&amp;1; red_rc=\$?; set -e; cat \"\$red_log\"; test \"\$red_rc\" -ne 0; ! grep -Eq -- 'undefined:|build failed' \"\$red_log\"; }; red1=\$(mktemp); red2=\$(mktemp); red3=\$(mktemp); red4=\$(mktemp); red5=\$(mktemp); red6=\$(mktemp); red7=\$(mktemp); trap 'rm -f \"\$red1\" \"\$red2\" \"\$red3\" \"\$red4\" \"\$red5\" \"\$red6\" \"\$red7\"' EXIT; run_red ./core/cli TestWorktreeMutationCLIOutcomeRenderingAndContext \"\$red1\"; grep -Eq -- '--- FAIL: TestWorktreeMutationCLIOutcomeRenderingAndContext([[:space:]]|$)' \"\$red1\"; run_red ./core/cli TestRuntimeMutationFactoryBindsDescriptorRepository \"\$red2\"; grep -Eq -- '--- FAIL: TestRuntimeMutationFactoryBindsDescriptorRepository([[:space:]]|$)' \"\$red2\"; run_red ./core/cli TestRuntimeMutationFrameRejectsMalformedReplayedAndForeignAuthority \"\$red3\"; grep -Eq -- '--- FAIL: TestRuntimeMutationFrameRejectsMalformedReplayedAndForeignAuthority([[:space:]]|$)' \"\$red3\"; run_red ./core/shell/zsh TestWorktreeMutationPendingLifecycle \"\$red4\"; grep -Eq -- '--- FAIL: TestWorktreeMutationPendingLifecycle([[:space:]]|$)' \"\$red4\"; run_red ./core/shell/zsh TestWorktreeCheckoutLostResponseRecoversExactOutcome \"\$red5\"; grep -Eq -- '--- FAIL: TestWorktreeCheckoutLostResponseRecoversExactOutcome([[:space:]]|$)' \"\$red5\"; run_red ./core/shell/zsh TestWorktreeCheckoutCreateLostResponseRecoversExactOutcome \"\$red6\"; grep -Eq -- '--- FAIL: TestWorktreeCheckoutCreateLostResponseRecoversExactOutcome([[:space:]]|$)' \"\$red6\"; run_red ./core/shell/zsh TestWorktreeResetHardLostResponseRecoversExactOutcome \"\$red7\"; grep -Eq -- '--- FAIL: TestWorktreeResetHardLostResponseRecoversExactOutcome([[:space:]]|$)' \"\$red7\""</automated>
  </verify>
  <done>Seven contracts compile and each fails individually at the missing context/runtime/recovery behavior before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement context-aware CLI and strict descriptor-bound mutation frames</name>
  <files>core/cli/cli.go, core/cli/worktree.go, core/cli/emitter.go, core/cli/runtime.go</files>
  <behavior>
    - Public CLI mutation receives caller cancellation and renders exact value-free result classes.
    - Runtime bind supplies both StateStore and Repository from authenticated descriptors.
    - `RuntimeWorktree` remains exactly five methods; mutate/result use its mode-aware `Attach` request/response envelope, and only the private service interface gains durable mutation/recovery methods.
    - Attach mutate/result modes use strict bounded frames and result-only recovery cannot execute.
  </behavior>
  <action>
    Add `CLI.RunContext(ctx, args, stdout, stderr)` and make `Run` delegate with `context.Background()` for compatibility. Pass the supplied context into `runWorktree`; reject nil context. Keep full argv validation before dependency access. Render only fixed classes for not committed, committed, and committed/recovery-required with canonical revision when present; never include underlying error, branch, OID, root, source, or request data.

    Keep `RuntimeWorktree` exactly at its existing five methods: `Attach`, `Publish`, `Prepare`, `Acknowledge`, and `Resolve`. Do not add `Mutate` or `RecoverMutation`. Evolve the private Attach argument/result into a strict tagged request/response envelope that represents commit, release, mutate, and mutation-result modes; the mutate variants carry Plan 07-18's typed request/result while legal fields for every other mode remain disjoint. Extend only the private `runtimeWorktreeService` behind `runtimeWorktreeAdapter` with `ExecuteWorkflowMutation` and `RecoverWorkflowMutation` (alongside Plan 07-17's private release method), and have the adapter's existing `Attach` method switch on the validated mode to call the appropriate private service method.

    Extend `RuntimeWorktreeFactory` with a runtime Repository constructor. `Bind` must obtain StateStore and Repository from the same caller-owned `RuntimeRoot` descriptors, construct `NewService(state, registry, repository)`, and return one closer that owns only created duplicates/resources. Preserve typed-nil guards and close every partial construction on failure.

    In `runtime.go`, retain exactly five operation tags and add exact `attach` modes `mutate` and `mutation-result`. Add ordered tags for mutation verb, branch, and canonical create bit; mutate requires credential, operation ID, verb/create/branch, and final record, while result requires the identical request identity and final record. Neither mode accepts snapshot/current/baseline/token/apply/reverse fields. After frame validation, construct the corresponding private Attach envelope, call only `RuntimeWorktree.Attach`, and encode the response variant matching the requested mode. Keep the 2,101,248-byte, 10,016-record, final-record/EOF, one-read, and one absolute `context.WithTimeout` gates.

    Inside the private adapter, route mutate to `ExecuteWorkflowMutation` and result to `RecoverWorkflowMutation`; encode a single fixed versioned reply containing committed bit, recovery-required bit, and canonical revision with strict legal combinations. The response-stage seam runs after service completion so a test can drop the whole reply without changing durable execution. Result-only mode must remain read/recovery-only when no receipt/journal exists. Clear decoded capability/request buffers on all exits and keep stderr value-free. Commit `fix(07-19): add bounded mutation runtime protocol` only after the three CLI tests and `TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations` prove the exact five-method interface remains unchanged, plus existing frame/budget suites pass.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^(TestWorktreeMutationCLIOutcomeRenderingAndContext|TestRuntimeMutationFactoryBindsDescriptorRepository|TestRuntimeMutationFrameRejectsMalformedReplayedAndForeignAuthority|TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations|TestRuntimeWorktreeFrameExactByteAndRecordBounds|TestWorktreeFakeClockCumulative249And251Milliseconds)$' -timeout=240s &amp;&amp; GOTOOLCHAIN=local go test -race -count=1 ./core/cli -run 'Mutation|RuntimeWorktree|WorktreeRunContext' -timeout=240s</automated>
  </verify>
  <done>CLI cancellation reaches mutations, the runtime Service has descriptor-bound Git/state authority, strict mutate/result Attach envelopes preserve exactly five RuntimeWorktree methods, and foreign/malformed recovery cannot mutate or disclose.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire bounded parent-shell outcome recovery and exact convergence</name>
  <files>core/shell/zsh/hook.go, core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <behavior>
    - Checkout, checkout -b, and reset use private bounded mutation frames and preserve pending result identity after complete response loss.
    - Sync recovers result before any publish, never repeats mutation, and acknowledges the exact revision before clearing pending state.
    - Composition root supplies the runtime Repository factory from authenticated descriptors.
    - Built-loader ingest exposes exactly `_zp_worktree_mutate`, `_zp_worktree_recover_mutation`, and the seven named `ZP_WORKTREE_MUTATION_*` parameters added by this task, with no wildcard or unintended symbol allowance.
  </behavior>
  <action>
    In `main.go`, construct the extended `RuntimeWorktreeFactory` with a Repository callback that uses `RuntimeRoot.Files()` and `store.NewRuntime(repository, vaultParent, provider)`. Do not reuse `cliStore`, `canonicalRoot`, or the public path-bound Service after runtime root authentication.

    In `hook.go`, extend `_zp_worktree_write_frame` and `_zp_worktree_invoke` for attach `mutate`/`mutation-result` modes while preserving Plan 07-16's exact owned descriptor/direct-child lifecycle and Plan 07-17's release mode. Start the mutation deadline before ensure-attached/publish and pass the same absolute timestamp through frame write, helper response, recovery decision, pull/apply/fresh capture/acknowledge; never start another 250 ms window inside the invocation.

    Replace the checkout/reset arm's synchronous public binary call with `_zp_worktree_mutate`. Generate one operation ID and retain non-exported pending verb/create/branch/request identity until terminal recovery and exact acknowledgement. A strict immediate not-committed result clears pending and performs no pull. A complete committed result enters the existing pull/apply/ack path for the returned revision. A timeout, missing/truncated reply, or recovery-required result sets the fixed `mutation outcome pending; run zsh-pro sync` error and returns within budget without claiming the shell is unchanged.

    At the start of explicit `_zp_worktree_sync`, before capture/publish, call `_zp_worktree_recover_mutation` when pending identity exists. It must send `mutation-result`, never `mutate`. Exact not committed clears pending with no pull; exact committed proceeds to prepare/apply/fresh-capture/acknowledge; exact recovery-required or malformed/unauthorized/stale reply remains pending and fails closed. Clear pending only when `ZP_WORKTREE_APPLIED_REVISION` equals the recovered revision after exact acknowledgement. If reconciliation fails after committed truth, preserve pending and report `mutation committed; shell reconciliation pending; run zsh-pro sync`.

    Update `activate`/`checkout` compatibility wrappers to preserve the committed-pending result instead of saying shell state is unchanged. Keep public shell wrappers fail-open in their existing contract while `ZP_LAST_RUNTIME_STATUS`, `ZP_LAST_RUNTIME_ERROR`, and `ZP_WORKTREE_LAST_ERROR` expose the exact fixed class. Clear call-local response/capability/request copies in `always`; `_zp_worktree_disable` clears pending mutation fields only when terminal-local disable is authoritative.

    Keep the new loader-visible surface exact: install only `_zp_worktree_mutate` and `_zp_worktree_recover_mutation` as new global helper functions, and store pending identity in exactly `ZP_WORKTREE_MUTATION_OPERATION_ID`, `ZP_WORKTREE_MUTATION_VERB`, `ZP_WORKTREE_MUTATION_CREATE`, `ZP_WORKTREE_MUTATION_BRANCH`, `ZP_WORKTREE_MUTATION_FINGERPRINT`, `ZP_WORKTREE_MUTATION_STATUS`, and `ZP_WORKTREE_MUTATION_REVISION`. Update `TestMainIngestAllowsOnlyExactLoaderSymbols` in `main_test.go` by adding those two exact function names to `wantFunctions` and those seven exact parameter names to `wantParameters`. Do not use a prefix, wildcard, relaxed subset, or introduce another global helper/parameter without naming it in the exact lists and this plan's security evidence. Commit `fix(07-19): recover shell mutation outcomes without replay` only after all shell/live tests, the exact loader-symbol test, and the exact-five-method interface test pass.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreeMutationPendingLifecycle|TestWorktreeCheckoutLostResponseRecoversExactOutcome|TestWorktreeCheckoutCreateLostResponseRecoversExactOutcome|TestWorktreeResetHardLostResponseRecoversExactOutcome|TestWorktreePortableOwnedTransport|TestWorktreeAbsoluteDeadlineAdversarialTransport|TestWorktreeTwoShellEndToEnd|TestWorktreePartialPatchFailureRecovery|TestWorktreeFirstExplicitSyncReconciles)$' -timeout=300s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cmd/zsh-pro -run 'Worktree|Runtime' -timeout=120s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cmd/zsh-pro -run '^TestMainIngestAllowsOnlyExactLoaderSymbols$' -timeout=120s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations$' -timeout=60s</automated>
  </verify>
  <done>All three Git-shaped mutations survive deterministic post-completion response loss, return by 250 ms, recover exact truth without replay, converge once at the exact acknowledged revision, retain exactly five RuntimeWorktree methods, and expose only the explicitly allowlisted loader symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained zsh -> runtime frame | Private bearer/request data crosses one bounded stdin stream while argv remains non-authorizing. |
| authenticated RuntimeRoot -> StateStore and Repository | State and Git must bind to the same retained descriptors after path validation. |
| completed helper -> lost parent response | Durable truth exists while the parent has only pending non-exported request identity. |
| recovered mutation -> shell reconciliation | Exact canonical revision must drive apply/ack without repeating the durable mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-19-01 | Spoofing | mutation-result frame | critical | mitigate | Original shell/capability, operation ID, exact request identity, descriptor root, and constant-time Service authentication. |
| T-07-19-02 | Tampering | mutate/result Attach grammar | critical | mitigate | Exact ordered tags, duplicate/extra/trailing rejection, final record plus EOF, canonical fields, mode-tag match, and a discriminated Attach envelope before private Service dispatch. |
| T-07-19-03 | Elevation of Privilege | runtime repository binding | critical | mitigate | Construct Store Repository and StateStore from the same authenticated RuntimeRoot descriptors; mutation/recovery are reachable only through the private service adapter behind RuntimeWorktree.Attach, with no path reopen or public Service reuse. |
| T-07-19-04 | Tampering | duplicate mutation | critical | mitigate | Recovery invokes result-only mode; repository/state counters and three retained-zsh tests prove execute occurs once. |
| T-07-19-05 | Repudiation | lost response | critical | mitigate | Pending identity plus authenticated canonical receipt recovery returns exact committed/recovery-required/revision truth. |
| T-07-19-06 | Denial of Service | helper/output stall | high | mitigate | One absolute 250 ms deadline, final cleanup reserve, portable owned TERM/KILL/reap, and next-command sentinels for all three verbs. |
| T-07-19-07 | Information Disclosure | argv/env/reply/error | high | mitigate | Credential/request only on stdin, fixed result/error vocabulary, clearing on all exits, and canary inspection of process/state/output. |
| T-07-19-08 | Tampering | stale-shell publish after mutation | high | mitigate | Sync resolves pending mutation before capture/publish and reconciles only the returned exact revision. |
| T-07-19-09 | Repudiation | reconciliation failure | high | mitigate | Committed truth persists independently; fixed committed-pending class remains until exact acknowledgement. |
| T-07-19-10 | Denial of Service | malformed/replayed frames | high | mitigate | Existing frame byte/record caps, one-shot read, strict replay mismatch, and uniform value-free rejection. |
| T-07-19-11 | Elevation of Privilege | RuntimeWorktree interface expansion | high | mitigate | Keep exactly Attach/Publish/Prepare/Acknowledge/Resolve; the reflection contract test blocks Mutate, RecoverMutation, or any sixth method. |
| T-07-19-12 | Elevation of Privilege | loader global namespace | high | mitigate | Exact built-loader symbol-delta allowlist enumerates the two helpers and seven mutation parameters and rejects every unplanned global delta. |
</threat_model>

<verification>
After Task 3:

1. RED history: confirm Task 1 precedes both GREEN commits and every one of the seven named tests has an individual `--- FAIL:` record.
2. Focused CR-05 runtime/shell matrix: run all Task 2 and Task 3 commands.
3. Lost-response stress: `GOTOOLCHAIN=local go test -count=20 ./core/shell/zsh -run '^(TestWorktreeCheckoutLostResponseRecoversExactOutcome|TestWorktreeCheckoutCreateLostResponseRecoversExactOutcome|TestWorktreeResetHardLostResponseRecoversExactOutcome)$' -timeout=360s`.
4. Cross-plan closure regressions: `GOTOOLCHAIN=local go test -count=1 ./core/worktree ./core/store ./core/cli ./core/shell/zsh -run 'PortableOwnedTransport|SemanticEquality|FooterAllowsMarker|AuthenticatedRelease|Mutation|Checkout|ResetHard|RecoveredPersistence|PartialPatch|FirstExplicitSync|TwoShell' -timeout=360s`.
5. Full suite: `GOTOOLCHAIN=local go test -count=1 ./... -timeout=360s`.
6. Race suite: `GOTOOLCHAIN=local go test -race -count=1 ./core/worktree ./core/store ./core/cli ./core/shell/zsh -timeout=420s`.
7. Static/build gates: `GOTOOLCHAIN=local go vet ./... &amp;&amp; GOTOOLCHAIN=local golangci-lint run ./... &amp;&amp; GOTOOLCHAIN=local go build ./...`.
8. Runtime budget/performance: build the current binary, run `GOTOOLCHAIN=local scripts/perf-worktree.sh`, retain the absolute 250 ms ceiling, and confirm prompt hooks issue zero Git calls outside explicit mutation.
9. Exact-five-method/security surface: `GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^(TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations|TestRuntimeWorktreeFrameExactByteAndRecordBounds|TestRuntimeMutationFrameRejectsMalformedReplayedAndForeignAuthority)$' -timeout=180s` must keep the `RuntimeWorktree` reflection contract exactly at Attach/Publish/Prepare/Acknowledge/Resolve, plus process/output/state canary scans for capability, token, branch, source, path, and live values.
10. Exact loader-symbol surface: `GOTOOLCHAIN=local go test -count=1 ./core/cmd/zsh-pro -run '^TestMainIngestAllowsOnlyExactLoaderSymbols$' -timeout=120s`.
11. Native evidence dependency: verify `07-16-SUMMARY.md` contains the exact-SHA native macOS retained-zsh PASS record; a Darwin cross-build alone is not closure evidence and must not be substituted here.
</verification>

<acceptance_matrix>
## Single Consolidated Five-Blocker Closure Mapping

| Blocker | Owning plan | Required RED evidence | Terminal acceptance evidence |
|---|---|---|---|
| CR-01 macOS-disabled private transport | 07-16 | Portable retained-zsh contract fails at the production procfs gate | No production procfs dependency; exact owned FD/direct-child cleanup passes on Linux; exact implementation SHA has retained native macOS PASS evidence. |
| CR-02 positional live-state equality | 07-16 | Order-only equality and attach/repair tests fail while value differences remain meaningful | One identity-keyed/value-exact comparator drives attach, repair, replace, and behind truth; ordered PATH/FPATH contents remain byte-significant. |
| CR-03 payload text collides with footer validation | 07-16 | Real emitted payload containing all footer marker strings fails the raw-count validator | Exact trusted final suffix accepts quoted marker text and rejects missing/reordered/changed/duplicated/trailing structure. |
| CR-04 historical shells exhaust capacity | 07-17 | Authenticated release and 129th-shell tests fail without a lifecycle | Settled authenticated retirement, deterministic tombstone/receipt reclamation, protected-state non-eviction, replay/concurrency, and retained-zsh churn pass. |
| CR-05 committed mutation reported as generic failure | 07-18 and 07-19 | Journal/ref/state fault matrix plus three separate retained-zsh lost-response tests fail before persistence/runtime changes | Journal precedes every ref/state effect; canonical semantic receipt and reopen recovery return exact truth; checkout, checkout -b, and reset each use mode-aware RuntimeWorktree.Attach under the unchanged five-method boundary, return by 250 ms, never replay mutation, converge once, and expose only exactly allowlisted loader symbols. |
</acceptance_matrix>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One canonical Git-like shared environment with truthful independent-terminal mutation and recovery | All three tasks plus consolidated acceptance matrix | COVERED |
| REQ | WORK-03 | Strict checkout/checkout -b/reset frame, result, and recovery flow | COVERED |
| REQ | SYNC-01 | One bounded safe-boundary recovery/reconciliation pipeline | COVERED |
| REQ | SYNC-02 | Exact revision-aware result replay, no duplicate mutation, and acknowledgement | COVERED |
| RESEARCH | 250 ms deadline, descriptor binding, exact five private operations, value-free errors, no hook Git, exact acknowledgement | Scope fence, actions, threats, and verification gates | COVERED |
| CONTEXT | No Phase 07 CONTEXT.md; user supplied the locked consolidated closure structure in this revision | Four-plan dependency chain, persistence/runtime split, native checkpoint, and WR-01 exclusion are implemented exactly | COVERED |
| REVIEW | CR-01 through CR-05 | Single consolidated mapping assigns CR-01/02/03 to 07-16, CR-04 to 07-17, CR-05 to 07-18/19 | COVERED |
| REVIEW | WR-01 `LiveCaptureSource` warning | Explicitly excluded by user direction | EXCLUDED |
| LEGACY REGISTRY | ROADMAP/REQUIREMENTS omit the Phase 07 row/IDs | Existing reviewed Phase 07 IDs are retained; no registry rewrite was authorized | EXCLUDED |
</source_coverage_audit>

<success_criteria>
- Task 1 commits seven individually proven failing tests before production changes; Tasks 2 and 3 make them GREEN in ordered commits with normal hooks.
- The dependency chain is exactly 07-16 -> 07-17 -> 07-18 -> 07-19, and the native exact-SHA checkpoint remains a hard prerequisite.
- Checkout, checkout -b, and reset each pass deterministic retained-zsh post-completion response-loss tests under the original 250 ms deadline.
- Result-only recovery rejects malformed/replayed/foreign authority, never repeats mutation, and returns exact committed/recovery-required/revision truth before one reconciliation.
- `RuntimeWorktree` remains exactly Attach/Publish/Prepare/Acknowledge/Resolve; mutate and mutation-result route through its private mode-aware Attach envelope and private service adapter.
- `TestMainIngestAllowsOnlyExactLoaderSymbols` passes with exactly the two planned helpers and seven planned mutation parameters added to its allowlists.
- The single five-blocker acceptance matrix is satisfied; full, race, vet, lint, build, performance, exact-five, security, and native-evidence gates pass; WR-01 remains excluded.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-19-SUMMARY.md` when done. Record the seven RED results, two GREEN commit SHAs, exact per-verb lost-response outcomes, and the consolidated CR-01 through CR-05 acceptance rows. Do not update STATE.md, ROADMAP.md, REQUIREMENTS.md, prior plans/summaries, stash, or unrelated paths.
</output>
