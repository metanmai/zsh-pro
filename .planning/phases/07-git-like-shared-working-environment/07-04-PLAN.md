---
phase: 07-git-like-shared-working-environment
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - core/activate/plan.go
  - core/activate/builder.go
  - core/activate/diff.go
  - core/activate/live_patch_test.go
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/zsh/regen.go
  - core/shell/zsh/regen_test.go
autonomous: true
requirements: [WORK-02, WORK-03, SYNC-01]
must_haves:
  truths:
    - "[WORK-02/WORK-03] The effective commit input is the complete source Profile plus a versioned typed final-state projection/tombstones, never a lossy mutation of Profile.Entries."
    - "[WORK-03] Exact runtime bytes, present-empty values, export state, multiline functions, ordered PATH/FPATH, options, and removals have a canonical commit-ready representation."
    - "[WORK-03] `core/shell/zsh/emit.go` owns every added executable zsh state-mutation command/string; `RegenerateWorktree` validates and lowers typed projection states through emit-owned helpers without assembling syntax in regen.go."
    - "[SYNC-01] A live patch carries forward mutations and one replacement reverse ownership plan, so apply then checkout/deactivate restores exact prior managed state."
    - "Legacy source-only Profile regeneration remains compatible, while malformed/partial committed projections fail closed without source output."
  artifacts:
    - path: "core/activate/plan.go"
      provides: "Closed forward-plus-replacement-reverse LivePatch contract"
      contains: "type LivePatch struct"
    - path: "core/activate/builder.go"
      provides: "Source-plus-overlay committed projection construction"
      contains: "func BuildEffective"
    - path: "core/activate/diff.go"
      provides: "Validated reversible live patch planning"
      contains: "func BuildLivePatch"
    - path: "core/shell/zsh/regen.go"
      provides: "Validated committed-worktree traversal and typed delegation into emit-owned rendering"
      contains: "func (Provider) RegenerateWorktree"
    - path: "core/shell/zsh/emit.go"
      provides: "Sole scalar/list/alias/function/option set-and-remove zsh state-mutation rendering for committed worktrees and live patches"
      contains: "func emitLiveIdentityState"
  key_links:
    - from: "model.Profile plus OverlayEntry"
      to: "model.CommittedWorktree"
      via: "activate.BuildEffective preserves source order and projects final state/tombstones"
      pattern: "BuildEffective"
    - from: "model.CommittedWorktree"
      to: "zsh.Provider.RegenerateWorktree"
      via: "typed shell.WorktreeRegenerator validation and traversal with no command literals in regen.go"
      pattern: "RegenerateWorktree"
    - from: "zsh.Provider.RegenerateWorktree"
      to: "core/shell/zsh/emit.go"
      via: "typed identity-state/tombstone helpers that solely own quoting, set, and removal command rendering"
      pattern: "emitLiveIdentityState"
    - from: "before/after LiveIdentityState"
      to: "activate.LivePatch"
      via: "complete validation before forward and replacement reverse plans"
      pattern: "BuildLivePatch"
---

<objective>
Make live effective state faithfully reversible and commit-ready.

Purpose: Close the gap between runtime semantic values and the existing source-shaped Store regenerator without violating the sole-zsh-codegen invariant.
Output: Committed projection builder, reversible live patch planner, and canonical zsh-owned worktree regenerator.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-01-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-02-SUMMARY.md
@core/activate/plan.go
@core/activate/builder.go
@core/activate/diff.go
@core/shell/zsh/emit.go
@core/shell/zsh/regen.go

<interfaces>
- `BuildEffective(base model.Profile, overlay []model.OverlayEntry) (model.CommittedWorktree, error)` retains the complete source profile and emits a separate projection.
- `shell.WorktreeRegenerator` from Plan 07-02 is `RegenerateWorktree(model.CommittedWorktree) ([]byte, error)`; `zsh.Provider` implements it without changing legacy `Regenerate(model.Entry) string`.
- `BuildLivePatch(before, after []model.LiveIdentityState)` returns typed forward and replacement reverse plans only; executable syntax remains concrete-zsh-owned.
</interfaces>
</context>

## Artifacts this phase produces

- `core/activate/plan.go` gains `LivePatch` and closed shell-neutral operations for exact live set/remove/list/option transitions.
- `core/activate/builder.go` gains `BuildEffective` producing a validated `model.CommittedWorktree` from source Profile plus overlay/tombstones.
- `core/activate/diff.go` gains `BuildLivePatch` with deterministic forward and replacement reverse ownership.
- New `core/activate/live_patch_test.go` covers equality, repeated-source tombstones, every category, inverse ownership, and malformed all-or-nothing behavior.
- `core/shell/zsh/regen.go` gains `Provider.RegenerateWorktree`, which validates/traverses committed final state and delegates every value/removal/option render request as typed data to emit-owned helpers.
- `core/shell/zsh/emit.go` gains the reusable typed committed/live identity render helpers and remains the sole owner of all added executable zsh state-mutation commands and strings.
- `core/shell/zsh/{regen,emit}_test.go` cover quoted/multiline/present-empty/export/functions/ordered lists/options/tombstones, `zsh -n`, live apply/read/deactivate semantics, and a structural ownership test that fails when regen.go contains added runtime command construction.
- No persistent DTO or Git/CLI/runtime operation is introduced in this plan.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build committed projection and replacement reverse ownership</name>
  <files>core/activate/plan.go, core/activate/builder.go, core/activate/diff.go, core/activate/live_patch_test.go</files>
  <read_first>
    - core/model/worktree.go: committed projection, values, overlays, and tombstones
    - core/activate/plan.go: private operation marker convention
    - core/activate/builder.go: final-source-occurrence, list composition, export, and representability rules
    - core/activate/diff.go: complete-target validation and deterministic order
    - core/activate/diff_test.go: malformed and inverse operation tests
    - .planning/phases/04-manifest-builder-emit/04-19-SUMMARY.md: persisted source-shape/no-residue invariants
  </read_first>
  <action>
    Preserve the current uncommitted `core/activate/tokenfree_test.go` and all source changes. Add closed semantic live operations and `LivePatch{Forward, ReplacementReverse}`. Implement `BuildEffective` as full source plus versioned final projection/tombstones; never rewrite or filter the source Entries slice. Preserve export presence, exact scalar/body bytes, ordered list elements, option booleans, and pinned SecretRef absence from projection. Implement `BuildLivePatch` by validating complete before/after states, normalizing each identity once, and producing exact forward plus inverse ownership. Add all-category, repeated-source, empty, equality, malformed, deterministic, and apply-then-reverse model tests.
  </action>
  <acceptance_criteria>
    - The committed document contains both complete source history and exact final live state/tombstones.
    - Every forward identity change has one inverse restoring the prior managed state.
    - Malformed input yields no partial projection or patch.
    - Activate remains shell-agnostic and token-free.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/activate -run 'Test(BuildEffective|BuildLivePatch|LivePatch|ActivateSourceIsTokenFree)' -count=1</automated>
  </verify>
  <done>Effective live state is represented exactly for both commit and reversible apply without source-IR corruption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render canonical committed worktree source in the zsh provider</name>
  <files>core/shell/zsh/emit.go, core/shell/zsh/emit_test.go, core/shell/zsh/regen.go, core/shell/zsh/regen_test.go</files>
  <read_first>
    - core/shell/zsh/regen.go: legacy Entry regeneration and representability fallback
    - core/shell/zsh/emit.go: safe names, zquote, removal, list, and option codegen patterns
    - core/shell/zsh/regen_test.go: legacy compatibility and real-zsh conventions
    - core/model/worktree.go: committed projection schema/value validation
    - core/shell/provider.go: WorktreeRegenerator interface
  </read_first>
  <action>
    Preserve the ROADMAP tier invariant exactly: add every new executable zsh state-mutation command/string and all scalar/alias/function/list/option set-and-remove lowering in `core/shell/zsh/emit.go`. Add typed emit-owned helpers that accept validated live identity state/tombstone operations and return canonical source using the existing name validators and quoting primitives. Implement `Provider.RegenerateWorktree` in `regen.go` only as complete-document validation, deterministic projection traversal, SecretRef/redaction checks, and delegation of typed operations to those emit-owned helpers; regen.go must not assemble removal commands, option commands, declarations, assignments, or quoting fragments. Tombstones delegate explicit removals so checkout/activation cannot reveal shadowed source entries. Preserve source Profile JSON, legacy `Regenerate` byte compatibility, exact present-empty/export/multiline/list semantics, and captured-literal exclusion for SecretRefs. Add behavior tests with zsh 5.9, `zsh -n`, Read-style reconstruction, and deactivate residue for every category, plus a Go structural test that parses regen.go and rejects added executable state-mutation command literals/format construction outside calls to emit-owned typed helpers.
  </action>
  <acceptance_criteria>
    - Quoted, multiline, empty, exported, function, list, option, and removal cases regenerate behavior-exact source through helpers defined in emit.go.
    - Existing legacy Profile regeneration is unchanged.
    - Invalid projections and hostile names emit zero bytes.
    - Structural tests prove regen.go contains validation/traversal/delegation only and every added executable committed-worktree string originates in emit.go.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh -run 'Test(RegenerateWorktree|Regenerate|CommittedProjection|Tombstone|WorktreeSyntaxOwnedByEmit)' -count=1</automated>
  </verify>
  <done>The exact live projection has a canonical zsh-owned committed source representation that can be read, applied, checked out, and deactivated faithfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live projection -> activate plan | Captured bytes become semantic executable intent |
| committed document -> zsh source | Typed values become source later evaluated in a shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-15 | Elevation of Privilege | RegenerateWorktree/emit-owned lowering | high | mitigate | Complete validation, hostile-name rejection, canonical zquote, emit.go-only command ownership, structural ownership tests, zero-output tests, and zsh -n in Task 2. |
| T-07-16 | Information Disclosure | SecretRef projection | high | mitigate | Pinned identities absent from live projection and secret canary absence from generated bytes in Tasks 1-2. |
| T-07-17 | Tampering | tombstone/repeated source | high | mitigate | Separate source/projection schema, explicit removal rendering, and live Read/checkout/deactivate tests. |
| T-07-18 | Repudiation | stale reverse ownership | high | mitigate | Forward and replacement reverse produced together and apply/reverse all-category tests in Task 1. |

ASVS Level 1 applies; every high-severity codegen/projection gate is blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/activate ./core/shell/zsh -count=1`
- `GOTOOLCHAIN=local go vet ./core/activate ./core/shell/zsh`
- Structural Go test confirms new committed-worktree executable syntax exists only in `core/shell/zsh/emit.go`; regen.go only validates, traverses, and calls typed emit helpers.
</verification>

<success_criteria>
- Live state can be committed and reconstructed without loss or invented source semantics.
- Live patch apply and later checkout/deactivate preserve exact reverse ownership and zero residue.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-04-SUMMARY.md` when done.
</output>
