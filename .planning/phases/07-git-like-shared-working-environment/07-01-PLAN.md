---
phase: 07-git-like-shared-working-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/model/worktree.go
  - core/model/worktree_test.go
  - core/worktree/diff.go
  - core/worktree/diff_test.go
autonomous: true
requirements: [WORK-02, SYNC-02]
must_haves:
  truths:
    - "[WORK-02] Supported live state has a bounded, lossless typed representation in which present-empty scalar/list/function values and explicit removals are distinct."
    - "[WORK-02] Identity equality is exact kind plus validated name; PATH/FPATH preserve element boundaries, duplicates, empties, and order."
    - "[WORK-02] Snapshot validation accepts 0, 1, 2 MiB, and 10,000-record boundaries and rejects either maximum plus one without partial output."
    - "[SYNC-02] Semantic diffs are per identity, reduce final occurrence, and have no whole-profile replacement request."
    - "The committed worktree contract stores the complete source Profile beside a versioned final-state projection and tombstones rather than mutating Profile.Entries into a map."
    - "Attachment and conflict-resolution requests carry exact baselines/revisions/tokens without putting captured values in status, conflict, exclusion, or forensic metadata."
  artifacts:
    - path: "core/model/worktree.go"
      provides: "Live identities/values/snapshots/deltas, committed projection, attachment, resolution, revision, conflict, and status contracts"
      contains: "type CommittedWorktree struct"
    - path: "core/worktree/diff.go"
      provides: "Complete-input semantic validation, per-identity diff, overlay application, and categorized ordering"
      contains: "func DiffSnapshot"
  key_links:
    - from: "model.LiveSnapshot"
      to: "worktree.DiffSnapshot"
      via: "typed semantic values rather than command text or serialized JSON"
      pattern: "DiffSnapshot"
    - from: "model.Profile plus model.LiveProjection"
      to: "model.CommittedWorktree"
      via: "separate source and final-state ownership with explicit tombstones"
      pattern: "CommittedWorktree"
  prohibitions:
    - statement: "MUST NOT make source-statement history the owner of live state when the observable shell has one final value per supported identity."
      category: fidelity
      status: unresolved
      flagged: true
---

<objective>
Define the lossless shell-neutral worktree contract and deterministic semantic diff.

Purpose: Give admission, persistence, Git publication, runtime hooks, attachment, and conflict resolution one exact data model before any side effects exist.
Output: Worktree domain types, committed source-plus-projection contract, bounded validators, and per-identity diff/projection helpers.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/model/profile.go
@core/model/manifest.go

<interfaces>
- Preserve `model.Profile` as the complete source-ordered ingest IR.
- Supported kinds are env, alias, function, PATH, FPATH, and option; no PWD/jobs/history/buffer/process-tree variant exists.
- `CommittedWorktree` owns `Source model.Profile` plus a schema-versioned `Projection` of final states/tombstones. Shared revision coordination remains outside this committed Git DTO.
</interfaces>
</context>

## Artifacts this phase produces

- New `core/model/worktree.go`: `LiveKind`, `Identity`, `LiveValue`, `LiveIdentityState`, `LiveSnapshot`, `LiveChange`, `OverlayEntry`, `LiveProjection`, `CommittedWorktree`, `RevisionEvent`, `Conflict`, `Exclusion`, publish/pull/acknowledge/attach/resolve requests and results, `ResolutionToken`, `WorktreeStatus`, `CategorizedDiff`, `WorktreeCommitResult`, `MaxSnapshotBytes`, and `MaxSnapshotRecords`.
- New `core/worktree/diff.go`: `ValidateSnapshot`, `DiffSnapshot`, `ApplyOverlay`, `BuildProjection`, and `CategorizeDiff`.
- Tests pin exact value presence, ordering, bounds, defensive copies, tombstones, final occurrence, and value-free metadata.
- No CLI verb, persistent file, shell syntax, package dependency, or production side effect is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define live, committed, attachment, and resolution domain contracts</name>
  <files>core/model/worktree.go, core/model/worktree_test.go</files>
  <read_first>
    - core/model/profile.go: complete source IR and presence-aware runtime fields
    - core/model/manifest.go: pointer-presence and ordered-list conventions
    - core/model/category.go: fixed category order and enum conventions
    - .planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md: Patterns 1-4, workflow semantics, and resolved open questions
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: acknowledgement, attachment, conflict, and compaction requirements
  </read_first>
  <action>
    Preserve the current working tree before editing. Add every type named in the artifact inventory. Model one explicit value presence plus kind-selected scalar/list/option payload, so present-empty is never inferred from a zero value and tombstones are explicit. Give `CommittedWorktree` an exact schema version, full source Profile, and final projection. Define idempotent attach contracts carrying the validated initial snapshot without a delta, and resolution contracts carrying an exact conflict identity/token and caller snapshot without raw diagnostics. Define `ResolveShared` results so the loader can apply authoritative state and acknowledge through the existing pending-revision path rather than clearing a conflict early. Keep value-bearing types structurally absent from status/conflict/exclusion/forensic shapes. Set 2 MiB and 10,000-record caps, validate uint64 transitions without wrap, and defensively copy all slices/pointers.
  </action>
  <acceptance_criteria>
    - Empty, present-empty, and removed states are independently representable for every compatible kind.
    - Exact-cap/cap-plus-one and MaxUint64 tests are non-vacuous.
    - Attachment publishes no delta by type, and resolution cannot clear conflict evidence without an acknowledgement token.
    - Committed projection and value-free public metadata have distinct types.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/model -run 'Test(Live|Worktree|Committed|Attach|Resolve|Snapshot|Revision)' -count=1</automated>
  </verify>
  <done>The domain model is bounded, lossless, shell-neutral, and sufficient for every later end-to-end operation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute deterministic per-identity deltas and committed projections</name>
  <files>core/worktree/diff.go, core/worktree/diff_test.go</files>
  <read_first>
    - core/model/worktree.go: Task 1 final types and validators
    - core/activate/diff.go: validate-complete-before-plan and final-occurrence analog
    - core/model/profile.go: repeated source entries and representability semantics
    - .planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md: diff.go ownership and deterministic ordering
  </read_first>
  <action>
    Implement complete-input validation, final-occurrence normalization, exact semantic comparison, overlay application, committed projection construction, and categorized output. Scalars and bodies compare exact bytes, options compare boolean state, and lists compare ordered elements. Emit one add/update/remove per final identity in env/alias/function/PATH/FPATH/option then exact-name order. `ApplyOverlay` and `BuildProjection` must preserve source Profile order while making tombstones authoritative over shadowed earlier entries. Reject malformed inputs before returning any partial change or projection; do not compare JSON or source command text.
  </action>
  <acceptance_criteria>
    - Equal snapshots are no-ops and no whole-profile replacement path exists.
    - Final-occurrence and category/name ordering are stable while list element order remains untouched.
    - Tombstones prevent repeated source assignments from reappearing.
    - Invalid pairs return neither a partial diff nor partial projection.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/worktree -run 'Test(ValidateSnapshot|Diff|Overlay|Projection|Categorize)' -count=1</automated>
  </verify>
  <done>Every later mutation can operate on one deterministic per-identity projection without corrupting the source IR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live frame -> model | Untrusted names, values, counts, and framing become typed state |
| source Profile -> committed projection | Repeated statements and removals become final managed ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01 | Tampering | snapshot/value validation | high | mitigate | Closed kinds, compatible variants, NUL rejection, complete-input validation, overflow checks, and exact boundary tests in Task 1. |
| T-07-02 | Information Disclosure | conflict/status/attach/resolve metadata | high | mitigate | Separate value-free metadata types and canary reflection/serialization tests in Task 1. |
| T-07-03 | Tampering | repeated source projection | high | mitigate | Source-plus-projection separation, explicit tombstones, final-occurrence tests, and no in-place Profile rewrite in Task 2. |
| T-07-04 | Denial of Service | snapshot/diff transforms | medium | mitigate | 2 MiB/10,000-record caps, overflow-safe accounting, bounded deterministic transforms, and cap-plus-one tests. |

ASVS Level 1 applies; every high-severity control is a blocking automated gate.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/model ./core/worktree -count=1`
- `GOTOOLCHAIN=local go vet ./core/model ./core/worktree`
- Confirm neither package imports `core/shell/zsh` and no model type contains executable shell text.
</verification>

<success_criteria>
- WORK-02 and SYNC-02 have exact bounded contracts for values, identity deltas, tombstones, attachment, and resolution.
- Source IR and committed live projection remain separate and deterministically composable.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-01-SUMMARY.md` when done.
</output>
