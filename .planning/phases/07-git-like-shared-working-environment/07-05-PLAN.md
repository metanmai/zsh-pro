---
phase: 07-git-like-shared-working-environment
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - core/store/dto.go
  - core/store/dto_test.go
  - core/store/git.go
  - core/store/git_test.go
  - core/store/store.go
  - core/store/store_test.go
autonomous: true
requirements: [WORK-01, WORK-03, SYNC-02]
must_haves:
  truths:
    - "[WORK-01/WORK-03] Git stores a versioned CommittedWorktree containing the full redacted source Profile plus exact final projection/tombstones; legacy profile.json remains readable."
    - "[WORK-03] `ReadWorktreeRevision` reconstructs the exact committed DTO, and its regenerated profile.zsh preserves quoted/multiline/empty/export/function/list/option/removal behavior."
    - "[WORK-01/WORK-03] A PublishedRevision is accepted only when the exact object in this repository is a commit whose fixed `profile.json` and `profile.zsh` tree entries each exist exactly once as regular blobs; valid-shaped blob, tree, tag, duplicate, wrong-mode/type/path, and malformed-tree inputs are rejected before decode."
    - "[WORK-03] `CreateFrom` forks the exact current base and `CommitWorktree` commits every effective dirty identity by expected-base ref CAS with no staging selection."
    - "[SYNC-02] A ref mismatch is a visible conflict; proven ref publication plus uncertain state save remains committed/recovery-required evidence rather than a false rollback."
    - "SecretRef source entries stay redacted, pinned runtime literals never enter projection or Git, and existing secret backend compensation remains authoritative."
    - "Migration is bidirectional at the declared compatibility boundary: the new decoder reads legacy source-only DTOs, and a frozen legacy decoder reads the Source portion of newly written additive DTOs; partial/unknown projections fail closed without corrupting legacy source reads."
    - "Existing exported `Store.Current`, `Create`, `Checkout`, and `Commit` are explicitly deprecated compatibility adapters after their Phase-7 replacements land; they are not removed out from under historical callers, but no shared-worktree service or CLI interface may call them or treat their process-local inputs as authority."
  artifacts:
    - path: "core/store/dto.go"
      provides: "Backward-compatible versioned CommittedWorktree marshal/unmarshal"
      contains: "func MarshalCommittedWorktree"
    - path: "core/store/store.go"
      provides: "Exact worktree revision reads, current-base creation, and expected-base commit"
      contains: "func (s *Store) CommitWorktree"
    - path: "core/store/git.go"
      provides: "Existing exact-ref/CAS helpers plus minimal commit-type and fixed blob-tree validation for worktree revision reads"
      contains: "func (g gitRunner) updateRefCAS"
  key_links:
    - from: "model.CommittedWorktree"
      to: "profile.json and profile.zsh"
      via: "store-local DTO plus injected shell.WorktreeRegenerator"
      pattern: "MarshalCommittedWorktree"
    - from: "State.BaseOID"
      to: "Store.CommitWorktree"
      via: "commit-tree parent and update-ref expected-old CAS"
      pattern: "expectedBaseOID"
    - from: "exact Git object"
      to: "Store.ReadWorktreeRevision"
      via: "existing validGitObjectID/profileAtRevision foundations plus direct commit type and exact fixed-entry blob/tree validation; no symbolic caller ref or inherited GIT control"
      pattern: "ReadWorktreeRevision"
---

<objective>
Persist and publish exact committed worktree projections through the hardened Git Store.

Purpose: Make live state faithfully durable in Git while preserving legacy Profile reads, secret boundaries, exact-object recovery, and expected-ref concurrency.
Output: Versioned DTO compatibility, exact revision read, current-base branch creation, and CAS-protected worktree commit.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-04-SUMMARY.md
@core/store/dto.go
@core/store/store.go
@core/store/git.go
@core/store/secret.go

<interfaces>
- Existing `MarshalProfile`/`UnmarshalProfile`, `Read`, and `Commit` remain compatible for source-only historical objects.
- New `MarshalCommittedWorktree`/`UnmarshalCommittedWorktree` use additive presence/version fields in profile.json; incomplete current-version projections fail closed.
- `Store` receives the narrow `shell.WorktreeRegenerator` at construction without importing concrete zsh.
- `CommitWorktree(ctx, branch, expectedBaseOID, doc, message)` returns model-owned value-free `WorktreeCommitResult` with exact published OID on authoritative success.
</interfaces>
</context>

## Artifacts this phase produces

- `core/store/dto.go` adds presence-aware committed-worktree schema fields and deterministic marshal/unmarshal while legacy source-only decoding remains exact.
- `core/store/git.go` reuses the existing `validGitObjectID`, `profileAtRevision`, `revParse`, commit/tree construction, and `updateRefCAS` machinery, extending only the allowlisted direct object-type and two-fixed-entry tree validation needed by worktree reads. These existing helpers are not described or reimplemented as new plumbing.
- `core/store/store.go` adds `ReadWorktreeRevision`, `CreateFrom`, and `CommitWorktree`, using `RegenerateWorktree` for profile.zsh and expected-old ref CAS.
- Store tests cover new-reader/legacy-writer and frozen-legacy-reader/new-writer DTO compatibility, partial projection failures, exact runtime representation, commit/tree/blob/tag and malformed-tree validation, SHA-shaped validation, current-base create, ref races, secret canaries/compensation, and raw diagnostic suppression.
- No CLI verb, worktree policy, runtime hook, package, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist backward-compatible committed source plus live projection</name>
  <files>core/store/dto.go, core/store/dto_test.go</files>
  <read_first>
    - core/store/dto.go: current store-local profile DTO and presence-aware structural fidelity
    - core/store/dto_test.go: compatibility, nil/empty, and defensive-copy tests
    - core/model/worktree.go: CommittedWorktree and projection schema
    - core/shell/zsh/regen_test.go: Plan 07-04 canonical representation cases
  </read_first>
  <action>
    Extend the store-local top-level DTO with additive, explicitly versioned committed-worktree projection/tombstone fields. Keep shared branch/revision/shell acknowledgement out of Git. `MarshalCommittedWorktree` stores the full source profile and exact projection; `UnmarshalCommittedWorktree` validates all presence/kind/value/tombstone rules and fails closed on partial/unknown current data. Addresses review concern M4: historical profile.json without worktree fields continues through `UnmarshalProfile` byte/semantic compatibility, while `UnmarshalProfile` of a current object returns its complete Source without inventing projection state. Pin both directions with immutable legacy bytes and a frozen legacy-decoder test helper representing the pre-Phase-7 `UnmarshalProfile` contract: new code reads old bytes, and the legacy helper reads the complete Source from new additive bytes while ignoring only the new projection fields. Malformed/partial current projection must fail `UnmarshalCommittedWorktree` yet leave an explicitly requested legacy Source read intact and uncorrupted. Preserve deterministic JSON, one trailing newline, nil/present-empty, exact ordered arrays, and defensive copies. Add raw legacy/current/partial/unknown/tombstone/secret-canary matrices.
  </action>
  <acceptance_criteria>
    - Legacy source-only objects remain readable and round-trip unchanged.
    - Current committed worktree documents round-trip complete source and exact final projection/tombstones.
    - Partial or unknown current projection data cannot be inferred.
    - Runtime secret canaries cannot appear in DTO bytes.
    - Frozen legacy-reader/new-writer and new-reader/legacy-writer fixtures prove the declared migration boundary in both directions.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/store -run 'Test(.*CommittedWorktreeDTO|.*MarshalProfile|.*UnmarshalProfile|.*DTOCompatibility)' -count=1</automated>
  </verify>
  <done>Git has a lossless compatible DTO for exact live state without polluting shared coordination data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Read and publish committed worktrees by exact Git revision and CAS</name>
  <files>core/store/git.go, core/store/git_test.go, core/store/store.go, core/store/store_test.go</files>
  <read_first>
    - core/store/git.go: GIT environment scrub, argv allowlist, deadlines, raw-error mapping, commit-tree, and update-ref transactions
    - core/store/store.go: Create hard-coded main base, Read/Commit, secret preparation, and recovery outcomes
    - core/store/secret.go: redaction, pending backend writes, and compensation ownership
    - core/shell/provider.go: WorktreeRegenerator seam
    - .planning/phases/06-ingest-end-to-end/06-05-SUMMARY.md: exact commit evidence and complete Profile invariant
  </read_first>
  <action>
    Preserve the user's current uncommitted secret transport and Store hardening. Addresses review concerns CodeRabbit 8, M3, and M5: inventory `validGitObjectID`, `profileAtRevision`, `revParse`, commit/tree construction, and `updateRefCAS` as existing helpers and reuse/strengthen them; do not introduce duplicate CAS or generic exact-revision plumbing. Extend the exact Git allowlist only as needed to query the direct object type and inspect both fixed tree paths. Before reading bytes, require the supplied OID to name a commit object in this repository without accepting a blob/tree/tag as the revision; parse `ls-tree -z` structurally and require exactly one `100644 blob` entry each for `profile.json` and `profile.zsh`, with valid object IDs and no duplicate/unexpected/malformed record. Read those validated blob objects and ensure the DTO/regenerated source contract agrees. Add Store methods named in the artifact inventory. Mark existing `Current`, `Create`, `Checkout`, and `Commit` with explicit Go deprecation comments and tests documenting them as source-compatible legacy adapters only; Plan 07-06 removes them from every Phase-7 interface/call path, so they never select or mutate shared worktree authority. Validate branch/OID/message before effects; prepare existing source SecretRefs with current backend rules; reject any pinned live-secret literal in the projection; marshal the exact document; call the injected WorktreeRegenerator; stage only typed fixed blobs; commit-tree with expected parent; and publish through the existing CAS. Existing branch/ref race returns conflict, raw stderr/values never escape, and backend compensation follows existing ownership. Add real Git tests for quoted/multiline/empty/export/functions/lists/options/tombstones through CommitWorktree -> ReadWorktreeRevision -> Regenerate/apply, plus current-base create, SHA-1/SHA-256-shaped commits, valid-shaped blob/tree/tag rejection, missing/duplicate/wrong-mode/wrong-type/malformed fixed entries, ref races, legacy-adapter isolation, and secret canaries.
  </action>
  <acceptance_criteria>
    - ReadWorktreeRevision reconstructs exact source/projection only from a direct validated commit with both fixed regular-blob entries.
    - CommitWorktree preserves every live-state form and publishes only by expected-base CAS.
    - Branch creation uses supplied current base and never overwrites an existing ref.
    - Existing Git environment, timeout, argv, secret, and raw-error protections remain green.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/store -run 'Test(.*ReadWorktreeRevision|.*CreateFrom|.*CommitWorktree|.*UpdateRef|.*GitArgs|.*Secret)' -count=1</automated>
  </verify>
  <done>The Store can faithfully read and CAS-publish exact live worktree commits without staging or secret regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed projection -> Git blobs | Captured final state becomes durable history |
| typed refs/messages -> Git process | User-controlled identifiers cross subprocess boundary |
| secret backend -> worktree commit | Redacted source and live projection must not reintroduce literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-19 | Information Disclosure | profile.json/profile.zsh | high | mitigate | Pinned-secret rejection, existing SecretRef preparation, all-blob canary tests, and canonical regenerator in Tasks 1-2. |
| T-07-20 | Tampering | DTO compatibility/projection | high | mitigate | Version/presence validation, deterministic round-trip, partial-schema rejection, and exact live representation tests. |
| T-07-21 | Elevation of Privilege | Git argv/environment | high | mitigate | Existing environment scrub, exact argv allowlist, typed arguments, deadlines, and hostile tests in Task 2. |
| T-07-22 | Tampering | ref publication | high | mitigate | Expected-base commit parent/update-ref CAS and real race tests in Task 2. |
| T-07-22A | Tampering | exact revision object/tree | high | mitigate | Direct commit-type check, strict two-entry `ls-tree -z` parsing, blob mode/type/OID validation, and blob/tree/tag/malformed-tree rejection tests in Task 2. |

ASVS Level 1 applies; all high-severity DTO, secret, subprocess, and CAS gates block downstream workflow.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/store -count=1`
- `GOTOOLCHAIN=local go test -race ./core/store -run 'Test(.*CommitWorktree|.*UpdateRef)' -count=1`
- `GOTOOLCHAIN=local go vet ./core/store`
- Confirm `core/store` does not import concrete zsh and go.mod is unchanged.
</verification>

<success_criteria>
- The live overlay is committed and read faithfully through existing Git/secret security boundaries.
- Current-base branch and all-dirty commit operations have exact CAS outcomes.
- Legacy/current DTO migration and exact commit/tree/blob validation fail closed without inventing or corrupting source state.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-05-SUMMARY.md` when done.
</output>
