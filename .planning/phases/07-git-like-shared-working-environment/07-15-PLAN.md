---
phase: 07-git-like-shared-working-environment
plan: 15
type: execute
wave: 11
depends_on: ["07-11", "07-14"]
files_modified:
  - core/model/worktree.go
  - core/model/worktree_test.go
  - core/worktree/service.go
  - core/worktree/service_test.go
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/worktree_live_test.go
autonomous: true
requirements: [WORK-01, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "Attach truthfully distinguishes an exact-at-head initial snapshot from a clean-reconcile/behind snapshot; a durable head revision is never represented as locally applied when the shell differs."
    - "The private attach response carries the reconcile-required bit through Service -> runtime encoder -> strict loader parser without exposing a capability, snapshot value, or additional authority."
    - "One explicit `zsh-pro sync` in a newly sourced, unattached real zsh with a mismatched canonical generation performs attach, prepare, apply, fresh capture, and exact acknowledgement before returning success."
    - "If any step of that first call fails, sync returns nonzero, keeps applied revision zero/prior, exposes failure/behind, retains Plan 07-14 recovery ownership after mutation, and leaves the next command usable."
    - "Repeated exact-head attach/sync is idempotent, concurrent canonical advancement cannot be falsely acknowledged, and auto-apply remains a per-shell policy over the same single durable worktree."
  artifacts:
    - path: "core/model/worktree.go"
      provides: "Truthful AttachResult reconcile metadata"
      contains: "ReconcileRequired"
    - path: "core/worktree/service.go"
      provides: "Exact attach result derived from ShellState attach/applied state"
      contains: "ReconcileRequired"
    - path: "core/cli/runtime.go"
      provides: "Strict private attach reply encoding for head and reconcile state"
      contains: "encodeRuntimeAttachResult"
    - path: "core/shell/zsh/hook.go"
      provides: "First-call attach-to-reconcile control flow without false applied revision"
      contains: "ZP_WORKTREE_RECONCILE_REQUIRED"
    - path: "core/shell/zsh/worktree_live_test.go"
      provides: "One-call installed-loader real-zsh reconciliation oracle"
      contains: "TestWorktreeFirstExplicitSyncReconciles"
  key_links:
    - from: "ShellState.AttachState/AppliedRevision/Behind"
      to: "model.AttachResult.ReconcileRequired"
      via: "Service.Attach derives reply metadata in the same canonical transaction and replay receipt"
      pattern: "ReconcileRequired"
    - from: "runtime attach response"
      to: "parent-shell applied revision"
      via: "strict ZPWA parsing sets head-as-applied only when reconcile is false; mismatch stays applied zero"
      pattern: "ZP_WORKTREE_RECONCILE_REQUIRED"
    - from: "first public sync"
      to: "exact head acknowledgement"
      via: "attach mismatch -> prepare -> Plan 07-14 apply/recovery -> fresh capture -> acknowledge"
      pattern: "TestWorktreeFirstExplicitSyncReconciles"
---

<objective>
Make first explicit sync truthful and complete for a newly attached mismatched shell.

Purpose: The first user call must either reconcile through the exact existing pipeline or report failure/behind; it cannot assign head-as-applied and return a false success.
Output: Attach reconcile metadata, strict runtime transport, corrected loader control flow, and one-call real-zsh success/failure evidence.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-11-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-14-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md

<interfaces>
- Add `AttachResult.ReconcileRequired bool`. `Revision` remains the durable head. Exact attach returns false; `AttachStateCleanReconcile`, applied zero, and Behind return true. Operation replay returns the identical result.
- The private response remains `ZPWA` and gains one canonical boolean field. Reject missing, extra, noncanonical, or contradictory fields before mutating loader globals.
- Loader state `ZP_WORKTREE_RECONCILE_REQUIRED` is non-exported and parent-local. Exact attach may set applied=head. Mismatched attach sets applied=0 and must proceed directly to pull on the same explicit sync call, never publish the mismatched initial snapshot first.
- Successful exact acknowledgement clears reconcile-required and updates applied/baseline. Every failure preserves it and leaves Service status Behind.
- Consume Plan 07-11 durable admission when building the canonical target and Plan 07-14 recovery ownership when apply succeeds before a later failure.
- Preserve unrelated edits in all listed files; read current live source/tests immediately before editing.
</interfaces>
</context>

## Artifacts this phase produces

Production symbols and fields:

- `model.AttachResult.ReconcileRequired bool` in `core/model/worktree.go`.
- Strict `ZPWA 1 <head-revision> 1 <reconcile-required:0|1>` encoding in `core/cli/runtime.go`.
- Non-exported parent-shell `ZP_WORKTREE_RECONCILE_REQUIRED` and corrected first-sync/pull/ack/deactivate lifecycle in `core/shell/zsh/hook.go`.

Test-only artifacts:

- `TestAttachResultTruthfullyReportsCleanReconcile` and replay/concurrent-head subtests in `core/worktree/service_test.go`.
- `TestRuntimeAttachReplyCarriesReconcileState` in `core/cli/runtime_test.go`.
- `TestWorktreeFirstExplicitSyncReconciles` and `TestWorktreeFirstExplicitSyncFailureStaysBehind` in `core/shell/zsh/worktree_live_test.go`.

No new public verb/flag/config, dependency, worktree, staging/remotes behavior, external service, or synchronized PWD/jobs/history/buffer/process state is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make AttachResult report exact-at-head versus clean-reconcile truth</name>
  <files>core/model/worktree.go, core/model/worktree_test.go, core/worktree/service.go, core/worktree/service_test.go</files>
  <read_first>
    - core/model/worktree.go: AttachRequest/AttachResult and clone/validation conventions
    - core/model/worktree_test.go: result boundary and canonical model tests
    - core/worktree/service.go: Attach transaction, AttachStateAtHead/CleanReconcile, AppliedRevision, Behind, and receipt replay
    - core/worktree/service_test.go: attach mismatch/exact/replay/concurrent-head fixtures and Plan 07-11 late-C test
    - core/worktree/state.go: ShellState attach/applied/behind invariants
    - .planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md: false first-sync root cause
  </read_first>
  <behavior>
    - RED: exact initial baseline returns Revision=head, Attached=true, ReconcileRequired=false and durable shell AppliedRevision=head/Behind=false.
    - RED: mismatched initial baseline returns Revision=head, Attached=true, ReconcileRequired=true and durable shell AppliedRevision=0/Behind=true/AttachStateCleanReconcile.
    - RED: lost-response replay returns the same bit; a concurrently advanced head cannot be reported applied until exact prepare/ack.
  </behavior>
  <action>
    Preserve all existing model/service tests. Add and commit the failing attach-truth tests before production changes. Extend `AttachResult` and its clone/fingerprint/receipt assertions with `ReconcileRequired`. In `Service.Attach`, derive the bit from the same equality decision that sets ShellState: exact baseline sets false and applied=head; mismatch sets true, applied zero, and Behind. Store the complete result in the operation receipt so lost-response replay is identical. Do not derive the bit from caller claims, per-process profile variables, or registry instance age. In concurrent-head tests, require the transaction's actual head and keep mismatch behind until PreparePull/Acknowledge validates the exact target. Run focused tests GREEN, including Plan 07-11 late-C admission.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/worktree -run '^(TestAttachResultTruthfullyReportsCleanReconcile|TestFreshServiceLateShellUsesDurableAdmission)$' -timeout=25s</automated>
  </verify>
  <acceptance_criteria>
    - AttachResult and ShellState cannot contradict each other about locally applied head state.
    - Exact, mismatch, replay, and concurrent-head cases have deterministic tested results.
    - No new authorization or value-bearing field enters the result/receipt.
    - The RED test commit precedes the GREEN implementation commit.
  </acceptance_criteria>
  <done>The Service exposes enough truthful attach state for the loader to choose reconcile without guessing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry reconcile-required through the strict private attach reply</name>
  <files>core/cli/runtime.go, core/cli/runtime_test.go</files>
  <read_first>
    - core/cli/runtime.go: runRuntimeWorktree attach branch, encodeRuntimeAttachResult, frame limits, deadline checks, and value-free errors
    - core/cli/runtime_test.go: runtimeTestWorktree Attach fixture, exact response, malformed/trailing response, capability, timeout, and write-failure tests
    - core/cli/emitter.go: RuntimeWorktree Attach adapter; read only unless compilation requires the model field to pass through unchanged
    - core/model/worktree.go: Task 1 AttachResult contract
    - .planning/phases/07-git-like-shared-working-environment/07-REVIEW.md: false convergence finding and capability boundary
  </read_first>
  <behavior>
    - RED: exact and reconcile-required AttachResults encode canonical 0/1 reconcile fields alongside the durable head.
    - RED: zero revision, unattached result, noncanonical boolean, missing/extra/trailing response shape, timeout, and short write fail without a partial success.
    - RED: response contains no shell ID, capability, verifier, snapshot value, or error detail beyond the fixed protocol fields.
  </behavior>
  <action>
    Add and commit failing exact/mismatch response tests first. Extend `encodeRuntimeAttachResult` to output the fixed five-field ZPWA record with a canonical numeric reconcile bit. Preserve revision/Attached validation, the existing absolute runtime context, full-write enforcement, and generic value-free failure messages. Update runtime test authorities to state reconcile intent explicitly. Add negative response-shape fixtures consumed by the loader tests in Task 3; never echo frame input, credential, snapshot, or internal Service errors. Run focused runtime tests GREEN.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^TestRuntimeAttachReplyCarriesReconcileState$' -timeout=15s</automated>
  </verify>
  <acceptance_criteria>
    - Exact and mismatched attach responses are distinct, canonical, bounded, and value-free.
    - Invalid or partial shapes cannot cause a successful attach or leak private input.
    - Existing credential/replay/deadline gates remain unchanged and green.
    - The RED response contract commit precedes GREEN.
  </acceptance_criteria>
  <done>The private transport conveys truthful reconcile state without expanding capability or value exposure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile a mismatched unattached real shell in exactly one explicit sync call</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/shell/zsh/worktree_live_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: ensure_attached response parse, ATTACHED_NOW, publish/pull short-circuits, apply_transition, sync, line-finish, acknowledgement, and disable cleanup
    - core/shell/zsh/hook_test.go: attach response grammar, sync routing, fail-open, reply cleanup, and source-time no-process tests
    - core/shell/zsh/worktree_live_test.go: production binary/installed loader retained-zsh harness, canonical mutation helpers, status/behind oracle, and next-command sentinel
    - core/cli/runtime.go: Task 2 exact ZPWA response grammar
    - core/shell/zsh/emit.go: Plan 07-14 fail-fast/recovery transition contract
  </read_first>
  <behavior>
    - RED: after canonical state differs, a fresh `zsh -f` sources the installed loader and exactly one `zsh-pro sync` performs attach, prepare, apply, fresh capture, and acknowledge at head.
    - RED: an exact-at-head fresh shell performs attach only and returns success idempotently without a needless pending transition.
    - RED: prepare/apply/deadline/capture/ack failure on the first call returns nonzero, remains reconcile-required/applied-zero/behind, retains recovery if mutation occurred, and executes the next command sentinel.
  </behavior>
  <action>
    Add the one-call success and failure real-zsh contracts first, confirm the current ATTACHED_NOW shortcut falsely succeeds, and commit RED. Parse exactly the new five-field ZPWA record. Add non-exported `ZP_WORKTREE_RECONCILE_REQUIRED`; on exact attach set applied=head and false, while mismatch sets applied=0 and true. In `_zp_worktree_sync`, after ensure_attached return immediately only for an exact attach. For a mismatch, skip publish, capture the current parent state, call the normal PreparePull/Plan 07-14 apply/fresh-capture/Acknowledge pipeline in the same absolute Plan 07-13 deadline, then clear reconcile-required only after exact acknowledgement. Adjust `_zp_worktree_pull_impl` so ATTACHED_NOW does not suppress a required reconcile. On any failure preserve zero/prior applied revision, reconcile-required, Service Behind, operation IDs needed for safe retry, and recovery owner after mutation; explicit sync returns nonzero while prompt hooks remain fail-open. Clear the new variable on successful ack and worktree disable. In the installed-loader test, mutate canonical state through shell A, start a fresh mismatched shell C, source once, invoke exactly one public sync, and assert the canonical value/head plus status Behind=false. Inject each failure stage and assert non-convergence, no secret/process-local mutation, cleanup, and next-command usability. Re-run exact second sync to prove idempotency and a concurrent-head advancement case to prove stale target cannot acknowledge.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreeFirstExplicitSyncReconciles|TestWorktreeFirstExplicitSyncFailureStaysBehind)$' -timeout=30s</automated>
  </verify>
  <acceptance_criteria>
    - One public sync call on a fresh mismatched shell reaches the exact canonical head and only then reports success/converged.
    - Exact fresh attach is idempotent and does not create a pending transition or extra revision.
    - Every first-call failure remains visibly behind/nonzero, keeps applied revision zero/prior, retains recovery after mutation, and leaves the next command usable.
    - PWD/jobs/history/buffer/foreground/process-local state and secret-like values remain independent/excluded.
    - The RED real-zsh contract commit precedes GREEN.
  </acceptance_criteria>
  <done>First explicit sync can no longer return a false success after mismatched attachment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| initial shell snapshot -> Service.Attach | Untrusted local state is compared with canonical Shared and classified exact/behind. |
| AttachResult -> private runtime reply -> loader globals | Reconcile metadata crosses authenticated process transport into parent-shell control flow. |
| first sync apply -> acknowledgement | Parent mutation must not become durable applied truth until fresh exact verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-15-01 | Tampering | attach/applied state | high | mitigate | Derive reconcile bit and ShellState under one transaction; mismatch always applied zero/behind until exact ack. |
| T-07-15-02 | Spoofing | private ZPWA response/capability | high | mitigate | Strict fixed response grammar, existing verifier-only authorization, no caller-controlled applied claim, reject partial/trailing/noncanonical fields. |
| T-07-15-03 | Information Disclosure | attach reply/errors/tests | high | mitigate | Reply carries only version/head/booleans; errors and fixtures are value-free; capability/snapshot canaries remain absent. |
| T-07-15-04 | Repudiation | first sync success | high | mitigate | Success requires prepare token/fingerprint, Plan 07-14 apply, fresh capture, exact Acknowledge, and Behind=false evidence. |
| T-07-15-05 | Tampering | concurrent head advance | high | mitigate | Pending revision/fingerprint and acknowledgement match exact target; stale target remains behind and retries. |
| T-07-15-06 | Denial of Service | first reconcile | high | mitigate | Reuse Plan 07-13 single absolute 250 ms deadline and fail-open cleanup/next-command proof. |
| T-07-15-07 | Elevation of Privilege | reconcile-required flag | medium | mitigate | Parent-local non-exported boolean selects only existing authenticated Prepare/Acknowledge paths and grants no state/capability authority. |
| T-07-15-08 | Tampering | partial first-call mutation | high | mitigate | Reuse Plan 07-14 pre-mutation reverse ownership; failure cannot acknowledge or discard public recovery. |

ASVS L1 gate: every high-severity mitigation above is blocking; the plan is incomplete until its automated proof passes.
</threat_model>

<verification>
- Run all task commands, then `GOTOOLCHAIN=local go test -count=1 ./core/model ./core/worktree ./core/cli ./core/shell/zsh -timeout=60s`.
- Inspect the real-zsh fixture's call log: one public sync; one attach allocate/commit; when mismatched exactly one prepare/apply/ack sequence; no publish before reconcile.
- Assert canonical/head/applied/behind agreement after success and zero/prior applied plus behind/error after every injected failure.
</verification>

<success_criteria>
- The verification report's first-explicit-sync truth is green through the installed loader and a retained real zsh.
- Attach metadata, runtime reply, loader globals, Service ShellState, and public status agree in exact, mismatch, replay, concurrent-head, success, and failure cases.
- One canonical durable worktree remains the sole truth; auto-apply is only a per-shell policy and no multiple-worktree/staging/remotes/process-state scope enters the implementation.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | Independent terminals converge safely at explicit/safe boundaries without false applied state | COVERED by truthful attach metadata and one-call full reconcile. |
| REQ | WORK-01, SYNC-01, SYNC-02 | COVERED explicitly. WORK-02/WORK-03 remain covered by Plan 07-10 and are not regressed; Plan 07-11 supplies durable admitted target identities. |
| RESEARCH | Attach/reconcile state machine, exact pending fingerprint/ack, per-shell auto-apply, fail-open parent mutation | COVERED through existing canonical pipeline and Plans 07-13/07-14 guarantees. |
| CONTEXT | No Phase 7 CONTEXT.md; assumption delta prohibits multiple worktrees and makes auto-apply a per-shell policy | COVERED as a no-change constraint in interfaces/tests. |
| SPECless fallback | First-call boundary precision, exact/no-op idempotency, concurrent head advancement | COVERED explicitly; executed Plan 07-10 remains the phase-level disposition for the complete 21-edge matrix. |

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-15-SUMMARY.md` when done.
</output>
