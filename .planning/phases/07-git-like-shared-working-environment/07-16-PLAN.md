---
phase: 07-git-like-shared-working-environment
plan: 16
type: execute
wave: 12
depends_on: ["07-12", "07-15"]
files_modified:
  - core/shell/zsh/hook.go
  - core/shell/zsh/worktree_live_test.go
  - core/worktree/state.go
  - core/worktree/state_test.go
  - core/worktree/service_test.go
  - core/cli/runtime.go
  - core/cli/runtime_test.go
autonomous: false
requirements: [WORK-02, WORK-03, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "[CR-01 / WORK-02 / SYNC-01 / SYNC-02] The retained-zsh transport owns and closes only descriptors and unreaped child processes created by that invocation, has no production procfs dependency, stays within one absolute 250 ms budget, and works natively on Linux and macOS."
    - "[CR-02 / WORK-02 / SYNC-01 / SYNC-02] Canonical live-state equality compares records by identity rather than slice position while preserving presence, exact scalar bytes, kind/name, option state, and ordered PATH/FPATH element semantics."
    - "[CR-03 / WORK-02 / WORK-03 / SYNC-01 / SYNC-02] Runtime transition validation authenticates the exact final structural footer without counting marker-looking strings inside legitimate quoted payload."
    - "Phase 07 cannot advance to Plan 07-17 until native macOS retained-zsh evidence for the exact committed Plan 07-16 SHA is supplied and recorded; Darwin cross-compilation is not completion evidence."
  artifacts:
    - path: core/shell/zsh/hook.go
      provides: "portable descriptor and direct-child lifecycle in _zp_worktree_invoke and _zp_worktree_abort_transport"
    - path: core/worktree/state.go
      provides: "single canonical semantic live-state equality authority"
    - path: core/cli/runtime.go
      provides: "exact structural transition-footer validation"
    - path: core/shell/zsh/worktree_live_test.go
      provides: "portable retained-zsh transport oracle plus stronger Linux-only adversarial observer"
    - path: core/worktree/state_test.go
      provides: "order-insensitive identity/value equality contract"
    - path: core/cli/runtime_test.go
      provides: "payload/footer collision and malformed-footer contract"
  key_links:
    - from: core/shell/zsh/hook.go
      to: core/shell/zsh/worktree_live_test.go
      via: "numeric descriptor ownership, direct child PID capture, bounded TERM/KILL/reap, and post-timeout shell sentinel"
      pattern: "_zp_worktree_(invoke|abort_transport|close_coproc_endpoints|pid_owned)"
    - from: core/worktree/state.go
      to: core/worktree/diff.go
      via: "equalLiveStates reuses the same identity/value semantics as DiffSnapshot"
      pattern: "equalLiveStates|DiffSnapshot|EqualLiveValue"
    - from: core/cli/runtime.go
      to: core/shell/zsh/emit.go
      via: "validateRuntimePatchPayload derives and matches the emitter's exact trusted footer suffix"
      pattern: "validateRuntimePatchPayload|EmitRuntimeTransition|RuntimePatchMetadata"
---

<objective>
Close CR-01, CR-02, and CR-03 without weakening the Phase 07 security and latency contracts, then require native macOS retained-zsh evidence against the exact committed implementation SHA.

Purpose: Remove the Linux-only production transport gate, make semantic convergence independent of record admission order, and stop legitimate payload marker text from colliding with protocol validation.
Output: Portable owned transport, canonical semantic equality, structural footer validation, focused RED/GREEN evidence, cross-build evidence, and a blocking native-Darwin verification record.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-12-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-13-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-15-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@scripts/verify-phase06-macos-runtime.sh
@core/shell/zsh/hook.go
@core/shell/zsh/worktree_live_test.go
@core/worktree/state.go
@core/worktree/diff.go
@core/worktree/state_test.go
@core/worktree/service_test.go
@core/cli/runtime.go
@core/cli/runtime_test.go
@core/shell/zsh/emit.go
</context>

<interfaces>
Current blocker seams that the executor must preserve or replace deliberately:

- `core/shell/zsh/hook.go::_zp_worktree_close_coproc_endpoints` enumerates `/proc/$$/fd`; `_zp_worktree_pid_owned` consults `/proc/$$/task/$$/children`; `_zp_worktree_invoke` rejects hosts without `/proc/$$/fd`.
- `_zp_worktree_invoke` already records helper/writer/closer PIDs and descriptors and owns the one 250 ms absolute deadline. Preserve that deadline and the final 25 ms termination reserve.
- `core/worktree/state.go::equalLiveStates` compares records positionally. `core/worktree/diff.go::DiffSnapshot` already validates and normalizes record identity while `model.EqualLiveValue` preserves exact value semantics; ordered list contents must not be normalized.
- `core/cli/runtime.go::validateRuntimePatchPayload` already checks the exact expected suffix, then incorrectly counts footer marker substrings throughout the complete program. `core/shell/zsh/emit.go::EmitRuntimeTransition` is the footer format authority.
- `scripts/verify-phase06-macos-runtime.sh <exact-implementation-sha>` is the existing self-sealing native-macOS harness: it checks the exact SHA, a clean tracked/untracked input set, Darwin host/target, local Go toolchain, and exact named tests. Reuse it as the native environment/SHA gate before the Phase 07 retained-zsh tests; do not treat a cross-build or a stubbed harness test as native evidence.
</interfaces>

<scope_fence>
- Close only CR-01, CR-02, and CR-03 in this plan. CR-04 is Plan 07-17; CR-05 is split between Plans 07-18 and 07-19.
- Keep WR-01 (`Provider.LiveCaptureSource`) documented and out of this closure pass.
- Preserve descriptor-bound store authority, capability secrecy, value-free replies/errors, whole-frame bounds, 10,000-record and 2 MiB caps, exact acknowledgement, first-lock-wins conflict behavior, reverse ownership, and the absence of process-local Git or ambient root discovery in prompt hooks.
- Do not add a dependency, broaden PID/process-group targeting, relax zsh syntax validation, sort PATH/FPATH elements, or extend the 250 ms parent-shell budget.
</scope_fence>

<scope_budget>
- Plan budget: 3 tasks, 7 unique implementation/test files, and one mandatory evidence checkpoint; no task owns more than 4 code/test files.
- Task 1: four blocker-specific RED tests across 4 files. Task 2: three independent GREEN edits across 3 production files. Task 3: native macOS evidence only; it changes no implementation file.
- The native Darwin gate is intentionally sequential and blocking. Plan 07-17 cannot begin until the exact Task 2 SHA has native retained-zsh PASS evidence.
</scope_budget>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit focused failing contracts for CR-01, CR-02, and CR-03</name>
  <files>core/shell/zsh/worktree_live_test.go, core/worktree/state_test.go, core/worktree/service_test.go, core/cli/runtime_test.go</files>
  <behavior>
    - CR-01: A retained zsh performs attach, publish, prepare/apply/acknowledge, timeout cancellation, and a subsequent valid transition without requiring procfs; only exact invocation-owned descriptors and unreaped direct children are closed/signalled.
    - CR-01: Linux retains the Plan 07-13 survivor/descriptor/PID-reuse negative controls; portable assertions run on Darwin without a skip and prove shell continuation inside 250 ms.
    - CR-02: Reordering top-level records with identical identities and values is equal, while presence, scalar bytes, kind/name, option state, and ordered PATH/FPATH differences remain unequal.
    - CR-02: Attach, repair, behind/status, commit readback, and generation replacement use the same semantic equality truth when later admission inserts an earlier-category identity.
    - CR-03: All five footer-assignment marker strings embedded in a scalar, alias, and multiline function survive real emission, validation, `zsh -n`, and execution.
    - CR-03: Missing, changed, reordered, duplicated, or trailed structural footer fields, mismatched metadata, incomplete frames, and invalid zsh still fail closed.
  </behavior>
  <action>
    RED CR-01: Add `TestWorktreePortableOwnedTransport` as the blocker-specific failing contract. Refactor `TestWorktreeAbsoluteDeadlineAdversarialTransport` only as a retained Linux regression observer; it is not a RED assertion and is not required to fail. Inject an unrelated/reused PID and assert it is never signalled. Make the new portable test Darwin-native-runtime-capable with no platform skip. Run it against current production, confirm failure at the procfs production gate rather than build/setup failure, and commit only the test change as `test(07-16): add failing portable transport contract`.

    RED CR-02: Add `TestEqualLiveStatesIsOrderIndependentAndValueExact` in `state_test.go` and `TestSemanticEqualityDrivesAttachRepairAndBehind` in `service_test.go`. Cover reordered records, later admission from an earlier category, present-empty versus absent, byte-different scalars, kind/name/option changes, and PATH/FPATH duplicates, empty adjacent elements, and reordering. Confirm the order-only cases fail at positional equality, then commit as `test(07-16): add failing semantic equality contract`.

    RED CR-03: Add `TestRuntimeTransitionFooterAllowsMarkerTextInManagedPayload` in `runtime_test.go`. Generate the program through `zsh.Provider.EmitRuntimeTransition`; do not hand-author a supposedly valid frame. Place every reply marker in environment, alias, and multiline function payloads, then mutate each structural suffix field and append a trailer for negative cases. Confirm valid payload currently fails only because of whole-program substring counting, then commit as `test(07-16): add failing structural footer contract`.

    Do not edit production in this task. Each RED commit must compile, fail the named behavioral assertion for its blocker, use normal hooks, and precede its matching GREEN commit.
  </action>
  <verify>
    <automated>bash -o pipefail -c "set -e; run_red() { red_pkg=\$1; red_name=\$2; red_log=\$3; set +e; GOTOOLCHAIN=local go test -count=1 \"\$red_pkg\" -run \"^\${red_name}\$\" -timeout=120s &gt;\"\$red_log\" 2&gt;&amp;1; red_rc=\$?; set -e; cat \"\$red_log\"; test \"\$red_rc\" -ne 0; ! grep -Eq -- 'undefined:|build failed' \"\$red_log\"; }; red1=\$(mktemp); red2=\$(mktemp); red3=\$(mktemp); red4=\$(mktemp); trap 'rm -f \"\$red1\" \"\$red2\" \"\$red3\" \"\$red4\"' EXIT; run_red ./core/shell/zsh TestWorktreePortableOwnedTransport \"\$red1\"; grep -Eq -- '--- FAIL: TestWorktreePortableOwnedTransport([[:space:]]|$)' \"\$red1\"; run_red ./core/worktree TestEqualLiveStatesIsOrderIndependentAndValueExact \"\$red2\"; grep -Eq -- '--- FAIL: TestEqualLiveStatesIsOrderIndependentAndValueExact([[:space:]]|$)' \"\$red2\"; run_red ./core/worktree TestSemanticEqualityDrivesAttachRepairAndBehind \"\$red3\"; grep -Eq -- '--- FAIL: TestSemanticEqualityDrivesAttachRepairAndBehind([[:space:]]|$)' \"\$red3\"; run_red ./core/cli TestRuntimeTransitionFooterAllowsMarkerTextInManagedPayload \"\$red4\"; grep -Eq -- '--- FAIL: TestRuntimeTransitionFooterAllowsMarkerTextInManagedPayload([[:space:]]|$)' \"\$red4\""</automated>
  </verify>
  <done>Three focused test commits exist before production changes; each compiles and fails for the intended current blocker, and every exactness/security negative remains represented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement portable ownership, canonical equality, and exact footer suffix validation</name>
  <files>core/shell/zsh/hook.go, core/worktree/state.go, core/cli/runtime.go</files>
  <behavior>
    - CR-01 tests from Task 1 pass on portable lifecycle assertions and Linux adversarial extensions.
    - CR-02 tests from Task 1 pass through one shared semantic equality implementation.
    - CR-03 tests from Task 1 pass without weakening structural footer, metadata, frame, or zsh-source validation.
  </behavior>
  <action>
    GREEN CR-01: Remove production procfs discovery and hard gating from `_zp_worktree_close_coproc_endpoints`, `_zp_worktree_pid_owned`, `_zp_worktree_abort_transport`, and `_zp_worktree_invoke`. Capture every numeric descriptor and helper/writer/closer PID at creation. Close only those descriptor parameters with zsh-native descriptor redirection, clear each after its idempotent close, and signal only a still-unreaped direct child recorded by this invocation. Express ownership as an explicit lifecycle state (created, reaped, cleared) so PID reuse cannot pass a late ownership check; never use `ps`, `pgrep`, process-name matching, broad process groups, or ambient descriptor enumeration. Preserve the one entry deadline, cancellable writer, response cap, final 25 ms termination reserve, TERM/KILL/reap sequence, capability clearing, protected xtrace/history, and fail-open response classes. Commit `fix(07-16): make worktree transport portable` after CR-01 focused tests pass.

    GREEN CR-02: Replace positional `equalLiveStates` with one validated semantic authority by using an empty `DiffSnapshot` result or the same normalized identity map and `model.EqualLiveValue`. Reject invalid/duplicate identities exactly as existing state validation does. Do not sort, deduplicate, or otherwise normalize values inside ordered list records. Ensure `validateSharedGeneration`, Attach, commit/readback repair, `replaceWorkflowGeneration`, and behind/status consumers retain this helper. Commit `fix(07-16): make live state equality semantic` after CR-02 focused and existing diff/fingerprint tests pass.

    GREEN CR-03: In `validateRuntimePatchPayload`, derive the exact footer bytes only from trusted fixed-version `RuntimePatchMetadata`, split the complete program at the final suffix boundary, and compare that suffix byte-for-byte. Validate the exact complete bytes with the existing bounded `zsh -n` path. Remove only body-wide marker counting; retain whole-frame/version/final-record/EOF/source-size/metadata/name checks and source/secret suppression. Reject caller-authored trailers and malformed suffixes. Commit `fix(07-16): validate the trusted footer suffix structurally` after CR-03 focused tests pass.

    Run focused tests after each GREEN commit, then the consolidated gates. Do not squash the three RED/GREEN histories and do not bypass hooks.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreePortableOwnedTransport|TestWorktreeAbsoluteDeadlineAdversarialTransport|TestWorktreeTransitionRealTime25And500MillisecondMargins|TestWorktreeTwoShellEndToEnd)$' -timeout=180s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/worktree -run '^(TestEqualLiveStatesIsOrderIndependentAndValueExact|TestSemanticEqualityDrivesAttachRepairAndBehind|TestDiffSnapshotFinalOccurrenceSemanticEqualityAndOrdering|TestFingerprintSnapshotCanonicalIdentityOrder)$' -timeout=120s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^(TestRuntimeTransitionFooterAllowsMarkerTextInManagedPayload|TestRuntimeWorktreeFrameExactByteAndRecordBounds|TestRuntimeWorktreeReplyOutputIsWholeAndNoOpIsEmpty)$' -timeout=120s &amp;&amp; GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 GOTOOLCHAIN=local go test -c ./core/shell/zsh -o /tmp/zsh-pro-07-16-darwin-zsh.test &amp;&amp; GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 GOTOOLCHAIN=local go test -c ./core/worktree -o /tmp/zsh-pro-07-16-darwin-worktree.test &amp;&amp; GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 GOTOOLCHAIN=local go test -c ./core/cli -o /tmp/zsh-pro-07-16-darwin-cli.test</automated>
  </verify>
  <done>CR-01 through CR-03 GREEN commits pass focused and Darwin cross-build checks; production has no procfs dependency, semantic equality is identity-based/value-exact, and valid payload marker text no longer collides with the trusted footer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Record native macOS retained-zsh evidence for the exact implementation SHA</name>
  <files>scripts/verify-phase06-macos-runtime.sh, core/shell/zsh/worktree_live_test.go</files>
  <action>Pause execution at this mandatory native-platform gate. The operator must run the exact clean Task 2 commit on a real macOS host, provide the complete evidence listed below, and the executor must record that evidence in `07-16-SUMMARY.md`. Do not auto-approve, substitute cross-build/stub output, push code, or trigger external CI to satisfy this checkpoint.</action>
  <what-built>CR-01 portable transport plus CR-02/CR-03 correctness changes, already passing Linux, race, static, and Darwin cross-build checks.</what-built>
  <how-to-verify>
    This gate is mandatory and must run on a real macOS host at the exact clean commit produced by Task 2. Cross-compilation, a Linux host with `GOOS=darwin`, and `PHASE6_VERIFIER_STUB_MODE=1` do not qualify.

    1. On the native macOS checkout, resolve the exact Task 2 commit: `implementation_sha=$(git rev-parse HEAD)`. Confirm it is the exact 40-character SHA supplied by the executor.
    2. Run the repository's self-sealing native harness at that SHA: `GOTOOLCHAIN=local scripts/verify-phase06-macos-runtime.sh "$implementation_sha"`. It must end with `status: PASS`, report a native Darwin host/target, and reject any tracked or untracked divergence.
    3. Without changing the checkout, run the Phase 07 retained-zsh oracle: `GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreePortableOwnedTransport|TestWorktreeAbsoluteDeadlineAdversarialTransport)$' -timeout=120s`.
    4. Confirm both exact test names execute and pass (no skip), the timeout path returns within 250 ms, the retained shell accepts its sentinel command and a subsequent valid transition, and no unrelated process is signalled.
    5. Paste the complete harness/test output, exact SHA, macOS version, architecture, filesystem, Go version, and UTC observation time into the checkpoint response. The executor must record these fields and PASS rows in `07-16-SUMMARY.md`; approval text without evidence is insufficient.
  </how-to-verify>
  <verify>
    <automated>GOTOOLCHAIN=local scripts/verify-phase06-macos-runtime.sh "$(git rev-parse HEAD)" &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreePortableOwnedTransport|TestWorktreeAbsoluteDeadlineAdversarialTransport)$' -timeout=120s</automated>
    <human-check>Confirm the commands ran on native macOS at the exact clean Task 2 SHA, both retained-zsh tests executed without skip, and the complete host/toolchain/filesystem/PASS output is present in the checkpoint response.</human-check>
  </verify>
  <done>The exact SHA and complete native macOS harness/retained-zsh PASS evidence are supplied and recorded in `07-16-SUMMARY.md`; without that record this task and plan remain incomplete.</done>
  <resume-signal>Paste the exact-SHA native output ending in PASS for both retained-zsh tests. Do not reply with approval alone.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained zsh -> helper processes/descriptors | Parent-shell code creates bounded child and FD capabilities that cleanup must not broaden. |
| live snapshot -> state equality | Untrusted record order and values cross into convergence/repair decisions. |
| emitted zsh body -> trusted reply footer | Legitimate quoted payload and fixed protocol structure share one byte stream. |
| Linux evidence -> supported macOS claim | Cross-builds cannot establish native zsh/process behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-16-01 | Denial of Service | `_zp_worktree_invoke` | high | mitigate | Preserve one absolute 250 ms deadline, cancellable writer, termination reserve, exact close/TERM/KILL/reap, shell sentinel, and subsequent-transition tests. |
| T-07-16-02 | Spoofing | child PID cleanup | high | mitigate | Signal only recorded, still-unreaped direct children; explicit lifecycle clears ownership after wait and rejects unrelated/reused PIDs. |
| T-07-16-03 | Tampering | descriptor cleanup | high | mitigate | Close only numeric descriptors captured at creation; no procfs enumeration, inherited-FD sweep, or ambient lookup. |
| T-07-16-04 | Tampering | live-state equality | high | mitigate | Validate unique identity and compare through the canonical identity/value authority; retain ordered list semantics. |
| T-07-16-05 | Elevation of Privilege | equality-driven repair | high | mitigate | Reuse the same semantic helper in validation, attach, repair, replacement, and status rather than divergent comparators. |
| T-07-16-06 | Tampering | runtime footer | high | mitigate | Derive the exact suffix from trusted metadata and compare final bytes; reject trailers and malformed/version-mismatched structures. |
| T-07-16-07 | Information Disclosure | runtime validation errors | high | mitigate | Keep all errors value-free and discard parser/source diagnostics; never echo payload, descriptors, capability, token, or raw secret. |
| T-07-16-08 | Repudiation | macOS portability claim | high | mitigate | Blocking native checkpoint binds clean inputs and exact SHA and records host/toolchain/test evidence; cross-build is explicitly insufficient. |
| T-07-16-09 | Tampering | validation TOCTOU | high | mitigate | Validate the exact bytes returned under existing descriptor-bound root authority; never reopen by ambient path. |
</threat_model>

<verification>
After all three task-local RED/GREEN cycles and before the native checkpoint:

1. Focused matrix: run the exact commands in Tasks 1 and 2.
2. Prior closure regression: `GOTOOLCHAIN=local go test -count=1 ./core/worktree ./core/cli ./core/shell/zsh -run 'Worktree|RuntimeTransition|FirstSync|PartialApply|AbsoluteDeadline|Canonical|Fingerprint|DiffSnapshot' -timeout=240s`.
3. Full suite: `GOTOOLCHAIN=local go test -count=1 ./... -timeout=300s`.
4. Race suite: `GOTOOLCHAIN=local go test -race -count=1 ./core/worktree ./core/cli ./core/shell/zsh -timeout=360s`.
5. Static/build gates: `GOTOOLCHAIN=local go vet ./... &amp;&amp; GOTOOLCHAIN=local golangci-lint run ./... &amp;&amp; GOTOOLCHAIN=local go build ./...`.
6. Production procfs/stub scan: `test -z "$(rg -n '/proc/|PHASE6_VERIFIER_STUB_MODE' core/shell/zsh/hook.go core/worktree/state.go core/cli/runtime.go)"`.
7. Native Darwin checkpoint: Task 3 must record exact-SHA PASS evidence before this plan, and therefore dependent Plan 07-17, can complete.
</verification>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | One canonical Git-like shared worktree with safe independent-terminal convergence and truthful recovery | Plans 07-16 through 07-19; terminal closure-pass mapping in 07-19 | COVERED |
| REQ | WORK-01 | Plan 07-17 authenticated retirement/reclamation | COVERED |
| REQ | WORK-02 | Plans 07-16 and 07-17 portable semantic live synchronization and lifecycle | COVERED |
| REQ | WORK-03 | Plans 07-16, 07-18, and 07-19 structural transition safety plus durable/runtime mutation truth | COVERED |
| REQ | SYNC-01 | Plans 07-16 through 07-19 bounded supported-platform lifecycle and recovery | COVERED |
| REQ | SYNC-02 | Plans 07-16 through 07-19 exact convergence, replay, concurrency, and acknowledgement | COVERED |
| RESEARCH | 250 ms transition, 10k/2 MiB bounds, descriptor authority, value-free surfaces, no hook Git, no ambient root discovery | Repeated as hard constraints, tests, and threat mitigations in all four closure plans | COVERED |
| RESEARCH | First-lock-wins, exact acknowledgement, reverse ownership, ordered PATH/FPATH, no staging/remotes/multiple worktrees | Preserved by scope fences and regression gates | COVERED |
| CONTEXT | No Phase 07 CONTEXT.md; user approved proceeding without it | No D-XX decisions invented; current verification/review and existing Phase 07 mappings are authoritative | COVERED |
| REVIEW | CR-01 through CR-05 | CR-01/02/03 in 07-16, CR-04 in 07-17, CR-05 persistence in 07-18 and runtime recovery in 07-19 | COVERED |
| REVIEW | WR-01 `LiveCaptureSource` warning | Explicitly excluded by user direction | EXCLUDED |
| LEGACY REGISTRY | ROADMAP/REQUIREMENTS contain no Phase 07 row/IDs | No registry rewrite or invented identifiers | EXCLUDED |
</source_coverage_audit>

<success_criteria>
- CR-01, CR-02, and CR-03 each have a focused behavioral RED commit before a dedicated GREEN implementation commit, with normal hooks.
- Every automated focused, regression, full, race, vet, lint, build, and Darwin cross-build gate passes.
- Native macOS retained-zsh evidence is tied to the exact clean implementation SHA and recorded in the summary; the plan remains blocked without it.
- The one 250 ms budget, value-free surfaces, descriptor/capability authority, ordered PATH/FPATH values, exact acknowledgement, and no-Git/no-root-discovery prompt-hook constraints remain intact.
- WR-01 is not changed or claimed closed.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-16-SUMMARY.md` when done. It must include the six RED/GREEN commit SHAs and exact native-macOS evidence fields from Task 3. Do not update STATE.md, ROADMAP.md, REQUIREMENTS.md, prior plans/summaries, stash, or unrelated paths.
</output>
