---
phase: 07-git-like-shared-working-environment
plan: 10
type: execute
wave: 8
depends_on: ["07-06", "07-07", "07-09"]
files_modified:
  - core/shell/zsh/worktree_live_test.go
  - core/cmd/zsh-pro/main_test.go
  - core/store/store_test.go
  - core/worktree/service_test.go
  - core/worktree/atomic_unix_test.go
  - core/cli/worktree_test.go
  - scripts/perf-worktree.sh
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "A production binary plus two retained independent zsh -f processes prove zero-subprocess loader source, authenticated stable no-delta attach, exact positive command arities (`checkout <branch>`, `checkout -b <branch>`, and `config set auto-apply true|false`), shared status/diff, every supported category add/change/remove, commit, current-base branch, dirty checkout, reset, checkout parent-shell convergence, and deactivate zero residue; bare/incomplete arities are negative cases with zero effects."
    - "A real-provider attachment proves inherited unmanaged and SecretRef-resolved identities stay excluded while a safe identity created after attachment is admitted and published."
    - "The receiving shell's next accepted command sees a synchronized alias; foreground and PWD/jobs/buffer/history/process-local state remain independent."
    - "Auto true converges at line-finish; persisted/shell false remains visibly behind; explicit sync converges; invalid override remains visible."
    - "Disjoint stale writers compose; same-identity race creates one winner/visible loser; explicit sync --resolve shared converges same-key (including capture-baseline-acknowledged losers) and dirty history-gap conflicts through apply/verify/reverse/acknowledge."
    - "Quoted/multiline/present-empty/export/function/ordered-list/option/tombstone committed state survives CommitWorktree, direct-commit/tree/blob-validated ReadWorktreeRevision, checkout, and deactivate exactly; PATH/FPATH single-element removal preserves ownership and reverses byte-identically."
    - "New-reader/legacy-writer and frozen-legacy-reader/new-writer fixtures pass, while partial projections and valid-shaped blob/tree/tag or malformed fixed-entry trees fail closed."
    - "One combined canonical worktree generation survives publish/resolve/acknowledge crash-before-replace, crash-after-replace/lost-response, and operation-ID replay without duplicate revisions or lost unpublished deltas."
    - "Every private Attach/Publish/Prepare/Acknowledge/Resolve call receives authorization only through the bounded versioned stdin envelope, is rejected under the canonical lock for a missing/wrong/cross-shell capability before receipt lookup, and leaves the raw capability absent from helper argv/environment and canonical state; valid lost-response replay stays idempotent and at-rest state contains only the verifier."
    - "Public path-bound and runtime authenticated-descriptor-bound StateStores reach the same canonical bytes with proven input-versus-duplicate close ownership and no runtime path reopen."
    - "Prepare/resolve deliver revision/token/fingerprint to the real parent through the exact emitter-owned reply assignments; at-head zero bytes, pending metadata-only, malformed/partial/stale replies, timeout cleanup, leakage, and post-apply/lost-response acknowledgement all have executable coverage."
    - "Secret-like and ambient values are absent from state, Git blobs, patch bytes, events, conflicts, stdout/stderr, and retained artifacts."
    - "Security and failure injectors preserve old-or-new state, truthful dirty/behind/conflict/recovery evidence, and a usable prompt."
    - "Fifty no-op prompt cycles invoke no Git/child-zsh introspection; an injected monotonic clock proves cumulative 249 ms admission and 251 ms timeout including late-stage consumption, active real-time 25 ms/500 ms stalls prove fail-open behavior for authentication, bind, helper read/execute, transport, protected eval, reverse replacement, fresh capture, and acknowledgement, and actual p50/p95/max/counts are reported without a fabricated tighter baseline."
  artifacts:
    - path: "core/shell/zsh/worktree_live_test.go"
      provides: "Production-binary two-independent-zsh workflow, attachment, resolution, and zero-residue oracle"
      contains: "TestWorktreeTwoShellEndToEnd"
    - path: "core/cmd/zsh-pro/main_test.go"
      provides: "Public/runtime canonical-authority equivalence, exact path/descriptor StateStore construction/close ownership, and concrete classifier proof without pointer-identity assumptions"
      contains: "TestCanonicalWorktreeAuthority"
    - path: "core/store/store_test.go"
      provides: "Committed projection and real-Git ReadWorktreeRevision/checkout/deactivate integration"
      contains: "TestWorktreeGitWorkflow"
    - path: "core/worktree/service_test.go"
      provides: "Exactly 21 edge probes including attach and resolution assertions"
      contains: "TestWorktreeEdgeContract"
    - path: "core/worktree/atomic_unix_test.go"
      provides: "Authenticated combined-generation persistence, path/descriptor constructor ownership, crash/replay, and security fault matrix"
      contains: "TestWorktreeSecurityFaultMatrix"
    - path: "core/cli/worktree_test.go"
      provides: "Aggregate exact public/private routing, shell-ID context, and forbidden-operation gate"
      contains: "TestWorktreePublicSurfaceAggregate"
    - path: "scripts/perf-worktree.sh"
      provides: "Production prompt sampler, deterministic cumulative-boundary oracle, active generous-margin whole-transition fail-open proof, and subprocess-count evidence"
      contains: "worktree"
  key_links:
    - from: "built binary/installed loader"
      to: "two independent zsh -f processes"
      via: "zero-process source plus exact sourced zsh-pro dispatcher over one shared authenticated generation but distinct PIDs, crypto-random ID/capability pairs, baselines, and reverse ownership"
      pattern: "TestWorktreeTwoShellEndToEnd"
    - from: "exact installed `zsh-pro checkout/reset/sync/status` syntax"
      to: "shared workflow and parent-shell state"
      via: "scoped opaque shell ID, command zsh-pro forwarding, lazy attach gate, and one validated prepare/apply/ack pipeline; legacy functions delegate rather than bypass"
      pattern: "TestWorktreePublicDispatcher"
    - from: "initial capture then live edit"
      to: "attachment exclusion/admission"
      via: "attach publishes no delta; inherited stays excluded; post-attach safe identity publishes"
      pattern: "Attach"
    - from: "private prepare/resolve helper exit"
      to: "post-apply Service.Acknowledge"
      via: "whole zsh-n-validated emitter program sets exact protocol/revision/token/fingerprint/complete globals; parent validates fresh fingerprint, acknowledges, and clears all reply/source state"
      pattern: "ZP_WORKTREE_REPLY_COMPLETE"
    - from: "private ShellCredential"
      to: "ShellState capability verifier"
      via: "all five Service operations validate under WithTransaction before receipts/transitions; runtime only allocates and accepts/forwards the presented credential through the versioned bounded stdin frame"
      pattern: "VerifyShellCapability"
    - from: "blocked private helper process"
      to: "capability replay boundary"
      via: "cmdline/environ inspection finds no raw capability, then ID-only and wrong shell-ID/capability pairing are rejected with zero causal-state effects"
      pattern: "TestWorktreeCapabilityProcessVisibility"
    - from: "same-key or dirty-gap conflict"
      to: "converged shell"
      via: "durable unpublished loser -> sync --resolve shared -> authoritative patch -> reverse replacement -> fresh snapshot -> acknowledge"
      pattern: "ResolveShared"
    - from: "live commit"
      to: "ReadRevision/checkout/deactivate"
      via: "versioned committed DTO and zsh-owned canonical projection rendering"
      pattern: "ReadWorktreeRevision"
  prohibitions:
    - statement: "MUST NOT turn an ambient inherited shell snapshot into a managed profile or silently capture a newly observed secret-like value."
      category: privacy
      status: unresolved
      flagged: true
    - statement: "MUST NOT make source-statement history the owner of live state when the user's observable shell has one final value per supported identity."
      category: fidelity
      status: unresolved
      flagged: true
    - statement: "MUST NOT let a per-process profile environment variable redefine or overwrite the durable shared branch, base OID, or revision."
      category: transparency
      status: unresolved
      flagged: true
    - statement: "MUST NOT expose captured values through state conflicts, exclusion records, forensic events, recovered-error text, or retained test artifacts."
      category: privacy
      status: unresolved
      flagged: true
    - statement: "MUST NOT discard a dirty shared diff except through the explicit destructive reset operation."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT silently broaden the local single-worktree workflow into staging, advanced history editing, remote collaboration, or applying externally authored profiles."
      category: product-safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT mutate a foreground command or synchronize PWD, jobs, command buffers, history, process trees, or terminal UI as though independent shells were one process."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT make a helper, lock, state, renderer, validator, eval, or acknowledgement failure prevent the user's next prompt or entered command."
      category: safety
      status: unresolved
      flagged: true
    - statement: "MUST NOT hide that auto-apply is disabled, invalidly overridden, behind, or conflicted while presenting a shell as converged."
      category: transparency
      status: unresolved
      flagged: true
---

<objective>
Prove the complete shared-worktree contract through exact installed public syntax, real independent shells, and adversarial failure injection.

Purpose: Turn model/storage/Git/capture/runtime claims into one non-vacuous user journey with zero-process source, lazy attachment, shared dispatcher routing, late-bound authority, secret admission, commit projection, conflict resolution, active Service/validator termination, zero residue, and all 21 edge probes.
Output: Retained exact-command two-zsh oracle, aggregate edge/security matrices, and repeatable bounded prompt/validator measurement.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-06-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-07-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-09-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@.codex/skills/spike-findings-zsh-pro/sources/001-live-state-capture-and-multi-terminal-sync/README.md
@core/shell/zsh/live_terminal_test.go
@core/shell/zsh/residue_test.go
@scripts/perf-hyperfine.sh

<interfaces>
- Exercise the built production binary and real installed/sourced loader; the copied spike is orchestration guidance, not the product under test.
- Each zsh process has independent PID/PWD/jobs/history/buffer/shell ID/applied revision/reverse ownership; only the authenticated local Store/worktree root is shared.
- Each shell's mutating private calls require its independent capability inside the exact `ZPWT` version-1 stdin envelope capped at 2,101,248 bytes/10,016 records; public status receives only the ID. The loader keeps a valid non-exported pair stable across re-source, clears every call-local copy/control/frame on exit, helper argv/environment contain no raw capability, and canonical state stores only the verifier.
- The real parent receives pending acknowledgement metadata only through Plan 07-08's emitter-owned reply globals. They are absent before invocation, canonical and complete after protected eval, supplied with the fresh snapshot to Acknowledge, and unset with the source buffer in all exits.
- The public Service uses `OpenStateStore(path)`; the runtime factory uses `NewStateStoreFromAuthenticatedRoot` on an already-authenticated RuntimeRoot descriptor. The latter owns/closes only its duplicate, while the caller retains/closes the input descriptor.
- Exact deadline-boundary tests use the Plan 07-09 fake monotonic clock with no real sleep; active production-path fail-open tests use 25 ms well-below-budget stalls and 500 ms clearly-over-budget stalls, keep the old acknowledged revision on timeout, and clean process groups. All tests use isolated HOME/ZDOTDIR/root and value-safe diagnostics.
</interfaces>
</context>

## Artifacts this phase produces

- New `core/shell/zsh/worktree_live_test.go` with retained production two-shell helpers and top-level zero-process-source/lazy-attach/exact-public-dispatch workflow, concurrency, admission, foreground/manual, history-resolution, secret exclusion, and zero-residue tests.
- `core/cmd/zsh-pro/main_test.go` gains public/runtime canonical-state equivalence, exact path-bound versus post-auth descriptor-bound construction/no-reopen/close-ownership proof, unchanged Service credential forwarding, and real classifier policy proof without requiring pointer identity.
- `core/store/store_test.go` gains committed DTO/real Git workflow integration through ReadWorktreeRevision and checkout/deactivate.
- `core/worktree/service_test.go` gains exactly 21 named edge predicates including already-acknowledged loser, five-operation capability spoof/cross-shell replay, and authenticated lost-response replay cases; `core/worktree/atomic_unix_test.go` gains path/descriptor ownership plus the combined-generation crash/security fault matrix; `core/cli/worktree_test.go` gains the aggregate public/private surface and exact-arity gate.
- New executable `scripts/perf-worktree.sh` runs 50 isolated production cycles, reports count/p50/p95/max/total, counts Git/child-zsh calls, runs deterministic fake-clock cumulative 249/251 stage matrices without sleeping at the boundary, and injects 25 ms/500 ms real authentication/bind/helper-read-execute/transport/eval/reverse-replacement/fresh-capture/acknowledgement stalls plus continuation/replay sentinels.
- No production symbol/field/verb/flag/config/package/persistent product file/external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive attachment, live edits, Git workflow, resolution, and zero residue through two production shells</name>
  <files>core/shell/zsh/worktree_live_test.go, core/cmd/zsh-pro/main_test.go, core/store/store_test.go</files>
  <read_first>
    - core/shell/zsh/live_terminal_test.go: built-binary sourced-zsh harness, hostile options, and cleanup
    - core/shell/zsh/residue_test.go: byte-visible full-state oracle and negative controls
    - core/cmd/zsh-pro/main_test.go: canonical public/runtime authority and late-bound descriptor tests
    - core/store/store_test.go: real Git/secret/transaction fixtures
    - .codex/skills/spike-findings-zsh-pro/sources/001-live-state-capture-and-multi-terminal-sync/README.md: independent-shell experiment groups
    - .planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md: testing and workflow semantics
  </read_first>
  <action>
    Preserve all current tests. Build one binary, isolate HOME/ZDOTDIR/root, install/ingest, and start two long-lived `zsh -f` processes over framed pipes. Source the real loader under an external-process counter and prove exactly zero source-time binary/Git/child-zsh invocations. Trigger the first safe boundary and prove descriptor-authenticated attach allocates distinct crypto-random 256-bit ID/capability pairs, returns them only through the bounded private response, commits one retained initial snapshot through the `ZPWT` version-1 stdin frame without event/diff/publication, stores only the Plan 07-03 verifier, and rejects collision/spoof/mismatch through the real under-lock Service check. Exercise each of Attach/Publish/Prepare/Acknowledge/Resolve with exact valid frames, then malformed/partial/trailing/duplicate/operation-mismatch/exact-cap/plus-one frames and wrong/cross-shell capabilities plus guessed operation receipts; assert complete-frame EOF enforcement, descriptor cleanup, no result replay, state/event/receipt mutation, or credential/verifier diagnostic. Pause a real helper after it accepts stdin but before Service invocation and inspect `/proc/<pid>/cmdline` plus `/proc/<pid>/environ` (or the platform inspector abstraction): the raw capability canary must occur in neither. From the sibling shell, replay the observed shell ID without capability and pair the first shell's capability with the sibling ID; both must reject before receipt access with zero causal-state effect. Re-source each loader and prove the valid non-exported pair is unchanged with no allocation/process at source, while every call-local credential/control/frame/response copy and descriptor is gone after success, malformed input, timeout, and signal. Assert distinct PIDs/IDs/capabilities plus one shared combined durable authority. Exercise exact installed commands, not internal helpers: `zsh-pro status`, `zsh-pro diff`, `zsh-pro commit -m <message>`, `zsh-pro branch`, `zsh-pro branch <name>`, `zsh-pro checkout <branch>`, `zsh-pro checkout -b <new-branch>`, `zsh-pro reset --hard`, `zsh-pro config set auto-apply true`, `zsh-pro config set auto-apply false`, `zsh-pro sync`, and `zsh-pro sync --resolve shared`. Put bare `zsh-pro checkout`, bare `zsh-pro checkout -b`, and bare `zsh-pro config set auto-apply` only in the negative zero-call matrix. Before positive checkout/reset, create a local delta and inject publish failure/conflict to prove the dispatcher makes zero branch/reset calls and retains the delta; then prove successful pre-publication plus the shared sync pipeline mutates the parent. Status receives ID only and shows correct applied/behind state; legacy checkout/activate/status/list delegates yield the same shared results and never invoke per-terminal authority. Verify a second source is idempotent and process-free.

    Prove an inherited ordinary identity stays excluded, a resolved SecretRef canary stays absent everywhere, and a safe identity created after attach is admitted. Exercise add/update/remove plus quotes/multiline/present-empty/export/list duplicates/options/tombstones. For PATH and FPATH, remove one owned duplicate/empty-adjacent element, prove the correct occurrence alone disappears, then reverse to byte-identical tied scalar/array state with unmanaged base elements intact. Prove B's next accepted alias command sees auto state. For every Prepare and Resolve response, observe the real private helper exit and assert that at-head is exact zero bytes/globals absent, while a pending target—including zero live mutations—evaluates one complete emitter-owned protocol/revision/token/fingerprint/complete reply. Corrupt each reply field, truncate before complete, inject stale globals, stall before/after eval, and lose the acknowledgement response: malformed/timeout paths never acknowledge and always clear reply/source/control/call-local credential state; the valid path fresh-captures, fingerprint-matches, acknowledges the exact pending target, and replays the same ack operation ID without duplicate revision after lost response. Assert reply variables, patch bytes, raw capability, and verifier are absent from argv, environment, exports, xtrace/history, public status/errors, state events, and retained call buffers after return. Commit all changes; inspect the new DTO with a frozen legacy reader and legacy DTO with the new reader; inject partial projection. Read only a direct commit whose fixed profile.json/profile.zsh entries are regular blobs, and reject valid-shaped blob/tree/tag plus missing/duplicate/wrong-mode/type/malformed trees before decode. Checkout/deactivate without semantic loss or residue. Exercise current-base branch, dirty checkout, reset, manual mode, process-local/foreground independence, disjoint barrier writes, same-key visible conflict where the loser capture baseline already contains its value then exact public resolution, dirty history-gap resolution, later-head/lost-ack replay, and failure-retained conflict. In main tests prove the public path-bound and late-bound authenticated-descriptor-bound Service objects read/mutate the same combined generation, runtime construction never reopens/falls back, constructor failure leaves the input descriptor owned by RuntimeRoot, successful StateStore Close closes only its duplicate exactly once, and no assertion depends on pointer identity. Use negative controls and redact buffers. Terminate process groups on every exit.
  </action>
  <acceptance_criteria>
    - Two real independent shells and the production binary/loader are the system under test.
    - Loader source starts zero processes; retained lazy no-delta attach and real-provider inherited/post-attach/SecretRef admission behavior are observed end to end.
    - Every exact installed public command and legacy delegate routes once through shared truth, with scoped shell-ID status and checkout/reset parent-shell convergence.
    - Positive commands use complete valid arities; incomplete checkout/config arities are negative zero-effect cases.
    - Failed/conflicted pre-mutation publication prevents checkout/reset and preserves the local delta.
    - Same-key and dirty-gap conflicts converge only via explicit shared resolution and verified acknowledgement.
    - Commit/ReadRevision/checkout/deactivate preserve exact live projection and zero residue.
    - Stable authenticated shell capabilities, exact object/tree/blob validation, DTO migration, and PATH/FPATH ownership removal are proven end to end.
    - The live blocked-helper oracle proves raw capability absence from argv/environment, and shell-ID-only/wrong-pair cross-shell replay plus malformed frame matrices produce zero causal effects.
    - All five private operations enforce the durable under-lock verifier contract, including spoof/cross-shell/receipt replay and crash/lost-response paths.
    - The emitter reply protocol carries exact acknowledgement inputs across a real helper exit; no-op, metadata-only, malformed, timeout, cleanup, leakage, post-apply ack, and replay outcomes are distinguished.
    - Public path and authenticated-descriptor StateStore lifecycles share one authority with exact descriptor ownership and no reopen.
    - Process-local state, temp roots, processes, and sensitive buffers remain isolated/cleaned.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh ./core/store ./core/cmd/zsh-pro -run 'Test(WorktreeTwoShell|WorktreeLoaderSourceZeroProcess|WorktreeLazyAttach|WorktreePublicDispatcher|WorktreeLegacyDelegates|WorktreeShellIDTransport|WorktreeCapabilityStdin|WorktreeCapabilityProcessVisibility|WorktreePrivateFrameCleanup|WorktreeReplyProtocol|WorktreeReplyCleanup|WorktreeAcknowledgeReplay|WorktreeCapabilitySpoof|WorktreeTransitionFakeClock|WorktreeForeground|WorktreeHistoryGap|WorktreeResolve|WorktreeSecretNoise|WorktreeGitWorkflow|CanonicalWorktreeAuthority|LateBoundRuntimeWorktree|StateStoreDescriptorOwnership)' -count=1 -timeout=55s</automated>
  </verify>
  <done>The exact installed public journey passes through zero-process source, lazy attach, real independent parent shells, late-bound canonical authority, real Git/classifier, and exact zero-residue lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate 21 edge probes, security faults, forbidden operations, and active deadline evidence</name>
  <files>core/worktree/service_test.go, core/worktree/atomic_unix_test.go, core/cli/worktree_test.go, scripts/perf-worktree.sh</files>
  <read_first>
    - core/worktree/service_test.go: attach/publish/resolve/history/revision/idempotency tests
    - core/worktree/atomic_unix_test.go: ownership/symlink/lock/old-or-new/fault seams
    - core/cli/worktree_test.go: public usage/effect matrix
    - core/cli/runtime_test.go: fake-clock cumulative 249/251 deadline, bounded stdin frame, descriptor, typed-nil, and source-redaction tests
    - scripts/perf-hyperfine.sh: external-binary/report conventions without requiring hyperfine
    - .codex/skills/spike-findings-zsh-pro/sources/001-live-state-capture-and-multi-terminal-sync/README.md: verifier groups and measurements
  </read_first>
  <action>
    Add one named edge contract table with exactly the 21 rows in the audit below; each row must invoke a concrete production assertion and the expected count must fail if any row is missing. Extend security faults with owner/mode/symlink/replacement, path-bound versus already-authenticated-descriptor StateStore equivalence, input/duplicate close and constructor-failure ownership, contention, short write/fsync/rename/dir-sync, abandoned temp, malformed state/frame, versioned stdin exact/max/plus-one/partial/trailing/duplicate/EOF/descriptor cleanup, blocked-helper argv/environment inspection, shell-ID-only/wrong-pair/capability spoof/collision/cross-shell/receipt guessing across all five private operations, secret canary, Git CAS/object/tree validation, render/validation/reply-parse/eval/reverse replacement/fresh-snapshot/acknowledgement, exact zero-byte no-op, pending metadata-only response, reply cleanup/leakage, lazy attach, pre-checkout/reset publication, dispatcher recursion/ID-only public context, late-bound factory reopen/fallback, and already-acknowledged loser resolution. For Attach, Publish, Prepare, ResolveShared, and Acknowledge inject crash before canonical replace, after replace before response, and response loss where applicable; reopen and replay the same operation ID first with a wrong capability and then the valid one, asserting authorization before receipt access, old-or-complete-new combined verifier/state, no duplicate revision, and no lost unpublished delta. Assert exact five private operations/arities, exact positive public arities, and zero calls for incomplete checkout/config, direct binary sync, staging/advanced/remote/multiple-worktree/arbitrary runtime path/ref/command/malformed config.

    Create the sampler using the real binary and isolated authenticated root: 50 no-op cycles, actual counts/p50/p95/max/total, zero Git/child introspection, and bounded combined state/events. Drive the same unexported monotonic budget seam with fake cumulative stage increments and prove 249 ms admitted versus 251 ms timed out for authentication, bind, helper read/execute, transport, protected eval/reply parse, replacement-reverse update, fresh capture, and acknowledgement; the last increment must be able to cross the deadline at each late stage and exact-boundary assertions must contain no real sleep. Separately inject active 25 ms success and 500 ms failure stalls at every named production stage. After every active timeout, run a sentinel in the same parent shell and assert reply globals/source/control/call-local credential buffers are cleared, no capability/verifier/source leaked, the old acknowledged revision remains, and applied/pending/conflict/unpublished/reverse/operation-receipt recovery remains truthful and replayable. Report measurements and the sealed configured finite default without asserting a tighter invented latency target.
  </action>
  <acceptance_criteria>
    - Exactly 21 probe rows execute concrete predicates.
    - Every injected fault proves canonical truth plus usable-shell recovery.
    - Forbidden public/private operations, malformed shell IDs, and runtime authority reopen/fallback attempts make zero unintended dependency calls.
    - Combined-generation crash/replay and shell-capability spoof/collision matrices are non-vacuous.
    - Path/descriptor StateStore equivalence and close-ownership matrices are non-vacuous and contain no pathname reopen.
    - Reply no-op/metadata-only/malformed/timeout/cleanup/leakage and post-apply acknowledgement matrices cross the real process boundary.
    - Fake monotonic time proves exact cumulative 249/251 behavior across every early and late stage without boundary sleeps; active 25/500 ms stalls prove fail-open recovery and old-ack retention, with real performance measured separately.
  </acceptance_criteria>
  <verify>
    <automated>set -eu; GOTOOLCHAIN=local go test ./core/worktree ./core/cli -run 'Test(WorktreeEdgeContract|WorktreeSecurityFaultMatrix|StateStoreDescriptorOwnership|WorktreeCapabilitySpoof|RuntimeWorktreeFrame|RuntimeCapabilityTransport|WorktreeFakeClock|WorktreeReplyProtocol|WorktreePublicSurfaceAggregate|WorktreeDeadline)' -count=1 -timeout=55s; test -x scripts/perf-worktree.sh; worktree_perf_binary=$(mktemp /tmp/zsh-pro-worktree-bin.XXXXXX); export ZSHPRO_BIN="$worktree_perf_binary"; trap 'rm -f "$worktree_perf_binary"' EXIT; GOTOOLCHAIN=local go build -o "$worktree_perf_binary" ./core/cmd/zsh-pro; scripts/perf-worktree.sh --cycles 50</automated>
  </verify>
  <done>Every edge/security/deadline/scope claim is non-vacuously covered and the real hot path is measured.</done>
</task>

</tasks>

<edge_probe_audit>

| Requirement | Probe category | Resolution predicate | Plan proof |
|---|---|---|---|
| WORK-01 | idempotency | Exact branch/OID/document materialization repeat is no-op | 07-03, 07-07, 07-10 |
| WORK-01 | concurrency | One combined generation/lock/operation receipt; post-commit split is recovery-required | 07-03, 07-06, 07-07, 07-10 |
| WORK-02 | boundary | 0/1/2 MiB/10,000 and plus-one frame/record predicates | 07-01, 07-08, 07-10 |
| WORK-02 | adjacency | Exact equality no-op; same identity updates; disjoint identities separate | 07-01, 07-03 |
| WORK-02 | empty | Empty snapshot/delta/value and attach-with-no-delta semantics | 07-01, 07-03, 07-09 |
| WORK-02 | encoding | NUL framing, exact bytes/export, ordered elements, commit DTO/tombstones | 07-01, 07-04, 07-05, 07-08 |
| WORK-02 | ordering | Category/name/final-occurrence ordering; list order retained | 07-01, 07-04 |
| WORK-02 | precision | Overflow-safe counts/revisions and all-or-nothing validation | 07-01, 07-03 |
| WORK-03 | adjacency | Current/target/existing branch and dirty boundary outcomes differ | 07-06 |
| WORK-03 | empty | Clean commit/reset/current checkout are no-ops | 07-06 |
| WORK-03 | ordering | Deterministic branches and categorized diff | 07-06 |
| WORK-03 | idempotency | Repeat clean operations do not create commits/revisions | 07-05, 07-06 |
| WORK-03 | concurrency | Expected-base CAS plus truthful committed/state recovery | 07-05, 07-06 |
| SYNC-01 | boundary | Config true/false/invalid, zero-process authenticated lazy attach, exact public arities/dispatcher, fake cumulative 249/251 boundary, and active generous-margin fail-open termination | 07-03, 07-09, 07-10 |
| SYNC-01 | adjacency | At-head no-op and behind final reduction before next command | 07-03, 07-09 |
| SYNC-01 | empty | Empty prepare/apply/ack and head sync are no-ops | 07-03, 07-09 |
| SYNC-01 | ordering | Final per-identity change order with ordered PATH/FPATH | 07-03, 07-04 |
| SYNC-01 | precision | Strict uint64, checked increment, exact pending/token acknowledgement | 07-01, 07-03, 07-09 |
| SYNC-01 | idempotency | Repeat attach/sync/ack at head changes nothing | 07-03, 07-09 |
| SYNC-01 | concurrency | Later head during prepare leaves exact ack and shell behind | 07-03, 07-09 |
| SYNC-02 | unclassified reviewed | Combined-generation atomic/replay state, verifier-at-rest under-lock shell capability checks, stdin-only bounded capability transport with cmdline/environ absence, path/descriptor constructor ownership, executable parent reply protocol, causal arbitration, already-acknowledged loser resolution, safe foreground, deterministic whole-transition deadline, fail-open | 07-03, 07-08, 07-09, 07-10 |

Coverage: 21 applicable, 21 resolved by explicit automated predicates, 0 silently dropped.
</edge_probe_audit>

<prohibition_probe>

The nine descriptor-less prohibitions in frontmatter remain the retained product-specific privacy, transparency, fidelity, product-safety, and shell-safety outcomes. Routine correctness is covered by the 21 edge predicates. Canon shell injection, path/symlink replacement, and generic secret-at-rest controls remain in threat models and security tests rather than being duplicated as new prohibitions. No descriptor is fabricated.
</prohibition_probe>

<source_audit>

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Ingest once; shared diff; Git workflow; default-on configurable convergence | 07-01..07-10 | COVERED | Production two-shell journey in 07-10 |
| REQ | WORK-01 | Bootstrap ingest and exact shared materialization | 07-03, 07-05, 07-07, 07-10 | COVERED | Exact DTO/OID, idempotency, repair |
| REQ | WORK-02 | Supported state, categorization, noise/secret boundary, no staging | 07-01, 07-02, 07-08, 07-09, 07-10 | COVERED | Real classifier, attach, capture, E2E |
| REQ | WORK-03 | Status/diff/commit/branch/checkout/reset dirty protection | 07-04, 07-05, 07-06, 07-10 | COVERED | Faithful projection plus strict workflow |
| REQ | SYNC-01 | Shared visibility, safe default auto-apply, config, explicit sync | 07-03, 07-06, 07-08, 07-09, 07-10 | COVERED | Attach/pull/apply/ack and manual recovery |
| REQ | SYNC-02 | Atomic causal writes, concurrency, foreground fail-open | 07-01, 07-03, 07-05, 07-09, 07-10 | COVERED | Conflict resolution and measured/tunable finite production-default deadline included |
| RESEARCH | — | Source Profile separate from projection/tombstones | 07-01, 07-04, 07-05 | COVERED | Commit/Read/checkout/deactivate exact |
| RESEARCH | — | Every added executable zsh state-mutation command/string originates in emit.go | 07-04, 07-08, 07-10 | COVERED | Regenerator delegates typed operations; structural ownership test |
| RESEARCH | — | Absent-at-attach admission only | 07-02, 07-03, 07-09, 07-10 | COVERED | Resolved decision and real-provider proof |
| RESEARCH | — | Exact-OID materialization recovery | 07-05, 07-07, 07-10 | COVERED | Resolved decision |
| RESEARCH | — | Persisted default plus shell-local override | 07-03, 07-06, 07-09, 07-10 | COVERED | Resolved decision |
| RESEARCH | — | Classifier-owned live secret boundary and SecretRef pinning | 07-02, 07-07, 07-10 | COVERED | Production injection and canary proof |
| RESEARCH | — | Explicit same-key/history-gap resolution | 07-03, 07-09, 07-10 | COVERED | Apply/verify/reverse/ack |
| RESEARCH | — | Measured/tunable finite 250 ms default over helper and complete parent transition | 07-09, 07-10 | COVERED | Fake cumulative 249/251 at every stage plus active 25/500 ms fail-open and old-ack recovery |
| RESEARCH | — | Zero-subprocess source, authenticated safe-boundary lazy attach, and exact sourced public routing | 07-06, 07-09, 07-10 | COVERED | Crypto-random stable ID/capability, scoped ID-only status, pre-mutation publish, command forwarding, parent convergence, legacy bypass retirement |
| RESEARCH | — | One canonical combined generation and crash-consistent transition state machine | 07-03, 07-07, 07-09, 07-10 | COVERED | Operation receipts and publish/resolve/acknowledge crash/replay matrix |
| RESEARCH | — | One semantic diff/projection authority | 07-01, 07-04, 07-10 | COVERED | Model-owned helpers plus cross-package golden equivalence |
| RESEARCH | — | Exact private source/value-free metadata separation and five private operations | 07-08, 07-09, 07-10 | COVERED | Unexported payload, reflection/serialization canaries, strict arities |
| RESEARCH | — | Durable shell authority at canonical transaction boundary | 07-03, 07-07, 07-09, 07-10 | COVERED | Verifier-only ShellState, all five under-lock checks, path/descriptor constructors, runtime allocation/transport only |
| RESEARCH | — | Parent-shell acknowledgement metadata survives helper exit | 07-08, 07-09, 07-10 | COVERED | Emitter-owned reply assignments, exact no-op/metadata-only semantics, parsing/cleanup/replay/leakage tests |
| RESEARCH | — | Direct commit and fixed blob-tree validation plus DTO migration | 07-05, 07-10 | COVERED | Blob/tree/tag/malformed rejection and bidirectional compatibility fixtures |
| RESEARCH | — | PATH/FPATH owned element removal reversibility | 07-04, 07-10 | COVERED | Duplicate/empty/unmanaged-base live zsh byte-identical round trip |
| RESEARCH | — | No dependency/staging/advanced Git/remotes/multiple worktrees/process-local sync | 07-01..07-10 | COVERED | Negative model/dispatch/hot-path tests |
| CONTEXT | — | No CONTEXT.md | — | N/A | No locked D-XX or deferred-context items |

No GOAL, REQ, RESEARCH, or CONTEXT item is missing. REQUIREMENTS future/out-of-scope items remain exclusions with negative coverage.
</source_audit>

<review_feedback_audit>

| Review finding | Disposition in executable plans |
|---|---|
| CodeRabbit 1 / OpenCode H1 / M3 — canonical authority versus Service/current legacy paths | Incorporated in 07-05/06/07/09/10: combined durable generation is primary; distinct public/runtime Service instances are expected; Store.Current/Create/Checkout/Commit are deprecated and absent from Phase-7 call paths; shell profile variables are applied markers only. |
| CodeRabbit 2 — unpublished local delta before checkout/reset | Incorporated in 07-09/10: sourced dispatcher must fresh-capture/publish and receive a no-unpublished receipt before mutation; failures/conflicts make zero workflow calls and retain the delta. |
| CodeRabbit 3 / OpenCode M1 — incomplete/fixed deadline | Incorporated in 07-09/10: 250 ms is the measured/tunable finite default, not a universal SLA; one guard spans helper validation, parent eval, reverse replacement, fresh capture, and acknowledgement, with fake cumulative 249/251 proof plus generous 25/500 ms active stalls. |
| CodeRabbit 4 — multi-document crash consistency | Incorporated in 07-03/10: one combined canonical generation, stable operation IDs/receipts, and crash-before/after-replace plus lost-response replay for publish/resolve/acknowledge. |
| CodeRabbit 5 — sync direct dispatch | Incorporated in 07-06/09/10: sync and resolve are sourced-loader-only parent-shell operations; direct binary forms fail before service access. |
| CodeRabbit 6 — invalid positive arities | Incorporated in 07-10 Task 1/2: positive checkout/config commands carry required branch/boolean operands; bare/incomplete forms are negative zero-effect cases. |
| CodeRabbit 7 — omitted private operations | Incorporated in 07-08/09/10: exactly Attach, Publish, Prepare, Acknowledge, Resolve with closed arities; Prepare remains distinct from Pull/query. |
| CodeRabbit 8 — commit/tree/blob validation | Incorporated in 07-05/10: direct commit type plus exact regular-blob tree entries; shaped blob/tree/tag and malformed trees are rejected. |
| CodeRabbit 9 — private exact-source channel | Incorporated in 07-08/09/10: unexported in-memory source payload is structurally separate from serializable value-free metadata and validated before private stdout. |
| OpenCode H2 — duplicate semantic projection | Incorporated in 07-01/04/10: model-owned normalization/equality helpers are the authority and cross-package golden tests pin worktree/activation equivalence. |
| OpenCode H3 — shell ID derivation | Incorporated in 07-09/10: descriptor-authenticated crypto-random 256-bit ID plus independent capability, verifier-at-rest, non-exported re-source stability, and collision/spoof tests. |
| OpenCode H4 — already locally acknowledged loser | Incorporated in 07-03/09/10: capture baseline and durable unpublished delta are separate; resolution consumes the stored loser and clears only after verified acknowledgement. |
| OpenCode M2 — PATH/FPATH removal | Incorporated in 07-04/10: occurrence-aware ownership removal and live tied-list byte-identical reverse tests. |
| OpenCode M4 — DTO migration | Incorporated in 07-05/10: new-reader/legacy-writer and frozen-legacy-reader/new-writer fixtures; partial projections fail closed. |
| OpenCode M5 — inaccurate source inventory | Incorporated in 07-05: `validGitObjectID`, `profileAtRevision`, `revParse`, commit/tree construction, and `updateRefCAS` are explicitly reused rather than described as new. |
| OpenCode L1 — brittle source AST test | Incorporated in 07-04: narrowed from executable-literal scanning to `RegenerateWorktree` call-ownership plus behavior tests. |
| OpenCode L2 — interface widening | Incorporated in 07-02/07: narrow optional consumer-owned interfaces and concrete-zsh injection only at the composition root. |
| Revision checker blocker — durable credential authority absent from core contracts | Incorporated in 07-03/07/08/09/10: model credential/verifier, verifier-only ShellState, under-lock validation before receipts for all five private operations, public path and authenticated-descriptor StateStore constructors with exact close ownership, runtime allocation/transport only, and spoof/replay/crash tests. |
| Revision checker blocker — prepare metadata stranded in exited helper | Incorporated in 07-08/09/10: concrete emitter appends exact protocol/revision/token/fingerprint/complete assignments to private stdout, parent validates/clears them, at-head and pending metadata-only behavior is exact, and malformed/timeout/leakage/post-apply/lost-ack tests cross the real process boundary. |
| Adjusted checker blocker — raw capability exposed in process argv | Incorporated in 07-09/10: one versioned 2,101,248-byte/10,016-record stdin protocol carries credentials/payload, argv/environment have no raw capability or fallback, call-local state is cleared, a blocked helper is inspected live, and ID-only/wrong-pair replay has zero effect. |
| Adjusted checker blocker — 249 ms real stall inside a 250 ms whole budget | Incorporated in 07-09/10: the sealed production budget has an unexported fake monotonic clock; exact cumulative 249/251 tests include late-stage consumption without sleep, while 25/500 ms real stalls retain active fail-open coverage and the old acknowledgement. |
| Adjusted checker warning — stale pattern map | Incorporated in 07-PATTERNS.md: sync/resolve are sourced-only, the private set is exactly Attach/Publish/Prepare/Acknowledge/Resolve, and distinct scoped Services share one canonical generation without pointer identity/reopen/fallback/singleton. |

No current actionable review finding is deferred or rejected.
</review_feedback_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| harness -> long-lived shells | Commands exercise parent-shell mutation and may capture sensitive output |
| isolated root -> production Store/worktree | Tests must prove real authority without user-state effects |
| fault/deadline injector -> canonical state | Non-vacuous failure must target the named boundary |
| private credential/reply transport -> canonical Service/parent shell | Tests must distinguish allocation/transport from under-lock verification and prove reply metadata is complete, short-lived, and acknowledgement-bound |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-41 | Information Disclosure | E2E buffers/artifacts | high | mitigate | Isolated fake values, fingerprints, canary absence, redacted failures, and cleanup in Task 1. |
| T-07-42 | Tampering | false-green E2E | high | mitigate | Real binary/loader/Git, distinct PIDs/IDs, direct DTO/state inspection, and negative controls. |
| T-07-43 | Denial of Service | retained shells/helper/validator/eval/capture/ack | high | mitigate | Cleanup-owned whole-transition guard, fake cumulative 249/251 proofs without sleeps, active 25/500 ms tests/process cleanup at every stage, old-ack retention, and same-parent-shell replay/sentinel recovery. |
| T-07-44 | Tampering | crash/fault seams | high | mitigate | Test-scoped injectors, isolated roots, combined canonical before/after, operation receipts, and exhaustive crash/replay matrix in Task 2. |
| T-07-45 | Repudiation | missing edge/prohibition coverage | high | mitigate | Exactly 21 counted predicates plus all nine retained prohibitions and source audit. |
| T-07-46 | Spoofing | shared-process/authority accident | high | mitigate | Distinct process/opaque IDs plus a Task 1 composition test proving distinct public and post-auth descriptor-bound runtime Service instances have equivalent access to the same canonical durable authority; runtime construction begins only after argument validation and authenticated descriptor acquisition, with no pointer-identity assertion, path reopen, or fallback authority. |
| T-07-47 | Spoofing / Information Disclosure | shell capability transport | high | mitigate | Crypto-random allocation after descriptor auth, one bounded stdin-only frame with no fallback, blocked-helper cmdline/environ absence, private-local cleanup, verifier-at-rest, under-lock verification before receipts on all five operations, ID-only public context, and wrong-pair/collision/spoof/cross-shell/crash-replay/re-source tests. |
| T-07-48 | Tampering | Git revision object/tree | high | mitigate | Direct commit-type and exact two-blob tree checks with shaped-object/malformed-tree rejection in Task 1. |
| T-07-49 | Tampering / Information Disclosure | parent reply and descriptor lifecycle | high | mitigate | Exact emitter reply grammar/whole-buffer validation/fresh fingerprint match/always cleanup plus path/descriptor equivalence and duplicate/input close-ownership tests across Tasks 1-2. |

ASVS Level 1 applies; no high-severity oracle, canary, atomicity, deadline, edge-count, or composition gate may fail.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test -count=1 ./...`
- `GOTOOLCHAIN=local go test -race ./core/worktree ./core/store -count=1`
- `GOTOOLCHAIN=local go vet ./...`
- `GOTOOLCHAIN=local go build ./...`
- `make check`
- Build a fresh binary and run `scripts/perf-worktree.sh --cycles 50`; retain the configured finite default, actual p50/p95/max/count, fake cumulative 249/251 evidence at every named stage, active 25/500 ms fail-open evidence, old-ack/replay sentinels, and capability cmdline/environ absence in the summary.
</verification>

<success_criteria>
- All five requirements pass through production binary, real classifier, real Git, and two independent shells.
- All 21 edge probes and nine descriptor-less prohibitions remain visible and non-vacuously covered.
- Zero-process source, authenticated stable attachment, durable under-lock capability verification, exact public arities/routing, pre-mutation publication, commit projection/object validation/migration, conflict resolution including acknowledged losers, executable cross-process reply metadata, crash/ack replay, secret/reply leakage exclusion, path/descriptor authority ownership, whole-transition deadline termination, PATH/FPATH reversibility, fail-open, and zero residue are proven end to end.
- Multi-source audit has no missing item and no out-of-scope surface appears.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-10-SUMMARY.md` when done.
</output>
