---
phase: 07-git-like-shared-working-environment
plan: 13
type: execute
wave: 9
depends_on: ["07-10"]
files_modified:
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/worktree_live_test.go
  - scripts/perf-worktree.sh
autonomous: true
requirements: [WORK-02, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "One absolute 250 ms deadline begins before shell capture and incrementally covers capture, record/byte accounting, serialization, frame delivery, helper response, termination escalation, and reap."
    - "Capture and serialization stop while iterating, before exceeding 10,000 records or 2 MiB of snapshot/frame data; they do not first build an unbounded array and check afterward."
    - "Frame delivery is cancellable when the helper does not read, and a helper that ignores TERM is KILLed and reaped within the same absolute operation budget with no descendant or descriptor leak."
    - "Real zsh oversized-capture, blocked-reader, and TERM-ignoring-helper cases fail open: no acknowledgement or false applied revision occurs, and the next prompt/entered command sentinel executes."
    - "Success, timeout, oversize, and cancellation clear source/snapshot/capability/reply buffers; no value appears in argv, environment, errors, logs, or retained test artifacts."
  artifacts:
    - path: "core/shell/zsh/hook.go"
      provides: "Deadline-aware incremental capture/serialization and cancellable bounded transport/reap"
      contains: "_zp_worktree_abort_transport"
    - path: "core/shell/zsh/worktree_live_test.go"
      provides: "Real-zsh oversized, backpressured, and non-cooperative helper continuation proofs"
      contains: "TestWorktreeAbsoluteDeadlineAdversarialTransport"
    - path: "scripts/perf-worktree.sh"
      provides: "Repeatable absolute-bound adversarial driver plus ordinary prompt metrics"
      contains: "term-ignoring-helper"
  key_links:
    - from: "safe-boundary entry"
      to: "capture and frame writer"
      via: "the same ZP_WORKTREE_TRANSITION_DEADLINE passed to every incremental stage"
      pattern: "ZP_WORKTREE_TRANSITION_DEADLINE"
    - from: "blocked writer/helper"
      to: "usable parent zsh"
      via: "deadline watcher, descriptor close, TERM reserve, KILL escalation, and bounded reap"
      pattern: "_zp_worktree_abort_transport"
    - from: "failed operation"
      to: "durable applied revision"
      via: "no reply validation or acknowledgement after timeout/oversize; visible error/behind state retained"
      pattern: "TestWorktreeAbsoluteDeadlineAdversarialTransport"
---

<objective>
Enforce one absolute 250 ms fail-open boundary around the entire parent-shell worktree operation.

Purpose: Capture, serialization, pipe backpressure, and non-cooperative process cleanup must not escape the advertised prompt/runtime bound or make the next command unusable.
Output: Incremental bounded capture/frame construction, cancellable delivery and reap, real-zsh adversarial tests, and a repeatable sampler.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-09-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-10-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md

<interfaces>
- The sole parent deadline is `ZP_WORKTREE_TRANSITION_DEADLINE = EPOCHREALTIME + 0.250`, created once at the public/safe-boundary entry and passed unchanged through capture, serialization, invoke, apply, capture-after-apply, and acknowledgement.
- Existing Go `DefaultWorktreeTransitionBudget` remains a nested defense, not a replacement clock that extends parent time.
- Use the existing maximums: 10,000 snapshot records and 2 MiB snapshot bytes; the private frame retains its bounded protocol overhead. Count incrementally before retaining or writing each record.
- Reserve the final 25 ms of the same deadline for TERM, descriptor shutdown, KILL escalation, and reap. Signal tracked owned PIDs individually unless code deliberately creates and verifies a distinct child-only process group; never assume zsh coprocesses are isolated from the parent process group. Do not sleep beyond the deadline or wait synchronously on a non-cooperative child.
- Prompt hooks remain fail-open and explicit sync returns nonzero/error/behind. Neither path may acknowledge or advance the applied revision on failure.
- Preserve unrelated live edits in all listed files and read current content before patching.
</interfaces>
</context>

## Artifacts this phase produces

Production symbols and behavior:

- Deadline parameter on `_zp_worktree_capture`, `_zp_worktree_write_snapshot_records`, and `_zp_worktree_write_frame` in `core/shell/zsh/hook.go`.
- `_zp_worktree_capture_append_record` for incremental record/byte/cap/deadline admission.
- `_zp_worktree_abort_transport` for descriptor shutdown, TERM/KILL escalation, and bounded child/writer/closer reap.
- Background cancellable frame-writer ownership in `_zp_worktree_invoke`; all PIDs and descriptors are cleared in `always`.

Test-only artifacts:

- `TestWorktreeCaptureAndFrameUseOneIncrementalDeadline`.
- `TestWorktreeAbsoluteDeadlineAdversarialTransport` with named oversized-capture, blocked-reader, and TERM-ignoring-helper subtests.
- `scripts/perf-worktree.sh` adversarial modes reporting elapsed time, exit/status, reap result, and next-command sentinel without captured values.

No new configurable timeout, dependency, synchronized shell category, worktree, staging/remotes feature, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make capture and frame serialization incremental under the absolute deadline</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: budget_begin/check, worktree_capture, scalar/snapshot writers, write_frame, and every capture caller
    - core/shell/zsh/hook_test.go: deterministic 249/251 ms seams, loader source assertions, and bounded runtime tests
    - core/model/worktree.go: MaxSnapshotRecords/MaxSnapshotBytes and strict frame semantics
    - core/cli/runtime.go: nested 250 ms runtime budget and MaxRuntimeWorktreeFrame limits; read only for contract alignment
    - .planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md: unbounded capture/serialization findings
  </read_first>
  <behavior>
    - RED: fake monotonic time consumed at any capture category or serialization record admits cumulative 249 ms and returns timeout at 251 ms under the original entry deadline.
    - RED: record 10,001 and byte 2 MiB plus one fail before being retained/written; exactly-at-limit input remains valid.
    - RED: every capture caller passes the existing transition deadline and clears partial arrays on timeout/oversize.
  </behavior>
  <action>
    Preserve current tests. Add and commit the failing deterministic boundary/cap tests before changing loader production source. Refactor capture through `_zp_worktree_capture_append_record`: before retaining each record, check the unchanged absolute deadline, record count, encoded byte count, kind/name validity, and category exclusions; abort immediately and clear partial arrays if any limit fails. Do the same before each scalar/snapshot frame write, with the private protocol overhead included. Pass the one entry deadline through attach baseline capture, publish capture, prepare/resolve capture, post-apply capture, and acknowledgement framing; never call budget_begin inside a stage that already has the deadline. Preserve exclusion of PWD/OLDPWD/jobs/history/BUFFER/process-local/internal/secret-like data and preserve exact ordering/duplicate list semantics. Run the focused deterministic tests GREEN and keep error text value-free.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^TestWorktreeCaptureAndFrameUseOneIncrementalDeadline$' -timeout=15s</automated>
  </verify>
  <acceptance_criteria>
    - Every record and byte is admitted before retention/write under the original absolute deadline and exact existing caps.
    - Boundary tests prove cumulative 249 ms admission, 251 ms timeout, exact-limit success, and plus-one failure.
    - Partial/oversized data and capability/reply locals are cleared on all exits, with no acknowledgement or applied-revision advance.
    - The RED test commit precedes the GREEN implementation commit.
  </acceptance_criteria>
  <done>Capture and serialization can no longer consume unbounded prompt time or memory before enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cancel blocked delivery and forcibly reap non-cooperative helpers within the same bound</name>
  <files>core/shell/zsh/hook.go, core/shell/zsh/hook_test.go, core/shell/zsh/worktree_live_test.go</files>
  <read_first>
    - core/shell/zsh/hook.go: `_zp_worktree_invoke` coprocess descriptors, synchronous writer, read loop, wait paths, and always cleanup
    - core/shell/zsh/hook_test.go: fake runtime helper and process visibility/cleanup patterns
    - core/shell/zsh/worktree_live_test.go: built production binary, retained real-zsh process, isolated HOME/ZDOTDIR/root, and next-command sentinel helpers
    - core/cli/runtime.go: EOF/cancellation behavior for blocked stdin and helper context cancellation
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: no foreground-command interruption and fail-open boundary constraints
  </read_first>
  <behavior>
    - RED: a helper that never reads stdin cannot block the parent frame writer beyond the one deadline; writer/helper/closer are terminated and reaped.
    - RED: a helper that traps/ignores TERM is escalated to KILL by the absolute deadline and has no surviving PID/descendant/pipe endpoint.
    - RED: timeout/kill returns 124 internally, prompt hook returns control, explicit sync reports failure/behind, and the next entered-command sentinel runs in the same zsh.
  </behavior>
  <action>
    Commit the failing transport/reap contract before implementation. Make frame delivery cancellable by giving the frame writer its own tracked PID and never synchronously waiting on it without the deadline. Track helper, writer, closer, read FD, and write FD from creation. Use the final 25 ms of the existing 250 ms window as a termination reserve: close parent endpoints and send TERM at reserve start, escalate remaining verified owned PIDs to KILL with enough time still reserved to reap them before the absolute deadline, and never perform an unbounded wait. Only use a group signal if this code explicitly created and verified a child-only PGID distinct from the parent zsh; never signal an assumed coprocess group. `_zp_worktree_abort_transport` must be idempotent and used both on ordinary errors and in `always`; it must never signal an unrelated/reused PID, so validate retained ownership before signalling and zero each PID immediately after reap. Preserve the non-exported capability boundary and clear frame/response/value locals. Add real retained-`zsh -f` cases with isolated fake helper shims for a blocked reader and TERM-ignoring helper, plus production oversized capture from Task 1. Assert no acknowledgement/applied revision advance, visible failure/behind for explicit sync, hook fail-open behavior, dead child PIDs, closed descriptors, and a next-command sentinel in the same shell. Run focused tests GREEN.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^TestWorktreeAbsoluteDeadlineAdversarialTransport$' -timeout=20s</automated>
  </verify>
  <acceptance_criteria>
    - Blocked frame delivery and TERM-ignoring helpers cannot retain the parent beyond the absolute operation budget, apart from a documented generous scheduler allowance in wall-clock tests.
    - Writer/helper/closer processes and every pipe descriptor are gone after success, oversize, blocked read, timeout, TERM-ignore, and signal.
    - Real zsh remains usable for the next command; explicit sync stays behind/error and prompt hooks fail open without false convergence.
    - No raw capability or captured value appears in argv, environment, diagnostics, or retained artifacts.
    - The RED test commit precedes the GREEN implementation commit.
  </acceptance_criteria>
  <done>Cancellable delivery and bounded termination/reap close every prompt-blocking path found by verification.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retain a repeatable real-zsh adversarial deadline driver</name>
  <files>core/shell/zsh/worktree_live_test.go, scripts/perf-worktree.sh</files>
  <read_first>
    - core/shell/zsh/worktree_live_test.go: Task 2 adversarial harness and production binary lifecycle
    - scripts/perf-worktree.sh: existing 50-cycle metrics, 25/500 ms injected stages, process counters, and cleanup traps
    - .planning/phases/07-git-like-shared-working-environment/07-10-PLAN.md: ordinary sampler and 21-edge phase-level disposition
  </read_first>
  <behavior>
    - RED: the driver runs all three adversarial cases, emits value-free elapsed/status/reap/continuation fields, and fails if any sentinel or cleanup assertion is missing.
    - RED: repeated timeout attempts do not accumulate PIDs/FDs, change the acknowledged revision, or convert behind/error into converged.
  </behavior>
  <action>
    Add the failing driver assertions and commit them before updating the script implementation. Extend the existing script rather than creating another authority: retain ordinary count/p50/p95/max and zero Git/child-zsh source counters, then add isolated `oversized-capture`, `blocked-reader`, and `term-ignoring-helper` modes that invoke the real-zsh tests and report only elapsed milliseconds, return class, cleanup counts, applied-revision unchanged, behind/error state, and continuation sentinel. Use temporary roots with trap-based cleanup, record PIDs explicitly, and fail the script if any child survives. Do not print snapshots, values, capabilities, emitted source, history, PWD, jobs, or buffer contents. Re-run each timeout case to prove idempotent cleanup and unchanged durable state.
  </action>
  <verify>
    <automated>bash -n scripts/perf-worktree.sh &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreeCaptureAndFrameUseOneIncrementalDeadline|TestWorktreeAbsoluteDeadlineAdversarialTransport)$' -timeout=25s</automated>
  </verify>
  <acceptance_criteria>
    - The retained driver covers all three missing adversarial workloads through the production loader/real-zsh harness.
    - Output is value-free and sufficient to prove deadline classification, cleanup/reap, no false acknowledgement, and next-command usability.
    - Repeated execution leaves no child, descriptor, state, or test-root residue.
    - The RED contract commit precedes the GREEN driver commit.
  </acceptance_criteria>
  <done>The whole-operation bound has repeatable production-path evidence instead of relying on ordinary small snapshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live parent shell -> capture arrays | Untrusted count and size of supported shell identities enter retained memory. |
| frame writer -> helper stdin | Backpressure crosses a process/pipe boundary carrying capability and snapshot values. |
| parent -> helper process group | Cancellation must target only owned processes and complete before returning control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-13-01 | Denial of Service | capture/serialization | high | mitigate | One entry deadline plus incremental record/byte checks before retention or write; exact boundary/cap tests block release. |
| T-07-13-02 | Denial of Service | blocked pipe/helper reap | high | mitigate | Tracked background writer, termination reserve, FD shutdown, TERM/KILL escalation, bounded reap, and real-zsh continuation proof. |
| T-07-13-03 | Information Disclosure | frame/capability/errors/tests | high | mitigate | Non-exported/call-local capability, protected xtrace/history, value-free errors, buffer clearing, and argv/environment/artifact canaries. |
| T-07-13-04 | Spoofing | PID/PGID reuse during cleanup | high | mitigate | Signal verified retained PIDs; use a group only if a distinct child-only PGID was created and checked; zero ownership immediately after reap and never use broad process matching. |
| T-07-13-05 | Tampering | timeout acknowledgement | high | mitigate | Timeout/oversize cannot parse a reply or call Acknowledge; applied revision remains unchanged and behind/error remains visible. |
| T-07-13-06 | Repudiation | prompt success after failure | medium | mitigate | Prompt hook explicitly fails open while retaining value-free error state; explicit sync returns nonzero and tests distinguish both. |
| T-07-13-07 | Elevation of Privilege | helper transport | high | mitigate | Preserve exact bounded stdin protocol and verifier-only Service authorization; cleanup grants no replay or new capability surface. |

ASVS L1 gate: every high-severity mitigation above is blocking; the plan is incomplete until its automated proof passes.
</threat_model>

<verification>
- Run all task commands and `GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -timeout=60s`.
- Run the isolated adversarial driver against a freshly built production binary and confirm three continuation sentinels, zero survivor counts, and unchanged applied revisions.
- Inspect driver output for the injected value/capability canaries; count must be zero.
</verification>

<success_criteria>
- The verification report's absolute-bound truth is green for capture, serialization, frame delivery, helper termination, and reap.
- Real-zsh oversized capture, blocked reader, and TERM-ignore failures all leave the next command usable and status truthful.
- Gap-specific boundary, idempotency, and repeated-cleanup predicates pass without weakening Plan 07-10's phase-level 21-edge matrix.
</success_criteria>

## Multi-Source Coverage Audit

| Source | Item | Disposition |
|---|---|---|
| GOAL | Safe-boundary synchronization remains bounded and fail-open | COVERED across all stages from entry through reap. |
| REQ | WORK-02, SYNC-01, SYNC-02 | COVERED explicitly. WORK-01/WORK-03 remain covered by Plan 07-10 and are not affected by this timing gap. |
| RESEARCH | 250 ms whole-transition budget, 10k/2 MiB caps, no foreground interruption, value-free diagnostics | COVERED with incremental enforcement and real-zsh evidence. |
| CONTEXT | No Phase 7 CONTEXT.md; auto-apply remains a per-shell policy over one canonical generation | COVERED; both auto and explicit paths consume the same bound without creating another truth source. |
| SPECless fallback | Timing boundary, repeated failure idempotency, and concurrent/process cleanup | COVERED by exact fake-clock limits plus repeated real process cleanup; Plan 07-10 retains the remaining phase-level matrix. |

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-13-SUMMARY.md` when done.
</output>
