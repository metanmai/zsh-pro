---
phase: 07-git-like-shared-working-environment
plan: 17
type: execute
wave: 13
depends_on: ["07-16"]
files_modified:
  - core/worktree/state.go
  - core/worktree/state_test.go
  - core/worktree/service.go
  - core/worktree/service_test.go
  - core/cli/emitter.go
  - core/cli/emitter_test.go
  - core/cli/runtime.go
  - core/cli/runtime_test.go
  - core/shell/zsh/hook.go
  - core/shell/zsh/hook_test.go
  - core/shell/zsh/worktree_live_test.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-01, WORK-02, SYNC-01, SYNC-02]
gap_closure: true
must_haves:
  truths:
    - "[CR-04 / WORK-01 / WORK-02 / SYNC-01 / SYNC-02] An authenticated, clean shell can retire its durable record; 128 ordinary attach/release cycles do not prevent the 129th distinct attach."
    - "Only explicitly retired terminal records are reclaimed, in deterministic revision/shell-ID order; active, unpublished, behind, pending, conflicted, recovery-bearing, or concurrently mutating shells are never inferred dead or evicted."
    - "Release is authenticated by the original descriptor-bound capability, idempotent while its bounded receipt remains, value-free on every surface, and safe against wrong-capability, cross-shell, guessed-operation, concurrent publish, and stale replay attempts."
    - "Normal `zshexit` and public deactivation publish settled local state before release, keep credentials until authenticated acknowledgement, and retain the one absolute 250 ms lifecycle budget."
    - "The loader's exact symbol allowlist names every new release helper and parameter explicitly, including `_zp_worktree_detach` and `zshexit_functions`, so no unintended global symbol is accepted."
  artifacts:
    - path: core/worktree/state.go
      provides: "validated detached tombstones, bounded release receipts, and deterministic reclamation eligibility/order"
    - path: core/worktree/service.go
      provides: "transactional authenticated Release and attach-time terminal-record reclamation"
    - path: core/cli/runtime.go
      provides: "strict release mode on the existing private attach operation"
    - path: core/shell/zsh/hook.go
      provides: "cooperative release from zshexit and deactivate"
    - path: core/worktree/service_test.go
      provides: "129th-shell, concurrency, replay, cap, and protected-state oracle"
    - path: core/shell/zsh/worktree_live_test.go
      provides: "retained-zsh churn and lifecycle evidence"
    - path: core/cmd/zsh-pro/main_test.go
      provides: "exact installed loader parameter/function allowlist for release lifecycle symbols"
  key_links:
    - from: core/shell/zsh/hook.go
      to: core/cli/runtime.go
      via: "_zp_worktree_detach uses mode=release on the existing descriptor-bound attach operation"
      pattern: "_zp_worktree_(detach|invoke)|mode.*release"
    - from: core/cli/emitter.go
      to: core/worktree/service.go
      via: "runtime adapter forwards the authenticated credential and operation ID without path re-resolution"
      pattern: "Release|runtimeWorktreeService"
    - from: core/worktree/service.go
      to: core/worktree/state.go
      via: "one StateStore transaction serializes release versus publish and prunes only validated detached tombstones"
      pattern: "MaxShellRecords|OperationReceipts|Detached"
    - from: core/shell/zsh/hook.go
      to: core/cmd/zsh-pro/main_test.go
      via: "TestMainIngestAllowsOnlyExactLoaderSymbols enumerates exactly the new _zp_worktree_detach helper and zshexit_functions parameter"
      pattern: "_zp_worktree_detach|zshexit_functions|TestMainIngestAllowsOnlyExactLoaderSymbols"
---

<objective>
Close CR-04 by adding an authenticated terminal shell lifecycle and deterministic safe reclamation, without weakening capability, receipt, concurrency, or prompt-latency contracts.

Purpose: Historical shell records must not permanently exhaust the 128-shell limit, while the system must never guess that a live authority is dead.
Output: Transactional release/tombstone semantics, attach-time deterministic reclamation, strict private runtime wiring, cooperative zsh exit/deactivation, and 129th-shell/replay/concurrency/security evidence.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-VERIFICATION.md
@.planning/phases/07-git-like-shared-working-environment/07-REVIEW.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-16-SUMMARY.md
@.codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md
@core/worktree/state.go
@core/worktree/state_test.go
@core/worktree/service.go
@core/worktree/service_test.go
@core/cli/emitter.go
@core/cli/emitter_test.go
@core/cli/runtime.go
@core/cli/runtime_test.go
@core/shell/zsh/hook.go
@core/shell/zsh/hook_test.go
@core/shell/zsh/worktree_live_test.go
@core/cmd/zsh-pro/main_test.go
</context>

<interfaces>
- `core/worktree/state.go` defines `MaxShellRecords = 128`, `MaxOperationReceiptsPerShell = 64`, `State.Shells`, `State.OperationReceipts`, and the currently unused/unsafe `CanGarbageCollectShell` decision surface.
- `core/worktree/service.go::Attach` rejects admission once `len(state.Shells) >= MaxShellRecords`; publish/prepare/acknowledge/resolve already serialize through `StateStore.WithTransaction` and authenticate shell credentials.
- Operation receipts currently reference an existing shell. Reclaim a detached shell and its receipts in the same transaction so state validation never observes a dangling reference.
- `core/cli/runtime.go::runRuntimeWorktree` exposes exactly five private operation tags: attach, publish, prepare, acknowledge, resolve. Extend the exact attach frame grammar with `mode=release`; do not add a sixth operation tag.
- `_zp_worktree_invoke` is descriptor/capability-bound and, after Plan 07-16, owns portable exact cleanup under one absolute 250 ms deadline.
- Hook registration currently covers `precmd_functions` and line-finish synchronization. Add cooperative `zshexit_functions` ownership without duplicating a registration or deleting another plugin's hook.
</interfaces>

<scope_fence>
- Close CR-04 only. CR-01 through CR-03 are Plan 07-16; CR-05 is split between Plans 07-18 and 07-19.
- Keep WR-01 (`Provider.LiveCaptureSource`) excluded.
- Do not infer liveness from PID, time, host, process name, `/proc`, heartbeat age, missing prompt activity, or record count. Only an authenticated committed release creates a reclaimable record.
- Do not expose raw capability/token/source/state values, add a sixth private runtime operation, run Git from hooks, discover the root ambiently, or extend the 250 ms parent-shell budget.
</scope_fence>

<scope_budget>
- Plan budget: 3 tasks and 12 unique implementation/test files; each task owns at most 5 files and one cohesive lifecycle boundary.
- Task 1: four independently failing RED contracts across 5 test files. Task 2: domain/private-runtime retirement across 5 files. Task 3: zsh lifecycle wiring and exact loader-symbol regression across 2 files.
- CR-05 persistence and runtime recovery remain outside this plan and execute only in dependent Plans 07-18 and 07-19.
</scope_budget>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Commit the failing authenticated retirement and 129th-shell contract</name>
  <files>core/worktree/state_test.go, core/worktree/service_test.go, core/cli/runtime_test.go, core/shell/zsh/hook_test.go, core/shell/zsh/worktree_live_test.go</files>
  <behavior>
    - 128 authenticated attach/release cycles followed by a 129th distinct attach stay within shell/receipt caps and select the same reclaim candidate deterministically.
    - A retained release replay returns the identical bounded value-free result; wrong capability, cross-shell credential, guessed operation ID, mismatched request, or replay after reclamation cannot delete or inspect another shell.
    - Active, unpublished, behind, pending, conflicted, recovery-bearing, and concurrently publishing shells are never candidates; a deterministic release-versus-publish barrier yields one serialized complete outcome.
    - The exact five private operation names remain unchanged while attach mode `release` enforces the minimal exact frame and rejects extra/missing/duplicate records.
    - Retained zsh normal exit and public deactivate publish settled state before release; credentials/hooks are cleared only after authenticated success, and failure leaves safe retryable authority without blocking exit.
  </behavior>
  <action>
    Add `TestAuthenticatedReleaseReclaimsOnlyRetiredShells` in `service_test.go` and supporting state validation cases in `state_test.go`. Exercise exactly 128 distinct credentials through attach/release, then admit shell 129; assert deterministic `(retired revision, shell ID)` ordering, exact cap/receipt bounds, and atomic removal of the tombstone plus its receipts. Include protected lifecycle matrices and a channel/barrier-controlled publish-versus-release interleaving with no sleeps.

    Add `TestRuntimeAttachReleaseLifecycle` in `runtime_test.go`, `TestWorktreeDetachLifecycle` in `hook_test.go`, and `TestWorktreeAuthenticatedReleaseChurn` in `worktree_live_test.go`. Cover exact frame grammar, same-operation replay, cross-shell/wrong-capability/guessed-ID/mismatched-payload rejection, success/failure cleanup order, 129 retained-zsh lifecycles, and a next-command sentinel. Assert errors and replies contain none of the injected capability, token, descriptor, shell payload, alias, function, or root-path canaries.

    Run the focused tests against current production and confirm they fail because no authenticated terminal lifecycle exists and Attach permanently counts history, not because of compilation or fixture setup. Commit all blocker-focused RED tests as `test(07-17): add failing authenticated shell retirement contract` with normal hooks. Do not edit production in this task.
  </action>
  <verify>
    <automated>bash -o pipefail -c "set -e; run_red() { red_pkg=\$1; red_name=\$2; red_log=\$3; set +e; GOTOOLCHAIN=local go test -count=1 \"\$red_pkg\" -run \"^\${red_name}\$\" -timeout=180s &gt;\"\$red_log\" 2&gt;&amp;1; red_rc=\$?; set -e; cat \"\$red_log\"; test \"\$red_rc\" -ne 0; ! grep -Eq -- 'undefined:|build failed' \"\$red_log\"; }; red1=\$(mktemp); red2=\$(mktemp); red3=\$(mktemp); red4=\$(mktemp); trap 'rm -f \"\$red1\" \"\$red2\" \"\$red3\" \"\$red4\"' EXIT; run_red ./core/worktree TestAuthenticatedReleaseReclaimsOnlyRetiredShells \"\$red1\"; grep -Eq -- '--- FAIL: TestAuthenticatedReleaseReclaimsOnlyRetiredShells([[:space:]]|$)' \"\$red1\"; run_red ./core/cli TestRuntimeAttachReleaseLifecycle \"\$red2\"; grep -Eq -- '--- FAIL: TestRuntimeAttachReleaseLifecycle([[:space:]]|$)' \"\$red2\"; run_red ./core/shell/zsh TestWorktreeDetachLifecycle \"\$red3\"; grep -Eq -- '--- FAIL: TestWorktreeDetachLifecycle([[:space:]]|$)' \"\$red3\"; run_red ./core/shell/zsh TestWorktreeAuthenticatedReleaseChurn \"\$red4\"; grep -Eq -- '--- FAIL: TestWorktreeAuthenticatedReleaseChurn([[:space:]]|$)' \"\$red4\""</automated>
  </verify>
  <done>The CR-04 tests compile, fail at the missing retirement/reclamation behavior, and are committed before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional release, tombstones, and strict private runtime replay</name>
  <files>core/worktree/state.go, core/worktree/service.go, core/cli/emitter.go, core/cli/emitter_test.go, core/cli/runtime.go</files>
  <behavior>
    - Authenticated release is idempotent while retained, state-valid, value-free, and serialized against every shell mutation.
    - Attach reclaims only terminal released records and their receipts when necessary for admission, with deterministic ordering and no live-state heuristic.
    - Runtime release reuses the existing attach operation tag and exact descriptor-bound request channel.
  </behavior>
  <action>
    Replace `CanGarbageCollectShell` with an explicit validated terminal lifecycle representation on `ShellState` (for example, active versus detached plus a canonical retired revision). A release may transition only an authenticated shell whose published/current/applied state is settled at the exact shared revision, with no conflict, pending apply/ack, recovery marker, unpublished local delta, or concurrent mutation. Retain only the one-way capability verifier and the bounded terminal receipt needed for authenticated replay; scrub live baselines, conflict material, tokens, pending state, and other no-longer-needed authority. Never persist a raw capability or token.

    Add `Service.Release` under the same `StateStore.WithTransaction` lock used by publish/prepare/acknowledge. Authenticate in constant time before lifecycle-specific disclosure, match operation ID plus canonical request digest for replay, and return one fixed bounded result. The first successful release writes the detached tombstone and receipt atomically. A retained identical replay returns the exact receipt without revision/event churn; mismatched or unauthorized requests use the existing uniform invalid-authority class.

    Before a new Attach returns the cap error, deterministically select only validated detached tombstones ordered by retired revision then shell ID. Remove the selected tombstone and every receipt referencing it in the same transaction, revalidate caps, then admit the new shell. If no detached candidate exists, retain the 128-shell rejection. Never evict active or protected records and never use time/PID/host/activity heuristics.

    Extend `runtimeWorktreeService` and its test spy with release. In the existing `attach` operation, add exact `mode=release` grammar requiring only operation, mode, shell ID, capability, operation ID, and final record. Route it through the already authenticated descriptor-bound adapter and encode a fixed value-free result. Keep the public private-operation set at exactly five and preserve frame/record/size/deadline checks. Commit `fix(07-17): add authenticated bounded shell retirement` after service/runtime focused tests and the existing exact-five-operation test pass.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/worktree -run '^(TestAuthenticatedReleaseReclaimsOnlyRetiredShells|TestState|TestAttach|TestPublish|TestPrepare|TestAcknowledge|TestResolve)' -timeout=180s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^(TestRuntimeAttachReleaseLifecycle|TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations|TestRuntimeWorktreeFrameExactByteAndRecordBounds)' -timeout=120s</automated>
  </verify>
  <done>Core and private runtime tests prove authenticated terminal transition, exact replay, deterministic cap reclamation, protected-state non-eviction, receipt referential integrity, and exactly five private operation tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire cooperative zshexit and deactivate release under the existing deadline</name>
  <files>core/shell/zsh/hook.go, core/cmd/zsh-pro/main_test.go</files>
  <behavior>
    - Normal exit/deactivation performs fresh capture and publish before authenticated release.
    - Successful release unregisters only owned hooks and clears credentials after acknowledgement; failure preserves safe non-reclaimable authority and never blocks shell exit beyond the original deadline.
    - Repeated activation/deactivation and 129-shell churn leave no duplicate hooks, leaked descriptors, raw secrets, or live-record eviction.
    - Built-loader ingest adds only the exact intended lifecycle symbols: `_zp_worktree_detach` and the `zshexit_functions` parameter entry.
  </behavior>
  <action>
    Add `_zp_worktree_detach` using `_zp_worktree_invoke attach ... release` with a fresh operation ID and the original capability/descriptor authority. It must run inside the caller's existing absolute transition deadline, not start a new 250 ms window. Fresh-capture/publish settled local state first; if publish cannot establish the exact clean revision, do not release and leave the record active.

    Register `_zp_worktree_detach` itself once in `zshexit_functions` alongside existing hook ownership; do not introduce a second global exit-wrapper function. On normal exit, attempt publish then release, retain fail-open shell behavior, and never reverse-apply state to a shell that is exiting. On public `deactivate`, publish, release, then unregister owned publish/pull/exit hooks and perform existing reverse cleanup. Clear shell ID/capability/baselines only after the exact authenticated release acknowledgement. A timeout or malformed reply leaves credentials and active durable authority available for explicit retry; it must not mark the record detached locally.

    Update `TestMainIngestAllowsOnlyExactLoaderSymbols` in `main_test.go`: add exactly `_zp_worktree_detach` to `wantFunctions` and exactly `zshexit_functions` to `wantParameters`. Do not use a prefix, wildcard, relaxed subset, or an additional global exit-wrapper. The test must continue to reject any other loader-visible parameter or function delta.

    Preserve Plan 07-16's portable exact child/FD cleanup, xtrace/history protection, one absolute 250 ms budget, and next-command sentinel. Commit `fix(07-17): wire cooperative shell retirement` after all focused retained-zsh and hook tests pass. Use normal hooks and do not squash the RED commit.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=local go test -count=1 ./core/shell/zsh -run '^(TestWorktreeDetachLifecycle|TestWorktreeAuthenticatedReleaseChurn|TestWorktreePortableOwnedTransport|TestWorktreeAbsoluteDeadlineAdversarialTransport|TestWorktreeTwoShellEndToEnd)$' -timeout=240s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations$' -timeout=60s &amp;&amp; GOTOOLCHAIN=local go test -count=1 ./core/cmd/zsh-pro -run '^TestMainIngestAllowsOnlyExactLoaderSymbols$' -timeout=120s</automated>
  </verify>
  <done>Normal exit/deactivation retires only authenticated settled shells, 129th-shell churn succeeds, failure remains safe/retryable, hook/descriptor/capability cleanup stays exact and bounded, and the built loader exposes only the explicitly allowlisted lifecycle symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained zsh -> private runtime | Shell ID, operation ID, and capability cross a descriptor-bound authenticated frame. |
| active shell -> detached tombstone | A live mutation authority becomes reclaimable only through a serialized authenticated terminal transition. |
| detached tombstone -> Attach admission | Reclamation changes cap accounting and deletes receipts under the state lock. |
| exit/deactivate -> durable state | Ephemeral shell shutdown must publish before relinquishing authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-17-01 | Spoofing | `Service.Release` | critical | mitigate | Existing one-way capability verifier, constant-time comparison, shell binding, operation ID and request digest, uniform invalid-authority result. |
| T-07-17-02 | Tampering | release versus publish | critical | mitigate | One StateStore transaction/lock serializes the complete transition; barrier tests prove no mixed lifecycle. |
| T-07-17-03 | Elevation of Privilege | attach-time GC | critical | mitigate | Only explicit validated detached tombstones are eligible; no inferred liveness; deterministic order; protected-state matrix. |
| T-07-17-04 | Repudiation | release replay | high | mitigate | Bounded terminal receipt returns identical result while retained and rejects operation/request mismatch without revision churn. |
| T-07-17-05 | Information Disclosure | release replies/errors | high | mitigate | Fixed value-free encoding and canary scans prohibit capability, token, descriptor, source, state, or root leakage. |
| T-07-17-06 | Denial of Service | 128-shell cap | high | mitigate | Atomic terminal-record/receipt reclamation before cap rejection; 128/129 and repeated churn tests. |
| T-07-17-07 | Denial of Service | exit hook | high | mitigate | One inherited 250 ms deadline, portable owned cleanup, fail-open exit, no wait beyond the reserve. |
| T-07-17-08 | Tampering | receipt referential integrity | high | mitigate | Tombstone and all referencing receipts are deleted together and full state validation runs before commit. |
| T-07-17-09 | Information Disclosure | detached record | high | mitigate | Scrub live baselines/conflicts/tokens/pending values; retain only one-way verifier and bounded receipt metadata. |
| T-07-17-10 | Tampering | root/descriptor TOCTOU | high | mitigate | Reuse the authenticated runtime root descriptors; do not reopen from ambient path or add hook Git access. |
| T-07-17-11 | Elevation of Privilege | loader global namespace | high | mitigate | Exact built-loader symbol-delta allowlist names the detach/exit additions and rejects every unplanned global parameter or function. |
</threat_model>

<verification>
After Task 3:

1. Focused CR-04 gates: run all task-local commands.
2. Cap/concurrency/replay loop: `GOTOOLCHAIN=local go test -count=20 ./core/worktree -run '^TestAuthenticatedReleaseReclaimsOnlyRetiredShells$' -timeout=240s`.
3. Retained-zsh churn/adversarial: `GOTOOLCHAIN=local go test -count=5 ./core/shell/zsh -run '^(TestWorktreeAuthenticatedReleaseChurn|TestWorktreeDetachLifecycle|TestWorktreeAbsoluteDeadlineAdversarialTransport)$' -timeout=300s`.
4. Private-surface/security contract: `GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^(TestRuntimeAttachReleaseLifecycle|TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations|TestRuntimeWorktreeFrameExactByteAndRecordBounds|TestRuntimeWorktreeReply)' -timeout=120s`.
5. Full suite: `GOTOOLCHAIN=local go test -count=1 ./... -timeout=300s`.
6. Race suite: `GOTOOLCHAIN=local go test -race -count=1 ./core/worktree ./core/cli ./core/shell/zsh -timeout=360s`.
7. Static/build gates: `GOTOOLCHAIN=local go vet ./... &amp;&amp; GOTOOLCHAIN=local golangci-lint run ./... &amp;&amp; GOTOOLCHAIN=local go build ./...`.
8. No sixth private operation: `GOTOOLCHAIN=local go test -count=1 ./core/cli -run '^TestRuntimeWorktreeContractHasExactlyFiveCredentialForwardingOperations$'`.
9. Exact loader-symbol surface: `GOTOOLCHAIN=local go test -count=1 ./core/cmd/zsh-pro -run '^TestMainIngestAllowsOnlyExactLoaderSymbols$' -timeout=120s`.
</verification>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage | Status |
|---|---|---|---|
| GOAL | Historical shells cannot permanently exhaust safe shared-worktree admission | All three tasks | COVERED |
| REQ | WORK-01, WORK-02, SYNC-01, SYNC-02 | Frontmatter truths, release/reclamation tasks, focused/full gates | COVERED |
| RESEARCH | Descriptor/capability authority, value-free bounded replies, 250 ms budget, exact receipts, no hook Git/root discovery | Scope fence, actions, threat model, verification | COVERED |
| CONTEXT | No Phase 07 CONTEXT.md; user approved proceeding without it | No D-XX decisions invented | COVERED |
| REVIEW | CR-04 | RED/GREEN tasks, threat model, and focused/full verification gates | COVERED |
| REVIEW | CR-01/02/03 and CR-05 | Owned by prerequisite 07-16 and dependent 07-18/07-19 | EXCLUDED — other closure plans |
| REVIEW | WR-01 `LiveCaptureSource` warning | Explicitly excluded by user direction | EXCLUDED |
</source_coverage_audit>

<success_criteria>
- CR-04 has a focused RED commit before production and both GREEN commits use normal hooks.
- The 129th distinct shell attaches after ordinary authenticated release churn without exceeding shell/receipt caps.
- No active or protected record can be reclaimed; no liveness heuristic exists; concurrency and replay are deterministic.
- Release remains descriptor/capability-bound, value-free, exact-five-operation, and within the inherited 250 ms budget.
- The exact built-loader symbol allowlist includes the release lifecycle additions and rejects all other global deltas.
- CR-05 and WR-01 remain untouched for their declared owners.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-17-SUMMARY.md` when done. Do not update STATE.md, ROADMAP.md, REQUIREMENTS.md, prior plans/summaries, stash, or unrelated paths.
</output>
