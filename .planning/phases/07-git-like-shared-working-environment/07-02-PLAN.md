---
phase: 07-git-like-shared-working-environment
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - core/shell/provider.go
  - core/shell/zsh/classify.go
  - core/shell/zsh/classify_test.go
  - core/worktree/registry.go
  - core/worktree/registry_test.go
autonomous: true
requirements: [WORK-02, SYNC-02]
must_haves:
  truths:
    - "[WORK-02] Registry ownership starts only from final EffectiveManaged and Representable materialized identities."
    - "[WORK-02] Existing SecretRef identities are pinned as secret-owned and their resolved literals cannot enter snapshots, overlays, events, patches, or Git projections."
    - "[WORK-02] The production live-secret verdict is implemented by `zsh.Provider.IsLiveSecretIdentity` in `core/shell/zsh/classify.go`, reusing the existing classifier-owned `secretRe`/`Classify` policy rather than creating a second classifier."
    - "[WORK-02] Only a safe identity absent at attachment may be admitted; pre-existing unmanaged, volatile, bookkeeping, unsafe, unsupported, and secret identities remain excluded with value-free reasons."
    - "[SYNC-02] Registry attachment knowledge is category/name exact and can be durably copied into per-shell state by Plan 07-03."
  artifacts:
    - path: "core/shell/provider.go"
      provides: "Narrow LiveSecretPolicy, WorktreeRegenerator, live snapshot, and live patch interfaces for later consumers"
      contains: "type LiveSecretPolicy interface"
    - path: "core/shell/zsh/classify.go"
      provides: "Production live identity secret verdict owned by the existing zsh classifier"
      contains: "func (Provider) IsLiveSecretIdentity"
    - path: "core/worktree/registry.go"
      provides: "Materialized seeding, SecretRef pinning, attachment exclusions, and absent-at-attach admission"
      contains: "type Registry struct"
  key_links:
    - from: "zsh.Provider.IsLiveSecretIdentity"
      to: "worktree.Registry.Admit"
      via: "injected LiveSecretPolicy; no copied regex or Store-side reclassification"
      pattern: "IsLiveSecretIdentity"
    - from: "model.Entry.Secret"
      to: "Registry pinned secret identities"
      via: "materialization seed excludes runtime-resolved literals from all live value paths"
      pattern: "Secret"
    - from: "Registry.Attach"
      to: "Registry.Admit"
      via: "exact present-at-attach unmanaged identity set"
      pattern: "PresentAtAttach"
  prohibitions:
    - statement: "MUST NOT turn an ambient inherited shell snapshot into a managed profile or silently capture a newly observed secret-like value."
      category: privacy
      status: unresolved
      flagged: true
---

<objective>
Establish the production admission and live-secret boundary before persistence.

Purpose: Ensure no captured value crosses into shared state unless profile ownership or absent-at-attach safe admission explicitly authorizes it.
Output: Narrow shell interfaces, the real zsh classifier seam, SecretRef pinning, and category-aware registry admission.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-PATTERNS.md
@.planning/phases/07-git-like-shared-working-environment/07-01-SUMMARY.md
@core/shell/provider.go
@core/shell/zsh/classify.go
@core/store/secret.go
@core/model/profile.go

<interfaces>
- `shell.LiveSecretPolicy` has the exact method `IsLiveSecretIdentity(model.Identity) bool`; `zsh.Provider` implements it in the existing classifier file.
- `shell.WorktreeRegenerator`, `LiveCaptureSource`, `LiveSnapshotDecoder`, and `LivePatchEmitter` are narrow optional interfaces and are not added to the broad `shell.Provider` composite before their implementations exist.
- `Registry` consumes a LiveSecretPolicy and never imports `core/shell/zsh`.
</interfaces>
</context>

## Artifacts this phase produces

- `core/shell/provider.go` adds narrow interfaces: `LiveSecretPolicy`, `WorktreeRegenerator`, `LiveCaptureSource`, `LiveSnapshotDecoder`, and `LivePatchEmitter`; existing interfaces remain compatible.
- `core/shell/zsh/classify.go` adds `Provider.IsLiveSecretIdentity`, delegating the supported live-env identity through the existing classifier-owned secret rule; no new regex or value logger is added.
- New `core/worktree/registry.go`: `Registry`, `AdmissionReason`, `NewRegistry`, `Seed`, `Attach`, `Admit`, and immutable attachment/exclusion results.
- Registry seeding records profile ownership and separately pins valid `SecretRef` identities so their runtime values are never requested by live diff/projection code.
- Tests include the concrete zsh provider, not only fake functions, for ordinary/secret identity decisions.
- No persistent file, CLI verb, zsh mutation source, or package dependency is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose and implement the classifier-owned live-secret policy</name>
  <files>core/shell/provider.go, core/shell/zsh/classify.go, core/shell/zsh/classify_test.go</files>
  <read_first>
    - core/shell/provider.go: current interface segregation and Provider compatibility
    - core/shell/zsh/classify.go: existing secretRe and Classify precedence
    - core/shell/zsh/classify_test.go: production classifier matrix
    - core/store/secret.go: Store consumes Entry.Category and must remain shell-agnostic
    - core/model/worktree.go: exact live Identity kinds and validation
  </read_first>
  <action>
    Preserve all current files. Add the narrow interfaces listed above without widening the broad Provider composite. Implement `zsh.Provider.IsLiveSecretIdentity` in `classify.go`; for supported environment identities, construct the classifier-equivalent assignment identity and reuse the same `Provider.Classify`/`secretRe` authority that marks ingest entries CatSecrets. Other supported kinds follow the existing classifier category semantics and do not gain an invented value regex. Reject malformed kinds/names before classification. Add concrete-provider tests proving secret and non-secret env identity decisions, exact case behavior, malformed inputs, and unchanged historical `Classify` results. Do not import zsh from Store/worktree and do not copy `secretRe` into a new file.
  </action>
  <acceptance_criteria>
    - One existing zsh classifier policy owns both ingest and live identity secret verdicts.
    - The new seam is narrow and does not force unrelated Provider consumers to implement live methods.
    - Real-provider tests fail if the production classifier is bypassed or duplicated.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/shell/zsh -run 'Test(Classify|LiveSecret)' -count=1</automated>
  </verify>
  <done>The production classifier provides the sole live-secret identity authority through an injectable narrow seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed ownership, pin SecretRefs, and admit only absent-at-attach identities</name>
  <files>core/worktree/registry.go, core/worktree/registry_test.go</files>
  <read_first>
    - core/model/worktree.go: identity/value/attachment contracts
    - core/model/profile.go: EffectiveManaged, Representable, and SecretRef fields
    - core/ir/route.go: conservative reversible-state routing
    - core/shell/provider.go: Task 1 LiveSecretPolicy contract
    - .codex/skills/spike-findings-zsh-pro/references/live-shell-state-synchronization.md: registry and absent-at-attach rule
  </read_first>
  <action>
    Implement `Registry` as exact kind/name ownership plus injected policy, not a namespace regex. Seed only final EffectiveManaged and Representable profile identities. If a valid materialized entry has a `SecretRef`, mark it pinned-secret: retain identity ownership metadata while making its live value ineligible for baseline, overlay, event, patch, and committed projection output. `Attach` classifies the initial validated snapshot into managed baseline identities and value-free present-unmanaged exclusions. `Admit` permits only supported safe names that were absent at attachment and pass bookkeeping/volatile and injected production-secret checks. Return stable value-free reasons for every exclusion. Add fake-policy unit tables and a real `zsh.Provider{}` integration test covering inherited ordinary identities, inherited secret-like identities, pinned SecretRefs with resolved canaries, and safe identities created after attachment.
  </action>
  <acceptance_criteria>
    - Pre-existing unmanaged identities remain excluded for the shell lifetime.
    - Safe post-attach identities can be admitted; secret-like post-attach identities cannot.
    - Existing SecretRef resolved literals never enter any returned value-bearing collection.
    - Every rejection has a stable value-free reason.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/worktree ./core/shell/zsh -run 'Test(Registry|Admission|Attach|PinnedSecret|LiveSecret)' -count=1</automated>
  </verify>
  <done>The registry enforces profile ownership, exact attachment history, and the real classifier secret boundary before persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| live identity -> zsh classifier | Newly observed names request a production secret verdict |
| materialized Profile -> registry | Owned, ambient, and SecretRef identities are separated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-05 | Information Disclosure | live admission | high | mitigate | Real classifier seam before delta construction, SecretRef pinning, value-free reasons, and canary tests in Tasks 1-2. |
| T-07-06 | Tampering | ambient identity admission | high | mitigate | Exact attachment set plus absent-only admission and real-provider inherited/post-attach tests in Task 2. |
| T-07-07 | Elevation of Privilege | duplicated/changed secret classifier | high | mitigate | One implementation in classify.go reuses Classify/secretRe; structural and behavior tests prevent a second regex. |
| T-07-08 | Repudiation | exclusions | medium | mitigate | Stable reason enums and exact identity-only records in Task 2. |

ASVS Level 1 applies; all high-severity admission controls are blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/worktree ./core/shell/zsh -count=1`
- `GOTOOLCHAIN=local go vet ./core/worktree ./core/shell/zsh`
- Confirm only `core/shell/zsh/classify.go` defines the secret-name rule and `core/worktree` does not import concrete zsh.
</verification>

<success_criteria>
- The existing zsh classifier is the production authority for live secret identities.
- Profile-owned, pinned-secret, inherited-unmanaged, and safe-new identities have explicit non-overlapping outcomes.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-02-SUMMARY.md` when done.
</output>
