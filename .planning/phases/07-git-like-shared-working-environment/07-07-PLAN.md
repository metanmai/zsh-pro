---
phase: 07-git-like-shared-working-environment
plan: 07
type: execute
wave: 6
depends_on: ["07-02", "07-03", "07-05", "07-06"]
files_modified:
  - core/model/ingest_transaction.go
  - core/cli/ingest.go
  - core/cli/ingest_test.go
  - core/cmd/zsh-pro/main.go
  - core/cmd/zsh-pro/main_test.go
autonomous: true
requirements: [WORK-01, WORK-02, SYNC-01, SYNC-02]
must_haves:
  truths:
    - "[WORK-01] Authoritative ingest evidence carries the exact published OID and materializes one clean shared worktree after commit/finalize; exact repeat is a no-op."
    - "[WORK-01] Published commit plus materialization failure remains ProfileCommitted plus recovery-required and is automatically reconstructed from the exact committed DTO/OID on next status/sync before hooks operate."
    - "[WORK-02] `main.go` injects the concrete `zsh.Provider` LiveSecretPolicy into Registry and the same Registry into the one Service, so production never falls back to a fake/absent classifier."
    - "[WORK-02] Materialization pins existing SecretRef identities before any shell attachment can expose resolved literals."
    - "[SYNC-01/SYNC-02] One concrete Store, StateStore, Registry, and Service instance owns CLI/materializer/runtime consumers; construction failures remain fail-closed without replacement instances."
  artifacts:
    - path: "core/model/ingest_transaction.go"
      provides: "Validated exact PublishedRevision on authoritative ingest evidence"
      contains: "PublishedRevision"
    - path: "core/cli/ingest.go"
      provides: "Post-commit exact materialization and exact-OID repair"
      contains: "WorktreeMaterializer"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "One production Store/StateStore/Registry/Service graph with concrete zsh live-secret policy injection"
      contains: "NewRegistry"
  key_links:
    - from: "IngestCommitOutcome.PublishedRevision"
      to: "Service.Materialize"
      via: "exact committed full Profile/CommittedWorktree after authoritative publication evidence"
      pattern: "PublishedRevision"
    - from: "zsh.Provider{}"
      to: "worktree.Registry then Service"
      via: "main.go injects the concrete LiveSecretPolicy and reuses that exact registry/service identity"
      pattern: "IsLiveSecretIdentity"
    - from: "status/sync repair"
      to: "Store.ReadWorktreeRevision"
      via: "exact published OID rather than inferred current source"
      pattern: "ReadWorktreeRevision"
---

<objective>
Turn ingest into exact one-time bootstrap and wire the real production authority graph.

Purpose: Seed shared worktree state truthfully, recover split publication deterministically, and ensure the real zsh secret policy—not a test seam—guards every production admission.
Output: Exact ingest evidence/materialization, automatic exact-OID repair, and one composition-root Store/Registry/Service graph.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-git-like-shared-working-environment/07-RESEARCH.md
@.planning/phases/07-git-like-shared-working-environment/07-02-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-03-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-05-SUMMARY.md
@.planning/phases/07-git-like-shared-working-environment/07-06-SUMMARY.md
@core/model/ingest_transaction.go
@core/cli/ingest.go
@core/cmd/zsh-pro/main.go

<interfaces>
- `IngestCommitOutcome.PublishedRevision *string` is present only with authoritative committed/recovery evidence and is defensively copied/validated.
- `WorktreeMaterializer` receives the committed redacted full Profile plus exact branch/OID and delegates projection/materialization to the exact Service.
- `main.go` creates one concrete zsh provider value, passes it to `worktree.NewRegistry` as LiveSecretPolicy, seeds the registry during Materialize, and injects the same Service into CLI/workflow/materializer consumers.
</interfaces>
</context>

## Artifacts this phase produces

- `core/model/ingest_transaction.go` gains exact optional PublishedRevision with validation tied to authoritative publication truth.
- `core/cli/ingest.go` gains a narrow materializer/repair participant after committed evidence and existing finalize ordering.
- `core/cmd/zsh-pro/main.go` constructs exactly one Store, StateStore, zsh Provider, Registry, and Service; Registry receives the concrete provider policy and Service receives that exact Registry.
- Main/ingest tests prove first/repeat materialization, committed/state split truth, exact-OID status repair, SecretRef pinning, real-policy injection, instance identity, typed-nil construction failures, and unaffected analyze.
- No hook/private runtime operation, new public verb, package, dependency, or external service is introduced.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry exact commit evidence into idempotent materialization and repair</name>
  <files>core/model/ingest_transaction.go, core/cli/ingest.go, core/cli/ingest_test.go</files>
  <read_first>
    - core/model/ingest_transaction.go: publication/cleanup truth validation
    - core/cli/ingest.go: current commit/finalize/result ordering and recovery rendering
    - core/cli/ingest_test.go: commit publication, post-commit failure, and value-safe output matrices
    - core/store/store.go: ReadWorktreeRevision and exact published results
    - core/worktree/service.go: idempotent Materialize and repair outcomes
    - .planning/phases/06-ingest-end-to-end/06-05-SUMMARY.md: complete Profile and installed startup invariants
  </read_first>
  <action>
    Preserve all current ingest transaction hardening. Add defensively copied/validated PublishedRevision only to authoritative committed or committed-recovery outcomes. Add `WorktreeMaterializer` without breaking existing constructors/tests. After existing commit/finalize truth is established, build/materialize the exact redacted complete source document at branch main and PublishedRevision; pin SecretRefs during seeding. Exact repeat succeeds without revision movement. If materialization fails after Git publication, keep ProfileCommitted true, set RecoveryRequired, retain only value-free exact-OID evidence, and make status/sync call an idempotent repair that reads the exact committed DTO/OID before hooks are available. Noncommitted/conflict ingest performs no worktree effect. Add first/repeat/fault/noncommit/repair/canary tests.
  </action>
  <acceptance_criteria>
    - Successful ingest seeds exactly one clean worktree at the published object.
    - A split failure never claims rollback and repairs only from the exact authoritative object.
    - Noncommitted ingest cannot create worktree or shell attachment state.
    - Existing SecretRefs are pinned before any resolved live value can be captured.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/model ./core/cli -run 'Test(.*PublishedRevision|.*Ingest.*Material|.*Recovery.*Worktree|.*PinnedSecret)' -count=1</automated>
  </verify>
  <done>One ingest truthfully and idempotently bootstraps the exact shared worktree, including deterministic split repair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inject the concrete classifier policy into one exact production service graph</name>
  <files>core/cmd/zsh-pro/main.go, core/cmd/zsh-pro/main_test.go</files>
  <read_first>
    - core/cmd/zsh-pro/main.go: current sole concrete zsh/store/keychain/runtime composition and error capture
    - core/cmd/zsh-pro/main_test.go: exact Store identity and construction-failure tests
    - core/shell/zsh/classify.go: Provider.IsLiveSecretIdentity production implementation
    - core/worktree/registry.go: NewRegistry LiveSecretPolicy dependency and SecretRef pinning
    - core/worktree/service.go: Registry/repository/state constructor dependencies
    - core/cli/dependencies.go: typed-nil normalization convention
  </read_first>
  <action>
    Preserve the user's current main/runtime/keychain changes. Construct one concrete `zsh.Provider{}` and pass that same value to existing shell consumers, `worktree.NewRegistry` as `shell.LiveSecretPolicy`, and later live capture/emit factories. Construct one Store with the WorktreeRegenerator seam, one authenticated StateStore, one Registry, and one Service; inject that exact Service into reader/workflow/materializer interfaces. Do not reopen the root, create a package global, or construct a fallback instance after failure. Capture construction errors so analyze remains available and dependent verbs fail normally. Add identity tests that fail if Registry receives a fake/nil policy, if two Services/Stores appear, or if a real secret-like post-attach identity reaches a projection.
  </action>
  <acceptance_criteria>
    - main.go explicitly wires zsh.Provider.IsLiveSecretIdentity into Registry/Service.
    - CLI, materializer, and future runtime factory share one exact Store/StateStore/Registry/Service graph.
    - Construction failure never creates a replacement authority and does not break analyze.
    - Real-provider secret canary tests cross the composition root.
  </acceptance_criteria>
  <verify>
    <automated>GOTOOLCHAIN=local go test ./core/cmd/zsh-pro -run 'Test(MainWorktree|Exact.*Worktree|LiveSecretPolicy|Construction|Analyze)' -count=1</automated>
  </verify>
  <done>The production authority graph has one exact service and the real existing zsh classifier guards all live admission.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed ingest -> shared materialization | Published source becomes prompt-path authority |
| composition root -> admission/service | Concrete security and persistence dependencies receive production authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-27 | Tampering | ingest evidence/materialization | high | mitigate | Validated exact PublishedRevision, post-finalize ordering, exact-OID repair, and split tests in Task 1. |
| T-07-28 | Information Disclosure | concrete live admission | high | mitigate | Real zsh classifier injection, SecretRef pinning, and composition-root canary tests in Task 2. |
| T-07-29 | Spoofing | duplicate Store/Service authority | high | mitigate | One instance graph, exact identity tests, and no reopen/global/fallback construction in Task 2. |
| T-07-30 | Denial of Service | construction failure | medium | mitigate | Captured dependency errors, typed-nil fail-closed verbs, and analyze availability tests. |

ASVS Level 1 applies; all high-severity ingest, classifier, and authority-identity gates are blocking.
</threat_model>

<verification>
- `GOTOOLCHAIN=local go test ./core/model ./core/cli ./core/cmd/zsh-pro -count=1`
- `GOTOOLCHAIN=local go vet ./core/model ./core/cli ./core/cmd/zsh-pro`
- `GOTOOLCHAIN=local go build ./...`
</verification>

<success_criteria>
- One successful ingest seeds and repairs the exact shared worktree without routine re-ingest.
- The concrete zsh classifier and one exact service graph are visibly wired in production.
</success_criteria>

<output>
Create `.planning/phases/07-git-like-shared-working-environment/07-07-SUMMARY.md` when done.
</output>
