---
phase: 04-manifest-builder-emit
plan: 17
type: execute
wave: 14
depends_on: [04-16]
files_modified:
  - core/shell/zsh/pipeline_test.go
  - core/shell/zsh/residue_test.go
autonomous: true
gap_closure: true
requirements: [SW-01, SW-02]
must_haves:
  truths:
    - "A persisted forced-managed numeric or associative indexed assignment remains verbatim and reaches Diff/Emit as a deliberate no-operation."
    - "A persisted forced-managed `export -i` remains verbatim; it cannot degrade to a plain export and ordinary unflagged exports remain supported."
    - "Under zsh -f, apply and deactivate of rejected plans preserve scalar, indexed-array, associative-array, and integer-export sentinels including type, key/element, and arithmetic behavior."
    - "The durable full-state residue oracle proves a persisted mixed rejected profile leaves no type-only or keyed-element residue."
  artifacts:
    - path: core/shell/zsh/pipeline_test.go
      provides: "Persisted Parse-to-DTO-to-Diff-to-Emit live-zsh discriminators for indexed and semantic flagged-export forms."
    - path: core/shell/zsh/residue_test.go
      provides: "Full snapshot no-residue proof for a persisted mixed rejected profile."
  key_links:
    - from: core/store/dto.go
      to: core/shell/zsh/pipeline_test.go
      via: "Current and historical records re-save then enter OverrideManaged only as unknown or rejected entries."
      pattern: "MarshalProfile|UnmarshalProfile|OverrideManaged"
    - from: core/activate/builder.go
      to: core/shell/zsh/pipeline_test.go
      via: "Empty manifest intent still traverses Diff and Provider.Emit before zsh executes the plan."
      pattern: "Build|Diff|Emit"
    - from: core/shell/zsh/pipeline_test.go
      to: core/shell/zsh/residue_test.go
      via: "Focused semantic sentinels are complemented by the existing complete snapshot oracle."
      pattern: "zsh -f|snapshot"
---

<objective>
Prove the 04-16 shared source-fidelity gate on the complete persisted production path and in live zsh, including the final zero-residue evidence required by SW-02.

Purpose: demonstrate that rejected indexed and semantic flagged-export forms are deliberately inert after persistence, instead of silently changing a variable's type, subscripted state, export attribute, or switch residue.
Output: focused pipeline tests with type/key/arithmetic discriminators and a full-state residue regression for a persisted mixed rejected profile.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-16-PLAN.md
@.planning/phases/04-manifest-builder-emit/04-16-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
</context>

## Dependency graph

| Task | Needs | Creates | Checkpoint |
| --- | --- | --- | --- |
| 1 | 04-16 parser metadata, DTO v2, and shared representability gate | Persisted full-path live-zsh semantic discriminators | none |
| 2 | Task 1's persisted rejected-profile fixtures and existing snapshot oracle | Full-state no-residue regression | none |

This plan is Wave 14 because its tests must consume the completed 04-16 contract. Its two files do not overlap any Wave 13 plan files, and it is independently executable after 04-16.

## Source coverage audit

| Source | ID | Required outcome | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Resolved profiles emit reversible shell state without semantic loss or residue. | Tasks 1-2 | COVERED |
| REQ | SW-01 | Rejected source shapes cannot cross to an executable scalar/list operation. | Task 1 | COVERED |
| REQ | SW-02 | Applying/deactivating rejected plans leaves all managed and unmanaged sentinel state unchanged. | Tasks 1-2 | COVERED |
| RESEARCH | Responsibility map | Tests consume the shell-agnostic boundary and existing sole zsh emitter without new renderer behavior. | Tasks 1-2 | COVERED |
| RESEARCH | Dynamic/no-evaluation constraint | Tests prove verbatim regeneration and no-operation, not source evaluation or an invented operation. | Task 1 | COVERED |
| CONTEXT | D-05/D-06/D-07 | Builder remains shell-agnostic and refuses unsupported indexed forms rather than treating them as scalar/list input. | Task 1 | COVERED |
| CONTEXT | D-08/D-09 | Rejected profiles use the existing Build -> Diff -> Plan route, including empty intent. | Task 1 | COVERED |
| CONTEXT | D-10/D-11/D-12/D-13 | Existing emit path is consumed only; no new zsh renderer semantics or runtime undo behavior is added. | Task 1 | COVERED |
| CONTEXT | D-14/D-15/D-16/D-17 | Existing complete snapshot oracle and default-suite residue proof cover the mixed rejected profile. | Task 2 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise persisted rejected source shapes through Diff, Emit, and live zsh</name>
  <files>core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/shell/zsh/pipeline_test.go — persisted override matrix, legacy fidelity matrix, roundTripPipelineProfile, and emitPipelineListPlan helpers.
    - core/activate/diff.go and core/activate/plan.go — empty manifest intent still follows the production plan path.
    - core/shell/zsh/emit.go — sole renderer consumed by the test.
    - .planning/phases/04-manifest-builder-emit/04-REVIEW.md and 04-VERIFICATION.md — direct source controls and integer arithmetic discriminator.
  </read_first>
  <behavior>
    - Test 1: `FOO[2]=bar`, `MAP[key]=bar`, and `export -i FOO=1` each execute Parse -> IR Build -> DTO marshal/unmarshal -> persisted OverrideManaged -> Regenerate -> activate.Build -> Diff -> Emit -> `zsh -f`; regeneration equals original text and Build has no operation.
    - Test 2: direct numeric and associative source controls prove their type and element/key semantics, while generated no-operation plans preserve pre-seeded array and association sentinels after apply and deactivate.
    - Test 3: direct `export -i` reports integer-export and computes 3 after `+=2`; a plain-export control reports scalar-export and 12; the no-operation plan preserves an integer-export sentinel through apply/deactivate. Unflagged export and delimiter controls remain supported.
    - Test 4: absent, v1, partial-v2, and unsupported indexed/flagged records re-save as unknown, create no operation, and leave sentinels unchanged in the same full path.
  </behavior>
  <action>
    Expand the existing pipeline matrix instead of adding a parallel harness. For every current parser-backed row, round-trip through store marshal/unmarshal, set OverrideManaged only after persisted read, assert exact IR regeneration, inspect empty `activate.Build` intent, and then pass it through `activate.Diff` and `Provider.Emit` before the `zsh -f` assertion. Use numeric `FOO[2]=bar`, associative `MAP[key]=bar`, and `export -i FOO=1`. Direct controls demonstrate why the forms cannot be lowered: declare an associative MAP before its keyed assignment and assert both zsh type and keyed value.

Pre-seed distinctive scalar, indexed-array, associative-array, and integer-export sentinels in generated-plan scripts. Assert value, indexed element/key, and zsh type or parameter attribute immediately after apply and after deactivate. Assert the flagged-source 3-versus-plain-export 12 arithmetic discriminator and retained integer-export identity. Retain ordinary unflagged and delimiter exports as supported controls so the fail-closed flag rule cannot overblock them.

Add raw legacy fixtures for absent fidelity, complete v1 without new fields, each partial v2 marker/metadata row, and unsupported records. Drive each through raw JSON, re-save, persisted override, regeneration, Build, Diff, Emit, and `zsh -f`; verify no fidelity object is manufactured on re-save, no operation is produced, and all sentinels survive. Keep generated zsh only in test fixtures and consume existing emit behavior per D-10/D-11.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'TestPipeline(PersistedOverrideManaged|LegacyStructuralFidelityMatrix|.*(Indexed|Flagged|Export))$' -v</automated>
  </verify>
  <done>Every current and historical rejected indexed/flagged-export record completes the persisted production path as verbatim/no-operation, while live zsh distinguishes and preserves its original behavior-bearing state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin mixed rejected-source zero residue with the full-state snapshot oracle</name>
  <files>core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/shell/zsh/residue_test.go — source-driven fixture creation and collision-safe full-state snapshot helper.
    - core/shell/zsh/pipeline_test.go — Task 1 persisted rejected-profile fixtures and sentinel assertions.
    - core/shell/zsh/introspect.go — snapshot coverage and zsh subprocess pattern.
    - .planning/phases/04-manifest-builder-emit/04-CONTEXT.md — D-14 through D-17 property-test and snapshot requirements.
  </read_first>
  <behavior>
    - Test 1: a persisted forced-managed mixed profile containing numeric index, associative index, and `export -i` enters the existing Build -> Diff -> Emit path and makes no manifest operation.
    - Test 2: the existing complete snapshot before apply and after deactivate is byte-identical, including array/association keyed state and integer-export parameter attributes; a type-only or key-only mutation fails the assertion.
    - Test 3: the default zsh-backed suite executes this regression with the established LookPath skip guard rather than a new ad-hoc harness.
  </behavior>
  <action>
    Extend the durable `residue_test.go` oracle with a persisted mixed rejected profile assembled through the same parser, DTO re-save, and OverrideManaged route as Task 1. Drive the resulting empty manifest intent through existing Diff and Emit; do not bypass production code or introduce a bespoke emitter. Pre-seed scalar, indexed array, associative array, and integer-export sentinels that make value-only assertions insufficient.

Use the existing complete snapshot mechanism before apply and after deactivate, and explicitly include association key/value plus integer-export type/arithmetic evidence in the fixture's observable state. The assertion must be byte-identical and fail when a wrong scalar lowering, a keyed element mutation, or an attribute-only mutation occurs. Keep the established `zsh -f`, timeout, and `LookPath` skip behavior, retain the N-switch residue property, and add this case to its default-suite coverage per D-14 through D-17.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'Test(Residue.*(Structural|Fidelity|Noop)|ZeroResidueFullStateProperty)$' -v && GOTOOLCHAIN=auto go test -count=1 ./... && GOTOOLCHAIN=auto go build ./... && GOTOOLCHAIN=auto go vet ./...</automated>
  </verify>
  <done>The final persisted mixed rejected profile has a durable byte-identical full-state no-residue proof, including indexed numeric/associative and integer-export semantic state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| persisted Profile -> IR/manifest builder -> Diff/Emit | A persisted override could turn rejected source structure into executable scalar/list state. |
| emitted Plan -> live zsh | Incorrect lowering can mutate variable type, array key/element, export attribute, arithmetic behavior, or deactivation residue. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04-17-01 | Tampering | persisted source-shape pipeline | high | mitigate | Exercise current and historical records through marshal/re-save, OverrideManaged, Regenerate, Build, Diff, Emit, and zsh execution; require verbatim/no-operation. |
| T-04-17-02 | Tampering | live zsh variable semantics | high | mitigate | Use numeric/associative type and key/element sentinels plus integer-export 3-versus-12 arithmetic discriminator after apply and deactivate. |
| T-04-17-03 | Tampering | switch residue oracle | high | mitigate | Add mixed persisted rejected fixtures to the established byte-identical full-state snapshot test in the default suite. |
| T-04-17-SC | Tampering | dependency supply chain | low | accept | No dependency or package-manager install is introduced; tests use the pinned Go module and existing zsh harness. |
</threat_model>

<verification>
Run the focused pipeline and residue commands, then `GOTOOLCHAIN=auto go test -count=1 ./...`, `GOTOOLCHAIN=auto go build ./...`, and `GOTOOLCHAIN=auto go vet ./...`. With zsh available, require execution of numeric-index, associative-index, integer-export arithmetic, persisted re-save, no-operation, and full snapshot assertions.
</verification>

<success_criteria>
- Current and historical indexed/semantic-flagged records complete the persisted production path as verbatim/no-operation.
- Numeric and associative type/key semantics plus integer-export type/arithmetic semantics are demonstrably not lowered.
- Apply/deactivate leaves all pre-existing sentinels and the complete snapshot byte-identical.
- This plan supplies the final SW-02 proof and closes the two 04-REVIEW blockers after 04-16's source contract is implemented.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-17-SUMMARY.md` when done.
</output>
