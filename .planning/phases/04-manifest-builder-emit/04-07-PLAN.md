---
phase: 04-manifest-builder-emit
plan: 07
type: execute
wave: 6
depends_on: ["04-05"]
gap_closure: true
files_modified:
  - core/shell/zsh/parse.go
  - core/shell/zsh/dynamic_test.go
  - core/store/secret.go
  - core/store/secret_test.go
autonomous: true
requirements: [SW-01]
user_setup: []

must_haves:
  truths:
    - "Supported unquoted and double-quoted zsh escapes decode to the same bytes live zsh assigns; source spelling remains byte-for-byte in Value."
    - "Unknown, dangling, ANSI-C, or otherwise unmodeled escape forms remain ValueModeUnsupported and never enter the legacy fallback."
    - "A newly parsed literal secret is stored from its present RuntimeValue exactly, including empty, multiline, quotes, backslashes, and escaped source spelling."
    - "Dynamic secrets remain late-bound verbatim; only explicitly legacy entries may use the narrow historical non-empty Value fallback."
  assumptions:
    - statement: "SW-02 unclassified edge-probe item is not resolved by this SW-01 semantic-value plan and remains flagged for 04-12."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/shell/zsh/parse.go"
      provides: "context-aware fail-closed unquoted and double-quoted escape decoding"
      contains: "decodeLiteralParts"
    - path: "core/store/secret.go"
      provides: "semantic RuntimeValue secret capture including present-empty"
      contains: "RuntimeValue"
    - path: "core/store/secret_test.go"
      provides: "exact backend equality tests over source-spelling edge cases"
      contains: "LiteralSecret"
  key_links:
    - from: "core/shell/zsh/parse.go"
      to: "core/model.Entry.RuntimeValue"
      via: "ValueModeLiteral carries decoded data while Value keeps source spelling"
      pattern: "RuntimeValue"
    - from: "core/model.Entry.RuntimeValue"
      to: "core/store/secret.go"
      via: "literal secret backend writes consume semantic data, not source syntax"
      pattern: "ValueModeLiteral"
---

<objective>
Make the semantic-value contract exact for supported zsh escapes and consume that contract at the secret boundary.

Purpose: emitted values and secret backends must receive runtime data, never shell source spelling, while unmodeled forms continue to fail closed.
Output: context-aware escape decoding and exact present-empty secret capture tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode modeled zsh escape semantics without executing source</name>
  <files>core/shell/zsh/parse.go, core/shell/zsh/dynamic_test.go</files>
  <read_first>
    - core/shell/zsh/parse.go (decodeLiteralWord, decodeLiteralParts, source slicing)
    - core/shell/zsh/dynamic_test.go (ValueMode and RuntimeValue tables)
    - /home/metanmai/go/pkg/mod/mvdan.cc/sh/v3@v3.13.1/syntax/nodes.go (Lit, SglQuoted, DblQuoted AST contracts)
  </read_first>
  <behavior>
    - RED: unquoted escaped space, quote, backslash, glob metacharacter, and backslash-newline match a live zsh assignment byte-for-byte.
    - RED: inside double quotes, backslash is consumed only before dollar, backtick, quote, backslash, or newline; otherwise it remains data.
    - RED: single-quoted content remains verbatim and concatenated quote contexts produce one exact RuntimeValue.
    - RED: dangling or unmodeled escape spelling is Unsupported, not Literal or Legacy.
  </behavior>
  <action>
    Make decodeLiteralParts carry an explicit quote context. For unquoted Lit data, implement zsh's modeled backslash-pair rules: a backslash quotes the following byte and a backslash-newline pair is a continuation removed from runtime data; fail closed on a dangling or structurally ambiguous escape. For DblQuoted Lit data, consume backslash only for the zsh double-quote special set (dollar, backtick, double quote, backslash, newline) and preserve it otherwise. Keep SglQuoted data unchanged. Continue rejecting ANSI-C quotes, extglob, brace expansion, leading unquoted tilde, and unknown WordPart nodes.

    Tests must compare parser RuntimeValue with values produced by disposable zsh -f source assignments, not with model-recalled escape expectations. Preserve Value and Text exactly, import no interpreter/exec/environment expansion path in production parsing, and keep ValueModeUnsupported fail-closed.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Dynamic|RuntimeValue|Escape|Literal|Unsupported' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Every supported escape fixture matches live zsh runtime bytes.
    - Double-quote-only escape rules are not applied to unquoted or single-quoted contexts.
    - Source Value remains unchanged for every case.
    - Unsupported forms never gain RuntimeValue and never become Legacy.
    - Production parse.go performs AST inspection only and executes no source.
  </acceptance_criteria>
  <done>Supported literal spellings decode to exact runtime bytes and every other form remains explicitly unsupported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store semantic secret values exactly, including present-empty</name>
  <files>core/store/secret.go, core/store/secret_test.go</files>
  <read_first>
    - core/store/secret.go (current e.Value checks and backend Store call)
    - core/store/secret_test.go (real Parse to IR secret fixtures and vault assertions)
    - core/model/profile.go (ValueMode and RuntimeValue contract)
  </read_first>
  <behavior>
    - RED: single-quoted, double-quoted, escaped, multiline, and empty literal secrets reach the backend by exact equality with RuntimeValue.
    - RED: quote delimiters and escape backslashes from Value never enter the backend for new literal entries.
    - RED: Literal with nil RuntimeValue and Unsupported fail closed before a backend write.
    - RED: Dynamic remains verbatim and un-stored; legacy non-empty Value retains a documented compatibility path.
  </behavior>
  <action>
    Prevalidate secret semantic mode before mutation. A ValueModeLiteral entry requires non-nil RuntimeValue and may contain an empty string; pass exactly the dereferenced RuntimeValue to KeychainDriver.Store. ValueModeDynamic requires the existing Dynamic contract and remains committed verbatim. ValueModeUnsupported or malformed mode/value combinations fail closed. Keep a narrow ValueModeLegacy fallback for old stored/programmatic entries only, using non-empty historical Value; do not infer Literal from source text and do not make empty legacy source silently capturable.

    Keep secret redaction invariants after capture: remove literal bytes from Text, Value, and RuntimeValue; mark Unsupported; stamp the active backend kind; expose no secret value in errors/reports. Strengthen tests from Contains to exact equality and include present-empty plus source-escape cases.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/store -run 'Secret|RuntimeValue|Literal|Empty|Legacy|Dynamic' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - New literal secrets use exactly *RuntimeValue, including the empty string.
    - Source quotes/escapes never reach the backend for explicit Literal entries.
    - Malformed or Unsupported contracts perform zero backend writes.
    - Dynamic and legacy behaviors are explicit and separately tested.
    - Text, Value, and RuntimeValue redaction plus SecretRef authority remain intact.
  </acceptance_criteria>
  <done>The secret backend receives semantic runtime bytes exactly and fail-closed redaction remains authoritative.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level 1; blocking threshold high.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-04-07-01 | Tampering | literal decoder | high | mitigate | context-aware AST allowlist plus live-zsh byte oracle |
| T-04-07-02 | Elevation of Privilege | decoded value to emitted shell | high | mitigate | Unsupported on any unmodeled form; static values remain routed through zquote |
| T-04-07-03 | Information Disclosure | secret persistence | high | mitigate | store exact RuntimeValue, then clear all literal-bearing fields and retain SecretRef only |
| T-04-07-04 | Repudiation | secret error/report path | medium | mitigate | name/line-only report and typed errors; no values or raw backend stderr |
| T-04-07-SC | Tampering | dependency supply chain | low | accept | no installs or new dependencies |
</threat_model>

<prohibition_recall>
No bespoke prohibition survives. Secret plaintext leakage and shell injection are canon security concerns captured by the threat register and $gsd-secure-phase; empty/escape behavior is routine data-shape correctness covered by explicit tests.
</prohibition_recall>

<verification>
- GOTOOLCHAIN=auto go test ./core/shell/zsh ./core/store -count=1
- GOTOOLCHAIN=auto go test ./... -count=1
- GOTOOLCHAIN=auto go build ./...
- Value/Text regeneration remains byte-for-byte and no production parser execution path is added.
</verification>

<success_criteria>
- Escape decoding matches live zsh for the supported corpus and fails closed otherwise.
- New literal secrets persist exact semantic bytes, including empty, with all committed fields redacted.
- Dynamic and legacy compatibility remain explicit and narrow.
</success_criteria>

<artifacts_this_phase_produces>
- Quote-context-aware literal escape decoder helpers.
- Exact semantic secret-value selector.
- Live-zsh escape corpus and exact backend-value fixtures.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-07-SUMMARY.md` when done.
</output>
