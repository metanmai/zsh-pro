---
phase: 04-manifest-builder-emit
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
gap_closure: true
files_modified:
  - core/model/manifest.go
  - core/model/manifest_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/activate/diff.go
  - core/activate/diff_test.go
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/zsh/pipeline_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "Manifest stores explicit dynamic provenance for every newly built scalar and alias, so a quoted literal `$HOME` remains data while an unquoted `$HOME` expands late-bound; legacy manifests alone use the old heuristic fallback."
    - "functions.added remains the validated []string field; an optional bodies map uses key presence, not non-empty text, so empty and multiline functions reach AddFunc.Body while old manifests remain readable."
    - "Unsupported newly parsed values and parsed functions missing a body fail closed; they never become misleading manifest parts or silent empty activation operations."
    - "A source-to-live-zsh integration test traverses Parse -> IR -> Build -> Diff -> Emit and observes exact scalar/alias data plus defined, callable empty and multiline functions."
  artifacts:
    - path: "core/model/manifest.go"
      provides: "backward-compatible scalar/alias dynamic provenance and function body map"
      contains: "Bodies"
    - path: "core/activate/builder.go"
      provides: "ValueMode-aware fail-closed activation data selection"
      contains: "ValueModeUnsupported"
    - path: "core/shell/zsh/pipeline_test.go"
      provides: "real source-to-zsh regression test for verification blockers"
      contains: "Diff"
  key_links:
    - from: "core/model.Entry"
      to: "core/model.Manifest"
      via: "Build consumes explicit ValueMode and body presence"
      pattern: "RuntimeValue"
    - from: "core/model.Manifest"
      to: "core/activate.Plan"
      via: "Diff consumes stored dynamic provenance and body map-key presence"
      pattern: "AddFunc"
    - from: "core/activate.Plan"
      to: "live zsh"
      via: "Provider.Emit renders exact values and always assigns AddFunc bodies"
      pattern: "functions["
---

<objective>
Use the explicit semantic contract from 04-03 to close Phase 4's quoted-value and function-body failures across Manifest, Build, Diff, Emit, and live zsh.

Purpose: activation must preserve behavior, including the distinction between literal dollar text and late-bound expansion, without changing the established v1 manifest fields.
Output: additive manifest provenance/body fields, fail-closed builder/differ behavior, empty-function-safe emission, and a complete production-pipeline integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-03-SUMMARY.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Manifest compatibly and make Build/Diff consume explicit provenance</name>
  <files>core/model/manifest.go, core/model/manifest_test.go, core/activate/builder.go, core/activate/builder_test.go, core/activate/diff.go, core/activate/diff_test.go</files>
  <read_first>
    - core/model/profile.go (ValueMode, RuntimeValue, FunctionBody contract from 04-03)
    - core/model/manifest.go (validated v1 field shapes)
    - core/model/manifest_test.go (legacy fixture and tri-state tests)
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md (wire compatibility source)
    - core/activate/builder.go (current quote and function loss points)
    - core/activate/diff.go (current containsDynamic inference and empty AddFunc construction)
    - core/activate/plan.go (SetScalar/AddAlias/AddFunc operation fields)
  </read_first>
  <action>
    Extend Scalar with `Dynamic *bool \`json:"dynamic,omitempty"\``. Pointer presence distinguishes new explicit false (for decoded literal `$HOME`) from a legacy manifest with no provenance. Extend AliasSet with `Dynamic map[string]bool \`json:"dynamic,omitempty"\``; every newly built alias gets a key, including false, while map-key absence means legacy. Extend FuncSet with `Bodies map[string]string \`json:"bodies,omitempty"\`` while keeping `Added []string \`json:"added"\`` and Shadowed unchanged. Empty maps are omitted so the original Phase 1 fixture retains its existing shape; populated fields round-trip. Do not change SchemaV1 or replace functions.added with a map.

    Add one builder helper returning `(value string, dynamic bool, ok bool)` by exact Entry.ValueMode: Literal requires non-nil RuntimeValue and returns it with dynamic=false; Dynamic requires Entry.Dynamic and returns verbatim Value with dynamic=true; Unsupported returns ok=false; Legacy returns existing Value and Entry.Dynamic for backward-compatible programmatic entries. Use it for scalars and aliases. For every built scalar, allocate Dynamic to the returned bool; for every built alias, store the bool at Aliases.Dynamic[name]. An invalid Literal/Dynamic contract or Unsupported mode produces no manifest part. Never re-infer new-entry provenance from decoded text.

    For a managed function, require `Entry.FunctionBody != nil`; otherwise produce no function part. When present, append the name to Functions.Added and set Functions.Bodies[name] to the dereferenced value, including empty. In Diff activation, read explicit Scalar.Dynamic and Aliases.Dynamic key values; call containsDynamic only when provenance is absent in a legacy manifest. For each target function, require map-key presence (`body, ok := Bodies[name]`), not `body != ""`, and create AddFunc with that exact body. Validate the entire target before producing a Plan so missing-body errors return an empty Plan; `Diff(activeLegacy, nil)` must still succeed because deactivation only needs Added names. Tests cover literal/dynamic dollar pairs, empty decoded scalar, unsupported drop, legacy fallback, old manifest JSON, omitted additive keys, populated round-trip, empty/multiline function bodies, target missing-body rejection, and legacy active deactivation.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/model ./core/activate -run 'Manifest|Build|Diff|Dynamic|Runtime|Quoted|Function|Legacy|Unsupported' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Scalar explicit false survives JSON round-trip as a present false pointer; alias Dynamic contains a false-valued key for a literal `$HOME` body.
    - Old manifests with none of `dynamic`/`bodies` decode, and an empty additive map does not alter the Phase 1 fixture shape.
    - Diff never applies containsDynamic to a new scalar/alias with explicit provenance.
    - A Functions.Bodies key with value empty produces AddFunc{Body:""}; a missing key rejects target activation but does not block deactivating an old active manifest.
    - Unsupported/malformed new Entry semantic contracts yield no part, not legacy fallback.
  </acceptance_criteria>
  <done>Manifest and Plan carry exact runtime intent with explicit backward-compatible provenance and body presence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit empty functions and prove the complete production pipeline under zsh</name>
  <files>core/shell/zsh/emit.go, core/shell/zsh/emit_test.go, core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/shell/zsh/emit.go (renderValue and AddFunc branch)
    - core/shell/zsh/emit_test.go (zsh execution and canary patterns)
    - core/shell/zsh/parse.go (pipeline entry)
    - core/ir/build.go (real classifier injection)
    - core/activate/builder.go (manifest build)
    - core/activate/diff.go (plan construction)
  </read_first>
  <action>
    Change AddFunc emission to always write `functions[name]=...` for a valid AddFunc operation, including Body == "". Presence was validated by Diff; empty content is a valid zsh function. Keep safe name validation, shadow capture, and quoteFunctionBody. Add focused emitter tests that both empty and multiline bodies create defined/callable functions and pass zsh syntax.

    Add `pipeline_test.go` starting from real source bytes, using Provider.Parse -> ir.Build with the real zsh classifier -> activate.Build -> activate.Diff(nil, &manifest) -> Provider.Emit. Include: `LITERAL='$HOME'`; `DYNAMIC=$HOME`; a quoted scalar with spaces/metacharacters; `alias literal='echo $HOME'`; a dynamic alias if admitted by existing routing; an alias body containing `=`; `empty(){}`; a multiline brace function; a subshell-bodied function `subshell() ( print hi )`; and a redirected brace function whose output file lives under `t.TempDir()`. Run emitted code in disposable `zsh -f` with a controlled HOME and file/fd-backed collision-safe assertions. Prove literal dollar data remains `$HOME`, dynamic data expands to controlled HOME, quotes are not stored as data, aliases have exact bodies, empty/multiline/subshell functions exist and call correctly, and invoking the redirected function writes its expected file rather than stdout. Assert no canary fires. After deactivate, verify introduced names are absent/restored. The test must use the actual built Manifest and Plan, never a corrected hand-built operation. Skip only when zsh is unavailable.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Pipeline|Emit|Function|Quoted|Dynamic' -count=1 &amp;&amp; GOTOOLCHAIN=auto go test ./... -count=1 &amp;&amp; GOTOOLCHAIN=auto go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - AddFunc with Body empty emits an assignment, defines the function, and calling it succeeds.
    - Literal `$HOME` and dynamic `$HOME` take opposite correct paths end to end under a controlled child HOME.
    - Quoted scalar/alias values contain semantic data without source delimiters; alias bodies containing `=` remain intact.
    - Empty, multiline brace, subshell-bodied, and redirected-brace parsed functions preserve behavior, then are removed/restored on deactivate.
    - No canary is created; full tests and build pass.
  </acceptance_criteria>
  <done>The two data-fidelity reproductions pass through every production stage and observable live-zsh behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Risk | Mitigation in this plan |
|---|---|---|
| semantic value -> eval'd emitted zsh | Decoding source quotes could make literal `$` or metacharacters executable. | Store explicit dynamic provenance and send static semantic data through zquote exactly once; controlled-HOME and canary tests contrast both paths. |
| old manifest -> new Diff | Missing additive provenance/body fields could silently change behavior. | Provenance absence alone enables legacy heuristic; missing target function bodies reject activation; legacy deactivation remains available. |
| owner function body -> functions map | Body is executable trusted owner code, while name/body assignment is injection-sensitive. | Retain name validation and quoteFunctionBody; key presence distinguishes empty from absent; live zsh tests cover both. |

No new dependencies or package-manager operations are permitted.
</threat_model>

<verification>
- `GOTOOLCHAIN=auto go test ./core/model ./core/activate ./core/shell/zsh -count=1`
- `GOTOOLCHAIN=auto go test ./... -count=1`
- `GOTOOLCHAIN=auto go build ./...`
- Existing source regeneration, schema, token-free, name-validation, injection, and zsh-syntax tests remain green.
</verification>

<success_criteria>
- Both data-fidelity blockers from 04-VERIFICATION.md have source-to-live-zsh regression tests.
- Literal-dollar and dynamic-dollar values cannot be conflated after Manifest serialization.
- Empty and multiline functions survive Build/Diff/Emit by body-map key presence.
- Backward compatibility is explicit and fail-closed where activation data is missing.
</success_criteria>

<artifacts_produced>
- `.planning/phases/04-manifest-builder-emit/04-04-SUMMARY.md`
- Additive manifest provenance/body contract and compatibility tests.
- Complete source-to-live-zsh quoted-value/function regression suite.
</artifacts_produced>
