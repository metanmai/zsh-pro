---
phase: 04-manifest-builder-emit
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/provider.go
  - core/cmd/zsh-pro/main.go
  - core/shell/zsh/residue_test.go
  - core/shell/zsh/invariant_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "core/shell/zsh/emit.go renders a core/activate.Plan to zsh apply + deactivate code as PLAIN loader functions (no emulate -L / LOCAL_OPTIONS) that pass zsh -n; it is the SOLE place reverse zsh tokens are generated; the PATH-rebuild and value-quoting rendering are factored into injectable PACKAGE-LEVEL funcs (renderList/renderValue) so a mutant test can substitute them against a stateless Provider{} (SW-01) [Addresses review concern: CH-5]"
    - "A static user-controlled value containing ' ; $(...) backtick or newline is emitted single-quote-wrapped ('\\'' escape) so it is always an inert literal under eval; a dynamic value stays verbatim so zsh expands it per-machine — the split keys strictly on the op's Dynamic bool (SW-01, threat T-01-06)"
    - "AddAlias/AddFunc emitted APPLY code captures the pre-existing (shadowed) alias/function body BEFORE overwriting, guarded by ${+name}, into the shadow-prior runtime slot — so deactivate's RestoreShadowedAlias/RestoreShadowedFunc has a prior to restore; capture is ${+slot}-guarded idempotent so a double-checkout does not overwrite the clean prior (SW-02) [Addresses review concern: CH-4, MEDIUM double-checkout]"
    - "Deactivate is ownership-aware and drift-guarded: an env scalar is restored only if live==applied (${(P)+var}==1 for unset-vs-empty); PATH rebuilds from a captured base with QUOTED-RHS literal-equality element handling ([[ $e == \"$target\" ]], NOT glob); a shadowed alias/function is restored via ${+name} set-tests; function bodies restored verbatim via functions[name]=$captured, never zquote'd; deactivate UNSETS its own per-switch undo slots (__ZP_ORIG_*, ZP_<profile>_PRIOR_*, sentinels) while ZP_BASE_PATH stays session-persistent (SW-02) [Addresses review concern: CH-3, CH-6]"
    - "A zero-residue property test over N>=20 random switch sequences under sandboxed zsh -f leaves a FULL-env snapshot (ALL ${(@kv)parameters}, exported AND non-exported, name+VALUE, NUL-framed) plus function BODIES + $#path byte-identical; two independently-asserted mutants (blind-append PATH; dropped-zquote on a metacharacter static value) each make it FAIL; the snapshot instrument itself is meta-asserted to catch a non-exported leak and an exported value-only change (SW-02, the regression pin) [Addresses review concern: CH-2, CH-5]"
  artifacts:
    - path: "core/shell/zsh/emit.go"
      provides: "Emit(activate.Plan)->(apply,deactivate string,err); package-level renderList/renderValue vars (CH-5); zquote; Dynamic-keyed split; shadow-prior capture-before-overwrite (CH-4); quoted-RHS literal PATH removal (CH-6); per-switch slot cleanup (CH-3); slot-name sanitization (D-10..D-13, OQ-8/OQ-10/OQ-18)"
      contains: "var renderList"
    - path: "core/shell/provider.go"
      provides: "shell.Emitter seam mirroring shell.Regenerator (D-10)"
      contains: "Emitter interface"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "Emitter wiring at the sole composition root (D-10)"
      contains: "Provider{}"
    - path: "core/shell/zsh/residue_test.go"
      provides: "zero-residue property test, N>=20, full-env snapshot, TWO independent mutants + meta-assert (D-16/D-17, SW-02 pin, CH-2/CH-5)"
      contains: "parameters"
    - path: "core/shell/zsh/invariant_test.go"
      provides: "tree-grep: reverse zsh tokens generated only under emit.go (D-11)"
      contains: "unalias"
  key_links:
    - from: "core/shell/zsh/emit.go"
      to: "core/activate.Plan"
      via: "Emit consumes the Plan op union defined in 04-01"
      pattern: "activate.Plan"
    - from: "core/cmd/zsh-pro/main.go"
      to: "shell.Emitter"
      via: "zsh.Provider{} satisfies the Emitter seam, injected at the composition root"
      pattern: "zsh.Provider"
    - from: "residue_test.go"
      to: "full-env ${(@kv)parameters} snapshot instrument"
      via: "a NEW full-env snapshot (NOT the exported-only ##ENV## section), reused inline in one zsh -f process"
      pattern: "(@kv)parameters"
---

<objective>
Render the agnostic `Plan` (from 04-01) to injection-safe, drift-guarded, ownership-aware zsh apply/deactivate code in `core/shell/zsh/emit.go` — the single place reverse zsh syntax lives — reached via a new `shell.Emitter` seam wired at the composition root; and pin zero-residue with a full-env property test whose mutants genuinely fail.

Purpose: SW-01 requires the sole emit path; SW-02 requires zero residue verified by a property test that can detect residue. This is the phase's highest-risk work (threat T-01-06: shell-code emission is where a quoting bug becomes an injection bug).
Output: `core/shell/zsh/emit.go` + `Emitter` seam + main.go wiring + the zero-residue property test + the single-emit-path invariant test.

Cycle-1 review resolutions carried here: CH-1a/b (base-ownership property sub-check + multi-managed out-of-scope prose), CH-2 (full-env snapshot instrument), CH-3 (deactivate cleans its per-switch slots), CH-4 (shadow-prior capture-before-overwrite), CH-5 (injectable renderList/renderValue + two independent mutants + meta-assert), CH-6 (quoted-RHS literal PATH removal, C25 UNVERIFIED), and MEDIUMs (double-checkout capture idempotency, injection corpus through slot-name path, OQ-18 bare zp_* calls).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-SPEC.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-PATTERNS.md
@.planning/phases/04-manifest-builder-emit/04-EVIDENCE.md
@.planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md
@.planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from 04-01 (Wave 1) and the codebase. The 04-01-SUMMARY records the FINAL Plan op union — read it for exact identifiers. -->

From core/activate/plan.go (04-01 — the Plan this Emit renders; op set is a sealed union carrying agnostic data only; RebuildListFromBase and ApplyListDelta are DISTINCT ops with distinct semantics per CH-1c; RestoreOption has NO authoritative WasOn per CH-7):
```go
type Plan struct { Deactivate []Op; Activate []Op }
// deactivate ops: RestoreScalar{Name,Applied; Original *string}, UnsetScalar{Name,Applied},
//   RebuildListFromBase{Name}  // rebuild TO base, NO additions to re-add (CH-1c),
//   Unalias{Name}, RestoreShadowedAlias{Name}, UnsetFunc{Name}, RestoreShadowedFunc{Name},
//   RestoreOption{Name}  // WasOn dropped/non-authoritative — live prior captured in emit.go (CH-7)
// activate ops: SetScalar{Name,Applied; Dynamic bool}, ApplyListDelta{Name; Additions,Deletions},
//   AddAlias{Name,Body; Dynamic bool}, AddFunc{Name,Body}, SetOption{Name; Enabled bool}
```

From core/shell/provider.go (MODIFY — mirror the Regenerator seam):
```go
type Regenerator interface { Regenerate(e model.Entry) string }
type Provider interface { Parser; Classifier; Introspector; Regenerator }
// ADD: type Emitter interface { Emit(p activate.Plan) (apply, deactivate string, err error) } (signature is discretion)
```

From core/shell/zsh/regen.go (the codegen STYLE to copy — method on Provider{}, fmt.Sprintf/strings.Builder, total switch — but emit.go is a DISTINCT file that shares no code and ADDS a Dynamic-keyed quote branch regen.go does NOT have, C7). NOTE (CH-5): Provider is a zero-value struct with NO fields, so the mutant test cannot flip a struct flag — factor the PATH-rebuild and value-quoting into package-level vars the test substitutes:
```go
func (Provider) Regenerate(e model.Entry) string // emits e.Value VERBATIM in every branch, never reads Dynamic
```

From core/cmd/zsh-pro/main.go (the sole composition root — wire Emitter like the Regenerator; downstream consumer is Phase 5, so injectable-but-not-yet-driven is fine, mirror the `_, _ = s, err` comment):
```go
provider := zsh.Provider{} // is Parser/Classifier/Introspector AND Regenerator; add: AND Emitter
```

From core/shell/zsh/introspect.go (04-01 extended — the body-dump instrument). CH-2: the property test must NOT reuse the exported-only `##ENV##` section (it is names-only + exported-only, the C19 false-green). It needs a NEW full-env snapshot section:
```go
const introspectScript = `... ##ALIASBODIES## (NUL) ... ##FUNCTIONBODIES## (NUL) ... ##END##`
// exported-only ##ENV## (names): [[ $v == *export* ]] — DO NOT reuse for the residue snapshot
```
</interfaces>

<validated_ground>
Refuted mechanisms — DO NOT reintroduce (04-EVIDENCE.md / OQ-8 / the validated-ground facts / CH-2..CH-6):
1. Shadow-restore guards use `${+name}` set-tests — NEVER `-n`/`-z` truthiness, NEVER `${(P)+literalName}` (C1/OQ-8: `(P)` indirects through the VALUE, wrong for a literal slot name). The env-path `${(P)+var}` where `var` HOLDS the name is the correct exception (C2/C21).
2. Function bodies are LIVE CODE — restore via verbatim `functions[name]=$capturedBody`, NOT single-quote-wrapped-to-inert (C6). zquote (`'\''`) wrapping is for env scalar values + alias bodies ONLY.
3. emit.go is a NEW activation-codegen file, distinct from regen.go; the Dynamic-keyed static-quoted-vs-dynamic-verbatim split is NEW behavior emit.go implements (C7) — regen.go never reads Dynamic.
4. Ownership-aware PATH element removal uses LITERAL-equality matching with a QUOTED RHS `[[ $e == "$target" ]]`, NOT `${path:#pattern}` (C10 glob) and NOT an UNQUOTED RHS `[[ $e == $target ]]` (CH-6: unquoted RHS is ALSO glob-matched — reintroduces C10). Restore correct under `typeset -U path`: rebuild-from-base + re-apply, NOT naive per-addition string subtraction (C11). C25 (quoted-RHS literal-equality) is UNVERIFIED — pass-2 must prove it.
5. The zero-residue property test snapshots function BODIES and a FULL env (ALL `${(@kv)parameters}`, exported AND non-exported, name+VALUE), NOT the exported-only names `##ENV##` section (C19/CH-2).
6. emit.go emits PLAIN functions — NO `emulate -L`, NO `LOCAL_OPTIONS` (Pitfall 1/C8). Do NOT copy introspectScript's `emulate -L zsh` line.
7. Undo-slot names sanitized to `[A-Za-z0-9_]` (hash-suffixed to avoid `a/b`↔`a_b` collisions) OR an assoc-array keyed by raw name (C24/OQ-10); a profile named `feature/x` makes an invalid `typeset -g` target and is an injection vector — the adversarial NAME corpus must run through the slot-name derivation path (MEDIUM).
8. `${(P)+var}==1` for env unset-vs-empty (C2/C21, where var holds the name — the correct indirect form).
9. OQ-18: emit bare `zp_*` calls; the property test supplies helper defs inline. Hybrid-preamble option dropped.
10. CH-3: deactivate UNSETS its own per-switch undo slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels); `ZP_BASE_PATH` is session-persistent and stays.
11. CH-4: `AddAlias`/`AddFunc` APPLY code captures the shadowed prior BEFORE overwrite, `${+name}`/`${+slot}`-guarded (idempotent so double-checkout does not clobber the clean prior — MEDIUM).
</validated_ground>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core/shell/zsh/emit.go — reverse codegen + Emitter seam + composition-root wiring</name>
  <files>core/shell/zsh/emit.go, core/shell/zsh/emit_test.go, core/shell/provider.go, core/cmd/zsh-pro/main.go, core/shell/zsh/invariant_test.go</files>
  <read_first>
    - core/shell/zsh/regen.go (the codegen STYLE to copy — method on Provider{}, fmt.Sprintf/strings.Builder, total switch, verbatim-value discipline for dynamics; note it never reads Dynamic — emit.go ADDS that branch, C7)
    - core/shell/provider.go (the Regenerator interface + composite Provider to mirror for the new Emitter seam)
    - core/cmd/zsh-pro/main.go (the sole composition root; wire Emitter like the Regenerator, injectable-but-not-driven like the store)
    - core/activate/plan.go + .planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md (the FINAL Plan op union — exact identifiers to switch on; the CH-1c distinct RebuildListFromBase/ApplyListDelta and CH-7 no-WasOn semantics)
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md (the Loader Reference Snippet — reproduce its STRUCTURE with the OQ-8 correction, NOT verbatim)
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §4 (zquote verified safe POC-Inj/Eval; static-vs-dynamic split POC-Dyn/C7; plain-fn POC-Z5; slot naming OQ-10; §"Don't Hand-Roll")
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C25 (UNVERIFIED quoted-RHS literal PATH removal — pass-2 pending), C24 (slot-name sanitization)
    - .planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md OQ-14 (deactivate slot cleanup vs filter), OQ-18 (bare zp_* calls)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"emit.go", §"provider.go", §"main.go"
  </read_first>
  <behavior>
    - Injection test (T-01-06): a static SetScalar/AddAlias value containing each of `it's`, `foo; touch $CANARY`, `x$(touch $CANARY)y`, a backtick-command, `line1\ntouch $CANARY\nline3`, an `a'\''b` chain, and the kitchen-sink combo emits a single-quote-wrapped literal such that sourcing the emitted block under zsh -f fires NO canary and the value round-trips byte-literal (POC-Inj/Eval).
    - Static-vs-dynamic test (OQ-6, first-class): a value `$HOME/go` on an op with Dynamic==true emits VERBATIM (zsh expands it); the SAME value with Dynamic==false emits zquote'd (stays literal `$HOME/go`). A static value carrying shell metacharacters is ALWAYS quoted regardless of any flag path.
    - Shadow-prior capture test (CH-4 — Addresses review concern: CH-4): the emitted APPLY code for `AddAlias{ll}`/`AddFunc{ff}` captures the pre-existing shadowed body BEFORE overwriting, guarded by `${+name}` (grep the emitted apply for a `${+` guard wrapping the capture of `ll`/`ff` into the shadow-prior slot); a functional test: apply over a pre-existing `ll`/`ff`, then run deactivate's RestoreShadowedAlias/RestoreShadowedFunc, and assert the restored body byte-equals the pre-existing one (the captured prior IS what deactivate restores). Capture is `${+slot}`-guarded idempotent (MEDIUM double-checkout): applying twice does not overwrite the clean-base prior with already-applied state — assert a second apply leaves the slot unchanged.
    - Function-body test (C6): AddFunc / RestoreShadowedFunc emits `functions[name]=$captured` (verbatim capture-and-reassign), NOT a zquote'd literal; a body with `;`/`'`/newline restores byte-identical and is NOT executed at restore time.
    - Plain-function test (Pitfall 1): the emitted apply/deactivate functions contain no `emulate -L` and no `LOCAL_OPTIONS`; a `setopt` inside apply persists after the function returns.
    - Guard-correctness test (OQ-8): shadow restore uses `${+name}` set-tests (grep the emitted deactivate for `${+` and assert NO `${(P)+ZP_` literal-name form and NO `[[ -n ` shadow guard); env restore uses `${(P)+var}==1` where var holds the name.
    - PATH literal-equality test (CH-6 — Addresses review concern: CH-6): the emitted PATH element-removal uses a QUOTED-RHS `[[ $e == "$target" ]]` (grep the emitted deactivate/rebuild for `== "$target"`, quoted); assert the emitted source contains NO unquoted `== $target` and NO `:#` glob form. Functional (skip-guarded on zsh): with a `$path` element `/opt/tool*` (containing a `*`) and a `$target` of a sibling `/opt/toolX`, the rebuild does NOT drop `/opt/tool*` (no over-match). Note: C25 is UNVERIFIED — pass-2 proves the quoted form is literal-equality before this is trusted.
    - Slot-cleanup test (CH-3 — Addresses review concern: CH-3): the emitted deactivate UNSETS its own per-switch undo slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels) — grep the emitted deactivate for `unset` of those slot patterns; a functional check that after apply→deactivate none of the per-switch slots remain set, while `ZP_BASE_PATH` (session-persistent) is NOT unset by deactivate.
    - Slot-name test (OQ-10 + MEDIUM injection-corpus-through-slot-path): a profile name / alias name with a non-`[A-Za-z0-9_]` char (e.g. `feature/x`) AND an adversarial injection name (e.g. `x$(touch $CANARY)y`) both produce a valid `typeset -g` target (sanitized) OR an assoc-array keyed by raw name — the emitted block parses under `zsh -n` and firing NO canary through the slot-name path.
    - zsh -n test (C18): both emitted apply and deactivate blocks pass `zsh -n` (syntax-valid) on the RAW block (not eval-wrapped).
    - Injectability test (CH-5 — Addresses review concern: CH-5): the PATH-rebuild and value-quoting rendering are package-level funcs (`var renderList`, `var renderValue`) — a test can save, substitute, and restore them; assert Emit routes list/value rendering THROUGH these vars (a substituted stub is observably reflected in Emit's output).
    - Invariant test (D-11): a tree grep finds reverse zsh tokens (`unalias`, `unset -f`, `unsetopt`, PATH-array rebuild) generated only under `core/shell/zsh/emit.go` (and its `_test.go`), never in regen.go / core/activate / core/model.
  </behavior>
  <action>
    Create `core/shell/zsh/emit.go` (a DISTINCT new file; regen.go stays forward-only, D-11). Add `zquote(s string) string { return "'" + strings.ReplaceAll(s, "'", `'\''`) + "'" }` (verified injection-safe, D-13). CH-5: factor the two escape-sensitive rendering steps into PACKAGE-LEVEL funcs so the mutant test (Task 2) can substitute them against the stateless zero-value `Provider{}` — `var renderValue = func(applied string, dynamic bool) string { ... zquote if !dynamic else verbatim ... }` and `var renderList = func(name string, additions []string) string { ... "PATH=\"$ZP_BASE_PATH\"; path=(<additions> $path)" rebuild-from-base ... }` — Emit calls THROUGH these vars (never inlines the logic), so a test that reassigns `renderList`/`renderValue` observably changes Emit's output. Add a slot-name sanitizer (OQ-10 + MEDIUM): either replace every non-`[A-Za-z0-9_]` byte in the profile+name segments with `_` (append a short hash to avoid `a/b`↔`a_b` collisions), OR — the RESEARCH-preferred design — store priors in a `typeset -gA` associative array keyed by the RAW name (sidesteps identifier rules AND injection through the slot-name path; document the choice). Implement the Emitter: a method on `Provider{}` (e.g. `func (Provider) Emit(p activate.Plan) (apply, deactivate string, err error)`; single-method vs two-method signature is discretion per D-10) that renders the Plan via `strings.Builder` — NOT text/template (RESEARCH §4). Emit apply from `p.Activate` and deactivate from `p.Deactivate` as PLAIN functions (NO `emulate -L`, NO `LOCAL_OPTIONS` — Pitfall 1/C8). Per-op rendering: `SetScalar` → capture the LIVE prior into per-terminal runtime undo state (`typeset -g` globals / assoc-array keyed slot, `${+slot}`-guarded idempotent capture — MEDIUM double-checkout — NOT the manifest's declarative `original`, D-12) then `export name=<renderValue(Applied,Dynamic)>` (C7 NEW branch, routed through `renderValue`); `AddAlias` → CH-4: `${+name}`-guarded capture-before-overwrite of the pre-existing alias body into the shadow-prior slot (idempotent), THEN `alias name=<renderValue(Body,Dynamic)>`; `AddFunc` → CH-4: `${+functions[name]}`-guarded capture of the pre-existing function body into the shadow-prior slot (idempotent), THEN `functions[name]=$capturedBody` verbatim (C6 — NEVER zquote a function body); `ApplyListDelta` (activate) → `renderList(name, additions)` rebuild-from-base RE-ADDING this profile's additions; `RebuildListFromBase` (deactivate, CH-1c distinct) → rebuild TO base with NO additions re-added (`PATH="$ZP_BASE_PATH"; path=($path)` from the captured base only), with any element removal by QUOTED-RHS literal-equality rebuild (`for e in $path; do [[ $e == "$target" ]] || newpath+=($e); done` — CH-6, quoted RHS is mandatory; NEVER unquoted `== $target` and NEVER `${path:#pattern}` C10; C25 UNVERIFIED, pass-2 proves it); `RestoreScalar`/`UnsetScalar` → drift-guarded `${(P)+var}==1` (var holds the name, C2/C21) restore-only-if-live==applied; `Unalias`/`UnsetFunc` → `unalias`/`unset -f` the added names; `RestoreShadowedAlias`/`RestoreShadowedFunc` → guarded by `${+name}` set-tests (NOT `-n`, NOT `${(P)+literalName}` — OQ-8/C1), restoring the captured prior (alias via `alias name=$prior`, function via `functions[name]=$prior`); `RestoreOption` → CH-7: read the RUNTIME-captured `was_on` slot (captured live at apply time, like scalars/shadows — the op carries no authoritative WasOn), then `setopt`/`unsetopt` to that captured value, not a blind toggle. CH-3: at the END of the emitted deactivate, UNSET every per-switch undo slot this switch created (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels, the captured `was_on` slot) — but do NOT unset `ZP_BASE_PATH` (session-persistent, OQ-3/OQ-14). Apply-side capture must be idempotent so a double-checkout (apply over already-applied state) does not clobber the clean-base prior (`${+slot}`-guarded — only capture when the slot is not already set). OQ-18: emit BARE `zp_*` helper CALLS; the property test (Task 2) supplies the helper defs inline (drop the Hybrid preamble). In `core/shell/provider.go`, add an `Emitter` interface alongside `Regenerator`, doc-commented as the SOLE reverse-syntax codegen seam (mirror the Regenerator doc language); it takes a `core/activate.Plan`. Keep ISP: whether `Emitter` joins the composite `Provider` or stands alone is discretion — resolve import placement so no cycle forms (`shell` importing `core/activate`, an agnostic value package, is acceptable). In `core/cmd/zsh-pro/main.go`, wire `zsh.Provider{}` as the `shell.Emitter` exactly like the Regenerator; the downstream consumer (loader/CLI verbs) is Phase 5, so construct + make injectable now but leave un-driven (mirror the store's `_, _ = s, err` non-fatal comment). Add `invariant_test.go`: a tree grep asserting reverse zsh tokens appear only under `core/shell/zsh/emit.go` (+ its `_test.go`).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go build ./... && GOTOOLCHAIN=auto go test ./core/shell/... -run 'Emit|Inject|Dynamic|Plain|Guard|Slot|Invariant|Zshn|Shadow|Capture|PathLiteral|Cleanup|Render' 2>&1 | tail -10; test ${PIPESTATUS[1]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-01-06 (shell injection via emitted eval'd code — THE phase risk): the injection test proves every static value site is zquote'd inert; the static-metacharacter-always-quoted test guards misclassification (OQ-6); the slot-name injection corpus proves the name→slot path cannot inject (MEDIUM). Threat T-04-06 (portability freeze / under-quote): the Dynamic-keyed split keeps `$HOME` late-bound yet a static metacharacter value always quoted. Threat T-04-07 (invalid typeset target / slot-name injection): OQ-10 sanitization/assoc-array keeps slot names valid+safe. Threat T-04-08 (silent shadow-restore no-op): `${+name}` guards (OQ-8) replace the buggy `-n`/`${(P)+literal}`; CH-4 capture-before-overwrite ensures a prior EXISTS to restore. Threat T-04-13 (glob over-match on PATH removal, CH-6): quoted-RHS `[[ $e == "$target" ]]` literal-equality (C25, pass-2) prevents the C10 collateral deletion; the metacharacter-element functional test proves no over-match. Threat T-04-03 (single-emit-path): invariant_test.go confines reverse tokens to emit.go. All emitted code is `zsh -n`-validated (C18).
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go build ./...` succeeds; `GOTOOLCHAIN=auto go test ./core/shell/...` (the Emit subset) exits 0
    - emitted blocks are `zsh -n`-valid: the emit_test asserts `zsh -n` exit 0 on both apply and deactivate (skip-guarded on LookPath zsh)
    - PLAIN functions: `grep -c 'emulate -L' core/shell/zsh/emit.go` returns 0 AND `grep -c 'LOCAL_OPTIONS' core/shell/zsh/emit.go` returns 0
    - `${+name}` shadow guards, no buggy forms: `grep -c '${+' core/shell/zsh/emit.go` >= 1 AND the emitted deactivate string contains no `${(P)+ZP_` literal-name form and no `[[ -n ` shadow-restore guard (asserted in emit_test)
    - CH-6 quoted-RHS literal PATH removal: `grep -c '== "$target"' core/shell/zsh/emit.go` >= 1 AND `grep -c ':#' core/shell/zsh/emit.go` returns 0 AND the emitted source contains no unquoted `== $target` (asserted in emit_test); the metacharacter-element functional test passes
    - CH-4 shadow-prior capture-before-overwrite: emit_test asserts the emitted apply for AddAlias/AddFunc contains a `${+`-guarded capture of the prior BEFORE the overwrite, and a functional test asserts deactivate restores the captured pre-existing body byte-identically; a second apply does not clobber the captured prior (idempotent)
    - CH-3 slot cleanup: emit_test asserts the emitted deactivate `unset`s its per-switch slots (`__ZP_ORIG_`/`ZP_..._PRIOR_`/sentinel patterns) and does NOT unset `ZP_BASE_PATH`
    - CH-5 injectable rendering: `grep -E 'var renderList' core/shell/zsh/emit.go` AND `grep -E 'var renderValue' core/shell/zsh/emit.go` both match; a test substitutes them and observes the change in Emit output
    - CH-7 option restore reads runtime slot: the emitted RestoreOption reads a captured `was_on` runtime slot (not a Go-side WasOn) — emit_test asserts the emitted deactivate references the captured option slot
    - single-emit-path invariant: `invariant_test.go` passes — reverse tokens (`\bunalias\b`, `\bunset -f\b`, `\bunsetopt\b`, PATH-array rebuild) generated only under core/shell/zsh/emit.go (+ its _test.go)
    - `shell.Emitter` interface exists: `grep -E 'Emitter interface' core/shell/provider.go` matches; main.go wires it (`grep -c 'zsh.Provider{}' core/cmd/zsh-pro/main.go` >= 1)
  </acceptance_criteria>
  <done>emit.go renders the Plan to injection-safe (zquote static / verbatim dynamic), drift-guarded (${(P)+var}==1), ownership-aware (QUOTED-RHS literal-equality PATH rebuild-from-base, CH-6), `${+name}`-guarded with capture-before-overwrite (CH-4), self-cleaning per-switch slots (CH-3), runtime-captured option restore (CH-7), PLAIN loader functions that pass `zsh -n`; rendering factored into injectable renderList/renderValue vars (CH-5); the Emitter seam is wired at the composition root; reverse tokens live only in emit.go.</done>
</task>

<task type="auto">
  <name>Task 2: zero-residue property test (N>=20, full-env snapshot, two independent mutants + meta-assert) — the SW-02 pin</name>
  <files>core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/store/roundtrip_test.go (the LookPath-guarded, zsh-requiring, external-test-package full-cycle round-trip precedent + the roundTripSrc fixture style)
    - core/shell/zsh/introspect_test.go (the LookPath skip-guard shape; the extended introspectScript body sections)
    - core/shell/zsh/emit.go (Task 1 output — Emit + the injectable renderList/renderValue vars the mutants substitute, CH-5; bare zp_* calls needing inline helper defs)
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §6 (harness structure, single-zsh-f-process snapshot Pitfall 5/POC-Z9a, mutated-emitter POC-Z6, six-class-full-fidelity C19, fixtures)
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C19 (full-env requirement), C25 (metacharacter PATH element for pass-2)
    - .planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md OQ-13 (base-ownership, not multi-managed), OQ-14 (slot cleanup / ZP_BASE_PATH present-in-both)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"Property test"
  </read_first>
  <behavior>
    - Full-env snapshot meta-assert (CH-2 — Addresses review concern: CH-2): the snapshot instrument iterates ALL `${(@kv)parameters}` (exported AND non-exported), emitting name+VALUE with NUL framing (NOT the exported-only names `##ENV##` section). Meta-test: the instrument's diff FIRES on (i) an injected non-exported var leak (`typeset localonly=x`) and (ii) an exported-var value-only change (`export EXISTING=changed`) — proving it would catch what the old names-only instrument missed.
    - Base-ownership sub-check (CH-1a/b — Addresses review concern: CH-1a, CH-1b): base owns `/usr/local/bin` (it is in `ZP_BASE_PATH`); a profile ALSO adds `/usr/local/bin`; after apply→deactivate, `/usr/local/bin` is STILL present because RebuildListFromBase restores `PATH="$ZP_BASE_PATH"` which already contains it — NOT a two-active-managed-profile scenario. A test comment documents multi-managed co-ownership as OUT OF SCOPE for single-active v2.0 (OQ-13).
    - Two independent mutants + per-mutant assertion (CH-5 — Addresses review concern: CH-5): (1) substitute `renderList` with a blind-append variant (`path=(<add> $path)` with no rebuild-from-base) → the property snapshot diff is NON-empty / `$#path` grows; (2) substitute `renderValue` to DROP zquote on a static metacharacter value (`x; touch $CANARY`) → the canary FIRES / the value is not literal. Each mutant is asserted INDEPENDENTLY (its own sub-test) to produce a non-empty residue diff / fired canary, verified separately from the happy path (meta-test that the pin actually detects residue).
    - PATH metacharacter element (CH-6 — Addresses review concern: CH-6): the fixtures include a PATH element containing a `*`/`?` metacharacter; the residue test proves rebuild-from-base does not over-match/collateral-delete it (ties to C25, pass-2).
    - Slot-cleanup consistency (CH-3): the full-env snapshot is taken with `ZP_BASE_PATH` set BEFORE the baseline snapshot (present in both baseline and post, nets to zero) OR excluded via a single audited documented `ZP_BASE_PATH`-only filter (state the choice); per-switch `ZP_`/`__ZP_` slots are unset by deactivate so they never appear as residue.
    - Happy-path property: for each of N>=20 random switch sequences, the full-env + function-BODY + `$#path` snapshot is byte-identical pre-vs-post.
  </behavior>
  <action>
    Create `core/shell/zsh/residue_test.go` (default suite, NOT behind a `spike` tag — D-17; `exec.LookPath("zsh")` skip-guard like introspect_test.go). Build >=2 profiles exercising all six classes with: at least one shadow collision (both profiles override the same pre-existing `ll` alias and `ff` function — exercising CH-4 capture/restore), one BASE-owned shared PATH addition (`/usr/local/bin` in `ZP_BASE_PATH` AND added by a profile — the CH-1a/b base-ownership criterion, NOT two active managed profiles), one PATH element containing a metacharacter (`/opt/tool*` — CH-6), and one dynamic value (`export GOPATH=$HOME/go`). For each of N>=20 random switch sequences: `activate.Build` → Manifest → `activate.Diff` → Plan → `Provider{}.Emit` → apply/deactivate strings. Run the WHOLE sequence in ONE sourced `zsh -f` script (Pitfall 5 — `zsh -f` inherits parent env, so cross-process snapshots give false diffs): set `ZP_BASE_PATH` (containing `/usr/local/bin` and `/opt/tool*`) BEFORE the baseline snapshot (CH-3 — so it nets to zero, or use the single documented filter), define the pre-existing `ll`/`ff` + the `zp_*` helper defs inline (emit.go emits bare calls per OQ-18), snapshot, source each emitted apply/deactivate in random order, snapshot again. CH-2: the snapshot instrument is a NEW full-env section iterating ALL `${(@kv)parameters}` (exported AND non-exported) emitting name+VALUE with NUL framing, PLUS function BODIES (reuse the extended introspect body sections), PLUS the PATH scalar + `$path` array + `$#path` count — do NOT reuse the exported-only names `##ENV##` section (C19/CH-2). Assert a LITERAL empty diff of pre-vs-post snapshot strings and `$#path` stable. Honor `testing.Short()` by reducing N but KEEPING both mutants. Mutants (CH-5, first-class, each independently asserted): save/substitute/restore the package-level `renderList`/`renderValue` vars from Task 1 — mutant (1) blind-append PATH (swap renderList's rebuild-from-base for `path=(<add> $path)`) MUST produce a non-empty diff / grown `$#path`; mutant (2) drop-zquote (swap renderValue to emit a static metacharacter value verbatim) MUST fire the canary / leave a non-literal value — assert EACH in its own sub-test, independent of the happy path. Add a meta-assert sub-test proving the full-env instrument itself catches (i) a non-exported var leak and (ii) an exported value-only change (CH-2). Sub-checks: ownership (CH-1a/b base-ownership: `/usr/local/bin` survives deactivate via rebuild-from-base; a comment marks multi-managed OUT OF SCOPE per OQ-13); drift (a hand-edited managed env var survives deactivate; an untouched one is reversed); shadow (the shadowed `ll`/`ff` restored byte-for-byte via the CH-4-captured prior; profile-added names gone); metacharacter-PATH (CH-6: `/opt/tool*` not over-matched).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go test ./core/shell/zsh/ -run 'Residue|ZeroResidue|Ownership|Drift|Shadow|Mutated|Mutant|FullEnv|MetaAssert' -count=1 2>&1 | tail -12; test ${PIPESTATUS[0]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-04-09 (residue accumulation escaping the test): the property test snapshots function BODIES + FULL env (all `${(@kv)parameters}`, exported AND non-exported, name+VALUE) + `$#path` (C19/CH-2), so body-level and non-exported/value-only residue cannot pass silently — the meta-assert proves the instrument catches both. The two independent mutants prove the test actually DETECTS residue: blind-append PATH flips it (SW-02 PATH acceptance) and dropped-zquote fires the canary (SW-02 injection acceptance) — asserted separately from the happy path (CH-5). Threat T-04-13 (PATH glob over-match): the metacharacter PATH element proves rebuild-from-base does not collateral-delete (CH-6/C25). Ties to T-01-06 (owner-trust): the dynamic `$HOME/go` exercises the verbatim path end-to-end without executing an injection.
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go test ./core/shell/zsh/ -run Residue -count=1` exits 0 when zsh is present (skips cleanly when absent)
    - N>=20: the test drives >=20 random switch sequences (grep the test source for the N constant >= 20)
    - CH-2 full-env instrument: `grep -c '(@kv)parameters' core/shell/zsh/residue_test.go` >= 1 (iterates ALL params, not the exported-only `##ENV##` names section); the meta-assert sub-test (non-exported leak + exported value-only change fire the diff) passes
    - CH-5 two independent mutants: the sub-test swapping `renderList` for blind-append asserts a non-empty diff / grown `$#path`; a SECOND sub-test swapping `renderValue` to drop zquote asserts a fired canary / non-literal value; each mutant's failure is asserted in its own sub-test independent of the happy path
    - CH-1a/b base-ownership: the ownership sub-check asserts `/usr/local/bin` (in ZP_BASE_PATH, also profile-added) survives deactivate via rebuild-from-base; a comment marks multi-managed co-ownership out of scope (OQ-13)
    - CH-6 metacharacter element: a `/opt/tool*`-style PATH element is present in the fixtures and the test asserts it is not over-matched/deleted
    - runs in the DEFAULT suite (no `//go:build spike` tag): `grep -c 'go:build spike' core/shell/zsh/residue_test.go` returns 0
    - snapshot captures function BODIES and full env (not names-only): the snapshot reuses the extended introspect body sections AND the full-env `${(@kv)parameters}` section
  </acceptance_criteria>
  <done>A default-suite, LookPath-guarded property test drives N>=20 random switch sequences, asserts a byte-identical full-env (all params, name+VALUE, exported AND non-exported) + function-BODY + `$#path` snapshot, includes base-ownership/drift/shadow/metacharacter-PATH sub-checks, a meta-assert proving the full-env instrument catches non-exported+value-only residue (CH-2), and TWO independently-asserted mutants (blind-append PATH; dropped-zquote) that each genuinely fail it (CH-5) — the SW-02 regression pin, composed alongside the testgen oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile value → emitted `eval`'d zsh code | THE phase boundary. A user-controlled env value, alias body, function body, or profile/alias NAME (used in an undo-slot) crossing into emitted code that a sourced loader `eval`s. A quoting bug here becomes a shell-injection bug (T-01-06, ROADMAP line 214). The adversarial corpus runs through BOTH the value path and the slot-NAME derivation path (MEDIUM). |
| owner's profile (v2.0) | Same trust boundary as the owner's own `.zshrc` — a verbatim dynamic value executing is equivalent to it being in their `.zshrc`. Shared/imported profiles (SHARE-01) are a future milestone with its own trust gate (residual/deferred). |
| live runtime state → deactivate | Deactivate reads captured undo state (`ZP_BASE_PATH`, `__ZP_ORIG_*`, shadow-prior slots, captured `was_on`); a drifted (hand-edited) live value must NOT be clobbered; per-switch slots are cleaned up, `ZP_BASE_PATH` persists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Elevation of Privilege / Tampering | emitted `eval`'d code (static scalar/alias VALUE contexts) | mitigate | `zquote()` single-quote `'\''` wrapping at EVERY static value site (POC-Inj/Eval); a static value with `'`/`;`/`$(...)`/backtick/newline is always an inert literal (Task 1). The adversarial NAME corpus also runs through the slot-name derivation path (MEDIUM). |
| T-04-06 | Tampering | Dynamic-keyed emit split | mitigate | Split keys strictly on the op's `Dynamic` bool (carried from the IR, never re-derived); a static metacharacter value is ALWAYS quoted regardless of flag path — asserted by a dedicated test (OQ-6). Over-quoting (freezing `$HOME`) and under-quoting (injection) both prevented. The dropped-zquote mutant (Task 2) proves the residue pin detects an under-quote. |
| T-04-07 | Elevation of Privilege | runtime undo-slot naming | mitigate | Slot names sanitized to `[A-Za-z0-9_]` (hash-suffixed) OR a `typeset -gA` assoc-array keyed by raw name (OQ-10/C24); a profile named `feature/x` cannot produce an invalid/injecting `typeset -g` target — the injection corpus through the slot-name path proves no canary fires (MEDIUM). |
| T-04-08 | Tampering | shadow-restore guards + capture | mitigate | `${+name}` set-tests (NOT `-n`, NOT `${(P)+literalName}` — OQ-8/C1); env path uses `${(P)+var}==1` (var holds the name, C2/C21). CH-4: `AddAlias`/`AddFunc` capture the shadowed prior BEFORE overwrite (`${+`-guarded, idempotent), so a prior EXISTS for deactivate to restore. |
| T-04-10 | Tampering | function-body restore | mitigate | Function bodies restored verbatim via `functions[name]=$capturedBody` (C6) — NEVER zquote'd-to-inert (a function body is live code). The captured body is trusted live code (already in the user's env). |
| T-04-13 | Tampering | PATH element removal glob over-match | mitigate | QUOTED-RHS literal-equality `[[ $e == "$target" ]]` (CH-6) — an UNQUOTED RHS or `${path:#pattern}` glob over-matches a `* ? [ ]` element (C10). C25 UNVERIFIED — pass-2 proves the quoted form is literal; the metacharacter-element functional + residue tests are the runtime backstop (Task 1 + Task 2). |
| T-04-14 | Tampering | per-switch undo-slot residue | mitigate | CH-3: emitted deactivate UNSETS its own per-switch slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels, captured `was_on`); `ZP_BASE_PATH` is session-persistent and preserved. The full-env residue snapshot (CH-2) would otherwise flag the slots — so cleanup is verified, not filtered-away (OQ-14). |
| T-04-03 | Elevation of Privilege | single-emit-path invariant | mitigate | `invariant_test.go` tree-grep: reverse zsh tokens generated only under `emit.go` — all injection surface confined to one auditable file (Task 1). |
| T-04-09 | Tampering | residue accumulation | mitigate | Zero-residue property test snapshots BODIES + FULL env (all params, exported+non-exported, name+VALUE) + `$#path` (C19/CH-2); a meta-assert proves the instrument catches non-exported+value-only residue; two independent mutants (blind-append PATH; dropped-zquote) prove the test detects residue (Task 2, SW-02 pin, CH-2/CH-5). |
| T-04-02 | Tampering | drift guard | mitigate | Deactivate restores an env scalar only if `live == applied` (`${(P)+var}==1` unset-vs-empty); a hand-edited managed var survives — drift sub-check in the property test (Task 2). |
| T-04-12 | Denial of Service | partial-apply / interrupted switch | accept (Phase 5 seam) | Plain functions, no transaction/rollback; a switch interrupted mid-way can leave partial state, recoverable by re-running `checkout` (idempotent rebuild-from-base) or a fresh terminal. A transactional recovery contract is deferred to the Phase 5 loader — documented seam. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (stdlib + existing `mvdan.cc/sh`); no package-manager install tasks — no legitimacy gate needed. |
</threat_model>

<verification>
- `GOTOOLCHAIN=auto go build ./...` succeeds (Emitter seam + main.go wiring compile).
- `GOTOOLCHAIN=auto go test ./...` passes (emit + residue + invariant + all prior suites).
- Emitted apply and deactivate blocks pass `zsh -n` (C18, asserted in emit_test on the RAW block).
- CH-6 quoted-RHS present, no glob: `grep -c '== "$target"' core/shell/zsh/emit.go` >= 1 AND `grep -c ':#' core/shell/zsh/emit.go` == 0.
- CH-5 injectable rendering present: `grep -E 'var renderList|var renderValue' core/shell/zsh/emit.go` matches.
- `make check` (fmt-check + vet + lint + test) stays green; the zero-residue property test runs in the default suite (skips when zsh absent) and composes alongside — never replaces — the testgen oracle pin.
- No new dependency: `go.mod` require block unchanged.
</verification>

<success_criteria>
- `core/shell/zsh/emit.go` is the sole place reverse zsh tokens are generated (invariant test), renders the Plan to PLAIN, `zsh -n`-valid apply/deactivate functions, injection-safe (zquote static / verbatim dynamic), drift-guarded (`${(P)+var}==1`), ownership-aware (QUOTED-RHS literal-equality PATH rebuild-from-base, CH-6), `${+name}`-guarded shadow restore WITH capture-before-overwrite (CH-4), self-cleaning per-switch slots (CH-3), runtime-captured option restore (CH-7), rendering factored into injectable renderList/renderValue vars (CH-5). (SW-01, T-01-06)
- The `shell.Emitter` seam mirrors `shell.Regenerator` and is wired at the composition root. (SW-01)
- A default-suite property test over N>=20 random switch sequences leaves the FULL-env (all params, name+VALUE) + function-BODY + `$#path` snapshot byte-identical (CH-2); a meta-assert proves the instrument catches non-exported+value-only residue; TWO independent mutants (blind-append PATH; dropped-zquote) each fail it (CH-5); base-ownership `/usr/local/bin` survives deactivate (CH-1a/b). (SW-02)
- All decisions D-10..D-13, D-16, D-17 implemented; the refuted mechanisms (OQ-8 `-n`/`${(P)+literal}`; C6 zquote'd function body; C10 `${path:#}`/unquoted `== $target` glob; Pitfall 1 `emulate -L`) are NOT reintroduced; CH-1..CH-8 and the addressed MEDIUMs are carried.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-02-SUMMARY.md` when done, recording: the Emitter seam signature chosen, the CH-5 renderList/renderValue package-level-var factoring, the OQ-18 bare-zp_*-calls decision, the OQ-10 slot-naming approach (sanitize vs assoc-array) and the injection-corpus-through-slot-path result, the CH-3 per-switch slot-cleanup list + ZP_BASE_PATH-persistence handling, the CH-4 capture-before-overwrite mechanism, the CH-6 quoted-RHS PATH removal (flag C25 UNVERIFIED for pass-2), the CH-2 full-env snapshot instrument, the property-test N + the TWO mutant mechanisms + the meta-assert, and confirmation that no refuted mechanism was reintroduced.
</output>
