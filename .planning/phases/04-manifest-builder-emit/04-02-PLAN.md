---
phase: 04-manifest-builder-emit
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/provider.go
  - core/cmd/zsh-pro/main.go
  - core/shell/zsh/residue_test.go
  - core/shell/zsh/invariant_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "core/shell/zsh/emit.go renders a core/activate.Plan to zsh apply + deactivate code as PLAIN loader functions (no emulate -L / LOCAL_OPTIONS) that pass zsh -n; it is the SOLE place reverse zsh tokens are generated; the PATH-rebuild and value-quoting rendering are factored into injectable PACKAGE-LEVEL funcs (renderList/renderValue) so a mutant test can substitute them against a stateless Provider{} (SW-01) [Addresses review concern: CH-5]"
    - "A static user-controlled value containing ' ; $(...) backtick or newline is emitted single-quote-wrapped ('\\'' escape) so it is always an inert literal under eval; a dynamic value stays verbatim so zsh expands it per-machine — the split keys strictly on the op's Dynamic bool (SW-01, threat T-01-06)"
    - "renderList applies the SAME static-zquote / dynamic-verbatim discipline to EACH PATH/FPATH addition segment (not RAW splicing): a $PATH/$path/$HOME/$(...)-style DYNAMIC self-reference segment stays VERBATIM in the eval'd path=(<additions> $path) array (portability, D-05/D-10 — never hard-quoted), a STATIC segment (incl. a static glob /opt/tool*) is zquote'd so it is an inert literal — so an addition segment cannot break out of the array literal under the loader double-eval (the 04-01 builder already drops an unsafe static metacharacter/command-sub segment to no-part; emit's per-segment split is the sole-codegen-seam enforcement, defense in depth); this completes the uniform injection boundary — values C5/C6/C7, names C24/C29/C31, addition segments C32 (SW-01, threat T-01-06) [Addresses review concern: CH-24]"
    - "AddAlias/AddFunc emitted APPLY code captures the pre-existing (shadowed) alias/function body BEFORE overwriting, guarded by ${+name}, into the shadow-prior runtime slot — so deactivate's RestoreShadowedAlias/RestoreShadowedFunc has a prior to restore; capture is ${+slot}-guarded idempotent so a double-checkout does not overwrite the clean prior (SW-02) [Addresses review concern: CH-4, MEDIUM double-checkout]"
    - "SetOption emitted APPLY code captures the LIVE option state ([[ -o optname ]]) into a per-switch was_on runtime slot, ${+slot}-guarded idempotent, BEFORE emitting setopt/unsetopt — so deactivate's RestoreOption reads a populated slot and restores the option to its exact prior (setopt/unsetopt on the captured value); covers was-on/was-off/was-toggled (SW-02) [Addresses review concern: CH-9]"
    - "SetOption emits the option NAME unquoted into [[ -o optname ]]/setopt/unsetopt/the was_on slot name, so emit.go validates OptionSet.Name against the bare-identifier grammar ^[A-Za-z_][A-Za-z0-9_]*$ before emitting (reject/no-part on failure) — the adversarial NAME corpus (foo; touch $CANARY / x$(...)y / foo -o bar) fires no canary through the SetOption path while extendedglob still round-trips; the option name is on the T-01-06 trust boundary (SW-01, threat T-01-06/T-04-17) [Addresses review concern: CH-17]"
    - "AddAlias/AddFunc/Unalias/UnsetFunc/RestoreShadowedAlias/RestoreShadowedFunc emit the alias/function NAME unquoted into eval'd command positions (unalias name / alias name=... / functions[name]=... / unset -f name / the shadow-prior slot name), and zsh PERMITS shell metacharacters in alias/func names — so emit.go re-validates each name against the conservative safe grammar ^[A-Za-z0-9_][A-Za-z0-9_.-]*$ before emitting (reject/no-part on failure, defense in depth over the 04-01 builder drop) — the adversarial NAME corpus (gs;touch $CANARY / x$(...)y / backtick / a|b / a b / a\\nb / backslash forms gs\\;x) fires no canary through ANY of the AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowed* paths while realistic names (gs / ll / git-foo / _helper / foo.bar) still round-trip; the alias/func name is on the T-01-06 trust boundary (SW-01, threat T-01-06/T-04-18) [Addresses review concern: CH-19]"
    - "Deactivate is ownership-aware and drift-guarded: an env scalar is restored only if live==applied (${(P)+var}==1 for unset-vs-empty); PATH list reversal is a FULL rebuild-to-base (RebuildListFromBase, NO per-element removal loop on the deactivate hot path in single-active v2.0 — CH-11/CH-12), so a profile addition is dropped by rebuilding to base; the quoted-RHS literal-equality form ([[ $e == \"$target\" ]], NOT glob) is documented as the RESERVED (currently unexercised) form for future element deletion; a shadowed alias/function is restored via ${+name} set-tests; function bodies restored verbatim via functions[name]=$captured, never zquote'd; deactivate UNSETS its own per-switch undo slots (__ZP_ORIG_*, ZP_<profile>_PRIOR_*, sentinels, the was_on slot) while ZP_BASE_PATH stays session-persistent (SW-02) [Addresses review concern: CH-3, CH-11, CH-12]"
    - "A zero-residue property test over N>=20 random BALANCED switch sequences (each apply matched by its deactivate before a different profile applies — CH-14) under sandboxed zsh -f leaves a FULL-env snapshot (ALL ${(@kv)parameters}, exported AND non-exported, name+VALUE, NUL-framed, made SELF-STABLE by a TYPE-CLASS filter that skips any param whose type value — read for free from the ${(@kv)parameters} VALUE side — matches (*special*|*tied*|*hide*|undefined), keeping only concrete user-scope types (scalar[-export]/array/association/integer/float), plus a reserved ZP__ prefix for the instrument's own capture locals + fd-capture — CH-10, C28 correction) CONCATENATED WITH the SORTED function-BODY section (${(@ok)functions} — CH-22) + a dedicated PATH section carrying BOTH $#path AND the sorted $path array CONTENTS (CH-25 — a count-preserving PATH reorder/swap is caught by the general property, not only $#path) + the two dedicated SORTED filter-independent sections (option-state ${(@ok)options}→optname=<on|off> and alias-body ${(@ok)aliases}→name<NUL>body — CH-16/CH-20) ALL byte-identical across the general N-sequence compare (CH-21/CH-25 — the alias/option/PATH-content classes are bound into the general property, not only isolated sub-checks); the base-ownership sub-check is strengthened to assert $#path byte-identical to baseline AND /usr/local/bin appears exactly once (CH-15); a post-sequence assertion confirms ZP_BASE_PATH equals its baseline (session-persistent invariant — CH-23); THREE independently-asserted mutants (blind-append PATH; dropped-zquote on a metacharacter static value; base-strip) each make it FAIL; the snapshot instrument itself is meta-asserted to be self-stable (empty diff across two no-op snapshots) AND to catch a non-exported leak and an exported value-only change; a STATIC metacharacter/command-sub PATH addition segment sourced through the ApplyListDelta emit path fires NO canary while a DYNAMIC segment (`$HOME/bin`) expands late-bound (CH-24, C32); the general compare binds the ordered `$path` array CONTENTS not only `$#path` (CH-25) (SW-02, the regression pin) [Addresses review concern: CH-2, CH-5, CH-10, CH-14, CH-15, CH-16, CH-20, CH-21, CH-22, CH-23, CH-24, CH-25]"
  artifacts:
    - path: "core/shell/zsh/emit.go"
      provides: "Emit(activate.Plan)->(apply,deactivate string,err); package-level renderList/renderValue vars (CH-5); zquote; Dynamic-keyed value split; renderList per-SEGMENT static-zquote/dynamic-verbatim split for PATH additions (CH-24); shadow-prior capture-before-overwrite (CH-4); SetOption apply-side live-option capture (CH-9); option-name identifier-grammar validation (CH-17); alias/func-name safe-grammar re-validation via aliasFuncNameRe (CH-19); deactivate = full rebuild-to-base, quoted-RHS element removal reserved/documented (CH-11/CH-12); per-switch slot cleanup incl. was_on slot (CH-3); slot-name sanitization (D-10..D-13, OQ-8/OQ-10/OQ-18)"
      contains: "var renderList"
    - path: "core/shell/provider.go"
      provides: "shell.Emitter seam mirroring shell.Regenerator (D-10)"
      contains: "Emitter interface"
    - path: "core/cmd/zsh-pro/main.go"
      provides: "Emitter wiring at the sole composition root (D-10)"
      contains: "Provider{}"
    - path: "core/shell/zsh/residue_test.go"
      provides: "zero-residue property test, N>=20 BALANCED sequences (CH-14), self-stable full-env snapshot (CH-10) CONCATENATED with SORTED dedicated option-state/alias-body (${(@ok)options}/${(@ok)aliases} — CH-16/CH-20) + SORTED function-body (${(@ok)functions} — CH-22) + a dedicated PATH section carrying $#path AND the ordered $path array CONTENTS (CH-25) into the general byte-identical compare (CH-21/CH-25), THREE independent mutants + self-stability & leak meta-asserts (D-16/D-17, SW-02 pin, CH-2/CH-5/CH-10/CH-15), a PATH addition-segment injection sub-test (static segment inert / dynamic segment late-bound — CH-24, C32), option-drift fixture (CH-9), ZP_BASE_PATH-persistence post-sequence assertion (CH-23)"
      contains: "parameters"
    - path: "core/shell/zsh/invariant_test.go"
      provides: "tree-grep: reverse zsh tokens generated only under emit.go (D-11)"
      contains: "unalias"
  key_links:
    - from: "core/shell/zsh/emit.go"
      to: "core/activate.Plan"
      via: "Emit consumes the Plan op union defined in 04-01"
      pattern: "activate.Plan"
    - from: "core/cmd/zsh-pro/main.go"
      to: "shell.Emitter"
      via: "zsh.Provider{} satisfies the Emitter seam, injected at the composition root"
      pattern: "zsh.Provider"
    - from: "residue_test.go"
      to: "full-env ${(@kv)parameters} snapshot instrument"
      via: "a NEW full-env snapshot (NOT the exported-only ##ENV## section), reused inline in one zsh -f process"
      pattern: "(@kv)parameters"
---

<objective>
Render the agnostic `Plan` (from 04-01) to injection-safe, drift-guarded, ownership-aware zsh apply/deactivate code in `core/shell/zsh/emit.go` — the single place reverse zsh syntax lives — reached via a new `shell.Emitter` seam wired at the composition root; and pin zero-residue with a full-env property test whose mutants genuinely fail.

Purpose: SW-01 requires the sole emit path; SW-02 requires zero residue verified by a property test that can detect residue. This is the phase's highest-risk work (threat T-01-06: shell-code emission is where a quoting bug becomes an injection bug).
Output: `core/shell/zsh/emit.go` + `Emitter` seam + main.go wiring + the zero-residue property test + the single-emit-path invariant test.

Cycle-1 review resolutions carried here: CH-1a/b (base-ownership property sub-check + multi-managed out-of-scope prose), CH-2 (full-env snapshot instrument), CH-3 (deactivate cleans its per-switch slots), CH-4 (shadow-prior capture-before-overwrite), CH-5 (injectable renderList/renderValue + two independent mutants + meta-assert), CH-6 (quoted-RHS literal PATH removal — now reserved/documented per CH-11/CH-12), and MEDIUMs (double-checkout capture idempotency, injection corpus through slot-name path, OQ-18 bare zp_* calls).
Cycle-2 review resolutions carried here: CH-9 (SetOption apply-side live-option capture + option-drift fixture), CH-10 (self-stable full-env snapshot: TYPE-CLASS filter skipping `(*special*|*tied*|*hide*|undefined)`-typed params + reserved `ZP__` prefix + fd-capture + self-stability meta-test), CH-11+CH-12 (deactivate = full rebuild-to-base; per-element quoted-RHS removal loop REMOVED from deactivate and documented as the reserved form for future element deletion; metacharacter fixture asserts base-restore DROPS a profile-added `/opt/tool*`), CH-14 (balanced switch-sequence generator + invariant meta-check), CH-15 (base-ownership sub-check strengthened: `$#path` byte-identical AND `/usr/local/bin` exactly once + a base-strip negative-control mutant). C25 is PROVEN, C26 STATIC-VALIDATED, and C27 (live option capture/restore round-trip) + C28 (self-stable full-env snapshot instrument via the type-class filter) are BOTH PROVEN per 04-EVIDENCE.md — no pass-2 POC remains for any of them.
Cycle-3 review resolutions carried here: CH-16 (dedicated, filter-INDEPENDENT option-state + alias-body snapshot sections — the `aliases`/`options` classes are `*special*`-typed so the C28 type-class filter excludes them from the generic `${(@kv)parameters}` section; they need dedicated sections like PATH/function-bodies; + `zmodload zsh/parameter` preamble + populated-map baseline assertion), CH-17 (SetOption option-name validated against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` before emit — reject/no-part on failure; adversarial NAME corpus through the SetOption path; option name added to the T-01-06 trust boundary), CH-18 (stale-cruft cleanup: `<done>`/verification labels reworded to the CH-11/CH-12 full-rebuild-to-base mechanism; C27/C28 relabelled PROVEN). C29 (hostile option name rejected/inert — CH-17) and C30 (dedicated option-state + alias-body sections self-stable with firing meta-asserts — CH-16) are BOTH PROVEN per 04-EVIDENCE.md — no pass-3 POC remains.
Cycle-4 review resolutions carried here: CH-19 (emit.go re-validates the alias AND function NAMES against the conservative safe grammar `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting them unquoted into unalias/alias=/functions[]=/unset -f/RestoreShadowed* — the two name classes zsh permits metacharacters in, left open by CH-17 which covered only option names; defense in depth over the 04-01 builder drop; threat T-04-18), CH-20 (dedicated option/alias snapshot sections use SORTED `${(@ok)options}`/`${(@ok)aliases}` matching PROVEN C30, not unsorted `${(@k)}`), CH-21 (the dedicated sorted option-state + alias-body sections are CONCATENATED into the general N-sequence byte-identical happy-path snapshot, not only isolated sub-checks), CH-22 (the reused FUNCTIONBODIES snapshot section iterates SORTED `${(@ok)functions}` and is in the self-stability assertion), CH-23 (relabel C29/C30 PROVEN; ZP_BASE_PATH-persistence post-sequence assertion; PATH/`path` *tied*/*special* note; optional `$fpath` parity). C31 (hostile alias/func name rejected/inert across the unalias/alias=/functions[]=/unset -f/RestoreShadowed* emit paths — CH-19) is PROVEN per 04-EVIDENCE.md (pass-4) — no POC remains to run for it.
Cycle-5 review resolutions carried here: CH-24 (renderList applies the SAME renderValue static-zquote/dynamic-verbatim split to EACH PATH addition segment — a dynamic self-reference segment stays verbatim for portability, a static metacharacter/command-sub segment is zquote'd inert; the 04-01 builder already drops an unsafe static segment to no-part, so emit's split completes the uniform injection boundary: values C5/C6/C7, names C24/C29/C31, addition segments C32), CH-25 (the general N-sequence byte-identical compare binds the sorted `$path` array CONTENTS, not only `$#path`, closing the PATH-class residual as CH-21 did for aliases/options/functions), CH-26 (C31 relabelled PROVEN). NEW UNVERIFIED claim for pass-5: C32 (a STATIC metacharacter/command-sub PATH addition segment is emitted inert — no canary through the ApplyListDelta double-eval — AND a DYNAMIC segment `$HOME/bin` expands late-bound — CH-24).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-SPEC.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-PATTERNS.md
@.planning/phases/04-manifest-builder-emit/04-EVIDENCE.md
@.planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md
@.planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md

<interfaces>
<!-- Contracts from 04-01 (Wave 1) and the codebase. The 04-01-SUMMARY records the FINAL Plan op union — read it for exact identifiers. -->

From core/activate/plan.go (04-01 — the Plan this Emit renders; op set is a sealed union carrying agnostic data only; RebuildListFromBase and ApplyListDelta are DISTINCT ops with distinct semantics per CH-1c; RestoreOption has NO authoritative WasOn per CH-7):
```go
type Plan struct { Deactivate []Op; Activate []Op }
// deactivate ops: RestoreScalar{Name,Applied; Original *string}, UnsetScalar{Name,Applied},
//   RebuildListFromBase{Name}  // rebuild TO base, NO additions to re-add (CH-1c),
//   Unalias{Name}, RestoreShadowedAlias{Name}, UnsetFunc{Name}, RestoreShadowedFunc{Name},
//   RestoreOption{Name}  // WasOn dropped/non-authoritative — live prior captured in emit.go (CH-7)
// activate ops: SetScalar{Name,Applied; Dynamic bool}, ApplyListDelta{Name; Additions,Deletions},
//   AddAlias{Name,Body; Dynamic bool}, AddFunc{Name,Body}, SetOption{Name; Enabled bool}
```

From core/shell/provider.go (MODIFY — mirror the Regenerator seam):
```go
type Regenerator interface { Regenerate(e model.Entry) string }
type Provider interface { Parser; Classifier; Introspector; Regenerator }
// ADD: type Emitter interface { Emit(p activate.Plan) (apply, deactivate string, err error) } (signature is discretion)
```

From core/shell/zsh/regen.go (the codegen STYLE to copy — method on Provider{}, fmt.Sprintf/strings.Builder, total switch — but emit.go is a DISTINCT file that shares no code and ADDS a Dynamic-keyed quote branch regen.go does NOT have, C7). NOTE (CH-5): Provider is a zero-value struct with NO fields, so the mutant test cannot flip a struct flag — factor the PATH-rebuild and value-quoting into package-level vars the test substitutes:
```go
func (Provider) Regenerate(e model.Entry) string // emits e.Value VERBATIM in every branch, never reads Dynamic
```

From core/cmd/zsh-pro/main.go (the sole composition root — wire Emitter like the Regenerator; downstream consumer is Phase 5, so injectable-but-not-yet-driven is fine, mirror the `_, _ = s, err` comment):
```go
provider := zsh.Provider{} // is Parser/Classifier/Introspector AND Regenerator; add: AND Emitter
```

From core/shell/zsh/introspect.go (04-01 extended — the body-dump instrument). CH-2: the property test must NOT reuse the exported-only `##ENV##` section (it is names-only + exported-only, the C19 false-green). It needs a NEW full-env snapshot section:
```go
const introspectScript = `... ##ALIASBODIES## (NUL) ... ##FUNCTIONBODIES## (NUL) ... ##END##`
// exported-only ##ENV## (names): [[ $v == *export* ]] — DO NOT reuse for the residue snapshot
```
</interfaces>

<validated_ground>
Refuted mechanisms — DO NOT reintroduce (04-EVIDENCE.md / OQ-8 / the validated-ground facts / CH-2..CH-5 + CH-11/CH-12; CH-6 superseded by CH-11/CH-12):
1. Shadow-restore guards use `${+name}` set-tests — NEVER `-n`/`-z` truthiness, NEVER `${(P)+literalName}` (C1/OQ-8: `(P)` indirects through the VALUE, wrong for a literal slot name). The env-path `${(P)+var}` where `var` HOLDS the name is the correct exception (C2/C21).
2. Function bodies are LIVE CODE — restore via verbatim `functions[name]=$capturedBody`, NOT single-quote-wrapped-to-inert (C6). zquote (`'\''`) wrapping is for env scalar values + alias bodies ONLY.
3. emit.go is a NEW activation-codegen file, distinct from regen.go; the Dynamic-keyed static-quoted-vs-dynamic-verbatim split is NEW behavior emit.go implements (C7) — regen.go never reads Dynamic.
4. Deactivate list reversal is a FULL rebuild-to-base (CH-11/CH-12) — NO per-element removal loop is emitted on the deactivate hot path in single-active v2.0; a profile-added element is dropped by rebuilding to base. The quoted-RHS `[[ $e == "$target" ]]` literal-equality form (C25 PROVEN) is the DOCUMENTED, RESERVED form for a FUTURE per-element removal path (`ListDelta.Deletions` / multi-managed) — currently unexercised; quoting is a GLOB_SUBST/robustness guard there, NOT the sole barrier. NEVER emit `${path:#pattern}` (C10 glob) and NEVER an unquoted `== $target`. Restore correct under `typeset -U path`: rebuild-from-base, NOT naive per-addition string subtraction (C11).
5. The zero-residue property test snapshots function BODIES and a FULL env (ALL `${(@kv)parameters}`, exported AND non-exported, name+VALUE), NOT the exported-only names `##ENV##` section (C19/CH-2).
6. emit.go emits PLAIN functions — NO `emulate -L`, NO `LOCAL_OPTIONS` (Pitfall 1/C8). Do NOT copy introspectScript's `emulate -L zsh` line.
7. Undo-slot names sanitized to `[A-Za-z0-9_]` (hash-suffixed to avoid `a/b`↔`a_b` collisions) OR an assoc-array keyed by raw name (C24/OQ-10); a profile named `feature/x` makes an invalid `typeset -g` target and is an injection vector — the adversarial NAME corpus must run through the slot-name derivation path (MEDIUM).
8. `${(P)+var}==1` for env unset-vs-empty (C2/C21, where var holds the name — the correct indirect form).
9. OQ-18: emit bare `zp_*` calls; the property test supplies helper defs inline. Hybrid-preamble option dropped.
10. CH-3: deactivate UNSETS its own per-switch undo slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels); `ZP_BASE_PATH` is session-persistent and stays.
11. CH-4: `AddAlias`/`AddFunc` APPLY code captures the shadowed prior BEFORE overwrite, `${+name}`/`${+slot}`-guarded (idempotent so double-checkout does not clobber the clean prior — MEDIUM).
12. CH-9: `SetOption` APPLY code captures the LIVE option (`[[ -o optname ]]`) into a `${+slot}`-guarded idempotent was_on slot BEFORE `setopt`/`unsetopt` — mirroring SetScalar/AddAlias. Without it `RestoreOption` reads an unwritten slot and option restore silently fails. C27 (live option capture/restore round-trip) is PROVEN per 04-EVIDENCE.md (20/20 asserts, was-ON/OFF/toggled + idempotent double-apply + double-eval) — the Plan Task 1 SetOption/RestoreOption rules are correct as written; do NOT re-run a pass-2 POC. CH-17: the option NAME (`OptionSet.Name`, user-controlled) is validated against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` BEFORE emit (reject/no-part on failure) so it cannot inject through `[[ -o optname ]]`/`setopt`/`unsetopt`/the was_on slot-name derivation; C29 (hostile option name rejected/inert) is PROVEN per 04-EVIDENCE.md.
15. CH-19: the alias AND function NAMES (`AddAlias`/`AddFunc`/`Unalias`/`UnsetFunc`/`RestoreShadowedAlias`/`RestoreShadowedFunc` `.Name`, user-controlled) are emitted UNQUOTED into eval'd command positions (`unalias name`/`alias name=...`/`functions[name]=...`/`unset -f name`/the shadow-prior slot name), and zsh PERMITS shell metacharacters in alias/func names (unlike env-scalar names) — so emit.go RE-validates each name against the conservative safe grammar `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` BEFORE emit (reject/no-part on failure, defense in depth over the 04-01 builder drop). The 04-01 builder is the first line of defense (drop-to-no-part on RAW extracted bytes); emit re-validates because it is the sole reverse-syntax codegen seam. NEVER emit a raw alias/func name into a command position without validating. C31 (hostile alias/func name rejected/inert across the unalias/alias=/functions[]=/unset -f/RestoreShadowed* emit paths) is PROVEN per 04-EVIDENCE.md (pass-4, like C29 for option names, C24 for slot names) — no POC remains to run for it. [Addresses review concern: CH-19]
16. CH-24: a PATH/FPATH `ApplyListDelta.Additions` SEGMENT is a THIRD injectable class emitted into the eval'd `path=(<additions> $path)` array literal. `renderList` MUST NOT splice a segment RAW — it applies the SAME renderValue static-zquote/dynamic-verbatim split PER SEGMENT: a DYNAMIC self-reference segment (`$PATH`/`$path`/`$HOME/bin`/`${...}`/`$(...)` clean self-reference) stays VERBATIM so zsh expands it late-bound (portability, D-05/D-10 — do NOT hard-quote it, that would freeze `$HOME`); a STATIC segment (no `$`/backtick) is zquote'd so it is an inert literal in the array (a static glob `/opt/tool*` is a legitimate literal element and zquotes cleanly). The 04-01 builder already drops any unsafe static metacharacter/command-sub segment to no-part (drop-to-no-part), so a genuinely unsafe static segment never reaches renderList; emit's per-segment split is the sole-codegen-seam enforcement (defense in depth). Classify DYNAMIC-vs-STATIC per segment using the SAME Dynamic signal the value path uses (the op carries the entry's Dynamic bool; a segment is dynamic when it contains a `$`/`${...}`/`$(...)`/backtick construct). NEVER splice an addition segment into the array literal without the split. C32 (a STATIC metacharacter/command-sub segment is inert — no canary through the ApplyListDelta double-eval — AND a DYNAMIC segment expands late-bound) is NEW UNVERIFIED — pass-5 must POC-prove it (like C29/C31 for names, C5 for values). [Addresses review concern: CH-24]
13. CH-10: the full-env `${(@kv)parameters}` snapshot is NOT self-stable naively — but a NAMED-scalar volatile-param exclusion allowlist (`SECONDS RANDOM LINENO funcstack pipestatus _ ...`) is INSUFFICIENT and was REFUTED (C28): two no-op snapshots still drift on ~40 UNLISTED params — the module-backed `undefined`-typed associations (`aliases/builtins/functions/history/options/termcap/...`), the funcstack-family (`funcfiletrace/argv/parameters`), and LAZILY-instantiated specials (`LOGCHECK/WATCHFMT/zle_bracketed_paste`) that the first snapshot's reads materialize. A name-list cannot cover a whole CLASS. Instead use a TYPE-CLASS filter (C28 correction, PROVEN self-stable 5/5 with both meta-asserts firing): iterate `${(@kv)parameters}` and SKIP any key whose TYPE value (the map's VALUE side, read for FREE without touching `${(P)k}`) matches `(*special*|*tied*|*hide*|undefined)`, keeping only concrete user-scope types (`scalar`/`scalar-export`/`array`/`association`/`integer`/`float`). Because the filter never reads `${(P)k}` on a special, it never triggers a lazy sibling — killing the lazy-init drift at the root. Reserve a `ZP__` prefix for the instrument's own capture-target locals so they are excluded too. RETAIN fd/temp-file capture (NOT `$(...)`) — that part was correct. C28 is now PROVEN (the corrected type-class filter was proven self-stable 5/5, empty diff, with both meta-asserts firing: a non-exported `scalar` leak caught + an exported value-only change isolated). The original named-list mechanism remains historically REFUTED. [Addresses review concern: C28 correction (type-class filter)]
14. CH-11/CH-12: deactivate is a FULL rebuild-to-base — do NOT emit a per-element removal loop there; the quoted-RHS `[[ $e == "$target" ]]` form is the RESERVED (currently unexercised) element-removal form for future deletion/multi-managed, like `ListDelta.Deletions`. C25 PROVEN, C26 STATIC-VALIDATED — no pass-2 POC pending for either.
</validated_ground>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core/shell/zsh/emit.go — reverse codegen + Emitter seam + composition-root wiring</name>
  <files>core/shell/zsh/emit.go, core/shell/zsh/emit_test.go, core/shell/provider.go, core/cmd/zsh-pro/main.go, core/shell/zsh/invariant_test.go</files>
  <read_first>
    - core/shell/zsh/regen.go (the codegen STYLE to copy — method on Provider{}, fmt.Sprintf/strings.Builder, total switch, verbatim-value discipline for dynamics; note it never reads Dynamic — emit.go ADDS that branch, C7)
    - core/shell/provider.go (the Regenerator interface + composite Provider to mirror for the new Emitter seam)
    - core/cmd/zsh-pro/main.go (the sole composition root; wire Emitter like the Regenerator, injectable-but-not-driven like the store)
    - core/activate/plan.go + .planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md (the FINAL Plan op union — exact identifiers to switch on; the CH-1c distinct RebuildListFromBase/ApplyListDelta and CH-7 no-WasOn semantics)
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md (the Loader Reference Snippet — reproduce its STRUCTURE with the OQ-8 correction, NOT verbatim)
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §4 (zquote verified safe POC-Inj/Eval; static-vs-dynamic split POC-Dyn/C7; plain-fn POC-Z5; slot naming OQ-10; §"Don't Hand-Roll")
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C25 (PROVEN quoted-RHS literal-equality — the reserved element-removal form, no re-POC needed), C24 (PROVEN slot-name sanitization — the C29 option-name validator mirrors it), C27 (PROVEN — live option capture/restore round-trips exactly; SetOption/RestoreOption rules correct as written, no re-POC), C28 (PROVEN — self-stable full-env snapshot instrument via the type-class filter, no re-POC), C29 (PROVEN — hostile option-name rejected by the identifier grammar or emitted inert, no re-POC), C31 (PROVEN pass-4 — hostile alias/func name rejected/inert across the unalias/alias=/functions[]=/unset -f/RestoreShadowed* emit paths; the C31 alias/func-name validator mirrors C29/C24; no re-POC), C32 (NEW UNVERIFIED — a STATIC metacharacter/command-sub PATH addition segment is emitted inert AND a DYNAMIC segment expands late-bound through the ApplyListDelta emit path, pass-5 must prove; the renderList per-segment split mirrors renderValue/C5)
    - .planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md OQ-14 (deactivate slot cleanup vs filter), OQ-18 (bare zp_* calls)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"emit.go", §"provider.go", §"main.go"
  </read_first>
  <behavior>
    - Injection test (T-01-06): a static SetScalar/AddAlias value containing each of `it's`, `foo; touch $CANARY`, `x$(touch $CANARY)y`, a backtick-command, `line1\ntouch $CANARY\nline3`, an `a'\''b` chain, and the kitchen-sink combo emits a single-quote-wrapped literal such that sourcing the emitted block under zsh -f fires NO canary and the value round-trips byte-literal (POC-Inj/Eval). CH-24: the SAME adversarial corpus placed as a STATIC `ApplyListDelta` addition SEGMENT (e.g. a segment `x; touch $CANARY`, `/opt/x$(touch $CANARY)`, backtick, space, newline) emits zquote'd inert in the `path=(<additions> $path)` array — sourcing the emitted apply fires NO canary.
    - Static-vs-dynamic test (OQ-6, first-class): a value `$HOME/go` on an op with Dynamic==true emits VERBATIM (zsh expands it); the SAME value with Dynamic==false emits zquote'd (stays literal `$HOME/go`). A static value carrying shell metacharacters is ALWAYS quoted regardless of any flag path.
    - PATH addition-segment split test (CH-24 — Addresses review concern: CH-24): `renderList` emits each addition segment through the SAME static-zquote/dynamic-verbatim discipline as `renderValue`, NOT raw. A DYNAMIC self-reference segment (`$HOME/bin`, `$PATH`) emits VERBATIM in the `path=(<additions> $path)` array (grep the emitted apply: the `$HOME/bin` segment is unquoted so zsh expands it). A STATIC segment (`/opt/tool*`, `/usr/local/bin`) emits zquote'd (single-quote-wrapped inert literal). A STATIC segment carrying a shell metacharacter/command-sub (`x; touch $CANARY`, `/opt/x$(touch $CANARY)`, backtick, space, newline) — if it were to reach renderList (the 04-01 builder normally drops it to no-part) — emits zquote'd so it is inert (no canary when the apply block is sourced under `zsh -f`), NOT spliced raw. Assert: sourcing the emitted apply for a static metacharacter/command-sub addition segment fires NO canary, AND a dynamic `$HOME/bin` addition segment expands to the late-bound `$HOME`-derived path (not a literal `$HOME/bin`). Classify DYNAMIC-vs-STATIC per segment using the op's Dynamic bool / a `$`-or-backtick construct scan (same signal the value path uses).
    - Shadow-prior capture test (CH-4 — Addresses review concern: CH-4): the emitted APPLY code for `AddAlias{ll}`/`AddFunc{ff}` captures the pre-existing shadowed body BEFORE overwriting, guarded by `${+name}` (grep the emitted apply for a `${+` guard wrapping the capture of `ll`/`ff` into the shadow-prior slot); a functional test: apply over a pre-existing `ll`/`ff`, then run deactivate's RestoreShadowedAlias/RestoreShadowedFunc, and assert the restored body byte-equals the pre-existing one (the captured prior IS what deactivate restores). Capture is `${+slot}`-guarded idempotent (MEDIUM double-checkout): applying twice does not overwrite the clean-base prior with already-applied state — assert a second apply leaves the slot unchanged.
    - Option live-capture test (CH-9 — Addresses review concern: CH-9): the emitted APPLY code for a `SetOption{Name,Enabled}` op captures the LIVE option state BEFORE toggling — mirroring the SetScalar/AddAlias capture rule. Grep the emitted apply for a `${+slot}`-guarded idempotent capture of the live option into a per-switch was_on slot using `[[ -o optname ]]` (the zsh live-option test), emitted BEFORE the `setopt`/`unsetopt`. A functional test: with `extendedglob` live-ON before apply and a profile that `unsetopt extendedglob`, apply captures was_on=on into the slot; then deactivate's `RestoreOption` reads that captured slot and `setopt extendedglob` restores it to exactly ON. Cover all three: (i) option was live-ON, profile turns it OFF → restored ON; (ii) option was live-OFF, profile turns it ON → restored OFF; (iii) option was live-ON, profile also sets it ON (no net change) → restored ON. Capture is `${+slot}`-guarded idempotent (double-checkout does not clobber the clean-base captured was_on).
    - Option-name injection test (CH-17 — Addresses review concern: CH-17): the option NAME (`OptionSet.Name`, ultimately user-controlled parser word-literals) is emitted UNQUOTED into `[[ -o optname ]]`, `setopt optname`, `unsetopt optname`, and the was_on slot-name derivation — so it MUST be validated against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` before emit. Run the adversarial NAME corpus (`foo; touch $CANARY`, `x$(touch $CANARY)y`, a backtick-command name, `foo -o bar`, `ext glob`, `a'\''b`) through the SetOption emit path: emit.go either produces NO SetOption output for the hostile name (rejected, matching the builder-side drop-to-no-part) OR the emitted block sourced under `zsh -f` fires NO canary. A conforming name (`extendedglob`) still emits and round-trips (C27 regression — the validator does not over-reject). This mirrors the OQ-10/C24 slot-name defense already applied to profile/alias names; the option name is now on the same trust boundary.
    - Alias/function-name injection test (CH-19 — Addresses review concern: CH-19): the alias/function NAME (`AddAlias`/`AddFunc`/`Unalias`/`UnsetFunc`/`RestoreShadowedAlias`/`RestoreShadowedFunc` `.Name`, ultimately RAW user-controlled parser word-literals — zsh PERMITS metacharacters in these names) is emitted UNQUOTED into `unalias name`, `alias name=...`, `functions[name]=...`, `unset -f name`, and the shadow-prior slot-name derivation — so it MUST be re-validated against the conservative safe grammar `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emit (defense in depth over the 04-01 builder drop). Run the adversarial NAME corpus (`gs;touch $CANARY`, `x$(touch $CANARY)y`, a backtick-command name, `a|b`, `a b`, `a<newline>b`, the backslash forms parse.go retains `gs\;x`) through EACH of the AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowedAlias/RestoreShadowedFunc emit paths: emit.go either produces NO output for the hostile name (rejected, matching the builder-side drop-to-no-part) OR the emitted block sourced/double-eval'd under `zsh -f` fires NO canary. Realistic names (`gs`, `ll`, `git-foo`, `_helper`, `foo.bar`) still emit and round-trip (the validator does not over-reject). This mirrors the CH-17 option-name defense and the OQ-10/C24 slot-name defense; the alias/func name is now on the same T-01-06 trust boundary. IMPORTANT: validate the RAW name bytes (parse.go retains the backslash in `gs\;x`).
    - Plain-function test (Pitfall 1): the emitted apply/deactivate functions contain no `emulate -L` and no `LOCAL_OPTIONS`; a `setopt` inside apply persists after the function returns.
    - Guard-correctness test (OQ-8): shadow restore uses `${+name}` set-tests (grep the emitted deactivate for `${+` and assert NO `${(P)+ZP_` literal-name form and NO `[[ -n ` shadow guard); env restore uses `${(P)+var}==1` where var holds the name.
    - PATH element-removal form is DOCUMENTED-BUT-UNEXERCISED (CH-11 + CH-12 — Addresses review concern: CH-11, CH-12): deactivate's list reversal is a full REBUILD-TO-BASE (`RebuildListFromBase`), so there is NO per-element `$target` comparison on the deactivate hot path in single-active v2.0 — the quoted-RHS removal loop is NOT emitted there. The quoted-RHS `[[ $e == "$target" ]]` literal-equality form (C25 PROVEN) is documented in an emit.go code comment as the CORRECT form RESERVED for element-level removal WHEN it is genuinely needed (future per-element deletion / `ListDelta.Deletions` / multi-managed co-ownership) — currently unexercised, exactly like `ListDelta.Deletions` (04-01). Test: assert emit.go's source CONTAINS the quoted `== "$target"` form in a comment/reserved helper AND that no ACTIVE (non-comment) emitted `RebuildListFromBase` output contains an element-removal loop; assert NO `:#` glob form and NO unquoted `== $target` anywhere in emit.go. Correct the prose: with a runtime-VARIABLE `$target` and GLOB_SUBST OFF (zsh default), quoting the RHS is a robustness / GLOB_SUBST guard for WHEN the removal path is used — NOT the sole barrier and NOT something a functional test on the (unused) deactivate path can pin. Do NOT present a deactivate functional test as pinning the C10 fix.
    - Slot-cleanup test (CH-3 — Addresses review concern: CH-3): the emitted deactivate UNSETS its own per-switch undo slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels) — grep the emitted deactivate for `unset` of those slot patterns; a functional check that after apply→deactivate none of the per-switch slots remain set, while `ZP_BASE_PATH` (session-persistent) is NOT unset by deactivate.
    - Slot-name test (OQ-10 + MEDIUM injection-corpus-through-slot-path): a profile name / alias name with a non-`[A-Za-z0-9_]` char (e.g. `feature/x`) AND an adversarial injection name (e.g. `x$(touch $CANARY)y`) both produce a valid `typeset -g` target (sanitized) OR an assoc-array keyed by raw name — the emitted block parses under `zsh -n` and firing NO canary through the slot-name path.
    - zsh -n test (C18): both emitted apply and deactivate blocks pass `zsh -n` (syntax-valid) on the RAW block (not eval-wrapped).
    - Injectability test (CH-5 — Addresses review concern: CH-5): the PATH-rebuild and value-quoting rendering are package-level funcs (`var renderList`, `var renderValue`) — a test can save, substitute, and restore them; assert Emit routes list/value rendering THROUGH these vars (a substituted stub is observably reflected in Emit's output).
    - Invariant test (D-11): a tree grep finds reverse zsh tokens (`unalias`, `unset -f`, `unsetopt`, PATH-array rebuild) generated only under `core/shell/zsh/emit.go` (and its `_test.go`), never in regen.go / core/activate / core/model.
  </behavior>
  <action>
    Create `core/shell/zsh/emit.go` (a DISTINCT new file; regen.go stays forward-only, D-11). Add `zquote(s string) string { return "'" + strings.ReplaceAll(s, "'", `'\''`) + "'" }` (verified injection-safe, D-13). CH-5: factor the two escape-sensitive rendering steps into PACKAGE-LEVEL funcs so the mutant test (Task 2) can substitute them against the stateless zero-value `Provider{}` — `var renderValue = func(applied string, dynamic bool) string { ... zquote if !dynamic else verbatim ... }` and `var renderList = func(name string, additions []string, dynamic bool) string { ... "PATH=\"$ZP_BASE_PATH\"; path=(<rendered-additions> $path)" rebuild-from-base, where EACH addition segment is run through the SAME renderValue static-zquote/dynamic-verbatim discipline (CH-24) — a $PATH/$path/$HOME/${...}/$(...)-style DYNAMIC self-reference segment stays VERBATIM (portability, D-05/D-10, NEVER hard-quoted), a STATIC segment is zquote'd inert; classify DYNAMIC-vs-STATIC per segment from the op's Dynamic bool / a `$`-or-backtick construct scan (the same signal renderValue uses) ... }` — Emit calls THROUGH these vars (never inlines the logic), so a test that reassigns `renderList`/`renderValue` observably changes Emit's output. **CH-24: renderList MUST NOT splice an addition segment RAW into the array literal — a static metacharacter/command-sub segment would break out under the loader double-eval (`export PATH=/opt/x$(touch $CANARY):$PATH`). The 04-01 builder already drops an unsafe static segment to no-part, so renderList normally sees only safe segments, but emit's per-segment zquote is the sole-codegen-seam enforcement (defense in depth) and is where C32 is proven.** Add a slot-name sanitizer (OQ-10 + MEDIUM): either replace every non-`[A-Za-z0-9_]` byte in the profile+name segments with `_` (append a short hash to avoid `a/b`↔`a_b` collisions), OR — the RESEARCH-preferred design — store priors in a `typeset -gA` associative array keyed by the RAW name (sidesteps identifier rules AND injection through the slot-name path; document the choice). Implement the Emitter: a method on `Provider{}` (e.g. `func (Provider) Emit(p activate.Plan) (apply, deactivate string, err error)`; single-method vs two-method signature is discretion per D-10) that renders the Plan via `strings.Builder` — NOT text/template (RESEARCH §4). Emit apply from `p.Activate` and deactivate from `p.Deactivate` as PLAIN functions (NO `emulate -L`, NO `LOCAL_OPTIONS` — Pitfall 1/C8). Add a package-level alias/func NAME validator (CH-19): `var aliasFuncNameRe = regexp.MustCompile(`^[A-Za-z0-9_][A-Za-z0-9_.-]*$`)` (or an inline byte scan) — a conservative safe grammar that rejects any shell metacharacter (`;` `&` `|` `$` backtick `(` `)` `<` `>` space newline backslash glob chars) while accepting realistic names (`gs`/`ll`/`git-foo`/`_helper`/`foo.bar`). Every op that emits an alias/function NAME into a command position — `AddAlias`, `AddFunc`, `Unalias`, `UnsetFunc`, `RestoreShadowedAlias`, `RestoreShadowedFunc` — MUST first validate `Name` against this grammar and emit NO output for that op on failure (reject/no-part, defense in depth over the 04-01 builder drop; validate the RAW name bytes — parse.go retains the backslash in `gs\;x`). NEVER emit a raw alias/func name into `unalias name`/`alias name=...`/`functions[name]=...`/`unset -f name`/the shadow-prior slot-name derivation without this validation. Per-op rendering: `SetScalar` → capture the LIVE prior into per-terminal runtime undo state (`typeset -g` globals / assoc-array keyed slot, `${+slot}`-guarded idempotent capture — MEDIUM double-checkout — NOT the manifest's declarative `original`, D-12) then `export name=<renderValue(Applied,Dynamic)>` (C7 NEW branch, routed through `renderValue`); `AddAlias` → CH-19: FIRST validate `Name` against `aliasFuncNameRe`; on failure emit NO part for this op. For a valid name → CH-4: `${+name}`-guarded capture-before-overwrite of the pre-existing alias body into the shadow-prior slot (idempotent), THEN `alias name=<renderValue(Body,Dynamic)>`; `AddFunc` → CH-19: FIRST validate `Name` against `aliasFuncNameRe`; on failure emit NO part. For a valid name → CH-4: `${+functions[name]}`-guarded capture of the pre-existing function body into the shadow-prior slot (idempotent), THEN `functions[name]=$capturedBody` verbatim (C6 — NEVER zquote a function body); `ApplyListDelta` (activate) → `renderList(name, additions, Dynamic)` rebuild-from-base RE-ADDING this profile's additions, with EACH addition segment run through the CH-24 static-zquote/dynamic-verbatim split (dynamic self-reference verbatim, static segment zquote'd — NOT spliced raw); `RebuildListFromBase` (deactivate, CH-1c distinct) → CH-12: deactivate's list reversal is a FULL REBUILD-TO-BASE — emit `PATH="$ZP_BASE_PATH"; path=("${(@s.:.)ZP_BASE_PATH}")` (or equivalent that restores the list from the captured base only), with NO per-element `$target` comparison and NO re-adding of this profile's additions. In single-active v2.0 there is no `$target` element to selectively remove at deactivate — a profile's additions are dropped simply by rebuilding to base, so the per-element quoted-RHS removal loop is NOT emitted here (it would be dead code — CH-12). Do NOT emit a `for e in $path; do [[ $e == "$target" ]] ...` loop in `RebuildListFromBase`. CH-11: DOCUMENT the quoted-RHS `[[ $e == "$target" ]]` literal-equality form (C25 PROVEN) in an emit.go code comment as the CORRECT element-removal form RESERVED for when per-element removal is genuinely needed (future `ListDelta.Deletions` / multi-managed co-ownership) — currently unexercised, like `ListDelta.Deletions`. State in the comment that quoting the RHS is a GLOB_SUBST/robustness guard for that reserved path (with a runtime-variable `$target` and GLOB_SUBST off — zsh default — quoted and unquoted behave alike; quoting hardens against GLOB_SUBST-on and glob metacharacters), NOT the sole barrier and NOT the deactivate hot path. NEVER emit an unquoted `== $target` and NEVER `${path:#pattern}` (C10 glob) anywhere. `SetOption` (activate) → CH-17: FIRST validate `Name` against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` (a package-level `regexp.MustCompile` or an inline byte scan); if it fails, emit NO part for this op (drop-to-no-part — the option name flows UNQUOTED into `[[ -o optname ]]`/`setopt`/`unsetopt`/the was_on slot name, so a non-conforming name is a double-eval injection vector; the builder already drops these in 04-01 but emit re-validates for defense in depth). For a valid name → CH-9: BEFORE the `setopt`/`unsetopt`, capture the LIVE option state into a per-switch was_on runtime slot, `${+slot}`-guarded idempotent (mirrors the SetScalar/AddAlias capture rule so a double-checkout does not clobber the clean-base prior), using the zsh live-option test `[[ -o optname ]]` (records on/off as the captured was_on); THEN emit `setopt optname` if `Enabled` else `unsetopt optname`. This is the apply-side capture that populates the slot `RestoreOption` reads at deactivate — without it, `RestoreOption` reads an unwritten slot and option restore silently fails (zero-residue violation on the options class); `RestoreScalar`/`UnsetScalar` → drift-guarded `${(P)+var}==1` (var holds the name, C2/C21) restore-only-if-live==applied; `Unalias`/`UnsetFunc` → CH-19: FIRST validate `Name` against `aliasFuncNameRe`; on failure emit NO part. For a valid name → `unalias`/`unset -f` the added names; `RestoreShadowedAlias`/`RestoreShadowedFunc` → CH-19: FIRST validate `Name` against `aliasFuncNameRe`; on failure emit NO part. For a valid name → guarded by `${+name}` set-tests (NOT `-n`, NOT `${(P)+literalName}` — OQ-8/C1), restoring the captured prior (alias via `alias name=$prior`, function via `functions[name]=$prior`); `RestoreOption` → CH-7: read the RUNTIME-captured `was_on` slot (captured live at apply time by the CH-9 SetOption rule, like scalars/shadows — the op carries no authoritative WasOn), then `setopt`/`unsetopt` to that captured value, not a blind toggle. CH-3: at the END of the emitted deactivate, UNSET every per-switch undo slot this switch created (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels, the captured `was_on` slot) — but do NOT unset `ZP_BASE_PATH` (session-persistent, OQ-3/OQ-14). Apply-side capture must be idempotent so a double-checkout (apply over already-applied state) does not clobber the clean-base prior (`${+slot}`-guarded — only capture when the slot is not already set). OQ-18: emit BARE `zp_*` helper CALLS; the property test (Task 2) supplies the helper defs inline (drop the Hybrid preamble). In `core/shell/provider.go`, add an `Emitter` interface alongside `Regenerator`, doc-commented as the SOLE reverse-syntax codegen seam (mirror the Regenerator doc language); it takes a `core/activate.Plan`. Keep ISP: whether `Emitter` joins the composite `Provider` or stands alone is discretion — resolve import placement so no cycle forms (`shell` importing `core/activate`, an agnostic value package, is acceptable). In `core/cmd/zsh-pro/main.go`, wire `zsh.Provider{}` as the `shell.Emitter` exactly like the Regenerator; the downstream consumer (loader/CLI verbs) is Phase 5, so construct + make injectable now but leave un-driven (mirror the store's `_, _ = s, err` non-fatal comment). Add `invariant_test.go`: a tree grep asserting reverse zsh tokens appear only under `core/shell/zsh/emit.go` (+ its `_test.go`).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go build ./... && GOTOOLCHAIN=auto go test ./core/shell/... -run 'Emit|Inject|Dynamic|Plain|Guard|Slot|Invariant|Zshn|Shadow|Capture|PathLiteral|Cleanup|Render' 2>&1 | tail -10; test ${PIPESTATUS[1]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-01-06 (shell injection via emitted eval'd code — THE phase risk): the injection test proves every static value site is zquote'd inert; the static-metacharacter-always-quoted test guards misclassification (OQ-6); the slot-name injection corpus proves the name→slot path cannot inject (MEDIUM). Threat T-04-06 (portability freeze / under-quote): the Dynamic-keyed split keeps `$HOME` late-bound yet a static metacharacter value always quoted. Threat T-04-19 (PATH addition-segment injection, CH-24): renderList runs each addition segment through the same static-zquote/dynamic-verbatim split — a static metacharacter/command-sub segment is zquote'd inert (no canary through the ApplyListDelta double-eval) while a dynamic self-reference stays verbatim (late-bound); the 04-01 builder drops an unsafe static segment to no-part upstream; C32 (pass-5) proves inertness. Completes the uniform injection boundary (values C5/C6/C7, names C24/C29/C31, segments C32). Threat T-04-07 (invalid typeset target / slot-name injection): OQ-10 sanitization/assoc-array keeps slot names valid+safe. Threat T-04-18 (alias/function-name injection, CH-19): emit.go re-validates each alias/func NAME against `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting it unquoted into unalias/alias=/functions[]=/unset -f/RestoreShadowed* (defense in depth over the 04-01 builder drop) — the adversarial NAME corpus through those paths fires no canary; C31 (PROVEN pass-4) proves inertness. Threat T-04-08 (silent shadow-restore no-op): `${+name}` guards (OQ-8) replace the buggy `-n`/`${(P)+literal}`; CH-4 capture-before-overwrite ensures a prior EXISTS to restore. Threat T-04-13 (glob over-match on PATH removal, CH-11/CH-12): NOT on the deactivate hot path — deactivate is full rebuild-to-base with no per-element removal, so a profile-added metacharacter element is dropped by base-restore (CH-12d), not by a quoted comparison. The quoted-RHS `[[ $e == "$target" ]]` form (C25 PROVEN) is the reserved-for-future element-removal form (quoting is a GLOB_SUBST/robustness guard there); `${path:#pattern}`/unquoted `== $target` are never emitted. Threat T-04-09 (CH-9 option residue): the SetOption apply captures the live option before toggling so RestoreOption can restore it exactly. Threat T-04-03 (single-emit-path): invariant_test.go confines reverse tokens to emit.go. All emitted code is `zsh -n`-validated (C18).
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go build ./...` succeeds; `GOTOOLCHAIN=auto go test ./core/shell/...` (the Emit subset) exits 0
    - emitted blocks are `zsh -n`-valid: the emit_test asserts `zsh -n` exit 0 on both apply and deactivate (skip-guarded on LookPath zsh)
    - PLAIN functions: `grep -c 'emulate -L' core/shell/zsh/emit.go` returns 0 AND `grep -c 'LOCAL_OPTIONS' core/shell/zsh/emit.go` returns 0
    - `${+name}` shadow guards, no buggy forms: `grep -c '${+' core/shell/zsh/emit.go` >= 1 AND the emitted deactivate string contains no `${(P)+ZP_` literal-name form and no `[[ -n ` shadow-restore guard (asserted in emit_test)
    - CH-11/CH-12 element-removal form documented-but-unexercised: `grep -c ':#' core/shell/zsh/emit.go` returns 0 AND emit.go contains no ACTIVE (non-comment) unquoted `== $target` AND no active element-removal loop in `RebuildListFromBase` (deactivate is full rebuild-to-base); the reserved quoted `== "$target"` form appears only in a comment / reserved helper (documented for future `ListDelta.Deletions` / multi-managed) — asserted in emit_test
    - CH-4 shadow-prior capture-before-overwrite: emit_test asserts the emitted apply for AddAlias/AddFunc contains a `${+`-guarded capture of the prior BEFORE the overwrite, and a functional test asserts deactivate restores the captured pre-existing body byte-identically; a second apply does not clobber the captured prior (idempotent)
    - CH-3 slot cleanup: emit_test asserts the emitted deactivate `unset`s its per-switch slots (`__ZP_ORIG_`/`ZP_..._PRIOR_`/sentinel patterns) and does NOT unset `ZP_BASE_PATH`
    - CH-5 injectable rendering: `grep -E 'var renderList' core/shell/zsh/emit.go` AND `grep -E 'var renderValue' core/shell/zsh/emit.go` both match; a test substitutes them and observes the change in Emit output
    - CH-24 PATH addition-segment split: `renderList` runs EACH addition segment through the static-zquote/dynamic-verbatim discipline (not raw splicing) — emit_test asserts a DYNAMIC `$HOME/bin` addition segment emits VERBATIM (unquoted in `path=(...)`, expands late-bound) while a STATIC segment (`/opt/tool*` and a metacharacter/command-sub segment) emits zquote'd inert; sourcing the emitted apply for a static metacharacter/command-sub addition segment fires NO canary under `zsh -f`; the PATH addition segment is listed in the T-01-06 trust-boundary enumeration
    - CH-9 option live-capture on apply: `grep -c '\[\[ -o ' core/shell/zsh/emit.go` >= 1 (the emitted SetOption apply captures the live option via `[[ -o optname ]]` BEFORE toggling, `${+slot}`-guarded); a functional test asserts the three cases (live-ON→profile-OFF→restored-ON; live-OFF→profile-ON→restored-OFF; live-ON→profile-ON→restored-ON) round-trip via the captured slot, and a double-apply does not clobber the captured was_on
    - CH-7 option restore reads runtime slot: the emitted RestoreOption reads a captured `was_on` runtime slot (not a Go-side WasOn) — emit_test asserts the emitted deactivate references the captured option slot (the slot the CH-9 apply rule populated)
    - CH-17 option-name validation: emit.go validates `OptionSet.Name` against `^[A-Za-z_][A-Za-z0-9_]*$` before emitting it into any command position — `grep -E 'A-Za-z_|MustCompile' core/shell/zsh/emit.go` shows the identifier check; the adversarial NAME corpus through the SetOption emit path (`foo; touch $CANARY`, `x$(...)y`, `foo -o bar`) fires NO canary (rejected or inert) while `extendedglob` still round-trips; the option name is listed in the T-01-06 trust-boundary enumeration
    - CH-19 alias/func-name validation: emit.go validates each alias/function `Name` against `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting it into any command position — `grep -E 'A-Za-z0-9_\]\[A-Za-z0-9_\.-|aliasFuncNameRe' core/shell/zsh/emit.go` shows the name grammar; the adversarial NAME corpus (`gs;touch $CANARY`, `x$(...)y`, backtick, `a|b`, `a b`, `a\nb`, backslash forms `gs\;x`) run through EACH of the AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowedAlias/RestoreShadowedFunc emit paths fires NO canary (rejected or inert) while realistic names (`gs`, `ll`, `git-foo`, `_helper`, `foo.bar`) still emit and round-trip; the alias/func name is listed in the T-01-06 trust-boundary enumeration
    - single-emit-path invariant: `invariant_test.go` passes — reverse tokens (`\bunalias\b`, `\bunset -f\b`, `\bunsetopt\b`, PATH-array rebuild) generated only under core/shell/zsh/emit.go (+ its _test.go)
    - `shell.Emitter` interface exists: `grep -E 'Emitter interface' core/shell/provider.go` matches; main.go wires it (`grep -c 'zsh.Provider{}' core/cmd/zsh-pro/main.go` >= 1)
  </acceptance_criteria>
  <done>emit.go renders the Plan to injection-safe (zquote static / verbatim dynamic), drift-guarded (${(P)+var}==1), ownership-aware (deactivate = full rebuild-to-base, CH-11/CH-12; the quoted-RHS `[[ $e == "$target" ]]` element removal is the reserved/unexercised form documented in a comment for future `ListDelta.Deletions`/multi-managed, NOT the deactivate hot path), `${+name}`-guarded with capture-before-overwrite (CH-4), SetOption apply-side live-option capture with the option NAME validated against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` (CH-9/CH-17) and the alias/function NAMES re-validated against the safe grammar `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting into unalias/alias=/functions[]=/unset -f/RestoreShadowed* (CH-19, defense in depth), self-cleaning per-switch slots (CH-3), runtime-captured option restore (CH-7), PLAIN loader functions that pass `zsh -n`; rendering factored into injectable renderList/renderValue vars (CH-5); the Emitter seam is wired at the composition root; reverse tokens live only in emit.go.</done>
</task>

<task type="auto">
  <name>Task 2: zero-residue property test (N>=20, full-env snapshot, two independent mutants + meta-assert) — the SW-02 pin</name>
  <files>core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/store/roundtrip_test.go (the LookPath-guarded, zsh-requiring, external-test-package full-cycle round-trip precedent + the roundTripSrc fixture style)
    - core/shell/zsh/introspect_test.go (the LookPath skip-guard shape; the extended introspectScript body sections)
    - core/shell/zsh/emit.go (Task 1 output — Emit + the injectable renderList/renderValue vars the mutants substitute, CH-5; bare zp_* calls needing inline helper defs)
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §6 (harness structure, single-zsh-f-process snapshot Pitfall 5/POC-Z9a, mutated-emitter POC-Z6, six-class-full-fidelity C19, fixtures)
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C19 (full-env requirement), C25 (PROVEN — metacharacter PATH element handling), C27 (PROVEN — option capture/restore; the option-drift fixture is its runtime backstop), C28 (PROVEN — self-stable full-env snapshot via the type-class filter; the self-stability + leak meta-tests are its backstop), C30 (PROVEN — the dedicated filter-independent option-state + alias-body snapshot sections are self-stable and their meta-asserts fire on injected option-drift / alias-body-change; no re-POC)
    - .planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md OQ-13 (base-ownership, not multi-managed), OQ-14 (slot cleanup / ZP_BASE_PATH present-in-both)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"Property test"
  </read_first>
  <behavior>
    - Full-env snapshot meta-assert (CH-2 — Addresses review concern: CH-2): the snapshot instrument iterates ALL `${(@kv)parameters}` (exported AND non-exported), emitting name+VALUE with NUL framing (NOT the exported-only names `##ENV##` section). Meta-test: the instrument's diff FIRES on (i) an injected non-exported var leak (`typeset localonly=x`) and (ii) an exported-var value-only change (`export EXISTING=changed`) — proving it would catch what the old names-only instrument missed.
    - Full-env snapshot SELF-STABILITY (CH-10 — Addresses review concern: CH-10, C28 correction (type-class filter)): the instrument is NOT self-stable naively — `${(@kv)parameters}` includes volatile specials AND ~40 module-backed associations / funcstack-family / lazily-instantiated specials. A NAMED-scalar volatile-param exclusion allowlist (`SECONDS RANDOM LINENO funcstack pipestatus _ ...`) is INSUFFICIENT (C28 REFUTED it): two no-op snapshots still drift on unlisted `undefined`-typed module associations (`aliases/builtins/functions/history/options/termcap/...`), the funcstack-family (`funcfiletrace/argv/parameters`), and lazily-instantiated specials (`LOGCHECK/WATCHFMT/zle_bracketed_paste`) that the first snapshot's reads materialize — a name-list cannot cover a whole class. Instead use a TYPE-CLASS filter (C28 correction, PROVEN self-stable 5/5): the VALUE side of `${(@kv)parameters}` IS the param's type string (read for FREE), so SKIP any key whose type matches `(*special*|*tied*|*hide*|undefined)` and keep only concrete user-scope types (`scalar`/`scalar-export`/`array`/`association`/`integer`/`float`). Never call `${(P)k}` on a filtered key (the type alone decides), so the instrument never materializes a lazy special — killing that drift class at the root. Reserve a `ZP__` prefix for the instrument's own capture-target locals so they are excluded too. Capture each snapshot by REDIRECTING to a temp file / dedicated fd — NOT `$(...)` command-substitution, which itself forks and perturbs params (RETAINED — that requirement was correct). Self-stability meta-test: two consecutive no-op snapshots (no apply between them) produce a BYTE-IDENTICAL empty diff (proving the type-class filter + fd-capture neutralize volatility). The self-stability meta-test is asserted alongside the leak meta-test (i)/(ii) — the type-class filter must not be so broad it also masks the leak/value-change cases (both meta-tests must pass with the SAME filter).
    - Base-ownership sub-check (CH-1a/b + CH-15 — Addresses review concern: CH-1a, CH-1b, CH-15): base owns `/usr/local/bin` (it is in `ZP_BASE_PATH`); a profile ALSO adds `/usr/local/bin`; after apply→deactivate the sub-check asserts (STRENGTHENED per CH-15, not a tautology): (a) `$#path` is BYTE-IDENTICAL to the baseline count (guards against both a stripped base entry AND a duplicate), AND (b) `/usr/local/bin` appears EXACTLY ONCE in `$path` (guards both strip and duplicate) — NOT merely "it is still present" (which is trivially true for any base entry regardless of whether the profile's addition was mishandled). Negative control (CH-15): a mutant emitter that STRIPS base entries (e.g. renderList that omits/over-removes base) MUST fail this sub-check — assert the mutant produces a non-baseline `$#path` or a missing `/usr/local/bin`. A test comment documents multi-managed co-ownership as OUT OF SCOPE for single-active v2.0 (OQ-13).
    - Two independent mutants + per-mutant assertion (CH-5 — Addresses review concern: CH-5): (1) substitute `renderList` with a blind-append variant (`path=(<add> $path)` with no rebuild-from-base) → the property snapshot diff is NON-empty / `$#path` grows; (2) substitute `renderValue` to DROP zquote on a static metacharacter value (`x; touch $CANARY`) → the canary FIRES / the value is not literal. Each mutant is asserted INDEPENDENTLY (its own sub-test) to produce a non-empty residue diff / fired canary, verified separately from the happy path (meta-test that the pin actually detects residue).
    - PATH metacharacter element (CH-12d — Addresses review concern: CH-12): the fixtures include a PROFILE-ADDED PATH element containing a `*`/`?` metacharacter (`/opt/tool*`) that is ABSENT from `ZP_BASE_PATH`; the residue sub-check asserts that after apply→deactivate the rebuild-TO-base DROPS `/opt/tool*` (it is a profile addition, not in base) — which is exactly what full rebuild-to-base does. This is the correct assertion for the single-active deactivate path (a metacharacter element that IS a profile addition is dropped by base-restore), NOT a "quoted comparison spared it" assertion (there is no per-element comparison on the deactivate path — CH-12). The metacharacter element also proves rebuild-to-base does not choke on glob chars during the array rebuild.
    - PATH addition-segment injection sub-test (CH-24 — Addresses review concern: CH-24): in its OWN isolated single-sourced `zsh -f` baseline (OQ-26), drive the ApplyListDelta emit path with (a) a STATIC metacharacter/command-sub addition segment (`x; touch $CANARY`, `/opt/x$(touch $CANARY)`, a backtick-command segment) and (b) a DYNAMIC self-reference segment (`$HOME/bin`). Assert: sourcing the emitted apply for the STATIC segment fires NO canary (the segment is zquote'd inert in the `path=(<additions> $path)` array — the emit-side C32 enforcement even if the 04-01 builder had not already dropped it), AND the DYNAMIC `$HOME/bin` segment expands correctly (the resulting `$path` contains the `$HOME`-derived directory, late-bound, NOT the literal string `$HOME/bin`). This closes the addition-segment injection surface end-to-end (C32) and, with the value (C5/C6/C7) and name (C24/C29/C31) tests, makes the injection boundary UNIFORM/complete.
    - Dedicated filter-INDEPENDENT option-state + alias-body snapshot sections (CH-16 — Addresses review concern: CH-16): on zsh 5.9 the `aliases` and `options` module-backed associations are typed `association-hide-hideval-special`, so the C28 TYPE-CLASS filter (skip `(*special*|*tied*|*hide*|undefined)`) EXCLUDES them from the generic `${(@kv)parameters}` full-env section — a leaked alias or an unrestored option would pass byte-identical (a C19-class false-green on 2 of the 6 residue classes, contradicting SPEC Req 5). So the snapshot instrument adds two DEDICATED, filter-independent sections captured IDENTICALLY in BOTH the pre and post snapshots (same pattern as the existing separate PATH and function-body sections): (a) an OPTION-STATE section iterating SORTED `${(@ok)options}` (the `o` sort flag — CH-20, matching PROVEN C30 which used `${(ok)...}`) emitting `optname=<on|off>` (via `[[ -o $opt ]]` or `${options[$opt]}`); (b) an ALIAS-BODY section iterating SORTED `${(@ok)aliases}` emitting `name<NUL>body` (like the function-body section). Both sections MUST use the `o` sort flag `${(@ok)...}` — NOT bare `${(@k)}` which returns non-deterministic hash order across insert/remove between snapshots → false-red or masked residue (CH-20); they iterate the same `undefined`/`*special*`-typed assocs C28 flagged as drift-prone, so ordering must be pinned — and must NOT trigger lazy-init drift. C30 meta-asserts (each must independently fire): an injected OPTION-DRIFT (an option left toggled between two snapshots) makes the option-state diff non-empty; an injected ALIAS-BODY-CHANGE makes the alias-body diff non-empty. Add `zmodload zsh/parameter` to the harness preamble (mirroring introspect.go) and assert a KNOWN USER SCALAR appears in the baseline full-env snapshot (proves the `parameters` map is populated, not silently empty — a silently-empty map would be an all-classes false-green). One-line note (OQ-26): PATH / options / alias-bodies / function-bodies get dedicated sections BECAUSE their backing params are `*special*`/`*tied*` and thus excluded by the type-class filter. [Addresses review concern: CH-16]
    - Option drift/restore fixture (CH-9 — Addresses review concern: CH-9): a shell OPTION (e.g. `extendedglob`) is live-ON in the baseline BEFORE apply, and a profile sets it differently (`unsetopt extendedglob`); after apply→deactivate the option is asserted restored to its EXACT prior live state (ON) — proving the CH-9 apply-side live-capture populated the was_on slot that RestoreOption reads. The option state is covered by the DEDICATED, filter-independent option-state section (CH-16) — NOT the `${(@kv)parameters}` full-env section (the `options` assoc is `*special*`-typed and excluded by the type-class filter) — so a failed option restore surfaces as residue in that dedicated section.
    - Drift sub-check split into two explicit assertions (OQ-26 / SPEC Req 6 — Addresses review concern: MEDIUM drift): assert BOTH (i) a hand-edited managed env var SURVIVES deactivate (the drift guard leaves it intact because live != applied) AND (ii) an UNTOUCHED managed var IS reversed to its prior — two distinct assertions, not one conflated check.
    - Isolated baselines for the targeted sub-checks (OQ-26 — Addresses review concern: MEDIUM baseline isolation): the drift / shadow / base-ownership / metacharacter-PATH / option-drift sub-checks each run in their OWN isolated single-sourced `zsh -f` baseline, DISTINCT from the N-sequence byte-identical property run — so a targeted-assertion failure is never conflated with a sequence-order residue diff.
    - Slot-cleanup consistency (CH-3): the full-env snapshot is taken with `ZP_BASE_PATH` set BEFORE the baseline snapshot (present in both baseline and post, nets to zero) OR excluded via a single audited documented `ZP_BASE_PATH`-only filter (state the choice); per-switch `ZP_`/`__ZP_` slots are unset by deactivate so they never appear as residue.
    - Balanced switch sequences (CH-14 — Addresses review concern: CH-14): the sequence generator models the REAL single-active checkout — every profile apply is matched by its deactivate BEFORE a different profile applies (deactivate-current-then-activate-next), so two applies are NEVER nested without an intervening deactivate. Without this, profile B applied over an already-applied A would capture A's `ll` (not base's) as its shadow prior, and deactivate-B would restore A's `ll` → residue vs base (a harness artifact, not an emit bug). Assert (a meta-check on the generator, independent of zsh): the generated sequence never contains two consecutive applies of different profiles without an intervening deactivate — i.e. at most one profile is "active" at any point in the sequence.
    - Happy-path property (CH-21 + CH-25 — Addresses review concern: CH-21, CH-25): for each of N>=20 random BALANCED switch sequences, the byte-identical pre-vs-post snapshot compared across ALL N sequences is the CONCATENATION of ALL these sections: the type-class-filtered full-env `${(@kv)parameters}` (CH-10), the SORTED function-BODY section (`${(@ok)functions)}` — CH-22), a dedicated PATH section carrying the `$#path` count AND the ordered `$path` array CONTENTS (CH-25 — `${(@)path}` joined element-by-element, NOT just the count; the PATH array is order-significant so it is emitted in `$path` order, not re-sorted), AND the two dedicated SORTED sections — the option-state section (`${(@ok)options}` → `optname=<on|off>`) and the alias-body section (`${(@ok)aliases}` → `name<NUL>body`). This binds the alias/option classes (2 of 6) AND the PATH-content class into the GENERAL zero-residue property under arbitrary switch order — not just the isolated fixture-specific sub-checks/meta-asserts — closing the residual C19-class blind spot for those classes under the general run (SPEC Req 5). A leaked alias body, an unrestored option, OR a count-preserving PATH content corruption (reorder/element swap) now makes the general N-sequence compare non-empty (CH-25 — `$#path` alone would miss a count-preserving reorder).
  </behavior>
  <action>
    Create `core/shell/zsh/residue_test.go` (default suite, NOT behind a `spike` tag — D-17; `exec.LookPath("zsh")` skip-guard like introspect_test.go). Build >=2 profiles exercising all six classes with: at least one shadow collision (both profiles override the same pre-existing `ll` alias and `ff` function — exercising CH-4 capture/restore), one BASE-owned shared PATH addition (`/usr/local/bin` in `ZP_BASE_PATH` AND added by a profile — the CH-1a/b base-ownership criterion, NOT two active managed profiles), one PROFILE-ADDED PATH element containing a metacharacter (`/opt/tool*`, ABSENT from `ZP_BASE_PATH` — CH-12d: rebuild-to-base must DROP it on deactivate), one dynamic value (`export GOPATH=$HOME/go`), and one OPTION live-ON in the baseline that a profile sets differently (`extendedglob` ON before apply, profile `unsetopt extendedglob` — CH-9: must be restored ON after deactivate via the captured was_on slot). CH-14: the sequence GENERATOR produces only BALANCED sequences — model single-active checkout as deactivate-current-then-activate-next, so at most one profile is active at any point and two applies are never nested without an intervening deactivate; add a generator meta-check (pure Go, no zsh) asserting this invariant on every generated sequence. For each of N>=20 random balanced switch sequences: `activate.Build` → Manifest → `activate.Diff` → Plan → `Provider{}.Emit` → apply/deactivate strings. Run the WHOLE sequence in ONE sourced `zsh -f` script (Pitfall 5 — `zsh -f` inherits parent env, so cross-process snapshots give false diffs): set `ZP_BASE_PATH` (containing `/usr/local/bin` — the base-owned shared entry; NOT `/opt/tool*`, which is a profile-only addition absent from base per CH-12d) BEFORE the baseline snapshot (CH-3 — so `ZP_BASE_PATH` nets to zero, or use the single documented filter), `setopt extendedglob` (the CH-9 option live-ON in the baseline) BEFORE the baseline snapshot, define the pre-existing `ll`/`ff` + the `zp_*` helper defs inline (emit.go emits bare calls per OQ-18), snapshot, source each emitted apply/deactivate in random order, snapshot again. Add `zmodload zsh/parameter` to the harness preamble (mirror introspect.go) so the `parameters`/`options`/`aliases` associations are populated, and assert a KNOWN USER SCALAR (e.g. a fixture-set `export MARKER_VAR=present`) appears in the baseline full-env snapshot (CH-16 populated-map guard — a silently-empty `parameters` map would be an all-classes false-green). CH-2: the snapshot instrument is a NEW full-env section iterating ALL `${(@kv)parameters}` (exported AND non-exported) emitting name+VALUE with NUL framing, PLUS function BODIES reusing the extended introspect body sections — which the 04-01 body-dump now iterates SORTED via `${(@ok)functions}` (CH-22 — the reused FUNCTIONBODIES section is deterministically ordered so it is byte-stable across snapshots; `functions` is the same drift-prone `*special*` class), PLUS a dedicated PATH section carrying BOTH the `$#path` count AND the ordered `$path` array CONTENTS (CH-25 — Addresses review concern: CH-25: concatenate the `$path` array elements, in order, into the general N-sequence byte-identical compare STRING, not only `$#path`; PATH is order-significant so emit the elements in `$path` order, e.g. one element per line or NUL-framed — do NOT re-sort it; keep `$#path` as a corroborating check. Without the array contents a count-preserving PATH corruption — a reorder or element swap that keeps `$#path` — would pass the general run) — do NOT reuse the exported-only names `##ENV##` section (C19/CH-2). CH-22: include the SORTED function-body section in the C30-analog self-stability assertion (two no-op snapshots byte-identical for the function-body section too, not only the option/alias sections). CH-16 (Addresses review concern: CH-16): because the `aliases` and `options` associations are `association-*-special`-typed on zsh 5.9, the C28 type-class filter EXCLUDES them from the `${(@kv)parameters)}` section — so add TWO DEDICATED, filter-independent sections captured identically in BOTH snapshots (same pattern as PATH + function-bodies): (a) an OPTION-STATE section iterating SORTED `${(@ok)options}` (CH-20 — the `o` sort flag, matching PROVEN C30 which used `${(ok)options}`; NOT bare `${(@k)}` hash order) → `optname=<on|off>` (via `[[ -o $opt ]]` or `${options[$opt]}`); (b) an ALIAS-BODY section iterating SORTED `${(@ok)aliases}` (CH-20 — the `o` sort flag, matching PROVEN C30's `${(ok)aliases}`) → `name<NUL>body` (like function bodies). Use the `o` sort flag on both so the section is byte-stable across snapshots (bare `${(@k)}` returns non-deterministic hash order → false-red or masked residue — CH-20); do NOT trigger lazy-init drift. Add C30 meta-asserts: an injected option-drift (an option left toggled) makes the option-state section diff non-empty, AND an injected alias-body-change makes the alias-body section diff non-empty — each fires independently (C30 PROVEN per 04-EVIDENCE.md — self-stability + firing meta-asserts confirmed; no re-POC). Add a one-line comment (OQ-26) that PATH/options/alias-bodies/function-bodies get dedicated sections BECAUSE their backing params are `*special*`/`*tied*` (excluded by the type-class filter). CH-10 (C28 correction (type-class filter)): make the instrument SELF-STABLE via a TYPE-CLASS filter, NOT a named allowlist (the named-scalar allowlist `SECONDS RANDOM LINENO funcstack functrace pipestatus _ HISTCMD ... ZSH_EVAL_CONTEXT` was REFUTED by C28 — it drifts on ~40 unlisted `undefined`-typed module associations, the funcstack-family, and lazily-materialized specials; a name-list cannot cover a whole class). (a) In the snapshot loop over `${(@kv)parameters}`, the map's VALUE side IS the param's type string — read it for FREE and SKIP any key whose type matches the pattern `(*special*|*tied*|*hide*|undefined)` (`[[ $type == (*special*|*tied*|*hide*|undefined) ]] && continue`), keeping only concrete user-scope types (`scalar`/`scalar-export`/`array`/`association`/`integer`/`float`). Decide the skip from the TYPE alone — NEVER call `${(P)k}` on a filtered key — so the instrument never reads (and thus never materializes) a lazily-instantiated special, killing that drift class at the root. Also reserve a `ZP__` prefix for the instrument's own capture-target locals (aligning with the emit.go `ZP_`/`__ZP_` slot convention) so they never self-register; document WHY the type-class filter is used in a comment. (b) capture each snapshot by REDIRECTING the emitting loop's output to a temp file / dedicated fd (`> $snapfile` or `{...} >&N`), NOT `$(...)` command-substitution (which forks and perturbs `_`/pipestatus/funcstack, making the instrument perturb what it measures) — RETAINED, that requirement was correct. Assert a LITERAL empty diff of pre-vs-post snapshot strings and `$#path` stable. Add a self-stability meta-test: two consecutive no-op snapshots (no apply between) diff to EMPTY under the same type-class filter that still lets the leak/value-change meta-tests fire. Honor `testing.Short()` by reducing N but KEEPING both mutants. Mutants (CH-5 + CH-15, first-class, each independently asserted): save/substitute/restore the package-level `renderList`/`renderValue` vars from Task 1 — mutant (1) blind-append PATH (swap renderList's rebuild-from-base for `path=(<add> $path)`) MUST produce a non-empty diff / grown `$#path`; mutant (2) drop-zquote (swap renderValue to emit a static metacharacter value verbatim) MUST fire the canary / leave a non-literal value; mutant (3, CH-15 base-strip negative control) swap renderList for a variant that omits/over-removes base entries MUST fail the strengthened base-ownership sub-check (non-baseline `$#path` or missing `/usr/local/bin`) — assert EACH in its own sub-test, independent of the happy path. Add a meta-assert sub-test proving the full-env instrument itself catches (i) a non-exported var leak and (ii) an exported value-only change (CH-2). Each targeted sub-check below runs in its OWN isolated single-sourced `zsh -f` baseline (OQ-26), DISTINCT from the N-sequence byte-identical property run, so a targeted-assertion failure is never conflated with a sequence-order residue. Ensure the `ZP_BASE_PATH` `${(@s.:.)ZP_BASE_PATH}` split DROPS empty fields (or use a no-trailing-colon `ZP_BASE_PATH` fixture) so `$#path` is deterministic (OQ-26 — a trailing colon yields a phantom empty element). Sub-checks: ownership (CH-1a/b + CH-15 base-ownership, STRENGTHENED: after apply→deactivate assert `$#path` byte-identical to baseline AND `/usr/local/bin` appears EXACTLY once — guards both strip and duplicate, not merely "still present"; a THIRD negative-control mutant that strips base entries MUST fail this sub-check; a comment marks multi-managed OUT OF SCOPE per OQ-13); drift — SPLIT into TWO explicit assertions (OQ-26 / SPEC Req 6): (i) a hand-edited managed env var SURVIVES deactivate (live != applied → left intact) AND (ii) an untouched managed var IS reversed to its prior; shadow (the shadowed `ll`/`ff` restored byte-for-byte via the CH-4-captured prior; profile-added names gone); option-drift (CH-9: an option live-ON before apply that a profile turns OFF is restored ON via the captured was_on slot, verified through the CH-16 dedicated option-state section); metacharacter-PATH (CH-12d: the profile-added `/opt/tool*`, absent from base, is DROPPED by rebuild-to-base on deactivate; no per-element comparison on this path). CH-23 additions: (a) after the full N-sequence run, assert `ZP_BASE_PATH` still equals its baseline value (session-persistent invariant — deactivate cleans per-switch slots but NEVER unsets `ZP_BASE_PATH`, OQ-3/OQ-14); (b) add a one-line comment that `PATH`/`path` are `*tied*`/`*special*`-typed and thus EXCLUDED by the type-class filter from the generic `${(@kv)parameters}` section — they are covered by the dedicated PATH section (`$path` array + `$#path`), same rationale as options/aliases/function-bodies (OQ-26); (c) NOTE (deferred): the emitted `${(@s.:.)ZP_BASE_PATH}` empty-field-strip concern is a Phase-5 base-capture matter (OQ-3) — this test uses a no-trailing-colon `ZP_BASE_PATH` fixture so `$#path` is deterministic here; (d) OPTIONAL parity: the dedicated PATH section MAY also snapshot `$fpath` + `$#fpath` (renderList is name-parameterized, so FPATH residue is covered by the same mechanism) — include if cheap, since FPATH is an admitted list class.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go test ./core/shell/zsh/ -run 'Residue|ZeroResidue|Ownership|Drift|Shadow|Mutated|Mutant|FullEnv|MetaAssert' -count=1 2>&1 | tail -12; test ${PIPESTATUS[0]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-04-09 (residue accumulation escaping the test): the property test snapshots function BODIES + FULL env (all `${(@kv)parameters}`, exported AND non-exported, name+VALUE) + `$#path` (C19/CH-2), so body-level and non-exported/value-only residue cannot pass silently — the meta-assert proves the instrument catches both. The two independent mutants prove the test actually DETECTS residue: blind-append PATH flips it (SW-02 PATH acceptance) and dropped-zquote fires the canary (SW-02 injection acceptance) — asserted separately from the happy path (CH-5). Threat T-04-13 (PATH element handling, CH-12): the profile-added metacharacter element `/opt/tool*` (absent from base) is DROPPED by rebuild-to-base on deactivate — the correct single-active behavior, with no per-element comparison (the C10 glob hazard is not on this path). Threat T-04-09 (option residue, CH-9): the option-drift fixture proves an option live-ON before apply that a profile changes is restored exactly via the captured was_on slot. Ties to T-01-06 (owner-trust): the dynamic `$HOME/go` exercises the verbatim path end-to-end without executing an injection.
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go test ./core/shell/zsh/ -run Residue -count=1` exits 0 when zsh is present (skips cleanly when absent)
    - N>=20: the test drives >=20 random switch sequences (grep the test source for the N constant >= 20)
    - CH-14 balanced sequences: the generator meta-check (pure Go) asserts every generated sequence has at most one active profile at any point — two applies of different profiles never nest without an intervening deactivate; the sub-test proving this invariant passes
    - CH-2 full-env instrument: `grep -c '(@kv)parameters' core/shell/zsh/residue_test.go` >= 1 (iterates ALL params, not the exported-only `##ENV##` names section); the meta-assert sub-test (non-exported leak + exported value-only change fire the diff) passes
    - CH-10 self-stable instrument (C28 correction (type-class filter)): the residue_test filters volatile params by TYPE CLASS, NOT a named allowlist — grep the test source for the type-class predicate (e.g. the `special`/`undefined` type pattern, `(*special*|*tied*|*hide*|undefined)`) rather than for literal names like `SECONDS`/`RANDOM` (which need NOT appear under the type-class filter) — AND captures snapshots via a temp file / fd redirect, NOT `$(...)`; the self-stability meta-test (two consecutive no-op snapshots → empty diff) passes with the SAME type-class filter that still lets the leak/value-change meta-tests fire
    - CH-5 + CH-15 three independent mutants: the sub-test swapping `renderList` for blind-append asserts a non-empty diff / grown `$#path`; a SECOND sub-test swapping `renderValue` to drop zquote asserts a fired canary / non-literal value; a THIRD (CH-15 negative control) swapping `renderList` for a base-stripping variant asserts the base-ownership sub-check FAILS (non-baseline `$#path` or missing `/usr/local/bin`); each mutant's failure is asserted in its own sub-test independent of the happy path
    - CH-1a/b + CH-15 base-ownership (strengthened, not a tautology): the ownership sub-check asserts, after apply→deactivate, `$#path` is byte-identical to baseline AND `/usr/local/bin` (in ZP_BASE_PATH, also profile-added) appears EXACTLY once (guards strip AND duplicate); a negative-control mutant that strips base entries FAILS this sub-check; a comment marks multi-managed co-ownership out of scope (OQ-13)
    - CH-12d metacharacter element: a profile-added `/opt/tool*`-style PATH element (absent from `ZP_BASE_PATH`) is present in the fixtures and the test asserts rebuild-to-base DROPS it on deactivate (correct base-restore behavior), not a "quoted comparison spared it" assertion
    - CH-24 addition-segment injection sub-test: a static metacharacter/command-sub addition segment driven through the ApplyListDelta emit path fires NO canary when the emitted apply is sourced under `zsh -f` (zquote'd inert), AND a dynamic `$HOME/bin` addition segment expands late-bound (the resulting `$path` holds the `$HOME`-derived dir, not the literal `$HOME/bin`) — asserted in its own isolated baseline; this pins C32 and completes the uniform injection boundary (values C5/C6/C7, names C24/C29/C31, segments C32)
    - CH-9 option drift/restore: the fixtures include an option live-ON before apply that a profile sets differently; the sub-check asserts it is restored to its exact prior after deactivate (via the CH-9-captured was_on slot), and the option state is captured by the DEDICATED filter-independent option-state section (CH-16), NOT the `${(@kv)parameters}` full-env section
    - CH-16 + CH-20 dedicated filter-independent SORTED sections: the dedicated option-state + alias-body sections use the `o` sort flag (CH-20) — `grep -Ec '\(ok\)options|\(@ok\)options' core/shell/zsh/residue_test.go` >= 1 AND `grep -Ec '\(ok\)aliases|\(@ok\)aliases' core/shell/zsh/residue_test.go` >= 1 (SORTED `${(@ok)options}`/`${(@ok)aliases}`, matching PROVEN C30; NOT bare `${(@k)}` hash order); they are distinct from the filtered `${(@kv)parameters}` section; the C30 meta-asserts pass — an injected option-drift makes the option-state diff non-empty AND an injected alias-body-change makes the alias-body diff non-empty (each fires independently); the harness preamble contains `zmodload zsh/parameter` (`grep -c 'zmodload zsh/parameter' core/shell/zsh/residue_test.go` >= 1) and a baseline assertion that a known user scalar appears in the full-env snapshot (populated-map guard)
    - OQ-26 drift split: the drift sub-check asserts BOTH a hand-edited managed var SURVIVES deactivate AND an untouched managed var IS reversed (two distinct assertions)
    - OQ-26 deterministic `$#path`: the `ZP_BASE_PATH` `${(@s.:.)}` split drops empty fields OR the fixture uses a no-trailing-colon `ZP_BASE_PATH` so `$#path` is deterministic (a trailing colon would yield a phantom empty element and a non-deterministic count)
    - runs in the DEFAULT suite (no `//go:build spike` tag): `grep -c 'go:build spike' core/shell/zsh/residue_test.go` returns 0
    - snapshot captures function BODIES and full env (not names-only): the snapshot reuses the extended introspect body sections AND the full-env `${(@kv)parameters}` section
    - CH-21 + CH-25 general byte-identical compare includes option/alias AND PATH-content classes: the N-sequence byte-identical pre-vs-post compare is the CONCATENATION of the type-class-filtered `${(@kv)parameters}` section + the SORTED function-body section + a dedicated PATH section carrying `$#path` AND the ordered `$path` array CONTENTS (CH-25 — not `$#path` alone) + the dedicated SORTED option-state (`${(@ok)options}`) + alias-body (`${(@ok)aliases}`) sections — the happy-path assertion string includes ALL of these (grep the test: the compare string includes the `$path` array contents, e.g. `${(@)path}`/`$path[@]`, not only `$#path`); a leaked alias body, an unrestored option, OR a count-preserving PATH reorder/swap makes the GENERAL N-sequence compare non-empty (not only an isolated sub-check)
    - CH-22 sorted function-body section + self-stability: the reused FUNCTIONBODIES section iterates SORTED `${(@ok)functions}` (04-01 body-dump is sorted) so it is byte-stable across snapshots, and it is included in the C30-analog self-stability assertion (two no-op snapshots byte-identical for the function-body section)
    - CH-23 ZP_BASE_PATH persistence: a post-sequence assertion confirms `ZP_BASE_PATH` equals its baseline value after the full switch sequence (session-persistent invariant — deactivate never unsets it)
  </acceptance_criteria>
  <done>A default-suite, LookPath-guarded property test drives N>=20 random switch sequences, asserts a byte-identical full-env (all params, name+VALUE, exported AND non-exported) + function-BODY + `$#path` snapshot, includes base-ownership/drift/shadow/metacharacter-PATH sub-checks, a meta-assert proving the full-env instrument catches non-exported+value-only residue (CH-2), and TWO independently-asserted mutants (blind-append PATH; dropped-zquote) that each genuinely fail it (CH-5) — the SW-02 regression pin, composed alongside the testgen oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile value → emitted `eval`'d zsh code | THE phase boundary. A user-controlled env value, alias body, function body, profile/alias NAME (used in an undo-slot), option NAME (emitted unquoted into `[[ -o optname ]]`/`setopt`/`unsetopt`/the was_on slot name — CH-17), alias/function NAME (emitted unquoted into `unalias name`/`alias name=...`/`functions[name]=...`/`unset -f name`/the shadow-prior slot name — CH-19; zsh PERMITS metacharacters in these names, and parse.go extracts them RAW), OR a PATH/FPATH ADDITION SEGMENT (spliced into the eval'd `path=(<additions> $path)` array literal — CH-24; a static segment must be zquote'd inert, a dynamic self-reference stays verbatim for portability) crossing into emitted code that a sourced loader `eval`s. A quoting bug here becomes a shell-injection bug (T-01-06, ROADMAP line 214). The adversarial corpus runs through ALL of: the value path, the slot-NAME derivation path, the SetOption option-name path (CH-17), the AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowed* alias/func-name paths (CH-19), AND the ApplyListDelta addition-SEGMENT path (CH-24). The injection boundary is now UNIFORM/complete: values (C5/C6/C7), names (C24/C29/C31), and addition segments (C32). |
| owner's profile (v2.0) | Same trust boundary as the owner's own `.zshrc` — a verbatim dynamic value executing is equivalent to it being in their `.zshrc`. Shared/imported profiles (SHARE-01) are a future milestone with its own trust gate (residual/deferred). |
| live runtime state → deactivate | Deactivate reads captured undo state (`ZP_BASE_PATH`, `__ZP_ORIG_*`, shadow-prior slots, captured `was_on`); a drifted (hand-edited) live value must NOT be clobbered; per-switch slots are cleaned up, `ZP_BASE_PATH` persists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Elevation of Privilege / Tampering | emitted `eval`'d code (static scalar/alias VALUE contexts) | mitigate | `zquote()` single-quote `'\''` wrapping at EVERY static value site (POC-Inj/Eval); a static value with `'`/`;`/`$(...)`/backtick/newline is always an inert literal (Task 1). The adversarial NAME corpus also runs through the slot-name derivation path (MEDIUM). |
| T-04-06 | Tampering | Dynamic-keyed emit split | mitigate | Split keys strictly on the op's `Dynamic` bool (carried from the IR, never re-derived); a static metacharacter value is ALWAYS quoted regardless of flag path — asserted by a dedicated test (OQ-6). Over-quoting (freezing `$HOME`) and under-quoting (injection) both prevented. The dropped-zquote mutant (Task 2) proves the residue pin detects an under-quote. |
| T-04-07 | Elevation of Privilege | runtime undo-slot naming | mitigate | Slot names sanitized to `[A-Za-z0-9_]` (hash-suffixed) OR a `typeset -gA` assoc-array keyed by raw name (OQ-10/C24); a profile named `feature/x` cannot produce an invalid/injecting `typeset -g` target — the injection corpus through the slot-name path proves no canary fires (MEDIUM). |
| T-04-08 | Tampering | shadow-restore guards + capture | mitigate | `${+name}` set-tests (NOT `-n`, NOT `${(P)+literalName}` — OQ-8/C1); env path uses `${(P)+var}==1` (var holds the name, C2/C21). CH-4: `AddAlias`/`AddFunc` capture the shadowed prior BEFORE overwrite (`${+`-guarded, idempotent), so a prior EXISTS for deactivate to restore. |
| T-04-10 | Tampering | function-body restore | mitigate | Function bodies restored verbatim via `functions[name]=$capturedBody` (C6) — NEVER zquote'd-to-inert (a function body is live code). The captured body is trusted live code (already in the user's env). |
| T-04-13 | Tampering | PATH element removal glob over-match | mitigate (reserved path) / N/A on deactivate hot path | CH-11/CH-12: in single-active v2.0 deactivate is a FULL rebuild-to-base with NO per-element removal, so the C10 glob-over-match hazard is NOT on the deactivate hot path — a profile addition (incl. a metacharacter element like `/opt/tool*`) is dropped simply by rebuilding to base (CH-12d residue sub-check). The quoted-RHS literal-equality `[[ $e == "$target" ]]` form (C25 PROVEN) is documented as the correct RESERVED form for a future per-element removal path (`ListDelta.Deletions` / multi-managed), where quoting is a GLOB_SUBST/robustness guard (not the sole barrier). `${path:#pattern}` (C10 glob) and unquoted `== $target` are NEVER emitted. |
| T-04-14 | Tampering | per-switch undo-slot residue | mitigate | CH-3: emitted deactivate UNSETS its own per-switch slots (`__ZP_ORIG_*`, `ZP_<profile>_PRIOR_*`, sentinels, captured `was_on`); `ZP_BASE_PATH` is session-persistent and preserved. The full-env residue snapshot (CH-2) would otherwise flag the slots — so cleanup is verified, not filtered-away (OQ-14). |
| T-04-03 | Elevation of Privilege | single-emit-path invariant | mitigate | `invariant_test.go` tree-grep: reverse zsh tokens generated only under `emit.go` — all injection surface confined to one auditable file (Task 1). |
| T-04-09 | Tampering | residue accumulation | mitigate | Zero-residue property test snapshots BODIES + FULL env (all params, exported+non-exported, name+VALUE) + `$#path` (C19/CH-2); a meta-assert proves the instrument catches non-exported+value-only residue AND is self-stable (empty diff across two no-op snapshots, CH-10/C28); THREE independent mutants (blind-append PATH; dropped-zquote; base-strip) prove the test detects residue (Task 2, SW-02 pin, CH-2/CH-5/CH-15). |
| T-04-15 | Tampering | option class residue | mitigate | CH-9: `SetOption` apply captures the LIVE option (`[[ -o optname ]]`) into a `${+slot}`-guarded was_on slot BEFORE `setopt`/`unsetopt`, so `RestoreOption` restores the exact prior (setopt/unsetopt on the captured value) — without capture, option restore silently no-ops (zero-residue violation on the options class). The option-drift fixture + the DEDICATED option-state snapshot section (CH-16 — options are `*special*`-typed and excluded by the type-class filter, so they are NOT covered by the generic `${(@kv)parameters}` section) are the runtime backstop. C27 is PROVEN (exact round-trip, 20/20). |
| T-04-17 | Elevation of Privilege | option-name injection (emit side) | mitigate | CH-17: `OptionSet.Name` (user-controlled) is emitted UNQUOTED into `[[ -o optname ]]`/`setopt`/`unsetopt`/the was_on slot name — a double-eval EoP surface. emit.go validates the name against the bare-identifier grammar `^[A-Za-z_][A-Za-z0-9_]*$` before emitting (reject/no-part on failure), re-validating the builder-side drop (04-01 T-04-17) for defense in depth. The adversarial NAME corpus runs through the SetOption path; C29 (PROVEN) proves a hostile name is rejected or inert. Mirrors the C24 slot-name defense. |
| T-04-18 | Elevation of Privilege | alias/function-name injection (emit side) | mitigate | CH-19: the alias/function NAME (`AddAlias`/`AddFunc`/`Unalias`/`UnsetFunc`/`RestoreShadowedAlias`/`RestoreShadowedFunc` `.Name`, RAW user-controlled — zsh PERMITS shell metacharacters in alias/func names, and parse.go retains them RAW e.g. `gs\;x`) is emitted UNQUOTED into `unalias name`/`alias name=...`/`functions[name]=...`/`unset -f name`/the shadow-prior slot name — a double-eval EoP surface (proven end-to-end: `eval "unalias gs;touch $CANARY"` fires the canary). emit.go re-validates each name against the conservative safe grammar `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting (reject/no-part on failure), re-validating the builder-side drop (04-01 T-04-18) for defense in depth. The adversarial NAME corpus runs through ALL of the AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowed* paths; C31 (PROVEN — pass-4) proves a hostile name is rejected or inert. Mirrors the CH-17 option-name / C24 slot-name defense for the two name classes zsh permits metacharacters in. |
| T-04-19 | Elevation of Privilege | PATH addition-segment injection (emit side) | mitigate | CH-24: an `ApplyListDelta.Additions` segment (user-controlled `CatPath` RHS) is spliced into the eval'd `path=(<additions> $path)` array literal — a double-eval EoP surface if raw (`export PATH=/opt/x$(touch $CANARY):$PATH` runs the command-sub on apply). `renderList` runs EACH segment through the SAME static-zquote/dynamic-verbatim discipline as `renderValue`: a DYNAMIC self-reference (`$PATH`/`$path`/`$HOME/bin`) stays verbatim (portability, D-05/D-10), a STATIC segment is zquote'd inert (a static glob `/opt/tool*` zquotes cleanly). The 04-01 builder already drops an unsafe static metacharacter/command-sub segment to no-part (T-04-19 builder side); emit's per-segment split is the sole-codegen-seam enforcement (defense in depth). The adversarial corpus runs through the ApplyListDelta segment path; C32 (UNVERIFIED — pass-5) proves a static segment is inert and a dynamic segment expands late-bound. Completes the uniform injection boundary alongside the value (C5/C6/C7) and name (C24/C29/C31) defenses. |
| T-04-16 | Tampering | self-instrument false-green/false-red | mitigate | CH-10 (C28 correction): the full-env `${(@kv)parameters}` instrument uses a TYPE-CLASS filter (skip any param whose type matches `(*special*|*tied*|*hide*|undefined)`, keeping user-scope `scalar[-export]`/`array`/`association`/`integer`/`float`) — NOT a named allowlist, which C28 REFUTED as insufficient — plus a reserved `ZP__`-prefix exclusion for instrument locals + fd/temp-file capture (not `$(...)`), so two no-op snapshots diff to empty (no false-red; the type-based skip never reads a lazy special) while the leak/value-change meta-tests still fire (no false-green re-opening the C19 blind spot). C28 is PROVEN for the corrected type-class filter (self-stable 5/5, both meta-asserts fire); the original named-list is historically REFUTED. |
| T-04-02 | Tampering | drift guard | mitigate | Deactivate restores an env scalar only if `live == applied` (`${(P)+var}==1` unset-vs-empty); a hand-edited managed var survives — drift sub-check in the property test (Task 2). |
| T-04-12 | Denial of Service | partial-apply / interrupted switch | accept (Phase 5 seam) | Plain functions, no transaction/rollback; a switch interrupted mid-way can leave partial state, recoverable by re-running `checkout` (idempotent rebuild-from-base) or a fresh terminal. A transactional recovery contract is deferred to the Phase 5 loader — documented seam. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies (stdlib + existing `mvdan.cc/sh`); no package-manager install tasks — no legitimacy gate needed. |
</threat_model>

<verification>
- `GOTOOLCHAIN=auto go build ./...` succeeds (Emitter seam + main.go wiring compile).
- `GOTOOLCHAIN=auto go test ./...` passes (emit + residue + invariant + all prior suites).
- Emitted apply and deactivate blocks pass `zsh -n` (C18, asserted in emit_test on the RAW block).
- CH-11/CH-12 element-removal form reserved (in a comment/reserved helper only), no active glob or unquoted compare: the quoted `== "$target"` form appears ONLY in a comment/reserved helper (`grep -c '== "$target"' core/shell/zsh/emit.go` >= 1, all matches non-active) AND there is NO active `== $target` (unquoted) AND `grep -c ':#' core/shell/zsh/emit.go` == 0 AND the emitted `RebuildListFromBase` output contains no per-element removal loop (deactivate is full rebuild-to-base; `${path:#}` count 0).
- CH-5 injectable rendering present: `grep -E 'var renderList|var renderValue' core/shell/zsh/emit.go` matches.
- `make check` (fmt-check + vet + lint + test) stays green; the zero-residue property test runs in the default suite (skips when zsh absent) and composes alongside — never replaces — the testgen oracle pin.
- No new dependency: `go.mod` require block unchanged.
</verification>

<success_criteria>
- `core/shell/zsh/emit.go` is the sole place reverse zsh tokens are generated (invariant test), renders the Plan to PLAIN, `zsh -n`-valid apply/deactivate functions, injection-safe (zquote static / verbatim dynamic), drift-guarded (`${(P)+var}==1`), ownership-aware (deactivate = full rebuild-to-base, CH-11/CH-12; quoted-RHS element removal is the reserved/unexercised form), `${+name}`-guarded shadow restore WITH capture-before-overwrite (CH-4), SetOption apply-side live-option capture (CH-9), self-cleaning per-switch slots incl. the was_on slot (CH-3), runtime-captured option restore (CH-7), rendering factored into injectable renderList/renderValue vars (CH-5). (SW-01, T-01-06)
- The `shell.Emitter` seam mirrors `shell.Regenerator` and is wired at the composition root. (SW-01)
- emit.go re-validates the alias AND function NAMES against `^[A-Za-z0-9_][A-Za-z0-9_.-]*$` before emitting them unquoted into unalias/alias=/functions[]=/unset -f/RestoreShadowed* (CH-19, threat T-04-18) — defense in depth over the 04-01 builder drop; the adversarial NAME corpus fires no canary through any of those paths while realistic names round-trip. (SW-01, T-01-06)
- A default-suite property test over N>=20 random BALANCED switch sequences (CH-14) leaves the FULL-env (all params, name+VALUE, self-stable via a TYPE-CLASS filter skipping `(*special*|*tied*|*hide*|undefined)`-typed params + reserved `ZP__` prefix + fd-capture, CH-10 / C28 correction) CONCATENATED WITH the SORTED function-BODY section (`${(@ok)functions}` — CH-22) + a dedicated PATH section carrying `$#path` AND the ordered `$path` array CONTENTS (CH-25 — a count-preserving PATH reorder/swap is caught, not only `$#path`) + the dedicated SORTED option-state (`${(@ok)options}`) + alias-body (`${(@ok)aliases}`) sections (CH-16/CH-20) ALL byte-identical across the general N-sequence compare (CH-21/CH-25 — the alias/option/PATH-content classes bound into the general property, not only isolated sub-checks); a meta-assert proves the instrument is self-stable AND catches non-exported+value-only residue; THREE independent mutants (blind-append PATH; dropped-zquote; base-strip) each fail it (CH-5/CH-15); the strengthened base-ownership sub-check asserts `$#path` byte-identical + `/usr/local/bin` exactly once (CH-15); a post-sequence assertion confirms `ZP_BASE_PATH` equals its baseline (CH-23); an option-drift fixture proves CH-9 option restore. (SW-02)
- All decisions D-10..D-13, D-16, D-17 implemented; the refuted mechanisms (OQ-8 `-n`/`${(P)+literal}`; C6 zquote'd function body; C10 `${path:#}`/unquoted `== $target` glob; Pitfall 1 `emulate -L`) are NOT reintroduced; CH-1..CH-23 and the addressed MEDIUMs are carried; C27 (option capture/restore), C29 (hostile option name rejected/inert), and C30 (dedicated sections self-stable) are PROVEN and C28 (self-stable snapshot) is PROVEN for the corrected TYPE-CLASS filter (the original named-list mechanism is historically REFUTED — see 04-EVIDENCE.md C28); C31 (hostile alias/func name rejected/inert — CH-19) is PROVEN (pass-4); C32 (static PATH addition segment inert / dynamic segment late-bound — CH-24) is UNVERIFIED, pass-5 must prove.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-02-SUMMARY.md` when done, recording: the Emitter seam signature chosen, the CH-5 renderList/renderValue package-level-var factoring, the OQ-18 bare-zp_*-calls decision, the OQ-10 slot-naming approach (sanitize vs assoc-array) and the injection-corpus-through-slot-path result, the CH-3 per-switch slot-cleanup list + ZP_BASE_PATH-persistence handling, the CH-4 capture-before-overwrite mechanism, the CH-9 SetOption apply-side live-option capture mechanism (`[[ -o optname ]]` into a `${+slot}`-guarded was_on slot), the CH-11/CH-12 deactivate=full-rebuild-to-base decision + the reserved (unexercised) quoted-RHS element-removal form (C25 PROVEN, no pass-2 pending), the CH-10 self-stable full-env snapshot instrument (TYPE-CLASS filter skipping `(*special*|*tied*|*hide*|undefined)`-typed params + reserved `ZP__` prefix + fd-capture + self-stability meta-test — C28 correction, replacing the refuted named-scalar allowlist), the CH-14 balanced-sequence generator + invariant, the CH-15 strengthened base-ownership sub-check (`$#path` byte-identical + exactly-once + base-strip negative control), the CH-16 dedicated filter-INDEPENDENT option-state (SORTED `${(@ok)options}` → `optname=<on|off>`) + alias-body (SORTED `${(@ok)aliases}` → `name<NUL>body`) snapshot sections (CH-20 — `o` sort flag, matching PROVEN C30) + the `zmodload zsh/parameter` preamble + the populated-map baseline assertion, the CH-21 concatenation of the dedicated option-state + alias-body sections into the general N-sequence byte-identical compare, the CH-22 SORTED function-body section (`${(@ok)functions}`) in the self-stability assertion, the CH-23 ZP_BASE_PATH-persistence post-sequence assertion + PATH/`path` *tied*/*special* dedicated-section note + optional `$fpath` parity, the CH-17 SetOption option-name identifier-grammar validation (`^[A-Za-z_][A-Za-z0-9_]*$`, reject/no-part) + the CH-19 alias/func-name identifier-grammar re-validation (`^[A-Za-z0-9_][A-Za-z0-9_.-]*$`, reject/no-part, defense in depth) + the adversarial-NAME-corpus-through-SetOption AND -through-AddAlias/Unalias/AddFunc/UnsetFunc/RestoreShadowed* results, the property-test N + the THREE mutant mechanisms (blind-append, dropped-zquote, base-strip) + the meta-asserts, the CH-24 renderList per-segment static-zquote/dynamic-verbatim split (a dynamic self-reference addition segment stays verbatim, a static metacharacter/command-sub segment is zquote'd inert — completing the uniform injection boundary alongside values C5/C6/C7 and names C24/C29/C31) + the addition-segment injection-corpus-through-ApplyListDelta result, the CH-25 binding of the sorted `$path` array CONTENTS (not only `$#path`) into the general N-sequence byte-identical compare, confirmation that C27 (option capture/restore), C28 (self-stable snapshot), C29 (hostile option name rejected/inert), C30 (dedicated sections self-stable), and C31 (hostile alias/func name rejected/inert) are ALL PROVEN (no pass-2/pass-3/pass-4 pending), the NEW UNVERIFIED claim C32 (a STATIC metacharacter/command-sub PATH addition segment is emitted inert — no canary through the ApplyListDelta double-eval — AND a DYNAMIC segment expands late-bound — CH-24) flagged for pass-5, and confirmation that no refuted mechanism was reintroduced.
</output>
