---
phase: 04-manifest-builder-emit
plan: 08
type: execute
wave: 7
depends_on: ["04-07"]
gap_closure: true
files_modified:
  - core/model/block.go
  - core/model/profile.go
  - core/shell/zsh/parse.go
  - core/shell/zsh/parse_test.go
  - core/shell/zsh/dynamic_test.go
  - core/ir/build.go
  - core/ir/build_test.go
  - core/store/dto.go
  - core/store/dto_test.go
autonomous: true
requirements: [SW-01]
user_setup: []

must_haves:
  truths:
    - "Every newly parsed PATH/path or FPATH/fpath scalar carries a structured semantic list contract with exactly one same-variable self-reference and ordered literal/dynamic segments."
    - "Quoted and mixed-quoted segments preserve runtime data; PATH never accepts FPATH/fpath as its base and FPATH never accepts PATH/path."
    - "Nil semantic list data means legacy/unsupported absence, while a present list with zero additions and one self-reference is valid; no source text is rewritten."
    - "Dynamic additions retain validated expression source and are reconstructed in tied-scalar assignment context, so a runtime expansion may contribute zero, one, or multiple colon-separated PATH/FPATH elements exactly as direct zsh does."
    - "The semantic list contract survives Parse to IR to deterministic profile JSON with deep-copy and legacy-omission compatibility."
  assumptions:
    - statement: "SW-02 unclassified edge-probe item remains flagged; final list restoration is proven after Build/Emit integration in 04-09 and the oracle in 04-12."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/model/profile.go"
      provides: "ListValue and ListSegment shell-agnostic semantic contract, including scalar-context dynamic source"
      contains: "type ListValue"
    - path: "core/shell/zsh/parse.go"
      provides: "AST-derived same-variable list segmentation without execution"
      contains: "ListValue"
    - path: "core/store/dto.go"
      provides: "optional backward-compatible list semantic persistence"
      contains: "listValue"
  key_links:
    - from: "core/shell/zsh/parse.go"
      to: "core/model.Block"
      via: "AST list decoder records self/literal/dynamic segments while Value remains source"
      pattern: "ListValue"
    - from: "core/model.Block"
      to: "core/model.Entry"
      via: "ir.Build deep-copies list semantics"
      pattern: "ListSegment"
    - from: "core/model.ListSegment.Source"
      to: "core/shell/zsh/emit.go"
      via: "validated dynamic additions remain scalar-context expressions whose runtime colon cardinality is decided by zsh"
      pattern: "Source"
    - from: "core/model.Entry"
      to: "profile JSON"
      via: "store DTO persists optional semantic list contract"
      pattern: "listValue"
---

<objective>
Create the semantic PATH/FPATH contract that activation can compose without parsing raw source spelling.

Purpose: list ownership must distinguish same-variable base references from literal/dynamic additions while keeping regeneration byte-identical and shell-specific parsing out of core/activate.
Output: additive model fields, AST list segmentation, IR propagation, and backward-compatible DTO persistence.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define presence-aware semantic list types and IR copying</name>
  <files>core/model/block.go, core/model/profile.go, core/ir/build.go, core/ir/build_test.go</files>
  <read_first>
    - core/model/block.go (parser-owned additive field conventions)
    - core/model/profile.go (ValueMode/RuntimeValue pointer-presence contract)
    - core/ir/build.go (Block to Entry deep-copy boundary)
    - core/ir/build_test.go (pointer/slice copy assertions)
  </read_first>
  <behavior>
    - RED: a present list value distinguishes one self marker with zero additions from nil/unmodeled absence.
    - RED: ordered literal and dynamic segment metadata copies from Block to Entry without shared slice backing.
    - RED: a dynamic segment has required validated Source and no pre-split cardinality claim; malformed Dynamic/Self/Source combinations fail closed.
    - RED: Entry.Value and RuntimeValue meanings remain unchanged.
  </behavior>
  <action>
    Add shell-agnostic ListValue and ListSegment types in core/model. ListValue records an ordered segment sequence. A literal segment carries exact semantic Value; a dynamic segment carries the parser-validated expression spelling in Source and an explicit Dynamic flag; a Self segment has no data/source payload and represents the current value of the same canonical tied list. Dynamic Source is deliberately not assigned an element cardinality in core/model: it is evaluated later as part of one PATH/FPATH scalar assignment, where zsh may produce zero, one, or multiple colon-separated elements. Reject impossible flag/payload combinations at the Build/Diff validation boundary. Add ListValue *ListValue to Block and Entry so nil means no verified semantic contract and a non-nil value may validly contain only Self. Deep-copy the pointer, struct, Source strings, and segment slice in ir.Build. Do not change ValueMode, RuntimeValue, FunctionBody, Text, or Value.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/ir -run 'Build|ListValue|ListSegment|DeepCopy|Empty' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Nil and present-self-only list states are distinguishable.
    - Segment order and explicit Dynamic/Self flags survive without aliasing.
    - Dynamic Source survives exactly and has no eager split or assumed one-element cardinality.
    - Existing model/IR semantic fields and tests remain unchanged and green.
  </acceptance_criteria>
  <done>The IR can carry exact list semantics without overloading raw Value text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive same-variable list segments from the zsh AST</name>
  <files>core/shell/zsh/parse.go, core/shell/zsh/parse_test.go, core/shell/zsh/dynamic_test.go</files>
  <read_first>
    - core/shell/zsh/parse.go (assignment loops, quote-context decoder, source offsets)
    - core/shell/zsh/parse_test.go (assignment shape tables)
    - core/shell/zsh/dynamic_test.go (live-zsh semantic comparisons)
    - /home/metanmai/go/pkg/mod/mvdan.cc/sh/v3@v3.13.1/syntax/nodes.go (ParamExp and quote nesting)
  </read_first>
  <behavior>
    - RED: PATH="$HOME/bin:$PATH" records dynamic $HOME/bin then one PATH self marker; FPATH="${HOME}/zfunc:$FPATH" does the FPATH equivalent.
    - RED: PATH=/a:$PATH, PATH=$PATH:/b, and mixed quoted concatenations preserve segment order and semantic bytes.
    - RED: PATH with $FPATH/$fpath, FPATH with $PATH/$path, zero self references, or multiple self references has nil ListValue and fails closed for activation.
    - RED: escaped/quoted delimiters and present-empty additions match live zsh list semantics.
    - RED: `$EXTRA` values containing two colon-separated elements and the empty string match direct source behavior for both PATH and FPATH.
  </behavior>
  <action>
    On single-name scalar assignments whose exact name is PATH/path/FPATH/fpath, run an AST-only list decoder after the general value semantic pass. Canonicalize PATH/path and FPATH/fpath only for deciding the permitted self parameter. Tokenize literal colon delimiters through nested quote-aware AST parts, concatenate adjacent literal and allowlisted simple-parameter fragments into an ordered scalar-expression segment, and preserve exact validated source spelling for each dynamic segment. Recognize exactly one whole-segment ParamExp of the same canonical list as Self. Do not eagerly split a dynamic expansion or model it as one array element: `$EXTRA` remains scalar-context source so runtime `EXTRA=/a:/b` contributes two tied-array elements and `EXTRA=` contributes the same empty-element behavior as direct assignment. Reject a cross-list reference, a missing or repeated Self, command/process/arithmetic substitutions, parameter operators/modifiers, unmodeled word parts, or ambiguous syntax by leaving ListValue nil.

    Use the Task 1 escape decoder for literal fragments so quotes and backslashes do not survive as data. Preserve the original Value byte-for-byte and execute no source. Add disposable zsh -f comparisons for quoted/mixed segment bytes, cross-variable rejection, and multi-element/empty `$EXTRA` expansion for PATH and FPATH.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Parse|ListValue|PathSemantic|CrossVariable|QuotedPath|Dynamic' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one same-canonical-variable self marker is required.
    - Cross-variable bases and ambiguous dynamic forms produce nil semantic list data.
    - Quoted/mixed additions carry runtime bytes and explicit dynamic provenance.
    - Multi-element and empty dynamic expansions match direct zsh tied-variable arrays for both list identities.
    - Value/Text remain byte-identical source and parse.go executes nothing.
  </acceptance_criteria>
  <done>PATH and FPATH source become precise ordered semantic segments without shell execution or string splitting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist semantic list data with legacy compatibility</name>
  <files>core/store/dto.go, core/store/dto_test.go</files>
  <read_first>
    - core/store/dto.go (optional semantic field DTO conventions)
    - core/store/dto_test.go (legacy omission, present-empty, deterministic round-trip tests)
    - core/model/profile.go (ListValue contract from Task 1)
  </read_first>
  <behavior>
    - RED: literal/dynamic/self segment sequences round-trip exactly and deterministically.
    - RED: a present self-only list survives JSON; omitted listValue decodes nil for legacy entries.
    - RED: DTO conversion deep-copies segment slices.
    - RED: dynamic Source round-trips byte-for-byte while legacy DTOs with no Source remain readable only for non-dynamic segments.
  </behavior>
  <action>
    Add an optional listValue field and matching flat segment DTO shape to entryDTO, including optional Source for validated dynamic scalar expressions. Marshal all explicit flags, including false, and preserve segment order. Omitted top-level fields decode to nil without inference or migration; a Dynamic segment missing Source is malformed and must fail closed rather than acquiring one-element semantics. Deep-copy both directions; never rewrite Entry.Value. Add deterministic repeated marshal tests and legacy JSON fixtures.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/store -run 'DTO|ListValue|ListSegment|Legacy|Deterministic' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Present-self-only and mixed segment sequences round-trip exactly.
    - Legacy JSON without listValue remains readable with nil semantic data.
    - Repeated marshals are byte-identical and no backing slice aliases input/output.
  </acceptance_criteria>
  <done>The semantic list contract survives the git-backed profile boundary without changing old branches.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level 1; blocking threshold high.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-04-08-01 | Tampering | PATH/FPATH base identity | high | mitigate | exact same-canonical-variable AST self marker; cross-variable rejection |
| T-04-08-02 | Elevation of Privilege | dynamic list segment | high | mitigate | explicit AST provenance and fail-closed rejection of command/process/arithmetic substitutions |
| T-04-08-05 | Tampering | dynamic expansion cardinality | high | mitigate | preserve allowlisted source and reconstruct in tied-scalar context; direct multi-element and empty PATH/FPATH comparisons |
| T-04-08-03 | Tampering | DTO omission/presence | medium | mitigate | optional pointer contract, explicit flags, legacy and present-empty tests |
| T-04-08-04 | Information Disclosure | source/runtime split | medium | mitigate | Value stays source; semantic additions are isolated and never logged as secrets |
| T-04-08-SC | Tampering | dependency supply chain | low | accept | no installs or new dependencies |
</threat_model>

<prohibition_recall>
No bespoke prohibition survives. Cross-variable base confusion and empty/ordering behavior are routine semantic correctness addressed by must-haves; command execution is canon injection security covered by the threat register.
</prohibition_recall>

<verification>
- GOTOOLCHAIN=auto go test ./core/model ./core/ir ./core/store ./core/shell/zsh -count=1
- GOTOOLCHAIN=auto go build ./...
- Source scan confirms no execution/environment/filesystem expansion in parse.go.
</verification>

<success_criteria>
- Every accepted PATH/FPATH entry has exact same-variable, ordered semantic segments.
- Dynamic additions preserve zsh scalar-context zero/one/many element behavior rather than being forced into one array element.
- Cross-variable/ambiguous shapes fail closed.
- The contract crosses IR and store losslessly while Value remains byte-for-byte source.
</success_criteria>

<artifacts_this_phase_produces>
- model.ListValue and model.ListSegment with validated dynamic Source.
- AST-only same-variable list decoder.
- Optional profile DTO listValue persistence fields and fixtures.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-08-SUMMARY.md` when done.
</output>
