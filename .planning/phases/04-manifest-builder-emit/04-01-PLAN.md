---
phase: 04-manifest-builder-emit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/model/manifest.go
  - core/model/manifest_test.go
  - core/model/identityset.go
  - core/activate/builder.go
  - core/activate/plan.go
  - core/activate/diff.go
  - core/activate/builder_test.go
  - core/activate/diff_test.go
  - core/activate/schema_test.go
  - core/activate/tokenfree_test.go
  - core/shell/zsh/introspect.go
  - core/shell/zsh/introspect_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "A model.Manifest carrying all four part types marshals to and unmarshals from the 01-MANIFEST-SHAPE.md JSON losslessly (SW-01: the reversible record exists)"
    - "core/activate builds a model.Manifest from a resolved model.Profile using only EffectiveManaged() entries; an imperative/forced-unmanaged entry produces no part; a CatPath value whose base marker is ambiguous produces no part (SW-01: build-from-profile, precision-over-recall)"
    - "core/activate produces an ordered deactivate-then-activate Plan value; the deactivate list op (RebuildListFromBase) is semantically DISTINCT from the activate list op (ApplyListDelta) — deactivate rebuilds TO base dropping this profile's additions, activate re-adds them; the package contains no zsh reverse-op token as a string literal (SW-01: agnostic plan, single-emit-path invariant preserved) [Addresses review concern: CH-1c]"
    - "Diff refuses to operate on a manifest whose Schema != SchemaV1 — a schema-mismatch manifest is rejected before any reverse op is generated (SW-01: forward-compat gate is a real check, not a bare const) [Addresses review concern: CH-8]"
    - "RestoreOption carries no builder-authored WasOn — the field is dropped/marked non-authoritative because the builder is FORBIDDEN to author was_on (D-06); the live prior is captured at apply time in emit.go (SW-01) [Addresses review concern: CH-7]"
    - "Introspect captures alias and function BODIES (not just names) via a NUL-framed dump bounded by a NUL-preceded sentinel; a multi-line function body round-trips byte-for-byte; existing name-only tests still pass; zsh-absent still degrades to Available:false (SW-02: shadow restore becomes possible)"
  artifacts:
    - path: "core/model/manifest.go"
      provides: "Manifest + Scalar/ListDelta/AliasSet/FuncSet/OptionSet types, directly json-tagged; SchemaV1 const (D-01/D-02/D-03/D-04/OQ-11)"
      contains: "type Manifest struct"
    - path: "core/activate/builder.go"
      provides: "Build(model.Profile) model.Manifest — classifies EffectiveManaged entries by Kind+Category; narrowed router-admitted PATH split, no-part on ambiguous base (D-05/D-06/D-07/OQ-16)"
      contains: "func Build"
    - path: "core/activate/plan.go"
      provides: "Plan value type + agnostic Op vocabulary (D-08); Dynamic bool carried on apply ops; RestoreOption WasOn dropped/non-authoritative (CH-7)"
      contains: "type Plan struct"
    - path: "core/activate/diff.go"
      provides: "Diff(active, target *model.Manifest) (Plan, error) — full-deactivate-then-full-activate, empty cases first-class, SchemaV1 gate (D-08/D-09/OQ-15/CH-8)"
      contains: "func Diff"
    - path: "core/model/identityset.go"
      provides: "additive AliasBodies/FunctionBodies map[string]string companions (D-15)"
      contains: "FunctionBodies"
    - path: "core/shell/zsh/introspect.go"
      provides: "additive NUL-framed body-dump sections bounded by a NUL-preceded sentinel + dedicated non-line body parser (D-14/OQ-17)"
      contains: "print -rN"
  key_links:
    - from: "core/activate/builder.go"
      to: "core/ir/route.go routing shape"
      via: "reuse the same Kind+Category classification checks on Entry (not re-derive the admitted set)"
      pattern: "EffectiveManaged"
    - from: "core/activate"
      to: "core/model only"
      via: "package import graph — never core/shell/zsh"
      pattern: "zsh-pro/core/model"
---

<objective>
Build the shell-agnostic foundation of Phase 4: the reversible record type (`model.Manifest` + four parts), the `core/activate` package (builder + differ + agnostic `Plan`), and the additive introspect body-dump that makes shadow restore possible. Everything here is stdlib-only and shell-token-free; the zsh emission path is Plan 04-02 (Wave 2), which consumes the `Plan` type this plan defines.

Purpose: SW-01 needs a manifest and an agnostic plan; SW-02's shadow restore needs captured alias/function bodies. Defining the `Plan` op vocabulary and the body-dump encoding first gives 04-02 a fixed contract to render against (interface-first ordering — no scavenger hunt).
Output: `core/model/manifest.go`, `core/activate/{builder,plan,diff}.go`, extended `core/model/identityset.go` + `core/shell/zsh/introspect.go`, with round-trip, builder, schema-gate, token-free, and body round-trip tests.

Cycle-1 review resolutions carried here: CH-1c (distinct deactivate list op), CH-7 (RestoreOption WasOn non-authoritative), CH-8 (real SchemaV1 gate in Diff), and MEDIUMs OQ-16 (narrowed builder PATH split, C26 UNVERIFIED), OQ-17 (NUL-preceded sentinel body-section boundary), Deletions-present-but-unexercised note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-SPEC.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-PATTERNS.md
@.planning/phases/04-manifest-builder-emit/04-EVIDENCE.md
@.planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; do not re-explore. -->

From core/model/profile.go (the builder's input; note: Entry does NOT carry Flagged/Append/Array — those live on model.Block. The IR already applied routeManaged at Build time, so the builder trusts EffectiveManaged() and classifies by Kind+Category+Names+CmdName only):
```go
type Entry struct {
    Text string; StartLine int; Category Category; Kind BlockKind
    CmdName string; Names []string; Value string; Exported bool
    Managed bool; Override ManagedOverride; Dynamic bool; Secret *SecretRef
}
func (e Entry) EffectiveManaged() bool // OverrideManaged->true, OverrideUnmanaged->false, else Managed
type Profile struct { Entries []Entry }
```

From core/model/category.go and block.go (classification constants):
```go
CatEnvironment Category = "environment"; CatPath Category = "path"
CatSecrets Category = "secrets"; CatOptions Category = "options"
KindAssignment; KindAlias; KindFuncDecl; KindCommand (BlockKind)
```

From core/model/secretref.go (the direct-json-tag precedent to copy for the manifest):
```go
type SecretRef struct { Kind SecretRefKind `json:"kind"`; Key string `json:"key"` }
```

From core/model/identityset.go (MODIFY additively — leave existing fields intact):
```go
type IdentitySet struct {
    Aliases, Functions, Env, Options map[string]bool
    Path []string; Available bool
    // ADD: AliasBodies, FunctionBodies map[string]string
}
```

From core/shell/zsh/introspect.go (MODIFY additively; the subprocess shape and existing line-parser are UNCHANGED):
```go
const introspectScript = `... print -r -- '##ALIASES##' ... '##END##'` // extend AFTER name sections
func (p Provider) Introspect(path string) (model.IdentitySet, error) // zsh -f -c, 5s timeout, Available:false on error — DO NOT CHANGE
func (Provider) parseIntrospect(s string) model.IdentitySet // line-oriented strings.Split(s,"\n") switch — leave name sections unchanged; add a dedicated non-line body parser
```
</interfaces>

<validated_ground>
Refuted mechanisms — DO NOT reintroduce (see 04-EVIDENCE.md / OQ-8 / OQ-11 / CH-1 / CH-7 / CH-8):
- OQ-11 supersedes D-02's tentative "uniform map": use TWO distinct types — `AliasSet{Added map[string]string; Shadowed map[string]string}` and `FuncSet{Added []string; Shadowed map[string]string}`. A uniform `map[string]string` provably cannot unmarshal `functions.added: ["work_deploy"]` (POC-J2). Note the supersession of D-02 in the manifest.go doc comment.
- D-14 / OQ-17: body dump uses NUL-delimited `print -rN -- name body` records terminated by a NUL-preceded sentinel + a NEW multi-line-safe reader — NOT a `name\tbody` line format (truncates multi-line bodies, C14/C23), NOT the existing line-oriented `##DELIMITER##` reader, and NOT a bare `\n##` scan (a body line starting `##` fools it — OQ-17).
- D-08 token-free check is PRECISE: case-sensitive, word-boundary, string-literals only, scoped to reverse tokens (`unalias`/`unset -f`/`unsetopt`/PATH-array rebuild), excluding Go identifier type names (`Unalias`, `SetOption`) and comments (C22).
- CH-1 / C11 / OQ-13: multi-managed-profile PATH co-ownership is OUT OF SCOPE for single-active v2.0 (Phase 3 D-13); do NOT build a reference-count ownership model. The deactivate list op rebuilds TO base (dropping this profile's additions), NOT two-active-managed resolution.
- CH-7 / D-06: the builder is FORBIDDEN to author `was_on` — it is a runtime-captured live fact. Do NOT populate an authoritative WasOn on any op from the manifest.
- CH-8 / OQ-15: `SchemaV1` is NOT a decorative const — `Diff` (the reverse-logic entry point) must actually check `Schema != SchemaV1` and refuse.
- OQ-16 / C26 (UNVERIFIED): the builder PATH split is narrowed to router-admitted prepend/append shapes; ambiguous shapes produce NO part. C26 is UNVERIFIED — claim-validation pass 2 must prove the narrowed split before this task's behavior is trusted.
</validated_ground>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: model.Manifest + four parts, lossless round-trip vs 01-MANIFEST-SHAPE.md</name>
  <files>core/model/manifest.go, core/model/manifest_test.go</files>
  <read_first>
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md (the exact JSON keys the round-trip test asserts against — the oracle)
    - core/model/secretref.go (the direct-json-tag + typed-string-const precedent to copy: package model, stdlib-only, one type cluster, doc comment per type)
    - core/store/dto.go (the deterministic json.MarshalIndent(v,"","  ")+trailing-\n marshal STYLE only — NOT a DTO indirection layer; the manifest tags itself directly per D-01)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"core/model/manifest.go" (target layout, Fork A)
  </read_first>
  <behavior>
    - Test: a Manifest with all four parts populated (env Scalar with original present, another with original absent, another with original &""; a PATH ListDelta and an FPATH ListDelta; aliases.added map + aliases.shadowed map; functions.added array + functions.shadowed map; an OptionSet) marshals then unmarshals to a reflect.DeepEqual identical Manifest (lossless round-trip).
    - Test: marshaling the fixture from 01-MANIFEST-SHAPE.md (a Manifest literal mirroring it) produces JSON whose keys match `profile/schema/env/lists/aliases/functions/options` and the part keys `name/applied/original/additions/deletions/added/shadowed/enabled/was_on`.
    - Test (tri-state): Scalar.Original nil marshals with NO `original` key; &"" marshals `"original":""`; &"vim" marshals `"original":"vim"`; all three unmarshal back losslessly.
    - Test (Fork A): `functions.added` unmarshals from a JSON array `["work_deploy"]` into `FuncSet.Added []string`; `aliases.added` unmarshals from `{"gs":"git status"}` into `AliasSet.Added map[string]string`.
  </behavior>
  <action>
    Create `core/model/manifest.go` (package model, stdlib-only, no imports beyond encoding/json if a marshal helper is shipped). Define `Manifest{ Profile string json:"profile"; Schema string json:"schema"; Env []Scalar json:"env"; Lists []ListDelta json:"lists"; Aliases AliasSet json:"aliases"; Functions FuncSet json:"functions"; Options []OptionSet json:"options" }`. Define `Scalar{ Name string json:"name"; Applied string json:"applied"; Original *string json:"original,omitempty" }` — the `*string`+omitempty tri-state is the single load-bearing type choice (D-02): nil=was-unset, &""=was-empty, &"vim"=was-value. Define `ListDelta{ Name string json:"name"; Additions []string json:"additions"; Deletions []string json:"deletions" }` — note in the type doc comment that `Deletions` is validated-present-but-UNEXERCISED this phase (the builder never authors a deletion; SPEC Req 1's additions+deletions is half-covered — deletions are a Phase 5+ concern). Per OQ-11 (supersedes D-02's tentative uniform-map), define TWO distinct types: `AliasSet{ Added map[string]string json:"added"; Shadowed map[string]string json:"shadowed" }` and `FuncSet{ Added []string json:"added"; Shadowed map[string]string json:"shadowed" }` — document in the type doc comment that this supersedes D-02's uniform-map wording because a uniform map cannot unmarshal the fixture's `functions.added` array (POC-J2). Define `OptionSet{ Name string json:"name"; Enabled bool json:"enabled"; WasOn bool json:"was_on" }` (snake_case tag per D-04) — doc-comment `WasOn` as a RUNTIME-captured live fact that the builder is FORBIDDEN to author (D-06); it exists in the JSON record shape but is never populated Go-side by the builder (CH-7). Add `const SchemaV1 = "v1"` (D-03), the exact literal from the fixture, doc-commented as the forward-compat gate that `Diff` (Task 2) actually checks before generating reverse ops — NOT a decorative const (CH-8). Give every type an exported doc comment and short trailing field comments (CLAUDE.md convention). If a marshal helper is shipped, copy `core/store/dto.go`'s `json.MarshalIndent(v,"","  ")`+`append(b,'\n')` deterministic style (maps sort keys deterministically per the encoding/json Go guarantee).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go test ./core/model -run TestManifest -x 2>&1 | tail -5; test ${PIPESTATUS[0]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-04-01 (schema forward-compat): the SchemaV1 const is the value `Diff` checks (the real gate lands in Task 2, not here). Threat T-04-02 (tri-state loss): the nil/&""/&"vim" round-trip test proves unset-vs-empty-vs-value is not conflated (a plain string would fail this).
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go test ./core/model -run TestManifest` exits 0
    - `core/model/manifest.go` has zero imports of any `zsh-pro/core/*` package (stdlib-only): `grep -E '"zsh-pro/core' core/model/manifest.go` returns nothing
    - `AliasSet.Added` is `map[string]string` and `FuncSet.Added` is `[]string` (Fork A): `grep -E 'Added\s+map\[string\]string' core/model/manifest.go` AND `grep -E 'Added\s+\[\]string' core/model/manifest.go` each return a match
    - `Scalar.Original` is `*string` with omitempty: `grep -E 'Original \*string `json:"original,omitempty"`' core/model/manifest.go` returns a match
    - `const SchemaV1 = "v1"` present
    - CH-7: `OptionSet.WasOn` doc comment marks it runtime-captured/builder-forbidden — `grep -iE 'was_on.*runtime|runtime.*was_on|builder.*(forbidden|never)|never.*author' core/model/manifest.go` returns a match
    - Deletions-unexercised note present: `grep -iE 'Deletions.*(unexercised|not.*author|Phase 5)|(unexercised|half-cover).*[Dd]eletion' core/model/manifest.go` returns a match
  </acceptance_criteria>
  <done>Manifest + four parts exist with the exact validated JSON shape (Fork A two-type split), tri-state Original, SchemaV1 const doc-commented as the real Diff gate, WasOn doc-commented builder-forbidden, Deletions doc-commented unexercised; round-trip and tri-state tests pass; stdlib-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: core/activate — builder (Profile→Manifest), Plan value type, differ (agnostic, token-free, SchemaV1 gate)</name>
  <files>core/activate/builder.go, core/activate/plan.go, core/activate/diff.go, core/activate/builder_test.go, core/activate/diff_test.go, core/activate/schema_test.go, core/activate/tokenfree_test.go</files>
  <read_first>
    - core/ir/route.go (routeManaged — the exact Kind+Category classification shape to REUSE, not re-derive; note it operates on model.Block but the same Kind/Category/CmdName/Names logic maps onto model.Entry; the ADMITTED PATH prepend/append shapes are the narrowing scope for the builder split, OQ-16)
    - core/model/profile.go (Entry fields + EffectiveManaged(); Entry does NOT carry Flagged/Append/Array — the IR already applied those guards at Build time, so the builder trusts EffectiveManaged() and classifies by Kind+Category)
    - core/model/manifest.go (Task 1 output — the parts the builder fills; the SchemaV1 const Diff gates on)
    - core/store/dto.go lines 1-7 (the agnostic-package boundary discipline: "never import the concrete zsh provider")
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §3 (builder classification table, Plan op vocabulary, ordering, empty cases, the precise token-free check C22, "do not optimize the diff")
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C26 (UNVERIFIED narrowed PATH split — the fixtures Task 2 tests must assert; pass-2 validation pending)
    - .planning/phases/04-manifest-builder-emit/04-OPEN-QUESTIONS.md OQ-13 (multi-managed out of scope), OQ-15 (SchemaV1 real check), OQ-16 (narrowed PATH split)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"core/activate/builder.go", §"diff.go", §"plan.go"
  </read_first>
  <behavior>
    - Build test: a Profile with one entry of each admitted class (CatEnvironment scalar; CatSecrets scalar; CatPath assignment; KindAlias; KindFuncDecl; setopt) yields a Manifest whose Env/Lists/Aliases.Added/Functions.Added/Options contain exactly those entries. An imperative entry (OverrideUnmanaged, or a KindCommand that is not setopt/unsetopt, or a Kind the gate rejects) produces NO part.
    - Build test: the builder fills declarative intent only (added/applied/enabled/additions); Shadowed/Original/WasOn/Deletions are left nil/empty (runtime-reconciled per D-06). CH-7: assert no op and no OptionSet carries a builder-authored non-zero WasOn.
    - Build test (D-07 / OQ-16 narrowed PATH split — Addresses review concern: MEDIUM builder-PATH-split; fixtures per EVIDENCE C26, UNVERIFIED): (a) `export PATH=$HOME/bin:$PATH` yields `ListDelta{Name:"PATH", Additions:["$HOME/bin"]}`; (b) `export PATH=$PATH:$HOME/bin` (tail-append) yields `Additions:["$HOME/bin"]`; (c) the `${PATH}` brace form is recognized as the base marker (same as `$PATH`); (d) mid-list base `$HOME/bin:$PATH:$HOME/go/bin` produces NO part (ambiguous — routed imperative, precision-over-recall); (e) no-base-marker `$HOME/bin:/usr/bin` produces NO part. A dynamic addition segment stays verbatim in Additions (not hard-quoted here — quoting is emit.go's job). Note in the builder doc comment that C26 is UNVERIFIED and pass-2 must prove this split.
    - Diff test (ordering + empty cases): `Diff(A, B)` returns a Plan whose Deactivate slice (all of A's reverse ops) fully precedes the Activate slice (all of B's apply ops). `Diff(A, nil)` = pure deactivate; `Diff(nil, B)` = pure activate. The diff is naive full-deactivate-then-full-activate (shared names are NOT skipped — RESEARCH §3 correctness hazard).
    - Diff test (CH-1c distinct list-op semantics — Addresses review concern: CH-1c): a Manifest with a PATH ListDelta produces, on the deactivate side, a `RebuildListFromBase` op (rebuild TO base, dropping this profile's additions) and, on the activate side, an `ApplyListDelta` op (re-add additions) — assert they are DIFFERENT op types carrying the same list name, and that RebuildListFromBase does NOT carry this profile's additions as "to re-add" (it rebuilds to base). Document that multi-managed co-ownership is out of scope (OQ-13).
    - Diff test (CH-8 SchemaV1 gate — Addresses review concern: CH-8): `Diff` with an `active` OR `target` manifest whose `Schema != model.SchemaV1` returns a non-nil error (or sentinel) and generates NO ops; `Diff` with matching SchemaV1 succeeds. Assert both directions (bad active, bad target).
    - Diff test (CH-7 RestoreOption non-authoritative WasOn — Addresses review concern: CH-7): the `RestoreOption` op emitted from an OptionSet carries no builder-authored WasOn value (the field is dropped from the op, or if present is documented non-authoritative and left zero) — assert the op struct has no populated WasOn from the manifest path.
    - Diff test: apply ops (SetScalar/AddAlias) carry a `Dynamic bool` propagated from the source entry (so emit.go can key quote-vs-verbatim on it without re-deriving).
  </behavior>
  <action>
    Create package `core/activate` importing `zsh-pro/core/model` ONLY (never core/shell/zsh). `plan.go`: define `Plan{ Deactivate []Op; Activate []Op }` where `Op` is a sealed interface (a tagged union of op structs carrying only agnostic data — no zsh tokens). Deactivate ops: `RestoreScalar{Name,Applied string; Original *string}`, `UnsetScalar{Name,Applied string}`, `RebuildListFromBase{Name string}` — CH-1c: this op rebuilds the list TO base and carries NO additions to re-add (distinct from the activate ApplyListDelta which re-adds; add a doc comment stating the distinct deactivate-vs-activate semantics and that multi-managed co-ownership is out of scope per OQ-13), `Unalias{Name string}`, `RestoreShadowedAlias{Name string}`, `UnsetFunc{Name string}`, `RestoreShadowedFunc{Name string}`, `RestoreOption{Name string}` — CH-7: DROP the `WasOn bool` field (or, if kept for shape parity, doc-comment it non-authoritative and never populate it; the runtime `was_on` is captured in emit.go). Activate ops: `SetScalar{Name,Applied string; Dynamic bool}`, `ApplyListDelta{Name string; Additions,Deletions []string}` — this op re-adds this profile's additions (distinct from RebuildListFromBase), `AddAlias{Name,Body string; Dynamic bool}`, `AddFunc{Name,Body string}`, `SetOption{Name string; Enabled bool}`. (Exact identifiers/union-vs-enum are Claude's discretion per D-08; keep the agnostic-value + no-zsh-token + Dynamic-bool-carried constraints, and the CH-1c/CH-7 semantics.) `builder.go`: `func Build(p model.Profile) model.Manifest` — iterate `p.Entries`, skip `!e.EffectiveManaged()`; classify each surviving entry by the SAME Kind+Category shape checks routeManaged uses (D-06): `KindAssignment` + `CatEnvironment`/`CatSecrets` + len(Names)==1 → `Scalar{Name:Names[0], Applied:Value}` (leave Original nil — runtime-reconciled); `KindAssignment` + `CatPath` → apply the NARROWED split (OQ-16/C26): recognize a base self-reference `$PATH`/`$path`/`${PATH}`/`$FPATH`/`$fpath`/`${FPATH}` at the head OR tail with colon-joined additions on the other side → `ListDelta{Name (PATH/FPATH by Names[0]), Additions:[the non-base segments], Deletions:nil}`; any ambiguous shape (no base marker, mid-list base, unrecognized brace form) → NO part (routed imperative, precision-over-recall); `KindAlias` + len(Names)==1 → `Aliases.Added[Names[0]]=Value`; `KindFuncDecl` → `Functions.Added = append(..., Names[0])`; `KindCommand` + CatOptions + CmdName in {setopt,unsetopt} + len(Names)>0 → one `OptionSet{Name, Enabled: CmdName=="setopt"}` per name (WasOn left zero — builder-forbidden, CH-7). Set Manifest.Schema=model.SchemaV1. Anything else → no part. `diff.go`: `func Diff(active, target *model.Manifest) (Plan, error)` — CH-8: FIRST, if `active != nil && active.Schema != model.SchemaV1` OR `target != nil && target.Schema != model.SchemaV1`, return `(Plan{}, err)` (a wrapped error naming the offending schema) WITHOUT generating any op — this is the real forward-compat gate (OQ-15). On a valid schema, build Deactivate from `active` (RestoreScalar/UnsetScalar keyed on Original presence, one RebuildListFromBase per list [rebuild-to-base, no additions — CH-1c], Unalias+RestoreShadowedAlias per alias, UnsetFunc+RestoreShadowedFunc per func, RestoreOption per option [no authoritative WasOn — CH-7]; a stable order suffices — no cross-op dependency within a side) then Activate from `target` (SetScalar/ApplyListDelta[re-add additions]/AddAlias/AddFunc/SetOption, Dynamic carried through). Handle nil active (pure activate) and nil target (pure deactivate) as first-class (D-09). Do NOT optimize away shared names (RESEARCH §3). Constructor naming per CLAUDE.md: pure functions `Build`/`Diff` (noun-verb form, matching ir.Build). `schema_test.go`: the CH-8 gate tests. `tokenfree_test.go`: assert `core/activate` source contains no zsh reverse-op token as a string literal, using the PRECISE check (C22): case-sensitive, word-boundary `\bunalias\b`/`\bunset -f`/`\bunsetopt\b`/PATH-array-rebuild pattern, matching only zsh SYNTAX string literals — NOT the Go op-type identifiers `Unalias`/`UnsetFunc`/`SetOption` and NOT comments.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go build ./core/activate/... && GOTOOLCHAIN=auto go test ./core/activate/... 2>&1 | tail -8; test ${PIPESTATUS[1]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-04-01 (schema forward-compat, NOW mitigated by a real check — CH-8): `Diff` returns an error on a schema mismatch and generates no reverse op; the schema_test.go mismatch test proves a future-versioned manifest is rejected, not silently mis-reversed. Threat T-04-03 (single-emit-path breach): the token-free test + import-graph check enforce no zsh reverse syntax in the agnostic package. Threat T-04-04 (misclassified-imperative residue): the "imperative entry / ambiguous PATH yields no part" tests honor ING-02 precision-over-recall (OQ-16). Threat T-04-11 (distinct list-op semantics, CH-1c): RebuildListFromBase (deactivate) rebuilds to base without re-adding this profile's additions, so a deactivate does not resurrect them; the co-ownership-out-of-scope note (OQ-13) bounds this to single-active v2.0.
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go test ./core/activate/...` exits 0
    - `core/activate` imports core/model only: `grep -rhoE '"zsh-pro/core/[a-z/]+"' core/activate/*.go | sort -u` lists only `"zsh-pro/core/model"` (no `core/shell/zsh`)
    - token-free (precise, comments stripped, string-literal reverse tokens only): `grep -rvE '^\s*//' core/activate/*.go | grep -v '_test.go' | grep -E '"[^"]*\b(unalias|unset -f|unsetopt)\b' ; test $? -ne 0` (no reverse-op zsh syntax string literal in non-test source)
    - Build ordering: the diff test asserting `len(Plan.Deactivate)` ops all precede `Plan.Activate` passes
    - `func Build(p model.Profile) model.Manifest` and `func Diff(active, target *model.Manifest) (activate.Plan, error)` exist: `grep -E 'func Build\(' core/activate/builder.go` and `grep -E 'func Diff\(.*\) \(.*Plan, error\)' core/activate/diff.go` both match
    - CH-8 real gate: `grep -E '!=\s*model\.SchemaV1|Schema\s*!=' core/activate/diff.go` returns a match AND `schema_test.go` asserts a schema-mismatch manifest yields a non-nil error with zero ops (both bad-active and bad-target)
    - CH-1c distinct list ops: `RebuildListFromBase` (deactivate) and `ApplyListDelta` (activate) are DISTINCT types — `grep -E 'RebuildListFromBase' core/activate/plan.go` and `grep -E 'ApplyListDelta' core/activate/plan.go` both match; the diff test asserts a PATH list produces a RebuildListFromBase on Deactivate and an ApplyListDelta on Activate (different op types) and that RebuildListFromBase carries no re-add additions
    - CH-7 WasOn non-authoritative: `grep -E 'WasOn' core/activate/plan.go` either returns nothing (field dropped) OR the builder/diff test asserts no RestoreOption carries a non-zero WasOn sourced from the manifest
    - OQ-16 narrowed split: the builder test asserts the (a)-(e) PATH fixtures (head/tail base → additions; mid-list/no-marker → no part); `grep -iE 'C26|UNVERIFIED|pass.?2' core/activate/builder.go` returns a match (the doc-comment flag)
  </acceptance_criteria>
  <done>core/activate builds a Manifest from EffectiveManaged entries (imperative/ambiguous-PATH→nothing, narrowed split), produces an ordered deactivate-then-activate Plan of agnostic ops with Dynamic carried through, a DISTINCT RebuildListFromBase deactivate op (CH-1c), no builder-authored WasOn (CH-7), and a REAL SchemaV1 gate in Diff (CH-8); imports core/model only; contains no zsh reverse token; all tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: additive introspect body-dump (NUL-framed, sentinel-bounded) + IdentitySet companions</name>
  <files>core/model/identityset.go, core/shell/zsh/introspect.go, core/shell/zsh/introspect_test.go</files>
  <read_first>
    - core/shell/zsh/introspect.go (the WHOLE file — extend introspectScript AFTER the name sections; preserve the zsh -f -c + 5s timeout + Available:false shape; leave the line-oriented parseIntrospect switch UNCHANGED for name sections and ADD a dedicated non-line body parser)
    - core/model/identityset.go (add companion fields, leave existing map[string]bool intact)
    - core/shell/zsh/introspect_test.go (the existing name-only tests that MUST keep passing; the LookPath skip-guard shape)
    - .planning/phases/04-manifest-builder-emit/04-RESEARCH.md §5 (NUL-record encoding, POC-Z1a/Z2a/Z3a/Go-RT, C14/C23 framing rules, env-inheritance caveat)
    - .planning/phases/04-manifest-builder-emit/04-EVIDENCE.md C23 (framing correction), 04-OPEN-QUESTIONS.md OQ-17 (NUL-preceded sentinel boundary, not a `\n##` scan)
    - .planning/phases/04-manifest-builder-emit/04-PATTERNS.md §"introspect.go", §"identityset.go"
  </read_first>
  <behavior>
    - Test: Introspect on a fixture defining `alias gs='git status'` and a single-line function `foo() { echo hi }` returns `AliasBodies["gs"]=="git status"` (RHS only) and `FunctionBodies["foo"]` equal to the body without the `name(){` wrapper (per POC-Z1a/Z2a).
    - Test (multi-line, the load-bearing case): Introspect on a fixture with a function whose body contains a newline + tab + single-quote + `$` + `;` returns `FunctionBodies[name]` byte-identical to the original body (NUL framing survives embedded newlines — POC-Go-RT).
    - Test (OQ-17 boundary — Addresses review concern: MEDIUM NUL-body-parser boundary): Introspect on a fixture with a function body containing a line that STARTS with `##` (e.g. a comment line `## not a header`) returns that body byte-identical and does NOT truncate/corrupt the FUNCTIONBODIES section or the following END marker — proving the parser bounds the section by the NUL-preceded sentinel/record framing, not a `\n##` scan.
    - Test: the existing name-only tests (Aliases/Functions/Env/Path/Options presence maps) still pass unchanged (additive, non-breaking).
    - Test: zsh-absent / error path still returns `IdentitySet{Available:false}` (graceful degradation preserved).
  </behavior>
  <action>
    In `core/model/identityset.go`, ADD two companion fields to `IdentitySet` (leave `Aliases`/`Functions`/`Env`/`Path`/`Options`/`Available` intact): `AliasBodies map[string]string` (name→RHS body) and `FunctionBodies map[string]string` (name→body). This is source-compatible because all existing `IdentitySet{}` literals are keyed and `analyze` reads only `Available` (D-15). In `core/shell/zsh/introspect.go`, extend `introspectScript` ADDITIVELY: after the existing `##OPTIONS##` block and before `##END##`, emit two new NUL-framed sections — `print -r -- '##ALIASBODIES##'` then `for k in "${(@k)aliases}"; do print -rN -- "$k" "${aliases[$k]}"; done`, then `print -r -- '##FUNCTIONBODIES##'` then `for k in "${(@k)functions}"; do print -rN -- "$k" "${functions[$k]}"; done`, then the existing `##END##`. Do NOT touch the existing name sections or the `zsh -f -c`/5s-timeout/Available:false subprocess shape. In `parseIntrospect`, keep the existing line-oriented `strings.Split(s,"\n")` switch UNCHANGED for the name sections, and add a DEDICATED non-line body parser: locate the `##ALIASBODIES##` and `##FUNCTIONBODIES##` header markers by offset, then consume `\x00`-framed `name\x00body\x00` records; OQ-17 — bound the section by the NUL-preceded record framing / a NUL-preceded end-of-section sentinel (the section header markers are emitted by `print -r --` so they are newline-framed at the boundary, and the records between them are pure NUL-framed bytes), and do NOT line-scan the body bytes for a `\n##` marker (a body line starting `##` — a comment — would fool a `\n##` scan; C23/OQ-17). Populate `AliasBodies`/`FunctionBodies` from the records. Populate the new maps only on the success path; the error path still returns `Available:false` with nil body maps. Initialize `AliasBodies`/`FunctionBodies` as empty maps in the success-path `IdentitySet` literal.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/zsh-pro && GOTOOLCHAIN=auto go test ./core/shell/zsh/... -run 'Introspect|Body' 2>&1 | tail -8; test ${PIPESTATUS[0]:-$?} -eq 0</automated>
  </verify>
  <verification>
    Threat T-04-05 (body-dump parse corruption / injection via crafted body): a function body containing a `##`-prefixed line or a newline must not corrupt the parse of following sections; the multi-line round-trip test + the `##`-prefixed-body-line test + the non-line body parser (NUL-framed records bounded by a NUL-preceded sentinel, never line-scanning for `\n##` — OQ-17) mitigate C23. NUL framing is scoped to reparsed function/alias bodies which never carry a raw 0x00 (C14).
  </verification>
  <acceptance_criteria>
    - `GOTOOLCHAIN=auto go test ./core/shell/zsh/...` exits 0 (including the pre-existing name-only Introspect tests)
    - the body dump uses `print -rN`: `grep -c 'print -rN' core/shell/zsh/introspect.go` is >= 2
    - the subprocess shape is unchanged: `grep -c '5 \* time.Second' core/shell/zsh/introspect.go` is >= 1 AND `grep -c 'Available: false' core/shell/zsh/introspect.go` is >= 1
    - `IdentitySet` carries the companions: `grep -E 'AliasBodies\s+map\[string\]string' core/model/identityset.go` and `grep -E 'FunctionBodies\s+map\[string\]string' core/model/identityset.go` both match
    - OQ-17 boundary: the body parser does NOT bound sections by a `\n##` scan — `grep -E '\\n##|"\\n##"' core/shell/zsh/introspect.go` returns nothing in the body-parser path (the parser splits on `\x00`): `grep -c '\\x00\|\\000\|byte(0)' core/shell/zsh/introspect.go` is >= 1
    - the multi-line-body round-trip test AND the `##`-prefixed-body-line test asserting byte-identical `FunctionBodies[name]` pass
  </acceptance_criteria>
  <done>Introspect additively dumps alias/function bodies via NUL-framed records bounded by a NUL-preceded sentinel and parsed by a dedicated non-line reader; multi-line and `##`-prefixed bodies round-trip byte-for-byte without a `\n##` scan (OQ-17); IdentitySet carries AliasBodies/FunctionBodies; existing name-only tests and Available:false degradation are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile.json (git store) → model.Manifest | The persisted Profile the builder reads. Owner's own config — same trust boundary as their `.zshrc` (v2.0 single-user; shared profiles are SHARE-01, a future milestone with its own gate). |
| zsh introspect subprocess → parseIntrospect | Alias/function bodies dumped from a sandboxed `zsh -f` cross into Go parsing; a crafted body (incl. a `##`-prefixed line) must not corrupt section framing. |
| core/activate Plan → (Wave 2) emit.go | The agnostic Plan is the seam; it must carry NO zsh syntax so the single-emit-path invariant holds. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | model.Manifest schema | mitigate | `SchemaV1` is a REAL forward-compat gate: `Diff` returns an error and generates NO reverse op when `active`/`target`.Schema != SchemaV1 (Task 2, schema_test.go) — a future version is detectably rejected, not silently mis-reversed. **Not** a bare const (CH-8 resolved). |
| T-04-02 | Information Disclosure | Scalar.Original tri-state | mitigate | `*string`+omitempty distinguishes unset/empty/value; round-trip test proves no conflation (Task 1) — a wrong shape would clobber a was-unset var to empty on restore. |
| T-04-03 | Elevation of Privilege | single-emit-path invariant | mitigate | `core/activate` token-free test (precise, C22) + import-graph check: no zsh reverse syntax in the agnostic package (Task 2). This is the structural guard that keeps injection surface confined to emit.go (Wave 2). |
| T-04-04 | Tampering | builder classification | mitigate | "imperative entry / ambiguous PATH yields no part" tests honor ING-02 precision-over-recall; a misrouted imperative line or a mis-split PATH is a zero-residue violation by construction (Task 2, OQ-16). |
| T-04-05 | Tampering | introspect body-dump parse | mitigate | NUL-framed records bounded by a NUL-preceded sentinel + dedicated non-line body parser (never line-scan for `\n##`) so a crafted `##`-prefixed or newline-laden body cannot corrupt following sections (Task 3, C14/C23/OQ-17). |
| T-04-11 | Tampering | deactivate list-op semantics | mitigate | `RebuildListFromBase` (deactivate) rebuilds TO base, dropping this profile's additions and NOT re-adding them; distinct from `ApplyListDelta` (activate). Multi-managed co-ownership is out of scope for single-active v2.0 (OQ-13). Task 2 asserts the two ops differ (CH-1c). |
| T-04-12 | Denial of Service | partial-apply / interrupted switch | accept (Phase 5 seam) | Emitted apply/deactivate are plain functions with no transaction/rollback; a switch interrupted mid-way (SIGINT, error) can leave partial state. For single-user v2.0 this is recoverable by re-running `checkout` (idempotent rebuild-from-base) or opening a fresh terminal. A transactional recovery contract (all-or-nothing apply, journaled undo) is deferred to the Phase 5 loader — documented as a seam, not solved here. |
| T-01-06 | Elevation of Privilege | emitted `eval`'d code | transfer | THE phase risk (shell injection via emitted code) is mitigated in Plan 04-02's emit.go (zquote / verbatim-dynamic split / slot-name sanitization). This plan's contribution is keeping the agnostic tiers token-free (T-04-03) so all injection surface lives in one auditable file. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies introduced (stdlib + existing mvdan.cc/sh only); no package-manager install tasks in this phase. |
</threat_model>

<verification>
- `GOTOOLCHAIN=auto go build ./...` succeeds (no compile break from the additive IdentitySet fields).
- `GOTOOLCHAIN=auto go test ./core/model/... ./core/activate/... ./core/shell/zsh/...` all pass.
- `make check` (fmt-check + vet + lint + test) stays green — `gofmt -l core/model/manifest.go core/activate/*.go core/shell/zsh/introspect.go` returns nothing.
- No new dependency: `go.mod` require block unchanged (still only `mvdan.cc/sh/v3`).
- CH-8 gate is real code, not a const: `grep -E 'SchemaV1' core/activate/diff.go` returns a match in a conditional that returns an error.
</verification>

<success_criteria>
- `model.Manifest` + four parts (Fork A two-type split, tri-state Original, SchemaV1) round-trips losslessly to/from the `01-MANIFEST-SHAPE.md` JSON. (SW-01)
- `core/activate` builds a Manifest from a resolved Profile (EffectiveManaged only; imperative/ambiguous-PATH→nothing), produces an ordered deactivate-then-activate Plan of agnostic ops with a DISTINCT RebuildListFromBase deactivate op (CH-1c) and no builder-authored WasOn (CH-7), gated by a REAL SchemaV1 check in Diff (CH-8), importing core/model only and containing no zsh reverse token. (SW-01)
- Introspect captures alias/function bodies via NUL-framed records bounded by a NUL-preceded sentinel (OQ-17); multi-line and `##`-prefixed bodies round-trip byte-for-byte; name-only tests and Available:false degradation unchanged. (SW-02 prerequisite)
- All decisions this plan touches (D-01..D-09, D-14, D-15) are implemented; OQ-11 (Fork A) supersedes D-02's uniform-map wording, noted in code; CH-1c, CH-7, CH-8, and MEDIUMs (OQ-16 narrowed split, OQ-17 boundary, Deletions-unexercised) are carried.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md` when done, recording: the final Plan op vocabulary + Op union shape (the contract 04-02 renders against, incl. the DISTINCT RebuildListFromBase-vs-ApplyListDelta semantics and the CH-7 WasOn resolution), the CH-8 Diff schema-gate signature (`Diff(active, target *model.Manifest) (Plan, error)`), the OQ-16 narrowed-PATH-split admitted shapes + no-part fallback (flag C26 UNVERIFIED for pass-2), the exact NUL body-dump section names + NUL-preceded-sentinel boundary (OQ-17), and confirmation that Fork A (two types) was used with the D-02 supersession noted.
</output>
