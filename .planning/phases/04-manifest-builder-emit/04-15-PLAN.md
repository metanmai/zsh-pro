---
phase: 04-manifest-builder-emit
plan: 15
type: execute
wave: 12
depends_on: [04-14]
files_modified:
  - core/model/profile.go
  - core/model/profile_test.go
  - core/store/dto.go
  - core/store/dto_test.go
  - core/ir/build.go
  - core/ir/build_test.go
  - core/ir/regen.go
  - core/ir/regen_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/shell/zsh/pipeline_test.go
  - core/shell/zsh/residue_test.go
autonomous: true
gap_closure: true
requirements: [SW-01, SW-02]
must_haves:
  truths:
    - "A historical persisted OverrideManaged append, array, or flagged-alias entry whose structural markers are absent remains structurally unknown after a DTO re-save: it regenerates verbatim and creates no manifest operation."
    - "A persisted legacy PATH or FPATH addition containing a safe late-bound parameter expands in zsh at activation time, keeps source order when mixed with semantic list segments, and deactivates back to the captured base without residue."
    - "A parsed, persisted, forced-managed append assignment retains its structural fidelity and is regenerated verbatim instead of being converted to an overwrite or a manifest operation."
    - "The builder continues to reject arbitrary shell-evaluation-shaped legacy list additions while accepting existing safe literal and simple-parameter additions."
  artifacts:
    - path: core/model/profile.go
      provides: "Persisted Entry structural-fidelity markers and one shared representability predicate."
    - path: core/store/dto.go
      provides: "Presence-aware, versioned Profile DTO representation for Append, Array, and Flagged markers, including an explicit unknown historical state."
    - path: core/activate/builder.go
      provides: "Source-ordered legacy/semantic list reduction with per-addition dynamic provenance and representability-gated manifest construction."
    - path: core/shell/zsh/pipeline_test.go
      provides: "Direct legacy-JSON and current-serialization regressions through regenerate, Build, Diff, Emit, and zsh -f."
  key_links:
    - from: core/ir/build.go
      to: core/store/dto.go
      via: "A known current structural-fidelity version copies Append/Array/Flagged from Block and serializes every marker; an absent or partial historical version decodes as unknown and re-serializes without a fidelity object."
      pattern: "StructuralFidelity|Append|Array|Flagged"
    - from: core/activate/builder.go
      to: core/shell/zsh/emit.go
      via: "ListDelta.AdditionDynamic reaches renderListDelta so safe legacy parameter source is emitted late-bound."
      pattern: "AdditionDynamic"
    - from: core/model/profile.go
      to: core/ir/regen.go
      via: "The shared representability predicate selects verbatim regeneration for forced append, array, flagged-alias, and declaration forms."
      pattern: "Representable"
---

<objective>
Close the two remaining Phase 4 fidelity blockers discovered after 04-14: dynamic legacy PATH/FPATH additions must preserve late-bound provenance, and persisted structural syntax must remain safe even when historical DTOs omit append, array, or flagged-alias markers.

Purpose: make the persisted Profile boundary faithful before it can create manifest intent or zsh code, satisfying SW-01 and restoring the affected SW-02 zero-residue path.
Output: presence-aware Entry/DTO fidelity metadata with a conservative unknown state, one representability gate used by regeneration and manifest construction, provenance-correct legacy list reduction, and sequenced source plus live-zsh regression coverage.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-PATTERNS.md
@.planning/phases/04-manifest-builder-emit/04-14-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
</context>

## Artifacts this phase produces

- A shell-agnostic persisted `Entry` fidelity contract which can decide whether source may be templated after DTO storage.
- A canonical PATH/FPATH reducer whose list-delta metadata distinguishes simple late-bound parameter source from literal text without evaluating configuration.
- Source-level and `zsh -f` regression tests that make dynamic expansion, source order, append semantics, and zero-residue restoration observable.

The existing 12-file boundary is intentional, not scope expansion: it follows the one persisted `Entry` contract through its already-owned model, DTO, IR, builder, and zsh pipeline/residue tests. It adds no production subsystem or new test harness; the boundary is the smallest persistence-to-live-shell proof for every decision point that could otherwise turn source syntax into a manifest operation.

## Source coverage audit

| Source | ID | Required outcome | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Only faithfully representable state crosses Profile → Manifest → emitted zsh, with late binding and zero residue. | Tasks 1–3 | COVERED |
| REQ | SW-01 | Manifest/emitter applies only faithfully representable declarative state; PATH is a delta from captured base. | Tasks 1–3 | COVERED |
| REQ | SW-02 | Apply/deactivate is path-independent and residue-free for admitted list inputs. | Task 3 | COVERED |
| RESEARCH | D-05/D-06 | Keep `core/activate` shell-agnostic and build only effectively managed, faithfully modeled entries. | Tasks 1–3 | COVERED |
| RESEARCH | D-07/D-13 | Preserve PATH/FPATH delta and safe late-bound values; do not execute user configuration. | Task 3 | COVERED |
| CONTEXT | D-01/D-02 | Preserve the Profile's source-derived data across store serialization and source order. | Tasks 1–3 | COVERED |
| CONTEXT | D-04 | Maintain the existing profile DTO contract rather than moving serialization into model types. | Task 1 | COVERED |
| CONTEXT | D-05/D-06 | Keep builder shell-agnostic and exclude nonrepresentable entries even when override state is persisted. | Tasks 1–3 | COVERED |
| CONTEXT | D-07 | Preserve list delta behavior against captured base. | Task 3 | COVERED |
| CONTEXT | D-10/D-11 | Keep zsh syntax in the emitter; prove its existing metadata consumer through `zsh -f` rather than adding zsh strings to core packages. | Task 3 | COVERED |
| CONTEXT | D-13 | Leave only validated dynamic data unquoted and preserve static/injection-safe behavior. | Task 3 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode structural fidelity with a conservative historical-DTO state</name>
  <files>core/model/profile.go, core/model/profile_test.go, core/store/dto.go, core/store/dto_test.go</files>
  <read_first>
    - core/model/block.go — parser-owned Append, Array, and Flagged meanings.
    - core/model/profile.go — EffectiveManaged and current declaration-only representability gate.
    - core/store/dto.go — store-local Profile DTO mapping and wire-key conventions.
    - core/store/dto_test.go — lossless round-trip and forced-managed declaration fixture patterns.
    - .planning/phases/04-manifest-builder-emit/04-VERIFICATION.md — failed forced-append data-flow trace.
  </read_first>
  <behavior>
    - Test 1: every newly serialized Entry writes one current structural-fidelity version plus explicit Append, Array, and Flagged marker values; MarshalProfile then UnmarshalProfile preserves true and false marker values and reports that the structure is known.
    - Test 2: `TestStructuralFidelityDTOCompatibilityMatrix` uses executable raw DTO records for every compatibility shape: no fidelity object; a supported-version object missing only Append; one missing only Array; one missing only Flagged; and a complete object bearing an unsupported version. Each raw record is an OverrideManaged append, array, or flagged-alias source shape as applicable and runs UnmarshalProfile → MarshalProfile → UnmarshalProfile; both decoded Entries remain explicitly structurally unknown, the re-save contains no fidelity object rather than inferred known-false markers, and the forced override remains nonrepresentable/verbatim with no manifest operation.
    - Test 3: the canonical predicate rejects structurally unknown entries and known Append, Array, Flagged, or declaration-command forms regardless of Override; known plain and exported scalar assignments remain representable.
  </behavior>
  <action>
    Extend `model.Entry` with the parser-derived `Append`, `Array`, and `Flagged` markers and an explicit structural-fidelity-known state. The known state must be set for Entries made from a current parser Block, including ordinary false marker values, but must remain false when the persisted source cannot prove all marker values. Keep `model.Block` parser-owned and do not add shell imports or shell syntax to model/store code.

    Replace the declaration-only guard with one shared Entry representability predicate (rename if useful) that first requires known structural fidelity, then retains all existing declaration exclusions and rejects Append and Array assignments plus Flagged aliases. Its positive path must still allow an ordinary one-name scalar assignment and ordinary exported scalar assignment. Document that this predicate answers whether stored fields can reproduce source semantics, not whether an override chose the managed axis. This implements D-05/D-06 and preserves D-07's override semantics without allowing it to erase source fidelity.

    In the store-local `entryDTO`, add one optional structural-fidelity object with a schema/version field and presence-aware fields for all three markers (for example, pointers so a missing field cannot decode as false). `MarshalProfile` must emit that object only when the Entry structural-fidelity state is known; a legacy, partial, or unsupported Entry that decoded as unknown must re-serialize with the object absent, never as a supported version with false markers. Marshal known current entries with the supported version and every marker present. During UnmarshalProfile, accept only a supported, complete object as known; an absent object, a partial marker object, or an unknown version must produce the conservative unknown Entry state.

    In `dto_test.go`, make the compatibility matrix executable raw JSON rather than treating absent state as one generic fixture: cover no object; a supported version with only `append` omitted; a supported version with only `array` omitted; a supported version with only `flagged` omitted; and a complete marker object with an unsupported version. Give every fixture a forced-managed append, array, or flagged-alias entry with its normal source `Text`. For every row execute UnmarshalProfile → MarshalProfile → UnmarshalProfile, assert the explicit unknown state before and after re-save, inspect the bytes to confirm no fidelity object was emitted, and prove the override still cannot select templating or a manifest scalar/list operation. Retain the DTO design per D-04; do not put JSON tags on `model.Entry`, change Profile source order, or resolve dynamic values during serialization.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/model ./core/store -run 'Test(Entry.*Represent|StructuralFidelityDTOCompatibilityMatrix|RoundTrip.*(Structural|OverrideManaged|Lossless))$' -v</automated>
  </verify>
  <acceptance_criteria>
    Current profiles persist a complete, versioned marker set; omitted historical marker state is observable as unknown and the shared predicate cannot grant it manifest eligibility.
  </acceptance_criteria>
  <done>Persisted profiles distinguish known false markers from every absent, partial, and unsupported historical marker shape across a re-save, and no OverrideManaged entry can become representable merely because source structure was absent or unrecognized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the legacy-DTO safety boundary before runtime list work</name>
  <files>core/ir/build.go, core/ir/build_test.go, core/ir/regen.go, core/ir/regen_test.go, core/activate/builder.go, core/activate/builder_test.go, core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/ir/build.go and core/ir/route.go — current parser Block to Entry copying and pre-persistence routing guards.
    - core/ir/regen.go — current EffectiveManaged plus declaration representability decision.
    - core/ir/build_test.go, core/ir/regen_test.go, core/activate/builder_test.go, and core/shell/zsh/pipeline_test.go — fixture and live-zsh pipeline conventions.
    - core/model/profile.go and core/store/dto.go — the known/unknown structural-fidelity contract added by Task 1.
    - .planning/phases/04-manifest-builder-emit/04-14-SUMMARY.md — existing declaration verbatim behavior to preserve.
  </read_first>
  <behavior>
    - Test 1: Build copies Append, Array, Flagged, and known structural fidelity from each current Block without changing `routeManaged`'s existing imperative classification.
    - Test 2: `TestPipelineForcedManagedStructuralShapes` covers current parser-backed `FOO+=bar`, `plugins=(git zsh-autosuggestions)`, and `alias -g G='| grep'`. Each runs Parse → `ir.Build` → DTO marshal/unmarshal → set OverrideManaged → Regenerate → Build → Diff → Emit → `zsh -f`; it remains byte-for-byte verbatim, produces zero scalar/list/name operations as applicable, leaves distinctive scalar/array/alias sentinels untouched through apply/deactivate, and restores the original live state after deactivate.
    - Test 3: `TestPipelineLegacyStructuralFidelityMatrix` runs every Task 1 absent/partial/unsupported raw DTO row through UnmarshalProfile → MarshalProfile → UnmarshalProfile → Regenerate → Build → Diff → Emit → `zsh -f`; its re-saved DTO still has no fidelity object, regeneration is byte-for-byte `Entry.Text`, no scalar/list/name operation is produced, and the relevant sentinels are unchanged through apply/deactivate.
    - Test 4: a forced-managed known plain scalar remains delegated to the Regenerator, preserving the valid override path.
  </behavior>
  <action>
    Make `ir.Build` copy the three structural markers and set the known structural-fidelity state for every `model.Block` into the corresponding `model.Entry`, alongside the existing defensive copies and ValueMode/ListValue contract. Add shell-agnostic fixtures for append assignment, array assignment, and flagged alias so current parser output reaches persistence with a complete fidelity contract even when auto-routing marks the entry imperative.

    Change `ir.Regenerate` and the manifest-entry gate in `activate.Build` to consult the single shared predicate from Task 1 rather than any declaration-specific condition. A structurally unknown legacy entry and every known forced-managed append, array, flagged alias, or existing nonrepresentable declaration must emit exactly `Entry.Text` and produce no scalar/list manifest operation. Preserve source order, the valid forced-managed plain-scalar path, and the no-shell-import boundary.

    Before changing the dynamic PATH/FPATH reducer, extend the existing pipeline harness with the Task 1 direct raw JSON compatibility matrix. Each absent, single-marker-partial, and unsupported-version row must run raw JSON → UnmarshalProfile → MarshalProfile → UnmarshalProfile → Regenerate → Build → Diff → Emit → `zsh -f` against distinctive pre-existing scalar, array, and alias sentinels. Assert that re-save still has no fidelity object, regeneration remains exactly `Entry.Text`, the manifest contains no scalar/list/name operation for that source form, and each sentinel is identical both after apply and after deactivate.

    Add the separate current parser-backed matrix that the historical-DTO tests cannot prove: parse `FOO+=bar`, an array assignment such as `plugins=(git zsh-autosuggestions)`, and a flagged alias such as `alias -g G='| grep'`; then run Parse → `ir.Build` → DTO marshal/unmarshal → set OverrideManaged → Regenerate → Build → Diff → Emit → `zsh -f` for each. Assert byte-for-byte verbatim regeneration, zero scalar/list/name manifest operations as applicable, no overwrite of the targeted scalar, array, or alias sentinel during apply, and exact live-state restoration after deactivate. The current cases are required even though auto-routing calls them imperative: they prove stored known markers survive the persistence boundary before a forced override is considered. Retain `export PATH+=:/x` as the additional current append case. Do not use parser-only coverage as a replacement for the raw historical matrix, and do not use JSON-only coverage as a replacement for these parser-backed cases.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/ir ./core/activate ./core/shell/zsh -run 'Test(Build.*(Structural|Managed|Copies|Represent)|Regenerate.*(Represent|Declaration|SourceOrder|Legacy)|Pipeline(ForcedManagedStructuralShapes|LegacyStructuralFidelityMatrix|PersistedOverrideManaged))$' -v</automated>
  </verify>
  <acceptance_criteria>
    Current parser state and omitted historical DTO state both reach the same representability gate, so neither can be lowered to an operation that changes source semantics.
  </acceptance_criteria>
  <done>The full raw compatibility matrix and every current parser-backed forced-managed append, array, and flagged-alias shape have a proven verbatim/no-operation path through live zsh before later runtime list behavior is exercised; valid plain declarations retain their existing behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve legacy list dynamic provenance after the compatibility proof</name>
  <files>core/activate/builder.go, core/activate/builder_test.go, core/shell/zsh/pipeline_test.go, core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/activate/builder.go — `pathDelta`, `composeLegacyList`, `composeList`, and the manifest-entry gate.
    - core/activate/builder_test.go — canonical list reduction and reject-case coverage.
    - core/shell/zsh/emit.go — `renderListDelta` consumes explicit AdditionDynamic metadata and is the only zsh renderer.
    - core/shell/zsh/pipeline_test.go and core/shell/zsh/residue_test.go — DTO-to-zsh harnesses and snapshot cleanup conventions, including the completed legacy-DTO safety fixtures from Task 2.
    - .planning/phases/04-manifest-builder-emit/04-REVIEW.md and 04-VERIFICATION.md — CR-01, CR-02, and the exact missing live cases.
  </read_first>
  <behavior>
    - Test 1: legacy PATH and FPATH values with `$HOME/bin` and `$EXTRA/bin` produce `AdditionDynamic` entries aligned one-for-one with additions; mixed legacy/semantic input retains original source order.
    - Test 2: the legacy reducer still rejects command substitution, backticks, separators, whitespace/control characters, malformed unsafe list forms, cross-list references, and multiple/middle self references; it does not newly reject safe existing literal additions.
    - Test 3: Parse → DTO marshal/unmarshal → Build → Diff → Emit under `zsh -f` expands runtime HOME/EXTRA rather than retaining literal parameter text, then deactivation restores PATH and FPATH byte-for-byte.
    - Test 4: the Task 1 legacy DTO compatibility matrix and the Task 2 current parser-backed forced-managed append, array, flagged-alias, and `export PATH+=:/x` cases remain in the full live-zsh regression selection while dynamic list cases are added.
  </behavior>
  <action>
    In `core/activate`, keep the legacy compatibility grammar and source-order reducer established by 04-14, but derive and carry per-addition dynamic provenance for the safe simple-parameter forms already intended for late binding (including `$HOME/bin`, `$EXTRA/bin`, and their accepted braced equivalents). Use a strict shell-agnostic classifier: mark only simple parameter-reference source with literal path text as dynamic; preserve existing safe literal additions as literal; and continue rejecting arbitrary-evaluation-shaped input rather than evaluating, normalizing, or importing the zsh emitter. `composeLegacyList` must transfer each `AdditionDynamic` bit into its canonical token so mixing semantic and legacy statements preserves source order and dynamic cardinality. Make `Build` call the shared representability predicate from Task 1 before it creates any scalar/list manifest operation.

    Add builder-level source tests for exact addition ordering and provenance bits, including PATH and FPATH, dynamic legacy additions, semantic/legacy mixtures in both source orders, ordinary literal compatibility, and the existing invalid/evaluation-shaped rejection set. Do not broaden the legacy grammar or use shell evaluation to classify values.

    Extend the existing persistence-to-live-zsh pipeline tests rather than introducing a parallel harness. Set HOME and EXTRA only in the `zsh -f` runtime script to values that differ from their source spelling; compare emitted activation with direct-source behavior, assert the actual expanded PATH/FPATH contains no literal parameter text, and assert deactivation restores the original bases. Keep the Task 1 direct legacy-JSON compatibility matrix and every Task 2 current parser-backed forced-managed append, array, flagged-alias, and `export PATH+=:/x` case in this full selection; do not weaken their regenerate, no-operation, or sentinel-preservation assertions while adding the dynamic list cases. Extend the residue regression with the persisted dynamic legacy profile so snapshot equality proves no list residue after the real emitter path. Per D-10/D-11, do not add zsh strings outside `core/shell/zsh/emit.go`; the existing emitter is validated as the consumer of corrected metadata.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/activate ./core/shell/zsh -run 'Test(Build.*(Legacy|Semantic|Represent)|Pipeline(StoreRoundTripLegacy|PersistedOverrideManaged)|ResidueStoreRoundTripLegacy)$' -v && GOTOOLCHAIN=auto go test -count=1 ./... && GOTOOLCHAIN=auto go build ./...</automated>
  </verify>
  <acceptance_criteria>
    Dynamic legacy PATH/FPATH additions remain dynamic across canonical reduction and DTO storage, source order survives mixed input, arbitrary evaluation remains rejected, and the earlier legacy-DTO/current-append safety cases stay verbatim with zero manifest intent and no live overwrite.
  </acceptance_criteria>
  <done>Both verified blockers are closed by source-level tests and real `zsh -f` Parse → DTO → Build/Diff/Emit regressions that distinguish expansion from literal text and append from overwrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| parsed profile → persisted DTO | Source-derived syntax crosses into data that must retain enough structure for later safe decisions. |
| persisted profile → manifest builder | A forced override could otherwise turn incomplete syntax into emitted declarative intent. |
| manifest plan → sourced zsh code | Late-bound list source becomes code in the user's shell; metadata must distinguish validated parameter expansion from literal data. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04-15-01 | Tampering / Elevation of Privilege | legacy PATH/FPATH compatibility reducer | high | mitigate | Preserve dynamic provenance only for strict simple-parameter forms, retain the no-evaluation rejection boundary, and run emitted output under `zsh -f` with parameter values that expose literal-freezing defects. |
| T-04-15-02 | Tampering | historical Profile DTO → IR regeneration and manifest build | high | mitigate | Use a presence-aware, versioned marker object; missing, partial, or unsupported historical marker state remains unknown when re-saved and is therefore verbatim/no-operation. Pipeline tests run re-saved append, array, and flagged-alias legacy JSON through live zsh. |
| T-04-15-03 | Repudiation | regression evidence | medium | mitigate | Keep focused source tests plus DTO-to-live-zsh tests in the default Go suite so the exact accepted input and observed shell behavior are reproducible. |
| T-04-15-SC | Tampering | package supply chain | low | accept | This plan adds no dependency or package-manager installation; no package legitimacy decision is introduced. |
</threat_model>

<verification>
Run Task 1's persistence command, then complete Task 2's legacy-DTO safety proof before changing Task 3's dynamic reducer. Finish with `GOTOOLCHAIN=auto go test -count=1 ./...` and `GOTOOLCHAIN=auto go build ./...`. The live tests must use `zsh -f`, set runtime HOME/EXTRA explicitly for dynamic-list cases, compare direct-source and emitted behavior, and compare pre/post snapshots after deactivate.
</verification>

<success_criteria>
- SW-01: No persisted forced-managed append, array, flagged alias, or declaration command is lowered to a different manifest operation; omitted historical marker state remains conservatively verbatim/no-operation after a DTO re-save, while ordinary known representable declarations still work.
- SW-01: A persisted legacy `$HOME/bin` or `$EXTRA/bin` addition carries dynamic provenance into `ListDelta.AdditionDynamic`, while unsafe evaluation-shaped legacy text remains outside the managed list path.
- SW-02: Runtime tests show mixed legacy/semantic PATH and FPATH retain source order, expand against runtime values rather than literal parameter text, and deactivate to the captured base with no residue.
- Existing full workspace tests and build remain green without changing Phase 5 loader scope.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-15-SUMMARY.md` when done.
</output>
