---
phase: 04-manifest-builder-emit
plan: 06
type: execute
wave: 6
depends_on: ["04-05"]
gap_closure: true
files_modified:
  - core/model/manifest.go
  - core/model/manifest_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/activate/plan.go
  - core/activate/diff.go
  - core/activate/diff_test.go
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/zsh/pipeline_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "Repeated statements for one scalar, alias, function, or option merge by final effective shell identity with last-write-wins semantics; punctuation-distinct legal names remain separate identities."
    - "The normalized output is deterministic: ordered scalar/function/option records appear once by final source occurrence, while map-backed aliases retain deterministic lexicographic exact-key semantics; empty input yields no operations, a single entry remains unchanged, and present-empty data remains present."
    - "Runtime undo state uses an injective identity encoding and separate presence state; no user value, including __ZP_UNSET__, can be confused with absence."
    - "The original scalar/shadow is captured once, the scalar applied slot is refreshed after every write, and deactivation emits one restore sequence per final identity."
    - "Scalar export provenance is additive across repeated declarations: any managed export transition makes the final profile-owned scalar exported, plain assignments never clear a pre-existing export attribute, and deactivation restores the exact prior present/unset plus exported/unexported state."
    - "ValueMode fail-closed behavior, byte-for-byte Entry.Value/Text regeneration, and the core/activate shell-agnostic boundary remain unchanged."
  assumptions:
    - statement: "SW-02 unclassified edge-probe item remains flagged for manual verification against the final real-zsh oracle in 04-12."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/activate/builder.go"
      provides: "effective-identity reducers for repeated scalar/function/option declarations, including additive scalar export provenance"
      contains: "Build"
    - path: "core/model/manifest.go"
      provides: "backward-compatible explicit scalar export provenance"
      contains: "Exported"
    - path: "core/shell/zsh/emit.go"
      provides: "injective undo-slot keying, explicit presence slots, and final-applied scalar ownership"
      contains: "encodeSlot"
    - path: "core/shell/zsh/pipeline_test.go"
      provides: "source-to-live restoration regressions for repeated and colliding identities"
      contains: "Repeated"
  key_links:
    - from: "core/activate/builder.go"
      to: "core/activate/diff.go"
      via: "one manifest record and one deactivate operation per final effective identity"
      pattern: "Functions.Added"
    - from: "core/activate/diff.go"
      to: "core/shell/zsh/emit.go"
      via: "identity-unique operations key collision-free runtime slots"
      pattern: "encodeSlot"
    - from: "core/model.Scalar.Exported"
      to: "core/activate.SetScalar.Exported"
      via: "Diff carries explicit plain-versus-export activation provenance without source-text inference"
      pattern: "Exported"
---

<objective>
Correct the common restoration root cause: activation owns final effective shell identities, not source statements or lossy sanitized slot names.

Purpose: repeated declarations, legal punctuation collisions, and sentinel-valued prior state must apply and reverse exactly without stale or ambiguous undo state.
Output: deterministic identity reducers, collision-free presence-aware slots, and live-zsh regression tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-05-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: final effective shell identity (canonical variable/list identity or exact alias/function/option name), not a source statement.
Decision: promote.
Rationale: per-statement bookkeeping is input history only; manifest ownership, undo capture, drift comparison, and restoration must key by collision-free final identities. Task 1 and Task 3 encode this as an invariant.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce repeated declarations to deterministic final identities</name>
  <files>core/model/manifest.go, core/model/manifest_test.go, core/activate/builder.go, core/activate/builder_test.go, core/activate/plan.go</files>
  <read_first>
    - core/activate/builder.go (current append-per-statement behavior)
    - core/model/manifest.go (wire shapes that must remain compatible)
    - core/model/manifest_test.go (legacy manifest fixture and additive-field omission conventions)
    - core/model/profile.go (Entry.Exported source provenance)
    - core/activate/plan.go (SetScalar operation contract)
    - core/activate/builder_test.go (Build table-test conventions)
  </read_first>
  <behavior>
    - RED: two scalar assignments to one name produce one final Scalar carrying the second semantic value and provenance.
    - RED: duplicate function declarations produce one Functions.Added identity and the final body; duplicate option toggles produce one final option state.
    - RED: foo-bar, foo.bar, and foo_bar remain three distinct alias/function identities.
    - RED: final identities are stable by final source occurrence, while empty and single-entry profiles preserve the obvious empty/single result.
    - RED: plain/plain remains plain, plain/export/plain remains exported, and an explicit false export field survives JSON distinctly from an absent legacy field.
  </behavior>
  <action>
    Implement a source-order effective-state reduction before manifest parts are finalized. For scalar, function, and option identities, replace an earlier declaration with the later semantic value and move that identity to its final source-occurrence position; aliases already have map last-write semantics, retain exact distinct keys, and are emitted in deterministic lexicographic key order rather than claiming source order that the map cannot represent. Keep class namespaces distinct and canonicalize only true shell aliases such as PATH/path in the list-specific follow-up plan. Do not merge names merely because their old sanitized slot spelling matched.

    Add optional `Exported *bool` provenance to model.Scalar and `Exported bool` to activate.SetScalar. Newly built scalars always carry explicit pointer presence. Fold repeated scalar declarations with the value from the final declaration but export provenance as a shell-state transition: OR every managed Entry.Exported for that identity, because a later plain assignment does not clear an earlier export attribute. Diff propagates explicit provenance; absent legacy provenance retains the historical export-on-apply behavior. Preserve the byte-identical legacy fixture when the field is absent and reject malformed contracts before returning operations.

    Preserve SchemaV1 compatibility, explicit Dynamic provenance, function body map-key presence, and all fail-closed Unsupported behavior. Add the assumption-delta invariant at the manifest boundary: every ordered state-class/name identity occurs at most once before Diff consumes it.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/model ./core/activate -run 'Manifest|Build|Repeated|Identity|Collision|Empty|Ordering|Export' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Repeated scalar/function/option entries reduce to one final identity with last-write-wins semantics.
    - Scalar export provenance is explicit for new manifests, backward compatible when absent, and additive across repeated declarations.
    - foo-bar, foo.bar, and foo_bar never merge.
    - Empty input produces no parts/ops; single input produces exactly one; present-empty bodies remain represented.
    - core/activate imports no concrete shell package and emits no zsh source.
  </acceptance_criteria>
  <done>Manifest and Plan represent effective identities exactly once, preserving deterministic ordered-record occurrence and exact-key alias map semantics while carrying scalar export intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make runtime undo slots injective and presence-aware</name>
  <files>core/activate/diff.go, core/activate/diff_test.go, core/shell/zsh/emit.go, core/shell/zsh/emit_test.go</files>
  <read_first>
    - core/shell/zsh/emit.go (sanitizeSlot, runtimeHelpers, scalar/alias/function capture and restore)
    - core/shell/zsh/emit_test.go (syntax, live-zsh, and canary patterns)
    - core/activate/diff.go (scalar provenance propagation and duplicate legacy operation construction)
    - core/activate/diff_test.go (operation-order and schema-gate conventions)
    - .planning/phases/01-spike-zero-residue-live-hot-switch/01-FINDINGS.md (live-prior and drift-guard contract)
  </read_first>
  <behavior>
    - RED: slot keys for foo-bar, foo.bar, foo_bar, empty, and multibyte bytes are distinct valid zsh identifiers.
    - RED: a prior scalar, alias body, or function body exactly equal to __ZP_UNSET__ restores as data.
    - RED: original absence restores absence through a separate presence flag.
    - RED: repeated SetScalar updates the applied-value slot to the final live owned value while retaining the first original capture.
    - RED: unset, present-empty unexported, present-empty exported, and non-empty exported priors restore both bytes and export attribute exactly after plain/exported/repeated transitions.
    - RED: Diff defensively emits one deactivate pair per identity even for legacy/manually assembled duplicate function arrays.
  </behavior>
  <action>
    Replace sanitizeSlot with an injective byte encoding suitable for zsh identifiers, such as a fixed prefix plus lowercase hex of every UTF-8 byte. Keep state-class prefixes separate. Represent prior presence independently from prior value for scalars, aliases, and functions; remove every data sentinel from absence decisions. For scalars also capture prior export-attribute state in a separate first-write-only slot derived from zsh parameter metadata, never from value presence. Capture original presence/value/export state only once, but unconditionally refresh the applied-value slot after each successful SetScalar so the drift guard compares live state with the profile's final owned value.

    Apply SetScalar according to explicit provenance: exported entries use export assignment; plain entries use a global plain assignment that does not introduce export for a previously unset/unexported variable and does not clear an already exported variable, matching direct zsh assignment semantics. Deactivation restores exact prior value/presence first and then exact prior export attribute for present variables; an originally absent scalar is unset regardless of profile provenance. Use zsh-native attribute operations with validated names and cover unset/present-empty/non-empty plus repeated plain/export/plain transitions under disposable zsh -f.

    Defensively normalize active and target manifest identity arrays inside Diff before validation and operation construction so legacy or hand-built duplicate Functions.Added entries cannot produce repeated unset/restore pairs. Diff must also propagate Scalar.Exported into SetScalar.Exported without source inference. Preserve full-deactivate-before-activate ordering and require every operation list to contain at most one ownership-changing sequence for each final state-class/name identity.

    Deactivate must restore or remove based on the explicit captured-presence bit, then clean every per-switch presence/value/applied slot. Keep ZP_BASE_PATH/FPATH session-persistent. Preserve safe name validation, plain functions, quoteFunctionBody, renderValue, and sole reverse-zsh ownership in this file.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/activate ./core/shell/zsh -run 'Diff|Duplicate|Identity|Emit|Slot|Sentinel|RepeatedScalar|Shadow|Collision|Drift|Export' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Slot encoding is injective for every accepted name and emits only valid identifier bytes.
    - Absence is never represented by a user-reachable string value.
    - Repeated SetScalar preserves the first prior and records the final applied value.
    - Plain and exported application match direct zsh attribute behavior, and prior export state restores exactly and independently of value/presence.
    - Diff produces at most one function unset/restore pair per exact name even for a duplicate legacy manifest.
    - Alias and function punctuation-collision shadows restore independently and sentinel-valued bodies round-trip exactly.
    - Per-switch value/presence/applied slots are removed after deactivate.
  </acceptance_criteria>
  <done>Undo state is collision-free, data-independent, final-value-aware, and fully cleaned after restoration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove identity ownership through the real source-to-live pipeline</name>
  <files>core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/shell/zsh/pipeline_test.go (Parse to IR to Build to Diff to Emit harness)
    - core/shell/zsh/emit.go (final helper/slot contract from Task 2)
    - core/activate/builder.go (final identity reducer from Task 1)
  </read_first>
  <behavior>
    - RED then GREEN: source with FOO=one followed by FOO=two applies two and deactivates to the exact original.
    - RED then GREEN: duplicate function source applies only the final body and restores one pre-existing function exactly.
    - RED then GREEN: colliding legal alias/function names restore all independent prior bodies.
    - RED then GREEN: prior scalar/alias/function data equal to the former sentinel survives apply/deactivate unchanged.
    - RED then GREEN: exported, unexported, unset, and present-empty scalar baselines survive plain/exported/repeated source transitions with exact value and attribute restoration.
  </behavior>
  <action>
    Extend the disposable zsh -f production pipeline test with all independently reproduced identity failures, including scalar export-attribute transitions. Start from real source bytes and traverse Provider.Parse, ir.Build, activate.Build, activate.Diff, and Provider.Emit; never hand-correct operations. Capture exact prior and final values/bodies plus parameter export attributes using collision-safe qqqq/file output. Assert the final manifest/plan identity uniqueness as well as live apply/deactivate behavior. Do not touch or source user shell configuration.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Pipeline.*(Repeated|Collision|Sentinel|Duplicate|Export|Empty)|PipelinePreserves' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - All four identity break cases pass through the production pipeline under zsh -f.
    - Exported/unexported/unset/present-empty scalar baselines and repeated transitions pass through the same production pipeline.
    - The test asserts both final applied behavior and exact restoration, not only absence of a crash.
    - No user shell file or parent-shell state is read or changed.
  </acceptance_criteria>
  <done>Final-identity ownership is encoded as a source-to-live invariant and the known scalar/shadow failures are closed.</done>
</task>

</tasks>

<context_budget>
The ten-file plan is one identity contract split into isolated executor subsets: Task 1 owns five model/builder contract files, Task 2 owns four Diff/emitter files, and Task 3 owns only the production pipeline test. No task modifies more than five files, and Task 3 consumes the prior two contracts without reopening their implementation scope.
</context_budget>

<threat_model>
## ASVS Context

ASVS level 1; critical/high and primary-boundary medium threats are mitigated. Blocking threshold: high.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-04-06-01 | Tampering | undo-slot key derivation | high | mitigate | injective byte encoding plus exact collision-pair tests |
| T-04-06-02 | Tampering | absence/value representation | high | mitigate | separate presence and value state; no sentinel data value |
| T-04-06-03 | Tampering | repeated scalar ownership | high | mitigate | first-original/final-applied contract and live-zsh regression |
| T-04-06-05 | Tampering | scalar export attribute | high | mitigate | explicit additive provenance plus independent prior-attribute capture and exact live-zsh restoration matrix |
| T-04-06-04 | Elevation of Privilege | emitted shell names | medium | mitigate | retain conservative accepted-name validation before any command-position emission |
| T-04-06-SC | Tampering | dependency supply chain | low | accept | no package installs or dependency changes |
</threat_model>

<prohibition_recall>
No bespoke values/safety prohibition survived precision filtering. Slot collisions, sentinel ambiguity, ordering, and cleanup are concrete correctness edges covered above; shell injection is canon security handled by this threat model and later $gsd-secure-phase, so it is not duplicated as a descriptor-less prohibition.
</prohibition_recall>

<verification>
- GOTOOLCHAIN=auto go test ./core/activate ./core/shell/zsh -count=1
- GOTOOLCHAIN=auto go test ./... -count=1
- GOTOOLCHAIN=auto go build ./...
- Reverse-zsh source remains confined to core/shell/zsh/emit.go.
</verification>

<success_criteria>
- Every final scalar/function/option identity is represented once with last-write semantics.
- Undo slots are injective and presence-aware; former-sentinel data and punctuation-collision shadows restore exactly.
- Repeated scalar applied ownership follows the final write without losing the original.
- Plain/exported application and prior scalar export attributes match direct zsh semantics across unset, empty, and repeated transitions.
- The assumption-delta identity invariant passes in unit and live-zsh tests.
</success_criteria>

<artifacts_this_phase_produces>
- encodeSlot collision-free helper replacing lossy sanitizeSlot use.
- Effective-identity reduction helpers and defensive Diff normalization.
- Scalar.Exported and SetScalar.Exported provenance plus presence/value/export runtime slots.
- Source-to-live repeated/collision/sentinel/duplicate regression cases.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-06-SUMMARY.md` when done.
</output>
