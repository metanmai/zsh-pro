---
phase: 04-manifest-builder-emit
plan: 16
type: execute
wave: 13
depends_on: [04-15]
files_modified:
  - core/model/block.go
  - core/model/profile.go
  - core/model/profile_test.go
  - core/store/dto.go
  - core/store/dto_test.go
  - core/ir/build.go
  - core/ir/build_test.go
  - core/ir/regen.go
  - core/ir/regen_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/shell/zsh/parse.go
  - core/shell/zsh/parse_test.go
autonomous: true
gap_closure: true
requirements: [SW-01]
must_haves:
  truths:
    - "Numeric and associative indexed assignments, and semantic export attributes, retain explicit parser-owned source shape rather than becoming ordinary scalar/export data."
    - "Only a complete current structural-fidelity DTO is known; absent, v1, partial, or unsupported records remain unknown after a re-save."
    - "A forced managed override cannot make an unknown, indexed, or declaration-flagged entry template or lower into a manifest operation."
    - "Ordinary unflagged exports, including the `export -- NAME=value` delimiter form, retain their supported exported-scalar path."
  artifacts:
    - path: core/model/block.go
      provides: "Parser-owned indexed and declaration-flag source-shape metadata."
    - path: core/model/profile.go
      provides: "Presence-aware representability gate shared by regeneration and manifest construction."
    - path: core/store/dto.go
      provides: "Current-version, presence-aware structural-fidelity persistence."
    - path: core/shell/zsh/parse.go
      provides: "Subscript and semantic declaration-flag capture at every assignment-bearing parser path."
  key_links:
    - from: core/shell/zsh/parse.go
      to: core/store/dto.go
      via: "Indexed and declaration-flag metadata is copied into Entry, encoded only as a complete current record, and decoded unknown otherwise."
      pattern: "Indexed|DeclarationFlags|StructuralFidelity"
    - from: core/model/profile.go
      to: core/ir/regen.go
      via: "Representable selects verbatim Entry.Text for rejected source shapes even after OverrideManaged."
      pattern: "Representable"
    - from: core/model/profile.go
      to: core/activate/builder.go
      via: "The same predicate excludes rejected entries before scalar/list/name manifest lowering."
      pattern: "Representable"
---

<objective>
Create the complete source-fidelity and shared representability boundary required to close the indexed-assignment and semantic-export-flag loss paths found by review.

Purpose: satisfy SW-01 before any value can cross Profile to Manifest by preserving behavior-bearing syntax and failing closed when the locked manifest model cannot represent it.
Output: parser metadata, a v2 presence-aware DTO compatibility contract, shared IR/builder no-operation gating, and focused source-layer tests. Plan 17 consumes this contract for persisted live-zsh and residue proof.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-PATTERNS.md
@.planning/phases/04-manifest-builder-emit/04-15-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
</context>

## Dependency graph

| Task | Needs | Creates | Checkpoint |
| --- | --- | --- | --- |
| 1 | 04-15 structural-fidelity boundary and current parser assignment loops | Parser-owned indexed and declaration-flag contract | none |
| 2 | Task 1 source-shape contract | DTO v2 compatibility matrix and shared representability gate | none |

Task 2 follows Task 1 because it persists and consumes the parser metadata. Plan 17 is Wave 14 and depends on this plan, so it can exercise the finished contract without overlapping files.

## Source coverage audit

| Source | ID | Required outcome | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Profiles reach emitted shell state without semantic loss or residue. | 16 Task 2; 17 Tasks 1-2 | COVERED |
| REQ | SW-01 | Only faithfully representable declarative state crosses the emitted-shell boundary. | 16 Tasks 1-2; 17 Task 1 | COVERED |
| REQ | SW-02 | Rejected shapes leave live state and switch residue untouched. | 17 Tasks 1-2 | COVERED |
| RESEARCH | Responsibility map | Parser owns AST shape; model/store/IR/activate stay shell-agnostic; emitter remains sole zsh renderer. | 16 Tasks 1-2; 17 Tasks 1-2 | COVERED |
| RESEARCH | D-05/D-06/D-13 constraints | Do not promote syntax whose semantics are unavailable; preserve dynamic data structurally without evaluation. | 16 Tasks 1-2 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-04 | Preserve the Manifest wire contract and keep Profile persistence in the store DTO. | 16 Task 2 | COVERED |
| CONTEXT | D-05/D-06/D-07 | Keep builder shell-agnostic, use EffectiveManaged plus fidelity gate, and do not invent indexed list behavior. | 16 Task 2; 17 Task 1 | COVERED |
| CONTEXT | D-08/D-09 | Rejected entries create empty manifest intent and traverse the existing agnostic Diff/Plan behavior. | 16 Task 2; 17 Task 1 | COVERED |
| CONTEXT | D-10/D-11/D-12/D-13 | Keep renderer/runtime semantics unchanged; consume existing emitter and preserve structural, non-evaluated source data. | 16 Task 2; 17 Task 1 | COVERED |
| CONTEXT | D-14/D-15/D-16/D-17 | Reuse the existing complete state snapshot and durable default-suite residue oracle. | 17 Task 2 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture subscripts and semantic declaration flags in every parser assignment path</name>
  <files>core/model/block.go, core/shell/zsh/parse.go, core/shell/zsh/parse_test.go</files>
  <read_first>
    - core/model/block.go — current parser-owned Append, Array, and alias Flagged fields.
    - core/shell/zsh/parse.go — pure assignment, declaration CallExpr, and DeclClause assignment loops.
    - core/shell/zsh/parse_test.go — append/flag capture and declaration-path table-test patterns.
    - /home/metanmai/go/1.25.0/pkg/mod/mvdan.cc/sh/v3@*/syntax/nodes.go — installed AST fields for `Assign.Index` and declaration options.
    - .planning/phases/04-manifest-builder-emit/04-VERIFICATION.md — numeric/associative and export-attribute failure traces.
  </read_first>
  <behavior>
    - Test 1: `FOO[2]=bar` and `MAP[key]=bar` record an indexed source shape while retaining name, text, and value; plain `FOO=bar` records explicitly known-unindexed shape.
    - Test 2: `export -i FOO=1` retains its semantic declaration flag sequence, while `export FOO=1` and `export -- FOO=1` retain the ordinary-export shape.
    - Test 3: every installed-AST assignment-bearing path either captures the required shape or produces an opaque/nonrepresentable result; no unavailable field becomes known false.
  </behavior>
  <action>
    Add parser-owned source-shape fields to `model.Block`: an Indexed marker for any non-nil assignment index, and declaration-attribute metadata that retains actual semantic option words in source order. Keep alias Flagged distinct from declaration metadata. This implements D-05/D-06's parser responsibility boundary and D-13's requirement that source is never evaluated.

    In `zsh.Provider.describe`, inspect `Assign.Index` at each pure-leading assignment, declaration-like CallExpr, and DeclClause assignment loop. Any numeric or associative subscript makes the Block indexed; preserve existing Text, Names, Value, Append, Array, ValueMode, and ListValue rather than decoding a subscript or substituting a scalar/list operation. Collect real declaration attribute options from the installed AST. Treat `--` only as an end-of-options delimiter, not an attribute, and retain subsequent ordinary assignments. When an AST form cannot expose the required source shape, route it to opaque/nonrepresentable behavior rather than setting false metadata. Do not overblock plain `export` or alter alias-flag behavior.

    Extend parser table tests with numeric and associative subscript rows, ordinary assignment controls, semantic `export -i`, delimiter `export --`, and ordinary export. Assert source metadata as well as names/text/value, and name the exercised AST declaration path so future parser upgrades cannot silently bypass capture.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'TestParse(Captures|Retains)' -v</automated>
  </verify>
  <done>Every assignment-bearing parser path records index and semantic declaration-flag information or fails conservatively; only actual declaration flags are retained, while delimiter and unflagged exports remain ordinary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist complete fidelity and share the fail-closed regeneration and builder gate</name>
  <files>core/model/profile.go, core/model/profile_test.go, core/store/dto.go, core/store/dto_test.go, core/ir/build.go, core/ir/build_test.go, core/ir/regen.go, core/ir/regen_test.go, core/activate/builder.go, core/activate/builder_test.go</files>
  <read_first>
    - core/model/profile.go — StructuralFidelityKnown, DeclarationRepresentable, and Representable.
    - core/store/dto.go and dto_test.go — optional fidelity object and Plan 15 compatibility matrix.
    - core/ir/build.go and regen.go — Block-to-Entry copying and verbatim/template selection.
    - core/activate/builder.go and builder_test.go — predicate before scalar/list lowering.
    - core/shell/zsh/emit.go — confirm it only consumes manifest operations; do not add renderer behavior outside it per D-10/D-11.
  </read_first>
  <behavior>
    - Test 1: a parser-backed current Entry round-trips known values for Append, Array, alias Flagged, Indexed, and declaration flags; a complete empty declaration-flag list is distinct from absent metadata.
    - Test 2: no fidelity object, complete v1 metadata missing new fields, each partial v2 field, absent declaration flags, and unsupported version all remain unknown through Unmarshal -> Marshal -> Unmarshal and write no fidelity object on re-save.
    - Test 3: known indexed or declaration-flagged entries remain nonrepresentable under OverrideManaged, regenerate exactly Entry.Text, and create no scalar/list/name operation; an unflagged export remains an exported scalar.
    - Test 4: IR copies new parser fields without slice aliasing so metadata survives Parse to DTO.
  </behavior>
  <action>
    Extend `model.Entry` with Task 1's metadata and make Representable require a complete current structural-fidelity record before existing shape guards. An indexed assignment and any semantic declaration flag must be nonrepresentable regardless of command name, numeric versus associative subscript, or OverrideManaged. Preserve current append, array, alias Flagged, declaration-command exclusions, and the one-name unflagged exported-scalar positive path. This implements D-05/D-06: override selects managed intent but cannot erase behavior-bearing source semantics.

    Advance the store-local fidelity schema to a presence-aware v2 record. Persist Indexed and declaration-flag metadata alongside Append, Array, and alias Flagged; distinguish a complete empty flag sequence from absent metadata, clone slices at both DTO boundaries, and retain exact order. Decode only a complete supported v2 record as known. Decode missing, complete legacy v1, every partial v2 row, absent declaration flags, and unsupported versions as unknown. On unknown decode, re-save without any fidelity object; never manufacture current false or empty values. Keep DTO ownership in `core/store` and leave the Manifest wire schema untouched per D-01 through D-04.

    Copy the fields in `ir.Build` without aliasing. Use the one shared Representable predicate in regeneration and `activate.Build`: unknown, indexed, or declaration-flagged entries regenerate verbatim and create no scalar/list/name intent; add neither a Manifest operation nor an emitter special case. Add focused model, DTO, IR, and builder tests for current known data, the entire compatibility/re-save matrix, forced-managed numeric/associative indexed rows, forced-managed `export -i`, and supported unflagged export controls.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/model ./core/store ./core/ir ./core/activate -run 'Test(Entry.*Represent|StructuralFidelityDTOCompatibilityMatrix|RoundTrip.*(Structural|OverrideManaged)|Build.*(Structural|OverrideManaged|Represent)|Regenerate.*(Structural|Declaration))$' -v</automated>
  </verify>
  <done>The DTO defines known source fidelity only for complete v2 data, and the shared regeneration/builder gate makes forced indexed and semantic flagged-export entries verbatim/no-operation while keeping ordinary exports supported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| zsh source -> parser -> Profile DTO | Owner-authored subscripts and declaration attributes become persisted structural metadata. |
| persisted Profile -> IR/manifest builder | A stored override can attempt to promote incomplete source data into scalar/list intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04-16-01 | Tampering | parse.go / Block / Entry | high | mitigate | Capture `Assign.Index` and semantic declaration flags at every assignment-bearing parser path; unavailable AST shape is opaque. |
| T-04-16-02 | Tampering | dto.go structural fidelity | high | mitigate | Require every v2 presence-aware marker, preserve empty-versus-absent flags, and omit fidelity data on every unknown re-save. |
| T-04-16-03 | Elevation of Privilege / Tampering | regen.go and activate/builder.go | high | mitigate | Gate templating and manifest lowering through the shared Representable predicate so OverrideManaged cannot discard semantics. |
| T-04-16-SC | Tampering | dependency supply chain | low | accept | No dependency or package-manager install is introduced; only pinned Go modules and existing tests are used. |
</threat_model>

<verification>
Run both focused task commands, then `GOTOOLCHAIN=auto go test -count=1 ./core/model ./core/store ./core/ir ./core/activate ./core/shell/zsh`. Apply `gofmt` to all changed Go files. Plan 17 must run the persisted production-path and live-zsh proof after this source contract is complete.
</verification>

<success_criteria>
- All five fidelity dimensions—Append, Array, alias Flagged, Indexed, and declaration flags—are complete-and-known only in v2, otherwise unknown.
- No OverrideManaged indexed or semantic flagged-export Entry templates or becomes an Env, List, Alias, Function, or Option manifest operation.
- Ordinary unflagged exports, including delimiter form, preserve their exported-scalar behavior.
- Plan 17 has a concrete dependency on this plan for final persisted and residue proof.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-16-SUMMARY.md` when done.
</output>
