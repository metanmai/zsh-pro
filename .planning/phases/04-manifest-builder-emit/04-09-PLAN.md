---
phase: 04-manifest-builder-emit
plan: 09
type: execute
wave: 8
depends_on: ["04-06", "04-08"]
gap_closure: true
files_modified:
  - core/model/manifest.go
  - core/model/manifest_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/activate/plan.go
  - core/activate/diff.go
  - core/activate/diff_test.go
  - core/shell/zsh/emit.go
  - core/shell/zsh/emit_test.go
  - core/shell/zsh/pipeline_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "Build composes every repeated assignment to one canonical PATH or FPATH identity in source order and emits one final delta per list."
    - "The final delta records the exact base position plus explicit dynamic provenance for each addition; legacy manifests without additive metadata retain the old prepend behavior."
    - "Runtime base state records presence separately from bytes/elements, so an initially unset, present-empty, or non-empty PATH/FPATH is restored to that exact state; initially unset FPATH is a required live case."
    - "PATH/path and FPATH/fpath canonicalize only within their own identity; cross-variable bases were rejected upstream and can never become a manifest part."
    - "Emit reconstructs one tied-scalar PATH/FPATH assignment from ordered head/base/tail expressions, allowing dynamic additions to expand to zero, one, or many elements, and deactivate restores the captured base plus its presence state."
    - "Equal/repeated list identities merge deterministically by final source occurrence; empty profile/list input stays empty and a self-only assignment is a valid no-addition delta."
  assumptions:
    - statement: "SW-02 unclassified edge-probe item remains flagged until the combined PATH plus FPATH adversarial oracle in 04-12 passes."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/model/manifest.go"
      provides: "additive base-position and per-addition provenance fields on ListDelta"
      contains: "BaseIndex"
    - path: "core/activate/builder.go"
      provides: "symbolic source-order list composition keyed by final canonical identity"
      contains: "ListValue"
    - path: "core/shell/zsh/emit.go"
      provides: "exact scalar-context list rendering plus separate runtime base-presence capture/restoration"
      contains: "renderList"
    - path: "core/shell/zsh/pipeline_test.go"
      provides: "real quoted/repeated PATH and FPATH source-to-live tests"
      contains: "FPATH"
  key_links:
    - from: "core/model.Entry.ListValue"
      to: "core/model.ListDelta"
      via: "Build substitutes the current symbolic list at the Self marker in source order"
      pattern: "BaseIndex"
    - from: "core/model.ListDelta"
      to: "core/activate.ApplyListDelta"
      via: "Diff carries base position and per-addition dynamic flags without inference"
      pattern: "AdditionDynamic"
    - from: "core/activate.ApplyListDelta"
      to: "core/shell/zsh/emit.go"
      via: "renderList inserts captured base once into one tied-scalar expression and preserves dynamic expansion cardinality"
      pattern: "renderList"
    - from: "runtime ZP_BASE_PATH_PRESENT/ZP_BASE_FPATH_PRESENT"
      to: "RebuildListFromBase"
      via: "deactivation distinguishes exact unset from present-empty/non-empty base state"
      pattern: "BASE_.*_PRESENT"
---

<objective>
Compose semantic PATH/FPATH statements into one exact base-relative final identity and render it faithfully.

Purpose: quoted source and repeated assignments must preserve source-order shell behavior instead of resetting to base per statement or re-parsing raw spelling.
Output: additive list manifest metadata, symbolic builder composition, explicit plan fields, exact emitter rendering, and live-zsh integration tests.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-06-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-08-SUMMARY.md
@.planning/phases/01-spike-zero-residue-live-hot-switch/01-MANIFEST-SHAPE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend list manifest/plan metadata without breaking v1 JSON</name>
  <files>core/model/manifest.go, core/model/manifest_test.go, core/activate/plan.go, core/activate/diff.go, core/activate/diff_test.go</files>
  <read_first>
    - core/model/manifest.go (ListDelta v1 fields and additive-field conventions)
    - core/model/manifest_test.go (Phase 1 fixture shape and omission tests)
    - core/activate/plan.go (ApplyListDelta contract)
    - core/activate/diff.go (list operation construction)
  </read_first>
  <behavior>
    - RED: explicit base position zero, middle, and end survive JSON and Diff, including zero-valued position.
    - RED: false/true dynamic provenance aligns one-to-one with additions and survives JSON.
    - RED: legacy ListDelta without new fields decodes and retains base-after-additions behavior.
    - RED: malformed lengths or out-of-range base position fail target validation before any operation is returned.
  </behavior>
  <action>
    Add optional BaseIndex *int and AdditionDynamic []bool fields to model.ListDelta using additive omitempty JSON keys. Pointer presence distinguishes explicit zero from legacy absence. For new deltas, BaseIndex is the index among the final ordered additions where the captured base is inserted; AdditionDynamic has exactly one entry per addition. Keep Name, Additions, and Deletions unchanged so the validated Phase 1 fixture marshals byte-identically when additive fields are absent.

    Carry both fields onto ApplyListDelta. Diff validates new metadata atomically before constructing any deactivate/activate ops: BaseIndex in [0,len(Additions)], dynamic length either zero only for legacy or exactly len(Additions). Legacy absence maps to base after additions and may use the existing segment heuristic only for compatibility; new explicit metadata is authoritative. Preserve SchemaV1 and all non-list operations.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/model ./core/activate -run 'Manifest|ListDelta|BaseIndex|AdditionDynamic|Legacy|Diff' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Explicit BaseIndex zero is distinct from absent legacy metadata.
    - New dynamic metadata is length-aligned and authoritative.
    - Malformed target list metadata returns an empty Plan plus error.
    - Original fixture shape and existing ListDelta fields remain compatible.
  </acceptance_criteria>
  <done>The manifest/plan can express head/base/tail ordering and provenance without changing legacy v1 records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose repeated semantic list assignments into final state</name>
  <files>core/activate/builder.go, core/activate/builder_test.go</files>
  <read_first>
    - core/activate/builder.go (pathDelta raw split to replace; identity reducer from 04-06)
    - core/activate/builder_test.go (PATH precision and repeated-identity tests)
    - core/model/profile.go (ListValue/ListSegment from 04-08)
    - core/model/manifest.go (Task 1 metadata)
  </read_first>
  <behavior>
    - RED: PATH=/a:$PATH then PATH=/b:$PATH becomes /b,/a,base in one delta.
    - RED: PATH=/a:$PATH then PATH=$PATH:/b becomes /a,base,/b in one delta.
    - RED: PATH and FPATH sequences compose independently and canonical lowercase aliases share only their own list identity.
    - RED: self-only produces one zero-addition delta; nil ListValue produces no part.
    - RED: dynamic Source tokens retain scalar-expression order without eager splitting when their runtime value is empty or contains multiple colons.
  </behavior>
  <action>
    Delete activation's raw strings.Split pathDelta semantics for newly parsed entries. Maintain a symbolic sequence per canonical final list identity, initially one Base token. For each source-ordered Entry.ListValue, replace its Self token with the current symbolic sequence and carry literal Value or validated dynamic Source tokens around it exactly; never eagerly split dynamic source or assign it a one-element cardinality. The result becomes the current state for that identity. After all entries, emit one ListDelta per canonical PATH/FPATH identity, ordered by final source occurrence, with additions equal to non-base tokens, BaseIndex equal to the single Base position, and exact AdditionDynamic flags.

    Treat nil ListValue as unmodeled and emit no new list part; permit the documented narrow legacy path only for ValueModeLegacy entries so old tests/manifests remain usable. Never accept a cross-variable base or re-infer quote semantics here. Preserve list duplicates and order; do not apply typeset -U or deduplicate data elements.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/activate -run 'Build.*(Path|FPATH|Repeated|Head|Tail|Self|Legacy)|ListCompose' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Each canonical list identity produces at most one final ListDelta.
    - Head/head and head/tail examples yield exact symbolic ordering.
    - PATH and FPATH never share a base or composition state.
    - Nil semantic input fails closed; self-only is represented as a valid empty delta.
    - Data-element order and duplicates are preserved.
    - Empty and multi-colon dynamic values remain runtime scalar expansions rather than precomputed array elements.
  </acceptance_criteria>
  <done>Repeated list source statements reduce to one faithful final identity relative to exactly one captured base.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render exact composed PATH/FPATH and prove live behavior</name>
  <files>core/shell/zsh/emit.go, core/shell/zsh/emit_test.go, core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/shell/zsh/emit.go (renderList and dynamicSegment legacy heuristic)
    - core/shell/zsh/emit_test.go (list syntax/renderer tests)
    - core/shell/zsh/pipeline_test.go (production Parse to Emit zsh harness)
    - core/activate/plan.go (explicit ApplyListDelta metadata)
  </read_first>
  <behavior>
    - RED: explicit base at head/middle/end reconstructs exact ordered arrays for PATH and FPATH.
    - RED: new explicit false dynamic segments are zquoted even if their data contains dollar/backtick text; explicit true expands late-bound.
    - RED: fully quoted and mixed-quoted real source matches direct zsh evaluation.
    - RED: repeated PATH and FPATH assignments apply their composed state and deactivate to exact captured bases.
    - RED: `$EXTRA` with `/one:/two` and with an empty value matches direct source cardinality for PATH and FPATH.
    - RED: initially unset, present-empty, and non-empty bases restore exactly; the FPATH matrix includes an initially unset baseline.
  </behavior>
  <action>
    Change renderList to consume explicit BaseIndex and AdditionDynamic metadata and reconstruct one scalar-context assignment for PATH or FPATH with the captured base expression inserted exactly once at that position. Static additions are zquoted as data within that scalar expression; validated dynamic Source is emitted only at its explicit dynamic positions. Do not wrap a dynamic addition as one array element: allow zsh's tied scalar/array semantics to decide zero/one/many elements exactly as direct source does. Do not call dynamicSegment for new provenance; keep that heuristic only for legacy operations with absent metadata. Preserve sole zsh-syntax ownership in emit.go.

    Capture each runtime base once with two independent facts: a collision-safe presence bit from `${+PATH}`/`${+FPATH}` and exact base scalar/array bytes. Deactivation restores non-empty and present-empty bases as present, and unsets both tied scalar/array names only when the captured presence bit was false. Clean per-switch list state while retaining the explicitly session-persistent base snapshot contract already established by Phase 1.

    Extend focused emitter tests and the real pipeline test with fully quoted, mixed quoted, cross-variable rejection, head/head, head/tail, PATH plus FPATH, literal-dollar, dynamic-HOME, and `$EXTRA` multi-element/empty cases. Cover initially unset, present-empty, and non-empty bases, with initially unset FPATH mandatory. Compare applied arrays and scalar presence with direct zsh source behavior and then assert byte-identical base restoration. Use disposable zsh -f and no user shell files.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Emit.*List|Pipeline.*(Path|FPATH|Repeated|Quoted|Dynamic|Empty|Presence)|ListDelta|BasePresence' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Base is inserted exactly once at the explicit index for PATH and FPATH.
    - New provenance never falls back to string inspection.
    - Quoted/mixed and repeated assignments match direct zsh behavior.
    - Multi-element and empty dynamic expansions match direct scalar assignment for both list identities.
    - Deactivate restores exact PATH/FPATH presence plus ZP_BASE_PATH/ZP_BASE_FPATH bytes, including initially unset FPATH.
  </acceptance_criteria>
  <done>The real emitter applies one composed PATH/FPATH delta and reverses it exactly.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level 1; blocking threshold high.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-04-09-01 | Tampering | repeated list composition | high | mitigate | one symbolic final identity plus head/head and head/tail live-zsh tests |
| T-04-09-02 | Elevation of Privilege | list segment emission | high | mitigate | explicit provenance; static zquote; legacy heuristic isolated |
| T-04-09-03 | Tampering | PATH/FPATH base ownership | high | mitigate | separate canonical state and explicit single base position |
| T-04-09-05 | Tampering | list base presence | high | mitigate | independent presence capture and exact unset/present-empty/non-empty restoration tests |
| T-04-09-06 | Tampering | dynamic list cardinality | high | mitigate | one tied-scalar reconstruction plus direct multi-element/empty PATH/FPATH comparisons |
| T-04-09-04 | Denial of Service | malformed manifest metadata | medium | mitigate | full target validation before Plan construction |
| T-04-09-SC | Tampering | dependency supply chain | low | accept | no installs or new dependencies |
</threat_model>

<prohibition_recall>
No bespoke prohibition survives. Shell injection is canon security captured by explicit provenance and zquote; ordering, adjacency, and empty-list cases are routine semantic edges authored as truths/tests.
</prohibition_recall>

<phase5_runtime_seam>
Phase 4 owns capture and exact restoration of the one session base as structured runtime state: canonical list name, presence bit, and exact base bytes/elements. It carries `ListDelta.Deletions` through `ApplyListDelta` unchanged but does not infer or author deletions from source. Phase 5 may consume that explicit base-state plus Deletions for live multi-profile switching; it must not reconstruct base presence from an empty array, a sentinel, or manifest-authored live facts. No zsh syntax crosses into core/model or core/activate at this seam.
</phase5_runtime_seam>

<context_budget>
The ten-file scope is retained because this is one additive metadata seam, but executor context stays isolated: Task 1 owns five model/plan/Diff files, Task 2 owns only the two builder files, and Task 3 owns only the three zsh emitter/pipeline files. No task modifies more than five files, and later tasks consume the prior contract without reopening unrelated subsystems.
</context_budget>

<verification>
- GOTOOLCHAIN=auto go test ./core/model ./core/activate ./core/shell/zsh -count=1
- GOTOOLCHAIN=auto go test ./... -count=1
- GOTOOLCHAIN=auto go build ./...
- core/activate remains shell-agnostic; only core/shell/zsh/emit.go emits reverse zsh.
</verification>

<success_criteria>
- Quoted and repeated PATH/FPATH become one exact base-relative delta per final identity.
- Explicit base position/provenance cross Manifest, Diff, and Emit without heuristic re-parsing.
- Live zsh apply matches direct source and deactivate restores both bases.
- Runtime base presence remains distinct from present-empty and defines the explicit Phase 5 handoff.
</success_criteria>

<artifacts_this_phase_produces>
- ListDelta.BaseIndex and ListDelta.AdditionDynamic additive fields.
- Symbolic canonical list-composition helpers.
- Base-position-aware renderList and real PATH/FPATH integration fixtures.
- ZP_BASE_PATH_PRESENT/ZP_BASE_FPATH_PRESENT runtime state and exact unset/present-empty restoration tests.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-09-SUMMARY.md` when done.
</output>
