---
phase: 04-manifest-builder-emit
plan: 10
type: execute
wave: 6
depends_on: ["04-05"]
gap_closure: true
files_modified:
  - core/shell/zsh/introspect.go
  - core/shell/zsh/introspect_test.go
autonomous: true
requirements: [SW-02]
user_setup: []

must_haves:
  truths:
    - "A missing, syntactically invalid, or explicitly failing sourced file returns a non-nil error and IdentitySet.Available false; no later dump command can mask source status."
    - "The line-framed identity prefix is parsed only before body sections; exact protocol-marker lines inside alias/function bodies are never reinterpreted as section controls."
    - "NUL-framed alias/function body records preserve present-empty and multiline bodies, including lines equal to every protocol sentinel."
    - "Empty successful input yields an available empty identity snapshot, while absent/failed input is unavailable."
  assumptions:
    - statement: "SW-02 unclassified edge-probe item is resolved here only for introspection framing; full restoration remains flagged for 04-12."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/shell/zsh/introspect.go"
      provides: "source-status gate and prefix/body parser boundary"
      contains: "source"
    - path: "core/shell/zsh/introspect_test.go"
      provides: "missing/failing source and exact-sentinel body regressions"
      contains: "Available"
  key_links:
    - from: "introspectScript source command"
      to: "Provider.Introspect"
      via: "nonzero source status exits before protocol output and cmd.Run propagates error"
      pattern: "Available: false"
    - from: "identity prefix"
      to: "body parser"
      via: "raw output is split once at the first body header; line parser never sees body bytes"
      pattern: "parseBodies"
---

<objective>
Make introspection source failure honest and prevent body bytes from colliding with protocol control lines.

Purpose: Available true must mean the target was sourced successfully, and exact function/alias bodies must never corrupt other identity classes.
Output: fail-fast source status, strict prefix/body parser separation, and adversarial body fixtures.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail introspection immediately when source fails</name>
  <files>core/shell/zsh/introspect.go, core/shell/zsh/introspect_test.go</files>
  <read_first>
    - core/shell/zsh/introspect.go (source command and cmd.Run error mapping)
    - core/shell/zsh/introspect_test.go (current missing-source permissive test)
    - core/shell/provider.go (Introspector graceful-degradation contract)
  </read_first>
  <behavior>
    - RED: missing path returns non-nil error and Available false.
    - RED: syntax error or sourced explicit nonzero return does the same.
    - RED: a valid empty file returns nil error and Available true.
  </behavior>
  <action>
    Gate the embedded script's entire dump on successful sourcing. Capture source status immediately and exit nonzero before printing any protocol byte when it fails; do not allow print commands to become the script's final success status. Preserve zsh -f, emulate isolation, zsh/parameter load, timeout, output suppression, typed caller behavior, and absence of raw stderr in returned errors.

    Replace the permissive missing-source test with strict unavailable assertions and add syntax-failure plus valid-empty controls.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Introspect.*(Missing|Failure|Syntax|Empty|Unavailable)' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - No protocol output is accepted after any source failure.
    - Missing, syntax-failing, and explicit-failing files return error plus Available false.
    - A valid empty file remains an available empty snapshot.
  </acceptance_criteria>
  <done>Introspection availability truthfully reports whether the requested config sourced successfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate line identity parsing from NUL-framed body bytes</name>
  <files>core/shell/zsh/introspect.go, core/shell/zsh/introspect_test.go</files>
  <read_first>
    - core/shell/zsh/introspect.go (parseIntrospect whole-output line scan and parseBodies)
    - core/shell/zsh/introspect_test.go (multiline/marker body fixtures)
    - core/model/identityset.go (name/body result fields)
  </read_first>
  <behavior>
    - RED: a function body containing exact lines ##ALIASES##, ##FUNCTIONS##, ##ENV##, ##PATH##, ##OPTIONS##, ##ALIASBODIES##, ##FUNCTIONBODIES##, and ##END## round-trips exactly.
    - RED: those body lines create no spurious Env, Path, Options, Aliases, or Functions identities.
    - RED: present-empty alias/function bodies remain present.
  </behavior>
  <action>
    Partition raw output at the first body-section header before any strings.Split line scan. Run the existing line-state parser only over the identity prefix, whose records contain no body bytes. Parse alias/function body sections only with the established NUL record framing and explicit section boundaries; never feed their payload back through the marker switch. Harden zero-record and present-empty boundaries so missing records, empty bodies, and the transition between alias/function sections are unambiguous.

    Add one adversarial fixture whose function/heredoc body contains every exact sentinel line and assert byte-identical FunctionBodies plus clean unrelated maps/slices. Keep sorted body emission and additive IdentitySet fields.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Introspect|Body|Sentinel|Framing|Marker' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The line parser receives only bytes preceding the first body header.
    - Exact sentinel text in body data never changes parser section.
    - Empty and multiline bodies remain distinguishable and exact.
    - Existing name/body introspection tests remain green.
  </acceptance_criteria>
  <done>Introspection framing is collision-safe for all exact marker-shaped body lines.</done>
</task>

</tasks>

<threat_model>
## ASVS Context

ASVS level 1; blocking threshold high.

| Threat ID | STRIDE | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-04-10-01 | Spoofing | Introspection availability | high | mitigate | fail-fast source status before any dump output |
| T-04-10-02 | Tampering | protocol framing | high | mitigate | strict prefix/body partition and NUL-only body parser |
| T-04-10-03 | Denial of Service | malformed body framing | medium | mitigate | bounded parser, zero-record controls, existing 5-second process timeout |
| T-04-10-04 | Information Disclosure | sourced stderr/output | medium | mitigate | keep output suppression and do not surface raw shell stderr |
| T-04-10-SC | Tampering | dependency supply chain | low | accept | no installs or new dependencies |
</threat_model>

<prohibition_recall>
No bespoke prohibition survives. Protocol corruption and false availability are routine safety/correctness checks; shell execution and disclosure are canon concerns handled by the existing sandbox plus this threat model.
</prohibition_recall>

<verification>
- GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Introspect|Body|Sentinel|Failure' -count=1
- GOTOOLCHAIN=auto go test ./... -count=1
- GOTOOLCHAIN=auto go build ./...
</verification>

<success_criteria>
- Source failure always produces error plus unavailable state.
- Body bytes cannot be consumed by the line-framed identity parser.
- Exact sentinel lines and present-empty/multiline bodies round-trip without pollution.
</success_criteria>

<artifacts_this_phase_produces>
- Fail-fast source-status gate in introspectScript.
- Prefix/body output partition helper.
- Exact-sentinel body and missing/failing-source tests.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-10-SUMMARY.md` when done.
</output>
