---
phase: 04-manifest-builder-emit
plan: 18
type: execute
wave: 15
depends_on: [04-17]
files_modified:
  - core/model/block.go
  - core/model/profile.go
  - core/model/profile_test.go
  - core/ir/build.go
  - core/ir/build_test.go
  - core/store/dto.go
  - core/store/dto_test.go
  - core/shell/zsh/parse.go
  - core/shell/zsh/parse_test.go
autonomous: true
gap_closure: true
requirements: [SW-01, SW-02]
must_haves:
  truths:
    - "A persisted forced-managed command with more than one assignment, an alias query or multi-alias definition, or behavior-changing option syntax remains verbatim and yields no scalar, list, alias, or option manifest intent."
    - "Ordinary one-name scalar/export assignments, an empty but assigned one-name alias, and the existing simple setopt/unsetopt forms remain representable after the source-fidelity gate is strengthened."
    - "A persisted `export -- PATH=...` or `export -- FPATH=...` with one assignment retains parser-proven list semantics, late-bound expansion, source order, and captured-base restoration through emitted zsh."
    - "Historical absent, v1, v2, partial, and unsupported structural-fidelity records re-save as unknown and cannot gain the new source-shape metadata or manifest operations."
  artifacts:
    - path: core/model/block.go
      provides: "Parser-owned markers for alias assignment form and setopt/unsetopt invocation syntax."
    - path: core/model/profile.go
      provides: "Shared source-fidelity representability gate for exact-one assignment/alias forms and modeled option syntax."
    - path: core/store/dto.go
      provides: "Presence-aware current structural-fidelity DTO record that preserves the new markers without promoting historical records."
    - path: core/shell/zsh/parse.go
      provides: "AST-derived option syntax and delimiter PATH/FPATH list decoding."
  key_links:
    - from: core/shell/zsh/parse.go
      to: core/store/dto.go
      via: "Parser-owned alias/option/list fidelity is copied by IR and persisted as a complete versioned record."
      pattern: "AliasAssignment|OptionFlags|ListValue|structuralFidelity"
    - from: core/model/profile.go
      to: core/ir/regen.go
      via: "Representability controls templated regeneration; unrepresentable persisted entries use verbatim Text."
      pattern: "Representable|Regenerate"
    - from: core/model/profile.go
      to: core/activate/builder.go
      via: "The same gate prevents OverrideManaged entries from creating manifest intent."
      pattern: "EffectiveManaged|Representable"
    - from: core/ir/build.go
      to: core/store/dto.go
      via: "IR copies parser-owned marker slices without aliasing before the store serializes the complete v3 fidelity record."
      pattern: "StructuralFidelityKnown|AliasAssignment|OptionFlags"
---

<objective>
Close the final source-fidelity review findings by making persisted override handling fail closed for multi-value assignments, alias non-definitions, and option syntax that cannot be faithfully lowered, while retaining supported delimiter PATH/FPATH list behavior.

Purpose: a forced managed override may select the declarative axis, but it must never erase the zsh source shape that gives an assignment, alias, option command, or tied list its behavior.
Output: a v3 presence-aware structural-fidelity record and shared parser/IR/model/store admission contract; the dependent zsh regeneration, pipeline, and residue proof is isolated in 04-19.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-17-SUMMARY.md
</context>

## Dependency graph

| Task | Needs | Creates | Checkpoint |
| --- | --- | --- | --- |
| 1 | 04-17 delimiter declaration parsing and current Block-to-IR transfer | AST-derived alias-form, option-syntax, delimiter-list source markers, and mutation-safe IR copies | none |
| 2 | Task 1's complete Block markers; existing DTO v2 compatibility contract | Entry fidelity v3 persistence and the shared representability admission rule | none |

This is Wave 15 because it extends the source-fidelity boundary completed in 04-17. Tasks are sequential: parser provenance and mutation-safe IR copies must exist before DTO persistence and shared admission can consume them. The runtime proof is deliberately Wave 16 in 04-19, dependent on this contract; this keeps the 8-file source/persistence concern coherent and prevents the 12-file warning without losing any proof.

## Source coverage audit

| Source | ID | Required outcome | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Profiles become reversible shell state without semantic loss or residue. | 04-18 Tasks 1-2; 04-19 Tasks 1-2 | COVERED |
| REQ | SW-01 | Only faithfully representable source reaches the agnostic manifest and sole zsh emitter. | Tasks 1-2; 04-19 Task 1 | COVERED |
| REQ | SW-02 | Rejected source is inert and supported PATH/FPATH state applies then restores without residue. | 04-19 Tasks 1-2 | COVERED |
| RESEARCH | Architectural Responsibility Map | Parser owns zsh source shape, model/store own fidelity, activation remains shell-agnostic, and zsh tests consume the existing emitter. | Tasks 1-2; 04-19 Tasks 1-2 | COVERED |
| RESEARCH | Dynamic-list and no-evaluation constraints | Delimiter PATH/FPATH uses AST-derived late-bound list segments; no task executes source to infer meaning. | Task 1; 04-19 Task 1 | COVERED |
| RESEARCH | Pitfalls 1, 2, 5, 6 | Keep plain emitted functions and the existing captured-base/single-process snapshot path while proving dynamic list expansion and restoration. | 04-19 Tasks 1-2 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-04 | Leave the Manifest wire contract unchanged; preserve source order and keep Profile persistence in the store-local DTO. | Task 2 | COVERED |
| CONTEXT | D-05/D-06/D-07 | Keep source classification and representability separate from override choice; make the builder consume only faithfully modeled state. | Tasks 1-2; 04-19 Task 1 | COVERED |
| CONTEXT | D-08/D-09 | Rejected entries traverse the existing empty Build-to-Diff plan path rather than receiving a shell-specific bypass. | 04-19 Task 1 | COVERED |
| CONTEXT | D-10/D-11/D-12 | Do not add activation syntax outside the existing emitter or alter runtime undo semantics; tests consume emitted plans. | 04-19 Tasks 1-2 | COVERED |
| CONTEXT | D-13 | Preserve parsed late-bound PATH/FPATH source in list segments and do not freeze it during persistence or emission. | Task 1; 04-19 Task 1 | COVERED |
| CONTEXT | D-14/D-15 | Retain the additive introspection/snapshot representation used by the full-state residue test. | 04-19 Task 2 | COVERED |
| CONTEXT | D-16/D-17 | Extend default-suite zsh residue coverage without weakening the N-switch property or its negative controls. | 04-19 Task 2 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the remaining parser-owned command shapes without evaluating source</name>
  <files>core/model/block.go, core/ir/build.go, core/ir/build_test.go, core/shell/zsh/parse.go, core/shell/zsh/parse_test.go</files>
  <read_first>
    - core/shell/zsh/parse.go — CallExpr, DeclClause, alias, declaration, option, captureWordSemantics, and captureListValue behavior.
    - core/ir/build.go — parser Block to Entry transfer and defensive copy conventions.
    - core/ir/build_test.go — existing structural-fidelity, Names, ListValue, and pointer-copy assertions to extend rather than duplicate.
    - core/shell/zsh/parse_test.go — current multi-name, indexed, declaration-flag, and semantic list fixtures.
    - .planning/phases/04-manifest-builder-emit/04-REVIEW.md — CR-01 through CR-04 source shapes and their loss modes.
  </read_first>
  <behavior>
    - Test 1: Parse records that a one-name alias argument had an equals form, distinguishes `alias ll` from `alias ll=`, and keeps every name in `alias a=one b=two`.
    - Test 2: Parse records option invocation tokens in source order; no-token, delimiter, and `-o` forms retain their modeled declarative polarity, while `+o` and query/filter forms retain enough syntax for Task 2 to reject them rather than treating their words as option names.
    - Test 3: Parse a one-assignment `export -- PATH=$PATH:$EXTRA` and its FPATH counterpart into the same valid semantic ListValue contract as their ordinary export controls, including a same-list Self marker and late-bound dynamic segment.
    - Test 4: `core/ir/build_test.go` constructs Blocks for an alias query, an empty assigned alias, and each bare, `--`, `-o`, `+o`, and `-m` option command; Build preserves the alias-assignment marker, exact OptionFlags slice including nil-versus-empty/present values, and sets StructuralFidelityKnown on parser-produced Blocks.
    - Test 5: Mutating every source marker slice after Build, then mutating the Entry copy, leaves the other side unchanged; this covers Names, DeclarationFlags, and the new OptionFlags slice while scalar alias-form presence remains exact for both false/query and true/empty-definition cases.
  </behavior>
  <action>
    Add parser-owned source-fidelity fields to Block for alias assignment form and setopt/unsetopt invocation syntax, then transfer them through IR using the existing defensive-copy conventions. Record an alias as assignment-form only when its parsed argument carries an equals separator; preserve the empty right-hand side as a valid definition, while an alias query remains structurally distinguishable from that empty definition. Keep multi-alias names intact and do not infer a value for an alias query. This implements D-05/D-06 without changing how OverrideManaged is stored. Extend `core/ir/build_test.go` in the same task: assert the complete alias/option marker matrix, including false versus true alias-assignment presence, nil versus explicit-empty versus populated option-control slices, StructuralFidelityKnown, and two-way mutation-safe slice copies for every new slice marker.

    For setopt and unsetopt, parse option-control words separately from option names, preserve their source order, and make delimiter and `-o` forms explicit rather than discarded. Retain the semantic control words needed for Task 2 to admit only the forms whose Enabled meaning is modeled; capture plus-polarity and query/filter controls as syntax rather than option names. Do not execute or normalize shell input to determine its meaning, per D-13.

    Refactor list-value capture so exactly one declaration assignment represented as a word after the export delimiter is decoded by the same AST-derived PATH/FPATH path as an ordinary Assign. Continue to require one assignment, a same-list self reference, and a valid ListValue; multi-assignment declaration commands must retain their source shape but must not acquire a list delta. Add parser tests for plain, exported, and delimiter multi-assignment forms; alias query, empty definition, and multi-alias forms; setopt and unsetopt with bare, `--`, and `-o` controls plus rejected `+o` and `-m` query/filter controls; and delimiter PATH/FPATH dynamic lists. Keep the existing ordinary scalar/export, alias definition, and simple setopt/unsetopt behavior covered per D-05/D-06/D-07/D-13.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'TestParse(ClassifiesKinds|PreservesRoundTripShapes|CapturesIndexedAndDeclarationFlags|.*(Alias|Option|Delimiter|List))$' -v && GOTOOLCHAIN=auto go test -count=1 ./core/ir -run 'Test(BuildCopiesStructuralFidelity|BuildCopiesNamesSlice|RouteManaged|EffectiveManagedOverrideWins|Build.*(ListValue|RuntimeValueContract))$' -v</automated>
  </verify>
  <done>Every reported source form carries complete parser provenance: valid one-name delimiter PATH/FPATH lists receive semantic data, while multi-value, alias-query, and behavior-bearing option syntax retains the data needed to stay out of lossy lowering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist v3 source fidelity and centralize representability admission</name>
  <files>core/model/profile.go, core/model/profile_test.go, core/store/dto.go, core/store/dto_test.go</files>
  <read_first>
    - core/model/profile.go — EffectiveManaged, DeclarationRepresentable, and Representable contracts.
    - core/store/dto.go — v2 presence-aware structuralFidelityDTO encoding and historical unknown re-save handling.
    - core/store/dto_test.go — round-trip and compatibility matrix conventions.
    - core/shell/zsh/regen.go and core/activate/builder.go — existing consumers of Entry.Representable.
  </read_first>
  <behavior>
    - Test 1: Representable admits only exact-one-name assignment and exact-one-name assignment-form alias entries, retaining ordinary scalar/export and `alias ll=` support while rejecting persisted multi-assignment, alias query, and multi-alias entries regardless of OverrideManaged.
    - Test 2: Representable admits the complete modeled option matrix for both setopt and unsetopt: bare, `--`, and `-o` controls; it rejects `+o` and `-m` query/filter forms for both verbs so they create no option intent.
    - Test 3: Marshal and unmarshal preserve every current v3 marker, including false alias-assignment presence plus nil, explicit-empty, and populated option marker slices, without aliasing caller storage; parsed entries remain structurally known only with the complete v3 record.
    - Test 4: Absent, v1, complete v2, partial v3, and unsupported structural-fidelity records decode unknown, re-save without a fidelity object, and cannot become representable merely because OverrideManaged is persisted.
  </behavior>
  <action>
    Extend Entry's structural-fidelity state with Task 1's alias-form and option-syntax fields. Make one shared Representable predicate require known current fidelity before existing append, array, indexed, declaration-attribute, and declaration-command exclusions; additionally require exactly one assignment name, exactly one assignment-form alias name, and modeled option invocation syntax. Preserve the current positive paths for an ordinary one-name scalar, ordinary exported scalar, empty-but-assigned alias, and the explicitly modeled simple option forms. OverrideManaged must continue to determine only the managed axis, never override source fidelity, per D-05/D-06.

    Advance the store-local structural-fidelity DTO to a new complete, presence-aware version. Serialize the new boolean/slice fields with explicit false and empty values, clone slices at both boundaries, and decode structural fidelity as known only when every current marker is present. Treat every older complete version, every absent or partial current record, and unsupported versions as unknown; when unknown data is re-saved, omit the fidelity object instead of synthesizing false, empty, or supported markers. Keep JSON ownership in core/store and do not change model.Manifest's wire shape or resolve dynamic values, per D-01 through D-04 and D-13.

    Add model and DTO matrices that use persisted forced-managed entries for each report path. Assert the exact admission decision before and after a marshal/unmarshal re-save for alias query versus empty assigned alias, multi-alias, and both setopt/unsetopt bare/`--`/`-o` positive rows plus `+o` and `-m` rejected rows. Include historical records that previously looked complete under v2. Verify source order and existing ValueMode/ListValue fields remain intact while only complete current source-fidelity records can reach the positive representability branch.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/model ./core/store -run 'Test(Entry.*Represent|RoundTrip.*|StructuralFidelityDTOCompatibilityMatrix)' -v</automated>
  </verify>
  <done>The persisted contract preserves all current source-shape markers, keeps historical records fail-closed on re-save, and provides one representability gate used by both regeneration and manifest building.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| zsh source -> parser/IR -> persisted Profile | Valid shell syntax can carry behavior in assignment multiplicity, alias form, option controls, or tied-list declaration placement that a scalar-shaped IR can otherwise discard. |
| persisted Profile -> Regenerate/Build -> Diff/Emit | OverrideManaged is stored user intent and must not convert incomplete source provenance into executable manifest operations. |
| emitted Plan -> live zsh | A lossy operation can mutate aliases, options, PATH/FPATH ordering, export behavior, or leave shell residue after deactivate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04-18-01 | Tampering | persisted override admission | high | mitigate | Persist complete v3 parser provenance and require shared representability before regeneration or Build; historical/partial records re-save unknown. |
| T-04-18-02 | Tampering | parser-to-IR source marker transfer | high | mitigate | Pin all new alias/option markers, explicit-empty values, StructuralFidelityKnown, and defensive slice copies in `core/ir/build_test.go`. |
| T-04-18-03 | Tampering | delimiter PATH/FPATH lowering | high | mitigate | Decode delimiter declaration words through the AST list contract; 04-19 proves direct-source parity, late binding, source order, captured-base restoration, and no residue in zsh. |
| T-04-18-04 | Repudiation | persistence/admission evidence | medium | mitigate | Exercise parser provenance through IR copy, DTO re-save, and Representable matrices before the Wave 16 live-zsh pipeline test consumes it. |
| T-04-18-SC | Tampering | dependency supply chain | low | accept | No package-manager install or new dependency is introduced; changes use the existing pinned Go module, mvdan parser, and local zsh harness. |
</threat_model>

<verification>
Run Task 1 parser/IR copy checks and Task 2 model/store compatibility checks. Then hand the complete source-fidelity contract to 04-19 for targeted live-zsh pipeline and residue proof, followed by its uncached full Go suite, build, vet, and `make check`.
</verification>

<success_criteria>
- A forced persisted override cannot flatten multi-assignment, alias-query/multi-alias, or behavior-bearing option syntax into a different generated command or manifest operation.
- Current v3 source-fidelity records survive DTO round trips; absent, old, partial, and unsupported records remain unknown after re-save.
- Existing supported simple scalar/export, empty alias-definition, and simple option behavior remains representable.
- One-assignment delimiter PATH/FPATH declarations retain a parser-proven semantic list contract for the dependent runtime proof.
- `core/ir/build_test.go` proves every new alias/option marker's false, empty, and populated semantics survives IR construction without slice aliasing; 04-19 closes CR-01 through CR-04 with live-zsh SW-01/SW-02 evidence.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-18-SUMMARY.md` when done.
</output>
