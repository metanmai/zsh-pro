---
phase: 04-manifest-builder-emit
plan: 19
type: execute
wave: 16
depends_on: [04-18]
files_modified:
  - core/shell/zsh/regen.go
  - core/shell/zsh/regen_test.go
  - core/shell/zsh/pipeline_test.go
  - core/shell/zsh/residue_test.go
autonomous: true
gap_closure: true
requirements: [SW-01, SW-02]
must_haves:
  truths:
    - "A persisted forced-managed multi-assignment, alias query/multi-alias definition, or rejected option-control command regenerates verbatim and creates no scalar, list, alias, or option manifest intent."
    - "Both setopt and unsetopt retain supported bare, `--`, and `-o` behavior through the persisted path, while `+o` and `-m` query/filter forms are inert after emitted apply and deactivate."
    - "A persisted `export -- PATH=...` or `export -- FPATH=...` has direct-source-equivalent order and late-bound expansion and restores its captured base after emitted deactivate."
    - "The default zsh-backed full-state oracle remains byte-identical before apply and after deactivate for a persisted mixed rejected fixture, including alias and option state."
  artifacts:
    - path: core/shell/zsh/regen.go
      provides: "Defense-in-depth verbatim fallback for source shapes the shared model gate rejects."
    - path: core/shell/zsh/pipeline_test.go
      provides: "Persisted Parse-to-live-zsh matrix for supported and rejected alias/option source markers plus delimiter lists."
    - path: core/shell/zsh/residue_test.go
      provides: "Byte-identical no-residue proof for the mixed rejected persisted source-shape matrix."
  key_links:
    - from: core/model/profile.go
      to: core/shell/zsh/regen.go
      via: "The shared complete-fidelity admission contract is repeated as a zsh-provider guard, so incomplete persisted source shape returns Text."
      pattern: "Representable|StructuralFidelityKnown|Regenerate"
    - from: core/store/dto.go
      to: core/shell/zsh/pipeline_test.go
      via: "Parser markers become live tests only after MarshalProfile, UnmarshalProfile, and OverrideManaged exercise the persisted boundary."
      pattern: "MarshalProfile|UnmarshalProfile|OverrideManaged"
    - from: core/shell/zsh/pipeline_test.go
      to: core/shell/zsh/residue_test.go
      via: "Focused direct-source and generated-plan discriminators are complemented by a full-state snapshot of the production Build-to-Diff-to-Emit path."
      pattern: "Build|Diff|Emit|snapshot"
---

<objective>
Prove the 04-18 source-fidelity contract across regeneration, manifest construction, emitted zsh, and full-state deactivation residue.

Purpose: force-managed persisted profiles must not reinterpret source behavior, while the exact modeled alias, option, and delimiter-list forms must still work as reversible shell state.
Output: provider defense-in-depth plus focused persisted live-zsh and default-suite residue evidence for CR-01 through CR-04.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-RESEARCH.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-17-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-18-SUMMARY.md
</context>

## Dependency graph

| Task | Needs | Creates | Checkpoint |
| --- | --- | --- | --- |
| 1 | 04-18 parser/IR marker copies, DTO v3, and shared Representable matrix | Regeneration defense and persisted live-zsh behavior matrix | none |
| 2 | Task 1 persisted rejected fixtures and existing full-state snapshot helpers | Byte-identical mixed-profile residue proof | none |

This is Wave 16 because it consumes the completed 04-18 source/persistence contract. The plan owns only the four runtime pipeline/proof files, all in one zsh-test concern; it has no same-wave overlap and retains every former 04-18 runtime acceptance criterion.

## Source coverage audit

| Source | ID | Required outcome | Plan coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 4 | Profiles become reversible shell state without semantic loss or residue. | Tasks 1-2 | COVERED |
| REQ | SW-01 | Only faithfully representable source reaches the agnostic manifest and sole zsh emitter. | Task 1 | COVERED |
| REQ | SW-02 | Rejected source is inert and supported PATH/FPATH state applies then restores without residue. | Tasks 1-2 | COVERED |
| RESEARCH | Architectural Responsibility Map | Runtime proof consumes parser/model/store fidelity and the existing sole zsh emitter; activation stays shell-agnostic. | Tasks 1-2 | COVERED |
| RESEARCH | Dynamic-list and no-evaluation constraints | Tests compare AST-preserved source to zsh execution without evaluating source to classify it. | Task 1 | COVERED |
| RESEARCH | Pitfalls 1, 2, 5, 6 | Retain plain emitted functions and existing captured-base/single-process snapshot conventions while proving dynamic list expansion and restoration. | Tasks 1-2 | COVERED |
| CONTEXT | D-01/D-02/D-03/D-04 | Preserve the Manifest wire contract and store-local DTO boundary while exercising persisted profiles. | Task 1 | COVERED |
| CONTEXT | D-05/D-06/D-07 | Override choice cannot bypass source fidelity; supported late-bound lists retain their source contract. | Task 1 | COVERED |
| CONTEXT | D-08/D-09 | Rejected profiles use the existing empty Build-to-Diff plan route. | Task 1 | COVERED |
| CONTEXT | D-10/D-11/D-12 | Only the existing emitter executes zsh; no runtime undo behavior is changed. | Tasks 1-2 | COVERED |
| CONTEXT | D-13 | Preserve late-bound PATH/FPATH list source through emission rather than freezing dynamic values. | Task 1 | COVERED |
| CONTEXT | D-14/D-15/D-16/D-17 | Use the established snapshot/default-suite N-switch evidence to prove complete no residue. | Task 2 | COVERED |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exercise every persisted alias, option, and delimiter-list source form through live zsh</name>
  <files>core/shell/zsh/regen.go, core/shell/zsh/regen_test.go, core/shell/zsh/pipeline_test.go</files>
  <read_first>
    - core/ir/regen.go — effective-managed plus representability decision selecting templated versus verbatim source.
    - core/shell/zsh/regen.go and regen_test.go — current defensive regeneration guards and Text fallback contract.
    - core/shell/zsh/pipeline_test.go — persisted override matrix, roundTripPipelineProfile, emitPipelineListPlan, no-operation checks, and live-zsh sentinel conventions.
    - .planning/phases/04-manifest-builder-emit/04-18-SUMMARY.md — final marker names and DTO v3/current-fidelity contract.
    - .planning/phases/04-manifest-builder-emit/04-REVIEW.md — CR-01 through CR-04 source semantics and discriminating controls.
  </read_first>
  <behavior>
    - Test 1: After Parse -> IR Build -> DTO re-save -> OverrideManaged, plain, export, and `export --` multi-assignment commands; alias query; multi-alias definition; `setopt +o`; `unsetopt +o`; `setopt -m`; and `unsetopt -m` regenerate byte-for-byte from Text and yield no scalar, list, alias, or option intent through Build -> Diff -> Emit.
    - Test 2: For both setopt and unsetopt, bare, `--`, and `-o` rows enter the persisted pipeline, mutate the seeded option to the modeled enabled/disabled state on apply, and restore the captured seed on deactivate; direct `zsh -f` source controls establish expected behavior for each row.
    - Test 3: For both setopt and unsetopt, `+o` and `-m` rows execute direct source in isolated seeded-state controls, then generated persisted plans are proven inert: the same seeded option is unchanged immediately after apply and again after deactivate.
    - Test 4: A direct alias query stays a query and a direct multi-alias definition establishes both aliases, while generated rejected persisted plans preserve pre-seeded alias bodies unchanged after apply/deactivate; `alias ll=` remains a supported empty definition through the persisted path.
    - Test 5: Persisted one-assignment delimiter PATH and FPATH declarations expand runtime HOME/EXTRA values late, match direct source order, produce one semantic ListDelta each, and restore their captured bases exactly after emitted deactivate; delimiter multi-assignment remains a rejected no-list row.
  </behavior>
  <action>
    Add defense-in-depth guards in Provider.Regenerate that require the same exact-one-name assignment/alias and modeled-option predicate as 04-18 before forward syntax is constructed; otherwise return Entry.Text. Preserve `alias ll=` as an assignment-form alias with an empty body. Do not put zsh syntax in model, activate, or store, and do not change existing emitted runtime undo behavior, per D-08 through D-12.

    Extend the established persisted pipeline rather than creating a parallel harness. Every row must perform Parse -> IR Build -> MarshalProfile -> UnmarshalProfile -> set OverrideManaged -> Regenerate -> activate.Build -> activate.Diff -> Provider.Emit -> one `zsh -f` script. Rejected rows require exact Text regeneration and an empty scalar/list/alias/option manifest. For aliases, use a pre-seeded query target and two distinguishable pre-seeded multi-alias targets: direct source proves query versus two-definition behavior, but generated rejected plans prove no target changes after apply and deactivate. Keep an `alias ll=` persisted positive control to prove empty assigned is not confused with alias query.

    Implement the complete option behavior matrix for both verbs. Supported rows are `setopt NAME`, `setopt -- NAME`, `setopt -o NAME`, `unsetopt NAME`, `unsetopt -- NAME`, and `unsetopt -o NAME`; compare each direct-source control against its expected state, then assert generated apply makes that modeled state and generated deactivate restores the captured opposite seed. Rejected rows are `setopt +o NAME`, `unsetopt +o NAME`, `setopt -m NAME`, and `unsetopt -m NAME`; first capture direct-source behavior in isolated seeded zsh runs, then assert generated persisted output has no option intent and leaves the same seed unchanged after apply and after deactivate. This deliberately distinguishes source observation from generated inertness instead of treating a query/filter as an ordinary set/unset command.

    Add persisted one-assignment `export -- PATH=$PATH:$EXTRA` and FPATH cases with runtime HOME/EXTRA values deliberately different from source spelling. Compare emitted activation with direct source in the same `zsh -f` process, assert source-derived order, late-bound expansion, and one semantic ListDelta per list, then assert emitted deactivate restores captured PATH/FPATH bases exactly. Include delimiter multi-assignment list commands in the rejected matrix so no list delta can be created from a lossy aggregate, per D-05/D-06/D-07/D-13.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'Test(Regenerate|Pipeline.*(Persisted|Alias|Option|Delimiter|List))$' -v</automated>
  </verify>
  <done>All alias and option marker forms complete the persisted production path under live zsh: empty assigned aliases and the exact supported setopt/unsetopt controls work, while queries, multi-aliases, multi-assignments, plus polarity, and filter controls are verbatim/no-operation with unchanged sentinels. Delimiter PATH/FPATH keeps semantic list behavior and exact captured-base restoration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove mixed rejected source has no full-state residue after emitted deactivate</name>
  <files>core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/shell/zsh/residue_test.go — complete snapshot oracle, zsh discovery/timeout path, loader cleanup, and default N-switch property.
    - core/shell/zsh/pipeline_test.go — Task 1 persisted rejected fixtures and live sentinel assertions.
    - core/shell/zsh/introspect.go — type-aware alias/options snapshot representation.
    - .planning/phases/04-manifest-builder-emit/04-CONTEXT.md — D-14 through D-17 snapshot and N-switch requirements.
  </read_first>
  <behavior>
    - Test 1: A persisted forced-managed mixed profile containing plain/export/delimiter multi-assignment, alias query, multi-alias definition, plus-polarity options, and `-m` option query/filter commands traverses Build -> Diff -> Emit with no manifest operation.
    - Test 2: The complete snapshot before apply and after deactivate is byte-identical for scalar/list, alias body, and option state; a deliberate alias-body-only or option-state-only mutation makes the oracle fail.
    - Test 3: The default zsh-backed suite runs the persisted mixed profile under the existing LookPath skip guard and retains the N-switch property and renderer negative controls.
  </behavior>
  <action>
    Extend the durable `residue_test.go` oracle with a persisted mixed rejected profile assembled through the same parser, DTO re-save, and OverrideManaged route as Task 1. Include every new alias/option rejection form alongside all plain, export, and delimiter multi-assignment variants. Drive resulting empty manifest intent through existing Build, Diff, and Emit; do not bypass production code or add a bespoke emitter.

    Pre-seed aliases and option states that make a no-operation assertion discriminating, then use the established complete snapshot before apply and after deactivate. The snapshot must include the oracle's type-aware alias-body and option sections, so alias-only or option-only drift is visible; keep existing scalar/list and captured-base coverage. Retain the default `zsh -f`, timeout, LookPath skip behavior, N-switch property, and renderer controls required by D-14 through D-17. The proof is byte-identical state, not only an empty Go manifest.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'Test(Residue.*(Persisted|Structural|SourceShape|Noop)|ZeroResidueFullStateProperty)$' -v && GOTOOLCHAIN=auto go test -count=1 ./... && GOTOOLCHAIN=auto go build ./... && GOTOOLCHAIN=auto go vet ./... && make check</automated>
  </verify>
  <done>The default zsh-backed full-state oracle proves that no rejected persisted assignment, alias, or option source shape leaves scalar/list, alias-body, or option-state residue after emitted deactivate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| persisted Profile -> Regenerate/Build -> Diff/Emit | OverrideManaged must not turn incomplete assignment, alias, or option source provenance into a different executable command or manifest operation. |
| emitted Plan -> live zsh | Incorrect lowering can mutate aliases, options, PATH/FPATH order, exports, or leave state after deactivate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-04-19-01 | Tampering | persisted alias and assignment regeneration | high | mitigate | Require provider Text fallback and full persisted pipeline tests with direct-source alias discriminators and generated no-operation sentinels. |
| T-04-19-02 | Tampering | setopt/unsetopt lowering | high | mitigate | Execute bare/`--`/`-o` supported controls and both-verb `+o`/`-m` rejected controls under seeded direct-source and generated apply/deactivate checks. |
| T-04-19-03 | Tampering | delimiter PATH/FPATH lowering | high | mitigate | Prove source order, late-bound values, semantic ListDelta, captured-base restoration, and rejected delimiter multi-assignment behavior in live zsh. |
| T-04-19-04 | Repudiation | residue evidence | high | mitigate | Bind all rejected rows to the existing byte-identical full-state snapshot and N-switch default-suite property, including alias-body and option-state sections. |
| T-04-19-SC | Tampering | dependency supply chain | low | accept | No package-manager install or new dependency is introduced; tests use the pinned Go module and existing zsh harness. |
</threat_model>

<verification>
Run Task 1's focused persisted pipeline/regen suite and Task 2's complete residue suite. Then require the uncached full Go suite, build, vet, and `make check`. With zsh available, report direct-source discriminators and seeded generated-plan checks for every supported and rejected alias/option row, plus delimiter PATH/FPATH parity and byte-identical no-residue evidence.
</verification>

<success_criteria>
- A forced persisted override cannot flatten multi-assignment, alias-query/multi-alias, or `+o`/`-m` option source into a different regenerated command or manifest operation.
- Both setopt and unsetopt support only their bare, delimiter, and `-o` modeled forms; their plus-polarity and query/filter forms remain inert after emitted apply/deactivate.
- One-assignment delimiter PATH/FPATH declarations retain semantic list deltas, late binding, order, and exact captured-base restoration.
- The full zsh snapshot proves no scalar/list, alias-body, or option-state residue for the complete rejected matrix, closing CR-01 through CR-04 for SW-01 and SW-02.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-19-SUMMARY.md` when done.
</output>
