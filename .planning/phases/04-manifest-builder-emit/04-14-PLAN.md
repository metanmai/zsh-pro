---
phase: 04-manifest-builder-emit
plan: 14
type: execute
wave: 11
depends_on: [04-13]
files_modified:
  - core/model/profile.go
  - core/model/profile_test.go
  - core/activate/builder.go
  - core/activate/builder_test.go
  - core/ir/regen.go
  - core/ir/regen_test.go
  - core/store/dto_test.go
  - core/shell/zsh/pipeline_test.go
  - core/shell/zsh/residue_test.go
autonomous: true
gap_closure: true
requirements: [SW-01, SW-02]
must_haves:
  truths:
    - "Every valid PATH or FPATH entry, including stored legacy input, is reduced through one source-ordered composition path into exactly one ListDelta for its canonical list."
    - "Repeated legacy and mixed legacy-plus-semantic PATH/FPATH profiles apply every addition in source order and deactivate to an exact live-shell baseline."
    - "Persisted OverrideManaged declaration entries retain their verbatim text but never create scalar or list operations when their declaration semantics are not represented."
  artifacts:
    - path: "core/activate/builder.go"
      provides: "Canonical list composition and builder-side representability guard"
    - path: "core/model/profile.go"
      provides: "Shared declaration representability predicate that does not weaken OverrideManaged persistence"
    - path: "core/shell/zsh/pipeline_test.go"
      provides: "Parse-to-store-to-live-zsh regression coverage for legacy/semantic list ordering"
    - path: "core/shell/zsh/residue_test.go"
      provides: "Zero-residue proof using persisted source-derived profiles"
  key_links:
    - from: "core/activate/builder.go"
      to: "core/model/profile.go"
      via: "Builder checks the shared representability predicate after EffectiveManaged before emitting manifest intent"
      pattern: "EffectiveManaged|Represent"
    - from: "core/store/dto.go"
      to: "core/activate/builder.go"
      via: "Round-tripped CmdName, Override, ValueMode, and ListValue are consumed through the same builder boundary"
      pattern: "UnmarshalProfile|Build"
    - from: "core/shell/zsh/pipeline_test.go"
      to: "core/shell/zsh/emit.go"
      via: "Parse -> IR -> Store -> Build -> Diff -> Emit output is syntax-checked and sourced by zsh -f"
      pattern: "MarshalProfile|UnmarshalProfile|Emit|zsh"
---

<objective>
Close the final Phase 04 fidelity gaps by making all legacy and semantic list input share one canonical reducer, and by enforcing declaration representability at the persisted-profile boundary.

Purpose: SW-01 may apply only declarative state faithfully represented by the IR, and SW-02 must reverse every admitted profile without losing additions or leaving live-shell residue.
Output: a canonical ListDelta reducer, a persisted-entry declaration guard with verbatim regeneration, and source-to-live-zsh proofs for both paths.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-CONTEXT.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-13-SUMMARY.md

<interfaces>
From core/model/profile.go: `Entry.EffectiveManaged()` intentionally honors `OverrideManaged`; `Entry.Text` is the verbatim source anchor; `ListValue.Valid()` requires exactly one same-list self marker; `ValueModeLegacy` is the compatibility-only route for raw `Value` fallback.

From core/store/dto.go: `MarshalProfile` and `UnmarshalProfile` preserve `CmdName`, `Override`, `ValueMode`, `RuntimeValue`, and `ListValue` without reparsing source.

From core/activate/builder.go: `Build(model.Profile) model.Manifest` currently passes semantic lists through `composeList`, while legacy `pathDelta` has a separate direct manifest append. The closure must remove that split so one final `ListDelta` per canonical PATH/FPATH identity is emitted in profile source order.

From core/ir/regen.go: regeneration currently chooses templating from `EffectiveManaged`; the new shared boundary must retain original declaration text when an override cannot make the declaration representable.

From core/shell/zsh/pipeline_test.go: production proof follows `Provider.Parse` -> `ir.Build` -> `activate.Build` -> `activate.Diff` -> `Provider.Emit` -> syntax check -> sourced `zsh -f`. This closure inserts the existing DTO round trip before Build.
</interfaces>

The graph is stale; treat the concrete Phase 04 verification report and the source contracts above as authoritative.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce semantic and legacy PATH/FPATH entries through one canonical source-order composition path</name>
  <files>core/activate/builder.go, core/activate/builder_test.go</files>
  <read_first>core/activate/builder.go (`Build`, `composeList`, `pathDelta`, `canonicalList`); core/model/profile.go (`ListValue`, `ListSegment`, `ValueMode`); core/activate/builder_test.go; 04-13-SUMMARY.md.</read_first>
  <behavior>
    - Two valid legacy PATH entries produce one `ListDelta{Name: "PATH"}` whose additions retain both source occurrences and whose base position is the one captured list base.
    - The equivalent repeated FPATH profile produces one `ListDelta{Name: "FPATH"}` with its own base position and additions.
    - A valid legacy entry followed by a parser-proven semantic entry, and the reverse source order, each yield one final canonical delta with static and dynamic provenance aligned to every resulting addition.
    - Invalid legacy input, an invalid semantic `ListValue`, an unknown list name, and unsupported entries remain operation-free; no direct append path may bypass the canonical reducer.
  </behavior>
  <action>Refactor `Build` so every accepted PATH/FPATH contribution becomes the same internal ordered token representation before the final `m.Lists` reduction. Convert explicit `ValueModeLegacy` values through the existing same-list `pathDelta` compatibility parser, then translate its additions and base placement into that token stream; retain semantic `ListValue` handling through `composeList` and merge both forms in profile order. Emit exactly one final `ListDelta` per `canonicalList`, keeping `AdditionDynamic` aligned with additions, the self/base marker at the source-derived position, and the existing final-identity ordering rule. Preserve explicit legacy compatibility but do not infer legacy semantics for parsed entries. Keep `core/activate` shell-agnostic and preserve the existing manifest field contract per D-01, D-02, D-04, D-05, D-06, D-07, and D-08.</action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/activate -run 'TestBuild(Composes.*Legacy.*|Composes.*Semantic.*|RequiresSemanticListOrLegacySameList)$' -v</automated>
  </verify>
  <acceptance_criteria>Each canonical PATH/FPATH list has one composed manifest delta that preserves every valid legacy and semantic addition in source order; rejected input does not reach the manifest.</acceptance_criteria>
  <done>The builder has one composition pathway for list state, so repeated or mixed stored list entries cannot overwrite earlier additions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce declaration representability after persistence and preserve unsupported verbatim source on regeneration</name>
  <files>core/model/profile.go, core/model/profile_test.go, core/activate/builder.go, core/activate/builder_test.go, core/ir/regen.go, core/ir/regen_test.go, core/store/dto_test.go</files>
  <read_first>core/model/profile.go (`Entry`, `EffectiveManaged`); core/store/dto.go and dto_test.go; core/ir/regen.go and regen_test.go; core/activate/builder.go; core/ir/route.go; the declaration tests added by 04-13.</read_first>
  <behavior>
    - Parsed `typeset -i COUNT=2`, readonly assignment, tied PATH/path declaration, and `local` assignment survive `MarshalProfile` then `UnmarshalProfile` with original `Text`, `CmdName`, and `OverrideManaged` intact.
    - Each persisted declaration regenerates from its original text, and `activate.Build` creates neither an environment scalar nor a PATH/FPATH list delta from it even though `EffectiveManaged()` remains true for the override contract.
    - A representable forced-managed plain assignment and export keep their existing regeneration and manifest behavior, proving the new guard is declaration-specific rather than a blanket override rejection.
  </behavior>
  <action>Add a shared model-level representability predicate that recognizes declaration command markers whose integer attributes, readonly state, tied-list behavior, or local scope are absent from the IR. Keep `EffectiveManaged` unchanged so persisted override semantics remain durable; require both effective management and representability at the builder boundary before producing any scalar or list intent. Use the same predicate in `ir.Regenerate` to select verbatim `Entry.Text` for these unsupported declarations instead of templating a lossy assignment. Add table-driven persistence tests that parse all four source forms, set `OverrideManaged` only after IR creation, perform the real DTO marshal/unmarshal round trip, assert the original text returns unchanged from regeneration, and assert the resulting manifest contains no scalar or list operation. Preserve ordinary forced-managed assignment/export support and the existing router guard. This fulfills the persisted-state boundary required by D-05, D-06, D-07, D-11, D-13, and D-17 without changing the manifest wire format or adding zsh syntax outside the emitter.</action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/model ./core/store ./core/ir ./core/activate -run 'Test(Entry.*Represent|.*Persisted.*OverrideManaged.*Declaration|Regenerate.*Declaration|Build.*OverrideManaged)' -v</automated>
  </verify>
  <acceptance_criteria>The persisted integer, readonly, tied, and local declarations remain inspectable/regenerable verbatim and cannot produce a scalar or list operation despite OverrideManaged.</acceptance_criteria>
  <done>Router bypass through stored override state cannot translate unmodeled declaration semantics into emitted activation data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove list composition and persisted declaration guards through the production store-to-live-zsh pipeline</name>
  <files>core/shell/zsh/pipeline_test.go, core/shell/zsh/residue_test.go</files>
  <read_first>core/shell/zsh/pipeline_test.go; core/shell/zsh/residue_test.go (`TestZeroResidueFullStateProperty`, `TestEffectiveIdentitySourcePipeline`); core/store/dto.go; core/activate/diff.go; core/shell/zsh/emit.go; 04-VERIFICATION.md.</read_first>
  <behavior>
    - For PATH and FPATH independently, repeated legacy entries plus mixed legacy-then-semantic and semantic-then-legacy cases execute Parse -> IR -> DTO store round trip -> Build -> Diff -> Emit -> zsh -f, preserve every expected addition in source order, and return the exact pre-apply list after deactivate.
    - The test checks generated apply and deactivate source with `zsh -n` before sourcing it, uses a fresh captured base for each case, and demonstrates both static and dynamic list provenance at runtime.
    - The four persisted OverrideManaged declaration forms traverse the same profile persistence and builder boundary, produce no activation operations, and leave the live PATH/FPATH and scalar baseline unchanged when emitted code is sourced.
    - The existing randomized full-state residue property and renderer-mutant controls remain in the default suite and continue to pass unchanged.
  </behavior>
  <action>Add table-driven source-to-live regressions that build the semantic portions from real `Provider.Parse` and `ir.Build`, combine them with intentional historic `ValueModeLegacy` entries in their actual profile source positions, and round-trip the complete profile through `store.MarshalProfile` and `store.UnmarshalProfile` before Build. Cover repeated legacy PATH, repeated legacy FPATH, and legacy/semantic ordering in both directions for each canonical list. For each case, calculate the expected zsh list from the direct ordered source contract, assert a single manifest delta, run Diff and Provider.Emit, syntax-check both emitted blocks, then source them under `zsh -f` and verify apply plus exact deactivate restoration. Add persisted forced-managed declaration cases to the production chain and assert no scalar/list activation operation emerges while the original source text is still present in the stored/regenerated profile. Reuse the existing snapshot helpers in `residue_test.go` to pin exact before/after state for at least one mixed legacy/semantic PATH and one FPATH case; retain the full-state property, mutation controls, drift guard, and zsh skip guards per D-09, D-10, D-12, D-14, D-15, D-16, and D-17.</action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test -count=1 ./core/shell/zsh -run 'Test(Pipeline.*(Legacy|PersistedOverrideManaged)|Residue.*(Legacy|Semantic)|ZeroResidueFullStateProperty|ResidueRendererMutants)$' -v</automated>
  </verify>
  <acceptance_criteria>Real emitted zsh applies every valid composed list item once, reverses all cases to their exact captured state, and has no emitted operation for persisted forced declaration forms.</acceptance_criteria>
  <done>SW-01 and SW-02 have executable Parse-to-Store-to-Emit evidence for both final fidelity gaps, without relaxing existing zero-residue or injection-safety gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed/stored profile -> builder | User-authored and historical profile fields become declarative manifest intent after DTO persistence. |
| manifest -> diff -> emitter -> sourced zsh | Ordered operations cross from shell-agnostic Go values into code that mutates the live shell. |
| apply/deactivate lifecycle -> live shell baseline | Captured list base and prior scalar state must be restored without clobbering unrelated state. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14-01 | Tampering | `core/activate/builder.go` list reducer | high | mitigate | Route semantic and explicit legacy list entries through one ordered token stream, emit one canonical delta, and execute repeated/mixed PATH and FPATH regressions through real zsh. |
| T-04-14-02 | Tampering | persisted `Entry.Override` crossing into Build | high | mitigate | Require builder-side declaration representability independently of router outcome; round-trip forced integer, readonly, tied, and local forms through DTO before Build. |
| T-04-14-03 | Elevation of privilege | `ir.Regenerate` and emitted activation data | high | mitigate | Preserve unsupported declaration source verbatim instead of templating it, and prove no scalar/list operation reaches the emitter after persistence. |
| T-04-14-04 | Integrity | `core/shell/zsh/emit.go` apply/deactivate output | high | mitigate | Syntax-check emitted blocks, source them in `zsh -f`, assert exact snapshot restoration, and keep renderer-mutant plus full-state property controls active. |
| T-04-14-05 | Repudiation | local regression evidence | low | accept | Deterministic Go test names, persisted profile fixtures, and source-to-live traces are sufficient evidence for this single-user local tool; no remote audit service is in scope. |
</threat_model>

## Source coverage audit

| Source | ID | Item | Plan | Status | Notes |
|--------|----|------|------|--------|-------|
| GOAL | — | Faithfully build, diff, emit, and reverse a resolved profile with no residue | 04-14 | COVERED | All three tasks close the only verification blockers at the builder boundary and live-zsh boundary. |
| REQ | SW-01 | Apply declarative state only through the one emitted zsh path | 04-14 | COVERED | Tasks 1-3 admit only represented list/declaration state and prove the production emitter path. |
| REQ | SW-02 | Reverse managed state with zero residue and untouched base state | 04-14 | COVERED | Task 3 asserts exact apply/deactivate restoration for PATH and FPATH composition cases. |
| RESEARCH | — | Semantic source order, static/dynamic provenance, and legacy-only fallback are authoritative | 04-14 | COVERED | Task 1 uses one source-ordered token reducer; Task 3 tests mixed provenance live. |
| RESEARCH | — | Store is lossless and shell-agnostic; unsupported source must remain verbatim | 04-14 | COVERED | Task 2 round-trips DTO then preserves `Entry.Text`; Task 3 uses the persisted production path. |
| CONTEXT | D-01, D-02, D-03, D-04 | Stable Manifest schema and wire layout | 04-14 | COVERED | Tasks preserve the existing manifest structures while changing only how valid parts are composed. |
| CONTEXT | D-05, D-06, D-07 | Shell-agnostic builder and persisted override behavior | 04-14 | COVERED | Task 2 adds the independent representability boundary without changing override durability. |
| CONTEXT | D-08, D-09, D-10, D-11 | Agnostic diff plan and emitter-only zsh syntax | 04-14 | COVERED | Tasks 1-2 remain model/activate/IR work; Task 3 exercises the existing emitter seam. |
| CONTEXT | D-12, D-13, D-14, D-15 | Runtime capture, safe rendering, and exact restoration contracts | 04-14 | COVERED | Task 3 retains current source/snapshot and body/ownership controls while proving additions restore. |
| CONTEXT | D-16, D-17 | Default-suite real-zsh zero-residue regression pin | 04-14 | COVERED | Task 3 extends rather than replaces the property and mutant coverage. |
| VERIFICATION | G-01 | Repeated and mixed legacy list additions bypass composition and lose earlier source order | 04-14 | COVERED | Tasks 1 and 3 compose and run every required ordering. |
| VERIFICATION | G-02 | Persisted OverrideManaged declaration bypasses router-only fidelity guard | 04-14 | COVERED | Tasks 2 and 3 guard after persistence and prove no operation reaches emitted zsh. |

## Artifacts this plan produces

- A single canonical internal composition path for semantic and explicitly legacy PATH/FPATH contributions.
- A representability predicate consumed by both builder and regeneration while preserving `OverrideManaged` as persisted metadata.
- DTO-round-tripped, source-to-live-zsh evidence for all repeated/mixed list orders and forced declaration forms.

<verification>
Run the three task-level uncached commands, then run `GOTOOLCHAIN=auto go test -count=1 ./...`, `GOTOOLCHAIN=auto go build ./...`, `GOTOOLCHAIN=auto go vet ./...`, and `make check`. Confirm `TestZeroResidueFullStateProperty`, `TestResidueRendererMutants`, `TestPipelineComposedPathAndFPathDynamicExpansion`, and the new persisted pipeline cases execute without removal, skipping only through their existing `zsh` availability guard.
</verification>

<success_criteria>
- Every valid legacy, semantic, or mixed PATH/FPATH profile produces exactly one canonical delta with all additions in source order and aligned dynamic provenance.
- Repeated legacy and legacy/semantic combinations in both orders apply through emitted zsh and reverse to an exact pre-apply PATH/FPATH state.
- Integer, readonly, tied, and local declaration forms survive DTO/profile persistence with `OverrideManaged`, regenerate verbatim, and create no scalar/list manifest or emitted activation operation.
- The complete Go suite, build, vet, `make check`, real-zsh pipeline cases, residue property, and mutation controls pass.
</success_criteria>

<output>
Create `.planning/phases/04-manifest-builder-emit/04-14-SUMMARY.md` when done.
</output>
