---
phase: 04-manifest-builder-emit
plan: 12
type: execute
wave: 9
depends_on: ["04-09", "04-10", "04-11"]
gap_closure: true
files_modified:
  - core/shell/zsh/residue_test.go
  - core/shell/zsh/invariant_test.go
autonomous: true
requirements: [SW-01, SW-02]
user_setup: []

must_haves:
  truths:
    - "The real Provider Parse -> IR -> activation Build -> Diff -> Emit path restores an exact full-shell snapshot after deterministic balanced activate/deactivate sequences."
    - "The final oracle covers repeated scalar and function identities, plain/exported scalar transitions over unset/unexported/exported/present-empty baselines, legal foo-bar/foo.bar/foo_bar name collisions, real values equal to every historical emitter sentinel, repeated PATH head/tail additions, and repeated FPATH additions."
    - "The full-shell snapshot remains collision-safe and byte-sensitive for scalar/array/association values, alias/function bodies, options, PATH, FPATH, presence, export attributes, newlines, backslashes, and embedded NUL encodings."
    - "Every independently reproduced Phase 04 break case has either a direct adversarial regression or a named final gate command, and deliberately broken render/state-bookkeeping variants are rejected by the oracle."
    - "PATH/FPATH restore exact unset versus present-empty versus non-empty base state, including initially unset FPATH, and dynamic additions match direct zsh when expansion yields multiple colon-separated elements or the empty string."
    - "Reverse zsh syntax appears only in core/shell/zsh/emit.go; shell-agnostic core/activate, core/model, core/store, and core/shell remain free of reverse-zsh command construction."
  assumptions:
    - statement: "The primary noun in the restoration contract is the final effective shell identity, not every historical statement that assigned that identity."
      status: resolved
      decision: promote
      rationale: "Per-statement history cannot be independently restored once multiple source statements resolve to one live scalar, alias, or function identity; the final effective identity is the observable shell state."
    - statement: "The SW-02 edge probe remained unclassified by the spec-less fallback; this plan does not silently convert it into a bespoke prohibition and instead records the complete accepted-domain oracle as explicit evidence."
      status: unresolved
      flagged: true
  artifacts:
    - path: "core/shell/zsh/residue_test.go"
      provides: "source-derived real-emitter zero-residue oracle and complete adversarial fixture matrix"
      contains: "TestZeroResidue"
    - path: "core/shell/zsh/invariant_test.go"
      provides: "repository-wide reverse-zsh ownership guard"
      contains: "TestReverseSyntaxHasSingleEmitHome"
  key_links:
    - from: "zsh Provider.Parse"
      to: "activate.Build"
      via: "ir.Build preserves source order and Plan 04-06 effective-identity reducers select the final observable owner"
      pattern: "ir.Build"
    - from: "activate.Diff"
      to: "zsh Provider.Emit"
      via: "only real production plans and renderer are used by the final property run"
      pattern: "Provider{}.Emit"
    - from: "balanced action trace"
      to: "full-shell byte snapshot"
      via: "every profile is applied and deactivated equally and the final snapshot must equal the stable baseline exactly"
      pattern: "snapshotDifference"
---

<objective>
Turn the Phase 04 gap reproductions into one final, production-path restoration oracle and one code-ownership invariant.

Purpose: a narrow green test must not hide residue in repeated identities, legal name collisions, sentinel values, or composed PATH/FPATH state.
Output: expanded source-derived property fixtures, sensitivity/mutant proofs, and a repository-wide reverse-zsh syntax guard.
</objective>

<execution_context>
@/home/metanmai/.codex/gsd-core/workflows/execute-plan.md
@/home/metanmai/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-manifest-builder-emit/04-VERIFICATION.md
@.planning/phases/04-manifest-builder-emit/04-REVIEW.md
@.planning/phases/04-manifest-builder-emit/04-05-SUMMARY.md
@.planning/phases/04-manifest-builder-emit/04-06-PLAN.md
@.planning/phases/04-manifest-builder-emit/04-09-PLAN.md
@.planning/phases/04-manifest-builder-emit/04-10-PLAN.md
@.planning/phases/04-manifest-builder-emit/04-11-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden the production-path zero-residue oracle across every reproduced identity and list break case</name>
  <files>core/shell/zsh/residue_test.go</files>
  <read_first>
    - core/shell/zsh/residue_test.go (current snapshot framing, narrow hand-built manifests, balanced trace, meta-sensitivity, and renderer mutants)
    - core/shell/zsh/parse.go (source spelling and semantic RuntimeValue contract)
    - core/ir/build.go (ordered Profile construction)
    - core/activate/builder.go (effective identity and composed ListDelta production after Plans 04-06/04-09)
    - core/activate/diff.go (apply/deactivate plan construction)
    - core/shell/zsh/emit.go (real reverse emitter, slot codec, ownership state, PATH/FPATH rendering)
    - core/model/manifest.go (final Scalar, FuncSet, and ListDelta shapes)
    - .planning/phases/04-manifest-builder-emit/04-VERIFICATION.md (all six gap groups and concrete break cases)
  </read_first>
  <behavior>
    - RED: a source fixture containing repeated assignments/functions for the same name restores the pre-profile final effective identity and does not attempt per-statement restoration.
    - RED: scalar fixtures cover plain/plain and plain/export/plain transitions over unset, unexported empty/non-empty, and exported empty/non-empty baselines, with exact export-attribute restoration.
    - RED: simultaneously managed names foo-bar, foo.bar, and foo_bar use independent state slots and all restore exactly.
    - RED: baseline scalar, alias, and function values equal to the former __ZP_UNSET__ marker restore as present values rather than absence.
    - RED: repeated PATH additions at both base sides, including duplicates already in baseline and multiple source statements, restore exact order and multiplicity.
    - RED: repeated FPATH additions, including quoted colon-bearing elements and dynamic segments accepted by Plans 04-08/04-09, restore exact order and multiplicity.
    - RED: initially unset, present-empty, and non-empty PATH/FPATH bases are distinguished, with initially unset FPATH mandatory.
    - RED: allowlisted `$EXTRA` additions match direct zsh for multi-element (`/one:/two`) and empty expansions in both PATH and FPATH.
    - RED: balanced cross-profile sequences cover every profile at least once, run in one disposable zsh -f process, and compare the final byte snapshot with the self-stable baseline.
  </behavior>
  <action>
    Replace the narrow hand-authored manifest-only fixtures with source-derived fixtures for cases the parser accepts: Provider.Parse -> ir.Build -> activate.Build, followed by activate.Diff and the real Provider.Emit. Keep a minimal direct Manifest fixture only where the source contract cannot express a required state, label that exception in the helper, and never duplicate production reduction or list-splitting logic inside the test.

    Expand the baseline and two or more profiles to include: repeated scalar declarations; plain/plain and plain/export/plain scalar transitions; unset, unexported, exported, present-empty, and non-empty scalar priors; repeated definitions of one function; legal punctuated identities `foo-bar`, `foo.bar`, and `foo_bar`; present scalar/alias/function bodies exactly equal to historical marker text including `__ZP_UNSET__`; absent identities; aliases and functions with multiline/backslash/sentinel-like bodies; PATH head and tail compositions with the same element repeated in source and baseline; and independent repeated FPATH composition. Include initially unset, present-empty, and non-empty PATH/FPATH bases, with initially unset FPATH mandatory. Exercise validated `$EXTRA` source with multi-element and empty runtime values for each list and compare the applied state with direct source in the same disposable environment. Record scalar export attributes and PATH/FPATH presence plus qqqq-encoded array elements/multiplicity in the collision-safe snapshot rather than relying on joined scalar text.

    Preserve deterministic seed and balanced action generation, but parameterize it by the full fixture count and require that each profile has both an apply and deactivate action. On failure, print seed, action trace, fixture label, and first differing encoded snapshot field without printing secrets. Assert the final-effective-identity assumption directly: after a source profile with repeated declarations is applied, the last effective declaration is live; after deactivation, the single pre-profile identity is restored exactly.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'ZeroResidue|EffectiveIdentity|SlotCollision|Sentinel|Export|Path.*Multiplicity|FPATH|BasePresence|DynamicCardinality' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - The main property uses production Parse/IR/Build/Diff/Emit for accepted source syntax.
    - Repeated identities are reduced by final observable name and explicitly assert the promoted final-effective-identity decision.
    - Legal punctuation collisions and sentinel-valued present state restore independently.
    - PATH and FPATH order, multiplicity, literal quoting, and dynamic provenance are snapshot-visible.
    - Scalar export attributes and PATH/FPATH unset/present-empty/non-empty state are snapshot-visible and restored exactly.
    - Multi-element and empty dynamic list expansions equal direct zsh behavior for PATH and FPATH.
    - The final state equals the stable baseline byte for byte after every balanced trace.
  </acceptance_criteria>
  <done>The real production path proves zero residue over every known Phase 04 identity and list failure shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove oracle sensitivity, reject state-bookkeeping mutants, and enforce reverse-zsh ownership</name>
  <files>core/shell/zsh/residue_test.go, core/shell/zsh/invariant_test.go</files>
  <read_first>
    - core/shell/zsh/residue_test.go (existing meta-sensitivity checks, mutable render seams, t.Cleanup use, and final production rerun)
    - core/shell/zsh/invariant_test.go (current incomplete directory scan and incorrect regen.go exception)
    - core/shell/zsh/emit.go (the only permitted reverse-zsh syntax home and available test seams)
    - core/activate/builder.go (must remain shell-agnostic)
    - core/activate/diff.go (must remain shell-agnostic)
    - core/model/manifest.go (must contain data only)
    - core/store/store.go (must use Regenerator data seam, not reverse-zsh codegen)
    - core/shell/provider.go (shell-agnostic emitter interface)
  </read_first>
  <behavior>
    - RED: the snapshot detects changes in presence, export attribute, scalar/array/association value, alias body, function body, option, PATH/FPATH base presence, and PATH/FPATH order/multiplicity.
    - RED: blind append, base stripping, dropped static quoting, stale-applied restoration, punctuation-slot collision, and sentinel-as-absence mutants each produce an oracle difference or explicit wrong-state assertion.
    - RED: reverse command tokens or helper construction introduced in core/activate, core/model, core/store, core/shell, or any zsh file other than emit.go fail the invariant.
    - GREEN: every mutable renderer seam is restored with t.Cleanup, mutant subtests are non-parallel, and a final real-emitter property run passes after all cleanups.
  </behavior>
  <action>
    Extend oracle meta-sensitivity for FPATH plus scalar presence/export-attribute and list base-presence changes, retaining collision-safe NUL/newline/backslash coverage. Broaden the mutant suite from rendering-only mistakes to the exact reproduced state-bookkeeping failures: make emitted test text restore the first rather than final scalar value, force a plain scalar to export, drop prior export restoration, alias two legal names to one synthetic slot, treat a present marker-valued baseline as absent, collapse an unset FPATH base into present-empty, and force a dynamic multi-element addition into one array element. Each mutant must reach a unique marker proving it failed for the intended reason. Prefer mutating emitted loader text or existing package-local render seams; do not add a production package-global seam solely for this test.

    Retain t.Cleanup around every existing mutable renderer seam, prohibit t.Parallel in those subtests, and end the parent test with a fresh real Provider.Emit sequence that passes after cleanup. The mutant tests are required sensitivity evidence, not replacements for production-path assertions.

    Rewrite TestReverseSyntaxHasSingleEmitHome to recursively scan non-test `.go` files under core/activate, core/model, core/store, core/shell, and core/shell/zsh. Exclude exactly core/shell/zsh/emit.go. Detect reverse commands (`unalias`, function removal, option reversal) and emitter-only helper/protocol fragments precise enough to catch copied codegen without flagging comments, interface names, forward regeneration, or tests. Failure must print the token and relative file. The guard must affirm that activate/model/store/shell traffic is structured data and reverse zsh is owned only by emit.go.
  </action>
  <verify>
    <automated>GOTOOLCHAIN=auto go test ./core/shell/zsh -run 'Snapshot|ResidueRendererMutants|ReverseSyntaxHasSingleEmitHome|ZeroResidue' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - Every snapshot dimension used by the property has a sensitivity proof.
    - Export-attribute, list base-presence, and dynamic-cardinality mutants are rejected for their intended dimensions.
    - Every known renderer or state-bookkeeping mutant is rejected for its intended reason.
    - No mutable seam leaks beyond its subtest and the real emitter passes afterward.
    - The invariant scans all relevant non-test files recursively and permits reverse-zsh construction only in emit.go.
  </acceptance_criteria>
  <done>The final oracle is shown to be sensitive, mutation-resistant, cleanup-safe, and backed by an architectural ownership guard.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run the complete gap-closure gate over every independently reproduced break case</name>
  <files>core/shell/zsh/residue_test.go, core/shell/zsh/invariant_test.go</files>
  <read_first>
    - .planning/phases/04-manifest-builder-emit/04-06-PLAN.md (identity/slot regressions and final-effective-identity invariant)
    - .planning/phases/04-manifest-builder-emit/04-07-PLAN.md (escape and semantic secret regressions)
    - .planning/phases/04-manifest-builder-emit/04-08-PLAN.md (list AST/IR/store regressions)
    - .planning/phases/04-manifest-builder-emit/04-09-PLAN.md (list builder/emitter regressions)
    - .planning/phases/04-manifest-builder-emit/04-10-PLAN.md (source failure and protocol collision regressions)
    - .planning/phases/04-manifest-builder-emit/04-11-PLAN.md (secret transaction break cases)
    - core/shell/zsh/residue_test.go (final oracle and mutant test names)
    - core/store/keychain_test.go (exact OS retrieval framing names)
    - core/store/store_test.go (transaction pre-effect/post-effect/concurrent failure names)
  </read_first>
  <action>
    Define an explicit label/package/regex matrix for: effective identity plus scalar export/slot encoding; live escape semantics; list AST/IR; list DTO persistence; PATH/FPATH scalar-context composition plus base presence; introspection source/body framing; secret retrieval and transaction rollback/CAS ambiguity; and final zero-residue plus reverse-syntax ownership. For every row, first execute `go test <package> -list <regex>`, filter actual `Test...` names, and assert the resulting set is non-empty. Only after discovery succeeds may the same package/regex run with `-count=1`. A compile/list error must propagate and an empty match must print the row label and fail; do not use a fallback value or a successful zero-test `go test -run` as evidence. After every targeted row executes, run the entire repository suite uncached.

    Confirm no test relies on the user's interactive shell startup files: all live shell checks must invoke disposable `zsh -f` processes. Confirm all source files regenerated by storage/round-trip assertions are byte-for-byte equal to their original supported source fixtures, while activation uses semantic runtime values. The store row must include exact empty/one-or-more-trailing-newline retrieval and Store mutate-then-error/Delete-not-found/update-ref pre-effect/post-effect/concurrent cases. Do not weaken fail-closed ValueMode behavior, secret redaction, or shell-agnostic core boundaries to make the gate pass.
  </action>
  <verify>
    <automated>bash -ceu 'set -o pipefail; check() { label=$1; pkg=$2; re=$3; names=$(GOTOOLCHAIN=auto go test "$pkg" -list "$re" | awk "/^Test/{print}"); [ -n "$names" ] || { printf "no tests discovered: %s (%s %s)\n" "$label" "$pkg" "$re" &gt;&amp;2; exit 1; }; GOTOOLCHAIN=auto go test "$pkg" -run "$re" -count=1; }; check identity-export ./core/activate "Build.*(Repeated|Identity|Export)|Diff.*(Identity|Duplicate|Export)"; check escape-semantics ./core/shell/zsh "Escape|RuntimeValue|Unsupported"; check list-ir ./core/ir "ListValue|ListSegment|DeepCopy"; check list-dto ./core/store "DTO.*List|ListValue|ListSegment"; check list-live ./core/shell/zsh "Pipeline.*(Path|FPATH|Repeated|Dynamic)|Emit.*List|ListDelta"; check introspection ./core/shell/zsh "Introspect.*(Missing|Failure|Syntax|Body|Sentinel|Framing|Marker|Empty)"; check secret-transaction ./core/store "PrepareSecrets|Keychain.*(Missing|Empty|NotFound|Newline)|Commit.*(Secret|Rollback|UpdateRef|Atomic|Unavailable|Concurrent)"; check final-oracle ./core/shell/zsh "ZeroResidue|EffectiveIdentity|SlotCollision|Sentinel|Snapshot|ResidueRendererMutants|ReverseSyntaxHasSingleEmitHome"; GOTOOLCHAIN=auto go test ./... -count=1'</automated>
  </verify>
  <acceptance_criteria>
    - Every Plan 04-06 through 04-12 regression suite executes at least one test.
    - Each targeted regex is proven non-empty by go test -list before it is executed; compile errors and empty discovery fail the gate.
    - All live shell tests use zsh -f and do not read or modify ~/.zshrc or bash startup state.
    - Source regeneration remains byte-for-byte and activation remains semantic-value based.
    - The full repository test suite passes uncached with no high-severity threat left unmitigated.
  </acceptance_criteria>
  <done>All independently reproduced break cases and the final integrated oracle pass together.</done>
</task>

</tasks>

<verification>
GOTOOLCHAIN=auto go test ./core/activate ./core/ir ./core/store ./core/shell/zsh -count=1
GOTOOLCHAIN=auto go test ./... -count=1
</verification>

<success_criteria>
- The production Parse -> IR -> Build -> Diff -> Emit path restores exact full-shell state.
- Repeated identities, legal slot-name collisions, sentinel values, PATH, and FPATH are all in the final oracle.
- Scalar export attributes, PATH/FPATH base presence, and zero/one/many dynamic list cardinality are direct final-oracle dimensions.
- The promoted final-effective-identity assumption has a direct invariant test.
- Every independently reproduced gap has a direct regression and runs in the final gate.
- Reverse zsh syntax is confined to core/shell/zsh/emit.go.
</success_criteria>

<prohibition_recall>
Probe source: spec-less fallback edge and prohibition probes. SW-01 adjacency, empty, and ordering are active: repeated adjacent statements, present-empty/sentinel values, and exact PATH/FPATH order/multiplicity are explicit oracle dimensions. SW-02 remains unclassified and is not silently treated as a prohibition. No bespoke prohibition survived recall; architectural ownership and fail-closed behaviors are verified as invariants rather than invented policy.
</prohibition_recall>

<threat_model asvs_level="1" block_severity="high">
  <threat stride="T" severity="high" mitigation="The oracle snapshots exact live identity data and list multiplicity after deterministic cross-profile sequences using only the real production pipeline." verification="zero-residue byte comparison plus one sensitivity mutation per snapshot dimension" />
  <threat stride="T" severity="high" mitigation="Named regression gates prove test discovery before execution, preventing zero-match false green results." verification="go test -list non-empty assertion for every package/regex row before uncached targeted and full runs" />
  <threat stride="E" severity="high" mitigation="Legal punctuation cannot alias internal slots and marker-valued present state is distinct from absence." verification="foo-bar/foo.bar/foo_bar and __ZP_UNSET__ baseline cases plus explicit collision/sentinel mutants" />
  <threat stride="I" severity="high" mitigation="Final gates preserve source/runtime separation and secret canaries remain confined to backend-only assertions with redacted diagnostics." verification="store transaction suite, byte-for-byte source round-trip tests, and error/tree canary scans" />
  <threat stride="D" severity="medium" mitigation="All randomized traces are fixed-seed, bounded, single-process zsh -f runs with actionable first-difference output." verification="balanced action guard and stable no-op snapshot control" />
</threat_model>

<artifacts_this_phase_produces>
- A source-derived, real-emitter, full-shell zero-residue property suite.
- Explicit repeated-identity, slot-collision, sentinel, PATH, and FPATH fixtures.
- Export-attribute, list-base-presence, and multi-element/empty dynamic expansion fixtures.
- Sensitivity and mutant evidence for every oracle dimension and known failure mode.
- A recursive reverse-zsh code ownership invariant.
- A discovery-first executable gate matrix that cannot pass on zero regex matches.
</artifacts_this_phase_produces>

<output>
After completion, create `.planning/phases/04-manifest-builder-emit/04-12-SUMMARY.md`
</output>
