---
milestone: v1.0
audited: 2026-06-24
status: passed
scores:
  requirements: 4/4
  phases: 1/1
  integration: n/a (single-phase milestone)
  flows: pass
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 01-trustworthy-line-numbers
    items:
      - "WR-01 (advisory, latent): core/testgen/oracle.go Expected() silently emits Lines=0 if RenderZsh() was not called first — no guard. All current callers honor the documented order."
      - "WR-02 (advisory, latent): core/testgen/generator.go pick() has no bounds check on cp[:n]; a defect count exceeding the pool would panic. Current GenParams all fit."
nyquist: skipped (workflow.nyquist_validation=false)
---

# Milestone v1.0 Audit — trustworthy-line-numbers

**Verdict: PASSED.** All requirements satisfied, end-to-end flows verified through the real compiled binary, both fixes pinned and mutation-proven. No critical gaps; two latent advisory items recorded as minor tech debt.

## Scope

Single-phase milestone — Phase 1 (`trustworthy-line-numbers`) only. There are no cross-phase boundaries, so cross-phase integration is **N/A**; the relevant integration is the intra-pipeline data flow (`main → cli.Run → analyze.Analyze/countLines → reconciler/Block.StartLine → JSONRenderer → wire`), verified end-to-end.

## Requirements coverage (3-source cross-reference)

| REQ | REQUIREMENTS.md | Phase VERIFICATION.md | SUMMARY frontmatter | Final |
|-----|-----------------|------------------------|---------------------|-------|
| LINE-01 | `[x]` Complete | passed | 01-01 `requirements_completed: [LINE-01]` | **satisfied** |
| LINE-02 | `[x]` Complete | passed | 01-02 `requirements_completed: [LINE-02]` | **satisfied** |
| PIN-01 | `[x]` Complete | passed | 01-03 `requirements_completed: [PIN-01, PIN-02]` | **satisfied** |
| PIN-02 | `[x]` Complete | passed | 01-03 `requirements_completed: [PIN-01, PIN-02]` | **satisfied** |

4/4 satisfied. No orphaned requirements (every traceability entry is covered by a VERIFICATION + SUMMARY).

## End-to-end evidence (real binary, independently re-run)

- **LINE-01:** 0-byte file → `analysis.lines == 0`, exit 0; commented-dup fixture → `lines: 3` (trailing newline not over-counted).
- **LINE-02:** `# comment` / `alias gs=` / `alias gs=` → `duplicate_alias.lines == [2, 3]` (statement lines, not the comment line 1).
- **PIN-01:** `checkLines = true`; `TestOracleProperty` passes all 10 seeds; mutation-proven (unconditional `n++` and re-added parse overwrite both FAIL the suite).
- **PIN-02:** `manifests.json empty.zsh: lines 0`; `TestCorpusGolden/empty.zsh` passes; mutation-proven (`return 1` empty-branch FAILS).
- **Exit-code contract:** clean → 0, actionable → 3, missing file → 1, unknown command/flag/no-args → 2.
- **Agent contract:** success and failure each emit exactly one JSON object on stdout (stderr clean); human-mode failure goes to stderr.
- **Build/suite:** `go build ./...` and `go test ./... -count=1` green.

## Broken flows

None.

## Tech debt (non-blocking)

| ID | Location | Note |
|----|----------|------|
| WR-01 | core/testgen/oracle.go | `Expected()` emits `Lines=0` if `RenderZsh()` not called first — no guard (latent) |
| WR-02 | core/testgen/generator.go | `pick()` no bounds check on `cp[:n]` (latent) |

Both are test-infrastructure footguns from the Phase 1 code review (`01-REVIEW.md`); neither affects shipped behavior or any requirement.

## Note: PIN-01 / PIN-02 are complementary (not redundant)

The oracle property test (PIN-01) never exercises the 0-byte case — its generated configs always end in a newline — so the `empty → 0` guarantee is pinned **solely** by PIN-02's `empty.zsh`. Confirmed empirically: the `return 1` empty-branch mutation FAILS PIN-02 but leaves PIN-01 green. Both pins are required.
