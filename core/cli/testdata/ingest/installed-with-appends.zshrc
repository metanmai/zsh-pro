export PHASE6_APPEND_BEFORE='before'
# >>> zsh-pro >>>
# stale loader bytes replaced by ingest
true
# <<< zsh-pro <<<
# Ordinary post-END content must stay exactly here.
alias phase6_append_alias='print -r -- append-ok'
export PHASE6_APPEND_AFTER='after'
